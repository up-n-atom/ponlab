From fe73f52048829da5fe999225c5ad666d42c14822 Mon Sep 17 00:00:00 2001
From: thampan <joby.thampan@intel.com>
Date: Wed, 2 Dec 2020 11:12:25 +0800
Subject: [PATCH] DRV_DPM_SW-409: Cqm Lookup Mode set/get through DPM

                Warning and Klockwork Fix
---
 drivers/net/datapath/dpm/datapath_api.c            | 38 ++++++++++++++++++++++
 drivers/net/datapath/dpm/datapath_br_vlan.c        | 17 +++++-----
 drivers/net/datapath/dpm/datapath_misc.c           |  1 -
 drivers/net/datapath/dpm/datapath_proc.c           | 10 ++----
 drivers/net/datapath/dpm/datapath_proc_qos.c       |  2 +-
 drivers/net/datapath/dpm/gswip31/datapath_misc.c   |  8 +++--
 .../net/datapath/dpm/gswip31/datapath_ppv4_api.c   |  4 +--
 .../net/datapath/dpm/gswip31/datapath_switchdev.c  |  3 +-
 include/net/datapath_api.h                         | 15 +++++++++
 9 files changed, 74 insertions(+), 24 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index 48a3043af7f5..48b5367e600f 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -2692,6 +2692,44 @@ int dp_set_bp_attr(struct dp_bp_attr *conf, u32 flag)
 }
 EXPORT_SYMBOL(dp_set_bp_attr);
 
+int dp_lookup_mode_cfg(int inst, u32 lu_mode, struct dp_q_map *map,
+		       u32 flag)
+{
+	cbm_queue_map_entry_t entry = {0};
+	int ret = DP_SUCCESS;
+	struct pmac_port_info *port_info;
+
+	if (unlikely(!dp_init_ok)) {
+		pr_err("%s failed: datapath not initialized yet\n", __func__);
+		return DP_FAILURE;
+	}
+
+	if (is_invalid_port(map->dp_port))
+		return DP_FAILURE;
+
+	port_info = get_dp_port_info(inst, map->dp_port);
+	if (!port_info)
+		return DP_FAILURE;
+
+	DP_LIB_LOCK(&dp_lock);
+	entry.ep = map->dp_port;
+	entry.mpe1 = map->mpe1;
+	entry.mpe2 = map->mpe2;
+	if (flag & DP_CQM_LU_MODE_GET) {
+		ret = cqm_mode_table_get(inst, &lu_mode, &entry, 0);
+	} else {
+		ret = cqm_mode_table_set(inst, &entry, lu_mode,
+					 CBM_QUEUE_MAP_F_MPE1_DONTCARE |
+					 CBM_QUEUE_MAP_F_MPE2_DONTCARE);
+
+		port_info->cqe_lu_mode = lu_mode;
+	}
+	DP_LIB_UNLOCK(&dp_lock);
+
+	return ret;
+}
+EXPORT_SYMBOL(dp_lookup_mode_cfg);
+
 /*Return the table entry index based on dev:
  *success: >=0
  *fail: DP_FAILURE
diff --git a/drivers/net/datapath/dpm/datapath_br_vlan.c b/drivers/net/datapath/dpm/datapath_br_vlan.c
index 0860be5cd4d4..3e3ab1f19fbf 100644
--- a/drivers/net/datapath/dpm/datapath_br_vlan.c
+++ b/drivers/net/datapath/dpm/datapath_br_vlan.c
@@ -564,10 +564,9 @@ static int dp_add_rule(struct br_info *br_info, struct vlan_entry *ventry,
 	blk_info->subblk_size = VLAN_PCE_MAX_SUBBLK_SIZE;
 	blk_info->portid = subif->port_id;
 
-	if (!blk_info->subblk_name) {
-		strncpy(blk_info->subblk_name, "dp_br_vlan",
-			sizeof(blk_info->subblk_name) - 1);
-	}
+	strncpy(blk_info->subblk_name, "dp_br_vlan",
+		sizeof(blk_info->subblk_name) - 1);
+
 	pce->blk_info->region = GSW_PCE_RULE_CTP;
 
 	pce_rule->logicalportid = subif->port_id;
@@ -680,8 +679,8 @@ static struct vlan_entry *dp_add_vlan_entry(struct br_info *br_info,
 	bool alloc_fid = false;
 
 	DP_DEBUG(DP_DBG_FLAG_BR_VLAN,
-		 "%s: dev=%s vlanid=%d bport=%d flags=%08x\n", __func__,
-		 br_info->dev->name, vlanid, subif ? subif->bport : 0, flags);
+		 "%s: dev=%s vlanid=%d flags=%08x\n", __func__,
+		 br_info->dev->name, vlanid, flags);
 
 	if (flags & BRIDGE_FLAGS_MASTER)
 		curr_bp_ventry = get_vlist_entry_in_bp(br_info, subif->bport,
@@ -815,7 +814,7 @@ static int dp_del_vlan_pce_rule(struct vlan_entry *ventry,
 		/* Delete all the pce rules which is not monitored by anyone */
 		list_for_each_safe(q, t, &ventry->pce_list) {
 			p = list_entry(q, struct pce_entry, list);
-			if (!p || !p->blk_info)
+			if (!p->blk_info)
 				continue;
 			if (p->disable) {
 				if (dp_del_rule(ventry, p))
@@ -856,9 +855,9 @@ static int dp_del_vlan_entry(struct br_info *br_info,
 	ventry = vlist_entry->vlan_entry;
 
 	DP_DEBUG(DP_DBG_FLAG_BR_VLAN,
-		 "%s: %s entry in Br=%s Bp=%d vlanid=%d ref_cnt=%d\n",
+		 "%s: %s entry in Br=%s vlanid=%d ref_cnt=%d\n",
 		 __func__, (ventry->ref_cnt > 1) ? "Disabling" : "Deleting",
-		 br_info->dev->name, subif ? subif->bport : 0, ventry->vlan_id,
+		 br_info->dev->name, ventry->vlan_id,
 		 ventry->ref_cnt);
 
 	/* Disable pce rule index for this vid */
diff --git a/drivers/net/datapath/dpm/datapath_misc.c b/drivers/net/datapath/dpm/datapath_misc.c
index 107e2c307141..68f2c806ace0 100644
--- a/drivers/net/datapath/dpm/datapath_misc.c
+++ b/drivers/net/datapath/dpm/datapath_misc.c
@@ -1522,6 +1522,5 @@ int dp_getopt(char *cmd[], int cmd_size, int *cmd_offset,
 		}
 		return -1;
 	}
-	return -1;
 }
 EXPORT_SYMBOL(dp_getopt);
diff --git a/drivers/net/datapath/dpm/datapath_proc.c b/drivers/net/datapath/dpm/datapath_proc.c
index d82e4a495e2b..e2581bfdd9f2 100644
--- a/drivers/net/datapath/dpm/datapath_proc.c
+++ b/drivers/net/datapath/dpm/datapath_proc.c
@@ -125,8 +125,8 @@ static int proc_brvlan_dump(struct seq_file *s, int pos)
 			v = vlist_entry->vlan_entry;
 			if (vlan_entry) {
 				seq_printf(s, "\n");
-				seq_printf(s, "%-11s%-11s%-11s",
-					   "", "", "", "", "");
+				seq_printf(s, "%-11s%-11s",
+					   "", "");
 			}
 			seq_printf(s, "|%-10s", tmp->dev->name);
 			seq_printf(s, "|%-10d", tmp->bportid);
@@ -810,10 +810,6 @@ ssize_t proc_ctp_list_write(struct file *file, const char *buf, size_t count,
 
 	if (strcasecmp(param_list[0], "bp_ctp") == 0) {
 		bp_info = get_dp_bp_info(0, bp);
-		if (!bp_info) {
-			pr_info("bp:%d info not found\n", bp);
-			return count;
-		}
 		pr_info("%-10s%-10d\n", "Bp", bp);
 		if (!bp_info->ref_cnt) {
 			pr_info("dev(%s) is not a pmapper\n",
@@ -821,7 +817,7 @@ ssize_t proc_ctp_list_write(struct file *file, const char *buf, size_t count,
 			return count;
 		}
 		list_for_each_entry(tmp, &bp_info->ctp_dev, list) {
-			if ((!tmp) || (!tmp->dev))
+			if (!tmp->dev)
 				continue;
 			pr_info("%-s:%-s(%-s:%-d)\n", "ctp_dev",
 				tmp->dev->name, "ctp", tmp->ctp);
diff --git a/drivers/net/datapath/dpm/datapath_proc_qos.c b/drivers/net/datapath/dpm/datapath_proc_qos.c
index e2ba9becf464..26c5f1077bc7 100644
--- a/drivers/net/datapath/dpm/datapath_proc_qos.c
+++ b/drivers/net/datapath/dpm/datapath_proc_qos.c
@@ -819,7 +819,7 @@ static int deq_port_res_get(int inst, int dp_port, int deq_idx)
 	dp_res.cqm_deq_idx = deq_idx;
 	if (dp_deq_port_res_get(&dp_res, 0)) {
 		pr_err("dp_deq_port_res_get fail: inst=%d dp_port=%d idx:%d\n",
-				inst, dp_port, i);
+		       inst, dp_port, deq_idx);
 		kfree(queue_res);
 		return -1;
 	}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 0c87df7643f6..e3dbdd108eb5 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -2089,10 +2089,12 @@ static int subif_hw_reset(int inst, int portid, int subif_ix,
 				     dma_ch_offset)->ref_cnt);
 		if (i == 0) { /* Cannot reset BP for all qid */
 			if (sif->ctp_dev) { /* pmapper */
-				ctp_dev = ctp_dev_list_lookup(&bp_info->ctp_dev, sif->ctp_dev);
+				ctp_dev = ctp_dev_list_lookup(&bp_info->ctp_dev,
+							      sif->ctp_dev);
 				if (!ctp_dev) {
-					DP_DEBUG(DP_DBG_FLAG_DBG, "Device not found: %s\n",
-						 ctp_dev->dev->name);
+					DP_DEBUG(DP_DBG_FLAG_DBG,
+						 "Device not found: %s\n",
+						 __func__);
 					return -1;
 				}
 				DP_DEBUG(DP_DBG_FLAG_DBG, "del ctp dev list\n");
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
index 4596f3424294..37d7d4d3e407 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
@@ -3550,7 +3550,7 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 	struct hal_priv *priv;
 	int node_id, res;
 	u32 bw_limit;
-	u32 quanta;
+	u32 quanta = 0;
 
 	if (!cfg) {
 		pr_err("cfg cannot be NULL\n");
@@ -3727,7 +3727,7 @@ int dp_shaper_conf_get_31(struct dp_shaper_conf *cfg, int flag)
 	struct hal_priv *priv;
 	int node_id, res;
 	u32 bw_limit, pir = 0;
-	u32 quanta;
+	u32 quanta = 0;
 
 	if (!cfg) {
 		pr_err("cfg cannot be NULL\n");
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
index a331ea7ff70d..67b16893e6cc 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
@@ -51,7 +51,8 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 	GSW_BRIDGE_portConfig_t brportcfg;
 	struct core_ops *gsw_handle;
 	struct brdgport_ops *gsw_bp;
-	u32 dport, priv_flag = 0;
+	int dport;
+	u32 priv_flag = 0;
 	struct pmac_port_info *p_info;
 
 	p_info = get_dp_port_info(br_item->inst, br_item->dp_port);
diff --git a/include/net/datapath_api.h b/include/net/datapath_api.h
index cda1870ab14c..c3c2cd78c8b7 100644
--- a/include/net/datapath_api.h
+++ b/include/net/datapath_api.h
@@ -28,6 +28,7 @@
 #if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30) /*GRX500 GSWIP30*/
 #include <net/datapath_api_umt.h>
 #endif
+#include <net/datapath_api_qos.h>
 #include <net/datapath_api_vlan.h>
 #include <net/switch_api/lantiq_gsw_api.h>
 #include <net/switch_api/lantiq_gsw_flow.h>
@@ -2252,6 +2253,20 @@ struct dp_bp_attr {
  */
 int dp_set_bp_attr(struct dp_bp_attr *conf, uint32_t flag);
 
+#define DP_CQM_LU_MODE_SET BIT(0)
+#define DP_CQM_LU_MODE_GET BIT(1)
+
+/*!
+ *@brief dp_lookup_mode_cfg API
+ *@param[in] inst	: Instance
+ *@param[in/out] lu_mode: lu_mode set/get
+ *@param[in] map	: qmap values, to set/get lookup mode
+ *@param[in] flag	: DP_CQM_LU_MODE_SET/DP_CQM_LU_MODE_GET
+ *@return DP_SUCCESS on succeed and DP_FAILURE on failure
+ */
+int dp_lookup_mode_cfg(int inst, u32 lu_mode, struct dp_q_map *map,
+		       u32 flag);
+
 /*!
  *@brief dp_net_dev_get_ss_stat_strings API - function used by ethtool
  *	 to retrieve set of counters' names
