From 0f4ed9c78ddb71dff6bc3b6800233dc7d4f92459 Mon Sep 17 00:00:00 2001
From: thampan <joby.thampan@intel.com>
Date: Wed, 5 May 2021 14:10:21 +0800
Subject: [PATCH] DRVLIB_SW-4126: Add DPM changes corresponding to CQM change
 for alloc_complete data

---
 drivers/net/datapath/dpm/datapath_api.c | 271 +++++++++++++++++---------------
 drivers/net/datapath/dpm/datapath_cqm.c |  92 ++++++-----
 include/net/datapath_api.h              |   1 +
 include/net/datapath_dp_cqm.h           |   8 +
 include/net/lantiq_cbm_api.h            |   1 +
 5 files changed, 201 insertions(+), 172 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index e8fe6e28c5a3..654740ea9232 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -65,7 +65,6 @@ EXPORT_SYMBOL(dp_dbg_err);
 u32 reinsert_deq_port = 0;
 
 static int dp_register_dc(int inst, u32 port_id,
-			  struct cbm_dp_alloc_complete_data *cbm_data,
 			  struct dp_dev_data *data, u32 flags);
 
 static int dp_build_cqm_data(int inst, u32 port_id,
@@ -501,6 +500,7 @@ static int32_t dp_alloc_port_private(int inst,
 	/* Only clear those fields we need to clear */
 	port = get_dp_port_info(inst, port_id);
 	memset(port, 0, offsetof(struct pmac_port_info, tail));
+	port->type = DP_DATA_PORT_LINUX;
 	/*save info from caller */
 	port->owner = owner;
 	port->dev = dev;
@@ -517,10 +517,7 @@ static int32_t dp_alloc_port_private(int inst,
 		 cbm_data.dp_port, cbm_data.deq_port, cbm_data.deq_port_num);
 
 	port->num_dma_chan = cbm_data.num_dma_chan;
-#if 1  /* TODO: Hardcorded currently, later need to align with CQM */
-	port->policy_base = 0;
-	port->policy_num = 4;
-#endif
+
 	if (cbm_data.num_dma_chan) {
 		int dma_ch_base;
 
@@ -630,7 +627,6 @@ int dp_cbm_deq_port_enable(struct module *owner, int inst, int port_id,
 int32_t dp_register_subif_private(int inst, struct module *owner,
 				  struct net_device *dev,
 				  char *subif_name, dp_subif_t *subif_id,
-				  /*device related info*/
 				  struct dp_subif_data *data, u32 flags)
 {
 	int i, port_id, start, end, j;
@@ -686,7 +682,7 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 				port_info->vap_mask);
 		end = start + 1;
 	}
-	/*pr_info("search range: start=%d end=%d\n",start, end);*/
+
 	/*allocate a free subif */
 	for (i = start; i < end; i++) {
 		sif = get_dp_port_subif(port_info, i);
@@ -713,7 +709,8 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 		sif->prel2_len = 1;
 
 	cbm_data.dp_inst = inst;
-	cbm_data.num_dma_chan = dp_get_dma_ch_num(inst, port_id, data->num_deq_port);
+	cbm_data.num_dma_chan = dp_get_dma_ch_num(inst, port_id,
+				data->num_deq_port);
 	cbm_data.cbm_inst = dp_port_prop[inst].cbm_inst;
 	cbm_data.deq_port = port_info->deq_port_base + data->deq_port_idx;
 	if ((cbm_data.deq_port == 0) ||
@@ -721,10 +718,7 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 		pr_err("Wrong deq_port: %d\n", cbm_data.deq_port);
 		return DP_FAILURE;
 	}
-	if (!dp_dma_chan_tbl[inst]) {
-		pr_err("dp_dma_chan_tbl[%d] NULL\n", inst);
-		return DP_FAILURE;
-	}
+
 	for (j = 0; j < data->num_deq_port; j++) {
 		cqm_deq_port = sif->cqm_deq_port[j];
 		dma_ch_offset = DP_DEQ(inst, cqm_deq_port).dma_ch_offset;
@@ -734,32 +728,20 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 				    data->deq_port_idx + j;
 		if (get_dp_deqport_info(inst, cbm_data.deq_port)->ref_cnt)
 			continue;
-		/* No need to enable DMA if multiple DEQ->single
-		 * DMA
-		 */
+		/* No need to enable DMA if multiple DEQ->single DMA */
 		if (j != 0)
 			cbm_data.dma_chnl_init = 0;
 		else
 			/* PPA Directpath/LitePath don't have DMA CH */
-			if (dma_ch_ref_curr == 0 &&
-			    !is_directpath(port_info))
-				/*to enable DMA*/
+			if (dma_ch_ref_curr == 0 && !is_directpath(port_info))
 				cbm_data.dma_chnl_init = 1;
-		DP_DEBUG(DP_DBG_FLAG_REG,
-			 "%s%d%s%d%s%d%s%d%d\n",
-			 "cbm_dp_enable: dp_port=", port_id,
-			 " deq_port=", cbm_data.deq_port,
-			 " dma_chnl_init=", cbm_data.dma_chnl_init,
-			 " tx_dma_chan ref=", dma_ch_ref, dma_ch_ref_curr);
 		if (dp_enable_cqm_port(owner, port_info, &cbm_data, 0))
 			return DP_FAILURE;
 	}
 	DP_DEBUG(DP_DBG_FLAG_REG, "cbm_dp_enable ok\n");
-	/*now find a free subif or valid subif
-	 *need to do configuration HW
-	 */
-	if (dp_info->subif_platform_set(inst, port_id, i,
-					&platfrm_data, flags)) {
+	/* now find a free subif or valid subif need to do configuration HW */
+	if (dp_info->subif_platform_set(inst, port_id, i, &platfrm_data,
+					flags)) {
 		pr_err("subif_platform_set fail\n");
 		return DP_FAILURE;
 	}
@@ -768,16 +750,15 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 	sif->flags = 1;
 	sif->netif = dev;
 	port_info->port_id = port_id;
-	/*currently this field is used for EPON case. Later can further
-	 * enhance
-	 */
+
+	/* currently this field is used for EPON case. Later can enhance */
 	sif->num_qid = data->num_deq_port;
 	sif->deq_port_idx = data->deq_port_idx;
 
-	if (subif_id->subif < 0) /*dynamic:shift bits as HW defined*/
+	if (subif_id->subif < 0)
 		sif->subif = SET_VAP(i, port_info->vap_offset,
 				     port_info->vap_mask);
-	else /*provided by caller since it is alerady shifted properly*/
+	else /* provided by caller since it is alerady shifted properly */
 		sif->subif = subif_id->subif;
 	strncpy(sif->device_name, subif_name, sizeof(sif->device_name) - 1);
 	sif->subif_flag = flags;
@@ -1129,7 +1110,8 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 			DP_DEBUG(DP_DBG_FLAG_REG,
 				 "reduce old bp refcnt:%d\n", bp_info->ref_cnt);
 			dp_notifier_invoke(info->inst,
-					   old_ctp_dev, info->dp_port, info->subif,
+					   old_ctp_dev, info->dp_port,
+					   info->subif,
 					   DP_EVENT_DE_REGISTER_SUBIF);
 			f_rem_old_ctp_dev = 1;
 		}
@@ -1144,8 +1126,8 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 			 old_dev->name);
 		dp_inst_del_dev(old_dev, info->inst, info->dp_port, vap, 0);
 
-		dp_notifier_invoke(info->inst, old_dev, info->dp_port, info->subif,
-				   DP_EVENT_DE_REGISTER_SUBIF);
+		dp_notifier_invoke(info->inst, old_dev, info->dp_port,
+			info->subif, DP_EVENT_DE_REGISTER_SUBIF);
 		dp_free_bp_priv(info->inst, sif->bp);
 	}
 
@@ -1382,6 +1364,74 @@ static int remove_umt(int inst, const struct dp_umt_port *umt)
 }
 #endif
 
+static int32_t dp_deregister_dev(int inst, struct module *owner,
+				 u32 port_id, struct dp_dev_data *data,
+				 u32 flags)
+{
+	struct pmac_port_info *port_info = get_dp_port_info(inst, port_id);
+	struct cbm_dp_alloc_complete_data *cbm_data;
+#if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30)
+	struct cqm_port_info *deq;
+#endif
+	int i, ret;
+
+	if (port_info->status != PORT_DEV_REGISTERED) {
+		DP_DEBUG(DP_DBG_FLAG_REG,
+			 "%s de-register dev failed due to wrong state %d\n",
+			 owner->name, port_info->status);
+		return DP_FAILURE;
+	}
+	cbm_data = kzalloc(sizeof(*cbm_data), GFP_ATOMIC);
+	if (!cbm_data)
+		return DP_FAILURE;
+
+	dp_notifier_invoke(inst, port_info->dev, port_id, 0,
+			   DP_EVENT_DE_REGISTER_DEV);
+
+	port_info->status = PORT_ALLOCATED;
+	cbm_data->deq_port = port_info->deq_port_base;
+	cbm_data->num_rx_ring = port_info->num_rx_ring;
+	for (i = 0; i < port_info->num_rx_ring; i++) {
+		cbm_data->rx_ring[i] = &data->rx_ring[i];
+		cbm_data->rx_ring[i]->out_enq_port_id =
+			port_info->rx_ring[i].out_enq_port_id;
+		cbm_data->rx_ring[i]->out_dma_ch_to_gswip =
+			port_info->rx_ring[i].out_dma_ch_to_gswip;
+		cbm_data->rx_ring[i]->out_cqm_deq_port_id =
+			port_info->rx_ring[i].out_cqm_deq_port_id;
+		cbm_data->rx_ring[i]->rx_policy_base =
+			port_info->rx_ring[i].rx_policy_base;
+		cbm_data->rx_ring[i]->policy_num =
+			port_info->rx_ring[i].policy_num;
+	}
+	cbm_data->num_tx_ring = port_info->num_tx_ring;
+	for (i = 0; i < port_info->num_tx_ring; i++) {
+		cbm_data->tx_ring[i] = &data->tx_ring[i];
+		cbm_data->tx_ring[i]->txout_policy_base =
+			port_info->tx_ring[i].txout_policy_base;
+		cbm_data->tx_ring[i]->policy_num =
+			port_info->tx_ring[i].policy_num;
+	}
+	/* Free CQM resources allocated during dev register */
+	ret = dp_cqm_port_alloc_complete(owner, port_info, port_id,
+					 cbm_data, DP_F_DEREGISTER);
+	kfree(cbm_data);
+	if (ret == DP_FAILURE)
+		return ret;
+
+	DP_CB(inst, dev_platform_set)(inst, port_id, data, flags);
+
+#if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30)
+	for (i = 0; i < port_info->num_umt_port; i++) {
+		deq = get_dp_deqport_info(inst, port_info->deq_port_base + i);
+		if (deq->ref_cnt_umt && !(--deq->ref_cnt_umt))
+			remove_umt(inst, &port_info->umt[i]);
+	}
+#endif
+
+	return DP_SUCCESS;
+}
+
 int32_t dp_register_dev_ext(int inst, struct module *owner, u32 port_id,
 			    dp_cb_t *dp_cb, struct dp_dev_data *data,
 			    u32 flags)
@@ -1390,9 +1440,6 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, u32 port_id,
 	struct pmac_port_info *port_info;
 	struct dp_dev_data tmp_data = {0};
 	struct cbm_dp_alloc_complete_data *cbm_data = NULL;
-#if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30)
-	struct cqm_port_info *p;
-#endif
 
 	if (unlikely(!dp_init_ok)) {
 		pr_err("dp_register_dev failed for datapath not init yet\n");
@@ -1414,37 +1461,8 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, u32 port_id,
 
 	DP_LIB_LOCK(&dp_lock);
 	if (flags & DP_F_DEREGISTER) {	/*de-register */
-		if (port_info->status == PORT_DEV_REGISTERED) {
-			dp_notifier_invoke(inst, port_info->dev, port_id, 0,
-					   DP_EVENT_DE_REGISTER_DEV);
-			port_info->status = PORT_ALLOCATED;
-#if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30)
-			cbm_data = kzalloc(sizeof(*cbm_data), GFP_ATOMIC);
-			if (!cbm_data)
-				goto exit;
-
-			if (dp_build_cqm_data(inst, port_id, cbm_data, data))
-				goto exit;
-
-			/* Free CQM resources allocated during dev register */
-			if (dp_cqm_port_alloc_complete(owner, port_info,
-						       port_id, cbm_data,
-						       DP_F_DEREGISTER))
-				goto exit;
-
-			p = get_dp_deqport_info(inst, port_info->deq_port_base);
-			if (p->ref_cnt_umt && !(--p->ref_cnt_umt))
-				remove_umt(inst, &port_info->umt[0]);
-#endif
-			DP_CB(inst, dev_platform_set)(inst, port_id, data,
-						      flags);
-			res = DP_SUCCESS;
-		} else {
-			pr_err("(%s)de-register fail for status:%d,%s(%d)\n",
-			       owner->name,
-			       port_info->status, "num_subif",
-			       port_info->num_subif);
-		}
+		res = dp_deregister_dev(inst, owner, port_id, data,
+					flags);
 		goto exit;
 	}
 
@@ -1477,10 +1495,10 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, u32 port_id,
 				       flags))
 		goto exit;
 
-	if (data->num_rx_ring || data->num_tx_ring) {
-		if (dp_register_dc(inst, port_id, cbm_data, data, flags))
-			goto exit;
-	}
+	res = dp_register_dc(inst, port_id, data, flags);
+	if (res)
+		goto exit;
+
 
 	/* TODO: Need a HAL layer API for CQM and DMA Setup for CQM QoS path
 	 * especially for LGM 4 Ring case
@@ -1536,11 +1554,12 @@ int32_t dp_register_subif_ext(int inst, struct module *owner,
 	}
 
 	DP_DEBUG(DP_DBG_FLAG_REG,
-		 "%s:owner=%s dev=%s %s=%s port_id=%d subif=%d(%s) %s%s\n",
+		 "%s:owner=%s dev=%s(px=%px p=%p) %s=%s port_id=%d subif=%d(%s) %s%s\n",
 		 (flags & DP_F_DEREGISTER) ?
 		 "unregister subif:" : "register subif",
 		 owner ? owner->name : "NULL",
 		 dev ? dev->name : "NULL",
+		 dev, dev,
 		 "subif_name",
 		 subif_name,
 		 subif_id->port_id,
@@ -1658,9 +1677,9 @@ int32_t dp_get_netif_subifid(struct net_device *netif, struct sk_buff *skb,
 	int res = DP_FAILURE;
 
 	if (unlikely(!dp_init_ok)) {
-		 DP_DEBUG(DP_DBG_FLAG_DBG,
-			  "%s failed: datapath not initialized yet\n",
-			  __func__);
+		DP_DEBUG(DP_DBG_FLAG_DBG,
+			 "%s failed: datapath not initialized yet\n",
+			 __func__);
 		return res;
 	}
 
@@ -1880,11 +1899,9 @@ static int dp_build_cqm_data(int inst, u32 port_id,
 	/* For PRX300 No: of Tx Ring is 1 */
 	cbm_data->num_tx_ring = data->num_tx_ring;
 
-	/* TX Ring */
-	for (i = 0; i < data->num_tx_ring; i++) {
-		memcpy(&cbm_data->tx_ring[i], &data->tx_ring[i],
-		       sizeof(struct dp_tx_ring));
-	}
+	/* Pass TX ring info to CQM */
+	for (i = 0; i < data->num_tx_ring; i++)
+		cbm_data->tx_ring[i] = &data->tx_ring[i];
 
 	cbm_data->deq_port = port->deq_port_base;
 
@@ -1892,20 +1909,20 @@ static int dp_build_cqm_data(int inst, u32 port_id,
 	/* For PRX300 No: of Rx Ring is 1 */
 	cbm_data->num_rx_ring = data->num_rx_ring;
 
-	/* RX ring */
-	for (i = 0; i < data->num_rx_ring; i++) {
-		memcpy(&cbm_data->rx_ring[i], &data->rx_ring[i],
-		       sizeof(struct dp_rx_ring));
-	}
+	/* Pass RX ring info to CQM */
+	for (i = 0; i < data->num_rx_ring; i++)
+		cbm_data->rx_ring[i] = &data->rx_ring[i];
 
 	cbm_data->num_qid = data->num_resv_q;
 	cbm_data->qid_base = data->qos_resv_q_base;
+	cbm_data->alloc_flags = port->alloc_flags;
+	cbm_data->bm_policy_res_id = data->bm_policy_res_id;
+	cbm_data->opt_param = data->opt_param;
 
 	return 0;
 }
 
 static int dp_register_dc(int inst, u32 port_id,
-			  struct cbm_dp_alloc_complete_data *cbm_data,
 			  struct dp_dev_data *data, u32 flags)
 {
 	struct pmac_port_info *port = get_dp_port_info(inst, port_id);
@@ -1916,27 +1933,23 @@ static int dp_register_dc(int inst, u32 port_id,
 #endif
 	int i, ret = DP_SUCCESS;
 
+	if (!data->num_rx_ring)
+		return DP_SUCCESS;
+
 	/* Fill in the output data to the the DCDP driver for the RX rings
 	 * and Save Info for debugging
 	 */
-	for (i = 0; i < data->num_rx_ring; i++) {
-		memcpy(&data->rx_ring[i], &cbm_data->rx_ring[i],
-		       sizeof(struct dp_rx_ring));
-		memcpy(&port->rx_ring[i], &cbm_data->rx_ring[i],
-		       sizeof(struct dp_rx_ring));
-	}
+	for (i = 0; i < data->num_rx_ring; i++)
+		port->rx_ring[i] = data->rx_ring[i];
 
-	for (i = 0; i < data->num_tx_ring; i++) {
-		memcpy(&data->tx_ring[i], &cbm_data->tx_ring[i],
-		       sizeof(struct dp_tx_ring));
-		memcpy(&port->tx_ring[i], &cbm_data->tx_ring[i],
-		       sizeof(struct dp_tx_ring));
-	}
+	for (i = 0; i < data->num_tx_ring; i++)
+		port->tx_ring[i] = data->tx_ring[i];
 
 	/* Save info for debugging */
 	port->num_rx_ring = data->num_rx_ring;
 	port->num_tx_ring = data->num_tx_ring;
 
+	port->num_umt_port = min(data->num_umt_port, data->num_rx_ring);
 	/* UMT Interface is not supported for old products */
 #if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30)
 	res = &data->umt->res;
@@ -3178,10 +3191,10 @@ void dp_cleanup_module(void)
 /*!
  * @brief get network device's MTU
  * @param[in] dev: network device pointer
- * @param[out] mtu_size: return the MTU value in the specified memory if valid.
- * @Returns DP_SUCCESS on succeed and DP_FAILURE on failure
+ * @param[out] mtu_size: return the maximum MTU can be supported
+ *                       for this device based on current HW configuration
+ * @return DP_SUCCESS on succeed and DP_FAILURE on failure
  */
-/* This API will be enhanced later based on netdevice */
 int dp_get_mtu_size(struct net_device *dev, u32 *mtu_size)
 {
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP32)
@@ -3197,33 +3210,33 @@ int dp_get_mtu_size(struct net_device *dev, u32 *mtu_size)
 	}
 
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP31)
-		return cbm_get_mtu_size(mtu_size);
+	return cbm_get_mtu_size(mtu_size);
 #endif
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP32)
-		if (unlikely(dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif,
-						  0)))
-			return DP_FAILURE;
-
-		port = get_dp_port_info(subif.inst, subif.port_id);
-		sif = get_dp_port_subif(port, GET_VAP(subif.subif, port->vap_offset,
-					port->vap_mask));
-
-		mtu.cbm_inst = dp_port_prop[subif.inst].cbm_inst;
-		mtu.dp_port = subif.port_id;
-		mtu.policy_map = cqm_get_policy_map(subif.inst, sif->policy_base,
-						    sif->policy_num,
-						    port->alloc_flags,
-						    TX_POLICYMAP);
-		mtu.alloc_flag = port->alloc_flags;
-		mtu.subif_flag = sif->flags;
+	if (unlikely(dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif,
+					  0)))
+		return DP_FAILURE;
 
-		if (unlikely(cbm_get_mtu_size(&mtu)))
-			return DP_FAILURE;
-		mtu.mtu -= ETH_HLEN;
-		if (is_stream_port(port->alloc_flags))
-			mtu.mtu -= sizeof(struct pmac_tx_hdr);
-		if (likely(mtu_size))
-			*mtu_size = mtu.mtu;
+	port = get_dp_port_info(subif.inst, subif.port_id);
+	sif = get_dp_port_subif(port, GET_VAP(subif.subif, port->vap_offset,
+					      port->vap_mask));
+
+	mtu.cbm_inst = dp_port_prop[subif.inst].cbm_inst;
+	mtu.dp_port = subif.port_id;
+	mtu.policy_map = cqm_get_policy_map(subif.inst, sif->policy_base,
+					    sif->policy_num,
+					    port->alloc_flags,
+					    TX_POLICYMAP);
+	mtu.alloc_flag = port->alloc_flags;
+	mtu.subif_flag = sif->flags;
+
+	if (unlikely(cbm_get_mtu_size(&mtu)))
+		return DP_FAILURE;
+	mtu.mtu -= ETH_HLEN;
+	if (is_stream_port(port->alloc_flags))
+		mtu.mtu -= sizeof(struct pmac_tx_hdr);
+	if (likely(mtu_size))
+		*mtu_size = mtu.mtu;
 #endif
 	return DP_SUCCESS;
 }
diff --git a/drivers/net/datapath/dpm/datapath_cqm.c b/drivers/net/datapath/dpm/datapath_cqm.c
index 52ec26b34337..7fd72ea8fde5 100644
--- a/drivers/net/datapath/dpm/datapath_cqm.c
+++ b/drivers/net/datapath/dpm/datapath_cqm.c
@@ -56,54 +56,60 @@ int dump_ring_info(struct cbm_dp_alloc_complete_data *data)
 	for (i = 0; i < data->num_tx_ring; i++) {
 		pr_info("    DC TxRing:         %d\n", i);
 
-		pr_info("      TXIN  DeqRingSize/paddr:     %d/0x%px\n",
-			data->tx_ring[i].in_deq_ring_size,
-			data->tx_ring[i].in_deq_paddr);
-		pr_info("      TXOUT FreeRingSize/paddr:    %d/0x%px\n",
-			data->tx_ring[i].out_free_ring_size,
-			data->tx_ring[i].out_free_paddr);
-		pr_info("      TXOUT PolicyBase/Poolid:     %d/%d\n",
-			data->tx_ring[i].txout_policy_base,
-			data->tx_ring[i].tx_poolid);
-		pr_info("      PolicyNum:                   %d\n",
-			data->tx_ring[i].policy_num);
-		pr_info("      NumOfTxPkt/TxPktSize:        %d/%d\n",
-			data->tx_ring[i].num_tx_pkt,
-			data->tx_ring[i].tx_pkt_size);
+		if (data->tx_ring[i]) {
+			pr_info("      TXIN  DeqRingSize/paddr:     %d/0x%px\n",
+				data->tx_ring[i]->in_deq_ring_size,
+				data->tx_ring[i]->in_deq_paddr);
+			pr_info("      TXOUT FreeRingSize/paddr:    %d/0x%px\n",
+				data->tx_ring[i]->out_free_ring_size,
+				data->tx_ring[i]->out_free_paddr);
+			pr_info("      TXOUT PolicyBase/Poolid:     %d/%d\n",
+				data->tx_ring[i]->txout_policy_base,
+				data->tx_ring[i]->tx_poolid);
+			pr_info("      PolicyNum:                   %d\n",
+				data->tx_ring[i]->policy_num);
+			pr_info("      NumOfTxPkt/TxPktSize:        %d/%d\n",
+				data->tx_ring[i]->num_tx_pkt,
+				data->tx_ring[i]->tx_pkt_size);
+		}
 	}
 
 	for (i = 0; i < data->num_rx_ring; i++) {
 		pr_info("    DC RxRing:         %d\n", i);
 
-		pr_info("      %s:  %d/0x%px/%d\n",
-			"RXOUT EnqRingSize/paddr/pid",
-			data->rx_ring[i].out_enq_ring_size,
-			data->rx_ring[i].out_enq_paddr,
-			data->rx_ring[i].out_enq_port_id);
-		pr_info("      %s:             %d\n",
-			"RXOUT NumOfDmaCh",
-			data->rx_ring[i].num_out_tx_dma_ch);
-		dp_dma_parse_id(data->rx_ring[i].out_dma_ch_to_gswip,
-				&cid, &pid, &nid);
-		pr_info("      %s:     %d/%d/%d\n",
-			"RXOUT dma-ctrl/port/chan",
-			cid, pid, nid);
-		pr_info("      RXOUT NumOfCqmDqPort/pid:     %d/%d\n",
-			data->rx_ring[i].num_out_cqm_deq_port,
-			data->rx_ring[i].out_cqm_deq_port_id);
-		pr_info("      %s:  %d/0x%px\n",
-			"RXIN  InAllocRingSize/Paddr",
-			data->rx_ring[i].in_alloc_ring_size,
-			data->rx_ring[i].in_alloc_paddr);
-		pr_info("      %s:    %d/%d/%d\n",
-			"NumPkt/Pktsize/Policybase",
-			data->rx_ring[i].num_pkt,
-			data->rx_ring[i].rx_pkt_size,
-			data->rx_ring[i].rx_policy_base);
-		pr_info("      %s:        %d/0x%px\n",
-			"PreFillPktNum/PktBase",
-			data->rx_ring[i].prefill_pkt_num,
-			data->rx_ring[i].pkt_base_paddr);
+		if (data->rx_ring[i]) {
+			pr_info("      %s:  %d/0x%px/%d\n",
+				"RXOUT EnqRingSize/paddr/pid",
+				data->rx_ring[i]->out_enq_ring_size,
+				data->rx_ring[i]->out_enq_paddr,
+				data->rx_ring[i]->out_enq_port_id);
+			pr_info("      %s:             %d\n",
+				"RXOUT NumOfDmaCh",
+				data->rx_ring[i]->num_out_tx_dma_ch);
+			dp_dma_parse_id(data->rx_ring[i]->out_dma_ch_to_gswip,
+					&cid, &pid, &nid);
+			pr_info("      %s:     %d/%d/%d\n",
+				"RXOUT dma-ctrl/port/chan",
+				cid, pid, nid);
+			pr_info("      RXOUT NumOfCqmDqPort/pid:     %d/%d\n",
+				data->rx_ring[i]->num_out_cqm_deq_port,
+				data->rx_ring[i]->out_cqm_deq_port_id);
+			pr_info("      RXOUT Poolid:		    %d\n",
+				data->rx_ring[i]->rx_poolid);
+			pr_info("      %s:  %d/0x%px\n",
+				"RXIN  InAllocRingSize/Paddr",
+				data->rx_ring[i]->in_alloc_ring_size,
+				data->rx_ring[i]->in_alloc_paddr);
+			pr_info("      %s:    %d/%d/%d\n",
+				"NumPkt/Pktsize/Policybase",
+				data->rx_ring[i]->num_pkt,
+				data->rx_ring[i]->rx_pkt_size,
+				data->rx_ring[i]->rx_policy_base);
+			pr_info("      %s:        %d/0x%px\n",
+				"PreFillPktNum/PktBase",
+				data->rx_ring[i]->prefill_pkt_num,
+				data->rx_ring[i]->pkt_base_paddr);
+		}
 	}
 
 	return 0;
diff --git a/include/net/datapath_api.h b/include/net/datapath_api.h
index a5ab731b24e5..8f2cfe541e82 100644
--- a/include/net/datapath_api.h
+++ b/include/net/datapath_api.h
@@ -1371,6 +1371,7 @@ struct dp_dev_data {
 			       * linked to WIB
 			       */
 	u32 bm_policy_res_id; /*!< [in] buffer policy id */
+	struct dp_dev_opt_param opt_param; /*!< [in] optional parameters */
 };
 
 /*! @addtogroup Datapath_Driver_API */
diff --git a/include/net/datapath_dp_cqm.h b/include/net/datapath_dp_cqm.h
index 574e05c3a01d..fd9a749de828 100644
--- a/include/net/datapath_dp_cqm.h
+++ b/include/net/datapath_dp_cqm.h
@@ -84,4 +84,12 @@ struct dp_dc_tx_res {
 				 *         specified res_id
 				 */
 };
+
+/*! @brief struct dp_dev_opt_param, which used for top level to pass extra
+ *  optional paramters to dpm
+ */
+struct dp_dev_opt_param {
+	void *pcidata; /*!< for pci dev to pass pcidata in LGM b0 */
+};
+
 #endif
diff --git a/include/net/lantiq_cbm_api.h b/include/net/lantiq_cbm_api.h
index c257cfdd50e2..83899cb5c448 100644
--- a/include/net/lantiq_cbm_api.h
+++ b/include/net/lantiq_cbm_api.h
@@ -661,6 +661,7 @@ struct cbm_dp_alloc_complete_data {
 	u32 qid_base; /* [in] EPON baseport */
 	u32 num_qid; /* [in] EPON num qid */
 	u32 bm_policy_res_id; /* [in] buffer policy id */
+	struct dp_dev_opt_param opt_param; /* [in] optional parameters */
 };
 
 s32 cbm_dp_port_alloc_complete(
