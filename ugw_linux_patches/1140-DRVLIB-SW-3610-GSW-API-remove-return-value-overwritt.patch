From 015ab8e039f279a5fd75b8852d2374ea1d029057 Mon Sep 17 00:00:00 2001
From: Xu Liang <liang.xu@intel.com>
Date: Sun, 3 May 2020 16:17:36 +0800
Subject: [PATCH] DRVLIB_SW-3610: GSW API - remove return value overwritten in
 PAE APIs

---
 drivers/net/ethernet/lantiq/switch-api/gsw_pae.c | 39 ++++++------------------
 1 file changed, 9 insertions(+), 30 deletions(-)

diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c b/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c
index c525fdd0eb44..55f300e235b8 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c
@@ -1298,8 +1298,6 @@ int GSW_ROUTE_SessionEntryAdd(void *cdev, GSW_ROUTE_Entry_t *rpar)
 
 		if (sindex != 0x7FFF)
 			rt_ip_tbl_delete(cdev, &rthandler->rt_sub_tbl, sindex);
-
-		retval = GSW_statusOk;
 	} else {
 		retval = GSW_statusErr;
 	}
@@ -1317,7 +1315,7 @@ int GSW_ROUTE_SessionEntryDel(void *cdev, GSW_ROUTE_Entry_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
 	int hindex,  index;
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -1423,8 +1421,6 @@ int GSW_ROUTE_SessionEntryDel(void *cdev, GSW_ROUTE_Entry_t *rpar)
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 
 UNLOCK_AND_RETURN:
 
@@ -1439,7 +1435,7 @@ int GSW_ROUTE_SessionEntryRead(void *cdev,
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
 	u16 index;
-	int i, retval = 0;
+	int i, retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -1742,8 +1738,6 @@ int GSW_ROUTE_SessionEntryRead(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 #ifdef __KERNEL__
 	spin_unlock_bh(&ethdev->lock_pae);
 #endif
@@ -1754,7 +1748,7 @@ int GSW_ROUTE_TunnelEntryAdd(void *cdev,
 			     GSW_ROUTE_Tunnel_Entry_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int i, j, retval = 0;
+	int i, j, retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -1854,9 +1848,6 @@ int GSW_ROUTE_TunnelEntryAdd(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
-
 #ifdef __KERNEL__
 	spin_unlock_bh(&ethdev->lock_pae);
 #endif
@@ -1867,7 +1858,7 @@ int GSW_ROUTE_TunnelEntryRead(void *cdev,
 			      GSW_ROUTE_Tunnel_Entry_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -1923,8 +1914,6 @@ int GSW_ROUTE_TunnelEntryRead(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 #ifdef __KERNEL__
 	spin_unlock_bh(&ethdev->lock_pae);
 #endif
@@ -1936,7 +1925,7 @@ int GSW_ROUTE_TunnelEntryDel(void *cdev,
 			     GSW_ROUTE_Tunnel_Entry_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -1963,8 +1952,6 @@ int GSW_ROUTE_TunnelEntryDel(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 #ifdef __KERNEL__
 	spin_unlock_bh(&ethdev->lock_pae);
 #endif
@@ -1975,7 +1962,7 @@ int GSW_ROUTE_L2NATCfgWrite(void *cdev,
 			    GSW_ROUTE_EgPort_L2NAT_Cfg_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -2021,8 +2008,6 @@ int GSW_ROUTE_L2NATCfgWrite(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 UNLOCK_AND_RETURN:
 
 #ifdef __KERNEL__
@@ -2036,7 +2021,7 @@ int GSW_ROUTE_L2NATCfgRead(void *cdev,
 			   GSW_ROUTE_EgPort_L2NAT_Cfg_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -2085,8 +2070,6 @@ int GSW_ROUTE_L2NATCfgRead(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 UNLOCK_AND_RETURN:
 
 #ifdef __KERNEL__
@@ -2100,7 +2083,7 @@ int GSW_ROUTE_SessHitOp(void *cdev,
 			GSW_ROUTE_Session_Hit_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 	if (ethdev == NULL) {
 		pr_err("%s:%s:%d \n", __FILE__, __func__, __LINE__);
@@ -2170,8 +2153,6 @@ int GSW_ROUTE_SessHitOp(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 #ifdef __KERNEL__
 	spin_unlock_bh(&ethdev->lock_pae);
 #endif
@@ -2182,7 +2163,7 @@ int GSW_ROUTE_SessDestModify(void *cdev,
 			     GSW_ROUTE_Session_Dest_t *rpar)
 {
 	ethsw_api_dev_t *ethdev = GSW_PDATA_GET(cdev);
-	int retval = 0;
+	int retval = GSW_statusOk;
 
 
 	if (ethdev == NULL) {
@@ -2217,8 +2198,6 @@ int GSW_ROUTE_SessDestModify(void *cdev,
 		retval = GSW_statusErr;
 	}
 
-	retval = GSW_statusOk;
-
 #ifdef __KERNEL__
 	spin_unlock_bh(&ethdev->lock_pae);
 #endif
