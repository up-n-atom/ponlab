From dcc2064304e4c36f90be499cae046ea4cfed3d95 Mon Sep 17 00:00:00 2001
From: Oren Bakshe <oren.bakshe@intel.com>
Date: Wed, 25 Dec 2019 15:35:36 +0200
Subject: [PATCH] PONRTSYS-6269: Add WRED new debug counters for B step

---
 .../net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c    |  90 ++++++++++++++++++
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c     | 101 +++++++++++++++++++--
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h     |   3 +-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c   |   8 +-
 .../net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h    |  47 +++++++++-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c  |   8 +-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h  |   7 --
 drivers/net/ppv4/pp-hal/session-info/pp_si.h       |   2 +-
 .../net/ppv4/pp-tools/debugfs/pp_debugfs_common.h  |   4 +-
 9 files changed, 241 insertions(+), 29 deletions(-)

diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
index eddfd9d54889..8321d7ef543e 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
@@ -19,6 +19,7 @@
 #include "pp_qos_utils.h"
 #include "pp_qos_fw.h"
 #include "pp_regs.h"
+#include "infra.h"
 
 #define PP_QOS_DEBUGFS_DIR "ppv4_qos"
 
@@ -1895,6 +1896,93 @@ static int __g_raw_config_get(void *data, u64 *val)
 
 PP_DEFINE_DBGFS_ATT(raw_config, __g_raw_config_get, __g_raw_config_set);
 
+void pp_qos_dbg_quanta_get(struct seq_file *s)
+{
+	struct pp_qos_dev *qdev;
+	u32 quanta;
+
+	qdev = pp_qos_dev_open((unsigned long)s->private);
+	if (unlikely(ptr_is_null(qdev)))
+		return;
+
+	if (unlikely(!qos_device_ready(qdev))) {
+		seq_puts(s, "Device is not ready\n");
+		return;
+	}
+
+	if (pp_qos_get_quanta(qdev, &quanta) == 0)
+		seq_printf(s, "quanta: %u\n", quanta);
+	else
+		seq_puts(s, "pp_qos_get_quanta failed\n");
+}
+
+PP_DEFINE_DEBUGFS(quanta, pp_qos_dbg_quanta_get, NULL);
+
+void pp_qos_dbg_system_stats_show(struct seq_file *s)
+{
+	struct pp_qos_dev *qdev;
+	struct system_stats_s sys_stats;
+	s32 ret = 0;
+
+	qdev = pp_qos_dev_open((unsigned long)s->private);
+	if (unlikely(ptr_is_null(qdev)))
+		return;
+
+	if (unlikely(!qos_device_ready(qdev))) {
+		seq_puts(s, "Device is not ready\n");
+		return;
+	}
+
+	create_get_sys_info_cmd(qdev, qdev->hwconf.fw_stats_ddr_phys,
+				&sys_stats, false);
+	update_cmd_id(&qdev->drvcmds);
+	ret = transmit_cmds(qdev);
+	if (ret) {
+		QOS_LOG_ERR("transmit_cmds failed (%d)\n", ret);
+		return;
+	}
+
+	seq_puts(s, "System stats:\n");
+	seq_puts(s, "=============\n");
+	seq_printf(s, "tscd_infinite_loop_error_occurred: %u\n",
+		   sys_stats.tscd_infinite_loop_error_occurred);
+	seq_printf(s, "tscd_bwl_update_error_occurred: %u\n",
+		   sys_stats.tscd_bwl_update_error_occurred);
+	seq_printf(s, "tscd_quanta: %u\n", sys_stats.tscd_quanta);
+
+#if IS_ENABLED(CONFIG_PPV4_LGM)
+	if (pp_silicon_step_get() == PP_SSTEP_B) {
+		seq_printf(s, "wred_fake_pops: %u\n", sys_stats.wred_fake_pops);
+		seq_printf(s, "wred_pop_underflow_count: %u\n",
+			   sys_stats.wred_pop_underflow_count);
+		seq_printf(s, "wred_pop_underflow_sum: %u\n",
+			   sys_stats.wred_pop_underflow_sum);
+		seq_printf(s, "wred_last_push_address_high: %u\n",
+			   sys_stats.wred_last_push_address_high);
+		seq_printf(s, "wred_last_push_address_low: %u\n",
+			   sys_stats.wred_last_push_address_low);
+		seq_printf(s, "wred_last_push_drop: %u\n",
+			   sys_stats.wred_last_push_drop);
+		seq_printf(s, "wred_last_push_info_q_id: %u\n",
+			   sys_stats.wred_last_push_info_q_id);
+		seq_printf(s, "wred_last_push_info_color: %u\n",
+			   sys_stats.wred_last_push_info_color);
+		seq_printf(s, "wred_last_push_info_pkt_size: %u\n",
+			   sys_stats.wred_last_push_info_pkt_size);
+		seq_printf(s, "wred_last_pop_info_q_id: %u\n",
+			   sys_stats.wred_last_pop_info_q_id);
+		seq_printf(s, "wred_last_pop_info_fake: %u\n",
+			   sys_stats.wred_last_pop_info_fake);
+		seq_printf(s, "wred_last_pop_info_color: %u\n",
+			   sys_stats.wred_last_pop_info_color);
+		seq_printf(s, "wred_last_pop_info_pkt_size: %u\n",
+			   sys_stats.wred_last_pop_info_pkt_size);
+	}
+#endif
+}
+
+PP_DEFINE_DEBUGFS(system_stats, pp_qos_dbg_system_stats_show, NULL);
+
 static struct debugfs_file qos_debugfs_files[] = {
 	{"node", &PP_DEBUGFS_FOPS(gnode)},
 	{"raw_config", &PP_DEBUGFS_FOPS(raw_config)},
@@ -1924,6 +2012,8 @@ static struct debugfs_file qos_debugfs_files[] = {
 	{"allocate", &PP_DEBUGFS_FOPS(allocate)},
 	{"remove", &PP_DEBUGFS_FOPS(remove)},
 	{"flush", &PP_DEBUGFS_FOPS(flush)},
+	{"quanta", &PP_DEBUGFS_FOPS(quanta)},
+	{"system_stats", &PP_DEBUGFS_FOPS(system_stats)},
 #ifndef PP_QOS_DISABLE_CMDQ
 	{"cmd", &PP_DEBUGFS_FOPS(cmd)},
 #endif
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
index f51800ded2bb..a3259ab5bc78 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
@@ -419,6 +419,7 @@ enum cmd_type {
 	CMD_TYPE_GET_SYSTEM_INFO,
 	CMD_TYPE_UPDATE_PORT_TREE,
 	CMD_TYPE_SUSPEND_PORT_TREE,
+	CMD_TYPE_GET_QUANTA,
 	CMD_TYPE_INTERNAL,
 	CMD_TYPE_LAST
 };
@@ -453,6 +454,7 @@ static const char *const cmd_str[] = {
 	[CMD_TYPE_FLUSH_QUEUE] = "CMD_TYPE_FLUSH_QUEUE",
 	[CMD_TYPE_GET_SYSTEM_INFO] = "CMD_TYPE_GET_SYSTEM_INFO",
 	[CMD_TYPE_UPDATE_PORT_TREE] = "CMD_TYPE_UPDATE_PORT_TREE",
+	[CMD_TYPE_GET_QUANTA] = "CMD_TYPE_GET_QUANTA",
 	[CMD_TYPE_INTERNAL] = "CMD_TYPE_INTERNAL"
 };
 
@@ -634,6 +636,11 @@ struct cmd_suspend_port_tree {
 	struct fw_cmd_suspend_port_tree fw;
 } __attribute__((packed));
 
+struct cmd_get_quanta {
+	struct cmd base;
+	struct fw_cmd_get_quanta fw;
+} __attribute__((packed));
+
 union driver_cmd {
 	struct cmd                     cmd;
 	struct stub_cmd                stub;
@@ -2254,7 +2261,7 @@ void create_get_table_entry_cmd(struct pp_qos_dev *qdev, u32 phy,
 }
 
 void create_get_sys_info_cmd(struct pp_qos_dev *qdev,
-			     u32 addr, struct qos_hw_info *info,
+			     u32 addr, struct system_stats_s *info,
 			     bool dump_entries)
 {
 	struct cmd_get_system_info cmd;
@@ -2279,6 +2286,25 @@ void create_get_sys_info_cmd(struct pp_qos_dev *qdev,
 	cmd_queue_put(qdev->drvcmds.cmdq, &cmd, sizeof(cmd));
 }
 
+void create_get_quanta_cmd(struct pp_qos_dev *qdev, u32 *quanta)
+{
+	struct cmd_get_quanta cmd;
+
+	if (PP_QOS_DEVICE_IS_ASSERT(qdev))
+		return;
+
+	memset(&cmd, 0, sizeof(cmd));
+	cmd_init(qdev, &cmd.base, CMD_TYPE_GET_QUANTA, sizeof(cmd),
+		 CMD_FLAGS_POST_PROCESS_INT, quanta,
+		 offsetof(struct fw_cmd_get_quanta, quanta),
+		 CMD_INT_RESPONSE_SZ);
+	cmd.fw.base.type = UC_QOS_CMD_GET_QUANTA;
+
+	QOS_LOG_DEBUG("cmd %u: CMD_TYPE_GET_QUANTA\n",
+		      qdev->drvcmds.cmd_id);
+	cmd_queue_put(qdev->drvcmds.cmdq, &cmd, sizeof(cmd));
+}
+
 void create_add_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit)
 {
 	struct cmd_add_shared_group cmd;
@@ -2733,6 +2759,10 @@ static void post_process_internal(struct pp_qos_dev *qdev,
 		node->actual_bandwidth_limit = response;
 		break;
 
+	case CMD_TYPE_GET_QUANTA:
+		*(u32 *)dcmd->cmd.response = response;
+		break;
+
 	default:
 		QOS_ASSERT(0, "Unexpected cmd %d for post process\n", type);
 		return;
@@ -2750,7 +2780,7 @@ static s32 post_process_external(struct pp_qos_dev *qdev,
 	struct queue_stat_info *qinfo;
 	struct pp_qos_codel_stat *cstat;
 	struct pp_qos_port_stat *pstat;
-	struct qos_hw_info *hw_info;
+	struct system_stats_s *sys_stats;
 	struct qm_info *qminfo;
 	u32 stats_poll = 0;
 	u32 i, fw_signal = FW_OK_CODE;
@@ -2950,25 +2980,82 @@ static s32 post_process_external(struct pp_qos_dev *qdev,
 		break;
 
 	case CMD_TYPE_GET_SYSTEM_INFO:
-		hw_info = (struct qos_hw_info *)response;
-		hw_info->num_used =
+		sys_stats = (struct system_stats_s *)response;
+		sys_stats->tscd_num_of_used_nodes =
 			DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
 				 offsetof(struct system_stats_s,
 					  tscd_num_of_used_nodes));
 
-		hw_info->infinite_loop_error_occurred =
+		sys_stats->tscd_infinite_loop_error_occurred =
 			DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
 				 offsetof(struct system_stats_s,
 					  tscd_infinite_loop_error_occurred));
 
-		hw_info->bwl_update_error_occurred =
+		sys_stats->tscd_bwl_update_error_occurred =
 			DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
 				 offsetof(struct system_stats_s,
 					  tscd_bwl_update_error_occurred));
 
-		hw_info->quanta =
+		sys_stats->tscd_quanta =
 			DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
 				 offsetof(struct system_stats_s, tscd_quanta));
+
+#if IS_ENABLED(CONFIG_PPV4_LGM)
+		if (pp_silicon_step_get() == PP_SSTEP_B) {
+			sys_stats->wred_fake_pops =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_fake_pops));
+			sys_stats->wred_pop_underflow_count =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_pop_underflow_count));
+			sys_stats->wred_pop_underflow_sum =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_pop_underflow_sum));
+			sys_stats->wred_last_push_address_high =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_push_address_high));
+			sys_stats->wred_last_push_address_low =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_push_address_low));
+			sys_stats->wred_last_push_drop =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_push_drop));
+			sys_stats->wred_last_push_info_q_id =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_push_info_q_id));
+			sys_stats->wred_last_push_info_color =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_push_info_color));
+			sys_stats->wred_last_push_info_pkt_size =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_push_info_pkt_size));
+			sys_stats->wred_last_pop_info_q_id =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_pop_info_q_id));
+			sys_stats->wred_last_pop_info_fake =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_pop_info_fake));
+			sys_stats->wred_last_pop_info_color =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_pop_info_color));
+			sys_stats->wred_last_pop_info_pkt_size =
+				DDR_RD32((u8 *)qdev->hwconf.fw_stats_ddr_virt +
+					offsetof(struct system_stats_s,
+						 wred_last_pop_info_pkt_size));
+		}
+#endif
 		break;
 
 	case CMD_TYPE_GET_NODE_INFO:
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h
index b15d8fc19e1c..408eea2298bc 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h
@@ -112,8 +112,9 @@ void suspend_node(struct pp_qos_dev *qdev, u32 phy, u32 suspend);
 void add_suspend_port(struct pp_qos_dev *qdev, u32 port);
 void signal_uc(struct pp_qos_dev *qdev);
 void create_get_sys_info_cmd(struct pp_qos_dev *qdev, u32 addr,
-			     struct qos_hw_info *sys_info,
+			     struct system_stats_s *sys_info,
 			     bool dump_entries);
+void create_get_quanta_cmd(struct pp_qos_dev *qdev, u32 *quanta);
 void print_fw_log(struct pp_qos_dev *qdev);
 
 #elif defined(PRINT_CREATE_CMD)
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
index 53c1b448fa52..e02e419b6d04 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
@@ -4009,8 +4009,7 @@ EXPORT_SYMBOL(pp_qos_dev_open);
 
 s32 pp_qos_get_quanta(struct pp_qos_dev *qdev, u32 *quanta)
 {
-	struct qos_hw_info info = {0};
-	s32    rc = 0;
+	s32 rc = 0;
 
 	QOS_LOCK(qdev);
 	PP_QOS_ENTER_FUNC();
@@ -4020,13 +4019,10 @@ s32 pp_qos_get_quanta(struct pp_qos_dev *qdev, u32 *quanta)
 		goto out;
 	}
 
-	create_get_sys_info_cmd(qdev, qdev->hwconf.fw_stats_ddr_phys,
-				&info, false);
+	create_get_quanta_cmd(qdev, quanta);
 	update_cmd_id(&qdev->drvcmds);
 	rc = transmit_cmds(qdev);
 
-	*quanta = info.quanta;
-
 out:
 	QOS_UNLOCK(qdev);
 	return rc;
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
index cd3298bc4182..4313f106aa7c 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
@@ -11,7 +11,7 @@
 
 /* UC version */
 #define UC_VERSION_MAJOR (1)
-#define UC_VERSION_MINOR (8)
+#define UC_VERSION_MINOR (9)
 #define UC_VERSION_BUILD (0)
 
 #define QOS_MAX_PREDECESSORS       (6)
@@ -534,6 +534,45 @@ struct system_stats_s {
 
 	/* Quanta size in KB */
 	u32 tscd_quanta;
+
+	/* WRED Fake pops - LGM B step */
+	u32 wred_fake_pops;
+
+	/* WRED pop byte underflow count - LGM B step */
+	u32 wred_pop_underflow_count;
+
+	/* WRED pop byte underflow sum - LGM B step */
+	u32 wred_pop_underflow_sum;
+
+	/* WRED last push high address - LGM B step */
+	u32 wred_last_push_address_high;
+
+	/* WRED last push low address - LGM B step */
+	u32 wred_last_push_address_low;
+
+	/* WRED last push drop - LGM B step */
+	u32 wred_last_push_drop;
+
+	/* WRED last push info Q id - LGM B step */
+	u32 wred_last_push_info_q_id;
+
+	/* WRED last push info color - LGM B step */
+	u32 wred_last_push_info_color;
+
+	/* WRED last push info packet size - LGM B step */
+	u32 wred_last_push_info_pkt_size;
+
+	/* WRED last pop info Q id - LGM B step */
+	u32 wred_last_pop_info_q_id;
+
+	/* WRED last pop info fake - LGM B step */
+	u32 wred_last_pop_info_fake;
+
+	/* WRED last pop info color - LGM B step */
+	u32 wred_last_pop_info_color;
+
+	/* WRED last pop info packet size - LGM B step */
+	u32 wred_last_pop_info_pkt_size;
 } __attribute__((packed));
 
 /**************************************************************************
@@ -592,6 +631,7 @@ enum uc_qos_command {
 	UC_QOS_CMD_NODES_REFRESH,
 	UC_QOS_CMD_GET_USED_NODES,
 	UC_QOS_CMD_SUSPEND_PORT_TREE,
+	UC_QOS_CMD_GET_QUANTA,
 };
 
 /**************************************************************************
@@ -959,6 +999,11 @@ struct fw_cmd_add_credit_to_port {
 	u32 credits; /* Credit to add */
 } __attribute__((packed));
 
+struct fw_cmd_get_quanta {
+	struct uc_qos_cmd_base base;
+	u32 quanta;   /*! [OUT]: the quanta in KB */
+} __attribute__((packed));
+
 union uc_qos_cmd_s {
 	struct uc_qos_cmd_base           base;
 	struct fw_cmd_multiple_cmds      multiple_cmds;
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
index 4ca948b961f4..209788828be9 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
@@ -3598,7 +3598,7 @@ s32 qos_get_node_info(struct pp_qos_dev *qdev, u32 id,
 s32 check_sync_with_fw(struct pp_qos_dev *qdev, bool dump_entries)
 {
 	const struct qos_node *node;
-	struct qos_hw_info hw_info = {0};
+	struct system_stats_s sys_stats = {0};
 	struct pp_pool *pool;
 	struct pp_qos_node_info info;
 	u32 i, used, id;
@@ -3624,17 +3624,17 @@ s32 check_sync_with_fw(struct pp_qos_dev *qdev, bool dump_entries)
 	}
 
 	create_get_sys_info_cmd(qdev, qdev->hwconf.fw_stats_ddr_phys,
-				&hw_info, dump_entries);
+				&sys_stats, dump_entries);
 	update_cmd_id(&qdev->drvcmds);
 	ret = transmit_cmds(qdev);
 
-	if (hw_info.num_used != used) {
+	if (sys_stats.tscd_num_of_used_nodes != used) {
 #ifdef DUMP_LOG_ON_FAILURE
 		qos_dbg_tree_show(qdev, false, NULL);
 		print_fw_log(qdev);
 #endif
 		QOS_LOG_ERR("Node Count Assertion: DB: %u, FW: %u\n",
-			    used, hw_info.num_used);
+			    used, sys_stats.tscd_num_of_used_nodes);
 		ret = -EINVAL;
 		goto done;
 	}
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
index 7949def7ebcb..610a63c963e3 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
@@ -92,13 +92,6 @@
 /*                              TYPES                                         */
 /******************************************************************************/
 
-struct qos_hw_info {
-	u32 num_used;
-	u32 infinite_loop_error_occurred;
-	u32 bwl_update_error_occurred;
-	u32 quanta;
-};
-
 /**
  * @struct fw_com - Communication to/from FW
  * @cmdbuf: Command buffer offset
diff --git a/drivers/net/ppv4/pp-hal/session-info/pp_si.h b/drivers/net/ppv4/pp-hal/session-info/pp_si.h
index cb1989262022..8ba651660b2f 100644
--- a/drivers/net/ppv4/pp-hal/session-info/pp_si.h
+++ b/drivers/net/ppv4/pp-hal/session-info/pp_si.h
@@ -733,7 +733,7 @@ s32 pp_si_tbm_info_set(u16 *tbm, u16 n_tbm, struct pp_si *si);
 const char * const pp_si_chck_flag_to_str(u32 flag);
 
 /**
- * @brief converts si fields enum to str 
+ * @brief converts si fields enum to str
  * @param fld enum value
  * @return enum string
  */
diff --git a/drivers/net/ppv4/pp-tools/debugfs/pp_debugfs_common.h b/drivers/net/ppv4/pp-tools/debugfs/pp_debugfs_common.h
index 702a39f07797..89a28c34279e 100644
--- a/drivers/net/ppv4/pp-tools/debugfs/pp_debugfs_common.h
+++ b/drivers/net/ppv4/pp-tools/debugfs/pp_debugfs_common.h
@@ -230,7 +230,7 @@ struct pp_dbg_props_cbs {
  * @param size     size of the props array
  * @param name     Property name
  * @param desc     description (Will be printed when user is doing 'cat file')
- * @param dst      Pointer to save the new value of this parameter 
+ * @param dst      Pointer to save the new value of this parameter
  * @param dst_sz   Size of the field
  */
 void pp_dbg_add_prop(struct pp_dbg_prop *props, u16 *pos, u16 size,
@@ -243,7 +243,7 @@ void pp_dbg_add_prop(struct pp_dbg_prop *props, u16 *pos, u16 size,
  * @param size     size of the props array
  * @param name     Property name
  * @param desc     description (Will be printed when user is doing 'cat file')
- * @param dst      Pointer to save the new value of this parameter 
+ * @param dst      Pointer to save the new value of this parameter
  */
 void pp_dbg_add_prop_ptr(struct pp_dbg_prop *props, u16 *pos, u16 size,
 			 const char *name, const char *desc,
