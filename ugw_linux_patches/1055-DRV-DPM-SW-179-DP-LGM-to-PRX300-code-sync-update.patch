From a7fb99507dc0f222c5bce2ce62385b76a6bdb981 Mon Sep 17 00:00:00 2001
From: Rekha Eswaran <rekha.eswaran@intel.com>
Date: Thu, 5 Mar 2020 13:16:01 +0800
Subject: [PATCH] DRV_DPM_SW-179 - DP LGM to PRX300 code sync update

---
 drivers/net/datapath/dpm/Makefile                  |   2 +-
 drivers/net/datapath/dpm/datapath.h                |  28 +-
 drivers/net/datapath/dpm/datapath_api.c            |  72 ++-
 drivers/net/datapath/dpm/datapath_cqm.c            | 339 ++++++++++++
 drivers/net/datapath/dpm/datapath_instance.c       |  21 +-
 drivers/net/datapath/dpm/datapath_instance.h       |   8 +-
 drivers/net/datapath/dpm/datapath_ioctl.c          |   5 +
 drivers/net/datapath/dpm/datapath_ioctl.h          |   5 +
 drivers/net/datapath/dpm/datapath_logical_dev.c    |   8 +-
 drivers/net/datapath/dpm/datapath_misc.c           |  98 ++--
 drivers/net/datapath/dpm/datapath_notifier.c       |   8 +-
 drivers/net/datapath/dpm/datapath_proc.c           |  23 +-
 drivers/net/datapath/dpm/datapath_proc_api.c       |  10 +-
 drivers/net/datapath/dpm/datapath_proc_qos.c       |  26 +-
 drivers/net/datapath/dpm/datapath_qos.c            |   5 +
 drivers/net/datapath/dpm/datapath_soc.c            |   8 +-
 drivers/net/datapath/dpm/datapath_swdev.c          |  33 +-
 drivers/net/datapath/dpm/datapath_swdev.h          |   5 +
 drivers/net/datapath/dpm/datapath_swdev_api.h      |  11 +-
 drivers/net/datapath/dpm/datapath_tx.c             |   5 +
 drivers/net/datapath/dpm/datapath_tx.h             |  11 +-
 drivers/net/datapath/dpm/gswip31/datapath_coc.c    |   9 +-
 .../net/datapath/dpm/gswip31/datapath_ext_vlan.c   |  11 +-
 drivers/net/datapath/dpm/gswip31/datapath_gswip.c  |  35 +-
 .../datapath/dpm/gswip31/datapath_lookup_proc.c    |   8 +-
 drivers/net/datapath/dpm/gswip31/datapath_mib.c    |   8 +-
 drivers/net/datapath/dpm/gswip31/datapath_mib.h    |   8 +-
 drivers/net/datapath/dpm/gswip31/datapath_misc.c   |  29 +-
 drivers/net/datapath/dpm/gswip31/datapath_misc.h   |   8 +-
 drivers/net/datapath/dpm/gswip31/datapath_ppv4.c   | 589 ++++++++++++++-------
 drivers/net/datapath/dpm/gswip31/datapath_ppv4.h   |  97 ++--
 .../net/datapath/dpm/gswip31/datapath_ppv4_api.c   | 218 ++++----
 drivers/net/datapath/dpm/gswip31/datapath_proc.c   |  18 +-
 drivers/net/datapath/dpm/gswip31/datapath_rx.c     |  30 +-
 .../net/datapath/dpm/gswip31/datapath_switchdev.c  |  21 +-
 .../net/datapath/dpm/gswip31/datapath_switchdev.h  |   9 +-
 .../datapath/dpm/gswip31/datapath_tc_asym_vlan.c   |  25 +-
 drivers/net/datapath/dpm/gswip31/datapath_tx.c     |  25 +-
 include/net/datapath_api.h                         |  27 +-
 include/net/datapath_api_gswip30.h                 |   5 +-
 include/net/datapath_api_gswip31.h                 |   9 +-
 include/net/datapath_api_ppv4.h                    |   9 +-
 include/net/datapath_api_qos.h                     |  10 +-
 include/net/datapath_api_skb.h                     |   8 +-
 include/net/datapath_api_tx.h                      |   5 +
 include/net/datapath_api_umt.h                     |   3 +-
 include/net/datapath_api_vlan.h                    |   9 +-
 include/net/datapath_inst.h                        |   8 +-
 include/net/datapath_proc_api.h                    |   8 +-
 49 files changed, 1196 insertions(+), 784 deletions(-)

diff --git a/drivers/net/datapath/dpm/Makefile b/drivers/net/datapath/dpm/Makefile
index d7221ce73879..c27441c4d432 100644
--- a/drivers/net/datapath/dpm/Makefile
+++ b/drivers/net/datapath/dpm/Makefile
@@ -1,4 +1,4 @@
-obj-$(CONFIG_INTEL_DATAPATH) = datapath_api.o datapath_proc_api.o datapath_proc.o  datapath_misc.o datapath_notifier.o datapath_logical_dev.o datapath_instance.o datapath_soc.o datapath_qos.o datapath_proc_qos.o datapath_tx.o
+obj-$(CONFIG_INTEL_DATAPATH) = datapath_api.o datapath_proc_api.o datapath_proc.o  datapath_misc.o datapath_notifier.o datapath_logical_dev.o datapath_instance.o datapath_soc.o datapath_qos.o datapath_proc_qos.o datapath_tx.o datapath_cqm.o
 obj-$(CONFIG_INTEL_DATAPATH) += datapath_stats.o
 
 ifneq ($(CONFIG_INTEL_DATAPATH_HAL_GSWIP32),)
diff --git a/drivers/net/datapath/dpm/datapath.h b/drivers/net/datapath/dpm/datapath.h
index f3085affd085..6f35b6db032f 100644
--- a/drivers/net/datapath/dpm/datapath.h
+++ b/drivers/net/datapath/dpm/datapath.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_H
@@ -1085,5 +1081,25 @@ static inline bool is_dsl(struct pmac_port_info *port)
 {
 	return (port->alloc_flags & DP_F_FAST_DSL);
 }
+
+int dp_dealloc_cqm_port(struct module *owner, u32 dev_port,
+			struct pmac_port_info *port,
+			struct cbm_dp_alloc_data *data, u32 flags);
+
+int dp_alloc_cqm_port(struct module *owner, struct net_device *dev,
+		      u32 dev_port, struct pmac_port_info *port,
+		      struct cbm_dp_alloc_data *cbm_data, u32 flags);
+
+int dp_enable_cqm_port(struct module *owner, struct pmac_port_info *port,
+		       struct cbm_dp_en_data *data, u32 flags);
+
+int dp_cqm_port_alloc_complete(struct module *owner,
+			       struct pmac_port_info *port, s32 dp_port,
+			       struct cbm_dp_alloc_complete_data *data,
+			       u32 flags);
+
+int dp_cqm_deq_port_res_get(u32 dp_port, cbm_dq_port_res_t *res,
+			    u32 flags);
+
 #endif /*DATAPATH_H */
 
diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index 44dddcad3d1d..647517b4f2c2 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <net/datapath_api.h>
@@ -484,7 +480,7 @@ static int32_t dp_alloc_port_private(int inst,
 		port->txpush_addr_qos =	(void *)cbm_data.txpush_addr_qos;
 	}
 	if (cbm_data.flags & CBM_PORT_RING_SIZE_SET)
-	port->tx_ring_size = cbm_data.tx_ring_size;
+		port->tx_ring_size = cbm_data.tx_ring_size;
 	if (cbm_data.flags & CBM_PORT_RING_OFFSET_SET)
 		port->tx_ring_offset = cbm_data.tx_ring_offset;
 	if (cbm_data.num_dma_chan > 1 &&
@@ -538,7 +534,7 @@ int dp_cbm_deq_port_enable(struct module *owner, int inst, int port_id,
 
 	cqm_deq_port = deq_port;
 	dma_ch_ref = atomic_read(&(dp_dma_chan_tbl[inst] +
-				dma_ch_offset)->ref_cnt);
+				   dma_ch_offset)->ref_cnt);
 	cbm_data.deq_port = cqm_deq_port;
 	/* Enhance below condition later to support
 	 * both dp_register_subif & deq_update_info API
@@ -630,7 +626,7 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 	}
 
 	/*pr_info("search range: start=%d end=%d\n",start, end);*/
-    /*allocate a free subif */
+	/*allocate a free subif */
 	for (i = start; i < end; i++) {
 		sif = get_dp_port_subif(port_info, i);
 		if (sif->flags) /*used already & not free*/
@@ -680,6 +676,7 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 			STATS_SET(sif->rx_flag, !!data->rx_en_flag);
 		else
 			STATS_SET(sif->rx_flag, 1);
+
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
 		if (data->flag_ops & DP_SUBIF_SWDEV)
 			sif->swdev_en = data->swdev_en_flag;
@@ -713,22 +710,22 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 			for (j = 0; j < data->num_deq_port; j++) {
 				cqm_deq_port = sif->cqm_deq_port[j];
 				dma_ch_offset =
-				DP_DEQ(inst, cqm_deq_port).dma_ch_offset;
+					DP_DEQ(inst, cqm_deq_port).dma_ch_offset;
 				dma_ch_ref =
 					atomic_read(&(dp_dma_chan_tbl[inst] +
-						 dma_ch_offset)->ref_cnt);
+						      dma_ch_offset)->ref_cnt);
 				cbm_data.deq_port = port_info->deq_port_base +
-							data->deq_port_idx + j;
+						    data->deq_port_idx + j;
 				/* No need to enable DMA if multiple DEQ->single
 				 * DMA
 				 */
 				if (j != 0)
 					cbm_data.dma_chnl_init = 0;
 				else
-				/* PPA Directpath/LitePath don't have DMA CH */
+					/* PPA Directpath/LitePath don't have DMA CH */
 					if (dma_ch_ref_curr == 0 &&
 					    !(port_info->alloc_flags &
-					    DP_F_DIRECT))
+					      DP_F_DIRECT))
 						/*to enable DMA*/
 						cbm_data.dma_chnl_init = 1;
 				DP_DEBUG(DP_DBG_FLAG_REG,
@@ -907,15 +904,11 @@ int32_t dp_deregister_subif_private(int inst, struct module *owner,
 		/* PPA Directpath/LitePath don't have DMA CH */
 		if (!is_directpath(port_info) && !dma_ch_ref)
 			cbm_data.dma_chnl_init = 1;
-		if (cbm_dp_enable(owner, port_id, &cbm_data,
-				  CBM_PORT_F_DISABLE,
-				  port_info->alloc_flags)) {
-			DP_DEBUG(DP_DBG_FLAG_REG,
-				 "cbm_dp_disable fail:port=%d subix=%d %s=%d\n",
-				 port_id, i, "dma_chnl_init",
-				 cbm_data.dma_chnl_init);
-			return res;
-		}
+
+		if (dp_enable_cqm_port(owner, port_info, &cbm_data,
+				       CBM_PORT_F_DISABLE))
+			return DP_FAILURE;
+
 		DP_DEBUG(DP_DBG_FLAG_REG,
 			 "cbm_dp_enable ok:port=%d subix=%d cqm_port=%d\n",
 			 port_id, i, cqm_port);
@@ -1068,15 +1061,15 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 				bp_info->flag = 1;
 				notif_data.dev = info->new_dev;
 				atomic_notifier_call_chain(
-						&dp_evt_notif_list,
-						DP_EVENT_REGISTER_SUBIF,
-						&notif_data);
+					&dp_evt_notif_list,
+					DP_EVENT_REGISTER_SUBIF,
+					&notif_data);
 				/* call notifier for ctp_dev */
 				notif_data.dev = info->new_ctp_dev;
 				atomic_notifier_call_chain(
-						&dp_evt_notif_list,
-						DP_EVENT_REGISTER_SUBIF,
-						&notif_data);
+					&dp_evt_notif_list,
+					DP_EVENT_REGISTER_SUBIF,
+					&notif_data);
 			}
 			dp_inst_add_dev(info->new_dev, NULL, info->inst,
 					info->dp_port, bp, vap, 0);
@@ -1096,9 +1089,9 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 				bp_info->ref_cnt++;
 				notif_data.dev = info->new_ctp_dev;
 				atomic_notifier_call_chain(
-						&dp_evt_notif_list,
-						DP_EVENT_REGISTER_SUBIF,
-						&notif_data);
+					&dp_evt_notif_list,
+					DP_EVENT_REGISTER_SUBIF,
+					&notif_data);
 				DP_DEBUG(DP_DBG_FLAG_REG,
 					 "update bp refcnt:%d\n",
 					 bp_info->ref_cnt);
@@ -1427,14 +1420,9 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, u32 port_id,
 		return res;
 	}
 
-	/*register a device */
-	if (cbm_dp_port_alloc_complete(owner,
-				       port_info->dev,
-				       port_info->dev_port,
-				       port_id,
-				       cbm_data,
-				       port_info->alloc_flags)) {
-		DP_DEBUG(DP_DBG_FLAG_REG, "CBM port alloc failed\n");
+	/* Register device to CQM */
+	if (dp_cqm_port_alloc_complete(owner, port_info, port_id, cbm_data,
+				       flags)) {
 		devm_kfree(&g_dp_dev->dev, cbm_data);
 		DP_LIB_UNLOCK(&dp_lock);
 		return DP_FAILURE;
@@ -2453,7 +2441,7 @@ int dp_lan_wan_bridging(int port_id, struct sk_buff *skb)
 
 	((struct dma_tx_desc_1 *)&skb->DW1)->field.ep = subif.port_id;
 	((struct dma_tx_desc_0 *)&skb->DW0)->field.dest_sub_if_id =
-	    subif.subif;
+								subif.subif;
 
 	dp_xmit(dev, &subif, skb, skb->len, 0);
 	return DP_SUCCESS;
@@ -2574,7 +2562,7 @@ int dp_vlan_set(struct dp_tc_vlan *vlan, int flags)
 	DP_DEBUG(DP_DBG_FLAG_PAE, "dev_type:0x%x\n", info.dev_type);
 	if (DP_CB(subif.inst, dp_tc_vlan_set))
 		return DP_CB(subif.inst, dp_tc_vlan_set)
-			    (dp_port_prop[subif.inst].ops[0],
+			     (dp_port_prop[subif.inst].ops[0],
 			     vlan, &info, flags);
 	return DP_FAILURE;
 }
diff --git a/drivers/net/datapath/dpm/datapath_cqm.c b/drivers/net/datapath/dpm/datapath_cqm.c
new file mode 100644
index 000000000000..548bb357585e
--- /dev/null
+++ b/drivers/net/datapath/dpm/datapath_cqm.c
@@ -0,0 +1,339 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
+#include <linux/kernel.h>
+#include <linux/types.h>
+#include <linux/etherdevice.h>
+#include <net/datapath_api.h>
+#include "datapath.h"
+
+void print_cbm_alloc_data(struct cbm_dp_alloc_data *data)
+{
+	pr_info("===========================\n");
+	pr_info("dp_inst                = %d\n", data->dp_inst);
+	pr_info("cbm_inst               = %d\n", data->cbm_inst);
+	pr_info("flags                  = %d\n", data->flags);
+	pr_info("dp_port                = %d\n", data->dp_port);
+	pr_info("deq_port_num           = %d\n", data->deq_port_num);
+	pr_info("deq_port               = %d\n", data->deq_port);
+	pr_info("dma_chan               = %08x\n", data->dma_chan);
+	pr_info("tx_pkt_credit          = %d\n", data->tx_pkt_credit);
+	pr_info("tx_b_credit            = %d\n", data->tx_b_credit);
+	pr_info("txpush_addr_qos        = %p\n", (void *)data->txpush_addr_qos);
+	pr_info("txpush_addr            = %p\n", (void *)data->txpush_addr);
+	pr_info("tx_ring_size           = %d\n", data->tx_ring_size);
+	pr_info("tx_ring_offset         = %d\n", data->tx_ring_offset);
+	pr_info("num_dma_chan           = %d\n", data->num_dma_chan);
+	pr_info("\n\n");
+}
+
+void print_cbm_en_data(struct cbm_dp_en_data *data)
+{
+	pr_info("===========================\n");
+	pr_info("dp_inst                = %d\n", data->dp_inst);
+	pr_info("cbm_inst               = %d\n", data->cbm_inst);
+	pr_info("deq_port               = %d\n", data->deq_port);
+	pr_info("dma_chnl_init          = %d\n", data->dma_chnl_init);
+	pr_info("tx_ring_size           = %d\n", data->tx_ring_size);
+
+	pr_info("num_dma_chan           = %d\n", data->num_dma_chan);
+	pr_info("\n\n");
+}
+
+int dump_ring_info(struct cbm_dp_alloc_complete_data *data)
+{
+	u8 cid, pid;
+	int i = 0;
+	u16 nid;
+
+	for (i = 0; i < data->num_tx_ring; i++) {
+		pr_info("    DC TxRing:         %d\n", i);
+
+		pr_info("      TXIN  DeqRingSize/paddr:     %d/0x%px\n",
+			data->tx_ring[i].in_deq_ring_size,
+			data->tx_ring[i].in_deq_paddr);
+		pr_info("      TXOUT FreeRingSize/paddr:    %d/0x%px\n",
+			data->tx_ring[i].out_free_ring_size,
+			data->tx_ring[i].out_free_paddr);
+		pr_info("      TXOUT PolicyBase/Poolid:     %d/%d\n",
+			data->tx_ring[i].txout_policy_base,
+			data->tx_ring[i].tx_poolid);
+		pr_info("      PolicyNum:                   %d\n",
+			data->tx_ring[i].policy_num);
+		pr_info("      NumOfTxPkt/TxPktSize:        %d/%d\n",
+			data->tx_ring[i].num_tx_pkt,
+			data->tx_ring[i].tx_pkt_size);
+	}
+
+	for (i = 0; i < data->num_rx_ring; i++) {
+		pr_info("    DC RxRing:         %d\n", i);
+
+		pr_info("      %s:  %d/0x%px/%d\n",
+			"RXOUT EnqRingSize/paddr/pid",
+			data->rx_ring[i].out_enq_ring_size,
+			data->rx_ring[i].out_enq_paddr,
+			data->rx_ring[i].out_enq_port_id);
+		pr_info("      %s:             %d\n",
+			"RXOUT NumOfDmaCh",
+			data->rx_ring[i].num_out_tx_dma_ch);
+		dp_dma_parse_id(data->rx_ring[i].out_dma_ch_to_gswip,
+				&cid, &pid, &nid);
+		pr_info("      %s:     %d/%d/%d\n",
+			"RXOUT dma-ctrl/port/chan",
+			cid, pid, nid);
+		pr_info("      RXOUT NumOfCqmDqPort/pid:     %d/%d\n",
+			data->rx_ring[i].num_out_cqm_deq_port,
+			data->rx_ring[i].out_cqm_deq_port_id);
+		pr_info("      %s:  %d/0x%px\n",
+			"RXIN  InAllocRingSize/Paddr",
+			data->rx_ring[i].in_alloc_ring_size,
+			data->rx_ring[i].in_alloc_paddr);
+		pr_info("      %s:    %d/%d/%d\n",
+			"NumPkt/Pktsize/Policybase",
+			data->rx_ring[i].num_pkt,
+			data->rx_ring[i].rx_pkt_size,
+			data->rx_ring[i].rx_policy_base);
+		pr_info("      %s:        %d/0x%px\n",
+			"PreFillPktNum/PktBase",
+			data->rx_ring[i].prefill_pkt_num,
+			data->rx_ring[i].pkt_base_paddr);
+	}
+
+	return 0;
+}
+
+void print_cqm_alloc_comp_data(struct cbm_dp_alloc_complete_data *data)
+{
+	pr_info("===========================\n");
+	pr_info("num_rx_ring                = %d\n", data->num_rx_ring);
+	pr_info("num_tx_ring                = %d\n", data->num_tx_ring);
+	pr_info("num_umt_port               = %d\n", data->num_umt_port);
+	pr_info("enable_cqm_meta            = %d\n", data->enable_cqm_meta);
+	pr_info("alloc_flags                = %d\n", data->alloc_flags);
+	pr_info("deq_port                   = %d\n", data->deq_port);
+	pr_info("qid_base                   = %d\n", data->qid_base);
+	pr_info("num_qid                    = %d\n", data->num_qid);
+	pr_info("\n\n");
+	dump_ring_info(data);
+	pr_info("\n\n");
+}
+
+void print_cqm_deq_res(cbm_dq_port_res_t *res)
+{
+	int i = 0;
+	cbm_dq_info_t	*deq_info;
+
+	pr_info("===========================\n");
+	pr_info("cbm_buf_free_base      = %px\n", res->cbm_buf_free_base);
+	pr_info("num_free_entries       = %d\n", res->num_free_entries);
+	pr_info("num_deq_ports          = %d\n", res->num_deq_ports);
+
+	for (i = 0; i < res->num_deq_ports; i++) {
+		deq_info = &res->deq_info[i];
+		pr_info("deq port               = %d\n", i);
+		pr_info("port_no                = %d\n", deq_info->port_no);
+		pr_info("cbm_dq_port_base       = %px\n",
+			deq_info->cbm_dq_port_base);
+		pr_info("dma_tx_chan            = %d\n", deq_info->dma_tx_chan);
+		pr_info("num_desc               = %d\n", deq_info->num_desc);
+		pr_info("num_free_burst         = %d\n",
+			deq_info->num_free_burst);
+	}
+	pr_info("\n\n");
+}
+
+int dp_dealloc_cqm_port(struct module *owner, u32 dev_port,
+			struct pmac_port_info *port,
+			struct cbm_dp_alloc_data *data, u32 flags)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG) {
+		pr_info("%s : %s :%px %s :%d %s :%d %s :%d\n",
+			__func__,
+			"owner", owner,
+			"dev_port", dev_port,
+			"cbm_port_id", port->port_id,
+			"flags", flags | DP_F_DEREGISTER);
+		print_cbm_alloc_data(data);
+	}
+#endif
+
+	if (cbm_dp_port_dealloc(owner, dev_port, port->port_id, data,
+				flags | DP_F_DEREGISTER)) {
+		pr_err("%s : %s :%px %s :%d %s :%d %s :%d\n",
+		       "cbm_dp_port_dealloc Failed",
+		       "owner", owner,
+		       "dev_port", dev_port,
+		       "cbm_port_id", port->port_id,
+		       "flags", flags | DP_F_DEREGISTER);
+		print_cbm_alloc_data(data);
+		return DP_FAILURE;
+	}
+
+	/* Only clear those fields we need to clear */
+	memset(port, 0, offsetof(struct pmac_port_info, tail));
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG)
+		pr_info("%s : Successful\n", __func__);
+#endif
+
+	return DP_SUCCESS;
+}
+
+int dp_alloc_cqm_port(struct module *owner, struct net_device *dev,
+		      u32 dev_port, struct pmac_port_info *port,
+		      struct cbm_dp_alloc_data *cbm_data, u32 flags)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG) {
+		pr_info("%s : %s :%px %s :%s %s :%d %s :%d %s :%d\n",
+			__func__,
+			"owner", owner,
+			"dev_name", dev ? dev->name : "NULL",
+			"dev_port", dev_port,
+			"cbm_port_id", port->port_id,
+			"flags", flags);
+		print_cbm_alloc_data(cbm_data);
+	}
+#endif
+
+	if (cbm_dp_port_alloc(owner, dev, dev_port, port->port_id, cbm_data,
+			      flags)) {
+		pr_info("%s : %s :%px %s :%s %s :%d %s :%d %s :%d\n",
+			__func__,
+			"owner", owner,
+			"dev_name", dev ? dev->name : "NULL",
+			"dev_port", dev_port,
+			"cbm_port_id", port->port_id,
+			"flags", flags);
+		print_cbm_alloc_data(cbm_data);
+		return DP_FAILURE;
+	}
+
+	if (!(cbm_data->flags & CBM_PORT_DP_SET) &&
+	    !(cbm_data->flags & CBM_PORT_DQ_SET)) {
+		pr_err("%s NO DP_SET/DQ_SET(%x):%s/dev_port %d\n",
+		       "cbm_dp_port_alloc",
+		       cbm_data->flags,
+		       owner->name, dev_port);
+		return DP_FAILURE;
+	}
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG) {
+		print_cbm_alloc_data(cbm_data);
+		pr_info("%s : Successful\n", __func__);
+	}
+#endif
+
+	return DP_SUCCESS;
+}
+
+int dp_enable_cqm_port(struct module *owner, struct pmac_port_info *port,
+		       struct cbm_dp_en_data *data, u32 flags)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG) {
+		pr_info("%s : %s :%px %s :%d %s :%d %s :%d\n",
+			__func__,
+			"owner", owner,
+			"dp_port", port->port_id,
+			"flags", flags,
+			"alloc_flags", port->alloc_flags);
+		print_cbm_en_data(data);
+	}
+#endif
+
+	if (cbm_dp_enable(owner, port->port_id, data, flags,
+			  port->alloc_flags)) {
+		pr_err("%s : %s :%px %s :%d %s :%d %s :%d\n",
+		       "cbm_dp_enable Failed",
+		       "owner", owner,
+		       "dp_port", port->port_id,
+		       "flags", flags,
+		       "alloc_flags", port->alloc_flags);
+		return DP_FAILURE;
+	}
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG)
+		pr_info("%s : Successful\n", __func__);
+#endif
+
+	return DP_SUCCESS;
+}
+
+int dp_cqm_port_alloc_complete(struct module *owner,
+			       struct pmac_port_info *port, s32 dp_port,
+			       struct cbm_dp_alloc_complete_data *data,
+			       u32 flags)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG) {
+		pr_info("%s : %s :%px %s :%s %s :%d %s :%d %s :%d\n",
+			__func__,
+			"owner", owner,
+			"dev_name", port->dev ? port->dev->name : "NULL",
+			"dev_port", port->dev_port,
+			"dp_port", dp_port,
+			"alloc_flags", port->alloc_flags);
+		print_cqm_alloc_comp_data(data);
+	}
+#endif
+
+	if (cbm_dp_port_alloc_complete(owner,
+				       port->dev,
+				       port->dev_port,
+				       dp_port,
+				       data,
+				       port->alloc_flags | flags)) {
+		pr_err("%s : %s :%px %s :%s %s :%d %s :%d %s :%d\n",
+		       "cbm_dp_port_alloc_complete Failed",
+		       "owner", owner,
+		       "dev_name", port->dev ? port->dev->name : "NULL",
+		       "dev_port", port->dev_port,
+		       "dp_port", dp_port,
+		       "alloc_flags", port->alloc_flags);
+		print_cqm_alloc_comp_data(data);
+
+		return DP_FAILURE;
+	}
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG) {
+		print_cqm_alloc_comp_data(data);
+		pr_info("%s : Successful\n", __func__);
+	}
+#endif
+
+	return DP_SUCCESS;
+}
+
+int dp_cqm_deq_port_res_get(u32 dp_port, cbm_dq_port_res_t *res,
+			    u32 flags)
+{
+	if (cbm_dequeue_port_resources_get(dp_port, res, flags)) {
+		pr_err("%s :%s :%d %s :%d\n",
+		       "cbm_dequeue_port_resources_get Failed",
+		       "dp_port", dp_port,
+		       "flags", flags);
+		print_cqm_deq_res(res);
+		return DP_FAILURE;
+	}
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG)
+		print_cqm_deq_res(res);
+#endif
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_REG)
+		pr_info("%s : Successful\n", __func__);
+#endif
+
+	return DP_SUCCESS;
+}
+
diff --git a/drivers/net/datapath/dpm/datapath_instance.c b/drivers/net/datapath/dpm/datapath_instance.c
index d7134bb604f4..84d6e19a4b5c 100644
--- a/drivers/net/datapath/dpm/datapath_instance.c
+++ b/drivers/net/datapath/dpm/datapath_instance.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/types.h>
@@ -297,15 +293,16 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 				return DP_FAILURE;
 		}
 #endif
-	vap = GET_VAP(subif->subif, port_info->vap_offset, port_info->vap_mask);
-	sif = get_dp_port_subif(port_info, vap);
+		vap = GET_VAP(subif->subif, port_info->vap_offset,
+			      port_info->vap_mask);
+		sif = get_dp_port_subif(port_info, vap);
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-	if ((!(flag & DP_F_SUBIF_LOGICAL)) &&
-	    (sif->swdev_en == 1))
-		dp_register_switchdev_ops(dp_dev, dev);
+		if ((!(flag & DP_F_SUBIF_LOGICAL)) &&
+		    (sif->swdev_en == 1))
+			dp_register_switchdev_ops(dp_dev, dev);
 #endif
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_PTP1588)
-	dp_register_ptp_ioctl(dp_dev, dev, inst);
+		dp_register_ptp_ioctl(dp_dev, dev, inst);
 #endif
 	}
 	dp_dev->count++;
diff --git a/drivers/net/datapath/dpm/datapath_instance.h b/drivers/net/datapath/dpm/datapath_instance.h
index 06574471b78d..b863d013623f 100644
--- a/drivers/net/datapath/dpm/datapath_instance.h
+++ b/drivers/net/datapath/dpm/datapath_instance.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_INSTANCE_H
diff --git a/drivers/net/datapath/dpm/datapath_ioctl.c b/drivers/net/datapath/dpm/datapath_ioctl.c
index 2d25f39dc485..030208642a96 100644
--- a/drivers/net/datapath/dpm/datapath_ioctl.c
+++ b/drivers/net/datapath/dpm/datapath_ioctl.c
@@ -1,3 +1,8 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
 #include "datapath_ioctl.h"
 
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_PTP1588)
diff --git a/drivers/net/datapath/dpm/datapath_ioctl.h b/drivers/net/datapath/dpm/datapath_ioctl.h
index 1935dffc89dc..c60f30592a1e 100644
--- a/drivers/net/datapath/dpm/datapath_ioctl.h
+++ b/drivers/net/datapath/dpm/datapath_ioctl.h
@@ -1,3 +1,8 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
 #ifndef DATAPATH_IOCTL_H
 #define DATAPATH_IOCTL_H
 
diff --git a/drivers/net/datapath/dpm/datapath_logical_dev.c b/drivers/net/datapath/dpm/datapath_logical_dev.c
index 000f7fe5039b..04a787ada418 100644
--- a/drivers/net/datapath/dpm/datapath_logical_dev.c
+++ b/drivers/net/datapath/dpm/datapath_logical_dev.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/types.h>
diff --git a/drivers/net/datapath/dpm/datapath_misc.c b/drivers/net/datapath/dpm/datapath_misc.c
index 1046879ca48f..6d52c9f95046 100644
--- a/drivers/net/datapath/dpm/datapath_misc.c
+++ b/drivers/net/datapath/dpm/datapath_misc.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/types.h>
@@ -321,7 +317,7 @@ int get_ip_hdr_info(u8 *pdata, int len, struct ip_hdr_info *info)
 
 				if (!first_extension)
 					udp_tcp_h_offset +=
-					    IPV6_EXTENSION_SIZE + p[1];
+						IPV6_EXTENSION_SIZE + p[1];
 
 				info->udp_tcp_offset = udp_tcp_h_offset;
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG_PROTOCOL_PARSE)
@@ -338,7 +334,7 @@ int get_ip_hdr_info(u8 *pdata, int len, struct ip_hdr_info *info)
 			} else if (next_hdr == PROTOCOL_IPIP) {	/*dslite */
 				if (!first_extension)
 					udp_tcp_h_offset +=
-					    IPV6_EXTENSION_SIZE + p[1];
+						IPV6_EXTENSION_SIZE + p[1];
 
 				info->next_ip_hdr_offset = udp_tcp_h_offset;
 				return 0;
@@ -362,7 +358,7 @@ int get_ip_hdr_info(u8 *pdata, int len, struct ip_hdr_info *info)
 			} else {
 				/*TO NEXT */
 				udp_tcp_h_offset +=
-				    IPV6_EXTENSION_SIZE + p[1];
+					IPV6_EXTENSION_SIZE + p[1];
 				p += IPV6_EXTENSION_SIZE + p[1];
 			}
 			next_hdr = p[0];
@@ -436,7 +432,7 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 	while (1) {
 		if (get_ip_hdr_info(p, len, &pkt_info[ip_num]) == 0) {
 			pkt_info[ip_num].ip_offset = (uintptr_t)p -
-				(unsigned long)p_l2_mac;
+						     (unsigned long)p_l2_mac;
 
 			if (pkt_info[ip_num].next_ip_hdr_offset) {
 				p += pkt_info[ip_num].next_ip_hdr_offset;
@@ -459,7 +455,7 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 		} else {
 			/*Not UDP/TCP and cannot do checksum calculation */
 			pr_info_once
-			    ("Not UDP/TCP and cannot do checksum cal!\n");
+			("Not UDP/TCP and cannot do checksum cal!\n");
 			return -1;
 		}
 	}
@@ -485,20 +481,20 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 				/*clear original udp checksum */
 				if (!pkt_info[0].is_fragment)
 					*(uint16_t *)(p_l2_mac + *ip_offset +
-						       *tcp_h_offset +
-						       UDP_CHKSUM_OFFSET) = 0;
+						      *tcp_h_offset +
+						      UDP_CHKSUM_OFFSET) = 0;
 			} else {
 				*tcp_type = TCP_OVER_IPV4;
 				/*clear original TCP checksum */
 				*(uint16_t *)(p_l2_mac + *ip_offset +
-					       *tcp_h_offset +
-					       TCP_CHKSUM_OFFSET) = 0;
+					      *tcp_h_offset +
+					      TCP_CHKSUM_OFFSET) = 0;
 			}
 
 			if (!pkt_info[0].is_fragment) {
 				/*clear original ip4 checksum */
 				*(uint16_t *)(p_l2_mac + *ip_offset +
-					       IP_CHKSUM_OFFSET_IPV4) = 0;
+					      IP_CHKSUM_OFFSET_IPV4) = 0;
 			} else {
 				return 1;
 			}
@@ -513,15 +509,15 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 				if (!pkt_info[0].is_fragment)
 					/*clear original udp checksum */
 					*(uint16_t *)(p_l2_mac + *ip_offset +
-						       *tcp_h_offset +
-						       UDP_CHKSUM_OFFSET) = 0;
+						      *tcp_h_offset +
+						      UDP_CHKSUM_OFFSET) = 0;
 			} else {
 				*tcp_type = TCP_OVER_IPV6;
 				/*clear original TCP checksum */
 				if (!pkt_info[0].is_fragment) {
 					*(uint16_t *)(p_l2_mac + *ip_offset +
-						       *tcp_h_offset +
-						       TCP_CHKSUM_OFFSET) = 0;
+						      *tcp_h_offset +
+						      TCP_CHKSUM_OFFSET) = 0;
 				} else {
 					return 1;
 				}
@@ -536,28 +532,28 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 			/*6rd */
 			*ip_offset = pkt_info[0].ip_offset;
 			*tcp_h_offset =
-			    (pkt_info[0].next_ip_hdr_offset +
-			     pkt_info[1].udp_tcp_offset);
+				(pkt_info[0].next_ip_hdr_offset +
+				 pkt_info[1].udp_tcp_offset);
 
 			if (pkt_info[1].proto == PROTOCOL_UDP) {
 				*tcp_type = UDP_OVER_IPV6_IPV4;
 				/*clear original udp checksum */
 				if (!pkt_info[0].is_fragment)
 					*(uint16_t *)(p_l2_mac + *ip_offset +
-						       *tcp_h_offset +
-						       UDP_CHKSUM_OFFSET) = 0;
+						      *tcp_h_offset +
+						      UDP_CHKSUM_OFFSET) = 0;
 			} else {
 				*tcp_type = TCP_OVER_IPV6_IPV4;
 				/*clear original udp checksum */
 				*(uint16_t *)(p_l2_mac + *ip_offset +
-					       *tcp_h_offset +
-					       TCP_CHKSUM_OFFSET) = 0;
+					      *tcp_h_offset +
+					      TCP_CHKSUM_OFFSET) = 0;
 			}
 
 			if (!pkt_info[0].is_fragment) {
 				/*clear original ip4 checksum */
 				*(uint16_t *)(p_l2_mac + *ip_offset +
-					       IP_CHKSUM_OFFSET_IPV4) = 0;
+					      IP_CHKSUM_OFFSET_IPV4) = 0;
 			} else {
 				return 1;
 			}
@@ -565,34 +561,34 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 			return 0;
 
 		} else if ((pkt_info[0].ip_ver == DP_IP_VER6) &&
-			(pkt_info[1].ip_ver == DP_IP_VER4)) {	/*dslite */
+			   (pkt_info[1].ip_ver == DP_IP_VER4)) {/*dslite */
 			*ip_offset = pkt_info[0].ip_offset;
 			*tcp_h_offset =
-			    (pkt_info[0].next_ip_hdr_offset +
-			     pkt_info[1].udp_tcp_offset);
+				(pkt_info[0].next_ip_hdr_offset +
+				 pkt_info[1].udp_tcp_offset);
 
 			if (pkt_info[1].proto == PROTOCOL_UDP) {
 				*tcp_type = UDP_OVER_IPV4_IPV6;
 				if (!pkt_info[0].is_fragment)
 					/*clear original udp checksum */
 					*(uint16_t *)(p_l2_mac +
-						       pkt_info[1].ip_offset +
-						       *tcp_h_offset +
-						       UDP_CHKSUM_OFFSET) = 0;
+						      pkt_info[1].ip_offset +
+						      *tcp_h_offset +
+						      UDP_CHKSUM_OFFSET) = 0;
 			} else {
 				*tcp_type = TCP_OVER_IPV4_IPV6;
 				/*clear original udp checksum */
 				*(uint16_t *)(p_l2_mac +
-					       pkt_info[1].ip_offset +
-					       pkt_info[1].udp_tcp_offset +
-					       TCP_CHKSUM_OFFSET) = 0;
+					      pkt_info[1].ip_offset +
+					      pkt_info[1].udp_tcp_offset +
+					      TCP_CHKSUM_OFFSET) = 0;
 			}
 
 			if (!pkt_info[0].is_fragment) {
 				/*clear original ip4 checksum */
 				*(uint16_t *)(p_l2_mac +
-					       pkt_info[1].ip_offset +
-					       IP_CHKSUM_OFFSET_IPV4) = 0;
+					      pkt_info[1].ip_offset +
+					      IP_CHKSUM_OFFSET_IPV4) = 0;
 			} else {
 				return 1;
 			}
@@ -631,17 +627,17 @@ int get_offset_clear_chksum(struct sk_buff *skb, u32 *ip_offset,
 	if (skb->encapsulation) {
 		iph = (struct iphdr *)skb_inner_network_header(skb);
 		*ip_offset =
-		    (uint32_t)(skb_inner_network_header(skb) - skb->data);
+			(uint32_t)(skb_inner_network_header(skb) - skb->data);
 		*tcp_h_offset =
-		    (uint32_t)(skb_inner_transport_header(skb) -
-				skb_inner_network_header(skb));
+			(uint32_t)(skb_inner_transport_header(skb) -
+				   skb_inner_network_header(skb));
 		l4_p = skb_inner_transport_header(skb);
 	} else {
 		iph = (struct iphdr *)skb_network_header(skb);
 		*ip_offset = (uint32_t)(skb_network_header(skb) - skb->data);
 		*tcp_h_offset =
-		    (uint32_t)(skb_transport_header(skb) -
-				skb_network_header(skb));
+			(uint32_t)(skb_transport_header(skb) -
+				   skb_network_header(skb));
 		l4_p = skb_transport_header(skb);
 	}
 	if (((int)(ip_offset) <= 0) || ((int)(tcp_h_offset) <= 0)) {
@@ -849,7 +845,7 @@ u32 dp_subif_hash(struct net_device *dev)
 	index = (unsigned long)dev;
 	/*Note: it is 4K alignment. Need tune later */
 	return (u32)((index >>
-			DP_SUBIF_LIST_HASH_SHIFT) % DP_SUBIF_LIST_HASH_SIZE);
+		      DP_SUBIF_LIST_HASH_SHIFT) % DP_SUBIF_LIST_HASH_SIZE);
 }
 
 int dp_subif_list_init(void)
@@ -1071,13 +1067,13 @@ int dp_cpufreq_notify_init(int inst)
 {
 	if (cpufreq_register_notifier
 	    (&dp_coc_cpufreq_transition_notifier_block,
-	    CPUFREQ_TRANSITION_NOTIFIER)) {
+	     CPUFREQ_TRANSITION_NOTIFIER)) {
 		pr_err("cpufreq transiiton register_notifier failed?\n");
 		return -1;
 	}
 	if (cpufreq_register_notifier
 	    (&dp_coc_cpufreq_policy_notifier_block,
-	    CPUFREQ_POLICY_NOTIFIER)) {
+	     CPUFREQ_POLICY_NOTIFIER)) {
 		pr_err("cpufreq policy register_notifier failed?\n");
 		return -1;
 	}
@@ -1088,13 +1084,13 @@ int dp_cpufreq_notify_exit(void)
 {
 	if (cpufreq_unregister_notifier
 	    (&dp_coc_cpufreq_transition_notifier_block,
-	    CPUFREQ_TRANSITION_NOTIFIER)) {
+	     CPUFREQ_TRANSITION_NOTIFIER)) {
 		pr_err("cpufreq transition unregister_notifier failed?\n");
 		return -1;
 	}
 	if (cpufreq_unregister_notifier
 	    (&dp_coc_cpufreq_policy_notifier_block,
-	    CPUFREQ_POLICY_NOTIFIER)) {
+	     CPUFREQ_POLICY_NOTIFIER)) {
 		pr_err("cpufreq policy unregister_notifier failed?\n");
 		return -1;
 	}
@@ -1123,7 +1119,7 @@ u32 get_dma_chan_idx(int inst, int num_dma_chan)
 		for (match = 0; (match < num_dma_chan) &&
 		     ((base + match) < DP_MAX_DMA_CHAN); match++) {
 			if (atomic_read(&(dp_dma_chan_tbl[inst] +
-					(base + match))->ref_cnt))
+					  (base + match))->ref_cnt))
 				break;
 		}
 		if (match == num_dma_chan)
@@ -1142,7 +1138,7 @@ u32 alloc_dma_chan_tbl(int inst)
 {
 	dp_dma_chan_tbl[inst] = devm_kzalloc(&g_dp_dev->dev,
 					     (sizeof(struct dma_chan_info) *
-					     DP_MAX_DMA_CHAN), GFP_ATOMIC);
+					      DP_MAX_DMA_CHAN), GFP_ATOMIC);
 	if (!dp_dma_chan_tbl[inst])
 		return DP_FAILURE;
 	return DP_SUCCESS;
@@ -1170,7 +1166,7 @@ u32 alloc_dp_port_subif_info(int inst)
 
 	dp_port_info[inst] = devm_kzalloc(&g_dp_dev->dev,
 					  (sizeof(struct pmac_port_info) *
-					  max_dp_ports), GFP_ATOMIC);
+					   max_dp_ports), GFP_ATOMIC);
 	if (!dp_port_info[inst])
 		return DP_FAILURE;
 	for (port_id = 0; port_id < max_dp_ports; port_id++) {
diff --git a/drivers/net/datapath/dpm/datapath_notifier.c b/drivers/net/datapath/dpm/datapath_notifier.c
index 2c2e9391ac99..3ea8c3a30a8f 100644
--- a/drivers/net/datapath/dpm/datapath_notifier.c
+++ b/drivers/net/datapath/dpm/datapath_notifier.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/module.h>
diff --git a/drivers/net/datapath/dpm/datapath_proc.c b/drivers/net/datapath/dpm/datapath_proc.c
index a0a14b0c4956..463352032230 100644
--- a/drivers/net/datapath/dpm/datapath_proc.c
+++ b/drivers/net/datapath/dpm/datapath_proc.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/module.h>
@@ -58,8 +54,9 @@ int proc_port_init(void)
 
 int dump_dc_info(struct seq_file *s, struct pmac_port_info *port)
 {
+	u8 cid, pid;
 	int i = 0;
-	u32 cid, pid, nid;
+	u16 nid;
 
 	for (i = 0; i < port->num_tx_ring; i++) {
 		seq_printf(s, "    DC TxRing:         %d\n", i);
@@ -87,9 +84,8 @@ int dump_dc_info(struct seq_file *s, struct pmac_port_info *port)
 			   port->rx_ring[i].out_enq_port_id);
 		seq_printf(s, "      RXOUT NumOfDmaCh:             %d\n",
 			   port->rx_ring[i].num_out_tx_dma_ch);
-		cid = _DMA_CONTROLLER(port->rx_ring[i].out_dma_ch_to_gswip);
-		pid = _DMA_PORT(port->rx_ring[i].out_dma_ch_to_gswip);
-		nid = _DMA_CHANNEL(port->rx_ring[i].out_dma_ch_to_gswip);
+		dp_dma_parse_id(port->rx_ring[i].out_dma_ch_to_gswip, &cid,
+				&pid, &nid);
 		seq_printf(s, "      RXOUT dma-ctrl/port/chan:     %d/%d/%d\n",
 			   cid, pid, nid);
 		seq_printf(s, "      RXOUT NumOfCqmDqPort/pid:     %d/%d\n",
@@ -213,7 +209,8 @@ int proc_port_dump(struct seq_file *s, int pos)
 	struct dp_subif_info *sif;
 	struct pmac_port_info *port = get_dp_port_info(tmp_inst, pos);
 	u16 start = 0;
-	u32 cid, pid, nid;
+	u8 cid, pid;
+	u16 nid;
 	int loop;
 	struct inst_info *info = NULL;
 	struct dma_chan_info *dma;
@@ -363,9 +360,7 @@ int proc_port_dump(struct seq_file *s, int pos)
 			cqm = get_dp_deqport_info(tmp_inst, cqm_p);
 			seq_printf(s, "%18sport/node:   %d/%d(ref=%d)\n", "",
 				   cqm_p, sif->qos_deq_port[j], cqm->ref_cnt);
-			cid = _DMA_CONTROLLER(cqm->dma_chan);
-			pid = _DMA_PORT(cqm->dma_chan);
-			nid = _DMA_CHANNEL(cqm->dma_chan);
+			dp_dma_parse_id(cqm->dma_chan, &cid, &pid, &nid);
 			dma = dp_dma_chan_tbl[tmp_inst];
 			if (port->num_dma_chan && dma) {
 				dma += cqm->dma_ch_offset;
diff --git a/drivers/net/datapath/dpm/datapath_proc_api.c b/drivers/net/datapath/dpm/datapath_proc_api.c
index d7f9637739a3..cbbf9e951f4e 100644
--- a/drivers/net/datapath/dpm/datapath_proc_api.c
+++ b/drivers/net/datapath/dpm/datapath_proc_api.c
@@ -1,14 +1,10 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/fs.h>
-#include<linux/slab.h>
+#include <linux/slab.h>
 #include <linux/kernel.h>
 #include <net/datapath_proc_api.h>
 #include <net/datapath_api.h>
diff --git a/drivers/net/datapath/dpm/datapath_proc_qos.c b/drivers/net/datapath/dpm/datapath_proc_qos.c
index 6cd2cab990e9..83ed5c759782 100644
--- a/drivers/net/datapath/dpm/datapath_proc_qos.c
+++ b/drivers/net/datapath/dpm/datapath_proc_qos.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <net/datapath_api.h>
@@ -186,7 +182,7 @@ void set_child_per_box(struct q_print_info *q_info)
 #define INFO_SIZE_PER_BOX   2 /* for box info, like node id and others */
 #define SUFIX_SIZE_PER_BOX  1 /* for closing ---- */
 #define EXTRA_SIZE_PER_BOX (PREFIX_SIZE_PER_BOX + INFO_SIZE_PER_BOX + \
-				SUFIX_SIZE_PER_BOX)
+			    SUFIX_SIZE_PER_BOX)
 #define PADDING_BETWEEN_BOX_X 2 /* axis x */
 #define PADDING_BETWEEN_BOX_Y 1 /* axis y */
 #define SIZE_PER_QUEUE      3
@@ -205,7 +201,7 @@ int set_location_size(struct box_info *box, int y)
 	y2 = box->l.y1 + PREFIX_SIZE_PER_BOX + INFO_SIZE_PER_BOX;
 	for (i = 0;
 	     (box->child[i].filled && (i < (box->n_q + box->n_sch)));
-		 i++) {
+	     i++) {
 		if (box->child[i].type == DP_NODE_QUEUE) {
 			box->child[i].l.x2 = box->l.x1;
 			box->child[i].l.y2 = y2;
@@ -242,7 +238,7 @@ int check_location(struct q_print_info *q_info)
 		if (!q_info->box[i].p_box)
 			continue;
 		if ((q_info->box[i].p_box->l.x1 - q_info->box[i].l.x2) !=
-			PADDING_BETWEEN_BOX_X) {
+		    PADDING_BETWEEN_BOX_X) {
 			pr_err("sched[%d]<->sched[%d]: %d - %d %s%d\n",
 			       q_info->box[i].node,
 			       q_info->box[i].p_box->node,
@@ -591,7 +587,7 @@ struct q_print_info *collect_info(struct seq_file *s,
 			       sizeof(q_info->box[q_info->box_num]));
 			q_info->box[q_info->box_num].filled = 1;
 			q_info->box[q_info->box_num].node =
-							res->q_res[i].sch_id[j];
+				res->q_res[i].sch_id[j];
 			q_info->box[q_info->box_num].id = -1; /* not valid */
 			q_info->box[q_info->box_num].box_x = curr_box_x + j;
 			q_info->box[q_info->box_num].box_y = curr_box_y;
@@ -1108,19 +1104,19 @@ ssize_t proc_qos_write(struct file *file, const char *buf, size_t count,
 		if (dp_strncmpi(param_list[3], "null", strlen("null")) == 0) {
 			node_prio.arbi = ARBITRATION_NULL;
 		} else if (dp_strncmpi(param_list[3], "sp",
-			   strlen("sp")) == 0) {
+				       strlen("sp")) == 0) {
 			node_prio.arbi = ARBITRATION_SP;
 		} else if (dp_strncmpi(param_list[3], "wsp",
-			   strlen("wsp")) == 0) {
+				       strlen("wsp")) == 0) {
 			node_prio.arbi = ARBITRATION_WSP;
 		} else if (dp_strncmpi(param_list[3], "wrr",
-			   strlen("wrr")) == 0) {
+				       strlen("wrr")) == 0) {
 			node_prio.arbi = ARBITRATION_WRR;
 		} else if (dp_strncmpi(param_list[3], "wsp_wrr",
-			   strlen("wsp_wrr")) == 0) {
+				       strlen("wsp_wrr")) == 0) {
 			node_prio.arbi = ARBITRATION_WSP_WRR;
 		} else if (dp_strncmpi(param_list[3], "wfq",
-			   strlen("wfq")) == 0) {
+				       strlen("wfq")) == 0) {
 			node_prio.arbi = ARBITRATION_WFQ;
 		} else {
 			pr_err("unknown type %s\n", param_list[3]);
diff --git a/drivers/net/datapath/dpm/datapath_qos.c b/drivers/net/datapath/dpm/datapath_qos.c
index ef64531afbac..6fae998a4c36 100644
--- a/drivers/net/datapath/dpm/datapath_qos.c
+++ b/drivers/net/datapath/dpm/datapath_qos.c
@@ -1,3 +1,8 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
 #include <net/datapath_api.h>
 #include <net/datapath_api_qos.h>
 #include "datapath.h"
diff --git a/drivers/net/datapath/dpm/datapath_soc.c b/drivers/net/datapath/dpm/datapath_soc.c
index a912096904d3..76e24571c58e 100644
--- a/drivers/net/datapath/dpm/datapath_soc.c
+++ b/drivers/net/datapath/dpm/datapath_soc.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <net/datapath_api.h>
diff --git a/drivers/net/datapath/dpm/datapath_swdev.c b/drivers/net/datapath/dpm/datapath_swdev.c
index 3993d11a36b1..edf581dd9eb1 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.c
+++ b/drivers/net/datapath/dpm/datapath_swdev.c
@@ -1,9 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 #include <linux/types.h>
@@ -845,19 +842,20 @@ static int dp_swdev_port_attr_set(struct net_device *dev,
 							  trans);
 		break;
 	case SWITCHDEV_ATTR_ID_BRIDGE_AGEING_TIME:
-		/*err = dp_swdev_port_attr_bridge_ageing_time_set(dp_swdev_port,
-		 *attr->u.ageing_time,
-		 *trans);
-		 */
+	/* err = dp_swdev_port_attr_bridge_ageing_time_set(dp_swdev_port,
+	 * attr->u.ageing_time,
+	 * trans);
+	 */
 		break;
 	case SWITCHDEV_ATTR_ID_BRIDGE_VLAN_FILTERING:
-		/*err = dp_swdev_port_attr_bridge_br_vlan_set(dev,
-		 *attr->orig_dev,trans);
+		/* err = dp_swdev_port_attr_bridge_br_vlan_set(dev,
+		 * attr->orig_dev,trans);
 		 */
 	case SWITCHDEV_ATTR_ID_PORT_LEARNING_LIMIT:
-		err = dp_swdev_port_attr_learning_limit_set(dev,
-							attr->u.learning_limit,
-							trans);
+		err =
+		dp_swdev_port_attr_learning_limit_set(dev,
+						      attr->u.learning_limit,
+						      trans);
 	default:
 		err = -EOPNOTSUPP;
 		break;
@@ -903,8 +901,9 @@ static int dp_swdev_port_attr_get(struct net_device *dev,
 		//err = 0;
 		break;
 	case SWITCHDEV_ATTR_ID_PORT_BRIDGE_FLAGS:
-		err = dp_swdev_port_attr_bridge_flags_get(dev,
-							  &attr->u.brport_flags);
+		err =
+		dp_swdev_port_attr_bridge_flags_get(dev,
+						    &attr->u.brport_flags);
 		break;
 	default:
 		return 0;
diff --git a/drivers/net/datapath/dpm/datapath_swdev.h b/drivers/net/datapath/dpm/datapath_swdev.h
index 2c9d0c17b66e..4057354e3e3a 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.h
+++ b/drivers/net/datapath/dpm/datapath_swdev.h
@@ -1,3 +1,8 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
 #ifndef DATAPATH_SWDEV_H
 #define DATAPATH_SWDEV_H
 
diff --git a/drivers/net/datapath/dpm/datapath_swdev_api.h b/drivers/net/datapath/dpm/datapath_swdev_api.h
index 1d14add3f627..2bc8a2078aff 100644
--- a/drivers/net/datapath/dpm/datapath_swdev_api.h
+++ b/drivers/net/datapath/dpm/datapath_swdev_api.h
@@ -1,9 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 #ifndef DATAPATH_SWIHDEV_API_H
@@ -11,7 +8,7 @@
 
 int dp_switchdev_init(void);
 int dp_register_switchdev_ops(struct dp_dev *dp_dev,
-			       struct net_device *dev);
+			      struct net_device *dev);
 int dp_del_br_if(struct net_device *dev, struct net_device *br_dev,
 		 int inst, int bport);
 int dp_notif_br_alloc(struct net_device *br_dev);
diff --git a/drivers/net/datapath/dpm/datapath_tx.c b/drivers/net/datapath/dpm/datapath_tx.c
index 92a44b853009..83bc6908a6b1 100644
--- a/drivers/net/datapath/dpm/datapath_tx.c
+++ b/drivers/net/datapath/dpm/datapath_tx.c
@@ -1,3 +1,8 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
 #include <linux/kernel.h>
 #include <linux/types.h>
 #include "datapath.h"
diff --git a/drivers/net/datapath/dpm/datapath_tx.h b/drivers/net/datapath/dpm/datapath_tx.h
index b7bb7f8b5326..3bebc2057801 100644
--- a/drivers/net/datapath/dpm/datapath_tx.h
+++ b/drivers/net/datapath/dpm/datapath_tx.h
@@ -1,5 +1,10 @@
-#ifndef DATAPATH_TX_H_7PAJCKO6
-#define DATAPATH_TX_H_7PAJCKO6
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
+#ifndef DATAPATH_TX_H
+#define DATAPATH_TX_H
 
 #include <net/datapath_api_tx.h>
 
@@ -21,4 +26,4 @@ void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
 	       struct pmac_tx_hdr *pmac, struct dp_subif *rx_subif,
 	       int need_pmac, int gso, int checksum);
 
-#endif /* end of include guard: DATAPATH_TX_H_7PAJCKO6 */
+#endif /* end of include guard: DATAPATH_TX_H */
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_coc.c b/drivers/net/datapath/dpm/gswip31/datapath_coc.c
index b9a18fc2116a..2bd46ddebee8 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_coc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_coc.c
@@ -1,11 +1,8 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
+
 #define pr_fmt(fmt) KBUILD_MODNAME ": " fmt
 
 #include <linux/module.h>
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c b/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c
index 22907fc7acbe..71f605c193c5 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c
@@ -1,9 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 #ifdef NON_LINUX
@@ -222,7 +219,7 @@ static int update_ctp(struct core_ops *ops, struct ext_vlan_info *vlan)
 
 	DP_DEBUG(DP_DBG_FLAG_SWDEV, "bp=%d v1=%d v1_d=%d v2=%d v2_d=%d\n",
 		 vlan->bp, vlan->n_vlan1, vlan->n_vlan1_drop,
-		vlan->n_vlan2, vlan->n_vlan2_drop);
+		 vlan->n_vlan2, vlan->n_vlan2_drop);
 	memset(&ctp, 0, sizeof(ctp));
 	memset(&alloc, 0, sizeof(GSW_EXTENDEDVLAN_alloc_t));
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
index f6a2f90550ac..c56fb9940edf 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/kernel.h>
@@ -114,12 +110,9 @@ int dp_pmac_set_31(int inst, u32 port, dp_pmac_cfg_t *pmac_cfg)
 	/*set ingress port via DMA tx channel */
 	if (pmac_cfg->ig_pmac_flags) {
 		/*Read back igcfg from gsw first */
-		ret = cbm_dequeue_port_resources_get(port, &dqport, flag);
-
-		if (ret == -1) {
-			pr_err("cbm_dequeue_port_resources_get failed\n");
+		ret = dp_cqm_deq_port_res_get(port, &dqport, flag);
+		if (ret == -1)
 			return -1;
-		}
 
 		memset(&igcfg, 0, sizeof(GSW_PMAC_Ig_Cfg_t));
 
@@ -562,7 +555,7 @@ struct gsw_itf *ctp_port_assign(int inst, u8 ep, int bp_default,
 
 	for (i = 0; i < ARRAY_SIZE(ctp_assign_info); i++) {
 		if ((ctp_assign_info[i].flag & alloc_flag) ==
-			ctp_assign_info[i].flag) {
+		    ctp_assign_info[i].flag) {
 			assign = &ctp_assign_info[i];
 			break;
 		}
@@ -588,7 +581,7 @@ struct gsw_itf *ctp_port_assign(int inst, u8 ep, int bp_default,
 	itf_assign[ep].n = ctp_assign.nNumberOfCtpPort;
 	itf_assign[ep].start = ctp_assign.nFirstCtpPortId;
 	itf_assign[ep].end = ctp_assign.nFirstCtpPortId +
-		 ctp_assign.nNumberOfCtpPort - 1;
+			     ctp_assign.nNumberOfCtpPort - 1;
 	itf_assign[ep].ep = ep;
 	port_info->subif_max = assign->num_subif;
 	port_info->ctp_max = ctp_assign.nNumberOfCtpPort;
@@ -609,7 +602,7 @@ int set_port_lookup_mode_31(int inst, u8 ep, u32 flags)
 	alloc_flag = port_info->alloc_flags;
 	for (i = 0; i < ARRAY_SIZE(ctp_assign_info); i++) {
 		if ((ctp_assign_info[i].flag & alloc_flag) ==
-			ctp_assign_info[i].flag) {
+		    ctp_assign_info[i].flag) {
 			assign = &ctp_assign_info[i];
 			break;
 		}
@@ -653,9 +646,9 @@ int alloc_bridge_port(int inst, int port_id, int subif_ix,
 	else
 		bp_cfg.bSrcMacLearningDisable = 0;
 	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID |
-		GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP |
-		GSW_BRIDGE_PORT_CONFIG_MASK_MC_SRC_MAC_LEARNING |
-		GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_CTP_MAPPING;
+		       GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP |
+		       GSW_BRIDGE_PORT_CONFIG_MASK_MC_SRC_MAC_LEARNING |
+		       GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_CTP_MAPPING;
 	bp_cfg.nBridgeId = fid;
 
 	SET_BP_MAP(bp_cfg.nBridgePortMap, bp_member); /*CPU*/
@@ -1194,8 +1187,8 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 		ctp_cfg->nLogicalPortId = mtr_subif->subif.port_id;
 		ctp_cfg->nSubIfIdGroup  = GET_VAP(mtr_subif->subif.subif,
-						 port_info->vap_offset,
-						 port_info->vap_mask);
+						  port_info->vap_offset,
+						  port_info->vap_mask);
 		ret = gsw_ctp->CTP_PortConfigGet(gsw_handle, ctp_cfg);
 		if (ret != GSW_statusOk) {
 			pr_err("PortConfigGet API failed :%d\n", ret);
@@ -1382,8 +1375,8 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 		ctp_cfg->nLogicalPortId = mtr_subif->subif.port_id;
 		ctp_cfg->nSubIfIdGroup = GET_VAP(mtr_subif->subif.subif,
-							port_info->vap_offset,
-							port_info->vap_mask);
+						 port_info->vap_offset,
+						 port_info->vap_mask);
 		if (meter->dir == DP_DIR_EGRESS)
 			ctp_cfg->nEgressTrafficMeterId =  meter->meter_id;
 		else if (meter->dir == DP_DIR_INGRESS)
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c b/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c
index b444c0f3e515..3160ba9d2109 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/init.h>
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_mib.c b/drivers/net/datapath/dpm/gswip31/datapath_mib.c
index 2ce1e9cce009..2b4dad7dded0 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_mib.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_mib.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <net/datapath_api.h>
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_mib.h b/drivers/net/datapath/dpm/gswip31/datapath_mib.h
index ad14817c7895..40bcd3871e70 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_mib.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_mib.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_MIB_H
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 3d1e0e2153dd..673e6cc7fcee 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #define DATAPATH_HAL_LAYER   /*must put before include datapath_api.h in
@@ -1180,7 +1176,6 @@ static int dp_platform_set(int inst, u32 flag)
 			init_dma_desc_mask();
 			dp_sub_proc_install_31();
 		}
-		init_qos_fn();
 		/*just for debugging purpose */
 		sif = get_dp_port_subif(get_dp_port_info(inst, 0), 0);
 		sif->bp = CPU_BP;
@@ -1195,8 +1190,8 @@ static int dp_platform_set(int inst, u32 flag)
 			mib_init(0, inst);
 		dp_get_gsw_parser_31(NULL, NULL, NULL, NULL);
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_CPUFREQ)
-	if (!inst)
-		dp_coc_cpufreq_init();
+		if (!inst)
+			dp_coc_cpufreq_init();
 #endif
 		/*disable egress VLAN modification for CPU port*/
 		port_remark.nPortId = 0;
@@ -1572,7 +1567,7 @@ static int deq_update_info(struct dp_subif_upd_info *info)
 	/* update new deq info */
 	for (i = 0; i < info->new_num_cqm_deq; i++) {
 		q_port.cqe_deq = port_info->deq_port_base +
-					info->new_cqm_deq_idx + i;
+				 info->new_cqm_deq_idx + i;
 		new_deq_pinfo =
 			get_dp_deqport_info(info->inst, q_port.cqe_deq);
 		q_port.inst = info->inst;
@@ -1642,7 +1637,7 @@ static int deq_update_info(struct dp_subif_upd_info *info)
 	/* remove old deq port info */
 	for (i = 0; i < old_num_cqm_deq; i++) {
 		old_cqm_deq_port = port_info->deq_port_base +
-						old_cqm_deq_idx + i;
+				   old_cqm_deq_idx + i;
 		old_deq_pinfo =	get_dp_deqport_info(info->inst,
 						    old_cqm_deq_port);
 		/* retrieve the qid corresponding to old deq port */
@@ -1667,7 +1662,7 @@ static int deq_update_info(struct dp_subif_upd_info *info)
 		dma_ch_offset =	old_deq_pinfo->dma_ch_offset;
 		if (port_info->num_dma_chan)
 			atomic_dec(&(dp_dma_chan_tbl[info->inst] +
-				   dma_ch_offset)->ref_cnt);
+				     dma_ch_offset)->ref_cnt);
 		if (dp_cbm_deq_port_enable(port_info->owner, info->inst,
 					   info->dp_port, old_cqm_deq_port,
 					   old_num_cqm_deq, CBM_PORT_F_DISABLE,
@@ -2137,7 +2132,7 @@ static int supported_logic_dev(int inst, struct net_device *dev,
 }
 
 static int subif_platform_set_unexplicit(int inst, int port_id,
-					 struct logic_dev *logic_dev, u32 flags)
+		struct logic_dev *logic_dev, u32 flags)
 {
 	if (flags & DP_F_DEREGISTER) {
 		free_bridge_port(inst, logic_dev->bp);
@@ -2215,7 +2210,7 @@ static int not_valid_rx_ep(int ep)
 static void set_pmac_subif(struct pmac_tx_hdr *pmac, int32_t subif)
 {
 	pmac->src_dst_subif_id_lsb = subif & 0xff;
-	pmac->src_dst_subif_id_msb =  (subif >> 8) & 0x1f;
+	pmac->src_dst_subif_id_msb = (subif >> 8) & 0x1f;
 }
 
 static void update_port_vap(int inst, u32 *ep, int *vap,
@@ -2259,9 +2254,9 @@ static void get_dma_pmac_templ(int index, struct pmac_tx_hdr *pmac,
 	if (likely(pmac))
 		memcpy(pmac, &dp_info->pmac_template[index], sizeof(*pmac));
 	desc_0->all = (desc_0->all & dp_info->dma0_mask_template[index].all) |
-				dp_info->dma0_template[index].all;
+		      dp_info->dma0_template[index].all;
 	desc_1->all = (desc_1->all & dp_info->dma1_mask_template[index].all) |
-				dp_info->dma1_template[index].all;
+		      dp_info->dma1_template[index].all;
 }
 
 static int check_csum_cap(void)
@@ -2352,7 +2347,7 @@ int register_dp_cap_gswip31(int flag)
 	cap.info.swdev_bridge_port_flags_set = dp_swdev_bridge_port_flags_set;
 	cap.info.swdev_bridge_port_flags_get = dp_swdev_bridge_port_flags_get;
 	cap.info.swdev_port_learning_limit_set =
-					dp_swdev_port_learning_limit_set;
+		dp_swdev_port_learning_limit_set;
 	cap.info.dp_mac_set = dp_gswip_mac_entry_add;
 	cap.info.dp_mac_reset = dp_gswip_mac_entry_del;
 	cap.info.dp_cfg_vlan = dp_gswip_ext_vlan; /*for symmetric VLAN */
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.h b/drivers/net/datapath/dpm/gswip31/datapath_misc.h
index 223bb84bb345..6ed74af79a84 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_MISC31_H
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
index 18ec794cae94..7b959e7e0dbd 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
@@ -1,80 +1,380 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <net/datapath_api.h>
 #include "../datapath.h"
 #include "datapath_misc.h"
 
-int (*qos_queue_remove)(struct pp_qos_dev *qos_dev, unsigned int id);
-int (*qos_queue_allocate)(struct pp_qos_dev *qos_dev, unsigned int *id);
-int (*qos_queue_allocate_id_phy)(struct pp_qos_dev *qdev, u32 *id,
-				 u32 *phy);
-int (*qos_queue_info_get)(struct pp_qos_dev *qos_dev, unsigned int id,
-			  struct pp_qos_queue_info *info);
-int (*qos_port_remove)(struct pp_qos_dev *qos_dev, unsigned int id);
-int (*qos_sched_allocate)(struct pp_qos_dev *qos_dev, unsigned int *id);
-int (*qos_sched_remove)(struct pp_qos_dev *qos_dev, unsigned int id);
-int (*qos_port_allocate)(struct pp_qos_dev *qos_dev,
-			 unsigned int physical_id,
-			 unsigned int *id);
-int (*qos_port_set)(struct pp_qos_dev *qos_dev, unsigned int id,
-		    const struct pp_qos_port_conf *conf);
-void (*qos_port_conf_set_default)(struct pp_qos_port_conf *conf);
-void (*qos_queue_conf_set_default)(struct pp_qos_queue_conf *conf);
-int (*qos_queue_set)(struct pp_qos_dev *qos_dev, unsigned int id,
-		     const struct pp_qos_queue_conf *conf);
-void (*qos_sched_conf_set_default)(struct pp_qos_sched_conf *conf);
-int (*qos_sched_set)(struct pp_qos_dev *qos_dev, unsigned int id,
-		     const struct pp_qos_sched_conf *conf);
-int (*qos_queue_conf_get)(struct pp_qos_dev *qos_dev, unsigned int id,
-			  struct pp_qos_queue_conf *conf);
-int (*qos_sched_conf_get)(struct pp_qos_dev *qos_dev, unsigned int id,
-			  struct pp_qos_sched_conf *conf);
-int (*qos_sched_get_queues)(struct pp_qos_dev *qos_dev, unsigned int id,
-			    u16 *queue_ids, unsigned int size,
-			    unsigned int *queues_num);
-int (*qos_port_get_queues)(struct pp_qos_dev *qos_dev, unsigned int id,
-			   u16 *queue_ids, unsigned int size,
-				  unsigned int *queues_num);
-int (*qos_port_conf_get)(struct pp_qos_dev *qdev, unsigned int id,
-			 struct pp_qos_port_conf *conf);
-int (*qos_port_info_get)(struct pp_qos_dev *qdev, unsigned int id,
-			 struct pp_qos_port_info *info);
-struct pp_qos_dev *(*qos_dev_open)(unsigned int id);
-
-void init_qos_fn(void)
+void dp_print_sched_conf(const struct pp_qos_sched_conf *conf)
 {
-#if IS_ENABLED(CONFIG_PPV4)
-	qos_queue_remove = pp_qos_queue_remove;
-	qos_queue_allocate = pp_qos_queue_allocate;
-	qos_queue_allocate_id_phy = pp_qos_queue_allocate_id_phy;
-	qos_queue_info_get = pp_qos_queue_info_get;
-	qos_port_remove = pp_qos_port_remove;
-	qos_sched_allocate = pp_qos_sched_allocate;
-	qos_sched_remove = pp_qos_sched_remove;
-	qos_port_allocate = pp_qos_port_allocate;
-	qos_port_set = pp_qos_port_set;
-	qos_port_conf_set_default = pp_qos_port_conf_set_default;
-	qos_queue_conf_set_default = pp_qos_queue_conf_set_default;
-	qos_queue_set = pp_qos_queue_set;
-	qos_sched_conf_set_default = pp_qos_sched_conf_set_default;
-	qos_sched_set = pp_qos_sched_set;
-	qos_queue_conf_get = pp_qos_queue_conf_get;
-	qos_sched_conf_get = pp_qos_sched_conf_get;
-	qos_sched_get_queues = pp_qos_sched_get_queues;
-	qos_port_get_queues = pp_qos_port_get_queues;
-	qos_port_conf_get = pp_qos_port_conf_get;
-	qos_dev_open = pp_qos_dev_open;
-#else
-	/*all NULL function pointer */
-	DP_DEBUG(DP_DBG_FLAG_QOS, "call QOS function pointer set to NULL\n");
-#endif /*CONFIG_PPV4*/
+	pr_info("=====Common Node properties====\n");
+	pr_info("bw_limit              %x\n",
+		conf->common_prop.bandwidth_limit);
+	pr_info("shared_bw_grp         %x\n",
+		conf->common_prop.shared_bw_group);
+
+	pr_info("=====Parent Node properties====\n");
+	pr_info("best_effort_enable    %x\n",
+		conf->sched_parent_prop.best_effort_enable);
+
+	if (conf->sched_parent_prop.arbitration == PP_QOS_ARBITRATION_WSP)
+		pr_info("arbi                  %s\n", "WSP");
+	else if (conf->sched_parent_prop.arbitration == PP_QOS_ARBITRATION_WRR)
+		pr_info("arbi                  %s\n", "WRR");
+	else
+		pr_info("arbi                  %s\n", "WFQ");
+
+	pr_info("=====Child Node properties====\n");
+	pr_info("parent	               %x\n", conf->sched_child_prop.parent);
+	pr_info("priority              %x\n", conf->sched_child_prop.priority);
+	pr_info("bandwidth_share       %x\n",
+		conf->sched_child_prop.wrr_weight);
+
+	pr_info("\n\n");
+}
+
+void dp_print_port_conf(const struct pp_qos_port_conf *conf)
+{
+	pr_info("=====Common Node properties====\n");
+
+	pr_info("bw_limit              %x\n",
+		conf->common_prop.bandwidth_limit);
+	pr_info("shared_bw_grp         %x\n",
+		conf->common_prop.shared_bw_group);
+
+	pr_info("=====Parent Node properties====\n");
+	pr_info("best_effort_enable    %x\n",
+		conf->port_parent_prop.best_effort_enable);
+
+	if (conf->port_parent_prop.arbitration == PP_QOS_ARBITRATION_WSP)
+		pr_info("arbi                  %s\n", "WSP");
+	else if (conf->port_parent_prop.arbitration == PP_QOS_ARBITRATION_WRR)
+		pr_info("arbi                  %s\n", "WRR");
+	else
+		pr_info("arbi                  %s\n", "WFQ");
+
+	pr_info("ring_address          %lx\n", conf->ring_address);
+	pr_info("ring_size             %x\n", conf->ring_size);
+	pr_info("packet_credit_enable  %x\n", conf->packet_credit_enable);
+	pr_info("credit                %x\n", conf->credit);
+	pr_info("disable               %x\n", conf->disable);
+	pr_info("green_threshold       %x\n", conf->green_threshold);
+	pr_info("yellow_threshold      %x\n", conf->yellow_threshold);
+}
+
+void dp_print_queue_conf(const struct pp_qos_queue_conf *conf)
+{
+	pr_info("=====Common Node properties====\n");
+	pr_info("bw_limit              %x\n",
+		conf->common_prop.bandwidth_limit);
+	pr_info("shared_bw_grp         %x\n",
+		conf->common_prop.shared_bw_group);
+
+	pr_info("=====Child Node properties====\n");
+	pr_info("parent	               %x\n", conf->queue_child_prop.parent);
+	pr_info("priority              %x\n", conf->queue_child_prop.priority);
+	pr_info("bandwidth_share       %x\n",
+		conf->queue_child_prop.wrr_weight);
+
+	pr_info("max_burst                   %x\n", conf->max_burst);
+	pr_info("blocked                     %x\n", conf->blocked);
+	pr_info("wred_enable                 %x\n", conf->wred_enable);
+	pr_info("wred_fixed_drop_prob_enable %x\n",
+		conf->wred_fixed_drop_prob_enable);
+	pr_info("wred_min_avg_green          %x\n", conf->wred_min_avg_green);
+	pr_info("wred_max_avg_green          %x\n", conf->wred_max_avg_green);
+	pr_info("wred_slope_green            %x\n", conf->wred_slope_green);
+	pr_info("wred_fixed_drop_prob_green  %x\n",
+		conf->wred_fixed_drop_prob_green);
+	pr_info("wred_min_avg_yellow         %x\n", conf->wred_min_avg_yellow);
+	pr_info("wred_slope_yellow           %x\n", conf->wred_slope_yellow);
+	pr_info("wred_fixed_drop_prob_yellow %x\n",
+		conf->wred_fixed_drop_prob_yellow);
+	pr_info("wred_min_guaranteed         %x\n", conf->wred_min_guaranteed);
+	pr_info("wred_max_allowed            %x\n", conf->wred_max_allowed);
+	pr_info("codel_en                    %x\n", conf->codel_en);
+	pr_info("eir                         %x\n", conf->eir);
+}
+
+int dp_qos_queue_remove(struct pp_qos_dev *qdev, u32 id)
+{
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	return pp_qos_queue_remove(qdev, id);
+}
+
+int dp_qos_queue_allocate(struct pp_qos_dev *qdev, u32 *id)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, *id);
+
+	ret = pp_qos_queue_allocate(qdev, id);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS)
+		pr_info("%s %s QoS Dev %p ID %d\n",
+			__func__, ret ? "Fail" : "OK", qdev, *id);
+#endif
+
+	return ret;
+}
+
+int dp_qos_queue_info_get(struct pp_qos_dev *qdev, u32 id,
+			  struct pp_qos_queue_info *info)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	ret = pp_qos_queue_info_get(qdev, id, info);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s %s QoS Dev %p ID %d\n",
+			__func__, ret ? "Fail" : "OK", qdev, id);
+		pr_info("================================\n");
+		pr_info("port ID         %x\n", info->port_id);
+		pr_info("physical ID     %x\n", info->physical_id);
+	}
+#endif
+	return ret;
+}
+
+int dp_qos_port_remove(struct pp_qos_dev *qdev, u32 id)
+{
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	return pp_qos_port_remove(qdev, id);
+}
+
+int dp_qos_sched_allocate(struct pp_qos_dev *qdev, u32 *id)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, *id);
+
+	ret = pp_qos_sched_allocate(qdev, id);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS)
+		pr_info("%s %s QoS Dev %p ID %d\n",
+			__func__, ret ? "Fail" : "OK", qdev, *id);
+#endif
+	return ret;
+}
+
+int dp_qos_sched_remove(struct pp_qos_dev *qdev, u32 id)
+{
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	return pp_qos_sched_remove(qdev, id);
+}
+
+int dp_qos_port_allocate(struct pp_qos_dev *qdev,
+			 u32 physical_id, u32 *id)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, *id);
+
+	ret =  pp_qos_port_allocate(qdev, physical_id, id);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS)
+		pr_info("%s %s QoS Dev %p ID %d\n",
+			__func__, ret ? "Fail" : "OK", qdev, *id);
+#endif
+	return ret;
+}
+
+int dp_qos_port_set(struct pp_qos_dev *qdev, u32 id,
+		    const struct pp_qos_port_conf *conf)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s QoS Dev %p ID %d\n", __func__, qdev, id);
+		pr_info("================================\n");
+		dp_print_port_conf(conf);
+	}
+#endif
+
+	return pp_qos_port_set(qdev, id, conf);
+}
+
+void dp_qos_port_conf_set_default(struct pp_qos_port_conf *conf)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s\n", __func__);
+		pr_info("================================\n");
+		dp_print_port_conf(conf);
+	}
+#endif
+
+	pp_qos_port_conf_set_default(conf);
+}
+
+void dp_qos_queue_conf_set_default(struct pp_qos_queue_conf *conf)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s\n", __func__);
+		pr_info("================================\n");
+		dp_print_queue_conf(conf);
+	}
+#endif
+
+	pp_qos_queue_conf_set_default(conf);
+}
+
+int dp_qos_queue_set(struct pp_qos_dev *qdev, u32 id,
+		     const struct pp_qos_queue_conf *conf)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("Attach q_node=%d to parent_node=%d\n",
+			id, conf->queue_child_prop.parent);
+		pr_info("%s QoS Dev %p ID %d\n", __func__, qdev, id);
+		pr_info("================================\n");
+		dp_print_queue_conf(conf);
+	}
+#endif
+	return pp_qos_queue_set(qdev, id, conf);
+}
+
+void dp_qos_sched_conf_set_default(struct pp_qos_sched_conf *conf)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s\n", __func__);
+		pr_info("================================\n");
+		dp_print_sched_conf(conf);
+	}
+#endif
+	pp_qos_sched_conf_set_default(conf);
+}
+
+int dp_qos_sched_set(struct pp_qos_dev *qdev, u32 id,
+		     const struct pp_qos_sched_conf *conf)
+{
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s QoS Dev %p ID %d\n", __func__, qdev, id);
+		pr_info("================================\n");
+		dp_print_sched_conf(conf);
+	}
+#endif
+	return pp_qos_sched_set(qdev, id, conf);
+}
+
+int dp_qos_queue_conf_get(struct pp_qos_dev *qdev, u32 id,
+			  struct pp_qos_queue_conf *conf)
+{
+	int ret = 0;
+
+	ret = pp_qos_queue_conf_get(qdev, id, conf);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("%s QoS Dev %p ID %d\n", __func__, qdev, id);
+		pr_info("================================\n");
+		dp_print_queue_conf(conf);
+	}
+#endif
+	return ret;
+}
+
+int dp_qos_sched_conf_get(struct pp_qos_dev *qdev, u32 id,
+			  struct pp_qos_sched_conf *conf)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	ret = pp_qos_sched_conf_get(qdev, id, conf);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
+		pr_info("================================\n");
+		dp_print_sched_conf(conf);
+	}
+#endif
+	return ret;
+}
+
+int dp_qos_sched_get_queues(struct pp_qos_dev *qdev, u32 id,
+			    u16 *queue_ids, u32 size,
+			    u32 *queues_num)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	ret = pp_qos_sched_get_queues(qdev, id, queue_ids, size,
+				      queues_num);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS)
+		pr_info("%s ret = %d queue_ids %d size %d queue_num %d\n",
+			__func__, ret, *queue_ids, size, *queues_num);
+#endif
+	return ret;
+}
+
+int dp_qos_port_get_queues(struct pp_qos_dev *qdev, u32 id,
+			   u16 *queue_ids, u32 size,
+			   u32 *queues_num)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	ret = pp_qos_port_get_queues(qdev, id, queue_ids, size, queues_num);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS)
+		pr_info("%s ret = %d queue_ids %d size %d queue_num %d\n",
+			__func__, ret, *queue_ids, size, *queues_num);
+#endif
+	return ret;
+}
+
+int dp_qos_port_conf_get(struct pp_qos_dev *qdev, u32 id,
+			 struct pp_qos_port_conf *conf)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, id);
+
+	ret = pp_qos_port_conf_get(qdev, id, conf);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
+	if (dp_dbg_flag & DP_DBG_FLAG_QOS)
+		dp_print_port_conf(conf);
+#endif
+	return ret;
+}
+
+int dp_qos_queue_allocate_id_phy(struct pp_qos_dev *qdev, u32 *id,
+				 u32 *phy)
+{
+	int ret = 0;
+
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s QoS Dev %p ID %d\n", __func__, qdev, *id);
+
+	ret = pp_qos_queue_allocate_id_phy(qdev, id, phy);
+
+	return ret;
+}
+
+struct pp_qos_dev *dp_qos_dev_open(u32 id)
+{
+	DP_DEBUG(DP_DBG_FLAG_QOS, "%s ID %d\n", __func__, id);
+
+	return pp_qos_dev_open(id);
 }
 
 int dp_pp_alloc_port(struct ppv4_port *info)
@@ -84,50 +384,27 @@ int dp_pp_alloc_port(struct ppv4_port *info)
 	struct hal_priv *priv = HAL(info->inst);
 	struct pp_qos_dev *qos_dev = priv->qdev;
 
-	if (qos_port_allocate(qos_dev,
-			      info->cqm_deq_port,
-			      &qos_p_id)) {
+	if (dp_qos_port_allocate(qos_dev,
+				 info->cqm_deq_port,
+				 &qos_p_id)) {
 		pr_err("Failed to alloc QoS for deq_port=%d\n",
 		       info->cqm_deq_port);
 		return -1;
 	}
-	DP_DEBUG(DP_DBG_FLAG_QOS,
-		 "qos_port_allocate ok with port(cqm/qos)=%d/%d\n",
-		 info->cqm_deq_port, qos_p_id);
 
-	qos_port_conf_set_default(&conf);
+	dp_qos_port_conf_set_default(&conf);
 	conf.port_parent_prop.arbitration = PP_QOS_ARBITRATION_WSP;
 	conf.ring_address = (ulong)info->tx_ring_addr;
 	conf.ring_size = info->tx_ring_size;
 	conf.packet_credit_enable = 1;
 	conf.credit = info->tx_pkt_credit;
-#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
-	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
-		DP_DEBUG(DP_DBG_FLAG_QOS,
-			 "qos_port_set info for p[%d/%d] dp_port=%d:\n",
-			 info->cqm_deq_port, qos_p_id,
-			 info->dp_port);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  arbitration=%d\n",
-			 conf.port_parent_prop.arbitration);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  ring_address=0x%x\n",
-			 (unsigned int)conf.ring_address);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  ring_size=%d\n",
-			 conf.ring_size);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  packet_credit_enable=%d\n",
-			 conf.packet_credit_enable);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  credit=%d\n",
-			 conf.credit);
-	}
-#endif
-	if (qos_port_set(qos_dev, qos_p_id, &conf)) {
+
+	if (dp_qos_port_set(qos_dev, qos_p_id, &conf)) {
 		pr_err("qos_port_set fail for port(cqm/qos) %d/%d\n",
 		       info->cqm_deq_port, qos_p_id);
-		qos_port_remove(qos_dev, qos_p_id);
+		dp_qos_port_remove(qos_dev, qos_p_id);
 		return -1;
 	}
-	DP_DEBUG(DP_DBG_FLAG_QOS,
-		 "qos_port_set ok for port(cqm/qos) %d/%d\n",
-		       info->cqm_deq_port, qos_p_id);
 	info->node_id = qos_p_id;
 	priv->deq_port_stat[info->cqm_deq_port].flag = PP_NODE_ALLOC;
 	priv->deq_port_stat[info->cqm_deq_port].node_id = qos_p_id;
@@ -142,8 +419,8 @@ int dp_pp_alloc_queue(struct ppv4_queue *info)
 	struct pp_qos_dev *qos_dev = priv->qdev;
 	int q_node_id;
 
-	if (qos_queue_allocate_id_phy(qos_dev, &q_node_id,
-				      &q_info.physical_id)) {
+	if (dp_qos_queue_allocate_id_phy(qos_dev, &q_node_id,
+					 &q_info.physical_id)) {
 		pr_err("qos_queue_allocate fail\n");
 		return -1;
 	}
@@ -183,12 +460,13 @@ int init_ppv4_qos(int inst, int flag)
 		priv->qdev = NULL;
 		return DP_SUCCESS;
 	}
-	priv->qdev = qos_dev_open(dp_port_prop[inst].qos_inst);
+	priv->qdev = dp_qos_dev_open(dp_port_prop[inst].qos_inst);
 	if (!priv->qdev) {
 		pr_err("Could not open qos instance %d\n",
 		       dp_port_prop[inst].qos_inst);
 		return DP_FAILURE;
 	}
+
 	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
 	if (!t)
 		return DP_FAILURE;
@@ -223,14 +501,14 @@ int init_ppv4_qos(int inst, int flag)
 	DP_DEBUG(DP_DBG_FLAG_DBG, "priv=%p deq_port_stat=%p q_dev=%p\n",
 		 priv, priv ? priv->deq_port_stat : NULL,
 		 priv ? priv->qdev : NULL);
-	if (qos_port_allocate(priv->qdev,
-			      priv->cqm_drop_p,
-			      &priv->ppv4_drop_p)) {
+	if (dp_qos_port_allocate(priv->qdev,
+				 priv->cqm_drop_p,
+				 &priv->ppv4_drop_p)) {
 		pr_err("Failed to alloc  qos drop port=%d\n",
 		       priv->cqm_drop_p);
 		goto EXIT;
 	}
-	qos_port_conf_set_default(&t->p_conf);
+	dp_qos_port_conf_set_default(&t->p_conf);
 	t->p_conf.port_parent_prop.arbitration = PP_QOS_ARBITRATION_WRR;
 	t->p_conf.ring_address = (unsigned long)deq_pinfo->txpush_addr_qos;
 	t->p_conf.ring_size = deq_pinfo->tx_ring_size;
@@ -238,46 +516,27 @@ int init_ppv4_qos(int inst, int flag)
 	t->p_conf.credit = deq_pinfo->tx_pkt_credit;
 	t->p_conf.disable = 0; /*allowed for dequeue*/
 
-#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
-	if (dp_dbg_flag & DP_DBG_FLAG_QOS) {
-		DP_DEBUG(DP_DBG_FLAG_QOS,
-			 "qos_port_set param: %d/%d for drop pot:\n",
-			 priv->cqm_drop_p, priv->ppv4_drop_p);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  arbitration=%d\n",
-			 t->p_conf.port_parent_prop.arbitration);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  ring_address=0x%x\n",
-			 (unsigned int)t->p_conf.ring_address);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  ring_size=%d\n",
-			 t->p_conf.ring_size);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  packet_credit_enable=%d\n",
-			 t->p_conf.packet_credit_enable);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  credit=%d\n",
-			 t->p_conf.credit);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  disabled=%d\n",
-			 t->p_conf.disable);
-	}
-#endif
-	if (qos_port_set(priv->qdev, priv->ppv4_drop_p, &t->p_conf)) {
+	if (dp_qos_port_set(priv->qdev, priv->ppv4_drop_p, &t->p_conf)) {
 		pr_err("qos_port_set fail for port(cqm/qos) %d/%d\n",
 		       priv->cqm_drop_p, priv->ppv4_drop_p);
-		qos_port_remove(priv->qdev, priv->ppv4_drop_p);
+		dp_qos_port_remove(priv->qdev, priv->ppv4_drop_p);
 		goto EXIT;
 	}
 
-	if (qos_queue_allocate_id_phy(priv->qdev, &q, &t->q_info.physical_id)) {
+	if (dp_qos_queue_allocate_id_phy(priv->qdev, &q, &t->q_info.physical_id)) {
 		pr_err("qos_queue_allocate fail\n");
-		qos_port_remove(priv->qdev, q);
+		dp_qos_port_remove(priv->qdev, q);
 		goto EXIT;
 	}
 	DP_DEBUG(DP_DBG_FLAG_QOS, "ppv4_drop_q alloc ok q_node=%d\n", q);
 
 	/* Making Q to Drop Port */
-	qos_queue_conf_set_default(&t->q_conf);
+	dp_qos_queue_conf_set_default(&t->q_conf);
 	t->q_conf.blocked = 1; /*drop mode */
 	t->q_conf.wred_enable = 0;
 	t->q_conf.wred_max_allowed = 0; /*max qocc in pkt */
 	t->q_conf.queue_child_prop.parent = priv->ppv4_drop_p;
-	if (qos_queue_set(priv->qdev, q, &t->q_conf)) {
+	if (dp_qos_queue_set(priv->qdev, q, &t->q_conf)) {
 		pr_err("qos_queue_set fail for node_id=%d to parent=%d\n",
 		       q, t->q_conf.queue_child_prop.parent);
 		goto EXIT;
@@ -299,71 +558,3 @@ int init_ppv4_qos(int inst, int flag)
 	return res;
 }
 
-/**
- * __mark_ppv4_port - Mark the allocated ppv4 ports.
- * @inst:	DP instance ID.
- * @*priv:	hal private structure info.
- * @base:	base of the continuous allocated ports.
- * @mark:	no of ports allocated to be marked as 1.
- **/
-static void __mark_ppv4_port(int inst, int base, int mark)
-{
-	int tmp;
-	struct hal_priv *priv = HAL(inst);
-
-	for (tmp = base; (tmp < mark) && (tmp < MAX_PPV4_PORTS); tmp++)
-		priv->ppv4_port_map.flags[tmp] = 1;
-}
-
-/**
- * ppv4_alloc_port_31 -	Allocate continuous requested deq_ports.
- * @inst:			DP instance ID
- * @deq_port_num:	no'of continuous PPV4 ports to be allocated.
- *
- * Returns the base of the continuous allocated ports.
- * else -ERROR.
- **/
-int ppv4_alloc_port_31(int inst, int deq_port_num)
-{
-	u32 base, match;
-	struct hal_priv *priv = HAL(inst);
-
-	for (base = 0; base < MAX_PPV4_PORTS; base++) {
-		for (match = 0; (match < deq_port_num) && ((base + match)
-		     < MAX_PPV4_PORTS); match++) {
-			if (priv->ppv4_port_map.flags[base + match])
-				break;
-		}
-		if (match == deq_port_num) {
-			__mark_ppv4_port(inst, base, (base + match));
-			return (base + PPV4_PORT_BASE);
-		}
-	}
-	return DP_FAILURE; /* port not found */
-}
-
-/**
- * ppv4_port_free_31 - Unmark the ppv4 ports for inst.
- * @inst:			DP instance ID.
- * @base:			base of continuous allocated PPV4 ports.
- * @deq_port_num:	no'of continuous PPV4 ports allocated.
- *
- * Free the ports allocated by ppv4_alloc_port_31() by marking zero.
- **/
-int ppv4_port_free_31(int inst, int base, int deq_port_num)
-{
-	u32 tmp;
-	struct hal_priv *priv = HAL(inst);
-
-	/* Expecting base always greater than PPV4_PORT_BASE */
-	if (base < PPV4_PORT_BASE)
-		return DP_FAILURE;
-
-	base = base - PPV4_PORT_BASE;
-	for (tmp = base; (tmp < MAX_PPV4_PORTS) && deq_port_num; tmp++) {
-		priv->ppv4_port_map.flags[tmp] = 0;
-		deq_port_num--;
-	}
-
-	return DP_SUCCESS;
-}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.h b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.h
index 27b924dadcc8..312098c6cfa4 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_PPV4_H
@@ -152,43 +148,59 @@ struct dp_lookup_entry {
 	int num; /* num of valid lookup entries save in entry[] array */
 };
 
-void init_qos_fn(void);
-extern int (*qos_queue_remove)(struct pp_qos_dev *qos_dev, unsigned int id);
-extern int (*qos_queue_allocate)(struct pp_qos_dev *qos_dev, unsigned int *id);
-extern int (*qos_queue_allocate_id_phy)(struct pp_qos_dev *qdev, u32 *id,
-					u32 *phy);
-extern int (*qos_queue_info_get)(struct pp_qos_dev *qos_dev, unsigned int id,
-				 struct pp_qos_queue_info *info);
-extern int (*qos_port_remove)(struct pp_qos_dev *qos_dev, unsigned int id);
-extern int (*qos_sched_allocate)(struct pp_qos_dev *qos_dev, unsigned int *id);
-extern int (*qos_sched_remove)(struct pp_qos_dev *qos_dev, unsigned int id);
-extern int (*qos_port_allocate)(struct pp_qos_dev *qos_dev,
-				unsigned int physical_id,
-				unsigned int *id);
-extern int (*qos_port_set)(struct pp_qos_dev *qos_dev, unsigned int id,
-			   const struct pp_qos_port_conf *conf);
-extern void (*qos_port_conf_set_default)(struct pp_qos_port_conf *conf);
-extern void (*qos_queue_conf_set_default)(struct pp_qos_queue_conf *conf);
-extern int (*qos_queue_set)(struct pp_qos_dev *qos_dev, unsigned int id,
-			    const struct pp_qos_queue_conf *conf);
-extern void (*qos_sched_conf_set_default)(struct pp_qos_sched_conf *conf);
-extern int (*qos_sched_set)(struct pp_qos_dev *qos_dev, unsigned int id,
-			    const struct pp_qos_sched_conf *conf);
-extern int (*qos_queue_conf_get)(struct pp_qos_dev *qos_dev, unsigned int id,
-				 struct pp_qos_queue_conf *conf);
-extern int (*qos_sched_conf_get)(struct pp_qos_dev *qos_dev, unsigned int id,
-				 struct pp_qos_sched_conf *conf);
-extern int (*qos_sched_get_queues)(struct pp_qos_dev *qos_dev, unsigned int id,
-				   u16 *queue_ids, unsigned int size,
-				   unsigned int *queues_num);
-extern int (*qos_port_get_queues)(struct pp_qos_dev *qos_dev, unsigned int id,
-				  u16 *queue_ids, unsigned int size,
-				  unsigned int *queues_num);
-extern int (*qos_port_conf_get)(struct pp_qos_dev *qdev, unsigned int id,
-				struct pp_qos_port_conf *conf);
+void init_qos_fn_32(void);
+
+int dp_qos_queue_remove(struct pp_qos_dev *qdev, u32 id);
+
+int dp_qos_queue_allocate(struct pp_qos_dev *qdev, u32 *id);
+int dp_qos_queue_allocate_id_phy(struct pp_qos_dev *qdev, u32 *id,
+				 u32 *phy);
+int dp_qos_queue_info_get(struct pp_qos_dev *qdev, u32 id,
+			  struct pp_qos_queue_info *info);
+
+int dp_qos_port_remove(struct pp_qos_dev *qdev, u32 id);
+
+int dp_qos_sched_allocate(struct pp_qos_dev *qdev, u32 *id);
+
+int dp_qos_sched_remove(struct pp_qos_dev *qdev, u32 id);
+
+int dp_qos_port_allocate(struct pp_qos_dev *qdev,
+			 u32 physical_id, u32 *id);
+
+int dp_qos_port_set(struct pp_qos_dev *qdev, u32 id,
+		    const struct pp_qos_port_conf *conf);
+
+void dp_qos_port_conf_set_default(struct pp_qos_port_conf *conf);
+
+void dp_qos_queue_conf_set_default(struct pp_qos_queue_conf *conf);
+
+int dp_qos_queue_set(struct pp_qos_dev *qdev, u32 id,
+		     const struct pp_qos_queue_conf *conf);
+
+void dp_qos_sched_conf_set_default(struct pp_qos_sched_conf *conf);
+
+int dp_qos_sched_set(struct pp_qos_dev *qdev, u32 id,
+		     const struct pp_qos_sched_conf *conf);
+
+int dp_qos_queue_conf_get(struct pp_qos_dev *qdev, u32 id,
+			  struct pp_qos_queue_conf *conf);
+
+int dp_qos_sched_conf_get(struct pp_qos_dev *qdev, u32 id,
+			  struct pp_qos_sched_conf *conf);
+
+int dp_qos_sched_get_queues(struct pp_qos_dev *qdev, u32 id,
+			    u16 *queue_ids, u32 size,
+			    u32 *queues_num);
+
+int dp_qos_port_get_queues(struct pp_qos_dev *qdev, u32 id,
+			   u16 *queue_ids, u32 size,
+			   u32 *queues_num);
+
+int dp_qos_port_conf_get(struct pp_qos_dev *qdev, u32 id,
+			 struct pp_qos_port_conf *conf);
 extern int (*qos_port_info_get)(struct pp_qos_dev *qdev, unsigned int id,
 				struct pp_qos_port_info *info);
-extern struct pp_qos_dev *(*qos_dev_open)(unsigned int id);
+struct pp_qos_dev *dp_qos_dev_open(u32 id);
 int dp_map_to_drop_q(int inst, int q_id, struct dp_lookup_entry *lookup);
 int dp_pp_alloc_port(struct ppv4_port *info);
 int dp_pp_alloc_sched(struct ppv4_scheduler *info);
@@ -197,6 +209,5 @@ int alloc_q_to_port(struct ppv4_q_sch_port *info, u32 flag);
 extern struct cqm_deq_stat deq_port_stat[MAX_CQM_DEQ];
 extern struct pp_queue_stat qos_queue_stat[MAX_QUEUE];
 int init_ppv4_qos(int inst, int flag);
-int ppv4_alloc_port_31(int inst, int deq_port_num);
-int ppv4_port_free_31(int inst, int base, int deq_port_num);
+
 #endif /* DATAPATH_PPV4_H */
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
index b42e653102d9..917174c69ee4 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <net/datapath_api.h>
@@ -285,7 +281,7 @@ static int dp_qos_queue_set_spl_31(int inst,
 
 	/* To set queue to blocking state during Q movement */
 	cfg->blocked = 1;
-	if (qos_queue_set(qdev, node_id, cfg)) {
+	if (dp_qos_queue_set(qdev, node_id, cfg)) {
 		pr_err("qos_queue_set fail: q[%d/%d]\n",
 		       qid, node_id);
 		return DP_FAILURE;
@@ -341,7 +337,7 @@ static int queue_flush_31(int inst, int node_id, int flag)
 	}
 
 	if (flag & FLUSH_RESTORE_QOS_PORT) {
-		if (qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
+		if (dp_qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("qos_queue_conf_get fail: q[%d/%d]\n",
 			       qid, node_id);
 			res = DP_FAILURE;
@@ -402,7 +398,7 @@ static int queue_flush_31(int inst, int node_id, int flag)
 			 "Queue movement before flush");
 
 		/*move to drop port and set block and resume the queue */
-		qos_queue_conf_set_default(&tmp_q_cfg); /*use new variable */
+		dp_qos_queue_conf_set_default(&tmp_q_cfg); /*use new variable */
 		dp_wred_def(&tmp_q_cfg);
 		tmp_q_cfg.queue_child_prop.parent = priv->ppv4_drop_p;
 		if (dp_qos_queue_set_spl_31(inst, priv->qdev, node_id, qid,
@@ -1200,7 +1196,7 @@ static int dp_node_alloc_global_pool(struct dp_node_alloc *node, int flag)
 		goto EXIT;
 
 	if (node->type == DP_NODE_QUEUE) {
-		if (qos_queue_allocate_id_phy(priv->qdev, &id, &phy_id)) {
+		if (dp_qos_queue_allocate_id_phy(priv->qdev, &id, &phy_id)) {
 			pr_err("qos_queue_allocate fail\n");
 			goto EXIT;
 		}
@@ -1220,9 +1216,9 @@ static int dp_node_alloc_global_pool(struct dp_node_alloc *node, int flag)
 		res = DP_SUCCESS;
 		goto EXIT;
 	} else if (node->type == DP_NODE_SCH) {
-		if (qos_sched_allocate(priv->qdev, &id)) {
+		if (dp_qos_sched_allocate(priv->qdev, &id)) {
 			pr_err("failed to qos_sched_allocate\n");
-			qos_sched_remove(priv->qdev, id);
+			dp_qos_sched_remove(priv->qdev, id);
 			goto EXIT;
 		}
 		priv->qos_sch_stat[id].c_flag = PP_NODE_ALLOC;
@@ -1280,41 +1276,24 @@ static int dp_alloc_qos_port(struct dp_node_alloc *node, int flag)
 			 cqm_deq_port);
 		return priv->deq_port_stat[cqm_deq_port].node_id;
 	}
-	if (qos_port_allocate(priv->qdev, cqm_deq_port, &qos_port)) {
+	if (dp_qos_port_allocate(priv->qdev, cqm_deq_port, &qos_port)) {
 		pr_err("failed to qos_port_allocate:%d\n", cqm_deq_port);
 		goto EXIT;
 	}
 	deq = get_dp_deqport_info(inst, cqm_deq_port);
 	/* Configure QOS dequeue port */
-	qos_port_conf_set_default(&port_cfg);
+	dp_qos_port_conf_set_default(&port_cfg);
 	port_cfg.ring_address = (unsigned long)deq->txpush_addr_qos;
 	port_cfg.ring_size = deq->tx_ring_size;
 	port_cfg.credit = deq->tx_pkt_credit;
 	if (port_cfg.credit)
 		port_cfg.packet_credit_enable = 1;
 	port_cfg.port_parent_prop.arbitration = PP_QOS_ARBITRATION_WSP;
-#if IS_ENABLED(CONFIG_INTEL_DATAPATH_DBG)
-	if (unlikely(dp_dbg_flag & DP_DBG_FLAG_QOS)) {
-		DP_DEBUG(DP_DBG_FLAG_QOS, "qos_port_set parameter: %d/%d\n",
-			 cqm_deq_port, qos_port);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  ring_address=0x%lx\n",
-			 port_cfg.ring_address);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  ring_size=%d\n",
-			 port_cfg.ring_size);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  credit=%d\n",
-			 port_cfg.credit);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  packet_credit_enable=%d\n",
-			 port_cfg.packet_credit_enable);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "  Arbitration=%d\n",
-			 port_cfg.port_parent_prop.arbitration);
-		DP_DEBUG(DP_DBG_FLAG_QOS, "priv->qdev=0x%p\n",
-			 priv->qdev);
-	}
-#endif
-	if (qos_port_set(priv->qdev, qos_port, &port_cfg)) {
+
+	if (dp_qos_port_set(priv->qdev, qos_port, &port_cfg)) {
 		pr_err("qos_port_set fail for port %d/%d\n",
 		       cqm_deq_port, qos_port);
-		qos_port_remove(priv->qdev, qos_port);
+		dp_qos_port_remove(priv->qdev, qos_port);
 		goto EXIT;
 	}
 	priv->deq_port_stat[cqm_deq_port].flag = PP_NODE_ALLOC;
@@ -1790,7 +1769,7 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 						     t->parent_id, flag);
 		/* Remove Queue link only for global resource */
 		if (!(t->resv_flag & PP_NODE_RESERVE)) {
-			if (qos_queue_remove(priv->qdev, t->node_id)) {
+			if (dp_qos_queue_remove(priv->qdev, t->node_id)) {
 				pr_err("failed to qos_queue_remove\n");
 				goto EXIT;
 			}
@@ -1821,9 +1800,10 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 			 node->id.q_id, t->node_id);
 		/* Reserve Q temp attach to drop port */
 		if (t->resv_flag & PP_NODE_RESERVE) {
-			qos_queue_conf_set_default(&t->tmp_q);
+			dp_qos_queue_conf_set_default(&t->tmp_q);
 			t->tmp_q.queue_child_prop.parent = priv->ppv4_drop_p;
-			if (qos_queue_set(priv->qdev, t->node_id, &t->tmp_q)) {
+			if (dp_qos_queue_set(priv->qdev, t->node_id,
+					     &t->tmp_q)) {
 				pr_err("qos_queue_set %s=%d to parent=%d\n",
 				       "fail to reserve queue", t->node_id,
 				       t->tmp_q.queue_child_prop.parent);
@@ -1864,7 +1844,7 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 						     t->parent_id, flag);
 		/* Remove Sched link only if global resource */
 		if (!(t->resv_flag & PP_NODE_RESERVE)) {
-			if (qos_sched_remove(priv->qdev, t->sch_id)) {
+			if (dp_qos_sched_remove(priv->qdev, t->sch_id)) {
 				pr_err("failed to qos_sched_remove\n");
 				goto EXIT;
 			}
@@ -1896,9 +1876,10 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 			 node->id.sch_id);
 		/* Reserve Sched temp attach to drop port */
 		if (t->resv_flag & PP_NODE_RESERVE) {
-			qos_sched_conf_set_default(&t->tmp_sch);
+			dp_qos_sched_conf_set_default(&t->tmp_sch);
 			t->tmp_sch.sched_child_prop.parent = priv->ppv4_drop_p;
-			if (qos_sched_set(priv->qdev, t->sch_id, &t->tmp_sch)) {
+			if (dp_qos_sched_set(priv->qdev, t->sch_id,
+					     &t->tmp_sch)) {
 				pr_err("qos_sched_set %s=%d to parent=%d\n",
 				       "fail to reserve SCH", t->sch_id,
 				       t->tmp_sch.sched_child_prop.parent);
@@ -2001,7 +1982,7 @@ static int get_parent_arbi(int inst, int node_id, int flag)
 	pid = priv->qos_sch_stat[node_id].parent.node_id;
 
 	if (priv->qos_sch_stat[node_id].parent.type == DP_NODE_SCH) {
-		if (qos_sched_conf_get(priv->qdev, pid, &sched_cfg)) {
+		if (dp_qos_sched_conf_get(priv->qdev, pid, &sched_cfg)) {
 			pr_err("fail to get sched config\n");
 			return DP_FAILURE;
 		}
@@ -2013,7 +1994,7 @@ static int get_parent_arbi(int inst, int node_id, int flag)
 			       ? "sched" : "Q",
 			       node_id);
 	} else if (priv->qos_sch_stat[node_id].parent.type == DP_NODE_PORT) {
-		if (qos_port_conf_get(priv->qdev, pid, &port_cfg)) {
+		if (dp_qos_port_conf_get(priv->qdev, pid, &port_cfg)) {
 			pr_err("fail to get port config\n");
 			return DP_FAILURE;
 		}
@@ -2057,7 +2038,7 @@ int dp_node_link_get_31(struct dp_node_link *info, int flag)
 
 	if (info->node_type == DP_NODE_QUEUE) {
 		node_id = priv->qos_queue_stat[info->node_id.q_id].node_id;
-		if (qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
+		if (dp_qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("failed to qos_queue_conf_get\n");
 			return DP_FAILURE;
 		}
@@ -2088,8 +2069,8 @@ int dp_node_link_get_31(struct dp_node_link *info, int flag)
 		info->leaf = 0;
 		return DP_SUCCESS;
 	} else if (info->node_type == DP_NODE_SCH) {
-		if (qos_sched_conf_get(priv->qdev, info->node_id.sch_id,
-				       &sched_cfg)) {
+		if (dp_qos_sched_conf_get(priv->qdev, info->node_id.sch_id,
+					  &sched_cfg)) {
 			pr_err("failed to qos_sched_conf_get\n");
 			return DP_FAILURE;
 		}
@@ -2256,14 +2237,14 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 	}
 
 	if (info->node_type == DP_NODE_QUEUE) {
-		qos_queue_conf_set_default(queue_cfg);
+		dp_qos_queue_conf_set_default(queue_cfg);
 		node_id = priv->qos_queue_stat[info->node_id.q_id].node_id;
 		if ((priv->qos_queue_stat[info->node_id.q_id].flag &
-		      PP_NODE_ALLOC)) {
+		     PP_NODE_ALLOC)) {
 			dp_wred_def(queue_cfg);
 		} else {
-			if (qos_queue_conf_get(priv->qdev, node_id,
-					       queue_cfg)) {
+			if (dp_qos_queue_conf_get(priv->qdev, node_id,
+						  queue_cfg)) {
 				pr_err("fail to qos_queue_conf_get\n");
 				return DP_FAILURE;
 			}
@@ -2289,15 +2270,14 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 		if (dp_qos_queue_set_spl_31(info->inst, priv->qdev, node_id,
 					    info->node_id.q_id, queue_cfg)) {
 			pr_err("failed to qos_queue_set\n");
-			qos_queue_remove(priv->qdev, node_id);
+			dp_qos_queue_remove(priv->qdev, node_id);
 			goto ERROR_EXIT;
 		}
 
 		goto EXIT;
 	} else if (info->node_type == DP_NODE_SCH) {
-		qos_sched_conf_set_default(sched_cfg);
+		dp_qos_sched_conf_set_default(sched_cfg);
 		sched_cfg->sched_child_prop.parent = parent_node;
-
 		if (info->arbi == ARBITRATION_WRR)
 			sched_cfg->sched_child_prop.wrr_weight = info->prio_wfq;
 		else if (info->arbi == ARBITRATION_WSP)
@@ -2314,11 +2294,11 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 			 sched_cfg->sched_child_prop.parent,
 			 info->cqm_deq_port.cqm_deq_port);
 
-		if (qos_sched_set(priv->qdev, node_id, sched_cfg)) {
+		if (dp_qos_sched_set(priv->qdev, node_id, sched_cfg)) {
 			pr_err("failed to %s %d parent_node %d\n",
 			       "qos_sched_set node_id",
 			       node_id, parent_node);
-			qos_sched_remove(priv->qdev, node_id);
+			dp_qos_sched_remove(priv->qdev, node_id);
 			goto ERROR_EXIT;
 		}
 
@@ -2385,12 +2365,12 @@ static int set_parent_arbi(int inst, int node_id, int arbi, int flag)
 	}
 
 	if (priv->qos_sch_stat[node_id].parent.type == DP_NODE_SCH) {
-		if (qos_sched_conf_get(priv->qdev, pid, &sched_cfg)) {
+		if (dp_qos_sched_conf_get(priv->qdev, pid, &sched_cfg)) {
 			pr_err("fail to get sched config\n");
 			return DP_FAILURE;
 		}
 		sched_cfg.sched_parent_prop.arbitration = arbi;
-		if (qos_sched_set(priv->qdev, pid, &sched_cfg)) {
+		if (dp_qos_sched_set(priv->qdev, pid, &sched_cfg)) {
 			pr_err("fail to set arbi sched:%d parent of node:%d\n",
 			       pid, node_id);
 			return DP_FAILURE;
@@ -2401,13 +2381,13 @@ static int set_parent_arbi(int inst, int node_id, int arbi, int flag)
 		    (pid != priv->ppv4_drop_p))
 			return DP_SUCCESS;
 
-		if (qos_port_conf_get(priv->qdev, pid, &port_cfg)) {
+		if (dp_qos_port_conf_get(priv->qdev, pid, &port_cfg)) {
 			pr_err("fail to get port config\n");
 			return DP_FAILURE;
 		}
 
 		port_cfg.port_parent_prop.arbitration = arbi;
-		if (qos_port_set(priv->qdev, pid, &port_cfg)) {
+		if (dp_qos_port_set(priv->qdev, pid, &port_cfg)) {
 			pr_err("fail to set arbi port:%d parent of node:%d\n",
 			       pid, node_id);
 			return DP_FAILURE;
@@ -2454,7 +2434,7 @@ int dp_qos_link_prio_set_31(struct dp_node_prio *info, int flag)
 			return DP_FAILURE;
 		}
 		node_id = priv->qos_queue_stat[info->id.q_id].node_id;
-		if (qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
+		if (dp_qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("fail to get queue prio and parent\n");
 			return DP_FAILURE;
 		}
@@ -2466,7 +2446,8 @@ int dp_qos_link_prio_set_31(struct dp_node_prio *info, int flag)
 		DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 			 "Prio:%d paased to low level for queue[%d]\n",
 			 info->prio_wfq, info->id.q_id);
-		if (qos_queue_set(priv->qdev, node_id, &queue_cfg)) {
+
+		if (dp_qos_queue_set(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("failed to qos_queue_set\n");
 			return DP_FAILURE;
 		}
@@ -2495,8 +2476,8 @@ int dp_qos_link_prio_set_31(struct dp_node_prio *info, int flag)
 			       priv->qos_sch_stat[info->id.sch_id].p_flag);
 			return DP_FAILURE;
 		}
-		if (qos_sched_conf_get(priv->qdev, info->id.sch_id,
-				       &sched_cfg)) {
+		if (dp_qos_sched_conf_get(priv->qdev, info->id.sch_id,
+					  &sched_cfg)) {
 			pr_err("fail to get sched prio and parent\n");
 			return DP_FAILURE;
 		}
@@ -2508,7 +2489,7 @@ int dp_qos_link_prio_set_31(struct dp_node_prio *info, int flag)
 		DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 			 "Prio:%d paased to low level for Sched[%d]\n",
 			 info->prio_wfq, info->id.sch_id);
-		if (qos_sched_set(priv->qdev, info->id.sch_id, &sched_cfg)) {
+		if (dp_qos_sched_set(priv->qdev, info->id.sch_id, &sched_cfg)) {
 			pr_err("failed to qos_sched_set\n");
 			return DP_FAILURE;
 		}
@@ -2655,9 +2636,9 @@ int dp_deq_port_res_get_31(struct dp_dequeue_res *res, int flag)
 		if (priv->deq_port_stat[k].flag == PP_NODE_FREE)
 			continue;
 		q_num = 0;
-		if (qos_port_get_queues(priv->qdev,
-					priv->deq_port_stat[k].node_id,
-					q_ids, q_size, &q_num)) {
+		if (dp_qos_port_get_queues(priv->qdev,
+					   priv->deq_port_stat[k].node_id,
+					   q_ids, q_size, &q_num)) {
 			pr_err("qos_port_get_queues: port[%d/%d]\n",
 			       k,
 			       priv->deq_port_stat[k].node_id);
@@ -2673,8 +2654,8 @@ int dp_deq_port_res_get_31(struct dp_dequeue_res *res, int flag)
 				 "  q[/%d]\n", q_ids[i]);
 		for (i = 0; (i < q_num) && (idx < res->q_res_size); i++) {
 			memset(&t.q_info, 0, sizeof(t.q_info));
-			if (qos_queue_info_get(priv->qdev,
-					       q_ids[i], &t.q_info)) {
+			if (dp_qos_queue_info_get(priv->qdev,
+						  q_ids[i], &t.q_info)) {
 				pr_err("qos_port_info_get fail:q[/%d]\n",
 				       q_ids[i]);
 				continue;
@@ -2685,8 +2666,8 @@ int dp_deq_port_res_get_31(struct dp_dequeue_res *res, int flag)
 				 t.q_info.physical_id, q_ids[i]);
 			res->q_res[idx].sch_lvl = 0;
 			memset(&t.q_conf, 0, sizeof(t.q_conf));
-			if (qos_queue_conf_get(priv->qdev,
-					       q_ids[i], &t.q_conf)) {
+			if (dp_qos_queue_conf_get(priv->qdev,
+						  q_ids[i], &t.q_conf)) {
 				pr_err("qos_port_conf_get fail:q[/%d]\n",
 				       q_ids[i]);
 				continue;
@@ -2699,7 +2680,7 @@ int dp_deq_port_res_get_31(struct dp_dequeue_res *res, int flag)
 					res->q_res[idx].qos_deq_port = p_id;
 					res->q_res[idx].cqm_deq_port = k;
 					res->q_res[idx].cqm_deq_port_type =
-					dp_deq_port_tbl[res->inst][k].cpu_type;
+						dp_deq_port_tbl[res->inst][k].cpu_type;
 					break;
 				} else if (priv->qos_sch_stat[p_id].type !=
 					   DP_NODE_SCH) {
@@ -2713,8 +2694,8 @@ int dp_deq_port_res_get_31(struct dp_dequeue_res *res, int flag)
 				j++;
 				res->q_res[idx].sch_lvl = j;
 				/* get next parent */
-				if (qos_sched_conf_get(priv->qdev,
-						       p_id, &t.sched_conf)) {
+				if (dp_qos_sched_conf_get(priv->qdev, p_id,
+							  &t.sched_conf)) {
 					pr_err("qos_sched_conf_get %s[/%d]\n",
 					       "fail:sch", p_id);
 					break;
@@ -2773,8 +2754,8 @@ int dp_node_unlink_31(struct dp_node_link *info, int flag)
 			return DP_FAILURE;
 		}
 
-		if (qos_sched_get_queues(priv->qdev, info->node_id.sch_id,
-					 queue_buf, queue_size, &queue_num))
+		if (dp_qos_sched_get_queues(priv->qdev, info->node_id.sch_id,
+					    queue_buf, queue_size, &queue_num))
 			return DP_FAILURE;
 		for (i = 0; i < queue_num; i++) {
 			if (!(priv->qos_queue_stat[queue_buf[i]].flag &
@@ -2903,8 +2884,8 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		 */
 
 		t->node_id = priv->qos_queue_stat[info->node_id.q_id].node_id;
-		if (qos_queue_conf_get(priv->qdev, t->node_id,
-				       &t->queue_cfg) == 0) {
+		if (dp_qos_queue_conf_get(priv->qdev, t->node_id,
+					  &t->queue_cfg) == 0) {
 			t->queue_num = 1;
 			t->queue_buf[0] = t->node_id;
 			/* save original block/suspend status */
@@ -2982,23 +2963,23 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 			goto EXIT_ERR;
 		}
 		if ((t->f_child_free == 0) &&
-		    qos_sched_conf_get(priv->qdev, info->node_id.sch_id,
-				       &t->sched_cfg) == 0) {
+		    dp_qos_sched_conf_get(priv->qdev, info->node_id.sch_id,
+					  &t->sched_cfg) == 0) {
 			DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 				 "info->node_id.sch_id %d\n",
 				 info->node_id.sch_id);
-			if (qos_sched_get_queues(priv->qdev,
-						 info->node_id.sch_id,
-						 t->queue_buf, t->queue_size,
-						 &t->queue_num)) {
+			if (dp_qos_sched_get_queues(priv->qdev,
+						    info->node_id.sch_id,
+						    t->queue_buf, t->queue_size,
+						    &t->queue_num)) {
 				pr_err("Can not get queues:%d\n",
 				       info->node_id.sch_id);
 				goto EXIT_ERR;
 			}
 			for (i = 0; i < t->queue_num; i++) {
-				if (qos_queue_conf_get(priv->qdev,
-						       t->queue_buf[i],
-						       &t->queue_cfg))
+				if (dp_qos_queue_conf_get(priv->qdev,
+							  t->queue_buf[i],
+							  &t->queue_cfg))
 					continue;
 				if (t->queue_cfg.blocked == 0)
 					t->q_orig_block[i] =
@@ -3038,8 +3019,8 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		if (t->q_orig_block[i] < 0 &&/* non-valid block stat */
 		    t->q_orig_suspend[i] < 0)/* non-valid suspend stat */
 			continue;
-		if (qos_queue_conf_get(priv->qdev, t->queue_buf[i],
-				       &t->queue_cfg))
+		if (dp_qos_queue_conf_get(priv->qdev, t->queue_buf[i],
+					  &t->queue_cfg))
 			continue;
 		t->f_restore = 0;
 		if (t->q_orig_block[i] >= 0) {
@@ -3055,7 +3036,8 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 
 		if (!t->f_restore)
 			continue;
-		if (qos_queue_set(priv->qdev, t->queue_buf[i], &t->queue_cfg)) {
+		if (dp_qos_queue_set(priv->qdev, t->queue_buf[i],
+				     &t->queue_cfg)) {
 			pr_err("qos_queue_set fail for q[/%d]\n",
 			       t->queue_buf[i]);
 			res = DP_FAILURE;
@@ -3074,11 +3056,11 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		if (t->node.type == DP_NODE_QUEUE) {
 			DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 				 "queue remove id %d\n", t->node_id);
-			qos_queue_remove(priv->qdev, t->node_id);
+			dp_qos_queue_remove(priv->qdev, t->node_id);
 		} else if (t->node.type == DP_NODE_SCH) {
 			DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 				 "sched remove id %d\n", t->node.id.sch_id);
-			qos_sched_remove(priv->qdev, t->node.id.sch_id);
+			dp_qos_sched_remove(priv->qdev, t->node.id.sch_id);
 		} else {
 			pr_err("Unexpect node type %d\n", t->node.type);
 		}
@@ -3087,11 +3069,11 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		if (info->p_node_type == DP_NODE_PORT) {
 			DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 				 "port remove id %d\n", t->parent.node_id);
-			qos_port_remove(priv->qdev, t->parent.node_id);
+			dp_qos_port_remove(priv->qdev, t->parent.node_id);
 		} else if (info->p_node_type == DP_NODE_SCH) {
 			DP_DEBUG(DP_DBG_FLAG_QOS_DETAIL,
 				 "sched remove id %d\n", t->parent.node_id);
-			qos_sched_remove(priv->qdev, t->parent.node_id);
+			dp_qos_sched_remove(priv->qdev, t->parent.node_id);
 		} else {
 			pr_err("Unexpect node type %d\n", t->node.type);
 		}
@@ -3137,7 +3119,7 @@ int dp_queue_conf_set_31(struct dp_queue_conf *cfg, int flag)
 	}
 	node_id = priv->qos_queue_stat[cfg->q_id].node_id;
 
-	if (qos_queue_conf_get(priv->qdev, node_id, conf)) {
+	if (dp_qos_queue_conf_get(priv->qdev, node_id, conf)) {
 		pr_err("qos_queue_conf_get fail:%d\n", cfg->q_id);
 		goto EXIT;
 	}
@@ -3163,7 +3145,7 @@ int dp_queue_conf_set_31(struct dp_queue_conf *cfg, int flag)
 		conf->wred_min_guaranteed = cfg->wred_min_guaranteed;
 		conf->wred_max_allowed = cfg->wred_max_allowed;
 	}
-	if (qos_queue_set(priv->qdev, node_id, conf)) {
+	if (dp_qos_queue_set(priv->qdev, node_id, conf)) {
 		pr_err("failed to qos_queue_set:%d\n", cfg->q_id);
 		goto EXIT;
 	}
@@ -3210,7 +3192,7 @@ int dp_queue_conf_get_31(struct dp_queue_conf *cfg, int flag)
 	}
 	node_id = priv->qos_queue_stat[cfg->q_id].node_id;
 
-	if (qos_queue_conf_get(priv->qdev, node_id, conf)) {
+	if (dp_qos_queue_conf_get(priv->qdev, node_id, conf)) {
 		pr_err("qos_queue_conf_get fail\n");
 		goto EXIT;
 	}
@@ -3700,7 +3682,7 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 		}
 		node_id = priv->qos_queue_stat[cfg->id.q_id].node_id;
 
-		if (qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
+		if (dp_qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("qos_queue_conf_get fail:%d\n", cfg->id.q_id);
 			return DP_FAILURE;
 		}
@@ -3722,7 +3704,7 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 			return DP_FAILURE;
 		}
 
-		if (qos_queue_set(priv->qdev, node_id, &queue_cfg)) {
+		if (dp_qos_queue_set(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("qos_queue_set fail:%d\n", cfg->id.q_id);
 			return DP_FAILURE;
 		}
@@ -3739,8 +3721,8 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 			return DP_FAILURE;
 		}
 
-		if (qos_sched_conf_get(priv->qdev, cfg->id.sch_id,
-				       &sched_cfg)) {
+		if (dp_qos_sched_conf_get(priv->qdev, cfg->id.sch_id,
+					  &sched_cfg)) {
 			pr_err("qos_sched_conf_get fail:%d\n", cfg->id.sch_id);
 			return DP_FAILURE;
 		}
@@ -3762,7 +3744,7 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 			return DP_FAILURE;
 		}
 
-		if (qos_sched_set(priv->qdev, cfg->id.sch_id, &sched_cfg)) {
+		if (dp_qos_sched_set(priv->qdev, cfg->id.sch_id, &sched_cfg)) {
 			pr_err("qos_sched_set fail:%d\n", cfg->id.sch_id);
 			return DP_FAILURE;
 		}
@@ -3781,7 +3763,7 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 		}
 		node_id = priv->deq_port_stat[cfg->id.cqm_deq_port].node_id;
 
-		if (qos_port_conf_get(priv->qdev, node_id, &port_cfg)) {
+		if (dp_qos_port_conf_get(priv->qdev, node_id, &port_cfg)) {
 			pr_err("qos_port_conf_get fail:%d\n",
 			       cfg->id.cqm_deq_port);
 			return DP_FAILURE;
@@ -3804,7 +3786,7 @@ int dp_shaper_conf_set_31(struct dp_shaper_conf *cfg, int flag)
 			return DP_FAILURE;
 		}
 
-		if (qos_port_set(priv->qdev, node_id, &port_cfg)) {
+		if (dp_qos_port_set(priv->qdev, node_id, &port_cfg)) {
 			pr_err("qos_port_set fail:%d\n", cfg->id.cqm_deq_port);
 			return DP_FAILURE;
 		}
@@ -3852,7 +3834,7 @@ int dp_shaper_conf_get_31(struct dp_shaper_conf *cfg, int flag)
 		}
 		node_id = priv->qos_queue_stat[cfg->id.q_id].node_id;
 
-		if (qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
+		if (dp_qos_queue_conf_get(priv->qdev, node_id, &queue_cfg)) {
 			pr_err("qos_queue_conf_get fail:%d\n", cfg->id.q_id);
 			return DP_FAILURE;
 		}
@@ -3876,8 +3858,8 @@ int dp_shaper_conf_get_31(struct dp_shaper_conf *cfg, int flag)
 			return DP_FAILURE;
 		}
 
-		if (qos_sched_conf_get(priv->qdev, cfg->id.sch_id,
-				       &sched_cfg)) {
+		if (dp_qos_sched_conf_get(priv->qdev, cfg->id.sch_id,
+					  &sched_cfg)) {
 			pr_err("qos_sched_conf_get fail:%d\n", cfg->id.sch_id);
 			return DP_FAILURE;
 		}
@@ -3903,7 +3885,7 @@ int dp_shaper_conf_get_31(struct dp_shaper_conf *cfg, int flag)
 			return DP_FAILURE;
 		}
 		node_id = priv->deq_port_stat[cfg->id.cqm_deq_port].node_id;
-		if (qos_port_conf_get(priv->qdev, node_id, &port_cfg)) {
+		if (dp_qos_port_conf_get(priv->qdev, node_id, &port_cfg)) {
 			pr_err("qos_port_conf_get fail:%d\n",
 			       cfg->id.cqm_deq_port);
 			return DP_FAILURE;
@@ -4235,7 +4217,7 @@ static int dp_queue_alloc_conf(int inst, int *logical_id, int *qid)
 	struct pp_qos_queue_info info;
 	int res = DP_SUCCESS;
 
-	if (qos_queue_allocate_id_phy(priv->qdev, &id, &info.physical_id)) {
+	if (dp_qos_queue_allocate_id_phy(priv->qdev, &id, &info.physical_id)) {
 		pr_err("qos_queue_allocate failed\n");
 		return DP_FAILURE;
 	}
@@ -4320,7 +4302,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 clear_q:
 	/* clear other non-continuously allocated Q */
 	for (i = 0; i < curr_off; i++)
-		qos_queue_remove(priv->qdev, tmp_resv_q[i].id);
+		dp_qos_queue_remove(priv->qdev, tmp_resv_q[i].id);
 	devm_kfree(&g_dp_dev->dev, tmp_resv_q);
 	return res;
 FREE_EXIT:
@@ -4328,7 +4310,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 		struct resv_q  *resv_q = priv->resv[ep].resv_q;
 
 		for (i = 0; i < priv->resv[ep].num_resv_q; i++)
-			qos_queue_remove(priv->qdev, resv_q[i].id);
+			dp_qos_queue_remove(priv->qdev, resv_q[i].id);
 		devm_kfree(&g_dp_dev->dev, resv_q);
 		priv->resv[ep].resv_q = NULL;
 		priv->resv[ep].num_resv_q = 0;
@@ -4375,8 +4357,8 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 	DP_DEBUG(DP_DBG_FLAG_QOS, "queue size =%d for ep=%d\n", len, ep);
 	resv_q = priv->resv[ep].resv_q;
 	for (i = 0; i < data->num_resv_q; i++) {
-		if (qos_queue_allocate_id_phy(priv->qdev, &id,
-					      &info.physical_id)) {
+		if (dp_qos_queue_allocate_id_phy(priv->qdev, &id,
+						 &info.physical_id)) {
 			res = DP_FAILURE;
 			pr_err("qos_queue_allocate failed\n");
 			goto FREE_EXIT;
@@ -4405,7 +4387,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 		}
 		p_sch = priv->resv[ep].resv_sched;
 		for (i = 0; i < data->num_resv_sched; i++) {
-			if (qos_sched_allocate(priv->qdev, &id)) {
+			if (dp_qos_sched_allocate(priv->qdev, &id)) {
 				res = DP_FAILURE;
 				pr_err("qos_queue_allocate failed\n");
 				goto FREE_EXIT;
@@ -4425,7 +4407,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 		struct resv_q  *resv_q = priv->resv[ep].resv_q;
 
 		for (i = 0; i < priv->resv[ep].num_resv_q; i++)
-			qos_queue_remove(priv->qdev, resv_q[i].id);
+			dp_qos_queue_remove(priv->qdev, resv_q[i].id);
 		devm_kfree(&g_dp_dev->dev, resv_q);
 		priv->resv[ep].resv_q = NULL;
 		priv->resv[ep].num_resv_q = 0;
@@ -4434,7 +4416,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 		struct resv_sch *resv_sch = priv->resv[ep].resv_sched;
 
 		for (i = 0; i < priv->resv[ep].num_resv_sched; i++)
-			qos_sched_remove(priv->qdev, resv_sch[i].id);
+			dp_qos_sched_remove(priv->qdev, resv_sch[i].id);
 		devm_kfree(&g_dp_dev->dev, resv_sch);
 		priv->resv[ep].resv_sched = NULL;
 		priv->resv[ep].num_resv_sched = 0;
@@ -4492,13 +4474,13 @@ int dp_qos_port_conf_set_31(struct dp_port_cfg_info *info, int flag)
 	node_id = priv->deq_port_stat[info->pid].node_id;
 	DP_DEBUG(DP_DBG_FLAG_QOS, "%s cqm_deq:%d, qos_port:%d\n",
 		 __func__, info->pid, node_id);
-	if (qos_port_conf_get(priv->qdev, node_id, &port_cfg)) {
-		pr_err("failed qos_port_conf_get\n");
+	if (dp_qos_port_conf_get(priv->qdev, node_id, &port_cfg)) {
+		pr_err("failed qos_port_conf_get_32\n");
 		return DP_FAILURE;
 	}
 	port_cfg.green_threshold = info->green_threshold;
 	port_cfg.yellow_threshold = info->yellow_threshold;
-	if (qos_port_set(priv->qdev, node_id, &port_cfg)) {
+	if (dp_qos_port_set(priv->qdev, node_id, &port_cfg)) {
 		pr_err("fail to set yellow:%d green:%d for node:%d\n",
 		       info->yellow_threshold, info->green_threshold, node_id);
 		return DP_FAILURE;
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_proc.c b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
index 3ed356c2458c..8dfc6a1cc37a 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_proc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/module.h>
@@ -111,8 +107,8 @@ ssize_t proc_parser_write(struct file *file, const char *buf,
 			 mpe2, mpe1, cpu);
 		dp_set_gsw_parser_31(flag, cpu, mpe1, mpe2, mpe3);
 	} else if (dp_strncmpi(param_list[0],
-				"disable",
-				strlen("disable")) == 0) {
+			       "disable",
+			       strlen("disable")) == 0) {
 		for (i = 1; i < num; i++) {
 			if (dp_strncmpi(param_list[i],
 					"cpu",
@@ -156,8 +152,8 @@ ssize_t proc_parser_write(struct file *file, const char *buf,
 			 mpe2, mpe1, cpu);
 		dp_set_gsw_parser_31(flag, cpu, mpe1, mpe2, mpe3);
 	} else if (dp_strncmpi(param_list[0],
-				"refresh",
-				strlen("refresh")) == 0) {
+			       "refresh",
+			       strlen("refresh")) == 0) {
 		dp_get_gsw_parser_31(NULL, NULL, NULL, NULL);
 		pr_info("value:cpu=%d mpe1=%d mpe2=%d mpe3=%d\n", pinfo[0].v,
 			pinfo[1].v, pinfo[2].v, pinfo[3].v);
@@ -248,7 +244,7 @@ char *get_bp_member_string(int inst, u16 bp, char *buf, u32 len)
 	buf[0] = 0;
 	bp_cfg.nBridgePortId = bp;
 	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP |
-		GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID;
+		       GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID;
 	ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, &bp_cfg);
 	if (ret != GSW_statusOk) {
 		pr_err("Failed to get bridge port's member for bridgeport=%d\n",
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_rx.c b/drivers/net/datapath/dpm/gswip31/datapath_rx.c
index 04ce632c6630..1400535aa390 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_rx.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_rx.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/kernel.h>
@@ -36,7 +32,7 @@ static void rx_dbg(u32 f, struct sk_buff *skb, struct dma_rx_desc_0 *desc0,
 		 paser_exist);
 	if ((f) & DP_DBG_FLAG_DUMP_RX_DESCRIPTOR)
 		dp_port_prop[inst].info.dump_rx_dma_desc(desc0, desc1,
-							 desc2, desc3);
+				desc2, desc3);
 	if (paser_exist && (dp_dbg_flag & DP_DBG_FLAG_DUMP_RX_PASER))
 		dump_parser_flag(parser);
 	if ((f) & DP_DBG_FLAG_DUMP_RX_PMAC)
@@ -199,8 +195,8 @@ int32_t dp_rx_31(struct sk_buff *skb, u32 flags)
 
 	if (port_id == PMAC_CPU_ID) { /*To CPU and need check src pmac port */
 		dp_port_prop[inst].info.update_port_vap(inst, &port_id, &vap,
-			skb,
-			pmac, &decryp);
+							skb,
+							pmac, &decryp);
 	} else {		/*GSWIP-R already know the destination */
 		rx_tx_flag = 1;
 		vap = GET_VAP(desc_0->field.dest_sub_if_id,
@@ -267,9 +263,9 @@ int32_t dp_rx_31(struct sk_buff *skb, u32 flags)
 						 "pmac to top drv");
 				dp_dump_raw_data(skb->data + PMAC_SIZE,
 						 ((skb->len - PMAC_SIZE) >
-							dp_print_len) ?
-							skb->len - PMAC_SIZE :
-							dp_print_len,
+						  dp_print_len) ?
+						 skb->len - PMAC_SIZE :
+						 dp_print_len,
 						 "Data to top drv");
 			}
 			if (dp_dbg_flag & DP_DBG_FLAG_DUMP_RX_DESCRIPTOR)
@@ -282,7 +278,7 @@ int32_t dp_rx_31(struct sk_buff *skb, u32 flags)
 			ltq_mpe_fasthook_rx_fn(skb, 1, NULL);	/*with pmac */
 #endif
 		if (unlikely((enum TEST_MODE)dp_rx_test_mode ==
-			DP_RX_MODE_LAN_WAN_BRIDGE)) {
+			     DP_RX_MODE_LAN_WAN_BRIDGE)) {
 			/*for datapath performance test only */
 			dp_lan_wan_bridging(port_id, skb);
 			/*return DP_SUCCESS;*/
@@ -291,9 +287,9 @@ int32_t dp_rx_31(struct sk_buff *skb, u32 flags)
 		 *avoid forwarding duplicate packets from linux
 		 */
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-			if (sif->fid > 0)
-				skb->offload_fwd_mark = 1;
-		#endif
+		if (sif->fid > 0)
+			skb->offload_fwd_mark = 1;
+#endif
 		if (rx_tx_flag == 0) {
 			if (dp_port->lct_idx > 0)
 				ret_lct = dp_handle_lct(dp_port, skb, rx_fn);
@@ -318,7 +314,7 @@ int32_t dp_rx_31(struct sk_buff *skb, u32 flags)
 	}
 
 	if (unlikely(port_id >=
-	    dp_port_prop[inst].info.cap.max_num_dp_ports - 1)) {
+		     dp_port_prop[inst].info.cap.max_num_dp_ports - 1)) {
 		pr_err("Drop for wrong ep or src port id=%u ??\n",
 		       port_id);
 		goto RX_DROP;
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
index ff40a7a32ff2..dbd15d200ad4 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
@@ -1,9 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 #include <linux/kernel.h>
@@ -460,14 +457,14 @@ int dp_gswip_ext_vlan(int inst, int vap, int ep)
 					 vlan_prop.out_proto,
 					 vlan_prop.out_vid);
 				vlan->vlan2_list[v2].outer_vlan.vid =
-							vlan_prop.out_vid;
+					vlan_prop.out_vid;
 				vlan->vlan2_list[v2].outer_vlan.tpid =
-							vlan_prop.out_proto;
+					vlan_prop.out_proto;
 				vlan->vlan2_list[v2].ether_type = 0;
 				vlan->vlan2_list[v2].inner_vlan.vid =
-							vlan_prop.in_vid;
+					vlan_prop.in_vid;
 				vlan->vlan2_list[v2].inner_vlan.tpid =
-							vlan_prop.in_proto;
+					vlan_prop.in_proto;
 				vlan->vlan2_list[v2].bp = tmp->bp;
 				v2 += 1;
 			} else if (vlan_prop.num == 1) {
@@ -476,9 +473,9 @@ int dp_gswip_ext_vlan(int inst, int vap, int ep)
 					 vlan_prop.out_proto,
 					 vlan_prop.out_vid);
 				vlan->vlan1_list[v1].outer_vlan.vid =
-							vlan_prop.out_vid;
+					vlan_prop.out_vid;
 				vlan->vlan1_list[v1].outer_vlan.tpid =
-							vlan_prop.out_proto;
+					vlan_prop.out_proto;
 				vlan->vlan1_list[v1].bp = tmp->bp;
 				v1 += 1;
 			}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h
index e14b44551dea..1da2b321a799 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h
@@ -1,9 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 #ifndef DATAPATH_SWITCHDEV_H_
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_tc_asym_vlan.c b/drivers/net/datapath/dpm/gswip31/datapath_tc_asym_vlan.c
index ea13041a8999..ce36f9ed5aef 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_tc_asym_vlan.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_tc_asym_vlan.c
@@ -1,9 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 #include <net/switch_api/gsw_flow_ops.h>
@@ -29,7 +26,7 @@ static int update_bp(struct core_ops *ops,
 	bpcfg2.nBridgePortId = bpid;
 	if (ingress) {
 		bpcfg1.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_VLAN_FILTER
-				| GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_VLAN;
+			       | GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_VLAN;
 		if (!pfilter) {
 			bpcfg2.bIngressVlanFilterEnable = LTQ_FALSE;
 		} else {
@@ -48,7 +45,7 @@ static int update_bp(struct core_ops *ops,
 		}
 	} else {
 		bpcfg1.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_VLAN_FILTER1
-				| GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_VLAN;
+			       | GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_VLAN;
 		if (!pfilter) {
 			bpcfg2.bEgressVlanFilter1Enable = LTQ_FALSE;
 		} else {
@@ -131,10 +128,10 @@ static int update_ctp(struct core_ops *ops,
 				GSW_CTP_PORT_CONFIG_MASK_INGRESS_VLAN_IGMP;
 			if (!pextvlan) {
 				ctpcfg2.bIngressExtendedVlanIgmpEnable =
-								LTQ_FALSE;
+					LTQ_FALSE;
 			} else {
 				ctpcfg2.bIngressExtendedVlanIgmpEnable =
-								LTQ_TRUE;
+					LTQ_TRUE;
 				ctpcfg2.nIngressExtendedVlanBlockIdIgmp =
 					pextvlan->nExtendedVlanBlockId;
 				ctpcfg2.nIngressExtendedVlanBlockSizeIgmp = 0;
@@ -157,10 +154,10 @@ static int update_ctp(struct core_ops *ops,
 				GSW_CTP_PORT_CONFIG_MASK_EGRESS_VLAN_IGMP;
 			if (!pextvlan) {
 				ctpcfg2.bEgressExtendedVlanIgmpEnable =
-								LTQ_FALSE;
+					LTQ_FALSE;
 			} else {
 				ctpcfg2.bEgressExtendedVlanIgmpEnable =
-								LTQ_TRUE;
+					LTQ_TRUE;
 				ctpcfg2.nEgressExtendedVlanBlockIdIgmp =
 					pextvlan->nExtendedVlanBlockId;
 				ctpcfg2.nEgressExtendedVlanBlockSizeIgmp = 0;
@@ -406,10 +403,10 @@ static int tc_vlan_filter(struct core_ops *ops,
 				       0xFFF;
 			pcfg[j].nVal |= ((u32)
 					 vlan->vlan2_list[i].outer.prio & 7)
-					 << 13;
+					<< 13;
 			pcfg[j].nVal |= ((u32)
 					 vlan->vlan2_list[i].outer.dei & 1)
-					 << 12;
+					<< 12;
 			break;
 		default:
 			goto EXIT;
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_tx.c b/drivers/net/datapath/dpm/gswip31/datapath_tx.c
index cf005dfe69f6..aa0df7797e9d 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_tx.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_tx.c
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #include <linux/kernel.h>
@@ -51,11 +47,11 @@ void dp_xmit_dbg(
 			 "inner ip start=0x%x(%d), transport=0x%x(%d)\n",
 			 (unsigned int)skb_inner_network_header(skb),
 			 (int)(skb_inner_network_header(skb) -
-			 skb->data),
+			       skb->data),
 			 (unsigned int)
 			 skb_inner_transport_header(skb),
 			 (int)(skb_inner_transport_header(skb) -
-			 skb_inner_network_header(skb)));
+			       skb_inner_network_header(skb)));
 	else
 		DP_DEBUG(DP_DBG_FLAG_DUMP_TX_SUM,
 			 "ip start=0x%x(%d), transport=0x%x(%d)\n",
@@ -64,15 +60,15 @@ void dp_xmit_dbg(
 			 (int)(skb_network_header(skb) - skb->data),
 			 (unsigned int)skb_transport_header(skb),
 			 (int)(skb_transport_header(skb) -
-			 skb_network_header(skb)));
+			       skb_network_header(skb)));
 
 	if (dp_dbg_flag & DP_DBG_FLAG_DUMP_TX_DESCRIPTOR)
 #if defined(DP_SKB_HACK)
 		dp_port_prop[0].info.dump_tx_dma_desc(
-				(struct dma_tx_desc_0 *)&skb->DW0,
-				(struct dma_tx_desc_1 *)&skb->DW1,
-				(struct dma_tx_desc_2 *)&skb->DW2,
-				(struct dma_tx_desc_3 *)&skb->DW3);
+			(struct dma_tx_desc_0 *)&skb->DW0,
+			(struct dma_tx_desc_1 *)&skb->DW1,
+			(struct dma_tx_desc_2 *)&skb->DW2,
+			(struct dma_tx_desc_3 *)&skb->DW3);
 #endif
 
 	DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "flags=0x%x skb->len=%d\n",
@@ -100,8 +96,7 @@ void dp_xmit_dbg(
 	DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "\n\n");
 }
 
-static int dp_reinsert_pkt_cnt(struct pmac_port_info *dp_info,
-			       int vap, struct sk_buff *skb)
+static int dp_reinsert_pkt_cnt(struct pmac_port_info *dp_info, int vap, struct sk_buff *skb)
 {
 	struct dp_reinsert_count *stats;
 	int inst = 0;
diff --git a/include/net/datapath_api.h b/include/net/datapath_api.h
index ad07de0ea1b3..ea982b318add 100644
--- a/include/net/datapath_api.h
+++ b/include/net/datapath_api.h
@@ -1,10 +1,7 @@
+// SPDX-License-Identifier: GPL-2.0
 /*
  * Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_API_H
@@ -122,28 +119,6 @@
 				  */
 #define DP_TS_HDRLEN	10    /*!< Befault Timestamp Header Length to strip */
 #define DP_DFL_SESS_NUM 16    /*!< Maximum default egress session per subif */
-/* Note: In LGM kernel (4.19) branch,
- * DMA driver already adapted to linux kernel
- * DMA framework and some DMA macro (like
- * _DMA_CONTROLLER/_DMA_PORT/_DMA_CHANNEL)
- * was removed. In order to keep the interface between DP and CQM/UMT driver
- * same, DP has defined DMA macro accordingly
- */
-#define _DMA_CHANBITS   16
-#define _DMA_PORTBITS   8
-#define _DMA_CTRLBITS   8
-
-#define _DMA_CHANMASK   ((1 << _DMA_CHANBITS) - 1)
-#define _DMA_PORTMASK   ((1 << _DMA_PORTBITS) - 1)
-#define _DMA_CTRLMASK   ((1 << _DMA_CTRLBITS) - 1)
-
-#define _DMA_CHANSHIFT  0
-#define _DMA_PORTSHIFT  (_DMA_CHANSHIFT + _DMA_CHANBITS)
-#define _DMA_CTRLSHIFT  (_DMA_PORTSHIFT + _DMA_PORTBITS)
-
-#define _DMA_CONTROLLER(nr)     (((nr) >> _DMA_CTRLSHIFT) & _DMA_CTRLMASK)
-#define _DMA_PORT(nr)           (((nr) >> _DMA_PORTSHIFT) & _DMA_PORTMASK)
-#define _DMA_CHANNEL(nr)        (((nr) >> _DMA_CHANSHIFT) & _DMA_CHANMASK)
 
 /*! @addtogroup Datapath_Driver_Structures */
 /*! @brief  DP Sub-interface Data structure
diff --git a/include/net/datapath_api_gswip30.h b/include/net/datapath_api_gswip30.h
index b169f8c505d7..acec90d2af40 100644
--- a/include/net/datapath_api_gswip30.h
+++ b/include/net/datapath_api_gswip30.h
@@ -1,10 +1,7 @@
+// SPDX-License-Identifier: GPL-2.0
 /*
  * Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_API_GRX500_H
diff --git a/include/net/datapath_api_gswip31.h b/include/net/datapath_api_gswip31.h
index 9537238f5bbc..0220b7b7cb6f 100644
--- a/include/net/datapath_api_gswip31.h
+++ b/include/net/datapath_api_gswip31.h
@@ -1,11 +1,8 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
+
 #ifndef DATAPATH_API_PRX300_H
 #define DATAPATH_API_PRX300_H
 
diff --git a/include/net/datapath_api_ppv4.h b/include/net/datapath_api_ppv4.h
index 929de511264a..81bf6652ac5f 100644
--- a/include/net/datapath_api_ppv4.h
+++ b/include/net/datapath_api_ppv4.h
@@ -1,11 +1,8 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
+
 #ifndef DATAPATH_API_PPV4_H
 #define DATAPATH_API_PPV4_H
 
diff --git a/include/net/datapath_api_qos.h b/include/net/datapath_api_qos.h
index 6a52b42d4340..35ccfcf3e3af 100644
--- a/include/net/datapath_api_qos.h
+++ b/include/net/datapath_api_qos.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
- * Author Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
  */
 
 /*!
diff --git a/include/net/datapath_api_skb.h b/include/net/datapath_api_skb.h
index 9b49c8b8a53e..e132a5c30020 100644
--- a/include/net/datapath_api_skb.h
+++ b/include/net/datapath_api_skb.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_API_SKB_H
diff --git a/include/net/datapath_api_tx.h b/include/net/datapath_api_tx.h
index 8df16f7c9339..0b0a45c369cf 100644
--- a/include/net/datapath_api_tx.h
+++ b/include/net/datapath_api_tx.h
@@ -1,3 +1,8 @@
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
+ * Author: Shao Guohua <guohua.shao@intel.com>
+ */
+
 #ifndef _DATAPATH_API_TX_H
 #define _DATAPATH_API_TX_H
 
diff --git a/include/net/datapath_api_umt.h b/include/net/datapath_api_umt.h
index 8aa22019bae0..e702c5f3b769 100644
--- a/include/net/datapath_api_umt.h
+++ b/include/net/datapath_api_umt.h
@@ -1,6 +1,5 @@
 /* SPDX-License-Identifier: GPL-2.0 */
-/*
- *  Copyright (C) 2018 Intel Corporation.
+/* Copyright (C) 2018 Intel Corporation.
  *  Wu ZhiXian <zhixian.wu@intel.com>
  */
 
diff --git a/include/net/datapath_api_vlan.h b/include/net/datapath_api_vlan.h
index 87ba3df4d986..528ad41ea050 100644
--- a/include/net/datapath_api_vlan.h
+++ b/include/net/datapath_api_vlan.h
@@ -1,11 +1,8 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
+
 #ifndef DATAPATH_VLAN_H
 #define DATAPATH_VLAN_H
 #include <linux/if_ether.h>
diff --git a/include/net/datapath_inst.h b/include/net/datapath_inst.h
index d79ed5457058..fd8dccd29c45 100644
--- a/include/net/datapath_inst.h
+++ b/include/net/datapath_inst.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_INST_H
diff --git a/include/net/datapath_proc_api.h b/include/net/datapath_proc_api.h
index 938cab237501..64d4d53b1f3d 100644
--- a/include/net/datapath_proc_api.h
+++ b/include/net/datapath_proc_api.h
@@ -1,10 +1,6 @@
-/*
- * Copyright (C) Intel Corporation
+// SPDX-License-Identifier: GPL-2.0
+/* Copyright (C) Intel Corporation
  * Author: Shao Guohua <guohua.shao@intel.com>
- *
- * This program is free software; you can redistribute it and/or modify it
- * under the terms of the GNU General Public License version 2 as published
- * by the Free Software Foundation.
  */
 
 #ifndef DATAPATH_PROC_H
