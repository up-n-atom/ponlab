From 49bba9d61c696208aec17ce3c2d6da2f38f30830 Mon Sep 17 00:00:00 2001
From: Rekha Eswaran <rekha.eswaran@intel.com>
Date: Thu, 12 Dec 2019 11:27:08 +0800
Subject: [PATCH] Revert "DRV_DPM_SW-61 - change kmalloc to devm_kzalloc as per
 review comments"

This reverts commit 8f6fd2e3f8cd1e945caf20c0364c3ee150b114d4.
---
 drivers/net/datapath/dpm/datapath_api.c            | 34 +++++-----
 drivers/net/datapath/dpm/datapath_instance.c       | 13 ++--
 drivers/net/datapath/dpm/datapath_logical_dev.c    |  5 +-
 drivers/net/datapath/dpm/datapath_misc.c           | 40 ++++++------
 drivers/net/datapath/dpm/datapath_proc_api.c       |  5 +-
 drivers/net/datapath/dpm/datapath_proc_qos.c       | 12 ++--
 drivers/net/datapath/dpm/datapath_qos.c            |  5 +-
 drivers/net/datapath/dpm/datapath_swdev.c          | 47 +++++++-------
 drivers/net/datapath/dpm/datapath_tx.c             |  5 +-
 drivers/net/datapath/dpm/gswip30/datapath_mib.c    |  8 +--
 drivers/net/datapath/dpm/gswip31/datapath_gswip.c  | 53 ++++++----------
 drivers/net/datapath/dpm/gswip31/datapath_misc.c   | 18 +++---
 drivers/net/datapath/dpm/gswip31/datapath_ppv4.c   | 21 ++++++-
 .../net/datapath/dpm/gswip31/datapath_ppv4_api.c   | 72 +++++++++++-----------
 drivers/net/datapath/dpm/gswip31/datapath_proc.c   |  5 +-
 .../net/datapath/dpm/gswip31/datapath_switchdev.c  | 32 ++++++----
 16 files changed, 188 insertions(+), 187 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index a10ec941e3f4..4c438963059e 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -1046,7 +1046,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 	port_info->res_qid_base = data->qos_resv_q_base;
 	port_info->num_resv_q = data->num_resv_q;
 
-	cbm_data = devm_kzalloc(&g_dp_dev->dev, sizeof(*cbm_data), GFP_ATOMIC);
+	cbm_data = kzalloc(sizeof(*cbm_data), GFP_ATOMIC);
 	if (!cbm_data) {
 		DP_LIB_UNLOCK(&dp_lock);
 		return res;
@@ -1054,7 +1054,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 
 	res = dp_build_cqm_data(inst, port_id, cbm_data, data);
 	if (res == DP_FAILURE) {
-		devm_kfree(&g_dp_dev->dev, cbm_data);
+		kfree(cbm_data);
 		DP_LIB_UNLOCK(&dp_lock);
 		return res;
 	}
@@ -1067,7 +1067,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 				       cbm_data,
 				       port_info->alloc_flags)) {
 		DP_DEBUG(DP_DBG_FLAG_REG, "CBM port alloc failed\n");
-		devm_kfree(&g_dp_dev->dev, cbm_data);
+		kfree(cbm_data);
 		DP_LIB_UNLOCK(&dp_lock);
 		return DP_FAILURE;
 	}
@@ -1075,7 +1075,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 	if (data->num_rx_ring || data->num_tx_ring) {
 		res = dp_register_dc(inst, port_id, cbm_data, data, flags);
 		if (res == DP_FAILURE) {
-			devm_kfree(&g_dp_dev->dev, cbm_data);
+			kfree(cbm_data);
 			DP_LIB_UNLOCK(&dp_lock);
 			return res;
 		}
@@ -1088,7 +1088,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 	if (dp_cb)
 		port_info->cb = *dp_cb;
 	DP_LIB_UNLOCK(&dp_lock);
-	devm_kfree(&g_dp_dev->dev, cbm_data);
+	kfree(cbm_data);
 
 	return DP_SUCCESS;
 }
@@ -1185,8 +1185,7 @@ int32_t dp_register_subif_ext(int inst, struct module *owner,
 	if (!(flags & DP_F_SUBIF_LOGICAL))
 		subifid_fn_t = port_info->cb.get_subifid_fn;
 
-	subif_id_sync = devm_kzalloc(&g_dp_dev->dev,
-				     sizeof(*subif_id_sync) * 2, GFP_ATOMIC);
+	subif_id_sync = kmalloc(sizeof(*subif_id_sync) * 2, GFP_ATOMIC);
 	if (!subif_id_sync) {
 		DP_LIB_UNLOCK(&dp_lock);
 		return DP_FAILURE;
@@ -1198,7 +1197,7 @@ int32_t dp_register_subif_ext(int inst, struct module *owner,
 	if (!res)
 		dp_sync_subifid_priv(dev, subif_name, subif_id_sync, data,
 				     flags, subifid_fn_t, &f_subif);
-	devm_kfree(&g_dp_dev->dev, subif_id_sync);
+	kfree(subif_id_sync);
 	return res;
 }
 EXPORT_SYMBOL(dp_register_subif_ext);
@@ -1305,16 +1304,13 @@ int32_t dp_get_netif_subifid_priv(struct net_device *netif, struct sk_buff *skb,
 	struct logic_dev *tmp = NULL;
 	u16 gpid = 0;
 
-	subifs = devm_kzalloc(&g_dp_dev->dev,
-			      sizeof(*subifs) * DP_MAX_CTP_PER_DEV,
-			      GFP_ATOMIC);
+	subifs = kmalloc(sizeof(*subifs) * DP_MAX_CTP_PER_DEV, GFP_ATOMIC);
 	if (!subifs)
 		return res;
-	subif_flag = devm_kzalloc(&g_dp_dev->dev,
-				  sizeof(*subif_flag) * DP_MAX_CTP_PER_DEV,
-				  GFP_ATOMIC);
+	subif_flag = kmalloc(sizeof(*subif_flag) * DP_MAX_CTP_PER_DEV,
+			     GFP_ATOMIC);
 	if (!subif_flag) {
-		devm_kfree(&g_dp_dev->dev, subifs);
+		kfree(subifs);
 		return res;
 	}
 	if (!netif && !subif_data) {
@@ -1465,8 +1461,8 @@ int32_t dp_get_netif_subifid_priv(struct net_device *netif, struct sk_buff *skb,
 	}
 	res = 0;
 EXIT:
-	devm_kfree(&g_dp_dev->dev, subifs);
-	devm_kfree(&g_dp_dev->dev, subif_flag);
+	kfree(subifs);
+	kfree(subif_flag);
 	return res;
 }
 
@@ -1943,7 +1939,7 @@ int dp_set_pmapper(struct net_device *dev, struct dp_pmapper *mapper, u32 flag)
 		pr_err("BP port(%d) out of range %d\n", bport, DP_MAX_BP_NUM);
 		return DP_FAILURE;
 	}
-	map = devm_kzalloc(&g_dp_dev->dev, sizeof(*map), GFP_ATOMIC);
+	map = kmalloc(sizeof(*map), GFP_ATOMIC);
 	if (!map)
 		return DP_FAILURE;
 	memcpy(map, mapper, sizeof(*map));
@@ -1979,7 +1975,7 @@ int dp_set_pmapper(struct net_device *dev, struct dp_pmapper *mapper, u32 flag)
 		dp_bp_dev_tbl[inst][bport].dscp[i] = map->dscp_map[i];
 	res = DP_SUCCESS;
 EXIT:
-	devm_kfree(&g_dp_dev->dev, map);
+	kfree(map);
 	return res;
 }
 EXPORT_SYMBOL(dp_set_pmapper);
diff --git a/drivers/net/datapath/dpm/datapath_instance.c b/drivers/net/datapath/dpm/datapath_instance.c
index d95f0716976f..83d0f2594a74 100644
--- a/drivers/net/datapath/dpm/datapath_instance.c
+++ b/drivers/net/datapath/dpm/datapath_instance.c
@@ -257,9 +257,11 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 		return -1;
 	}
 	idx = dp_dev_hash(dev, subif_name);
-	subif = devm_kzalloc(&g_dp_dev->dev, sizeof(*subif), GFP_KERNEL);
-	if (!subif)
+	subif = kmalloc(sizeof(*subif), GFP_KERNEL);
+	if (!subif) {
+		pr_err("failed to alloc %zd bytes\n", sizeof(*subif));
 		return -1;
+	}
 
 	subif->subif = ctp;
 	dp_dev = dp_dev_lookup(&dp_dev_list[idx], dev, subif_name, flag);
@@ -285,7 +287,8 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 		}
 	}
 	if (!dp_dev) {
-		devm_kfree(&g_dp_dev->dev, subif);
+		pr_err("Failed to kmalloc %zd bytes\n", sizeof(*dp_dev));
+		kfree(subif);
 		return -1;
 	}
 	if (new_f) {
@@ -434,8 +437,10 @@ int dp_inst_insert_mod(struct module *owner, u16 ep, u32 inst, u32 flag)
 			new_f = 1;
 		}
 	}
-	if (!dp_mod)
+	if (!dp_mod) {
+		pr_err("Failed to kmalloc %zd bytes\n", sizeof(*dp_mod));
 		return -1;
+	}
 	if (new_f)
 		hlist_add_head(&dp_mod->hlist, &dp_mod_list[idx]);
 	DP_DEBUG(DP_DBG_FLAG_INST, "dp_inst_insert_mod:%s\n", owner->name);
diff --git a/drivers/net/datapath/dpm/datapath_logical_dev.c b/drivers/net/datapath/dpm/datapath_logical_dev.c
index d274f8471909..b3291d816362 100644
--- a/drivers/net/datapath/dpm/datapath_logical_dev.c
+++ b/drivers/net/datapath/dpm/datapath_logical_dev.c
@@ -158,8 +158,7 @@ int add_logic_dev(int inst, int port_id, struct net_device *dev,
 			 dev->name);
 		return -1;
 	}
-	logic_dev_tmp = devm_kzalloc(&g_dp_dev->dev,
-				     sizeof(*logic_dev_tmp), GFP_KERNEL);
+	logic_dev_tmp = kmalloc(sizeof(*logic_dev_tmp), GFP_KERNEL);
 	if (!logic_dev_tmp) {
 		DP_DEBUG(DP_DBG_FLAG_LOGIC, "kmalloc fail for %zd bytes\n",
 			 sizeof(*logic_dev_tmp));
@@ -203,7 +202,7 @@ int del_logic_dev(int inst, struct list_head *head, struct net_device *dev,
 		logic_dev, flags);
 	dp_inst_del_dev(dev, NULL, inst, logic_dev->ep, logic_dev->ctp, 0);
 	list_del(&logic_dev->list);
-	devm_kfree(&g_dp_dev->dev, logic_dev);
+	kfree(logic_dev);
 
 	return 0;
 }
diff --git a/drivers/net/datapath/dpm/datapath_misc.c b/drivers/net/datapath/dpm/datapath_misc.c
index fc79d7725706..ecc36410bc87 100644
--- a/drivers/net/datapath/dpm/datapath_misc.c
+++ b/drivers/net/datapath/dpm/datapath_misc.c
@@ -140,7 +140,7 @@ void dump_parser_flag(char *buf)
 		pr_err("dump_parser_flag buf NULL\n");
 		return;
 	}
-	p = devm_kzalloc(&g_dp_dev->dev, 2000, GFP_KERNEL);
+	p = kmalloc(2000, GFP_KERNEL);
 	if (!p)
 		return;
 
@@ -183,7 +183,7 @@ void dump_parser_flag(char *buf)
 			}
 		}
 	}
-	devm_kfree(&g_dp_dev->dev, p);
+	kfree(p);
 }
 
 /*will be used at any context */
@@ -199,7 +199,7 @@ void dp_dump_raw_data(char *buf, int len, char *prefix_str)
 		pr_err("dp_dump_raw_data: p NULL ?\n");
 		return;
 	}
-	s = devm_kzalloc(&g_dp_dev->dev, bytes, GFP_KERNEL);
+	s = kmalloc(bytes, GFP_KERNEL);
 	if (!s)
 		return;
 	sprintf(s, "%s in hex at 0x%px\n",
@@ -213,7 +213,7 @@ void dp_dump_raw_data(char *buf, int len, char *prefix_str)
 		sprintf(s + l, "\n");
 		pr_info("%s", s);
 	}
-	devm_kfree(&g_dp_dev->dev, s);
+	kfree(s);
 }
 EXPORT_SYMBOL(dp_dump_raw_data);
 
@@ -1499,11 +1499,14 @@ u32 get_dma_chan_idx(int inst, int num_dma_chan)
  */
 u32 alloc_dma_chan_tbl(int inst)
 {
-	dp_dma_chan_tbl[inst] = devm_kzalloc(&g_dp_dev->dev,
-					     (sizeof(struct dma_chan_info) *
-					     DP_MAX_DMA_CHAN), GFP_ATOMIC);
-	if (!dp_dma_chan_tbl[inst])
+	dp_dma_chan_tbl[inst] = kzalloc((sizeof(struct dma_chan_info) *
+					DP_MAX_DMA_CHAN), GFP_ATOMIC);
+
+	if (!dp_dma_chan_tbl[inst]) {
+		pr_err("Failed for kmalloc: %zu bytes\n",
+		       (sizeof(struct dma_chan_info) * DP_MAX_DMA_CHAN));
 		return DP_FAILURE;
+	}
 	return DP_SUCCESS;
 }
 
@@ -1527,23 +1530,20 @@ u32 alloc_dp_port_subif_info(int inst)
 	max_dp_ports = info->cap.max_num_dp_ports;
 	max_subif = info->cap.max_num_subif_per_port;
 
-	dp_port_info[inst] = devm_kzalloc(&g_dp_dev->dev,
-					  (sizeof(struct pmac_port_info) *
-					  max_dp_ports), GFP_KERNEL);
+	dp_port_info[inst] = kzalloc((sizeof(struct pmac_port_info) *
+				     max_dp_ports), GFP_KERNEL);
 	if (!dp_port_info[inst])
 		return DP_FAILURE;
 	for (port_id = 0; port_id < max_dp_ports; port_id++) {
 		get_dp_port_info(inst, port_id)->subif_info =
-			devm_kzalloc(&g_dp_dev->dev,
-				     sizeof(struct dp_subif_info) * max_subif,
-				     GFP_KERNEL);
+			kzalloc(sizeof(struct dp_subif_info) * max_subif,
+				GFP_KERNEL);
 		if (!get_dp_port_info(inst, port_id)->subif_info) {
 			pr_err("Failed for kmalloc: %zu bytes\n",
 			       max_subif * sizeof(struct dp_subif_info));
 			while (--port_id >= 0)
-				devm_kfree(&g_dp_dev->dev,
-					   get_dp_port_info(inst,
-					   port_id)->subif_info);
+				kfree(get_dp_port_info(inst,
+						       port_id)->subif_info);
 			return DP_FAILURE;
 		}
 	}
@@ -1557,7 +1557,7 @@ u32 alloc_dp_port_subif_info(int inst)
 void free_dma_chan_tbl(int inst)
 {
 	/* free dma chan tbl */
-	devm_kfree(&g_dp_dev->dev, dp_dma_chan_tbl[inst]);
+	kfree(dp_dma_chan_tbl[inst]);
 }
 
 /**
@@ -1578,8 +1578,8 @@ void free_dp_port_subif_info(int inst)
 	if (dp_port_info[inst]) {
 		for (port_id = 0; port_id < max_dp_ports; port_id++) {
 			port_info = get_dp_port_info(inst, port_id);
-			devm_kfree(&g_dp_dev->dev, port_info->subif_info);
+			kfree(port_info->subif_info);
 		}
-		devm_kfree(&g_dp_dev->dev, dp_port_info[inst]);
+		kfree(dp_port_info[inst]);
 	}
 }
diff --git a/drivers/net/datapath/dpm/datapath_proc_api.c b/drivers/net/datapath/dpm/datapath_proc_api.c
index 5f8bbf66caf3..7b852e9fd4df 100644
--- a/drivers/net/datapath/dpm/datapath_proc_api.c
+++ b/drivers/net/datapath/dpm/datapath_proc_api.c
@@ -232,7 +232,8 @@ static int dp_proc_open(struct inode *inode, struct file *file)
 		return ret;
 
 	s = file->private_data;
-	p = devm_kzalloc(&g_dp_dev->dev, sizeof(*p), GFP_KERNEL);
+	p = kmalloc(sizeof(*p), GFP_KERNEL);
+
 	if (!p) {
 		(void)seq_release(inode, file);
 		return -ENOMEM;
@@ -263,7 +264,7 @@ static int dp_proc_release(struct inode *inode, struct file *file)
 	struct seq_file *s;
 
 	s = file->private_data;
-	devm_kfree(&g_dp_dev->dev, s->private);
+	kfree(s->private);
 
 	return seq_release(inode, file);
 }
diff --git a/drivers/net/datapath/dpm/datapath_proc_qos.c b/drivers/net/datapath/dpm/datapath_proc_qos.c
index d4bcf08d155d..913eaeab6f2e 100644
--- a/drivers/net/datapath/dpm/datapath_proc_qos.c
+++ b/drivers/net/datapath/dpm/datapath_proc_qos.c
@@ -504,7 +504,7 @@ void print_all(struct seq_file *s, struct q_print_info *q_info)
 
 	if (cols <= 0)
 		return;
-	buf = devm_kzalloc(&g_dp_dev->dev, cols * rows + 1, GFP_KERNEL);
+	buf = kmalloc(cols * rows + 1, GFP_KERNEL);
 	if (!buf)
 		return;
 	memset(buf, ' ', cols * rows);
@@ -530,7 +530,7 @@ void print_all(struct seq_file *s, struct q_print_info *q_info)
 		seq_printf(s, "%s\n", p);
 		p += cols;
 	}
-	devm_kfree(&g_dp_dev->dev, buf);
+	kfree(buf);
 }
 
 /* print_box_lvl must bigger 1 than sch_lvl */
@@ -553,7 +553,7 @@ struct q_print_info *collect_info(struct seq_file *s,
 	}
 
 	size = sizeof(*q_info) + 1;
-	q_info = devm_kzalloc(&g_dp_dev->dev, size, GFP_KERNEL);
+	q_info = kmalloc(size, GFP_KERNEL);
 	if (!q_info)
 		return NULL;
 	memset(q_info, 0, size);
@@ -635,7 +635,7 @@ struct q_print_info *collect_info(struct seq_file *s,
 	}
 	return q_info;
 ERR_EXIT:
-	devm_kfree(&g_dp_dev->dev, q_info);
+	kfree(q_info);
 	return NULL;
 }
 
@@ -890,11 +890,11 @@ int qos_dump(struct seq_file *s, int pos)
 		set_child_per_box(q_info);
 		set_location_size(&q_info->port_box, 0);
 		if (check_location(q_info)) {
-			devm_kfree(&g_dp_dev->dev, q_info);
+			kfree(q_info);
 			continue;
 		}
 		print_all(s, q_info);
-		devm_kfree(&g_dp_dev->dev, q_info);
+		kfree(q_info);
 	}
 NEXT_DP_PORT:
 	if (!seq_has_overflowed(s))
diff --git a/drivers/net/datapath/dpm/datapath_qos.c b/drivers/net/datapath/dpm/datapath_qos.c
index c3d5ac3c2606..e1ff86f9119b 100644
--- a/drivers/net/datapath/dpm/datapath_qos.c
+++ b/drivers/net/datapath/dpm/datapath_qos.c
@@ -153,8 +153,7 @@ int dp_deq_port_res_get(struct dp_dequeue_res *res, int flag)
 	if (!res || !dp_port_prop[res->inst].info.dp_qos_platform_set)
 		return DP_FAILURE;
 	if (res->dev) { /*fill dp_port if dev is provided */
-		subif = devm_kzalloc(&g_dp_dev->dev, sizeof(*subif),
-				     GFP_ATOMIC);
+		subif = kzalloc(sizeof(*subif), GFP_ATOMIC);
 		if (!subif)
 			return DP_FAILURE;
 		dp_get_netif_subifid(res->dev, NULL, NULL, NULL, subif, 0);
@@ -163,7 +162,7 @@ int dp_deq_port_res_get(struct dp_dequeue_res *res, int flag)
 			return DP_FAILURE;
 		}
 		res->dp_port = subif->port_id;
-		devm_kfree(&g_dp_dev->dev, subif);
+		kfree(subif);
 		subif = NULL;
 	}
 	port_info = get_dp_port_info(res->inst, res->dp_port);
diff --git a/drivers/net/datapath/dpm/datapath_swdev.c b/drivers/net/datapath/dpm/datapath_swdev.c
index 7598a2b0c154..214c1a23a6bf 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.c
+++ b/drivers/net/datapath/dpm/datapath_swdev.c
@@ -60,14 +60,6 @@ u16 dp_swdev_cal_hash(unsigned char *name)
 	return (u16)(hash & 0x3F);
 }
 
-/* dp_kfree - used as destructor for
- * switchdev_trans_item_enqueue() API
- */
-void dp_kfree(void const *data)
-{
-	devm_kfree(&g_dp_dev->dev, (void *)data);
-}
-
 struct hlist_head *get_dp_g_bridge_id_entry_hash_table_info(int instance,
 							    int index)
 {
@@ -209,7 +201,7 @@ static int dp_swdev_del_bport_from_list(struct br_info *br_item,
 	}
 	if (found) {
 		list_del(&temp_list->list);
-		devm_kfree(&g_dp_dev->dev, temp_list);
+		kfree(temp_list);
 		return 1;
 	}
 	return 0;
@@ -230,9 +222,8 @@ static int dp_swdev_add_bport_to_list(struct br_info *br_item,
 	}
 	if (found == 0) {
 		bport_list = (struct bridge_member_port *)
-			devm_kzalloc(&g_dp_dev->dev,
-				     sizeof(struct bridge_member_port),
-				     GFP_KERNEL);
+			kmalloc(sizeof(struct bridge_member_port),
+				GFP_KERNEL);
 		if (!bport_list) {
 			pr_err("\n Node creation failed\n");
 			return -1;
@@ -329,8 +320,7 @@ static int dp_swdev_filter_vlan(struct net_device *dev,
 			       __func__, dev->name);
 			return -EOPNOTSUPP;
 		}
-		br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item),
-				       GFP_KERNEL);
+		br_item = kmalloc(sizeof(*br_item), GFP_KERNEL);
 		if (!br_item)
 			//TODO need to check dequeue if no memory
 			return -ENOMEM;
@@ -345,11 +335,11 @@ static int dp_swdev_filter_vlan(struct net_device *dev,
 			br_item->fid = br_info->fid;
 		} else {
 			swdev_unlock();
-			devm_kfree(&g_dp_dev->dev, br_item);
+			kfree(br_item);
 			return -EOPNOTSUPP;
 		}
 		switchdev_trans_item_enqueue(trans, br_item,
-					     dp_kfree, &br_item->tritem);
+					     kfree, &br_item->tritem);
 		swdev_unlock();
 		return 0;
 	}
@@ -377,8 +367,7 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 
 		ret = i_info->swdev_bridge_cfg_set(br_item->inst, br_item->fid);
 		if (ret == 0) {
-			br_info = devm_kzalloc(&g_dp_dev->dev,
-					       sizeof(*br_info), GFP_KERNEL);
+			br_info = kmalloc(sizeof(*br_info), GFP_KERNEL);
 			if (!br_info)
 				/*TODO need to check return value
 				 *for switchdev commit
@@ -504,8 +493,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 	sif = get_dp_port_subif(p_info, vap);
 	/* SWITCHDEV_TRANS_PREPARE phase */
 	if (switchdev_trans_ph_prepare(trans)) {
-		br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item),
-				       GFP_KERNEL);
+		br_item = kmalloc(sizeof(*br_item), GFP_KERNEL);
 		if (!br_item)
 			/*TODO need to check dequeue if no memory*/
 			return -ENOMEM;
@@ -544,7 +532,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 					br_item->fid = br_id;
 				} else {
 					pr_err("Switch config failed\r\n");
-					devm_kfree(&g_dp_dev->dev, br_item);
+					kfree(br_item);
 					swdev_unlock();
 					return -EOPNOTSUPP;
 				}
@@ -554,7 +542,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 			}
 		}
 		switchdev_trans_item_enqueue(trans, br_item,
-					     dp_kfree, &br_item->tritem);
+					     kfree, &br_item->tritem);
 		swdev_unlock();
 		return 0;
 	}
@@ -602,8 +590,15 @@ static int dp_swdev_del_if(struct net_device *dev,
 	sif = get_dp_port_subif(p_info, vap);
 	/* SWITCHDEV_TRANS_PREPARE phase */
 	if (switchdev_trans_ph_prepare(trans)) {
-		br_item = devm_kzalloc(&g_dp_dev->dev,
-				       sizeof(*br_item), GFP_KERNEL);
+		/*Get current BR_PORT ID from DP*/
+		if (dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0)) {
+			DP_DEBUG(DP_DBG_FLAG_SWDEV,
+				 "dp_get_netif_subifid failed for %s\n",
+				 dev->name);
+		/*	if (!is_vlan_dev(dev))*/
+				return -EINVAL;
+		}
+		br_item = kmalloc(sizeof(*br_item), GFP_KERNEL);
 		if (!br_item)
 			/*TODO need to check dequeue if no memory*/
 			return -ENOMEM;
@@ -633,7 +628,7 @@ static int dp_swdev_del_if(struct net_device *dev,
 			br_item->flags = BRIDGE_NO_ACTION;
 		}
 		/*TODO check return value & enqueue*/
-		switchdev_trans_item_enqueue(trans, br_item, dp_kfree,
+		switchdev_trans_item_enqueue(trans, br_item, kfree,
 					     &br_item->tritem);
 		swdev_unlock();
 		return 0;
@@ -726,7 +721,7 @@ int dp_del_br_if(struct net_device *dev, struct net_device *br_dev,
 	if (br_item)
 		dp_swdev_clr_gswip_cfg(br_item, addr);
 	swdev_unlock();
-	devm_kfree(&g_dp_dev->dev, br_item);
+	kfree(br_item);
 	return 0;
 }
 
diff --git a/drivers/net/datapath/dpm/datapath_tx.c b/drivers/net/datapath/dpm/datapath_tx.c
index b6a045847427..a59e389bed7b 100644
--- a/drivers/net/datapath/dpm/datapath_tx.c
+++ b/drivers/net/datapath/dpm/datapath_tx.c
@@ -205,7 +205,7 @@ int dp_tx_update_list(void)
 	u8 i;
 
 	/* update list for fast read */
-	new_data = devm_kzalloc(&g_dp_dev->dev, sizeof(struct tx_hook_list),
+	new_data = devm_kmalloc(&g_dp_dev->dev, sizeof(struct tx_hook_list),
 				GFP_ATOMIC);
 	if (!new_data)
 		return -ENOMEM;
@@ -284,8 +284,7 @@ int dp_tx_init(int inst)
 {
 	if (inst)
 		return 0;
-	dp_tx_ctx = devm_kzalloc(&g_dp_dev->dev, sizeof(struct dp_tx_context),
-				 GFP_ATOMIC);
+	dp_tx_ctx = kzalloc(sizeof(struct dp_tx_context), GFP_ATOMIC);
 	if (!dp_tx_ctx)
 		return -ENOMEM;
 	spin_lock_init(&dp_tx_ctx->lock);
diff --git a/drivers/net/datapath/dpm/gswip30/datapath_mib.c b/drivers/net/datapath/dpm/gswip30/datapath_mib.c
index d8153d59fa91..a510166d5c22 100644
--- a/drivers/net/datapath/dpm/gswip30/datapath_mib.c
+++ b/drivers/net/datapath/dpm/gswip30/datapath_mib.c
@@ -579,7 +579,7 @@ static int update_port_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 				 subif->port_id);
 		return -1;
 	}
-	curr = devm_kzalloc(&g_dp_dev->dev, sizeof(*curr), GFP_KERNEL);
+	curr = kmalloc(sizeof(*curr), GFP_KERNEL);
 	if (!curr)
 		return -1;
 	memset(curr, 0, sizeof(*curr));
@@ -677,7 +677,7 @@ static int update_port_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 HANDLE_WRAPWROUND:
 	ret =
 	    port_mib_wraparound(tmp.port_id, curr, &last[tmp.port_id]);
-	devm_kfree(&g_dp_dev->dev, curr);
+	kfree(curr);
 	return ret;
 }
 
@@ -728,7 +728,7 @@ static int update_vap_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 	if (!subif || (subif->port_id <= 0) || (subif->port_id >= PMAC_MAX_NUM))
 		return -1;
 
-	curr = devm_kzalloc(&g_dp_dev->dev, sizeof(*curr), GFP_KERNEL);
+	curr = kmalloc(sizeof(*curr), GFP_KERNEL);
 	if (!curr)
 		return -1;
 	memset(curr, 0, sizeof(*curr));
@@ -796,7 +796,7 @@ static int update_vap_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 
 	ret = vap_mib_wraparound(subif, curr, &last_vap[port_id][vap]);
 EXIT:
-	devm_kfree(&g_dp_dev->dev, curr);
+	kfree(curr);
 
 	return ret;
 }
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
index 0d5c5c665402..dca320d0fdb8 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
@@ -709,8 +709,8 @@ int free_bridge_port(int inst, int bp)
 	if (bp == CPU_BP)
 		return 0;
 
-	tmp = devm_kzalloc(&g_dp_dev->dev, sizeof(*tmp), GFP_KERNEL);
-	tmp2 = devm_kzalloc(&g_dp_dev->dev, sizeof(*tmp2), GFP_KERNEL);
+	tmp = kzalloc(sizeof(*tmp), GFP_KERNEL);
+	tmp2 = kzalloc(sizeof(*tmp2), GFP_KERNEL);
 	if (!tmp || !tmp2)
 		goto FREE_EXIT;
 
@@ -755,8 +755,8 @@ int free_bridge_port(int inst, int bp)
 	if (ret != GSW_statusOk)
 		pr_err("Failed to GSW_BRIDGE_PORT_FREE:%d\n", bp);
 FREE_EXIT:
-	devm_kfree(&g_dp_dev->dev, tmp);
-	devm_kfree(&g_dp_dev->dev, tmp2);
+	kfree(tmp);
+	kfree(tmp2);
 	return 0;
 }
 
@@ -1154,8 +1154,7 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 
 	if (flag & DP_METER_ATTACH_PCE) {
 		/* pattern setting */
-		pce_rule = devm_kzalloc(&g_dp_dev->dev,
-					sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
+		pce_rule = kzalloc(sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
 		if (!pce_rule) {
 			bret = -1;
 			goto err;
@@ -1179,9 +1178,7 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		ctp_cfg = devm_kzalloc(&g_dp_dev->dev,
-				       sizeof(GSW_CTP_portConfig_t),
-				       GFP_ATOMIC);
+		ctp_cfg = kzalloc(sizeof(GSW_CTP_portConfig_t), GFP_ATOMIC);
 		if (!ctp_cfg) {
 			bret = -1;
 			goto err;
@@ -1228,9 +1225,7 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		bp_cfg = devm_kzalloc(&g_dp_dev->dev,
-				      sizeof(GSW_BRIDGE_portConfig_t),
-				      GFP_ATOMIC);
+		bp_cfg = kzalloc(sizeof(GSW_BRIDGE_portConfig_t), GFP_ATOMIC);
 		if (!bp_cfg) {
 			bret = -1;
 			goto err;
@@ -1277,9 +1272,7 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			goto err;
 		}
 	} else if (flag & DP_METER_ATTACH_BRIDGE) {
-		br_cfg = devm_kzalloc(&g_dp_dev->dev,
-				      sizeof(GSW_BRIDGE_config_t),
-				      GFP_ATOMIC);
+		br_cfg = kzalloc(sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
 		if (!br_cfg) {
 			bret = -1;
 			goto err;
@@ -1317,10 +1310,10 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 	}
 err:
-	devm_kfree(&g_dp_dev->dev, br_cfg);
-	devm_kfree(&g_dp_dev->dev, bp_cfg);
-	devm_kfree(&g_dp_dev->dev, ctp_cfg);
-	devm_kfree(&g_dp_dev->dev, pce_rule);
+	kfree(br_cfg);
+	kfree(bp_cfg);
+	kfree(ctp_cfg);
+	kfree(pce_rule);
 	return bret;
 }
 
@@ -1351,8 +1344,7 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 	gsw_bp = &gsw_handle->gsw_brdgport_ops;
 	gsw_brdg = &gsw_handle->gsw_brdg_ops;
 	if (meter->dir & DP_METER_ATTACH_PCE) {
-		pce_rule = devm_kzalloc(&g_dp_dev->dev, sizeof(GSW_PCE_rule_t),
-					GFP_ATOMIC);
+		pce_rule = kzalloc(sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
 		if (!pce_rule) {
 			bret = -1;
 			goto err;
@@ -1375,9 +1367,7 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		ctp_cfg = devm_kzalloc(&g_dp_dev->dev,
-				       sizeof(GSW_CTP_portConfig_t),
-				       GFP_ATOMIC);
+		ctp_cfg = kzalloc(sizeof(GSW_CTP_portConfig_t), GFP_ATOMIC);
 		if (!ctp_cfg) {
 			bret = -1;
 			goto err;
@@ -1421,9 +1411,7 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		bp_cfg = devm_kzalloc(&g_dp_dev->dev,
-				      sizeof(GSW_BRIDGE_portConfig_t),
-				      GFP_ATOMIC);
+		bp_cfg = kzalloc(sizeof(GSW_BRIDGE_portConfig_t), GFP_ATOMIC);
 		if (!bp_cfg) {
 			bret = -1;
 			goto err;
@@ -1467,8 +1455,7 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 	}
 	if (flag & DP_METER_ATTACH_BRIDGE) {
-		br_cfg = devm_kzalloc(&g_dp_dev->dev,
-				      sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
+		br_cfg = kzalloc(sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
 		if (!br_cfg) {
 			bret = -1;
 			goto err;
@@ -1506,10 +1493,10 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 	}
 err:
-	devm_kfree(&g_dp_dev->dev, br_cfg);
-	devm_kfree(&g_dp_dev->dev, bp_cfg);
-	devm_kfree(&g_dp_dev->dev, ctp_cfg);
-	devm_kfree(&g_dp_dev->dev, pce_rule);
+	kfree(br_cfg);
+	kfree(bp_cfg);
+	kfree(ctp_cfg);
+	kfree(pce_rule);
 	return bret;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 051931150082..365037da071c 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -1175,9 +1175,12 @@ static int dp_platform_set(int inst, u32 flag)
 		struct dp_subif_info *sif;
 
 		dp_port_prop[inst].priv_hal =
-			devm_kzalloc(&g_dp_dev->dev, sizeof(*priv), GFP_KERNEL);
-		if (!dp_port_prop[inst].priv_hal)
+			kzalloc(sizeof(*priv), GFP_KERNEL);
+		if (!dp_port_prop[inst].priv_hal) {
+			pr_err("kmalloc failed: %d bytes\n",
+			       sizeof(struct hal_priv));
 			return -1;
+		}
 		priv = (struct hal_priv *)dp_port_prop[inst].priv_hal;
 		priv->ppv4_drop_q = MAX_QUEUE - 1; /*Need change later */
 		gsw_handle = dp_port_prop[inst].ops[0];
@@ -1249,7 +1252,7 @@ static int dp_platform_set(int inst, u32 flag)
 	if (priv->bp_def)
 		free_bridge_port(inst, priv->bp_def);
 	init_ppv4_qos(inst, flag); /*de-initialize qos */
-	devm_kfree(&g_dp_dev->dev, priv);
+	kfree(priv);
 	dp_port_prop[inst].priv_hal = NULL;
 	return 0;
 }
@@ -1791,11 +1794,9 @@ static int subif_hw_set(int inst, int portid, int subif_ix,
 		if (port_info->alloc_flags & DP_F_EPON) {
 			lookup.sub_if_id = deq_port_idx + i;
 		} else {
-			/*Note:CQM need full subif(15bits)*/
-			lookup.sub_if_id = subif;
+			lookup.sub_if_id = subif; /*Note:CQM need full subif(15bits)*/
 		}
-			/* For 1st subif and mode 0,
-			 * use CBM_QUEUE_MAP_F_SUBIF_DONTCARE,
+			/* For 1st subif and mode 0, use CBM_QUEUE_MAP_F_SUBIF_DONTCARE,
 			 * otherwise, don't use this flag
 			 */
 			if (!port_info->num_subif &&
@@ -1889,8 +1890,7 @@ static int subif_hw_reset(int inst, int portid, int subif_ix,
 				reset_ctp_bp(inst, subif_ix, portid, bp);
 				if (!dp_bp_dev_tbl[inst][bp].dev) {
 					/*NULL already, then free it */
-					DP_DEBUG(DP_DBG_FLAG_PAE,
-						 "Free BP[%d] vap=%d\n",
+					DP_DEBUG(DP_DBG_FLAG_PAE, "Free BP[%d] vap=%d\n",
 						 bp, subif_ix);
 					free_bridge_port(inst, bp);
 				}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
index 5e79eacf4c9e..b86e5314bd6d 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
@@ -202,9 +202,23 @@ int init_ppv4_qos(int inst, int flag)
 		       dp_port_prop[inst].qos_inst);
 		return DP_FAILURE;
 	}
-	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
-	if (!t)
+	t = kzalloc(sizeof(*t), GFP_ATOMIC);
+	if (!t) {
+		pr_err("kzalloc fail: %d bytes\n", sizeof(*t));
 		return DP_FAILURE;
+	}
+	t->param.wred_total_avail_resources = 0x38000;
+	t->param.wred_p_const = 512;
+	t->param.wred_max_q_size = 0x3fffff;
+	/*reserve all ppv4 port to 1:1 sequnce link cqm port */
+	for (i = 0; i < ARRAY_SIZE(t->param.reserved_ports); i++)
+		t->param.reserved_ports[i] = 1;
+	res = qos_dev_init(priv->qdev, &t->param);
+	if (res) {
+		pr_err("qos_dev_init fail for qos inst %d\n",
+		       dp_port_prop[inst].qos_inst);
+		goto EXIT;
+	}
 	if (cbm_cpu_port_get(&cpu_data, 0)) {
 		pr_err("cbm_cpu_port_get for CPU port?\n");
 		goto EXIT;
@@ -310,7 +324,8 @@ int init_ppv4_qos(int inst, int flag)
 	res = DP_SUCCESS;
 
 EXIT:
-	devm_kfree(&g_dp_dev->dev, t);
+	kfree(t);
+	t = NULL;
 	return res;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
index 7353f45bbb54..38b273c1933e 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
@@ -327,7 +327,7 @@ static int queue_flush_31(int inst, int node_id, int flag)
 		res = DP_FAILURE;
 		goto EXIT;
 	}
-	lookup = devm_kzalloc(&g_dp_dev->dev, sizeof(*lookup), GFP_ATOMIC);
+	lookup = kzalloc(sizeof(*lookup), GFP_ATOMIC);
 	if (!lookup) {
 		res = DP_FAILURE;
 		goto EXIT;
@@ -439,7 +439,7 @@ static int queue_flush_31(int inst, int node_id, int flag)
 		}
 	}
 EXIT:
-	devm_kfree(&g_dp_dev->dev, lookup);
+	kfree(lookup);
 	lookup = NULL;
 	return res;
 }
@@ -1237,12 +1237,11 @@ static int dp_node_alloc_global_pool(struct dp_node_alloc *node, int flag)
 		goto EXIT;
 	}
 
-	q_conf = devm_kzalloc(&g_dp_dev->dev, sizeof(*q_conf), GFP_KERNEL);
+	q_conf = kzalloc(sizeof(*q_conf), GFP_KERNEL);
 	if (!q_conf)
 		goto EXIT;
 
-	qos_queue_info = devm_kzalloc(&g_dp_dev->dev,
-				      sizeof(*qos_queue_info), GFP_KERNEL);
+	qos_queue_info = kzalloc(sizeof(*qos_queue_info), GFP_KERNEL);
 	if (!qos_queue_info)
 		goto EXIT;
 
@@ -1319,8 +1318,8 @@ static int dp_node_alloc_global_pool(struct dp_node_alloc *node, int flag)
 	}
 
 EXIT:
-	devm_kfree(&g_dp_dev->dev, q_conf);
-	devm_kfree(&g_dp_dev->dev, qos_queue_info);
+	kfree(q_conf);
+	kfree(qos_queue_info);
 	return res;
 }
 
@@ -1834,7 +1833,7 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 		pr_err("priv is NULL cannot proceed!!\n");
 		return DP_FAILURE;
 	}
-	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
+	t = kzalloc(sizeof(*t), GFP_ATOMIC);
 	if (!t)
 		return DP_FAILURE;
 
@@ -2049,7 +2048,8 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 	if (res == DP_FAILURE)
 		pr_err("failed to free node:%d res %d\n",
 		       node->id.q_id, res);
-	devm_kfree(&g_dp_dev->dev, t);
+	kfree(t);
+	t = NULL;
 	return res;
 }
 
@@ -2288,10 +2288,8 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 	struct pp_qos_queue_conf *queue_cfg;
 	struct pp_qos_sched_conf *sched_cfg;
 
-	queue_cfg = devm_kzalloc(&g_dp_dev->dev, sizeof(*queue_cfg),
-				 GFP_KERNEL);
-	sched_cfg = devm_kzalloc(&g_dp_dev->dev, sizeof(*sched_cfg),
-				 GFP_KERNEL);
+	queue_cfg = kzalloc(sizeof(*queue_cfg), GFP_KERNEL);
+	sched_cfg = kzalloc(sizeof(*sched_cfg), GFP_KERNEL);
 	if (!queue_cfg || !sched_cfg)
 		goto ERROR_EXIT;
 
@@ -2373,9 +2371,9 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 	dp_map_qid_to_cqmdeq(info, flag);
 ERROR_EXIT:
 
-	devm_kfree(&g_dp_dev->dev, queue_cfg);
+	kfree(queue_cfg);
 
-	devm_kfree(&g_dp_dev->dev, sched_cfg);
+	kfree(sched_cfg);
 	return res;
 }
 
@@ -2871,7 +2869,7 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		       info->dp_port, DP_PORT(info).dp_port);
 		info->dp_port = DP_PORT(info).dp_port;
 	}
-	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
+	t = kzalloc(sizeof(*t), GFP_ATOMIC);
 	if (!t)
 		return DP_FAILURE;
 	for (i = 0; i < ARRAY_SIZE(t->q_orig_block); i++) {
@@ -3091,7 +3089,8 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 			res = DP_FAILURE;
 		}
 	}
-	devm_kfree(&g_dp_dev->dev, t);
+	kfree(t);
+	t = NULL;
 	return res;
 
 EXIT_ERR:
@@ -3123,7 +3122,8 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		}
 	}
 
-	devm_kfree(&g_dp_dev->dev, t);
+	kfree(t);
+	t = NULL;
 	return res;
 }
 
@@ -3148,7 +3148,7 @@ int dp_queue_conf_set_31(struct dp_queue_conf *cfg, int flag)
 		return DP_FAILURE;
 	}
 
-	conf = devm_kzalloc(&g_dp_dev->dev, sizeof(*conf), GFP_ATOMIC);
+	conf = kzalloc(sizeof(*conf), GFP_ATOMIC);
 	if (!conf)
 		return DP_FAILURE;
 
@@ -3194,7 +3194,8 @@ int dp_queue_conf_set_31(struct dp_queue_conf *cfg, int flag)
 	res = DP_SUCCESS;
 
 EXIT:
-	devm_kfree(&g_dp_dev->dev, conf);
+	kfree(conf);
+	conf = NULL;
 	return res;
 }
 
@@ -3219,7 +3220,7 @@ int dp_queue_conf_get_31(struct dp_queue_conf *cfg, int flag)
 		return DP_FAILURE;
 	}
 
-	conf = devm_kzalloc(&g_dp_dev->dev, sizeof(*conf), GFP_ATOMIC);
+	conf = kzalloc(sizeof(*conf), GFP_ATOMIC);
 	if (!conf)
 		return DP_FAILURE;
 
@@ -3270,7 +3271,8 @@ int dp_queue_conf_get_31(struct dp_queue_conf *cfg, int flag)
 	res = DP_SUCCESS;
 
 EXIT:
-	devm_kfree(&g_dp_dev->dev, conf);
+	kfree(conf);
+	conf = NULL;
 	return res;
 }
 
@@ -4555,19 +4557,18 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 	}
 
 	len = sizeof(struct resv_q) * data->num_resv_q;
-	priv->resv[ep].resv_q = devm_kzalloc(&g_dp_dev->dev, len, GFP_ATOMIC);
+	priv->resv[ep].resv_q = kzalloc(len, GFP_ATOMIC);
 	if (!priv->resv[ep].resv_q)
 		return DP_FAILURE;
-	tmp_resv_q = devm_kzalloc(&g_dp_dev->dev,
-				  sizeof(*tmp_resv_q) * MAX_QUEUE, GFP_ATOMIC);
+	tmp_resv_q = kzalloc(sizeof(*tmp_resv_q) * MAX_QUEUE, GFP_ATOMIC);
 	if (!tmp_resv_q) {
-		devm_kfree(&g_dp_dev->dev, priv->resv[ep].resv_q);
+		kfree(priv->resv[ep].resv_q);
 		return DP_FAILURE;
 	}
 	if (dp_queue_alloc_conf(inst, &id, &qid)) {
 		pr_err("qos_queue_allocate failed\n");
-		devm_kfree(&g_dp_dev->dev, priv->resv[ep].resv_q);
-		devm_kfree(&g_dp_dev->dev, tmp_resv_q);
+		kfree(priv->resv[ep].resv_q);
+		kfree(tmp_resv_q);
 		return DP_FAILURE;
 	}
 	tmp_resv_q[curr_num].id = id;
@@ -4577,7 +4578,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 		if (dp_queue_alloc_conf(inst, &id, &qid)) {
 			res = DP_FAILURE;
 			pr_err("qos_queue_allocate failed\n");
-			devm_kfree(&g_dp_dev->dev, priv->resv[ep].resv_q);
+			kfree(priv->resv[ep].resv_q);
 			goto clear_q;
 		}
 		tmp_resv_q[curr_num].id = id;
@@ -4611,7 +4612,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 	/* clear other non-continuously allocated Q */
 	for (i = 0; i < curr_off; i++)
 		qos_queue_remove(priv->qdev, tmp_resv_q[i].id);
-	devm_kfree(&g_dp_dev->dev, tmp_resv_q);
+	kfree(tmp_resv_q);
 	return res;
 FREE_EXIT:
 	if (priv->resv[ep].resv_q) {
@@ -4619,7 +4620,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 
 		for (i = 0; i < priv->resv[ep].num_resv_q; i++)
 			qos_queue_remove(priv->qdev, resv_q[i].id);
-		devm_kfree(&g_dp_dev->dev, resv_q);
+		kfree(resv_q);
 		priv->resv[ep].resv_q = NULL;
 		priv->resv[ep].num_resv_q = 0;
 	}
@@ -4657,7 +4658,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 	if (data->num_resv_q <= 0)
 		goto RESERVE_SCHED;
 	len = sizeof(struct resv_q) * data->num_resv_q;
-	priv->resv[ep].resv_q = devm_kzalloc(&g_dp_dev->dev, len, GFP_ATOMIC);
+	priv->resv[ep].resv_q = kzalloc(len, GFP_ATOMIC);
 	if (!priv->resv[ep].resv_q) {
 		res = DP_FAILURE;
 		goto FREE_EXIT;
@@ -4707,8 +4708,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 		struct resv_sch *p_sch = priv->resv[ep].resv_sched;
 
 		len = sizeof(struct resv_sch) * data->num_resv_sched;
-		priv->resv[ep].resv_sched = devm_kzalloc(&g_dp_dev->dev,
-							 len, GFP_ATOMIC);
+		priv->resv[ep].resv_sched = kzalloc(len, GFP_ATOMIC);
 		DP_DEBUG(DP_DBG_FLAG_QOS, "sched size =%d for ep=%d\n",
 			 len, ep);
 		if (!priv->resv[ep].resv_sched) {
@@ -4738,7 +4738,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 
 		for (i = 0; i < priv->resv[ep].num_resv_q; i++)
 			qos_queue_remove(priv->qdev, resv_q[i].id);
-		devm_kfree(&g_dp_dev->dev, resv_q);
+		kfree(resv_q);
 		priv->resv[ep].resv_q = NULL;
 		priv->resv[ep].num_resv_q = 0;
 	}
@@ -4747,7 +4747,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 
 		for (i = 0; i < priv->resv[ep].num_resv_sched; i++)
 			qos_sched_remove(priv->qdev, resv_sch[i].id);
-		devm_kfree(&g_dp_dev->dev, resv_sch);
+		kfree(resv_sch);
 		priv->resv[ep].resv_sched = NULL;
 		priv->resv[ep].num_resv_sched = 0;
 	}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_proc.c b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
index 096a159dcb2c..70eacab499de 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_proc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
@@ -282,8 +282,7 @@ int proc_print_ctp_bp_info(struct seq_file *s, int inst,
 	struct logic_dev *tmp;
 	struct dp_subif_info *sif = get_dp_port_subif(port, subif_index);
 	int bp = sif->bp;
-	unsigned char *buf = devm_kzalloc(&g_dp_dev->dev, MAX_BP_NUM * 5 + 1,
-					  GFP_KERNEL);
+	unsigned char *buf = kmalloc(MAX_BP_NUM * 5 + 1, GFP_KERNEL);
 	u32 len = MAX_BP_NUM * 5 + 1;
 
 	seq_printf(s, "           bp=%d(member:%s)\n", bp,
@@ -294,7 +293,7 @@ int proc_print_ctp_bp_info(struct seq_file *s, int inst,
 			   get_bp_member_string(inst, tmp->bp, buf, len));
 	}
 
-	devm_kfree(&g_dp_dev->dev, buf);
+	kfree(buf);
 	return 0;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
index 4423151ce871..4acac6eaa644 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
@@ -97,7 +97,7 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 			SET_BP_MAP(brportcfg.nBridgePortMap,
 				   bport_list->portid);
 		} else if (bport_list->portid == bport) {
-			if (bport_list->hairpin) {
+			if(bport_list->hairpin) {
 				SET_BP_MAP(brportcfg.nBridgePortMap,
 					   bport);
 				DP_DEBUG(DP_DBG_FLAG_SWDEV, "set SPL BP:%d\n",
@@ -202,7 +202,7 @@ int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 			cnt++;
 	}
 	DP_DEBUG(DP_DBG_FLAG_SWDEV, "cnt:%d last bp:%d\n", cnt, bp);
-	if (hairpin == 1) {
+	if(hairpin == 1) {
 		UNSET_BP_MAP(brportcfg.nBridgePortMap, bport);
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "reset spl BP %d\n", bport);
 	}
@@ -377,17 +377,23 @@ int dp_gswip_ext_vlan(int inst, int vap, int ep)
 
 	gsw_handle = dp_port_prop[inst].ops[0];
 	port = get_dp_port_info(inst, ep);
-	vlan = devm_kzalloc(&g_dp_dev->dev, sizeof(*vlan), GFP_KERNEL);
-	if (!vlan)
+	vlan = kzalloc(sizeof(*vlan), GFP_KERNEL);
+	if (!vlan) {
+		pr_err("failed to alloc ext_vlan of %d bytes\n", sizeof(*vlan));
 		return 0;
-	vlan->vlan2_list = devm_kzalloc(&g_dp_dev->dev,
-					sizeof(*vlan->vlan2_list), GFP_KERNEL);
-	if (!vlan->vlan2_list)
+	}
+	vlan->vlan2_list = kzalloc(sizeof(*vlan->vlan2_list), GFP_KERNEL);
+	if (!vlan->vlan2_list) {
+		pr_err("failed to alloc ext_vlan of %d bytes\n",
+		       sizeof(*vlan->vlan2_list));
 		goto EXIT;
-	vlan->vlan1_list = devm_kzalloc(&g_dp_dev->dev,
-					sizeof(*vlan->vlan1_list), GFP_KERNEL);
-	if (!vlan->vlan1_list)
+	}
+	vlan->vlan1_list = kzalloc(sizeof(*vlan->vlan1_list), GFP_KERNEL);
+	if (!vlan->vlan1_list) {
+		pr_err("failed to alloc ext_vlan of %d bytes\n",
+		       sizeof(*vlan->vlan1_list));
 		goto EXIT;
+	}
 	sif = get_dp_port_subif(port, vap);
 	list_for_each_entry(tmp, &sif->logic_dev, list) {
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "tmp dev name:%s\n",
@@ -452,8 +458,8 @@ int dp_gswip_ext_vlan(int inst, int vap, int ep)
 		pr_err("set gswip ext vlan return error\n");
 
 EXIT:
-	devm_kfree(&g_dp_dev->dev, vlan->vlan2_list);
-	devm_kfree(&g_dp_dev->dev, vlan->vlan1_list);
-	devm_kfree(&g_dp_dev->dev, vlan);
+	kfree(vlan->vlan2_list);
+	kfree(vlan->vlan1_list);
+	kfree(vlan);
 	return 0; /*return -EIO from GSWIP but later cannot fail swdev*/
 }
