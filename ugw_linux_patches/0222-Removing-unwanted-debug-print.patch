From 58c63f09a47fa301a96d036c056b73dd7cf3806e Mon Sep 17 00:00:00 2001
From: thampan <joby.thampan@intel.com>
Date: Fri, 10 Aug 2018 16:26:30 +0800
Subject: [PATCH] Removing unwanted debug print

---
 drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c | 4 +---
 1 file changed, 1 insertion(+), 3 deletions(-)

diff --git a/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c b/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c
index 3ed26d747e63..af92101e3d9f 100644
--- a/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c
+++ b/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c
@@ -458,7 +458,7 @@ int xgmac_rx_hwts(void *pdev, struct sk_buff *skb)
 static void xgmac_get_rx_tstamp(struct mac_prv_data *pdata,
 				struct sk_buff *skb)
 {
-	__le64 regval;
+	__be64 regval;
 	u32 ts_hdr_len = 8;
 	u32 copy_hdr_len = 8;
 	struct skb_shared_hwtstamps *shhwtstamp = NULL;
@@ -496,8 +496,6 @@ static void xgmac_get_rx_tstamp(struct mac_prv_data *pdata,
 	ns = ((((regval & 0xffffffff00000000) >> 32) * NSEC_TO_SEC) +
 	      (regval & 0x00000000ffffffff));
 
-	printk("ns = %llx\n", ns);
-
 	shhwtstamp = skb_hwtstamps(skb);
 	memset(shhwtstamp, 0, sizeof(struct skb_shared_hwtstamps));
 	shhwtstamp->hwtstamp = ns_to_ktime(ns);
