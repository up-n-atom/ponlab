From 728fe2e568eb1614cc4a3c91216bc0fddee8e51a Mon Sep 17 00:00:00 2001
From: Elad Fernandes <elad.fernandes@intel.com>
Date: Thu, 30 Jan 2020 11:30:19 +0200
Subject: [PATCH] LMTSYS-6759: Unified the UC mailbox interfaces, adding a
 common uc h file

---
 .../ppv4/pp-api/pp-session-mgr/pp_session_mgr.c    |  14 +-
 .../pp-api/pp-session-mgr/pp_session_mgr_debugfs.c |  26 +--
 .../pp-session-mgr/pp_session_mgr_internal.h       |  55 +----
 .../pp-api/pp-session-mgr/pp_session_mgr_mcast.c   |  18 +-
 .../pp-api/pp-session-mgr/pp_session_mgr_tdox.c    |  22 +-
 drivers/net/ppv4/pp-hal/checker/checker.c          |   8 +-
 drivers/net/ppv4/pp-hal/checker/checker_internal.h |  12 +-
 drivers/net/ppv4/pp-hal/micro-controller/uc.c      |  82 ++-----
 drivers/net/ppv4/pp-hal/micro-controller/uc.h      |  93 +-------
 .../net/ppv4/pp-hal/micro-controller/uc_common.h   | 246 +++++++++++++++++++++
 .../net/ppv4/pp-hal/micro-controller/uc_egr_mbox.c | 231 ++++++++-----------
 .../net/ppv4/pp-hal/micro-controller/uc_internal.h |  36 +--
 drivers/net/ppv4/pp-hal/micro-controller/uc_regs.h |   3 -
 13 files changed, 425 insertions(+), 421 deletions(-)

diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
index 8325eb34babb..74c10f03f39e 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
@@ -2,7 +2,7 @@
  * Description: PP session manager driver
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2018-2019 Intel Corporation
+ * Copyright (C) 2018-2020 Intel Corporation
  */
 
 #define pr_fmt(fmt) "[PP_SMGR]:%s:%d: " fmt, __func__, __LINE__
@@ -1740,7 +1740,7 @@ enum smgr_state smgr_state_get(void)
 
 s32 pp_smgr_reassembly_stats_get(struct smgr_reass_stats *stats)
 {
-	struct smgr_reass_uc_cpu_stats cpu_stats;
+	struct reassembly_stats cpu_stats;
 	s32 ret;
 	u32 cid;
 
@@ -1748,8 +1748,8 @@ s32 pp_smgr_reassembly_stats_get(struct smgr_reass_stats *stats)
 		if (!uc_is_cpu_active(UC_IS_EGR, cid))
 			continue;
 
-		ret = uc_egr_mbox_cmd_send(UC_CMD_REASSEMBLY_STATS, cid,
-					   &cpu_stats, sizeof(cpu_stats));
+		ret = uc_egr_mbox_cmd_send(UC_CMD_REASSEMBLY_STATS, cid, NULL,
+					   0, &cpu_stats, sizeof(cpu_stats));
 		if (unlikely(ret)) {
 			pr_err("failed to get reassembly uc cpu %u counters\n",
 			       cid);
@@ -1773,7 +1773,7 @@ s32 pp_smgr_reassembly_stats_get(struct smgr_reass_stats *stats)
 	return 0;
 }
 
-s32 smgr_frag_uc_cpu_stats_get(u32 cid, struct smgr_frag_uc_cpu_stats *stats)
+s32 smgr_frag_uc_cpu_stats_get(u32 cid, struct frag_stats *stats)
 {
 	if (unlikely(ptr_is_null(stats)))
 		return -EINVAL;
@@ -1782,13 +1782,13 @@ s32 smgr_frag_uc_cpu_stats_get(u32 cid, struct smgr_frag_uc_cpu_stats *stats)
 		return -EPERM;
 
 	/* send command */
-	return uc_egr_mbox_cmd_send(UC_CMD_FRAG_STATS, cid, stats,
+	return uc_egr_mbox_cmd_send(UC_CMD_FRAG_STATS, cid, NULL, 0, stats,
 				    sizeof(*stats));
 }
 
 s32 pp_smgr_frag_stats_get(struct smgr_frag_stats *stats)
 {
-	struct smgr_frag_uc_cpu_stats cpu_stats;
+	struct frag_stats cpu_stats;
 	s32 ret;
 	u32 i;
 
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
index 01a077cce75c..922e57bda799 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
@@ -2,7 +2,7 @@
  * Description: PP session manager debugfs interface
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2018-2019 Intel Corporation
+ * Copyright (C) 2018-2020 Intel Corporation
  */
 
 #define pr_fmt(fmt) "[PP_SESS_MGR_DBG]:%s:%d: " fmt, __func__, __LINE__
@@ -429,7 +429,7 @@ PP_DEFINE_DEBUGFS(exception_session_si,
  */
 void __smgr_dbg_frag_stats(struct seq_file *f)
 {
-	struct smgr_frag_uc_cpu_stats stats[UC_CPUS_MAX], *it;
+	struct frag_stats stats[UC_CPUS_MAX], *it;
 	s32 ret;
 	u32 cid, i;
 	char **str;
@@ -445,7 +445,7 @@ void __smgr_dbg_frag_stats(struct seq_file *f)
 	};
 
 	for (cid = 0; cid < UC_CPUS_MAX; cid++) {
-		ret = uc_egr_mbox_cmd_send(UC_CMD_FRAG_STATS, cid,
+		ret = uc_egr_mbox_cmd_send(UC_CMD_FRAG_STATS, cid, NULL, 0,
 					   &stats[cid], sizeof(stats[cid]));
 		if (unlikely(ret)) {
 			pr_err("failed to get frag uc cpu %u counters\n", cid);
@@ -2173,7 +2173,7 @@ PP_DEFINE_DEBUGFS(sgc, __smgr_dbg_sgc_show_all, __smgr_dbg_sgc);
 
 void __smgr_dbg_reass_stats_show(struct seq_file *f)
 {
-	struct smgr_reass_uc_cpu_stats stats[UC_CPUS_MAX], *it;
+	struct reassembly_stats stats[UC_CPUS_MAX], *it;
 	s32 ret;
 	u32 cid, i;
 	char **str;
@@ -2196,8 +2196,8 @@ void __smgr_dbg_reass_stats_show(struct seq_file *f)
 	};
 
 	for (cid = 0; cid < UC_CPUS_MAX; cid++) {
-		ret = uc_egr_mbox_cmd_send(UC_CMD_REASSEMBLY_STATS, cid,
-					   &stats[cid], sizeof(stats[cid]));
+		ret = uc_egr_mbox_cmd_send(UC_CMD_REASSEMBLY_STATS, cid, NULL,
+					   0, &stats[cid], sizeof(stats[cid]));
 		if (unlikely(ret)) {
 			pr_err("failed to get reassembly uc cpu %u counters\n",
 			       cid);
@@ -2497,7 +2497,7 @@ PP_DEFINE_DEBUGFS(group, __smgr_dbg_group_help, __smgr_dbg_group_show);
  */
 static void __smgr_dbg_pkt_stats_show(struct seq_file *f)
 {
-	struct smgr_mcast_uc_cpu_pkt_stats stats[UC_CPUS_MAX];
+	struct mcast_stats stats[UC_CPUS_MAX];
 	u32 i;
 	u32 total_rx  = 0;
 	u32 total_tx  = 0;
@@ -2519,15 +2519,15 @@ static void __smgr_dbg_pkt_stats_show(struct seq_file *f)
 			return;
 		}
 		seq_printf(f, " | %-8u | %-9s | %-9u |\n", i, "RX",
-			   stats[i].rx);
+			   stats[i].rx_pkt);
 		seq_printf(f, " | %-8u | %-9s | %-9u |\n", i, "TX",
-			   stats[i].tx);
+			   stats[i].tx_pkt);
 		seq_printf(f, " | %-8u | %-9s | %-9u |\n", i, "DROP",
-			   stats[i].drop);
+			   stats[i].drop_pkt);
 		seq_puts(f, " +----------+-----------+-----------+\n");
-		total_rx  += stats[i].rx;
-		total_tx  += stats[i].tx;
-		total_drp += stats[i].drop;
+		total_rx  += stats[i].rx_pkt;
+		total_tx  += stats[i].tx_pkt;
+		total_drp += stats[i].drop_pkt;
 	}
 	seq_printf(f, " | %-8s | %-9s | %-9u |\n", "TOTAL", "RX", total_rx);
 	seq_printf(f, " | %-8s | %-9s | %-9u |\n", "TOTAL", "TX", total_tx);
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
index 7c1f9e386ce7..1aff4339875f 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
@@ -2,7 +2,7 @@
  * Description: Packet Processor Session Manager Internal Definitions
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2018-2019 Intel Corporation
+ * Copyright (C) 2018-2020 Intel Corporation
  */
 
 #ifndef __PP_SESSION_MGR_INTERNAL_H__
@@ -19,6 +19,7 @@
 
 #include "pp_si.h"
 #include "pp_fv.h"
+#include "uc_common.h"
 #include "pp_session_mgr.h"
 
 /**
@@ -295,28 +296,6 @@ struct sess_info {
 	} nat;
 };
 
-/**
- * @struct smgr_reass_uc_cpu_stats
- * @brief reassembly UC cpu statistics
- */
-struct smgr_reass_uc_cpu_stats {
-	u32 rx_pkts;
-	u32 tx_pkts;
-	u32 reassembled;
-	u32 accelerated;
-	u32 diverted;
-	u32 matched;
-	u32 not_matched;
-	u32 cntxs_starv;
-	u32 cntx_overflow;
-	u32 timedout;
-	u32 frags_starv;
-	u32 duplicates;
-	u32 unsupported;
-	u32 invalid_len;
-	u32 dropped;
-};
-
 /**
  * @define FRAG_INFO_FLAG defines, to be used with struct si_ud_frag_info.flags
  */
@@ -347,21 +326,6 @@ struct si_ud_frag_info {
 	u16	max_pkt_size;
 } __packed;
 
-/**
- * @struct smgr_frag_uc_cpu_stats
- * @brief fragmentation UC cpu statistics
- */
-struct smgr_frag_uc_cpu_stats {
-	u32 rx_pkt;
-	u32 tx_pkt;
-	u32 total_drops;
-	u32 bmgr_drops;
-	u32 df_drops;
-	u32 max_frags_drops;
-	u32 reserved1;
-	u32 reserved2;
-};
-
 /**
  * @brief Test whether a session is routed session.<br>
  *        Routed session is a session where the ingress destination
@@ -599,16 +563,6 @@ void smgr_sq_exit(void);
 /* ========================================================================== */
 /*                               Multicast                                    */
 /* ========================================================================== */
-/**
- * @struct smgr_mcast_uc_cpu_pkt_stats
- * @brief multicast packet statistics
- */
-struct smgr_mcast_uc_cpu_pkt_stats {
-	u32 rx;
-	u32 tx;
-	u32 drop;
-};
-
 /**
  * @brief Track session in the multicast group db
  * @param sess session to track
@@ -664,8 +618,7 @@ s32 smgr_mcast_sess_lookup(struct sess_info *sess);
  * @param stats multicast packet statistics
  * @return s32 0 on success, error code otherwise
  */
-s32 smgr_mcast_uc_cpu_stats_get(u32 cid,
-				struct smgr_mcast_uc_cpu_pkt_stats *stats);
+s32 smgr_mcast_uc_cpu_stats_get(u32 cid, struct mcast_stats *stats);
 
 /**
  * @brief get the nf frag packet counters
@@ -673,7 +626,7 @@ s32 smgr_mcast_uc_cpu_stats_get(u32 cid,
  * @param stats fragmentation statistics
  * @return s32 0 on success, error code otherwise
  */
-s32 smgr_frag_uc_cpu_stats_get(u32 cid, struct smgr_frag_uc_cpu_stats *stats);
+s32 smgr_frag_uc_cpu_stats_get(u32 cid, struct frag_stats *stats);
 
 /**
  * @brief Helper function to initialize multicast info
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_mcast.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_mcast.c
index e8b503b0308e..247bb9633b4c 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_mcast.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_mcast.c
@@ -2,7 +2,7 @@
  * Description: PP session manager multicast extension
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2019 Intel Corporation
+ * Copyright (C) 2019-2020 Intel Corporation
  */
 
 #define pr_fmt(fmt) "[PP_SESS_MGR_MCAST]:%s:%d: " fmt, __func__, __LINE__
@@ -117,7 +117,8 @@ static s32 __mcast_dst_set(u16 grp, ulong dst_bmap)
 	}
 
 	cmd_data = (u16)dst_bmap << 16 | grp;
-	return uc_egr_mbox_cmd_send(UC_CMD_MCAST_DST, cmd_data, NULL, 0);
+	return uc_egr_mbox_cmd_send(UC_CMD_MCAST_DST, cmd_data,
+				    NULL, 0, NULL, 0);
 }
 
 void smgr_mcast_sess_track(struct sess_info *sess)
@@ -300,8 +301,7 @@ s32 pp_mcast_group_info_get(u16 group, struct pp_mcast_grp_info *info)
 }
 EXPORT_SYMBOL(pp_mcast_group_info_get);
 
-s32 smgr_mcast_uc_cpu_stats_get(u32 cid,
-				struct smgr_mcast_uc_cpu_pkt_stats *stats)
+s32 smgr_mcast_uc_cpu_stats_get(u32 cid, struct mcast_stats *stats)
 {
 	if (unlikely(ptr_is_null(stats)))
 		return -EINVAL;
@@ -310,13 +310,13 @@ s32 smgr_mcast_uc_cpu_stats_get(u32 cid,
 		return -EPERM;
 
 	/* send command */
-	return uc_egr_mbox_cmd_send(UC_CMD_MCAST_STATS, cid, stats,
+	return uc_egr_mbox_cmd_send(UC_CMD_MCAST_STATS, cid, NULL, 0, stats,
 				    sizeof(*stats));
 }
 
 s32 pp_smgr_mcast_stats_get(struct smgr_mcast_pkt_stats *stats)
 {
-	struct smgr_mcast_uc_cpu_pkt_stats cpu_stats;
+	struct mcast_stats cpu_stats;
 	s32 ret;
 	u32 i;
 
@@ -329,9 +329,9 @@ s32 pp_smgr_mcast_stats_get(struct smgr_mcast_pkt_stats *stats)
 			pr_err("failed to get mcast uc cpu %u counters\n", i);
 			return ret;
 		}
-		stats->rx   += cpu_stats.rx;
-		stats->tx   += cpu_stats.tx;
-		stats->drop += cpu_stats.drop;
+		stats->rx   += cpu_stats.rx_pkt;
+		stats->tx   += cpu_stats.tx_pkt;
+		stats->drop += cpu_stats.drop_pkt;
 	}
 
 	return 0;
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
index fdf8b6d1dbc6..2fb76afcddbb 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
@@ -2,7 +2,7 @@
  * Description: PP session manager turbodox module
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2019 Intel Corporation
+ * Copyright (C) 2019-2020 Intel Corporation
  */
 
 #define pr_fmt(fmt) "[PP_SESS_MGR_TDOX]:%s:%d: " fmt, __func__, __LINE__
@@ -210,7 +210,8 @@ s32 smgr_tdox_record_create(struct sess_db_entry *sess_ent)
 
 #ifndef TDOX_NO_FW
 	/* send command */
-	ret = uc_egr_mbox_cmd_send_buff(UC_CMD_TDOX_CREATE, &cmd, sizeof(cmd));
+	ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_CREATE, 0, (const void *)&cmd,
+				   sizeof(cmd), NULL, 0);
 	if (ret) {
 		pr_err("UC_CMD_TDOX_CREATE %u command failed\n", tdox_flow);
 		return ret;
@@ -409,8 +410,9 @@ static s32 tdox_detach(struct sess_db_entry *sess_ent, bool update_session,
 	if (remove_record) {
 		cmd.tdox_id = tdox_ent->tdox_id;
 		/* send command */
-		ret = uc_egr_mbox_cmd_send_buff(UC_CMD_TDOX_REMOVE, &cmd,
-						sizeof(cmd));
+		ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_REMOVE, 0,
+					   (const void *)&cmd, sizeof(cmd),
+					   NULL, 0);
 		if (ret) {
 			pr_err("UC_CMD_TDOX_REMOVE %u command failed\n",
 			       tdox_ent->tdox_id);
@@ -721,12 +723,12 @@ s32 smgr_tdox_stats_get(struct smgr_tdox_stats *stats)
 
 	spin_lock_bh(&db->tdox_lock);
 	/** TBD add command
-	 * ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_STATS,
-	 *			      0, &stats, sizeof(stats));
-	 * if (unlikely(ret)) {
-	 *	pr_err("failed to get tdox uc cpu %u counters\n", cid);
-	 *	stats->uc_rx_pkt = 0;
-	 *	stats->uc_tx_pkt = 0;
+	 * ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_STATS,0, NULL, 0,
+	 *                            stats, sizeof(*stats));
+	 * if (unlikely(ret)) { 
+	 *      pr_err("failed to get tdox uc cpu %u  counters\n", cid);
+	 *      stats->uc_rx_pkt = 0;
+	 *      stats->uc_tx_pkt = 0;
 	 * }
 	 **/
 
diff --git a/drivers/net/ppv4/pp-hal/checker/checker.c b/drivers/net/ppv4/pp-hal/checker/checker.c
index 992f00476f3e..37fb25d79f49 100644
--- a/drivers/net/ppv4/pp-hal/checker/checker.c
+++ b/drivers/net/ppv4/pp-hal/checker/checker.c
@@ -2,7 +2,7 @@
  * Description: PP Checker Driver
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2018-2019 Intel Corporation
+ * Copyright (C) 2018-2020 Intel Corporation
  */
 
 #define pr_fmt(fmt) "[CHK]:%s:%d: " fmt, __func__, __LINE__
@@ -26,7 +26,7 @@
  * @brief Defines the default crawler scan time in microseconds for
  *        the maximum iteration size 'CHK_CRWLR_MAX_ITR_SIZE'
  */
-#define CRWLR_DFLT_SCAN_TIME_US         (32000U)
+#define CRWLR_DFLT_SCAN_TIME_US (32000U)
 
 struct chk_database {
 	spinlock_t lock;      /*! protecting hw and DB accesses */
@@ -103,8 +103,8 @@ static inline void __chk_cmd_dispatch(u32 id, u32 cmd)
 	pr_debug("id %u, cmd %#x\n", id, cmd);
 
 	/* send command */
-	ret = uc_egr_mbox_cmd_send_buff(UC_CMD_CHK_MBX, (u32 *)&uc_cmd,
-					sizeof(uc_cmd));
+	ret = uc_egr_mbox_cmd_send(UC_CMD_CHK_MBX, 0, (const void *)&uc_cmd,
+				   sizeof(uc_cmd), NULL, 0);
 	if (ret)
 		pr_err("UC_CMD_CHK_MBX command failed\n");
 }
diff --git a/drivers/net/ppv4/pp-hal/checker/checker_internal.h b/drivers/net/ppv4/pp-hal/checker/checker_internal.h
index e51c8908e391..2b99dad75485 100644
--- a/drivers/net/ppv4/pp-hal/checker/checker_internal.h
+++ b/drivers/net/ppv4/pp-hal/checker/checker_internal.h
@@ -2,7 +2,7 @@
  * Description: Packet Processor Checker Internal
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2018-2019 Intel Corporation
+ * Copyright (C) 2018-2020 Intel Corporation
  */
 #ifndef __CHECKER_INTERNAL_H__
 #define __CHECKER_INTERNAL_H__
@@ -105,16 +105,6 @@ enum chk_mbox_cmd {
 	CHK_MBOX_CMD_COUNTERS,
 };
 
-/**
- * @brief UC Mailbox command intended to checker
- * @param id session id
- * @param cmd checker mailbox command
- */
-struct uc_chk_cmd {
-	u32 id;
-	u32 cmd;
-};
-
 /**
  * @brief Checker mailbox commands fields encoding
  */
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc.c b/drivers/net/ppv4/pp-hal/micro-controller/uc.c
index ab67be636304..b1f41f2f888a 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc.c
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc.c
@@ -66,41 +66,6 @@
 
 #define UC_LOGGER_BUFF_SZ  (sizeof(struct uc_log_msg) * 512)
 
-/**
- * @brief UC init info
- */
-struct eg_uc_init_info {
-	/*! logger buffer base address  */
-	u32 logger_buff;
-	/*! logger buffer size in bytes */
-	u32 logger_buff_sz;
-	/* checker base address */
-	u32 chk_base;
-};
-
-/**
- * @brief Reassembly network function parameters for the FW
- */
-struct reassembly_info {
-	/*! host base policy */
-	u8  host_base_policy;
-	/*! host policies bitmap */
-	u8  host_policies_bmap;
-	/*! host pid */
-	u8  host_pid;
-	u8  reserved;
-	/*! host queue to send packets to host upon failure */
-	u16 host_q;
-	/*! 2nd round queue */
-	u16 pp_rx_q;
-	/*! exception si base address */
-	u32 excp_si_base;
-	/*! rxdma regs base for calculating buffer size */
-	u32 rxdma_base;
-	/*! si base address */
-	u32 si_base;
-};
-
 /**
  * @brief egress uc HW base address
  */
@@ -580,8 +545,8 @@ s32 uc_ver_get(bool uc_is_egr, u32 *ver)
 
 	if (uc_is_egr) {
 		/* send the mailbox command */
-		ret = uc_egr_mbox_cmd_send(UC_CMD_VERSION, 0, ver,
-					   sizeof(*ver));
+		ret = uc_egr_mbox_cmd_send(UC_CMD_VERSION, 0, NULL, 0,
+					   ver, sizeof(*ver));
 		if (unlikely(ret))
 			return ret;
 	} else {
@@ -968,21 +933,19 @@ s32 uc_aux_reg_read(u32 reg, u32 *val)
 	if (ptr_is_null(val))
 		return -EINVAL;
 
-	return uc_egr_mbox_cmd_send(UC_CMD_AUX_REG_RD, reg, val, sizeof(*val));
+	return uc_egr_mbox_cmd_send(UC_CMD_AUX_REG_RD, reg, NULL, 0,
+				    val, sizeof(*val));
 }
 
 s32 uc_aux_reg_write(u32 reg, u32 val)
 {
-	struct {
-		u32 reg;
-		u32 val;
-	} args;
+	struct mbox_aux_reg_wr args;
 
 	args.reg = reg;
 	args.val = val;
 
-	return uc_egr_mbox_cmd_send_buff(UC_CMD_AUX_REG_WR, (u32 *)&args,
-					 sizeof(args));
+	return uc_egr_mbox_cmd_send(UC_CMD_AUX_REG_WR, 0, (const void *)&args,
+				    sizeof(args), NULL, 0);
 }
 
 s32 __nf_reass_set(u16 tx_queue, u16 dflt_hif)
@@ -1009,8 +972,8 @@ s32 __nf_reass_set(u16 tx_queue, u16 dflt_hif)
 	info.excp_si_base       = UC_PP_ADDR(chk_excp_si_phys_base_get());
 	info.si_base            = (u32)cls_si_phys_base_get();
 
-	return uc_egr_mbox_cmd_send_buff(UC_CMD_REASSEMBLY_INFO,
-					 (const u32 *)&info, sizeof(info));
+	return uc_egr_mbox_cmd_send(UC_CMD_REASSEMBLY_INFO, 0,
+				    (const void *)&info, sizeof(info), NULL, 0);
 }
 
 static s32 __writer_port_enable(u32 uc_port, u16 qos_port)
@@ -1062,10 +1025,8 @@ static s32 __nf_tdox_set(void)
 #ifndef TDOX_NO_FW
 	struct tdox_uc_config cfg;
 
-	ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_CONF_GET,
-				   SMGR_TDOX_CPU_ID,
-				   &cfg,
-				   sizeof(cfg));
+	ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_CONF_GET, SMGR_TDOX_CPU_ID, NULL,
+				   0, &cfg, sizeof(cfg));
 	if (unlikely(ret)) {
 		pr_err("failed to get tdox config\n");
 		return ret;
@@ -1131,23 +1092,25 @@ s32 uc_nf_set(enum pp_nf_type nf, u16 pid, u16 qos_port, u16 tx_queue,
 		uc_port = UC_MCAST_PORT;
 		/* set the mcast tx queue */
 		ret = uc_egr_mbox_cmd_send(UC_CMD_MCAST_QUEUE, tx_queue, NULL,
-					   0);
+					   0, NULL, 0);
 		if (unlikely(ret))
 			return ret;
 		/* set the mcast pid */
-		ret = uc_egr_mbox_cmd_send(UC_CMD_MCAST_PID, pid, NULL, 0);
+		ret = uc_egr_mbox_cmd_send(UC_CMD_MCAST_PID, pid, NULL, 0,
+					   NULL, 0);
 		if (unlikely(ret))
 			return ret;
 		break;
 	case PP_NF_IPSEC:
 		uc_port = UC_IPSEC_PORT;
 		/* set the ipsec tx queue */
-		ret = uc_egr_mbox_cmd_send(UC_CMD_IPSEC_QUEUE, tx_queue, NULL,
-					   0);
+		ret = uc_egr_mbox_cmd_send(UC_CMD_IPSEC_QUEUE, tx_queue, NULL, 0,
+					   NULL, 0);
 		if (unlikely(ret))
 			return ret;
 		/* set the ipsec pid */
-		ret = uc_egr_mbox_cmd_send(UC_CMD_IPSEC_PID, pid, NULL, 0);
+		ret = uc_egr_mbox_cmd_send(UC_CMD_IPSEC_PID, pid, NULL, 0,
+					   NULL, 0);
 		if (unlikely(ret))
 			return ret;
 		break;
@@ -1258,8 +1221,8 @@ s32 __uc_egr_info_set(void)
 	info.logger_buff_sz = logger_buff_sz;
 	info.chk_base       = UC_PP_ADDR(chk_base_addr);
 
-	return uc_egr_mbox_cmd_send_buff(UC_CMD_INIT, (const u32 *)&info,
-					 sizeof(info));
+	return uc_egr_mbox_cmd_send(UC_CMD_INIT, 0, (const void *)&info,
+				    sizeof(info), NULL, 0);
 }
 
 /**
@@ -1410,12 +1373,13 @@ s32 uc_log_buff_info_get(void **buff, size_t *sz)
 
 s32 uc_log_reset(void)
 {
-	return uc_egr_mbox_cmd_send(UC_CMD_LOGGER_RESET, 0, NULL, 0);
+	return uc_egr_mbox_cmd_send(UC_CMD_LOGGER_RESET, 0, NULL, 0, NULL, 0);
 }
 
 s32 uc_log_level_set(enum uc_log_level level)
 {
-	return uc_egr_mbox_cmd_send(UC_CMD_LOGGER_LEVEL_SET, level, NULL, 0);
+	return uc_egr_mbox_cmd_send(UC_CMD_LOGGER_LEVEL_SET, level, NULL, 0,
+				    NULL, 0);
 }
 
 static void __uc_logger_init(void)
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc.h b/drivers/net/ppv4/pp-hal/micro-controller/uc.h
index a81546d3c01b..ec44a6c783c8 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc.h
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc.h
@@ -2,7 +2,7 @@
  * Description: PP micro-comtroller definitions
  *
  * SPDX-License-Identifier: GPL-2.0-only
- * Copyright (C) 2019 Intel Corporation
+ * Copyright (C) 2019-2020 Intel Corporation
  */
 
 #ifndef __PP_UC_H__
@@ -12,6 +12,8 @@
 #include <linux/debugfs.h>      /* struct dentry */
 #include <linux/pp_api.h>
 
+#include "uc_common.h"
+
 #define TDOX_NO_FW
 
 /**
@@ -31,11 +33,6 @@
  */
 #define UC_CPUS_MAX      (4)
 
-/**
- * @define uc mailbox command buffer size
- */
-#define UC_CMD_BUFF_SIZE (128)
-
 /**
  * @struct uc_cpu_params
  */
@@ -104,55 +101,6 @@ struct tdox_uc_config {
 	u32 record_state_offset;
 };
 
-/**
- * @enum uc_mbox_cmd_type
- * @brief mailbox command type
- */
-enum uc_mbox_cmd_type {
-	UC_CMD_FIRST,
-	/*! send init info to UC */
-	UC_CMD_INIT = UC_CMD_FIRST,
-	/*! get the egress uc version */
-	UC_CMD_VERSION,
-	/*! reset the UC logger */
-	UC_CMD_LOGGER_RESET,
-	/*! set UC logger level */
-	UC_CMD_LOGGER_LEVEL_SET,
-	/*! set the multicast pp port id */
-	UC_CMD_MCAST_PID,
-	/*! set the multicast pp rx queue id (second cycle) */
-	UC_CMD_MCAST_QUEUE,
-	/*! set the multicast group bitmap */
-	UC_CMD_MCAST_DST,
-	/*! get the multicast packets stats */
-	UC_CMD_MCAST_STATS,
-	/*! set the ipsec pp port id */
-	UC_CMD_IPSEC_PID,
-	/*! set the ipsec pp rx queue id (second cycle) */
-	UC_CMD_IPSEC_QUEUE,
-	/*! set the reassembly info */
-	UC_CMD_REASSEMBLY_INFO,
-	/*! get UC CPU reassembly stats */
-	UC_CMD_REASSEMBLY_STATS,
-	UC_CMD_AUX_REG_RD,
-	UC_CMD_AUX_REG_WR,
-	/*! get the fragmentation stats */
-	UC_CMD_FRAG_STATS,
-	/*! Get Tdox configuration */
-	UC_CMD_TDOX_CONF_GET,
-	/*! Create Tdox record */
-	UC_CMD_TDOX_CREATE,
-	/*! Create Tdox record */
-	UC_CMD_TDOX_REMOVE,
-	/*! Get Tdox record */
-	UC_CMD_TDOX_STATS,
-	/*! Checker mailbox */
-	UC_CMD_CHK_MBX,
-
-	UC_CMD_LAST      = UC_CMD_CHK_MBX,
-	UC_CMD_MAX       = U32_MAX,
-};
-
 /**
  * @enum uc_mbox_set
  * @brief mailbox set, 2 sets of mailbox registers are supported
@@ -164,40 +112,19 @@ enum uc_mbox_set {
 	UC_EGR_MBOX1,
 };
 
-/**
- * @enum uc_mbox_channel
- * @brief mailbox channel type
- */
-enum uc_mbox_channel {
-	UC_CH_FIRST,
-	/*! mailbox messages channel */
-	UC_CH_MESSAGE = UC_CH_FIRST,
-	/*! TODO: not supported in FW */
-	/*UC_CH_EVENT,*/
-	UC_CH_LAST    = UC_CH_MESSAGE,
-};
-
 /**
  * @brief send a mailbox command to the egress(only) uc
  * @param type command type
  * @param param command parameter (optional)
- * @param response command response address (optional)
- * @param response_len command response buffer size (optional)
- * @return s32 0 on success, error code otherwise
- */
-s32 uc_egr_mbox_cmd_send(enum uc_mbox_cmd_type type, u32 param, void *response,
-			 u16 response_len);
-
-/**
- * @brief send a mailbox command to the egress(only) uc w/o
- *        a response back
- * @param type command type
- * @param buff buffer with the params (optional)
- * @param len buffer length (optional)
+ * @param in_buf buffer to send (optional)
+ * @param in_len in_buf size in bytes
+ * @param out_buf buffer to send (optional)
+ * @param out_len out_buf size in bytes
  * @return s32 0 on success, error code otherwise
  */
-s32 uc_egr_mbox_cmd_send_buff(enum uc_mbox_cmd_type type, const u32 *buff,
-			      u16 len);
+s32 uc_egr_mbox_cmd_send(enum uc_mbox_cmd_type type, u32 param,
+			 const void *in_buf, u16 in_len,
+			 void *out_buf, u16 out_len);
 
 /**
  * @brief allocate a DDR buffer for mailbox command
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h b/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h
new file mode 100644
index 000000000000..a6a816e2bd12
--- /dev/null
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h
@@ -0,0 +1,246 @@
+/*
+ * Description: PP micro-controller common definitions
+ *
+ * SPDX-License-Identifier: GPL-2.0-only
+ * Copyright (C) 2020 Intel Corporation
+ */
+#ifndef __PP_UC_COMMON_H__
+#define __PP_UC_COMMON_H__
+
+/* UC version - version major and minor numbers must be aligned with
+ * the fw version, if the fw was changed without any interface changes
+ * only the build number should be changed and no need to update
+ * the version here
+ */
+#define EGRESS_VER_MAJOR  1
+#define EGRESS_VER_MINOR  9
+
+#define INGRESS_VER_MAJOR 1
+#define INGRESS_VER_MINOR 2
+/**
+ * @define CMD_DONE_REG_IDX
+ * @brief GP register index for the mailbox command done signal
+ */
+#define CMD_DONE_REG_IDX  2
+
+/**
+ * @enum uc_mbox_cmd_type
+ * @brief mailbox command type
+ */
+enum uc_mbox_cmd_type {
+	UC_CMD_FIRST,
+	/*! synchronized the host with the mbox queue and cpu id */
+	UC_CMD_SYNC = UC_CMD_FIRST,
+	/*! send init info to UC */
+	UC_CMD_INIT,
+	/*! get the egress uc version */
+	UC_CMD_VERSION,
+	/*! reset the UC logger */
+	UC_CMD_LOGGER_RESET,
+	/*! set UC logger level */
+	UC_CMD_LOGGER_LEVEL_SET,
+	/*! set the multicast pp port id */
+	UC_CMD_MCAST_PID,
+	/*! set the multicast pp rx queue id (second cycle) */
+	UC_CMD_MCAST_QUEUE,
+	/*! set the multicast group bitmap */
+	UC_CMD_MCAST_DST,
+	/*! get the multicast packets stats */
+	UC_CMD_MCAST_STATS,
+	/*! set the ipsec pp port id */
+	UC_CMD_IPSEC_PID,
+	/*! set the ipsec pp rx queue id (second cycle) */
+	UC_CMD_IPSEC_QUEUE,
+	/*! set the reassembly info */
+	UC_CMD_REASSEMBLY_INFO,
+	/*! get UC CPU reassembly stats */
+	UC_CMD_REASSEMBLY_STATS,
+	UC_CMD_AUX_REG_RD,
+	UC_CMD_AUX_REG_WR,
+	/*! get the fragmentation stats */
+	UC_CMD_FRAG_STATS,
+	/*! Get Tdox configuration */
+	UC_CMD_TDOX_CONF_GET,
+	/*! Create Tdox record */
+	UC_CMD_TDOX_CREATE,
+	/*! Create Tdox record */
+	UC_CMD_TDOX_REMOVE,
+	/*! Get Tdox record */
+	UC_CMD_TDOX_STATS,
+	/*! Checker mailbox */
+	UC_CMD_CHK_MBX,
+
+	UC_CMD_LAST = UC_CMD_CHK_MBX,
+	UC_CMD_MAX  = U32_MAX,
+};
+
+/**
+ * @enum mbox_cmd_err
+ * @brief mailbox command error code
+ */
+enum mbox_cmd_err {
+	MBOX_CMD_SUCCESS,
+	MBOX_CMD_BUSY,
+	MBOX_CMD_UNSUPPORTED,
+	MBOX_CMD_INVALID_PARAM,
+};
+
+/**
+ * @enum uc_log_level
+ * @brief logger log level
+ */
+enum uc_log_level {
+	UC_LOG_FATAL,
+	UC_LOG_ERROR,
+	UC_LOG_WARN,
+	UC_LOG_INFO,
+	UC_LOG_DEBUG,
+	UC_LOG_LEVELS_NUM
+};
+
+/**
+ * @struct uc_log_msg
+ * @brief UC log message
+ * @note keep the structure aligned to power of 2
+ */
+struct uc_log_msg {
+	u32  val;
+	u32  ts;
+	u8   level;
+	u8   rsrv;
+	char str[38];
+	char func[16];
+};
+
+/**
+ * @struct eg_uc_init_info
+ * @brief UC init info
+ */
+struct eg_uc_init_info {
+	/*! logger buffer base address  */
+	u32 logger_buff;
+	/*! logger buffer size in bytes */
+	u32 logger_buff_sz;
+	/* checker base address */
+	u32 chk_base;
+};
+
+/**
+ * @struct mbox_aux_reg_wr
+ * @brief UC Mailbox command intended to aux register
+ */
+struct mbox_aux_reg_wr {
+	/*! aux register */
+	u32 reg;
+	/*! register value */
+	u32 val;
+};
+
+/**
+ * @struct uc_chk_cmd
+ * @brief UC Mailbox command intended to checker
+ */
+struct uc_chk_cmd {
+	/*! session id */
+	u32 id;
+	/*! checker mailbox command */
+	u32 cmd;
+};
+
+/**
+ * @struct mcast_stats
+ */
+struct mcast_stats {
+	/*! RX packet counter */
+	u32 rx_pkt;
+	/*! TX packet counter */
+	u32 tx_pkt;
+	/*! drop packet counter */
+	u32 drop_pkt;
+	/*! reserved */
+	u32 reserved;
+};
+
+/**
+ * @struct ipsec_stats
+ */
+struct ipsec_stats {
+	/*! RX packet counter */
+	u32 rx_pkt;
+	/*! TX packet counter */
+	u32 tx_pkt;
+	/*! error packet counter */
+	u32 error_pkt;
+	/*! reserved */
+	u32 reserved;
+};
+
+/**
+ * @brief Reassembly network function parameters for the FW
+ */
+struct reassembly_info {
+	/*! host base policy */
+	u8  host_base_policy;
+	/*! host policies bitmap */
+	u8  host_policies_bmap;
+	/*! host pid */
+	u8  host_pid;
+	u8  reserved;
+	/*! host queue to send packets to host upon failure */
+	u16 host_q;
+	/*! 2nd round queue */
+	u16 pp_rx_q;
+	/*! exception si base address */
+	u32 excp_si_base;
+	/*! rxdma regs base for calculating buffer size */
+	u32 rxdma_base;
+	/*! si base address */
+	u32 si_base;
+};
+
+/**
+ * @struct smgr_reass_uc_cpu_stats
+ * @brief reassembly UC cpu statistics
+ */
+struct reassembly_stats {
+	u32 rx_pkts;
+	u32 tx_pkts;
+	u32 reassembled;
+	u32 accelerated;
+	u32 diverted;
+	u32 matched;
+	u32 not_matched;
+	u32 cntxs_starv;
+	u32 cntx_overflow;
+	u32 timedout;
+	u32 frags_starv;
+	u32 duplicates;
+	u32 unsupported;
+	u32 invalid_len;
+	u32 dropped;
+	u32 res;
+};
+
+/**
+ * @struct frag_stats
+ */
+struct frag_stats {
+	/*! RX packet counter */
+	u32 rx_pkt;
+	/*! TX packet counter */
+	u32 tx_pkt;
+	/*! Drop packet counter */
+	u32 total_drops;
+	/*! bmgr drops */
+	u32 bmgr_drops;
+	/*! don't frag drops */
+	u32 df_drops;
+	/*! More frags required than supported */
+	u32 max_frags_drops;
+	/*! Reserved */
+	u32 reserved1;
+	/*! Reserved */
+	u32 reserved2;
+};
+
+#endif /* __PP_UC_COMMON_H__ */
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc_egr_mbox.c b/drivers/net/ppv4/pp-hal/micro-controller/uc_egr_mbox.c
index 26d6daac1a54..c6b1f17f2675 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc_egr_mbox.c
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc_egr_mbox.c
@@ -21,39 +21,55 @@
 #include "uc_internal.h"
 #include "infra.h"
 
-#define UC_MBOX_MSG_FLAG_IRQ_MSK  BIT(WR_FLAG_INTERRUPT)
-#define UC_MBOX_MSG_FLAG_DATA_MSK BIT(WR_FLAG_IS_DATA)
-#define UC_CMD_BUSY               1
-#define UC_CMD_DONE               0
-#define CMD_DONE_REG_IDX          2
+/**
+ * @brief the offset of the command buffer inside the uc job
+ */
+#define UC_MBOX_CMD_BUFF_OFF 32
 
 /**
- * @enum uc_mbox_data_words
- * @brief mailbox data word index
+ * @brief the size of the command buffer inside the uc job
  */
-enum uc_mbox_data_words {
-	/*! command type word */
-	UC_MBOX_CMD_TYPE_WORD,
-	/*! command parameter word */
-	UC_MBOX_CMD_PARAM_WORD,
-	/*! number of command data words */
-	UC_MBOX_CMD_WORD_MAX,
-};
+#define UC_MBOX_CMD_BUFF_SZ  (256 - UC_MBOX_CMD_BUFF_OFF)
 
 /**
- * @brief locks database and HW accesses
+ * @brief the command buffer pointer from the uc point of view
  */
-static spinlock_t mbox_lock;
+#define UC_MBOX_CMD_BUFF_UCV (0xF1000000 + UC_MBOX_CMD_BUFF_OFF)
+
+/**
+ * @brief the command buffer pointer from the host point of view
+ */
+#define UC_MBOX_CMD_BUFF_ADR (UC_DCCM(EGRESS, 0, UC_MBOX_CMD_BUFF_OFF))
+
+/**
+ * @brief the mailbox command job index mask
+ */
+#define UC_MBOX_CMD_IDX_MSK  0x7
 
 /**
- * @brief mailbox DDR buffer cpu address
+ * @brief uc internal memory queue offset
  */
-static void *mbox_buf_virt;
+#define UC_MBOX_QUEUE_OFF    0x800
 
 /**
- * @brief mailbox DDR buffer phys address
+ * @brief uc internal memory cpu offset
  */
-static dma_addr_t mbox_buff_phys;
+#define UC_MBOX_CPU_OFF      0x40000
+
+/**
+ * @brief mailbox command job index (0..7)
+ */
+static u8 cmd_idx;
+
+/**
+ * @brief mailbox queue offset
+ */
+static u32 cmd_q_off;
+
+/**
+ * @brief locks database and HW accesses
+ */
+static spinlock_t mbox_lock;
 
 /**
  * @brief Acquire mbox lock
@@ -77,46 +93,6 @@ static inline void __mbox_unlock(void)
 	spin_unlock_bh(&mbox_lock);
 }
 
-static s32 __uc_egr_mbox_buff_alloc(void)
-{
-#ifdef CONFIG_PPV4_FPGA
-	mbox_buff_phys = (dma_addr_t)DDR_UC_MBOX_BUFF_BASE;
-	mbox_buf_virt = (void *)DDR_UC_MBOX_BUFF_BASE;
-#else
-	mbox_buf_virt = dma_zalloc_coherent(NULL, UC_CMD_BUFF_SIZE,
-					     &mbox_buff_phys, GFP_KERNEL);
-	if (unlikely(!mbox_buf_virt)) {
-		pr_err("Could not allocate %u bytes for uc mbox buff\n",
-		       (u32)UC_CMD_BUFF_SIZE);
-		return -ENOMEM;
-	}
-#endif
-	return 0;
-}
-
-static void __uc_egr_mbox_buff_free(void)
-{
-	if (!IS_ENABLED(CONFIG_PPV4_FPGA) && mbox_buf_virt) {
-		dma_free_coherent(NULL, UC_CMD_BUFF_SIZE, mbox_buf_virt,
-				  mbox_buff_phys);
-	}
-}
-
-static bool __cmd_port_is_ready(void)
-{
-	u64 reg;
-
-	if (pp_silicon_step_get() == PP_SSTEP_A) {
-		reg = UC_EGR_A_MBOX_MSG0_WR_STATUS;
-		if (unlikely(pp_reg_poll(reg, 0xFF, 0))) {
-			pr_err("command interface not ready\n");
-			return false;
-		}
-	}
-
-	return true;
-}
-
 static bool __cmd_is_done(void)
 {
 	u64 reg;
@@ -127,7 +103,7 @@ static bool __cmd_is_done(void)
 		reg = UC_B_CCU_GPREG_REG(EGRESS, CMD_DONE_REG_IDX);
 
 	/* wait for command done signal */
-	if (pp_reg_poll(reg, U32_MAX, UC_CMD_DONE)) {
+	if (pp_reg_poll(reg, U8_MAX, MBOX_CMD_SUCCESS)) {
 		pr_err("cmd error %u\n", PP_REG_RD32(reg));
 		return false;
 	}
@@ -135,35 +111,56 @@ static bool __cmd_is_done(void)
 	return true;
 }
 
-static s32 __cmd_send(u32 data0, u32 data1, u32 addr, u16 len, u16 flags)
+static void __cmd_q_off_set(void)
 {
-	if (unlikely(!__cmd_port_is_ready()))
-		return -EPERM;
+	u32 val;
+	u8  qid, cid;
+
+	if (pp_silicon_step_get() == PP_SSTEP_A)
+		val = PP_REG_RD32(UC_A_CCU_GPREG_REG(EGRESS, CMD_DONE_REG_IDX));
+	else
+		val = PP_REG_RD32(UC_B_CCU_GPREG_REG(EGRESS, CMD_DONE_REG_IDX));
+
+	qid = (val >> 8)  & U8_MAX;
+	cid = (val >> 16) & U8_MAX;
+
+	cmd_q_off = qid * UC_MBOX_QUEUE_OFF + cid * UC_MBOX_CPU_OFF;
+	pr_debug("cmd qid=%hhu cid=%hhu q_off=%u\n", qid, cid, cmd_q_off);
+}
 
+static s32 __cmd_send(u32 type, u32 param, u32 addr, u16 len)
+{
+	u32 flags = BIT(WR_FLAG_IS_DATA);
+
+	/* write the massage format to the HW interface */
 	if (pp_silicon_step_get() == PP_SSTEP_A) {
 		PP_REG_WR32(UC_A_CCU_GPREG_REG(EGRESS, CMD_DONE_REG_IDX),
-			    UC_CMD_BUSY);
-		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_DATA0,   data0);
-		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_DATA1,   data1);
+			    MBOX_CMD_BUSY);
+		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_DATA0,   type);
+		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_DATA1,   param);
 		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_ADDRESS, addr);
 		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_FLAGS,   flags);
 		PP_REG_WR32(UC_EGR_MBOX_MSG0_WR_LENGTH,  len);
 		/* trigger the command */
-		PP_REG_WR32(UC_SW_WRITER_MBOX_CH_TRIGGER, UC_CH_MESSAGE);
+		PP_REG_WR32(UC_SW_WRITER_MBOX_CH_TRIGGER, UC_MBOX_PORT);
 	} else {
 		PP_REG_WR32(UC_B_CCU_GPREG_REG(EGRESS, CMD_DONE_REG_IDX),
-			    UC_CMD_BUSY);
-		PP_REG_WR32(UC_EGR_B_MBOX_PORT_WR_DATA0(UC_MBOX_PORT),   data0);
-		PP_REG_WR32(UC_EGR_B_MBOX_PORT_WR_DATA1(UC_MBOX_PORT),   data1);
+			    MBOX_CMD_BUSY);
+		PP_REG_WR32(UC_EGR_B_MBOX_PORT_WR_DATA0(UC_MBOX_PORT),   type);
+		PP_REG_WR32(UC_EGR_B_MBOX_PORT_WR_DATA1(UC_MBOX_PORT),   param);
 		PP_REG_WR32(UC_EGR_B_MBOX_PORT_WR_ADDRESS(UC_MBOX_PORT), addr);
 		/* trigger the command */
 		PP_REG_WR32(UC_EGR_B_MBOX_PORT_WR_LENGTH_FLAGS(UC_MBOX_PORT),
 			    (flags << 16) | len);
 	}
 
+	/* verify command is done */
 	if (unlikely(!__cmd_is_done()))
 		return -EPERM;
 
+	if (type == UC_CMD_SYNC)
+		__cmd_q_off_set();
+
 	return 0;
 }
 
@@ -179,109 +176,71 @@ static bool __cmd_is_valid(enum uc_mbox_cmd_type type)
 
 static bool __cmd_is_buff_len_valid(u16 len)
 {
-	if (len > UC_CMD_BUFF_SIZE) {
+	if (len > UC_MBOX_CMD_BUFF_SZ) {
 		pr_err("buffer to write too big %hu, max is %u\n", len,
-		       (u32)UC_CMD_BUFF_SIZE);
+		       (u32)UC_MBOX_CMD_BUFF_SZ);
 		return false;
 	}
 
 	return true;
 }
 
-s32 uc_egr_mbox_cmd_send_buff(enum uc_mbox_cmd_type type, const u32 *buff,
-			      u16 len)
+static inline u16 __cmd_next_buf_off(void)
 {
-	u32 flags = UC_MBOX_MSG_FLAG_DATA_MSK;
-	s32 i, ret;
-
-	pr_debug("uc mbox cmd %u, buff %ps, len %hu\n", (u32)type, buff, len);
-
-	if (unlikely(!uc_is_cluster_valid(UC_IS_EGR)))
-		return -EPERM;
-
-	if (unlikely(!__cmd_is_valid(type)))
-		return -EINVAL;
-
-	if (unlikely(!__cmd_is_buff_len_valid(len)))
-		return -EINVAL;
-
-	__mbox_lock();
-	/* copy data to FW buffer */
-	for (i = 0; i < (len >> 2); i++, buff++)
-		DDR_WR32(mbox_buf_virt + i * 4, *buff);
-	/* cache flush/evict */
-	if (IS_ENABLED(CONFIG_OF))
-		dma_map_single(pp_dev_get(), mbox_buf_virt, len, DMA_TO_DEVICE);
-	/* send the command */
-	ret = __cmd_send((u32)type, (u32)mbox_buff_phys, 0, 0, flags);
-	__mbox_unlock();
-
-	return ret;
+	return (((cmd_idx++ & UC_MBOX_CMD_IDX_MSK) << 8) + cmd_q_off);
 }
 
-s32 uc_egr_mbox_cmd_send(enum uc_mbox_cmd_type type, u32 param, void *response,
-			 u16 response_len)
+s32 uc_egr_mbox_cmd_send(enum uc_mbox_cmd_type type, u32 param,
+			 const void *in_buf, u16 in_len,
+			 void *out_buf, u16 out_len)
 {
-	u32 flags = UC_MBOX_MSG_FLAG_DATA_MSK;
 	s32 ret;
+	u16 cmd_buf_off = __cmd_next_buf_off();
+	void *cmd_buff = (void *)(UC_MBOX_CMD_BUFF_ADR + cmd_buf_off);
 
-	pr_debug("uc mbox cmd %u, param %u, response buff len %hu\n",
-		 (u32)type, param, response_len);
+	pr_debug("uc mbox cmd %u, param %u, in_buf %ps, in_len %hu, out_buf %ps, out_len %hu\n",
+		 (u32)type, param, in_buf, in_len, out_buf, out_len);
 
-	if (unlikely(!uc_is_cluster_valid(UC_IS_EGR)))
+	if (!uc_is_cluster_valid(UC_IS_EGR))
 		return -EPERM;
 
-	if (unlikely(!__cmd_is_valid(type)))
+	if (!__cmd_is_valid(type))
 		return -EINVAL;
 
-	if (unlikely(!__cmd_is_buff_len_valid(response_len)))
+	if (!__cmd_is_buff_len_valid(in_len) ||
+	    !__cmd_is_buff_len_valid(out_len))
 		return -EINVAL;
 
 	__mbox_lock();
-	/* reset the mbox response buffer */
-	if (response && response_len) {
-		DDR_MEMSET(mbox_buf_virt, response_len, 0);
-		if (IS_ENABLED(CONFIG_OF))
-			dma_map_single(pp_dev_get(), mbox_buf_virt,
-				       response_len, DMA_TO_DEVICE);
-	}
+	/* copy data to FW buffer */
+	if (in_buf && in_len)
+		memcpy(cmd_buff, in_buf, in_len);
 
-	ret = __cmd_send((u32)type, param, (u32)mbox_buff_phys, response_len,
-			 flags);
-	if (unlikely(ret))
+	/* send the command */
+	ret = __cmd_send(type, param, UC_MBOX_CMD_BUFF_UCV, in_len);
+	if (ret)
 		goto unlock;
 
-	/* copy uc response to caller if needed */
-	if (response && response_len) {
-		if (IS_ENABLED(CONFIG_OF))
-			dma_map_single(pp_dev_get(), mbox_buf_virt,
-				       response_len, DMA_FROM_DEVICE);
-		DDR_RD_REP(mbox_buf_virt, response, response_len);
-	}
+	/* copy data to caller buffer */
+	if (out_buf && out_len)
+		memcpy(out_buf, cmd_buff, out_len);
+
 unlock:
 	__mbox_unlock();
-
 	return ret;
 }
 
 s32 uc_egr_mbox_init(void)
 {
-	s32 ret;
-
-	ret = __uc_egr_mbox_buff_alloc();
-	if (unlikely(ret))
-		return ret;
-
 	/* init lock */
 	spin_lock_init(&mbox_lock);
 
 	if (pp_silicon_step_get() != PP_SSTEP_A)
 		PP_REG_WR32(UC_EGR_B_WRITER_PORT_ENABLE(UC_MBOX_PORT), 1);
 
-	return 0;
+	return uc_egr_mbox_cmd_send(UC_CMD_SYNC, 0, NULL, 0, NULL, 0);
 }
 
 void uc_egr_mbox_exit(void)
 {
-	__uc_egr_mbox_buff_free();
 }
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc_internal.h b/drivers/net/ppv4/pp-hal/micro-controller/uc_internal.h
index 6e22c66ff2e7..3e4ed3a74a81 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc_internal.h
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc_internal.h
@@ -1,5 +1,5 @@
 /*
- * Description: PP micro-comtroller internal definitions
+ * Description: PP micro-controller internal definitions
  *
  * SPDX-License-Identifier: GPL-2.0-only
  * Copyright (C) 2019-2020 Intel Corporation
@@ -11,22 +11,10 @@
 #include <linux/types.h>
 #include "uc_regs.h"
 
-/* UC version - version major and minor numbers must be aligned with
- * the fw version, if the fw was changed without any interface changes
- * only the build number should be changed and no need to update
- * the version here
- */
-#define EGRESS_VER_MAJOR              (1)
-#define EGRESS_VER_MINOR              (8)
-#define INGRESS_VER_MAJOR             (1)
-#define INGRESS_VER_MINOR             (2)
 #define UC_VER_MAJOR(t)               (t ## _VER_MAJOR)
 #define UC_VER_MINOR(t)               (t ## _VER_MINOR)
 
 #define INGRESS_VER_SSRAM_OFF         (0x4000)
-#define UC_CCU_INTERRUPT_ALL          (0xFF)
-#define UC_READER_INTERRUPT_ALL       (0xFF)
-#define UC_WRITER_QU_MAX              (16)
 
 /* UC Egress SW implementation for A-STEP */
 /* CPU 3 DCCM address for the port credit inc address */
@@ -36,28 +24,6 @@
 /* DCCM address for triggering the command */
 #define UC_SW_WRITER_MBOX_CH_TRIGGER  UC_SW_WRITER_TX_PORT_CRED(16)
 
-enum uc_log_level {
-	UC_LOG_FATAL,
-	UC_LOG_ERROR,
-	UC_LOG_WARN,
-	UC_LOG_INFO,
-	UC_LOG_DEBUG,
-	UC_LOG_LEVELS_NUM
-};
-
-/**
- * @brief UC log message
- * @note keep the structure aligned to power of 2
- */
-struct uc_log_msg {
-	u32  val;
-	u32  ts;
-	u8   level;
-	u8   rsrv;
-	char str[38];
-	char func[16];
-};
-
 /**
  * @brief for B-step the tx manager uc ports starts from port 16
  */
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc_regs.h b/drivers/net/ppv4/pp-hal/micro-controller/uc_regs.h
index 77c2d52af460..3badc75c389d 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc_regs.h
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc_regs.h
@@ -311,9 +311,6 @@ extern u64 uc_ing_base_addr;
 #define UC_EGR_MBOX_PORT_WR_CHANNEL(p) \
 	UC_SHRD_REG_IDX(EGRESS, WRITER_A, MB_CHANNEL, 0)
 
-#define UC_EGR_A_MBOX_MSG0_WR_STATUS   \
-	UC_SHRD_REG_IDX(EGRESS, WRITER_A, MB_STATUS,  0)
-
 #define UC_EGR_MBOX_MSG1_WR_ADDRESS    \
 	UC_SHRD_REG_IDX(EGRESS, WRITER_A, MB_MESSAGE, 4)
 #define UC_EGR_MBOX_MSG1_WR_LENGTH     \
