From 0eb1c098cdf5e05239d0faa1ef69582488e548d1 Mon Sep 17 00:00:00 2001
From: Oren Bakshe <oren.bakshe@intel.com>
Date: Mon, 7 Sep 2020 14:58:53 +0300
Subject: [PATCH] PONRTSYS-7979: Add CBS/PBS support

---
 .../net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c    | 24 +++++--
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c     | 23 ++++--
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h     |  9 +--
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c   | 83 ++++++++++++++--------
 .../net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h    | 28 ++++++--
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c  | 24 ++++++-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h  |  9 ++-
 include/linux/pp_qos_api.h                         | 33 ++++++---
 8 files changed, 169 insertions(+), 64 deletions(-)

diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
index c33bb341b45c..0d9580004cdb 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
@@ -160,24 +160,24 @@ PP_DEFINE_DEBUGFS(flush, queue_flush_help, queue_flush);
 void add_shared_bwl_group(char *cmd_buf, void *data)
 {
 	struct pp_qos_dev *qdev;
-	u32 limit;
+	u32 max_burst;
 	u32 id = 0;
 
 	qdev = pp_qos_dev_open((unsigned long)data);
 
-	if (kstrtou32(cmd_buf, 10, &limit)) {
+	if (kstrtou32(cmd_buf, 10, &max_burst)) {
 		QOS_LOG_ERR("kstrtou32 err\n");
 		return;
 	}
 
-	pp_qos_shared_limit_group_add(qdev, limit, &id);
+	pp_qos_shared_limit_group_add(qdev, max_burst, &id);
 
-	QOS_LOG_INFO("id %u, limit %u\n", id, limit);
+	QOS_LOG_INFO("id %u, max_burst %u\n", id, max_burst);
 }
 
 void add_shared_bwl_group_help(struct seq_file *f)
 {
-	seq_puts(f, "<limit>\n");
+	seq_puts(f, "<max_burst>\n");
 }
 
 PP_DEFINE_DEBUGFS(shared_grp_add, add_shared_bwl_group_help,
@@ -414,6 +414,9 @@ static u16 create_port_props(struct dbg_prop *props, u16 size,
 	dbg_add_prop(props, &num, size, "shared",
 		     "Shared bw group: 1-511 (0 for remove group)",
 		     &pconf->common_prop.shared_bw_group);
+	dbg_add_prop(props, &num, size, "max_burst",
+		     "Defines the max quantas that can be accumulated (1 << shift)",
+		     &pconf->common_prop.max_burst);
 	dbg_add_prop(props, &num, size, "arb",
 		     "Arbitration: 0 - WSP, 1 - WRR",
 		     &pconf->port_parent_prop.arbitration);
@@ -529,6 +532,9 @@ static u16 create_sched_props(struct dbg_prop *props, u16 size,
 	dbg_add_prop(props, &num, size, "shared",
 		     "Shared bw group: 1-511 (0 for remove group)",
 		     &pconf->common_prop.shared_bw_group);
+	dbg_add_prop(props, &num, size, "max_burst",
+		     "Defines the max quantas that can be accumulated (1 << shift)",
+		     &pconf->common_prop.max_burst);
 	dbg_add_prop(props, &num, size, "arb",
 		     "Arbitration: 0 - WSP, 1 - WRR",
 		     &pconf->sched_parent_prop.arbitration);
@@ -634,6 +640,9 @@ static u16 create_queue_props(struct dbg_prop *props, u16 size,
 	dbg_add_prop(props, &num, size, "shared",
 		     "Shared bw group: 1-511 (0 for remove group)",
 		     &pconf->common_prop.shared_bw_group);
+	dbg_add_prop(props, &num, size, "max_burst",
+		     "Defines the max quantas that can be accumulated (1 << shift)",
+		     &pconf->common_prop.max_burst);
 	dbg_add_prop(props, &num, size, "parent", "logical parent id",
 		     &pconf->queue_child_prop.parent);
 	dbg_add_prop(props, &num, size, "priority",
@@ -641,8 +650,6 @@ static u16 create_queue_props(struct dbg_prop *props, u16 size,
 		     &pconf->queue_child_prop.priority);
 	dbg_add_prop(props, &num, size, "wrr_weight", "percentage from parent",
 		     &pconf->queue_child_prop.wrr_weight);
-	dbg_add_prop(props, &num, size, "max_burst", "in kbps (4KB steps)",
-		     &pconf->max_burst);
 	dbg_add_prop(props, &num, size, "blocked", "drop enqueued packets",
 		     &pconf->blocked);
 	dbg_add_prop(props, &num, size, "wred_enable", "enable wred drops",
@@ -684,6 +691,9 @@ static u16 create_queue_props(struct dbg_prop *props, u16 size,
 	dbg_add_prop(props, &num, size, "eir",
 		     "Excess Information Rate",
 		     &pconf->eir);
+	dbg_add_prop(props, &num, size, "ebs",
+		     "Excess Burst Size",
+		     &pconf->ebs);
 
 	return num;
 }
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
index 4b258ef279b3..6ec7a77d041d 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
@@ -697,6 +697,7 @@ struct fw_set_common {
 	s32 suspend;
 	u32 bw_limit;
 	u32 shared_bw_group;
+	u32 max_burst;
 	u32 port_phy; /* Relevant only for queue */
 };
 
@@ -787,6 +788,12 @@ static void set_common(struct pp_qos_dev *qdev,
 		QOS_BITS_SET(valid, TSCD_NODE_CONF_SHARED_BWL_GROUP);
 		common->shared_bw_group = conf->shared_bw_group;
 	}
+
+	if (QOS_BITS_IS_SET(modified, QOS_MODIFIED_MAX_BURST)) {
+		QOS_BITS_SET(valid, TSCD_NODE_CONF_MAX_BURST);
+		common->max_burst = conf->max_burst;
+	}
+
 	common->valid = valid;
 }
 
@@ -1328,6 +1335,7 @@ static void create_add_port_cmd(struct pp_qos_dev *qdev,
 	cmd.fw.params.best_effort = conf->port_parent_prop.best_effort_enable;
 	cmd.fw.params.first_wrr_node = 0;
 	cmd.fw.params.shared_bwl_entry = conf->common_prop.shared_bw_group;
+	cmd.fw.params.max_burst = conf->common_prop.max_burst;
 	cmd.fw.disable_byte_credit = conf->packet_credit_enable;
 	cmd.fw.ring_params.txmgr_ring_sz = conf->ring_size;
 	cmd.fw.ring_params.txmgr_ring_addr_h =
@@ -1370,6 +1378,7 @@ static void _create_set_port_cmd(struct pp_qos_dev *qdev,
 	if (common->valid) {
 		cmd.fw.params.shared_bwl_entry = common->shared_bw_group;
 		cmd.fw.params.bw_limit = common->bw_limit;
+		cmd.fw.params.max_burst = common->max_burst;
 	}
 
 	if (parent->valid)
@@ -1469,6 +1478,7 @@ static void create_add_sched_cmd(struct pp_qos_dev *qdev,
 	cmd.fw.best_effort = conf->sched_parent_prop.best_effort_enable;
 	cmd.fw.first_wrr_node = 0;
 	cmd.fw.shared_bwl_entry = conf->common_prop.shared_bw_group;
+	cmd.fw.max_burst = conf->common_prop.max_burst;
 	fill_preds(qdev->nodes, phy, cmd.fw.preds, QOS_MAX_PREDECESSORS);
 
 	QOS_LOG_DEBUG("cmd %u: CMD_TYPE_ADD_SCHED %u\n",
@@ -1507,6 +1517,7 @@ static void _create_set_sched_cmd(struct pp_qos_dev *qdev,
 	if (common->valid) {
 		cmd.fw.bw_limit = common->bw_limit;
 		cmd.fw.shared_bwl_entry = common->shared_bw_group;
+		cmd.fw.max_burst = common->max_burst;
 	}
 
 	if (child->valid) {
@@ -1601,6 +1612,7 @@ static void create_add_queue_cmd(struct pp_qos_dev *qdev,
 	cmd.fw.rlm = rlm;
 	cmd.fw.bw_limit = conf->common_prop.bandwidth_limit;
 	cmd.fw.shared_bwl_entry = conf->common_prop.shared_bw_group;
+	cmd.fw.max_burst = conf->common_prop.max_burst;
 
 	if (!is_orphaned) {
 		fill_preds(qdev->nodes, phy, cmd.fw.preds,
@@ -1695,6 +1707,7 @@ static void _create_set_queue_cmd(struct pp_qos_dev *qdev, u32 phy,
 	if (common->valid) {
 		cmd.fw.bw_limit = common->bw_limit;
 		cmd.fw.shared_bwl_entry = common->shared_bw_group;
+		cmd.fw.max_burst = common->max_burst;
 	}
 
 	if (child->valid) {
@@ -2409,7 +2422,8 @@ void create_mod_log_bmap_get_cmd(struct pp_qos_dev *qdev, u32 *bmap)
 	cmd_queue_put(qdev->drvcmds.cmdq, &cmd, sizeof(cmd));
 }
 
-void create_add_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit)
+void create_add_shared_group_cmd(struct pp_qos_dev *qdev, u32 id,
+				 u32 max_burst)
 {
 	struct cmd_add_shared_group cmd;
 
@@ -2421,7 +2435,7 @@ void create_add_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit)
 		 sizeof(cmd), 0, NULL, 0, 0);
 	cmd.fw.base.type = UC_QOS_CMD_ADD_SHARED_BW_LIMIT_GROUP;
 	cmd.fw.id = id;
-	cmd.fw.bw_limit = limit;
+	cmd.fw.max_burst = max_burst;
 
 	QOS_LOG_DEBUG("cmd %u: CMD_TYPE_ADD_SHARED_GROUP %u\n",
 		      qdev->drvcmds.cmd_id, id);
@@ -2429,7 +2443,8 @@ void create_add_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit)
 	cmd_queue_put(qdev->drvcmds.cmdq, &cmd, sizeof(cmd));
 }
 
-void create_set_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit)
+void create_set_shared_group_cmd(struct pp_qos_dev *qdev, u32 id,
+				 u32 max_burst)
 {
 	struct cmd_add_shared_group cmd;
 
@@ -2441,7 +2456,7 @@ void create_set_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit)
 		 sizeof(cmd), 0, NULL, 0, 0);
 	cmd.fw.base.type = UC_QOS_CMD_SET_SHARED_BW_LIMIT_GROUP;
 	cmd.fw.id = id;
-	cmd.fw.bw_limit = limit;
+	cmd.fw.max_burst = max_burst;
 
 	QOS_LOG_DEBUG("cmd %u: CMD_TYPE_SET_SHARED_GROUP %u\n",
 		      qdev->drvcmds.cmd_id, id);
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h
index 6b8d372cbb58..115ccaf7ff03 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.h
@@ -95,7 +95,8 @@ void create_init_logger_cmd(struct pp_qos_dev *qdev,
 
 void create_add_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit);
 void create_remove_shared_group_cmd(struct pp_qos_dev *qdev, u32 id);
-void create_set_shared_group_cmd(struct pp_qos_dev *qdev, u32 id, u32 limit);
+void create_set_shared_group_cmd(struct pp_qos_dev *qdev, u32 id,
+				 u32 max_burst);
 void create_push_desc_cmd(struct pp_qos_dev *qdev, u32 queue, u32 size,
 			  u32 pool, u32 policy, u32 color, u32 gpid,
 			  u32 data_off, u32 addr);
@@ -170,8 +171,8 @@ void print_fw_log(struct pp_qos_dev *qdev);
 #define create_add_shared_group_cmd(qdev, id, limit)                 \
 	QOS_LOG_DEBUG("ADD SHARED GROUP: id %d, limit %d\n", id, limit)
 
-#define create_set_shared_group_cmd(qdev, id, limit)                 \
-	QOS_LOG_DEBUG("SET SHARED GROUP: id %d, limit %d\n", id, limit)
+#define create_set_shared_group_cmd(qdev, id, max_burst)                 \
+	QOS_LOG_DEBUG("SET SHARED GROUP: id %d, limit %d\n", id, max_burst)
 
 #define create_remove_shared_group_cmd(qdev, id)                     \
 	QOS_LOG_DEBUG("REM SHARED GROUP: id %d\n", id)
@@ -235,7 +236,7 @@ void print_fw_log(struct pp_qos_dev *qdev);
 #define print_fw_log(qdev)
 #define create_init_logger_cmd(qdev, level, mode)
 #define create_add_shared_group_cmd(qdev, id, limit)
-#define create_set_shared_group_cmd(qdev, id, limit)
+#define create_set_shared_group_cmd(qdev, id, max_burst)
 #define create_remove_shared_group_cmd(qdev, id)
 #define create_get_queue_stats_cmd(qdev, phy, rlm, addr, qstat, reset)
 #define create_get_qm_stats_cmd(qdev, rlm, addr, num, qstat)
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
index 44fc371176b7..0a5674c4ea10 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
@@ -215,6 +215,7 @@ void pp_qos_port_conf_set_default(struct pp_qos_port_conf *conf)
 	conf->common_prop.bandwidth_limit = QOS_NO_BANDWIDTH_LIMIT;
 	conf->common_prop.shared_bw_group =
 		QOS_NO_SHARED_BANDWIDTH_GROUP;
+	conf->common_prop.max_burst = QOS_DEAFULT_MAX_BURST;
 	conf->port_parent_prop.arbitration = PP_QOS_ARBITRATION_WSP;
 	conf->packet_credit_enable = 1;
 	conf->green_threshold = 0xFFFFFFFF;
@@ -669,9 +670,10 @@ static void __port_api_dbg_print(struct pp_qos_dev *qdev, u32 id,
 	u32 len = 0;
 
 	pr_buf_cat(buf, QOS_LOG_API_STR_MAX, len,
-		   "port=%u bw=%u shared=%u arb=%u be=%u",
+		   "port=%u bw=%u shared=%u max_burst %u arb=%u be=%u",
 		   id, conf->common_prop.bandwidth_limit,
 		   conf->common_prop.shared_bw_group,
+		   conf->common_prop.max_burst,
 		   conf->port_parent_prop.arbitration,
 		   conf->port_parent_prop.best_effort_enable);
 
@@ -991,11 +993,6 @@ static s32 set_queue_specific_prop(struct pp_qos_dev *qdev,
 				   const struct pp_qos_queue_conf *conf,
 				   u32 *modified)
 {
-	if (node->data.queue.max_burst != conf->max_burst) {
-		node->data.queue.max_burst = conf->max_burst;
-		QOS_BITS_SET(*modified, QOS_MODIFIED_MAX_BURST);
-	}
-
 	if (node->data.queue.green_min != conf->wred_min_avg_green) {
 		node->data.queue.green_min = conf->wred_min_avg_green;
 		QOS_BITS_SET(*modified, QOS_MODIFIED_WRED_MIN_GREEN);
@@ -1026,11 +1023,6 @@ static s32 set_queue_specific_prop(struct pp_qos_dev *qdev,
 		QOS_BITS_SET(*modified, QOS_MODIFIED_WRED_SLOPE_YELLOW);
 	}
 
-	if (node->data.queue.max_burst != conf->max_burst) {
-		node->data.queue.max_burst = conf->max_burst;
-		QOS_BITS_SET(*modified, QOS_MODIFIED_MAX_BURST);
-	}
-
 	if (node->data.queue.max_allowed != conf->wred_max_allowed) {
 		node->data.queue.max_allowed = conf->wred_max_allowed;
 		QOS_BITS_SET(*modified, QOS_MODIFIED_WRED_MAX_ALLOWED);
@@ -1249,8 +1241,10 @@ void pp_qos_queue_conf_set_default(struct pp_qos_queue_conf *conf)
 	conf->common_prop.bandwidth_limit = QOS_NO_BANDWIDTH_LIMIT;
 	conf->common_prop.shared_bw_group =
 		QOS_NO_SHARED_BANDWIDTH_GROUP;
+	conf->common_prop.max_burst = QOS_DEAFULT_MAX_BURST;
 	conf->wred_max_allowed = WRED_MAX_ALLOWED_DEFAULT;
 	conf->queue_child_prop.wrr_weight = 1;
+	conf->ebs = QOS_DEAFULT_MAX_BURST;
 }
 EXPORT_SYMBOL(pp_qos_queue_conf_set_default);
 
@@ -1415,7 +1409,7 @@ s32 qos_sync_queue_add(struct pp_qos_dev *qdev, u32 id, u32 *rlm,
 {
 	s32 rc = 0;
 
-	QOS_LOG_API_DEBUG("queue=%u bw=%u shared=%u parent=%u"
+	QOS_LOG_API_DEBUG("queue=%u bw=%u shared=%u max_burst=%u parent=%u"
 			  " priority=%u wrr_weight=%u max_burst=%u blocked=%u"
 			  " wred_enable=%u fixed_drop_prob=%u"
 			  " min_avg_green=%u max_avg_green=%u"
@@ -1426,10 +1420,11 @@ s32 qos_sync_queue_add(struct pp_qos_dev *qdev, u32 id, u32 *rlm,
 			  " codel_en=%u > syncq\n",
 			  id, conf->common_prop.bandwidth_limit,
 			  conf->common_prop.shared_bw_group,
+			  conf->common_prop.max_burst,
 			  conf->queue_child_prop.parent,
 			  conf->queue_child_prop.priority,
 			  conf->queue_child_prop.wrr_weight,
-			  conf->max_burst,
+			  conf->common_prop.max_burst,
 			  conf->blocked,
 			  conf->wred_enable,
 			  conf->wred_fixed_drop_prob_enable,
@@ -1670,7 +1665,6 @@ static s32 _pp_qos_queue_conf_get(struct pp_qos_dev *qdev, u32 id,
 	if (!node)
 		return -EINVAL;
 
-	conf->max_burst = node->data.queue.max_burst;
 	conf->wred_min_avg_green = node->data.queue.green_min;
 	conf->wred_max_avg_green = node->data.queue.green_max;
 	conf->wred_slope_green = node->data.queue.green_slope;
@@ -1694,6 +1688,7 @@ static s32 _pp_qos_queue_conf_get(struct pp_qos_dev *qdev, u32 id,
 				NODE_FLAGS_QUEUE_BLOCKED);
 	conf->codel_en = !!node->data.queue.codel_en;
 	conf->eir = node->data.queue.eir;
+	conf->ebs = node->data.queue.ebs;
 
 	return get_node_prop(qdev, node, &conf->common_prop,
 			     NULL, &conf->queue_child_prop);
@@ -1706,14 +1701,16 @@ static void __queue_api_dbg_print(struct pp_qos_dev *qdev, u32 id, bool get_api,
 	u32 len = 0;
 
 	pr_buf_cat(buf, QOS_LOG_API_STR_MAX, len,
-		   "queue=%u bw=%u shared=%u parent=%u priority=%u"
-		   " wrr_weight=%u max_burst=%u blocked=%u wred_enable=%u",
+		   "queue=%u bw=%u shared=%u max_burst %u parent=%u"
+		   " priority=%u wrr_weight=%u max_burst=%u blocked=%u"
+		   " wred_enable=%u",
 		   id, conf->common_prop.bandwidth_limit,
 		   conf->common_prop.shared_bw_group,
+		   conf->common_prop.max_burst,
 		   conf->queue_child_prop.parent,
 		   conf->queue_child_prop.priority,
 		   conf->queue_child_prop.wrr_weight,
-		   conf->max_burst,
+		   conf->common_prop.max_burst,
 		   conf->blocked,
 		   conf->wred_enable);
 
@@ -1740,11 +1737,12 @@ static void __queue_api_dbg_print(struct pp_qos_dev *qdev, u32 id, bool get_api,
 
 	pr_buf_cat(buf, QOS_LOG_API_STR_MAX, len,
 		   " min_guaranteed=%u max_allowed=%u"
-		   " codel_en=%u eir=%u%s\n",
+		   " codel_en=%u eir=%u ebs=%u%s\n",
 		   conf->wred_min_guaranteed,
 		   conf->wred_max_allowed,
 		   conf->codel_en,
 		   conf->eir,
+		   conf->ebs,
 		   get_api ? "" : " > queue");
 
 	if (get_api)
@@ -2310,6 +2308,11 @@ static s32 _rep_queue_set(struct pp_qos_dev *qdev,
 	if (!is_tree_replica_exists(qdev, id))
 		return 0;
 
+	if (conf->ebs > QOS_MAX_MAX_BURST) {
+		QOS_LOG_ERR("ebs %u > max %u\n", conf->ebs, QOS_MAX_MAX_BURST);
+		return -EINVAL;
+	}
+
 	node = get_conform_node(qdev, id, node_queue);
 	if (!node) {
 		QOS_LOG_ERR("%s: Node is NULL, id %u\n", __func__, id);
@@ -2333,6 +2336,10 @@ static s32 _rep_queue_set(struct pp_qos_dev *qdev,
 	rep_conf.common_prop.bandwidth_limit = conf->eir;
 	rep_conf.eir = 0;
 
+	node->data.queue.ebs = conf->ebs;
+	rep_conf.common_prop.max_burst = conf->ebs;
+	rep_conf.ebs = 0;
+
 	dr_sched_id = get_dr_sched_id(qdev, conf->queue_child_prop.parent);
 
 	if (conf->queue_child_prop.parent == dr_sched_id) {
@@ -3218,6 +3225,7 @@ void pp_qos_sched_conf_set_default(struct pp_qos_sched_conf *conf)
 	conf->common_prop.bandwidth_limit = QOS_NO_BANDWIDTH_LIMIT;
 	conf->common_prop.shared_bw_group =
 		QOS_NO_SHARED_BANDWIDTH_GROUP;
+	conf->common_prop.max_burst = QOS_DEAFULT_MAX_BURST;
 	conf->sched_parent_prop.arbitration = PP_QOS_ARBITRATION_WSP;
 	conf->sched_child_prop.wrr_weight = 1;
 }
@@ -3499,10 +3507,11 @@ s32 pp_qos_sched_set(struct pp_qos_dev *qdev, u32 id,
 	struct qos_node *node;
 	struct pp_qos_sched_conf temp_conf;
 
-	QOS_LOG_API_DEBUG("sched=%u bw=%u shared=%u arb=%u be=%u"
+	QOS_LOG_API_DEBUG("sched=%u bw=%u shared=%u max_burst=%u arb=%u be=%u"
 			  " parent=%u priority=%u wrr_weight=%u > sched\n",
 			  id, conf->common_prop.bandwidth_limit,
 			  conf->common_prop.shared_bw_group,
+			  conf->common_prop.max_burst,
 			  conf->sched_parent_prop.arbitration,
 			  conf->sched_parent_prop.best_effort_enable,
 			  conf->sched_child_prop.parent,
@@ -3595,10 +3604,11 @@ s32 pp_qos_sched_conf_get(struct pp_qos_dev *qdev,
 				 conf->sched_child_prop.parent,
 				 &conf->sched_child_prop.parent);
 
-	QOS_LOG_GET_API_DEBUG("sched=%u bw=%u shared=%u arb=%u be=%u"
-			      " parent=%u priority=%u wrr_weight=%u\n",
+	QOS_LOG_GET_API_DEBUG("sched=%u bw=%u shared=%u max_burst%u arb=%u"
+			      " be=%u parent=%u priority=%u wrr_weight=%u\n",
 			      id, conf->common_prop.bandwidth_limit,
 			      conf->common_prop.shared_bw_group,
+			      conf->common_prop.max_burst,
 			      conf->sched_parent_prop.arbitration,
 			      conf->sched_parent_prop.best_effort_enable,
 			      conf->sched_child_prop.parent,
@@ -3727,7 +3737,7 @@ static s32 _pp_qos_shared_limit_group_get_members(struct pp_qos_dev *qdev,
 						  u32 *members_num);
 
 s32 pp_qos_shared_limit_group_add(struct pp_qos_dev *qdev,
-				  u32 limit,
+				  u32 max_burst,
 				  u32 *id)
 {
 	u32 i;
@@ -3741,6 +3751,12 @@ s32 pp_qos_shared_limit_group_add(struct pp_qos_dev *qdev,
 		goto out;
 	}
 
+	if (max_burst > QOS_MAX_MAX_BURST) {
+		QOS_LOG_ERR("max max_burst is %u\n", QOS_MAX_MAX_BURST);
+		rc = -EINVAL;
+		goto out;
+	}
+
 	grp = qdev->groups + 1;
 	for (i = 1; i <= QOS_MAX_SHARED_BANDWIDTH_GROUP; ++grp, ++i) {
 		if (!grp->used)
@@ -3754,10 +3770,10 @@ s32 pp_qos_shared_limit_group_add(struct pp_qos_dev *qdev,
 	}
 
 	grp->used = 1;
-	grp->limit = limit;
+	grp->max_burst = max_burst;
 	*id = i;
 
-	create_add_shared_group_cmd(qdev, i, limit);
+	create_add_shared_group_cmd(qdev, i, max_burst);
 	update_cmd_id(&qdev->drvcmds);
 	rc = transmit_cmds(qdev);
 out:
@@ -3804,15 +3820,21 @@ s32 pp_qos_shared_limit_group_remove(struct pp_qos_dev *qdev, u32 id)
 EXPORT_SYMBOL(pp_qos_shared_limit_group_remove);
 
 s32 pp_qos_shared_limit_group_modify(struct pp_qos_dev *qdev,
-				     u32 id, u32 limit)
+				     u32 id, u32 max_burst)
 {
 	s32 rc = 0;
 
+	if (max_burst > QOS_MAX_MAX_BURST) {
+		QOS_LOG_ERR("max max_burst is %u\n", QOS_MAX_MAX_BURST);
+		return -EINVAL;
+	}
+
 	QOS_LOCK(qdev);
 	PP_QOS_ENTER_FUNC();
+
 	if (QOS_BW_GRP_VALID(id) && qdev->groups[id].used) {
-		if (qdev->groups[id].limit != limit) {
-			create_set_shared_group_cmd(qdev, id, limit);
+		if (qdev->groups[id].max_burst != max_burst) {
+			create_set_shared_group_cmd(qdev, id, max_burst);
 			update_cmd_id(&qdev->drvcmds);
 			rc = transmit_cmds(qdev);
 		}
@@ -3820,6 +3842,7 @@ s32 pp_qos_shared_limit_group_modify(struct pp_qos_dev *qdev,
 		QOS_LOG_ERR("Shared group %u is not used\n", id);
 		rc = -EINVAL;
 	}
+
 	QOS_UNLOCK(qdev);
 	return rc;
 }
@@ -3962,8 +3985,10 @@ s32 __qos_ddr_allocate(struct pp_qos_dev *qdev)
 		goto rlm_container_alloc_err;
 	}
 
-	/* Allocate BWL allowed buffer */
-	cfg->bwl_sz = PAGE_ALIGN(NUM_OF_NODES * sizeof(u32));
+	/* Allocate BWL allowed buffer.
+	 * Buffer contains bw(u32) + max burst(u32) per node
+	 */
+	cfg->bwl_sz = PAGE_ALIGN(2 * NUM_OF_NODES * sizeof(u32));
 	cfg->bwl_ddr_virt = pp_dma_alloc(cfg->bwl_sz, GFP_KERNEL,
 					 &cfg->bwl_ddr_phys);
 	if (unlikely(!cfg->bwl_ddr_virt)) {
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
index 12253ef4aadc..b74e06a56f68 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
@@ -11,8 +11,8 @@
 
 /* UC version */
 #define UC_VERSION_MAJOR (1)
-#define UC_VERSION_MINOR (14)
-#define UC_VERSION_BUILD (1)
+#define UC_VERSION_MINOR (15)
+#define UC_VERSION_BUILD (0)
 
 #define QOS_MAX_PREDECESSORS        (6)
 #define QOS_AQM_CONTEXT_MAX_QUEUES  (8)
@@ -241,6 +241,9 @@ enum tscd_node_conf {
 	//!< Set if Queue's port was changed (Relevant only for queue)
 	TSCD_NODE_CONF_SET_PORT_TO_QUEUE  = BIT(14),
 
+	//!< Max burst size (number of quantas = 1 << shift)
+	TSCD_NODE_CONF_MAX_BURST          = BIT(15),
+
 	//!< All flags are set
 	TSCD_NODE_CONF_ALL                = 0xFFFF
 };
@@ -706,6 +709,18 @@ struct wsp_queues_t {
 	struct wsp_queue_t queue[QOS_ENHANCED_WSP_MAX_QUEUES];
 } __attribute__((packed));
 
+/**************************************************************************
+ *! \struct	bwl_ddr_info_t
+ **************************************************************************
+ *
+ * \brief BW limit information
+ *
+ **************************************************************************/
+struct bwl_ddr_info_t {
+	u32    bw_limit;
+	u32    max_burst;
+} __attribute__((packed));
+
 /**************************************************************************
  *! @enum UC_QOS_CMD
  **************************************************************************
@@ -848,6 +863,7 @@ struct fw_cmd_port_params {
 	u32 best_effort; /*! best_effort_enable */
 	u32 first_wrr_node; /*! first WRR node (0 if no WRR node) */
 	u32 shared_bwl_entry; /*! Shared BWL entry (0 if no group defined) */
+	u32 max_burst; /*! Defines the max quantas that can be accumulated (num quantas = 1 << (max_burst)) */
 } __attribute__((packed));
 
 struct fw_cmd_port_ring_params {
@@ -878,6 +894,7 @@ struct fw_cmd_add_sched {
 	u32 best_effort; /*! best_effort_enable */
 	u32 first_wrr_node; /*! first WRR node (0 if no WRR node) */
 	u32 shared_bwl_entry; /*! Shared BWL entry (0 if no group defined) */
+	u32 max_burst; /*! Defines the max quantas that can be accumulated (num quantas = 1 << (max_burst)) */
 	u32 preds[QOS_MAX_PREDECESSORS];
 } __attribute__((packed));
 
@@ -917,6 +934,7 @@ struct fw_cmd_add_queue {
 	u32 bw_limit; /*! bw_limit (0 - No BW limit) */
 	u32 actual_bw_limit; /*! [OUT]: actual bw limit returned */
 	u32 shared_bwl_entry; /*! Shared BWL entry (0 - no Shared BW group) */
+	u32 max_burst; /*! Defines the max quantas that can be accumulated (num quantas = 1 << (max_burst)) */
 	u32 preds[QOS_MAX_PREDECESSORS]; /*! Predecessors */
 	struct fw_cmd_queue_params params;
 } __attribute__((packed));
@@ -934,6 +952,7 @@ struct fw_cmd_set_sched {
 	u32 first_wrr_node; /*! first WRR node (0 if no WRR node) */
 	u32 node_weight; /*! Set 2-1-1 if first node is double weighted */
 	u32 shared_bwl_entry; /*! Shared BWL entry (0 if no group defined) */
+	u32 max_burst; /*! Defines the max quantas that can be accumulated (num quantas = 1 << (max_burst)) */
 	u32 preds[QOS_MAX_PREDECESSORS];
 } __attribute__((packed));
 
@@ -960,6 +979,7 @@ struct fw_cmd_set_queue {
 	u32 actual_bw_limit; /*! [OUT]: actual bw limit returned */
 	u32 node_weight; /*! Set 2-1-1 if first node is double weighted */
 	u32 shared_bwl_entry; /*! Shared BWL entry (0 - no Shared BW group) */
+	u32 max_burst; /*! Defines the max quantas that can be accumulated (num quantas = 1 << (max_burst)) */
 	u32 preds[QOS_MAX_PREDECESSORS]; /*! Predecessors */
 	u32 port; /*! Port ID */
 	u32 queue_valid; /*! Queue valid bitmap defined in enum queue_conf */
@@ -1035,13 +1055,13 @@ struct fw_cmd_update_port_tree {
 struct fw_cmd_add_bwl_group {
 	struct uc_qos_cmd_base base;
 	u32 id; /*! Shared BW limit group ID (supported values: 1-511) */
-	u32 bw_limit; /*! BW limit */
+	u32 max_burst; /*! burst size (number of quantas = 1 << max_burst) */
 } __attribute__((packed));
 
 struct fw_cmd_set_bwl_group {
 	struct uc_qos_cmd_base base;
 	u32 id; /*! Shared BW limit group ID (supported values: 1-511) */
-	u32 bw_limit; /*! BW limit */
+	u32 max_burst; /*! burst size (number of quantas = 1 << max_burst) */
 } __attribute__((packed));
 
 struct fw_cmd_rem_bwl_group {
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
index 3684cfad44ea..f1f672a72190 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
@@ -2281,11 +2281,14 @@ static s32 update_bwl_buffer_node(struct pp_qos_dev *qdev,
 	u32 phy;
 	unsigned long bwl_entry_addr;
 	unsigned long sbwl_entry_addr;
+	struct bwl_ddr_info_t *bwl_info;
 
 	phy = get_phy_from_node(qdev->nodes, node);
-	bwl_entry_addr = (ulong)qdev->hwconf.bwl_ddr_virt + (phy * 4);
-	*(u32 *)bwl_entry_addr = node->bandwidth_limit;
-
+	bwl_entry_addr = (ulong)qdev->hwconf.bwl_ddr_virt +
+			 (phy * sizeof(struct bwl_ddr_info_t));
+	bwl_info = (struct bwl_ddr_info_t *)bwl_entry_addr;
+	bwl_info->bw_limit = node->bandwidth_limit;
+	bwl_info->max_burst = node->max_burst;
 
 	sbwl_entry_addr = (ulong)qdev->hwconf.sbwl_ddr_virt + (phy * 4);
 	*(u32 *)(sbwl_entry_addr) = (u32)node->shared_bw_group;
@@ -2864,6 +2867,12 @@ static s32 common_cfg_valid(const struct pp_qos_dev *qdev,
 			    node->shared_bw_group);
 		return 0;
 	}
+
+	if (node->max_burst > QOS_MAX_MAX_BURST) {
+		QOS_LOG_ERR("Invalid max burst %u\n", node->max_burst);
+		return 0;
+	}
+
 	return 1;
 }
 
@@ -3094,6 +3103,7 @@ s32 get_node_prop(const struct pp_qos_dev *qdev,
 	if (common) {
 		common->bandwidth_limit = node->bandwidth_limit;
 		common->shared_bw_group = node->shared_bw_group;
+		common->max_burst = node->max_burst;
 	}
 
 	if (parent) {
@@ -3126,6 +3136,7 @@ static s32 set_common(struct pp_qos_dev *qdev,
 {
 	u32 grp_id;
 	u32 new_bw;
+	u32 burst;
 
 	if (node->bandwidth_limit != common->bandwidth_limit) {
 		new_bw = common->bandwidth_limit;
@@ -3151,6 +3162,12 @@ static s32 set_common(struct pp_qos_dev *qdev,
 		QOS_BITS_SET(*modified, QOS_MODIFIED_SHARED_GROUP_ID);
 	}
 
+	burst = common->max_burst;
+	if (burst != node->max_burst) {
+		node->max_burst = burst;
+		QOS_BITS_SET(*modified, QOS_MODIFIED_MAX_BURST);
+	}
+
 	return 0;
 }
 
@@ -3318,6 +3335,7 @@ static void node_common_init(struct qos_node *node)
 	node->flags = 0;
 	node->bandwidth_limit = QOS_NO_BANDWIDTH_LIMIT;
 	node->shared_bw_group = QOS_NO_SHARED_BANDWIDTH_GROUP;
+	node->max_burst = QOS_DEAFULT_MAX_BURST;
 }
 
 void node_init(const struct pp_qos_dev *qdev, struct qos_node *node,
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
index 66e71b13e270..76288926e2b0 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
@@ -325,6 +325,11 @@ struct qos_node {
 	/* Actual configured BW limit */
 	u32                           actual_bandwidth_limit;
 	u16                           shared_bw_group;
+	/* Max quantas that can be accumulated
+	 * num quantas = 1 << (max_burst)
+	 * Max value is 7
+	 */
+	u16                           max_burst;
 	/* child properties, relevant for sched and queue */
 	struct child_node_properties  child_prop;
 	/* parent properties, relevant for port and sched */
@@ -354,7 +359,6 @@ struct qos_node {
 			u32 min_guaranteed;
 			u32 fixed_green_prob;
 			u32 fixed_yellow_prob;
-			u16 max_burst;
 			u16 rlm;
 			u8  green_slope;
 			u8  yellow_slope;
@@ -362,6 +366,7 @@ struct qos_node {
 			u8  codel_en;
 			u16 is_alias;
 			u32 eir;
+			u16 ebs;
 			u16 last_active_port_phy;
 		} queue;
 	} data;
@@ -388,7 +393,7 @@ struct pp_nodes {
 };
 
 struct shared_bw_group {
-	u32 limit;
+	u32 max_burst;
 	u8  used;
 	u8  reserved;
 };
diff --git a/include/linux/pp_qos_api.h b/include/linux/pp_qos_api.h
index 281160689d8d..ad129b522359 100644
--- a/include/linux/pp_qos_api.h
+++ b/include/linux/pp_qos_api.h
@@ -121,6 +121,13 @@ struct pp_qos_common_node_properties {
 #define QOS_MAX_SHARED_BANDWIDTH_GROUP  511
 	/*! shared bandwidth group */
 	u32 shared_bw_group;
+
+#define QOS_DEAFULT_MAX_BURST           2 /* 4 Quantas */
+#define QOS_MAX_MAX_BURST               7
+	/*! Defines the max quantas that can be accumulated
+	 * num quantas = 1 << (max_burst)
+	 */
+	u32 max_burst;
 };
 
 /**
@@ -431,9 +438,6 @@ struct pp_qos_queue_conf {
 	/*! properties as a child node */
 	struct pp_qos_child_node_properties  queue_child_prop;
 
-	/*! max burst in Kbps */
-	u32 max_burst;
-
 	/*! queue will drop new enqueued packets */
 	s32 blocked;
 
@@ -477,10 +481,18 @@ struct pp_qos_queue_conf {
 	/*! codel enable */
 	s32 codel_en;
 
-	/*! Excess inforation rate */
-	/* User can set an extra bw per queue, which will be applied only */
-	/* after serving all "regular" queues in tree */
+	/*! Excess inforation rate
+	 * User can set an extra bw per queue, which will be applied only
+	 * after serving all "regular" queues in tree
+	 */
 	u32 eir;
+
+	/*! Excess burst size
+	 * Defines the max quantas that can be accumulated
+	 * for the excess node (eir)
+	 * max num quantas = 1 << (max_excess_burst)
+	 */
+	u32 ebs;
 };
 
 /**
@@ -575,7 +587,6 @@ struct pp_qos_codel_stat {
  *        will be filled with default values.
  *
  * Defaults values:
- *    max_burst - 0
  *    bandwidth_limit - 0
  *    shared_bandwidth_limit - 0
  *    parent - 0
@@ -881,11 +892,11 @@ s32 pp_qos_sched_get_queues(struct pp_qos_dev *qos_dev, u32 id,
 /**
  * @brief Add new shared bandwidth group
  * @param qos_dev handle to qos device instance obtained from pp_qos_dev_open
- * @param limit bandwidth limit in Kbps
+ * @param max_burst num quantas = 1 << (max_burst)
  * @param id upon success, new group's id
  * @return 0 on success
  */
-s32 pp_qos_shared_limit_group_add(struct pp_qos_dev *qos_dev, u32 limit,
+s32 pp_qos_shared_limit_group_add(struct pp_qos_dev *qos_dev, u32 max_burst,
 				  u32 *id);
 
 /**
@@ -902,11 +913,11 @@ s32 pp_qos_shared_limit_group_remove(struct pp_qos_dev *qos_dev, u32 id);
  * @param qos_dev handle to qos device instance obtained from
  *        pp_qos_dev_open
  * @param id group's id previously obtained when adding group
- * @param limit bandwidth limit in Kbps
+ * @param max_burst num quantas = 1 << (max_burst)
  * @return 0 on success
  */
 s32 pp_qos_shared_limit_group_modify(struct pp_qos_dev *qos_dev, u32 id,
-				     u32 limit);
+				     u32 max_burst);
 
 /**
  * @brief Get group's members
