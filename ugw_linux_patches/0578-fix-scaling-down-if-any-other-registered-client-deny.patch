From 66a4d69d6ed84b040495333c70647d91f3a44930 Mon Sep 17 00:00:00 2001
From: Thomas Bartholomae <t.bartholomae@intel.com>
Date: Tue, 7 May 2019 12:30:08 +0200
Subject: [PATCH] fix scaling down if any other registered client deny down
 scaling

---
 drivers/cpufreq/cpufreq_conservative.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/drivers/cpufreq/cpufreq_conservative.c b/drivers/cpufreq/cpufreq_conservative.c
index 1f661c857aec..ecac9d4dbe17 100644
--- a/drivers/cpufreq/cpufreq_conservative.c
+++ b/drivers/cpufreq/cpufreq_conservative.c
@@ -112,7 +112,7 @@ static unsigned int cs_dbs_timer(struct cpufreq_policy *policy)
 		/*
 		 * if we cannot reduce the frequency anymore, break out early
 		 */
-		if (requested_freq == policy->min)
+		if ((requested_freq == policy->min) && (requested_freq == policy->cur))
 			goto out;
 
 		freq_target = get_freq_target(cs_tuners, policy);
