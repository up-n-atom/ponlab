From eb3bc0b944b823ce23547ee2ee98b56acc171384 Mon Sep 17 00:00:00 2001
From: Xu Liang <liang.xu@intel.com>
Date: Fri, 12 Jun 2020 18:55:42 +0800
Subject: [PATCH] PONRTSYS-7569: ETH Driver - update PCS settings when updating
 PHY speed

---
 drivers/net/ethernet/lantiq/ltq_eth_drv_xrx500.c | 8 ++++++++
 1 file changed, 8 insertions(+)

diff --git a/drivers/net/ethernet/lantiq/ltq_eth_drv_xrx500.c b/drivers/net/ethernet/lantiq/ltq_eth_drv_xrx500.c
index bc6c7b7843f8..0b820f966504 100644
--- a/drivers/net/ethernet/lantiq/ltq_eth_drv_xrx500.c
+++ b/drivers/net/ethernet/lantiq/ltq_eth_drv_xrx500.c
@@ -86,6 +86,9 @@ static int phy_netdevice_event(struct notifier_block *nb, unsigned long action,
 static int xrx500_phy_connect(struct net_device *dev, struct xrx500_port *port);
 static int ltq_gsw_pmac_init(void);
 
+static int serdes_ethtool_set_link_ksettings(struct net_device *dev,
+				      const struct ethtool_link_ksettings *cmd);
+
 /**
  *  Datapath directpath functions
  */
@@ -231,6 +234,8 @@ static int get_link_ksettings(struct net_device *dev,
 static int set_link_ksettings(struct net_device *dev,
 			      const struct ethtool_link_ksettings *cmd)
 {
+	int ret;
+
 	/* Linux PHY framework does not yet support 2500baseT,
 	 * so we need to configure manually here.
 	 */
@@ -242,6 +247,9 @@ static int set_link_ksettings(struct net_device *dev,
 		__clear_bit(ETHTOOL_LINK_MODE_2500baseT_Full_BIT,
 			    dev->phydev->extended_advertising);
 
+	ret = serdes_ethtool_set_link_ksettings(dev, cmd);
+	pr_info("debug: serdes_ethtool_set_link_ksettings return %d\n", ret);
+
 	return phy_ethtool_set_link_ksettings(dev, cmd);
 }
 
