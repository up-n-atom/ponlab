From f8cfdabb72b5a779a2ee98a0f85945f874b45230 Mon Sep 17 00:00:00 2001
From: Jack Ping CHNG <jack.ping.chng@linux.intel.com>
Date: Tue, 8 Dec 2020 20:34:16 +0800
Subject: [PATCH] DRV_DPM_SW-390 - DPM re structure for gsw apis

---
 drivers/net/datapath/dpm/gswip31/datapath_gswip.c | 2289 ++++++++++++---------
 drivers/net/datapath/dpm/gswip31/datapath_misc.c  |   78 +-
 drivers/net/datapath/dpm/gswip31/datapath_misc.h  |  171 +-
 drivers/net/datapath/dpm/gswip31/datapath_proc.c  |    8 +-
 4 files changed, 1414 insertions(+), 1132 deletions(-)

diff --git a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
index 34a805862d33..00f5c5f780dc 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
@@ -1,601 +1,651 @@
 // SPDX-License-Identifier: GPL-2.0
-/* Copyright (C) Intel Corporation
- * Author: Shao Guohua <guohua.shao@intel.com>
+/*
+ * Copyright (C) 2016-2020 Intel Corporation
+ * Copyright (C) 2020 MaxLinear
  */
-
+#include <linux/bitfield.h>
 #include <linux/kernel.h>
-#include <linux/types.h>
-#include <linux/etherdevice.h>
 #include <net/datapath_api.h>
 #include "../datapath.h"
 #include "datapath_misc.h"
 
 #define BP_CFG(bp_cfg, _index, id, bflag) ({ \
-	typeof(_index) (index) = (_index); \
-	(bp_cfg)->bEgressSubMeteringEnable[(index)] = bflag; \
-	(bp_cfg)->nEgressTrafficSubMeterId[(index)] = id; })
+	       typeof(_index) (index) = (_index); \
+	       (bp_cfg)->bEgressSubMeteringEnable[(index)] = bflag; \
+	       (bp_cfg)->nEgressTrafficSubMeterId[(index)] = id; })
 
 #define BR_CFG(br_cfg, _index, id, bflag) ({\
-	typeof(_index) (index) = (_index); \
-	(br_cfg)->bSubMeteringEnable[(index)] = bflag; \
-	(br_cfg)->nTrafficSubMeterId[(index)] = id; })
+	       typeof(_index) (index) = (_index); \
+	       (br_cfg)->bSubMeteringEnable[(index)] = bflag; \
+	       (br_cfg)->nTrafficSubMeterId[(index)] = id; })
 
 #define METER_CIR(cir)  ((cir) / 1000)
 #define METER_PIR(pir)  ((pir) / 1000)
 
-static const struct ctp_assign ctp_assign_info[] = {
-	/*note: multiple flags must put first */
-	{DP_F_CPU, GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, CQE_LU_MODE0, 0},
-	{DP_F_GPON, GSW_LOGICAL_PORT_GPON, 256, 256, CQE_LU_MODE1, 1},
-	{DP_F_EPON, GSW_LOGICAL_PORT_EPON, 64, 64, CQE_LU_MODE1, 1},
-	{DP_F_GINT, GSW_LOGICAL_PORT_GINT, 16, 16, CQE_LU_MODE1, 1},
-	{DP_F_FAST_ETH_WAN, GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, CQE_LU_MODE2, 1},
-	{DP_F_FAST_ETH_LAN | DP_F_ALLOC_EXPLICIT_SUBIFID,
-		GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, CQE_LU_MODE2, 1},
-	{DP_F_FAST_ETH_LAN, GSW_LOGICAL_PORT_8BIT_WLAN, 4, 4, CQE_LU_MODE2, 1},
-	{DP_F_VUNI, GSW_LOGICAL_PORT_8BIT_WLAN, 2, 2, CQE_LU_MODE2, 1},
-	{DP_F_FAST_WLAN, GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, CQE_LU_MODE2, 1},
-	{DP_F_FAST_DSL, GSW_LOGICAL_PORT_8BIT_WLAN, 1, 16, CQE_LU_MODE0, 1},
-	{DP_F_FAST_WLAN_EXT, GSW_LOGICAL_PORT_9BIT_WLAN, 8, 8, CQE_LU_MODE2, 1}
-};
+#define MAX_TC		16
+#define MAX_FLOW	4
+#define SWDEV_DIS	0
+#define SWDEV_EN	1
 
-static const struct ctp_assign ctp_assign_def = {
-					   0xF, CQE_LU_MODE2, 1};
+#define GSW_MPE1		BIT(0)
+#define GSW_MPE2		BIT(1)
+#define GSW_REDUCED_TC		GENMASK(3, 2)
+#define GSW_ENC			BIT(2)
+#define GSW_DEC			BIT(3)
+#define ETHSW_CLK		0x1
+#define SLL3MEM_EN		BIT(0)
 
+/* FIXME: Need to support multiple instances */
 static struct gsw_itf itf_assign[PMAC_MAX_NUM] = {0};
 
-const struct ctp_assign *get_ctp_assign(int flags)
+enum {
+	CTP_DEFAULT,
+	CTP_CPU,
+	CTP_GPON,
+	CTP_EPON,
+	CTP_GINT,
+	CTP_FAST_ETH_WAN,
+	CTP_FAST_ETH_LAN,
+	CTP_FAST_ETH_LAN_EXP,
+	CTP_FAST_WLAN,
+	CTP_FAST_WLAN_EXT,
+	CTP_VUNI,
+	CTP_FAST_DSL,
+	CTP_DOCSIS,
+	CTP_MAX,
+};
+
+#define CTP_ASSIGN_MASK GENMASK(17, 0)
+
+static const struct ctp_assign ctp_assign_info[3][CTP_MAX] = {
+	{ 0 },
+	/* GSWIP 31 */
+	{
+	[CTP_DEFAULT]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, 0, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_CPU]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, 0, CQE_LU_MODE0, SWDEV_DIS },
+	[CTP_GPON]
+	{ GSW_LOGICAL_PORT_GPON, 256, 256, 0, CQE_LU_MODE1, SWDEV_EN },
+	[CTP_EPON]
+	{ GSW_LOGICAL_PORT_EPON, 64, 64, 0, CQE_LU_MODE1, SWDEV_EN },
+	[CTP_GINT]
+	{ GSW_LOGICAL_PORT_GINT, 16, 16, 0, CQE_LU_MODE1, SWDEV_EN },
+	[CTP_FAST_ETH_WAN]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, 0, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_FAST_ETH_LAN]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 4, 4, 0, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_FAST_ETH_LAN_EXP]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, 0, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_VUNI]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 2, 2, 0, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_FAST_WLAN]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, 0, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_FAST_DSL]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 1, 16, 0, CQE_LU_MODE0, SWDEV_EN },
+	[CTP_FAST_WLAN_EXT]
+	{ GSW_LOGICAL_PORT_9BIT_WLAN, 8, 8, 0, CQE_LU_MODE2, SWDEV_EN }
+	},
+	/* GSWIP 32 */
+	{
+	[CTP_DEFAULT]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, 8, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_CPU]
+	{ GSW_LOGICAL_PORT_9BIT_WLAN, 16, 16, 16, CQE_LU_MODE2, SWDEV_DIS },
+	[CTP_GPON]
+	{ GSW_LOGICAL_PORT_GPON, 128, 128, 1, CQE_LU_MODE1, SWDEV_EN },
+	[CTP_EPON]
+	{ GSW_LOGICAL_PORT_EPON, 64, 64, 128, CQE_LU_MODE1, SWDEV_EN },
+	[CTP_GINT]
+	{ GSW_LOGICAL_PORT_GINT, 16, 16, 8, CQE_LU_MODE1, SWDEV_DIS },
+	[CTP_FAST_ETH_WAN]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, 8, CQE_LU_MODE2, SWDEV_DIS },
+	[CTP_FAST_ETH_LAN]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 2, 2, 2, CQE_LU_MODE2, SWDEV_DIS },
+	[CTP_FAST_ETH_LAN_EXP]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 8, 8, 8, CQE_LU_MODE2, SWDEV_DIS },
+	[CTP_VUNI]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 2, 2, 1, CQE_LU_MODE2, SWDEV_EN },
+	[CTP_FAST_WLAN]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, 16, CQE_LU_MODE2, SWDEV_DIS },
+	[CTP_FAST_DSL]
+	{ GSW_LOGICAL_PORT_8BIT_WLAN, 1, 16, 1, CQE_LU_MODE0, SWDEV_DIS },
+	[CTP_FAST_WLAN_EXT]
+	{ GSW_LOGICAL_PORT_9BIT_WLAN, 8, 8, 8, CQE_LU_MODE2, SWDEV_DIS },
+	[CTP_DOCSIS]
+	{ GSW_LOGICAL_PORT_GPON, 36, 36, 36, CQE_LU_MODE4, SWDEV_DIS }
+	}
+};
+
+const struct ctp_assign *dp_gsw_get_ctp_assign(int inst, int flags)
 {
+	struct inst_info *inst_info = get_dp_prop_info(inst);
 	int i;
 
-	for (i = 0; i < ARRAY_SIZE(ctp_assign_info); i++) {
-		if ((ctp_assign_info[i].flag & flags) ==
-		    ctp_assign_info[i].flag)
-			return &ctp_assign_info[i];
+	if (inst_info->type == GSWIP31_TYPE ||
+	    inst_info->type == GSWIP32_TYPE) {
+		i = inst_info->type;
+	} else {
+		pr_err("%s: unsupported type %d\n", __func__, inst_info->type);
+		return NULL;
+	}
+
+	switch (flags & CTP_ASSIGN_MASK) {
+	case DP_F_CPU:
+		return &ctp_assign_info[i][CTP_CPU];
+	case DP_F_GPON:
+		return &ctp_assign_info[i][CTP_GPON];
+	case DP_F_EPON:
+		return &ctp_assign_info[i][CTP_EPON];
+	case DP_F_GINT:
+		return &ctp_assign_info[i][CTP_GINT];
+	case DP_F_FAST_ETH_WAN:
+		return &ctp_assign_info[i][CTP_FAST_ETH_WAN];
+	case DP_F_FAST_ETH_LAN:
+		return &ctp_assign_info[i][CTP_FAST_ETH_LAN];
+	case DP_F_FAST_ETH_LAN | DP_F_ALLOC_EXPLICIT_SUBIFID:
+		return &ctp_assign_info[i][CTP_FAST_ETH_LAN_EXP];
+	case DP_F_VUNI:
+		return &ctp_assign_info[i][CTP_VUNI];
+	case DP_F_FAST_WLAN:
+		return &ctp_assign_info[i][CTP_FAST_WLAN];
+	case DP_F_FAST_WLAN_EXT:
+		return &ctp_assign_info[i][CTP_FAST_WLAN_EXT];
+	case DP_F_FAST_DSL:
+		return &ctp_assign_info[i][CTP_FAST_DSL];
+	case DP_F_DOCSIS:
+		return &ctp_assign_info[i][CTP_DOCSIS];
+	default:
+		pr_err("%s: no matching device type %08x\n", __func__, flags);
+		return NULL;
 	}
-	return NULL;
 }
 
-static char *ctp_mode_string(GSW_LogicalPortMode_t type)
+static char *ctp_md_str(GSW_LogicalPortMode_t type)
 {
-	if (type == GSW_LOGICAL_PORT_8BIT_WLAN)
+	switch (type) {
+	case GSW_LOGICAL_PORT_8BIT_WLAN:
 		return "8BIT_WLAN";
-	if (type == GSW_LOGICAL_PORT_9BIT_WLAN)
+	case GSW_LOGICAL_PORT_9BIT_WLAN:
 		return "9BIT_WLAN";
-	if (type == GSW_LOGICAL_PORT_GPON)
+	case GSW_LOGICAL_PORT_GPON:
 		return "GPON";
-	if (type == GSW_LOGICAL_PORT_EPON)
+	case GSW_LOGICAL_PORT_EPON:
 		return "EPON";
-	if (type == GSW_LOGICAL_PORT_GINT)
+	case GSW_LOGICAL_PORT_GINT:
 		return "GINT";
-	if (type == GSW_LOGICAL_PORT_OTHER)
+	case GSW_LOGICAL_PORT_OTHER:
 		return "OTHER";
-	return "Undef";
+	default:
+		return "UNDEF";
+	}
 }
 
-/*This API is only for GSWIP-R PMAC modification, not for GSWIP-L */
-int dp_pmac_set_31(int inst, u32 port, dp_pmac_cfg_t *pmac_cfg)
+#if defined(CONFIG_INTEL_DATAPATH_DBG)
+static void dump_pmac_igcfg(u32 ep, GSW_PMAC_Ig_Cfg_t *igcfg)
 {
-	GSW_PMAC_Eg_Cfg_t egcfg;
-	GSW_PMAC_Ig_Cfg_t igcfg;
-	GSW_PMAC_Glbl_Cfg_t glblCfg = {0};
-	int i, j, k;
-	u32 flag = 0;
-	cbm_dq_port_res_t dqport;
-	s32 ret;
-	u32 pmacid;
-	struct core_ops *gsw_ops;
-
-	if (!pmac_cfg || !port) {
-		pr_err("dp_pmac_set:wrong parameter(pmac_cfg/port NULL)\n");
-		return -1;
-	}
-
-	if (!pmac_cfg->ig_pmac_flags && !pmac_cfg->eg_pmac_flags)
-		return 0;
-	if (port == 2)
-		pmacid = 1;
-	else
-		pmacid = 0;
-	memset(&dqport, 0, sizeof(cbm_dq_port_res_t));
+	int i;
 
-	/* Get GSWIP device handler */
-	gsw_ops = dp_port_prop[inst].ops[0];
+	if ((dp_dbg_flag & DP_DBG_FLAG_DBG)) {
+		pr_info("\nPort %d igcfg pmac configuration:\n", ep);
+		pr_info("nPmacId=%d\n", igcfg->nPmacId);
+		pr_info("nTxDmaChanId=%d\n", igcfg->nTxDmaChanId);
+		pr_info("bErrPktsDisc=%d\n", igcfg->bErrPktsDisc);
+		pr_info("bPmapDefault=%d\n", igcfg->bPmapDefault);
+		pr_info("bPmapEna=%d\n", igcfg->bPmapEna);
+		pr_info("bClassDefault=%d\n", igcfg->bClassDefault);
+		pr_info("bClassEna=%d\n", igcfg->bClassEna);
+		pr_info("bSubIdDefault=%d\n", igcfg->eSubId);
+		pr_info("bSpIdDefault=%d\n", igcfg->bSpIdDefault);
+		pr_info("bPmacPresent=%d\n", igcfg->bPmacPresent);
+		pr_info("defPmacHdr=");
+
+		for (i = 0; i < ARRAY_SIZE(igcfg->defPmacHdr); i++)
+			pr_info("0x%x ", igcfg->defPmacHdr[i]);
+
+		pr_info("\n");
+	}
+}
 
-	/*set ingress port via DMA tx channel */
-	if (pmac_cfg->ig_pmac_flags) {
-		/*Read back igcfg from gsw first */
-		ret = dp_cqm_deq_port_res_get(port, &dqport, flag);
-		if (ret == -1)
-			return -1;
-
-		memset(&igcfg, 0, sizeof(GSW_PMAC_Ig_Cfg_t));
-
-		for (i = 0; i < dqport.num_deq_ports; i++) {
-			igcfg.nPmacId = pmacid;
-			igcfg.nTxDmaChanId = dqport.deq_info[i].dma_tx_chan;
-			if (igcfg.nTxDmaChanId == (u8)-1) {
-				igcfg.nTxDmaChanId =
-					pmac_cfg->ig_pmac.tx_dma_chan;
+static void dump_pmac_egcfg(u32 ep, GSW_PMAC_Eg_Cfg_t *egcfg)
+{
+	if ((dp_dbg_flag & DP_DBG_FLAG_DBG)) {
+		pr_info("\nPort %d egcfg pmac configuration:\n", ep);
+		pr_info("nPmacId=%d\n", egcfg->nPmacId);
+		pr_info("nRxDmaChanId=%d\n", egcfg->nRxDmaChanId);
+		pr_info("bRemL2Hdr=%d\n", egcfg->bRemL2Hdr);
+		pr_info("numBytesRem=%d\n", egcfg->numBytesRem);
+		pr_info("bFcsEna=%d\n", egcfg->bFcsEna);
+		pr_info("bPmacEna=%d\n", egcfg->bPmacEna);
+		pr_info("bResDW1Ena=%d\n", egcfg->bResDW1Enable);
+		pr_info("nResDW1=%d\n", egcfg->nResDW1);
+		pr_info("bRes1DW0Ena=%d\n", egcfg->bRes1DW0Enable);
+		pr_info("nRes1DW0=%d\n", egcfg->nRes1DW0);
+		pr_info("bRes2DW0Ena=%d\n", egcfg->bRes2DW0Enable);
+		pr_info("nRes2DW0=%d\n", egcfg->nRes2DW0);
+		pr_info("nDestPortId=%d\n", egcfg->nDestPortId);
+		pr_info("bTCEnable=%d\n", egcfg->bTCEnable);
+		pr_info("nTrafficClass=%d\n", egcfg->nTrafficClass);
+		pr_info("nBslTrafficClass=%d\n", egcfg->nBslTrafficClass);
+		pr_info("nFlowIDMsb=%d\n", egcfg->nFlowIDMsb);
+		pr_info("bDecFlag=%d\n", egcfg->bDecFlag);
+		pr_info("bEncFlag=%d\n", egcfg->bEncFlag);
+		pr_info("bMpe1Flag=%d\n", egcfg->bMpe1Flag);
+		pr_info("bMpe2Flag=%d\n", egcfg->bMpe2Flag);
+	}
+}
+#endif
+
+static void _update_pmac_ingress_cfg(int inst, u32 ep, dp_pmac_cfg_t *pmac_cfg,
+				     GSW_PMAC_Ig_Cfg_t *igcfg)
+{
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	ingress_pmac_t ig_pmac = pmac_cfg->ig_pmac;
+	int pmac_hdr = ffs(IG_PMAC_F_PMACHDR1), i;
+	u32 flags = pmac_cfg->ig_pmac_flags;
+
+	/* Read PMAC ingress config from GSWIP */
+	gsw_ops->gsw_pmac_ops.Pmac_Ig_CfgGet(gsw_ops, igcfg);
+
+	/* Update PMAC ingress config */
+	if (flags & IG_PMAC_F_ERR_DISC)
+		igcfg->bErrPktsDisc = ig_pmac.err_disc;
+
+	if (flags & IG_PMAC_F_PRESENT)
+		igcfg->bPmacPresent = ig_pmac.pmac;
+
+	if (flags & IG_PMAC_F_SUBIF)
+		igcfg->eSubId = ig_pmac.def_pmac_subifid;
+
+	if (flags & IG_PMAC_F_SPID)
+		igcfg->bSpIdDefault = ig_pmac.def_pmac_src_port;
+
+	if (flags & IG_PMAC_F_CLASSENA)
+		igcfg->bClassEna = ig_pmac.def_pmac_en_tc;
+
+	if (flags & IG_PMAC_F_CLASS)
+		igcfg->bClassDefault = ig_pmac.def_pmac_tc;
+
+	if (flags & IG_PMAC_F_PMAPENA)
+		igcfg->bPmapEna = ig_pmac.def_pmac_en_pmap;
+
+	if (flags & IG_PMAC_F_PMAP)
+		igcfg->bPmapDefault = ig_pmac.def_pmac_pmap;
+
+	for (i = 0; i < ARRAY_SIZE(igcfg->defPmacHdr); i++) {
+		if (flags & BIT(pmac_hdr++))
+			igcfg->defPmacHdr[i] = ig_pmac.def_pmac_hdr[i];
+	}
+
+	/* Set PMAC ingress config to GSWIP */
+	gsw_ops->gsw_pmac_ops.Pmac_Ig_CfgSet(gsw_ops, igcfg);
+
+#if defined(CONFIG_INTEL_DATAPATH_DBG)
+	dump_pmac_igcfg(ep, igcfg);
+#endif
+}
+
+static int update_pmac_ingress_cfg(int inst, u32 ep, dp_pmac_cfg_t *pmac_cfg)
+{
+	struct pmac_port_info *port_info;
+	GSW_PMAC_Ig_Cfg_t igcfg = {0};
+	struct dp_rx_ring *rx_ring;
+	u8 cid, pid;
+	int i, j;
+	u16 nid;
+
+	port_info = get_dp_port_info(inst, ep);
+
+	/* Check for DC port */
+	if (port_info->alloc_flags & DP_F_ACA) {
+		if (!port_info->num_rx_ring) {
+			pr_err("%s no DMA chan: port %d ig pmac\n",
+			       __func__, ep);
+			return DP_FAILURE;
+		}
+
+		for (i = 0; i < port_info->num_rx_ring; i++) {
+			rx_ring = &port_info->rx_ring[i];
+			dp_dma_parse_id(rx_ring->out_dma_ch_to_gswip,
+					&cid, &pid, &nid);
+			igcfg.nPmacId = cid;
+			for (j = 0; j < rx_ring->num_out_tx_dma_ch; j++) {
+				igcfg.nTxDmaChanId = nid + j;
+				_update_pmac_ingress_cfg(inst, ep, pmac_cfg,
+							 &igcfg);
 			}
-			gsw_ops->gsw_pmac_ops.Pmac_Ig_CfgGet(gsw_ops, &igcfg);
-
-			/*update igcfg and write back to gsw */
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_ERR_DISC)
-				igcfg.bErrPktsDisc =
-					pmac_cfg->ig_pmac.err_disc;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PRESENT)
-				igcfg.bPmacPresent = pmac_cfg->ig_pmac.pmac;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_SUBIF)
-				//igcfg.bSubIdDefault =
-				igcfg.eSubId =
-					pmac_cfg->ig_pmac.def_pmac_subifid;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_SPID)
-				igcfg.bSpIdDefault =
-					pmac_cfg->ig_pmac.def_pmac_src_port;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_CLASSENA)
-				igcfg.bClassEna =
-					pmac_cfg->ig_pmac.def_pmac_en_tc;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_CLASS)
-				igcfg.bClassDefault =
-					pmac_cfg->ig_pmac.def_pmac_tc;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMAPENA)
-				igcfg.bPmapEna =
-					pmac_cfg->ig_pmac.def_pmac_en_pmap;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMAP)
-				igcfg.bPmapDefault =
-					pmac_cfg->ig_pmac.def_pmac_pmap;
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR1)
-				igcfg.defPmacHdr[0] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[0];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR2)
-				igcfg.defPmacHdr[1] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[1];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR3)
-				igcfg.defPmacHdr[2] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[2];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR4)
-				igcfg.defPmacHdr[3] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[3];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR5)
-				igcfg.defPmacHdr[4] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[4];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR6)
-				igcfg.defPmacHdr[5] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[5];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR7)
-				igcfg.defPmacHdr[6] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[6];
-
-			if (pmac_cfg->ig_pmac_flags & IG_PMAC_F_PMACHDR8)
-				igcfg.defPmacHdr[7] =
-					pmac_cfg->ig_pmac.def_pmac_hdr[7];
-
-			DP_DEBUG(DP_DBG_FLAG_DBG,
-				 "\nPMAC %d igcfg configuration:\n", port);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.nPmacId=%d\n",
-				 igcfg.nPmacId);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.nTxDmaChanId=%d\n",
-				 igcfg.nTxDmaChanId);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bErrPktsDisc=%d\n",
-				 igcfg.bErrPktsDisc);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bPmapDefault=%d\n",
-				 igcfg.bPmapDefault);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bPmapEna=%d\n",
-				 igcfg.bPmapEna);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bClassDefault=%d\n",
-				 igcfg.bClassDefault);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bClassEna=%d\n",
-				 igcfg.bClassEna);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bSubIdDefault=%d\n",
-				 igcfg.eSubId);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bSpIdDefault=%d\n",
-				 igcfg.bSpIdDefault);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.bPmacPresent=%d\n",
-				 igcfg.bPmacPresent);
-			DP_DEBUG(DP_DBG_FLAG_DBG, "igcfg.defPmacHdr=");
-
-			for (k = 0;
-			     k <
-			     sizeof(igcfg.defPmacHdr) /
-			     sizeof(igcfg.defPmacHdr[0]); k++)
-				DP_DEBUG(DP_DBG_FLAG_DBG, "0x%x ",
-					 igcfg.defPmacHdr[k]);
-
-			DP_DEBUG(DP_DBG_FLAG_DBG, "\n");
-
-			gsw_ops->gsw_pmac_ops.Pmac_Ig_CfgSet(gsw_ops, &igcfg);
+		}
+	} else {
+		/* Streaming port */
+		dp_dma_parse_id(port_info->dma_chan, &cid, &pid, &nid);
+		igcfg.nPmacId = cid;
+		if (!port_info->num_dma_chan) {
+			pr_err("%s no DMA chan: port %d ig pmac\n",
+			       __func__, ep);
+			return DP_FAILURE;
 		}
 
-		kfree(dqport.deq_info);
+		for (i = 0; i < port_info->num_dma_chan; i++) {
+			igcfg.nTxDmaChanId = nid + i;
+			_update_pmac_ingress_cfg(inst, ep, pmac_cfg, &igcfg);
+		}
 	}
 
-	/*set egress port via pmac port id */
-	if (!pmac_cfg->eg_pmac_flags)
-		return 0;
+	return DP_SUCCESS;
+}
+
+static int update_pmac_egress_cfg(int inst, u32 ep, dp_pmac_cfg_t *pmac_cfg)
+{
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	egress_pmac_t eg_pmac = pmac_cfg->eg_pmac;
+	u32 flags = pmac_cfg->eg_pmac_flags;
+	GSW_PMAC_Glbl_Cfg_t glbcfg = {0};
+	GSW_QoS_queuePort_t q_map = {0};
+	GSW_PMAC_Eg_Cfg_t egcfg = {0};
+	int i, j;
+
+	/* FIXME: need to simplify this as GSW already has all the info */
+	/* Get egress PMAC ID */
+	q_map.nPortId = ep;
+	if (gsw_ops->gsw_qos_ops.QoS_QueuePortGet(gsw_ops, &q_map)) {
+		pr_err("%s fail to get egress PMAC ID\n", __func__);
+		return DP_FAILURE;
+	}
+	egcfg.nPmacId = q_map.nRedirectPortId;
+	egcfg.nDestPortId = ep;
+	DP_DEBUG(DP_DBG_FLAG_DBG, "bProcFlagsSelect=%u\n",
+		 egcfg.bProcFlagsSelect);
 
-	ret = gsw_ops->gsw_pmac_ops.Pmac_Gbl_CfgGet(gsw_ops, &glblCfg);
-	if (ret) {
-		pr_err("Pmac_Gbl_CfgGet failed\n");
-		return -1;
+	if (gsw_ops->gsw_pmac_ops.Pmac_Gbl_CfgGet(gsw_ops, &glbcfg)) {
+		pr_err("%s GSW_PMAC_GLBL_CFG_GET fail\n", __func__);
+		return DP_FAILURE;
 	}
 
-	for (i = 0; i <= 15; i++) {	/*traffic class */
-		for (j = 0; j <= 3; j++) {	/* flow */
-			/*read back egcfg first from gsw */
-			memset(&egcfg, 0, sizeof(GSW_PMAC_Eg_Cfg_t));
-			egcfg.nPmacId = pmacid;
-			egcfg.nDestPortId = port;
+	for (i = 0; i < MAX_TC; i++) {
+		egcfg.nBslTrafficClass = i;
+		switch (glbcfg.eProcFlagsEgCfg) {
+		/* Use reduced traffic class (saturated to 3)
+		 * and flags (MPE1, MPE2)
+		 */
+		case GSW_PMAC_PROC_FLAGS_MIX:
+			egcfg.bProcFlagsSelect = 1;
+			egcfg.nTrafficClass = FIELD_PREP(GSW_REDUCED_TC, i);
+			egcfg.bMpe2Flag = FIELD_PREP(GSW_MPE2, i);
+			egcfg.bMpe1Flag = FIELD_PREP(GSW_MPE1, i);
+			break;
+		/* Use flags (MPE1, MPE2, DEC, ENC) */
+		case GSW_PMAC_PROC_FLAGS_FLAG:
+			egcfg.bProcFlagsSelect = 1;
+			egcfg.bDecFlag = FIELD_PREP(GSW_DEC, i);
+			egcfg.bEncFlag = FIELD_PREP(GSW_ENC, i);
+			egcfg.bMpe2Flag = FIELD_PREP(GSW_MPE2, i);
+			egcfg.bMpe1Flag = FIELD_PREP(GSW_MPE1, i);
+			break;
+		/* Use traffic class as full 4 bits */
+		default:
+			egcfg.nTrafficClass = i;
+			break;
+		}
+
+		for (j = 0; j < MAX_FLOW; j++) {
 			egcfg.nFlowIDMsb = j;
-			egcfg.nBslTrafficClass = i;
-
-			switch (glblCfg.eProcFlagsEgCfg) {
-			/* Use reduced traffic class (saturated to 3)
-			 * and flags (MPE1, MPE2)
-			 */
-			case GSW_PMAC_PROC_FLAGS_MIX:
-				egcfg.bProcFlagsSelect = 1;
-				egcfg.nTrafficClass = (i & 0xC) >> 2;
-				egcfg.bMpe2Flag = (i & 0x2) >> 1;
-				egcfg.bMpe1Flag = (i & 0x1);
-				break;
-			/* Use flags (MPE1, MPE2, DEC, ENC) */
-			case GSW_PMAC_PROC_FLAGS_FLAG:
-				egcfg.bProcFlagsSelect = 1;
-				egcfg.bDecFlag = (i & 0x8) >> 3;
-				egcfg.bEncFlag = (i & 0x4) >> 2;
-				egcfg.bMpe2Flag = (i & 0x2) >> 1;
-				egcfg.bMpe1Flag = (i & 0x1);
-				break;
-			/* Use traffic class as full 4 bits */
-			default:
-				egcfg.bProcFlagsSelect = 0;
-				egcfg.nTrafficClass = i;
-				break;
-			}
 
-			DP_DEBUG(DP_DBG_FLAG_DBG, "bProcFlagsSelect=%u\n",
-				 egcfg.bProcFlagsSelect);
 			gsw_ops->gsw_pmac_ops.Pmac_Eg_CfgGet(gsw_ops, &egcfg);
 
-			/*update egcfg and write back to gsw */
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_FCS)
-				egcfg.bFcsEna = pmac_cfg->eg_pmac.fcs;
+			if (flags & EG_PMAC_F_FCS)
+				egcfg.bFcsEna = eg_pmac.fcs;
 
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_L2HDR_RM) {
-				egcfg.bRemL2Hdr = pmac_cfg->eg_pmac.rm_l2hdr;
-				egcfg.numBytesRem =
-					pmac_cfg->eg_pmac.num_l2hdr_bytes_rm;
+			if (flags & EG_PMAC_F_L2HDR_RM) {
+				egcfg.bRemL2Hdr = eg_pmac.rm_l2hdr;
+				egcfg.numBytesRem = eg_pmac.num_l2hdr_bytes_rm;
 			}
 
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_PMAC)
-				egcfg.bPmacEna = pmac_cfg->eg_pmac.pmac;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RXID)
-				egcfg.nRxDmaChanId =
-					pmac_cfg->eg_pmac.rx_dma_chan;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_REDIREN)
-				egcfg.bRedirEnable = pmac_cfg->eg_pmac.redir;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_BSLSEG)
-				egcfg.bBslSegmentDisable =
-					pmac_cfg->eg_pmac.bsl_seg;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RESDW1EN)
-				egcfg.bResDW1Enable =
-					pmac_cfg->eg_pmac.res_endw1;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RESDW1)
-				egcfg.nResDW1 = pmac_cfg->eg_pmac.res_dw1;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RES1DW0EN)
-				egcfg.bRes1DW0Enable =
-					pmac_cfg->eg_pmac.res1_endw0;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RES1DW0)
-				egcfg.nRes1DW0 = pmac_cfg->eg_pmac.res1_dw0;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RES2DW0EN)
-				egcfg.bRes2DW0Enable =
-					pmac_cfg->eg_pmac.res2_endw0;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_RES2DW0)
-				egcfg.nRes2DW0 = pmac_cfg->eg_pmac.res2_dw0;
-
-			/*if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_TCENA)
-			 *    egcfg.bTCEnable = pmac_cfg->eg_pmac.tc_enable;
-			 */
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_DECFLG)
-				egcfg.bDecFlag = pmac_cfg->eg_pmac.dec_flag;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_ENCFLG)
-				egcfg.bEncFlag = pmac_cfg->eg_pmac.enc_flag;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_MPE1FLG)
-				egcfg.bMpe1Flag = pmac_cfg->eg_pmac.mpe1_flag;
-
-			if (pmac_cfg->eg_pmac_flags & EG_PMAC_F_MPE2FLG)
-				egcfg.bMpe2Flag = pmac_cfg->eg_pmac.mpe2_flag;
-#if defined(CONFIG_INTEL_DATAPATH_DBG) && CONFIG_INTEL_DATAPATH_DBG
-			if (dp_dbg_flag) {
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "\nPMAC %d egcfg configuration:\n",
-					 port);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nPmacId=%d\n",
-					 egcfg.nPmacId);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nRxDmaChanId=%d\n",
-					 egcfg.nRxDmaChanId);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bRemL2Hdr=%d\n",
-					 egcfg.bRemL2Hdr);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.numBytesRem=%d\n",
-					 egcfg.numBytesRem);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bFcsEna=%d\n", egcfg.bFcsEna);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bPmacEna=%d\n",
-					 egcfg.bPmacEna);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bResDW1Ena=%d\n",
-					 egcfg.bResDW1Enable);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nResDW1=%d\n", egcfg.nResDW1);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bRes1DW0Ena=%d\n",
-					 egcfg.bRes1DW0Enable);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nRes1DW0=%d\n",
-					 egcfg.nRes1DW0);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bRes2DW0Ena=%d\n",
-					 egcfg.bRes2DW0Enable);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nRes2DW0=%d\n",
-					 egcfg.nRes2DW0);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nDestPortId=%d\n",
-					 egcfg.nDestPortId);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bTCEnable=%d\n",
-					 egcfg.bTCEnable);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nTrafficClass=%d\n",
-					 egcfg.nTrafficClass);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nBslTrafficClass=%d\n",
-					 egcfg.nBslTrafficClass);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.nFlowIDMsb=%d\n",
-					 egcfg.nFlowIDMsb);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bDecFlag=%d\n",
-					 egcfg.bDecFlag);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bEncFlag=%d\n",
-					 egcfg.bEncFlag);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bMpe1Flag=%d\n",
-					 egcfg.bMpe1Flag);
-				DP_DEBUG(DP_DBG_FLAG_DBG,
-					 "egcfg.bMpe2Flag=%d\n",
-					 egcfg.bMpe2Flag);
-			}
-#endif
+			if (flags & EG_PMAC_F_PMAC)
+				egcfg.bPmacEna = eg_pmac.pmac;
+
+			if (flags & EG_PMAC_F_RXID)
+				egcfg.nRxDmaChanId = eg_pmac.rx_dma_chan;
+
+			if (flags & EG_PMAC_F_REDIREN)
+				egcfg.bRedirEnable = eg_pmac.redir;
+
+			if (flags & EG_PMAC_F_BSLSEG)
+				egcfg.bBslSegmentDisable = eg_pmac.bsl_seg;
+
+			if (flags & EG_PMAC_F_RESDW1EN)
+				egcfg.bResDW1Enable = eg_pmac.res_endw1;
+
+			if (flags & EG_PMAC_F_RESDW1)
+				egcfg.nResDW1 = eg_pmac.res_dw1;
+
+			if (flags & EG_PMAC_F_RES1DW0EN)
+				egcfg.bRes1DW0Enable = eg_pmac.res1_endw0;
+
+			if (flags & EG_PMAC_F_RES1DW0)
+				egcfg.nRes1DW0 = eg_pmac.res1_dw0;
+
+			if (flags & EG_PMAC_F_RES2DW0EN)
+				egcfg.bRes2DW0Enable = eg_pmac.res2_endw0;
+
+			if (flags & EG_PMAC_F_RES2DW0)
+				egcfg.nRes2DW0 = eg_pmac.res2_dw0;
+
+			if (flags & EG_PMAC_F_DECFLG)
+				egcfg.bDecFlag = eg_pmac.dec_flag;
+
+			if (flags & EG_PMAC_F_ENCFLG)
+				egcfg.bEncFlag = eg_pmac.enc_flag;
+
+			if (flags & EG_PMAC_F_MPE1FLG)
+				egcfg.bMpe1Flag = eg_pmac.mpe1_flag;
+
+			if (flags & EG_PMAC_F_MPE2FLG)
+				egcfg.bMpe2Flag = eg_pmac.mpe2_flag;
+
 			gsw_ops->gsw_pmac_ops.Pmac_Eg_CfgSet(gsw_ops, &egcfg);
+
+#if defined(CONFIG_INTEL_DATAPATH_DBG)
+			dump_pmac_egcfg(ep, &egcfg);
+#endif
 		}
 	}
 
-	return 0;
+	return DP_SUCCESS;
 }
 
-/*flag: bit 0 for cpu
- * bit 1 for mpe1,bit 2 for mpe2, bit 3 for mpe3;
- */
-#define GSW_L_BASE_ADDR        (0xBC000000)
-#define GSW_R_BASE_ADDR        (0xBA000000)
-#define FDMA_PASR_ADDR         (0xA47)
+int dp_gsw_set_pmac(int inst, u32 ep, dp_pmac_cfg_t *pmac_cfg)
+{
+	if (!pmac_cfg) {
+		pr_err("%s: pmac_cfg is NULL\n", __func__);
+		return DP_FAILURE;
+	}
+
+	if (pmac_cfg->ig_pmac_flags) {
+		if (update_pmac_ingress_cfg(inst, ep, pmac_cfg))
+			return DP_FAILURE;
+	}
 
-int dp_set_gsw_parser_31(u8 flag, u8 cpu, u8 mpe1,
-			 u8 mpe2, u8 mpe3)
+	if (pmac_cfg->eg_pmac_flags) {
+		if (update_pmac_egress_cfg(inst, ep, pmac_cfg))
+			return DP_FAILURE;
+	}
+
+	return DP_SUCCESS;
+}
+
+/* FIXME: Need to support multiple instances */
+int dp_gsw_set_parser(u8 flag, u8 cpu, u8 mpe1, u8 mpe2, u8 mpe3)
 {
-	GSW_CPU_PortCfg_t param = {0};
-	GSW_register_t reg;
-	struct core_ops *gsw_handle = dp_port_prop[0].ops[0];/*gswip o */
-
-	if (gsw_handle->gsw_common_ops.CPU_PortCfgGet(gsw_handle, &param)) {
-		pr_err("Failed GSW_CPU_PORT_CFG_GET\n");
-		return -1;
-	}
-	DP_DEBUG(DP_DBG_FLAG_DBG, "old flag=0x%x cpu=%d mpe1/2/3=%d/%d/%d\n",
-		 flag, param.eNoMPEParserCfg,
-		 param.eMPE1ParserCfg, param.eMPE2ParserCfg,
-		 param.eMPE1MPE2ParserCfg);
-	DP_DEBUG(DP_DBG_FLAG_DBG, "new flag=0x%x cpu=%d mpe1/2/3=%d/%d/%d\n",
+	struct core_ops *gsw_ops = dp_port_prop[0].ops[0];
+	GSW_CPU_PortCfg_t cpu_cfg = {0};
+	GSW_register_t reg = {0};
+
+	if (gsw_ops->gsw_common_ops.CPU_PortCfgGet(gsw_ops, &cpu_cfg)) {
+		pr_err("%s: GSW_CPU_PORT_CFG_GET fail\n", __func__);
+		return DP_FAILURE;
+	}
+
+	DP_DEBUG(DP_DBG_FLAG_DBG, "Org flag=0x%x cpu=%d mpe1/2/3=%d/%d/%d\n",
+		 flag, cpu_cfg.eNoMPEParserCfg, cpu_cfg.eMPE1ParserCfg,
+		 cpu_cfg.eMPE2ParserCfg, cpu_cfg.eMPE1MPE2ParserCfg);
+	DP_DEBUG(DP_DBG_FLAG_DBG, "New flag=0x%x cpu=%d mpe1/2/3=%d/%d/%d\n",
 		 flag, cpu, mpe1, mpe2, mpe3);
+
 	if (flag & F_MPE_NONE)
-		param.eNoMPEParserCfg = cpu;
+		cpu_cfg.eNoMPEParserCfg = cpu;
 
 	if (flag & F_MPE1_ONLY)
-		param.eMPE1ParserCfg = mpe1;
+		cpu_cfg.eMPE1ParserCfg = mpe1;
 
 	if (flag & F_MPE2_ONLY)
-		param.eMPE2ParserCfg = mpe2;
+		cpu_cfg.eMPE2ParserCfg = mpe2;
 
 	if (flag & F_MPE1_MPE2)
-		param.eMPE1MPE2ParserCfg = mpe3;
-
-	if (gsw_handle->gsw_common_ops.CPU_PortCfgSet(gsw_handle, &param)) {
-		pr_err("Failed GSW_CPU_PORT_CFG_SET\n");
-		return -1;
-	}
-	memset(&reg, 0, sizeof(reg));
-	reg.nRegAddr = 0x001;
-	gsw_handle->gsw_common_ops.RegisterGet(gsw_handle, &reg);
-	reg.nData |= 1 << 0; /* to enable SLL memory write.
-			      * GSWIP HW will write parser flags only when this
-			      * bit is set
-			      */
-	gsw_handle->gsw_common_ops.RegisterSet(gsw_handle, &reg);
-
-	dp_parser_info_refresh(param.eNoMPEParserCfg,
-			       param.eMPE1ParserCfg,
-			       param.eMPE2ParserCfg,
-			       param.eMPE1MPE2ParserCfg, 0);
-	return 0;
+		cpu_cfg.eMPE1MPE2ParserCfg = mpe3;
+
+	if (gsw_ops->gsw_common_ops.CPU_PortCfgSet(gsw_ops, &cpu_cfg)) {
+		pr_err("%s: GSW_CPU_PORT_CFG_SET fail\n", __func__);
+		return DP_FAILURE;
+	}
+
+	/* FIXME: GSW should check this */
+	/* GSWIP will write parser flags only when SLL memory is enable */
+	reg.nRegAddr = ETHSW_CLK;
+	gsw_ops->gsw_common_ops.RegisterGet(gsw_ops, &reg);
+	reg.nData |= SLL3MEM_EN;
+	gsw_ops->gsw_common_ops.RegisterSet(gsw_ops, &reg);
+
+	dp_parser_info_refresh(cpu_cfg.eNoMPEParserCfg,
+			       cpu_cfg.eMPE1ParserCfg,
+			       cpu_cfg.eMPE2ParserCfg,
+			       cpu_cfg.eMPE1MPE2ParserCfg,
+			       0);
+
+	return DP_SUCCESS;
 }
 
-int dp_get_gsw_parser_31(u8 *cpu, u8 *mpe1, u8 *mpe2,
-			 u8 *mpe3)
+/* FIXME: Need to support multiple instances */
+int dp_gsw_get_parser(u8 *cpu, u8 *mpe1, u8 *mpe2, u8 *mpe3)
 {
-	GSW_CPU_PortCfg_t param = {0};
-	struct core_ops *gsw_handle = dp_port_prop[0].ops[0]; /*gswip 0*/
+	struct core_ops *gsw_ops = dp_port_prop[0].ops[0];
+	GSW_CPU_PortCfg_t cpu_cfg = {0};
 
-	if (gsw_handle->gsw_common_ops.CPU_PortCfgGet(gsw_handle, &param)) {
-		pr_err("Failed GSW_CPU_PORT_CFG_GET\n");
-		return -1;
+	if (gsw_ops->gsw_common_ops.CPU_PortCfgGet(gsw_ops, &cpu_cfg)) {
+		pr_err("%s: GSW_CPU_PORT_CFG_GET fail\n", __func__);
+		return DP_FAILURE;
 	}
-	dp_parser_info_refresh(param.eNoMPEParserCfg,
-			       param.eMPE1ParserCfg,
-			       param.eMPE2ParserCfg,
-			       param.eMPE1MPE2ParserCfg, 1);
+
+	dp_parser_info_refresh(cpu_cfg.eNoMPEParserCfg,
+			       cpu_cfg.eMPE1ParserCfg,
+			       cpu_cfg.eMPE2ParserCfg,
+			       cpu_cfg.eMPE1MPE2ParserCfg,
+			       1);
 
 	if (cpu) {
-		*cpu = param.eNoMPEParserCfg;
+		*cpu = cpu_cfg.eNoMPEParserCfg;
 		DP_DEBUG(DP_DBG_FLAG_DBG, "  cpu=%d\n", *cpu);
 	}
 
 	if (mpe1) {
-		*mpe1 = param.eMPE1ParserCfg;
+		*mpe1 = cpu_cfg.eMPE1ParserCfg;
 		DP_DEBUG(DP_DBG_FLAG_DBG, "  mpe1=%d\n", *mpe1);
 	}
 
 	if (mpe2) {
-		*mpe2 = param.eMPE2ParserCfg;
+		*mpe2 = cpu_cfg.eMPE2ParserCfg;
 		DP_DEBUG(DP_DBG_FLAG_DBG, "  mpe2=%d\n", *mpe2);
 	}
 
 	if (mpe3) {
-		*mpe3 = param.eMPE1MPE2ParserCfg;
+		*mpe3 = cpu_cfg.eMPE1MPE2ParserCfg;
 		DP_DEBUG(DP_DBG_FLAG_DBG, "  mpe3=%d\n", *mpe3);
 	}
-	return 0;
+
+	return DP_SUCCESS;
 }
 
-int gsw_mib_reset_31(int dev, u32 flag)
+/* FIXME: Need to support multiple instances */
+int dp_gsw_reset_mib(int dev, u32 flag)
 {
-	struct core_ops *gsw_handle;
-	GSW_return_t ret;
+	struct core_ops *gsw_ops = dp_port_prop[0].ops[0];
 	GSW_RMON_clear_t rmon_clear;
 
-	gsw_handle = dp_port_prop[0].ops[0];
 	rmon_clear.eRmonType = GSW_RMON_ALL_TYPE;
-	ret = gsw_handle->gsw_rmon_ops.RMON_Clear(gsw_handle, &rmon_clear);
-
-	if (ret != GSW_statusOk) {
-		pr_err("R:GSW_RMON_CLEAR failed for GSW_RMON_ALL_TYPE\n");
-		return -1;
+	if (gsw_ops->gsw_rmon_ops.RMON_Clear(gsw_ops, &rmon_clear)) {
+		pr_err("Failed GSW_RMON_CLEAR for GSW_RMON_ALL_TYPE\n");
+		return DP_FAILURE;
 	}
-	return ret;
+
+	return DP_SUCCESS;
 }
 
-/* Return allocated ctp number */
-struct gsw_itf *ctp_port_assign(int inst, u8 ep, int bp_default,
-				u32 flags, struct dp_dev_data *data)
+/* Return allocated ctp info */
+struct gsw_itf *dp_gsw_assign_ctp(int inst, u8 ep, int bp_def, u32 flags,
+				  struct dp_dev_data *data)
 {
-	GSW_CTP_portAssignment_t ctp_assign;
-	const struct ctp_assign *assign = &ctp_assign_def;
-	int i, alloc_flag;
-	u16 num;
-	struct core_ops *gsw_handle;
-	struct ctp_ops *gsw_ctp;
 	struct pmac_port_info *port_info = get_dp_port_info(inst, ep);
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	const struct ctp_assign *ctp_assign;
+	GSW_CTP_portAssignment_t ctp = {0};
+	struct ctp_ops *gsw_ctp;
 	struct dp_cap *cap;
 
-	memset(&ctp_assign, 0, sizeof(ctp_assign));
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	gsw_ctp = &gsw_handle->gsw_ctp_ops;
-	alloc_flag = port_info->alloc_flags;
+	gsw_ctp = &gsw_ops->gsw_ctp_ops;
 
 	if (flags & DP_F_DEREGISTER) {
-		pr_err("Need to Free CTP Port here for ep=%d\n", ep);
-		ctp_assign.nLogicalPortId = ep;
-		ctp_assign.eMode = itf_assign[ep].mode;
-		ctp_assign.nFirstCtpPortId = itf_assign[ep].start;
-		ctp_assign.nNumberOfCtpPort = itf_assign[ep].n;
-		if (gsw_ctp->CTP_PortAssignmentFree(gsw_handle, &ctp_assign)) {
-			pr_err("Failed to allc CTP for ep=%d blk=%d mode=%d\n",
-			       ep, assign->num, assign->emode);
-			return NULL;
-		}
+		DP_DEBUG(DP_DBG_FLAG_DBG, "Free CTP Port for ep=%d\n", ep);
+		ctp.nLogicalPortId = ep;
+		ctp.eMode = itf_assign[ep].mode;
+		ctp.nFirstCtpPortId = itf_assign[ep].start;
+		ctp.nNumberOfCtpPort = itf_assign[ep].n;
+		if (gsw_ctp->CTP_PortAssignmentFree(gsw_ops, &ctp))
+			pr_err("%s GSW_CTP_PORT_ASSIGNMENT_FREE fail:",
+			       __func__);
+			pr_err(" ep=%d blk=%d mode=%d\n",
+			       ep, ctp.nNumberOfCtpPort, ctp.eMode);
 		return NULL;
 	}
 
-	for (i = 0; i < ARRAY_SIZE(ctp_assign_info); i++) {
-		if ((ctp_assign_info[i].flag & alloc_flag) ==
-		    ctp_assign_info[i].flag) {
-			assign = &ctp_assign_info[i];
-			break;
-		}
-	}
+	ctp_assign = dp_gsw_get_ctp_assign(inst, port_info->alloc_flags);
+	if (!ctp_assign)
+		ctp_assign = dp_gsw_get_ctp_assign(inst, CTP_DEFAULT);
+
 	if (data && data->max_ctp)
-		num = data->max_ctp;
+		ctp.nNumberOfCtpPort  = data->max_ctp;
 	else
-		num = assign->num;
-	ctp_assign.nLogicalPortId = ep;
-	ctp_assign.eMode = assign->emode;
-	ctp_assign.nBridgePortId = bp_default;
-	ctp_assign.nFirstCtpPortId = 0;
-	ctp_assign.nNumberOfCtpPort = num;
-	if (gsw_ctp->CTP_PortAssignmentAlloc(gsw_handle, &ctp_assign)) {
-		pr_err("Failed CTP Assignment for ep=%d blk size=%d mode=%s\n",
-		       ep, num, ctp_mode_string(assign->emode));
+		ctp.nNumberOfCtpPort  = ctp_assign->max_ctp;
+	ctp.eMode = ctp_assign->emode;
+	ctp.nBridgePortId = bp_def;
+	ctp.nLogicalPortId = ep;
+	if (gsw_ctp->CTP_PortAssignmentAlloc(gsw_ops, &ctp)) {
+		pr_err("%s GSW_CTP_PORT_ASSIGNMENT_ALLOC fail:", __func__);
+		pr_err(" ep=%d blk size=%d mode=%s\n",
+		       ep, ctp.nNumberOfCtpPort,
+		       ctp_md_str(ctp_assign->emode));
 		return NULL;
 	}
 
-	DP_DEBUG(DP_DBG_FLAG_DBG, "assign ep=%d with eMode=%d ctp_max:%d\n",
-		 ep, assign->emode, ctp_assign.nNumberOfCtpPort);
-	itf_assign[ep].mode = assign->emode;
-	itf_assign[ep].n = ctp_assign.nNumberOfCtpPort;
-	itf_assign[ep].start = ctp_assign.nFirstCtpPortId;
-	itf_assign[ep].end = ctp_assign.nFirstCtpPortId +
-			     ctp_assign.nNumberOfCtpPort - 1;
+	DP_DEBUG(DP_DBG_FLAG_DBG, "Assign ep=%d with eMode=%d ctp_max:%d\n",
+		 ep, ctp_assign->emode, ctp.nNumberOfCtpPort);
+
+	itf_assign[ep].mode = ctp_assign->emode;
+	itf_assign[ep].n = ctp.nNumberOfCtpPort;
+	itf_assign[ep].start = ctp.nFirstCtpPortId;
+	itf_assign[ep].end = ctp.nFirstCtpPortId + ctp.nNumberOfCtpPort - 1;
 	itf_assign[ep].ep = ep;
-	port_info->subif_max =
-			assign->num_subif >= num ? assign->num_subif : num;
-	port_info->ctp_max = ctp_assign.nNumberOfCtpPort;
+	port_info->subif_max = ctp_assign->max_subif >= ctp.nNumberOfCtpPort ?
+			       ctp_assign->max_subif : ctp.nNumberOfCtpPort;
+	port_info->ctp_max = ctp.nNumberOfCtpPort;
 
 	cap = &get_dp_prop_info(inst)->cap;
 
 	/* HW capaility have only 3 modes 8-bit, 9-bit or Non-WLAN mode */
-	if (assign->emode == GSW_LOGICAL_PORT_8BIT_WLAN) {
-		port_info->vap_offset =
-			cap->vap_offset[GSW_LOGICAL_PORT_8BIT_WLAN];
-		port_info->vap_mask =
-			cap->vap_mask[GSW_LOGICAL_PORT_8BIT_WLAN];
-	} else if (assign->emode == GSW_LOGICAL_PORT_9BIT_WLAN) {
+	if (ctp_assign->emode == GSW_LOGICAL_PORT_8BIT_WLAN ||
+	    ctp_assign->emode == GSW_LOGICAL_PORT_9BIT_WLAN) {
 		port_info->vap_offset =
-			cap->vap_offset[GSW_LOGICAL_PORT_9BIT_WLAN];
+			cap->vap_offset[ctp_assign->emode];
 		port_info->vap_mask =
-			cap->vap_mask[GSW_LOGICAL_PORT_9BIT_WLAN];
+			cap->vap_mask[ctp_assign->emode];
 	} else {
 		/* GPON, EPON, G.INT take this mode */
 		port_info->vap_offset =
@@ -605,101 +655,102 @@ struct gsw_itf *ctp_port_assign(int inst, u8 ep, int bp_default,
 	}
 
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-	port_info->swdev_en = assign->swdev_enable;
+	port_info->swdev_en = ctp_assign->swdev_en;
 #endif
+
 	return &itf_assign[ep];
 }
 
-int set_port_lookup_mode_31(int inst, u8 ep, u32 flags)
+int dp_gsw_set_port_lu_md(int inst, u8 ep, u32 flags)
 {
-	int i, alloc_flag;
-	const struct ctp_assign *assign = &ctp_assign_def;
 	struct pmac_port_info *port_info = get_dp_port_info(inst, ep);
+	const struct ctp_assign *ctp_assign;
 
-	alloc_flag = port_info->alloc_flags;
-	for (i = 0; i < ARRAY_SIZE(ctp_assign_info); i++) {
-		if ((ctp_assign_info[i].flag & alloc_flag) ==
-		    ctp_assign_info[i].flag) {
-			assign = &ctp_assign_info[i];
-			break;
-		}
-	}
-	port_info->cqe_lu_mode = assign->lookup_mode;
-	port_info->gsw_mode = (u32)assign->emode;
-	return 0;
+	ctp_assign = dp_gsw_get_ctp_assign(inst, port_info->alloc_flags);
+	if (!ctp_assign)
+		ctp_assign = dp_gsw_get_ctp_assign(inst, CTP_DEFAULT);
+	port_info->cqe_lu_mode = ctp_assign->cqe_lu_md;
+	port_info->gsw_mode = ctp_assign->emode;
+
+	return DP_SUCCESS;
 }
 
-/*Allocate a bridge port with specified FID and hardcoded CPU port member */
-int alloc_bridge_port(int inst, int port_id, int subif_ix,
-		      int fid, int bp_member, int flags)
+/* Allocate a bridge port with specified bridge ID (FID)
+ * and hardcoded CPU port member
+ */
+int dp_gsw_alloc_bp(int inst, int ep, int subif_ix, int fid, int bp_member,
+		    int flags)
 {
-	GSW_BRIDGE_portAlloc_t bp = {0};
+	struct pmac_port_info *port_info = get_dp_port_info(inst, ep);
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	GSW_BRIDGE_portAlloc_t bp_alloc = {0};
 	GSW_BRIDGE_portConfig_t bp_cfg = {0};
-	struct core_ops *gsw_handle;
+	struct dp_subif_info *subif_info;
 	struct brdgport_ops *gsw_bp;
-	GSW_return_t ret;
-
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
-	/*allocate a free bridge port */
-	memset(&bp, 0, sizeof(bp));
-	ret = gsw_bp->BridgePort_Alloc(gsw_handle, &bp);
-	if (ret != GSW_statusOk) {
-		pr_err("Failed to get a bridge port\n");
-		return -1;
-	}
-	/*set this new bridge port with specified bridge ID(fid)
-	 *and bridge port map
+
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
+
+	/* Allocate a free bridge port */
+	if (gsw_bp->BridgePort_Alloc(gsw_ops, &bp_alloc)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n", __func__);
+		return DP_FAILURE;
+	}
+
+	/* Set this new bridge port with specified bridge ID(fid)
+	 * and bridge port map
 	 */
-	bp_cfg.nBridgePortId = bp.nBridgePortId;
-	bp_cfg.nDestLogicalPortId = port_id;
+	bp_cfg.nBridgePortId = bp_alloc.nBridgePortId;
+	bp_cfg.nDestLogicalPortId = ep;
 	bp_cfg.nDestSubIfIdGroup = subif_ix;
-	/* By default Disable src mac learning for registered
+	/* By default disable src mac learning for registered
 	 * non CPU bridge port with DP
 	 */
-	if (get_dp_port_subif(get_dp_port_info(inst, port_id),
-			      subif_ix)->mac_learn_dis == DP_MAC_LEARNING_DIS)
+	subif_info = get_dp_port_subif(port_info, subif_ix);
+	if (subif_info->mac_learn_dis == DP_MAC_LEARNING_DIS)
 		bp_cfg.bSrcMacLearningDisable = 1;
 	else
 		bp_cfg.bSrcMacLearningDisable = 0;
-	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID |
+	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_MC_SRC_MAC_LEARNING |
+		       GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_CTP_MAPPING |
 		       GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP |
-		       GSW_BRIDGE_PORT_CONFIG_MASK_MC_SRC_MAC_LEARNING |
-		       GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_CTP_MAPPING;
+		       GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID;
 	bp_cfg.nBridgeId = fid;
 	if (!(flags & DP_SUBIF_BP_CPU_DISABLE))
-		SET_BP_MAP(bp_cfg.nBridgePortMap, bp_member); /*CPU*/
-	ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, &bp_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("Failed to set bridge id(%d) and port map for bp= %d\n",
+		SET_BP_MAP(bp_cfg.nBridgePortMap, bp_member); /* CPU */
+	if (gsw_bp->BridgePort_ConfigSet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail", __func__);
+		pr_err(" to set bridge id(%d) and port map for bp= %d\n",
 		       fid, bp_cfg.nBridgePortId);
 		goto err;
 	}
 
-	/* ADD this bridge port to CPU bridge port's member.
+	/* Add this bridge port to CPU bridge port's member.
 	 * Need read back first
 	 */
 	bp_cfg.nBridgePortId = bp_member;
 	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
-	ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, &bp_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("Failed to get bridge port's member for bridgeport=%d\n",
+	if (gsw_bp->BridgePort_ConfigGet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail", __func__);
+		pr_err(" to get bridge port's member for bridgeport=%d\n",
 		       bp_cfg.nBridgePortId);
 		goto err;
 	}
+
 	if (!(flags & DP_SUBIF_BP_CPU_DISABLE))
-		SET_BP_MAP(bp_cfg.nBridgePortMap, bp.nBridgePortId);
-	ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, &bp_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("Failed to set bridge port's member for bridgeport=%d\n",
+		SET_BP_MAP(bp_cfg.nBridgePortMap, bp_alloc.nBridgePortId);
+	if (gsw_bp->BridgePort_ConfigSet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail", __func__);
+		pr_err(" to set bridge port's member for bridgeport=%d\n",
 		       bp_cfg.nBridgePortId);
 		goto err;
 	}
 
-	return bp.nBridgePortId;
+	return bp_alloc.nBridgePortId;
+
 err:
-	gsw_bp->BridgePort_Free(gsw_handle, &bp);
-	return -1;
+	gsw_bp->BridgePort_Free(gsw_ops, &bp_alloc);
+
+	return DP_FAILURE;
 }
 
 /* Free one GSWIP bridge port
@@ -707,247 +758,230 @@ int alloc_bridge_port(int inst, int port_id, int subif_ix,
  * according to this port memeber, from this deleing bridge port
  * from its member's member Free this bridge port
  */
-int free_bridge_port(int inst, int bp)
+int dp_gsw_free_bp(int inst, int bp)
 {
-	GSW_BRIDGE_portConfig_t *tmp = NULL, *tmp2 = NULL;
-	int i, j;
-	GSW_return_t ret;
-	struct core_ops *gsw_handle;
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	GSW_BRIDGE_portConfig_t *bp_cfg, *tmp2 = NULL;
+	GSW_BRIDGE_portAlloc_t bp_alloc = {0};
+	struct device *devm = &g_dp_dev->dev;
 	struct brdgport_ops *gsw_bp;
+	int i, j;
 
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
 	if (bp == CPU_BP)
-		return 0;
-
-	tmp = devm_kzalloc(&g_dp_dev->dev, sizeof(*tmp), GFP_ATOMIC);
-	tmp2 = devm_kzalloc(&g_dp_dev->dev, sizeof(*tmp2), GFP_ATOMIC);
-	if (!tmp || !tmp2)
-		goto FREE_EXIT;
-
-	/*read out this delting bridge port's member*/
-	tmp->nBridgePortId = bp;
-	tmp->eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
-	ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, tmp);
-	if (ret != GSW_statusOk) {
-		pr_err("Failed GSW_BRIDGE_PORT_CONFIG_GET: %d\n", bp);
-		goto EXIT;
-	}
-	/*remove this delting bridgeport from other bridge port's member*/
-	for (i = 0; i < ARRAY_SIZE(tmp->nBridgePortMap); i++) {
-		for (j = 0; j < 16 /*u16*/; j++) {
-			if (!(tmp->nBridgePortMap[i] & (1 << j)))
-				continue; /*not memmber bit set */
+		return DP_SUCCESS;
+
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
+
+	bp_cfg = devm_kzalloc(devm, sizeof(*bp_cfg), GFP_ATOMIC);
+	if (!bp_cfg)
+		goto exit;
+
+	tmp2 = devm_kzalloc(devm, sizeof(*tmp2), GFP_ATOMIC);
+	if (!tmp2)
+		goto exit;
+
+	/* Read out this delting bridge port's member */
+	bp_cfg->nBridgePortId = bp;
+	bp_cfg->eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
+	if (gsw_bp->BridgePort_ConfigGet(gsw_ops, bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET: %d\n",
+		       __func__, bp);
+		goto err;
+	}
+
+	/* Remove this delting bridgeport from other bridge port's member*/
+	for (i = 0; i < ARRAY_SIZE(bp_cfg->nBridgePortMap); i++) {
+		for (j = 0; j < 16; j++) {
+			if (!(bp_cfg->nBridgePortMap[i] & BIT(j)))
+				continue; /*not member bit set */
 			memset(tmp2->nBridgePortMap, 0,
 			       sizeof(tmp2->nBridgePortMap));
 			tmp2->eMask =
 				GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
 			tmp2->nBridgePortId = i * 16 + j;
-			ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, tmp2);
-			if (ret != GSW_statusOk) {
-				pr_err("Failed GSW_BRIDGE_PORT_CONFIG_GET\n");
-				goto EXIT;
+			if (gsw_bp->BridgePort_ConfigGet(gsw_ops, tmp2)) {
+				pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n",
+				       __func__);
+				goto exit;
 			}
+
 			UNSET_BP_MAP(tmp2->nBridgePortMap, bp);
-			ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, tmp2);
-			if (ret != GSW_statusOk) {
-				pr_err("Failed GSW_BRIDGE_PORT_CONFIG_SET\n");
-				goto EXIT;
+			if (gsw_bp->BridgePort_ConfigSet(gsw_ops, tmp2)) {
+				pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail\n",
+				       __func__);
+				goto exit;
 			}
 		}
 	}
-EXIT:
-	/*Free this bridge port */
-	memset(tmp, 0, sizeof(*tmp));
-	tmp->nBridgePortId = bp;
-	tmp->eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
-	ret = gsw_bp->BridgePort_Free(gsw_handle,
-				      (GSW_BRIDGE_portAlloc_t *)tmp);
-	if (ret != GSW_statusOk)
-		pr_err("Failed to GSW_BRIDGE_PORT_FREE:%d\n", bp);
-FREE_EXIT:
-	devm_kfree(&g_dp_dev->dev, tmp);
-	devm_kfree(&g_dp_dev->dev, tmp2);
-	return 0;
+err:
+	bp_alloc.nBridgePortId = bp;
+	if (gsw_bp->BridgePort_Free(gsw_ops, &bp_alloc))
+		pr_err("%s GSW_BRIDGE_PORT_FREE: %d\n", __func__, bp);
+exit:
+	devm_kfree(devm, bp_cfg);
+	devm_kfree(devm, tmp2);
+
+	return DP_SUCCESS;
 }
 
-int dp_gswip_mac_entry_add(int bport, int fid, int inst, u8 *addr)
+int dp_gsw_add_mac_entry(int bp, int fid, int inst, u8 *addr)
 {
-	GSW_MAC_tableAdd_t tmp;
-	GSW_return_t ret;
-	struct core_ops *gsw_handle;
-
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	memset(&tmp, 0, sizeof(tmp));
-	tmp.bStaticEntry = 1;
-	tmp.nFId = fid;
-	tmp.nPortId = bport;
-	SET_BP_MAP(tmp.nPortMap, bport);
-	tmp.nSubIfId = 0;
-	memcpy(tmp.nMAC, addr, GSW_MAC_ADDR_LEN);
-	ret = gsw_handle->gsw_swmac_ops.MAC_TableEntryAdd(gsw_handle, &tmp);
-	if (ret != GSW_statusOk) {
-		pr_err("fail in setting MAC table static add entry\r\n");
-		return -1;
-	}
-	return 0;
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	GSW_MAC_tableAdd_t mac_tbl = {0};
+
+	mac_tbl.nFId = fid;
+	mac_tbl.nPortId = bp;
+	mac_tbl.bStaticEntry = 1;
+	SET_BP_MAP(mac_tbl.nPortMap, bp);
+	memcpy(mac_tbl.nMAC, addr, GSW_MAC_ADDR_LEN);
+	if (gsw_ops->gsw_swmac_ops.MAC_TableEntryAdd(gsw_ops, &mac_tbl)) {
+		pr_err("GSW_MAC_TABLE_ENTRY_ADD fail\n");
+		return DP_FAILURE;
+	}
+
+	return DP_SUCCESS;
 }
 
-int dp_gswip_mac_entry_del(int bport, int fid, int inst, u8 *addr)
+int dp_gsw_del_mac_entry(int bp, int fid, int inst, u8 *addr)
 {
-	GSW_MAC_tableRemove_t tmp;
-	GSW_MAC_tableQuery_t mac_query;
-	GSW_return_t ret;
-	struct core_ops *gsw_handle;
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	GSW_MAC_tableQuery_t mac_query = {0};
+	GSW_MAC_tableRemove_t mac_tbl = {0};
 	struct swmac_ops *gsw_swmac;
 
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	gsw_swmac = &gsw_handle->gsw_swmac_ops;
-	memset(&tmp, 0, sizeof(tmp));
-	memset(&mac_query, 0, sizeof(mac_query));
+	gsw_swmac = &gsw_ops->gsw_swmac_ops;
 	mac_query.nFId = fid;
 	memcpy(mac_query.nMAC, addr, GSW_MAC_ADDR_LEN);
-	ret = gsw_swmac->MAC_TableEntryQuery(gsw_handle, &mac_query);
-	if (ret != GSW_statusOk) {
-		pr_err("fail in getting MAC query entry\r\n");
-		return -1;
-	}
-	tmp.nFId = fid;
-	memcpy(tmp.nMAC, addr, GSW_MAC_ADDR_LEN);
-	ret = gsw_swmac->MAC_TableEntryRemove(gsw_handle, &tmp);
-	if (ret != GSW_statusOk) {
-		pr_err("fail in setting MAC static entry remove\r\n");
-		return -1;
-	}
-	return 0;
+	if (gsw_swmac->MAC_TableEntryQuery(gsw_ops, &mac_query)) {
+		pr_err("GSW_MAC_TABLE_ENTRY_QUERY fail\n");
+		return DP_FAILURE;
+	}
+
+	mac_tbl.nFId = fid;
+	memcpy(mac_tbl.nMAC, addr, GSW_MAC_ADDR_LEN);
+	if (gsw_swmac->MAC_TableEntryRemove(gsw_ops, &mac_tbl)) {
+		pr_err("GSW_MAC_TABLE_ENTRY_REMOVE fail\n");
+		return DP_FAILURE;
+	}
+
+	return DP_SUCCESS;
 }
 
-int cpu_vlan_mod_dis(int inst)
+int dp_gsw_dis_cpu_vlan_md(int inst)
 {
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
 	GSW_QoS_portRemarkingCfg_t cfg = {0};
-	struct core_ops *ops;
-	int ret;
-
-	ops = dp_port_prop[inst].ops[GSWIP_L];
 
-	cfg.nPortId = 0;
-	ret = ops->gsw_qos_ops.QoS_PortRemarkingCfgGet(ops, &cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("QoS_PortRemarkingCfgGet failed\n");
-		return -1;
+	if (gsw_ops->gsw_qos_ops.QoS_PortRemarkingCfgGet(gsw_ops, &cfg)) {
+		pr_err("GSW_QOS_PORT_REMARKING_CFG_GET fail\n");
+		return DP_FAILURE;
 	}
 
 	cfg.bPCP_EgressRemarkingEnable = LTQ_FALSE;
-	ret = ops->gsw_qos_ops.QoS_PortRemarkingCfgSet(ops, &cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("QoS_PortRemarkingCfgSet failed\n");
-		return -1;
+	if (gsw_ops->gsw_qos_ops.QoS_PortRemarkingCfgSet(gsw_ops, &cfg)) {
+		pr_err("GSW_QOS_PORT_REMARKING_CFG_SET fail\n");
+		return DP_FAILURE;
 	}
 
-	return 0;
+	return DP_SUCCESS;
 }
 
-int dp_set_gsw_pmapper_31(int inst, int bport, int lport,
-			  struct dp_pmapper *mapper, u32 flag)
+int dp_gsw_set_pmapper(int inst, int bp, int ep,
+		       struct dp_pmapper *mapper, u32 flag)
 {
+	struct pmac_port_info *port_info = get_dp_port_info(inst, ep);
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
 	GSW_BRIDGE_portConfig_t bp_cfg = {0};
-	struct core_ops *gsw_handle;
 	struct brdgport_ops *gsw_bp;
-	GSW_return_t ret;
-	int i, index;
-	int ctp;
-	struct pmac_port_info *port_info = get_dp_port_info(inst, lport);
+	int i, ctp, idx = 0;
+
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
 
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
-	DP_DEBUG(DP_DBG_FLAG_DBG,
-		 "set pmapper bport %d inst %d lport %d\n",
-		 bport, inst, lport);
-	bp_cfg.nBridgePortId = bport;
-	bp_cfg.nDestLogicalPortId = lport;
-	bp_cfg.bPmapperEnable = 1;
 	bp_cfg.ePmapperMappingMode = mapper->mode;
+	bp_cfg.nDestLogicalPortId = ep;
+	bp_cfg.nBridgePortId = bp;
+	bp_cfg.bPmapperEnable = 1;
+
+	DP_DEBUG(DP_DBG_FLAG_DBG,
+		 "Set pmapper bp %d inst %d ep %d\n",
+		 bp, inst, ep);
 
-	/* copy the sub if information in all pmapper list*/
-	if (mapper->def_ctp != DP_PMAPPER_DISCARD_CTP) {
+	/* Copy subif information into pmapper list */
+	if (mapper->def_ctp != DP_PMAPPER_DISCARD_CTP)
 		ctp = GET_VAP(mapper->def_ctp, port_info->vap_offset,
 			      port_info->vap_mask);
-	} else {
+	else
 		ctp = PMAPPER_DISC_CTP;
-	}
 	bp_cfg.sPmapper.nDestSubIfIdGroup[0] = ctp;
 
 	for (i = 0; i < DP_PMAP_PCP_NUM; i++) {
-		if (mapper->pcp_map[i] != DP_PMAPPER_DISCARD_CTP) {
+		if (mapper->pcp_map[i] != DP_PMAPPER_DISCARD_CTP)
 			ctp = GET_VAP(mapper->pcp_map[i],
 				      port_info->vap_offset,
 				      port_info->vap_mask);
-		} else {
+		else
 			ctp = PMAPPER_DISC_CTP;
-		}
-		bp_cfg.sPmapper.nDestSubIfIdGroup[1 + i] = ctp;
+
+		bp_cfg.sPmapper.nDestSubIfIdGroup[++idx] = ctp;
 	}
+
 	for (i = 0; i < DP_PMAP_DSCP_NUM; i++) {
-		if (mapper->dscp_map[i] != DP_PMAPPER_DISCARD_CTP) {
+		if (mapper->dscp_map[i] != DP_PMAPPER_DISCARD_CTP)
 			ctp = GET_VAP(mapper->dscp_map[i],
 				      port_info->vap_offset,
 				      port_info->vap_mask);
-		} else {
+		else
 			ctp = PMAPPER_DISC_CTP;
-		}
-		index = 1 + DP_PMAP_PCP_NUM + i;
-		bp_cfg.sPmapper.nDestSubIfIdGroup[index] = ctp;
+		bp_cfg.sPmapper.nDestSubIfIdGroup[++idx] = ctp;
 	}
 	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_CTP_MAPPING;
 
 	DP_DEBUG(DP_DBG_FLAG_DBG,
-		 "call switch api mode %d enable %d eMask 0x%x\n",
+		 "GSW_BRIDGE_PORT_CONFIG_SET mode %d enable %d eMask 0x%x\n",
 		 bp_cfg.ePmapperMappingMode, bp_cfg.bPmapperEnable,
 		 bp_cfg.eMask);
-	ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, &bp_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("fail in setting pmapper\r\n");
-		return -1;
+
+	if (gsw_bp->BridgePort_ConfigSet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail\n", __func__);
+		return DP_FAILURE;
 	}
-	return 0;
+
+	return DP_SUCCESS;
 }
 
-int dp_get_gsw_pmapper_31(int inst, int bport, int lport,
-			  struct dp_pmapper *mapper, u32 flag)
+int dp_gsw_get_pmapper(int inst, int bp, int ep, struct dp_pmapper *mapper,
+		       u32 flag)
 {
+	struct pmac_port_info *info = get_dp_port_info(inst, ep);
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
 	GSW_BRIDGE_portConfig_t bp_cfg = {0};
-	struct core_ops *gsw_handle;
 	struct brdgport_ops *gsw_bp;
-	GSW_return_t ret;
-	int i, index;
 	struct hal_priv *priv;
+	int i, idx = 0;
 	u16 dest;
-	struct pmac_port_info *info = get_dp_port_info(inst, lport);
 
 	priv = (struct hal_priv *)dp_port_prop[inst].priv_hal;
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
-	DP_DEBUG(DP_DBG_FLAG_DBG,
-		 "get bport %d inst %d lport %d\n",
-		 bport, inst, lport);
-	bp_cfg.nBridgePortId = bport;
-	bp_cfg.nDestLogicalPortId = lport;
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
+	bp_cfg.nBridgePortId = bp;
+	bp_cfg.nDestLogicalPortId = ep;
 	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_CTP_MAPPING;
+	DP_DEBUG(DP_DBG_FLAG_DBG, "Get bp %d inst %d ep %d\n",
+		 bp, inst, ep);
 
-	ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, &bp_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("fail in getting pmapper\r\n");
-		return -1;
+	if (gsw_bp->BridgePort_ConfigGet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n", __func__);
+		return DP_FAILURE;
 	}
 
 	DP_DEBUG(DP_DBG_FLAG_DBG,
-		 "after call switch api mode %d enable %d\n",
-		 bp_cfg.ePmapperMappingMode, bp_cfg.bPmapperEnable);
+		 "%s GSW_BRIDGE_PORT_CONFIG_GET mode %d enable %d\n",
+		 __func__, bp_cfg.ePmapperMappingMode, bp_cfg.bPmapperEnable);
 
 	if (!bp_cfg.bPmapperEnable) {
-		pr_err("pmapper not enabled\r\n");
-		return -1;
+		pr_err("%s pmapper not enabled\n", __func__);
+		return DP_FAILURE;
 	}
+
 	mapper->pmapper_id = bp_cfg.sPmapper.nPmapperId;
 	mapper->mode = bp_cfg.ePmapperMappingMode;
 
@@ -958,7 +992,7 @@ int dp_get_gsw_pmapper_31(int inst, int bport, int lport,
 		mapper->def_ctp = SET_VAP(dest, info->vap_offset,
 					  info->vap_mask);
 	for (i = 0; i < DP_PMAP_PCP_NUM; i++) {
-		dest = bp_cfg.sPmapper.nDestSubIfIdGroup[1 + i];
+		dest = bp_cfg.sPmapper.nDestSubIfIdGroup[++idx];
 		if (dest == PMAPPER_DISC_CTP)
 			mapper->pcp_map[i] = DP_PMAPPER_DISCARD_CTP;
 		else
@@ -966,149 +1000,149 @@ int dp_get_gsw_pmapper_31(int inst, int bport, int lport,
 						     info->vap_mask);
 	}
 	for (i = 0; i < DP_PMAP_DSCP_NUM; i++) {
-		index = 1 + DP_PMAP_PCP_NUM + i;
-		dest = bp_cfg.sPmapper.nDestSubIfIdGroup[index];
+		dest = bp_cfg.sPmapper.nDestSubIfIdGroup[++idx];
 		if (dest == PMAPPER_DISC_CTP)
 			mapper->dscp_map[i] = DP_PMAPPER_DISCARD_CTP;
 		else
 			mapper->dscp_map[i] = SET_VAP(dest, info->vap_offset,
 						      info->vap_mask);
 	}
-	return 0;
+
+	return DP_SUCCESS;
 }
 
-int dp_meter_alloc_31(int inst, int *meterid, int flag)
+int dp_gsw_alloc_meter(int inst, int *meterid, int flag)
 {
-	struct core_ops *gsw_handle;
-	struct qos_ops *gsw_qos;
+	struct core_ops *gsw_ops =  dp_port_prop[inst].ops[0];
 	GSW_QoS_meterCfg_t meter_cfg = {0};
-	GSW_return_t ret;
+	struct qos_ops *gsw_qos;
 
-	if (inst < 0) {
-		pr_err("inst invalid\n");
-		return -1;
-	}
 	if (!meterid) {
-		pr_err("meterid NULL\n");
-		return -1;
-	}
-	gsw_handle = dp_port_prop[inst].ops[GSWIP_L];
-	if (!gsw_handle) {
-		pr_err("gsw_handle NULL\n");
-		return -1;
+		pr_err("%s meterid NULL\n", __func__);
+		return DP_FAILURE;
 	}
-	gsw_qos = &gsw_handle->gsw_qos_ops;
+
+	gsw_qos = &gsw_ops->gsw_qos_ops;
+
 	if (flag == DP_F_DEREGISTER && *meterid >= 0) {
 		meter_cfg.nMeterId = *meterid;
-		ret = gsw_qos->QOS_MeterFree(gsw_handle, &meter_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("Meter dealloc failed: %d\n", ret);
-			return -1;
+		if (gsw_qos->QOS_MeterFree(gsw_ops, &meter_cfg)) {
+			pr_err("%s GSW_QOS_METER_FREE fail\n", __func__);
+			return DP_FAILURE;
 		}
-		return 0;
+
+		return DP_SUCCESS;
 	}
-	memset(&meter_cfg, 0, sizeof(meter_cfg));
-	ret = gsw_qos->QOS_MeterAlloc(gsw_handle, &meter_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("Failed to get a meter alloc\n");
+
+	if (gsw_qos->QOS_MeterAlloc(gsw_ops, &meter_cfg)) {
+		pr_err("%s GSW_QOS_METER_ALLOC fail\n", __func__);
 		*meterid = -1;
-		return -1;
+		return DP_FAILURE;
 	}
+
 	*meterid = meter_cfg.nMeterId;
-	return 0;
+
+	return DP_SUCCESS;
 }
 
 static int dp_set_col_mark(struct net_device *dev,
-			   struct dp_meter_cfg  *meter, int flag,
+			   struct dp_meter_cfg *mtr_cfg, int flag,
 			   struct dp_meter_subif *mtr_subif)
 {
-	struct core_ops *gsw_handle;
-	struct ctp_ops *gsw_ctp;
-	struct brdgport_ops *gsw_bp;
-	GSW_BRIDGE_portConfig_t bp_cfg;
-	GSW_CTP_portConfig_t ctp_cfg;
-	GSW_return_t ret;
+	struct core_ops *gsw_ops = dp_port_prop[mtr_subif->inst].ops[0];
+	GSW_BRIDGE_portConfig_t bp_cfg = {0};
+	GSW_CTP_portConfig_t ctp_cfg = {0};
 	struct pmac_port_info *port_info;
+	struct brdgport_ops *gsw_bp;
+	struct ctp_ops *gsw_ctp;
 
-	if (!mtr_subif) {
-		pr_err("mtr_subif NULL\n");
-		return -1;
-	}
-	memset(&bp_cfg, 0, sizeof(GSW_BRIDGE_portConfig_t));
-	memset(&ctp_cfg, 0, sizeof(GSW_CTP_portConfig_t));
-	gsw_handle = dp_port_prop[mtr_subif->inst].ops[GSWIP_L];
-	if (!gsw_handle)
-		return -1;
-	gsw_ctp = &gsw_handle->gsw_ctp_ops;
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
-	if (flag & DP_METER_ATTACH_CTP) {/* CTP port Flag */
+	if (!mtr_cfg || !mtr_subif) {
+		pr_err("%s mtr_cfg/mtr_subif is NULL\n", __func__);
+		return DP_FAILURE;
+	}
+	gsw_ctp = &gsw_ops->gsw_ctp_ops;
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
+
+	/* CTP port */
+	if (flag & DP_METER_ATTACH_CTP) {
 		if (mtr_subif->subif.flag_pmapper) {
-			pr_err("can't use CTP,pmapper is enable\n");
-			return -1;
+			pr_err("%s Cannot use CTP when pmapper is enable\n",
+			       __func__);
+			return DP_FAILURE;
 		}
+
 		port_info = get_dp_port_info(mtr_subif->subif.inst,
 					     mtr_subif->subif.port_id);
 		ctp_cfg.nLogicalPortId = mtr_subif->subif.port_id;
 		ctp_cfg.nSubIfIdGroup  = GET_VAP(mtr_subif->subif.subif,
 						 port_info->vap_offset,
 						 port_info->vap_mask);
-		ret = gsw_ctp->CTP_PortConfigGet(gsw_handle, &ctp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("PortConfigGet API failed :%d\n", ret);
-			return -1;
+		if (gsw_ctp->CTP_PortConfigGet(gsw_ops, &ctp_cfg)) {
+			pr_err("%s GSW_CTP_PORT_CONFIG_GET fail\n", __func__);
+			return DP_FAILURE;
 		}
-		if (meter->dir == DP_DIR_INGRESS) {
+
+		if (mtr_cfg->dir == DP_DIR_INGRESS) {
 			ctp_cfg.eMask = GSW_CTP_PORT_CONFIG_INGRESS_MARKING;
-			ctp_cfg.eIngressMarkingMode = meter->mode;
-		} else if (meter->dir == DP_DIR_EGRESS) {
-			if (meter->mode != DP_INTERNAL) {
+			ctp_cfg.eIngressMarkingMode = mtr_cfg->mode;
+		} else if (mtr_cfg->dir == DP_DIR_EGRESS) {
+			if (mtr_cfg->mode != DP_INTERNAL) {
 				ctp_cfg.eMask =
-				GSW_CTP_PORT_CONFIG_EGRESS_MARKING_OVERRIDE;
+				    GSW_CTP_PORT_CONFIG_EGRESS_MARKING_OVERRIDE;
 				ctp_cfg.bEgressMarkingOverrideEnable = 1;
 				ctp_cfg.eEgressMarkingModeOverride =
-					meter->mode;
+								  mtr_cfg->mode;
 			} else {
 				DP_DEBUG(DP_DBG_FLAG_PAE,
-					 "mode:internal invalid for egress\n");
+					 "%s invalid mode for egress\n",
+					 __func__);
+				return DP_FAILURE;
 			}
 		} else {
-			return -1;
+			return DP_FAILURE;
 		}
-		ret = gsw_ctp->CTP_PortConfigSet(gsw_handle, &ctp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("PortConfigSet API failed :%d\n", ret);
-			return -1;
+
+		if (gsw_ctp->CTP_PortConfigSet(gsw_ops, &ctp_cfg)) {
+			pr_err("%s GSW_CTP_PORT_CONFIG_SET fail\n", __func__);
+			return DP_FAILURE;
 		}
 	}
-	if (flag & DP_METER_ATTACH_BRPORT) {/*BRIDGE port Flag*/
+
+	/* Bridge port */
+	if (flag & DP_METER_ATTACH_BRPORT) {
 		if (!mtr_subif->subif.flag_bp) {
-			pr_err("flag_bp value 0\n");
-			return -1;
+			pr_err("%s flag_bp value 0\n", __func__);
+			return DP_FAILURE;
 		}
+
 		bp_cfg.nBridgePortId = mtr_subif->subif.bport;
-		ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, &bp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("BridgePort_ConfigGet API failed :%d\n", ret);
-			return -1;
+		if (gsw_bp->BridgePort_ConfigGet(gsw_ops, &bp_cfg)) {
+			pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n",
+			       __func__);
+			return DP_FAILURE;
 		}
-		if (meter->dir == DP_DIR_EGRESS) {
-			pr_err("No Egress color marking for bridge port\n");
-			return -1;
-		} else if (meter->dir == DP_DIR_INGRESS) {
+
+		if (mtr_cfg->dir == DP_DIR_EGRESS) {
+			pr_err("%s no egress color marking for bridge port\n",
+			       __func__);
+			return DP_FAILURE;
+		} else if (mtr_cfg->dir == DP_DIR_INGRESS) {
 			bp_cfg.eMask =
 				GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_MARKING;
-			bp_cfg.eIngressMarkingMode = meter->mode;
+			bp_cfg.eIngressMarkingMode = mtr_cfg->mode;
 		} else {
-			pr_err(" invalid color mark dir\n");
-			return -1;
+			pr_err("%s invalid color mark dir\n", __func__);
+			return DP_FAILURE;
 		}
-		ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, &bp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("BridgePort_ConfigSet API failed :%d\n", ret);
-			return -1;
+
+		if (gsw_bp->BridgePort_ConfigSet(gsw_ops, &bp_cfg)) {
+			pr_err("%s failed GSW_BRIDGE_PORT_CONFIG_SET\n",
+			       __func__);
+			return DP_FAILURE;
 		}
 	}
-	return 0;
+
+	return DP_SUCCESS;
 }
 
 struct dp_meter_pce_info {
@@ -1117,54 +1151,41 @@ struct dp_meter_pce_info {
 };
 
 struct dp_meter_pce_info meter_pce_info = {
-		.pce_meter_blk = {
-			.info = {
-				.subblk_size = 1,
-				.subblk_name = "dp_action_meter_1",
-			},
-			.region = GSW_PCE_RULE_COMMMON,
+	.pce_meter_blk = {
+		.info = {
+			.subblk_size = 1,
+			.subblk_name = "dp_action_meter_1",
 		},
-	};
+		.region = GSW_PCE_RULE_COMMMON,
+	},
+};
 
-int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
-		    int flag, struct dp_meter_subif *mtr_subif)
+int dp_gsw_add_meter(struct net_device *dev,  struct dp_meter_cfg  *mtr_cfg,
+		     int flag, struct dp_meter_subif *mtr_subif)
 {
-	struct core_ops *gsw_handle;
-	struct qos_ops *gsw_qos;
-	struct tflow_ops *gsw_tflow;
-	struct ctp_ops *gsw_ctp;
-	struct brdgport_ops *gsw_bp;
-	struct brdg_ops *gsw_brdg;
-	GSW_QoS_meterCfg_t meter_cfg;
+	struct core_ops *gsw_ops = dp_port_prop[mtr_subif->inst].ops[0];
+	GSW_QoS_meterCfg_t meter_cfg = {0};
+	struct device *devm = &g_dp_dev->dev;
 	GSW_BRIDGE_portConfig_t *bp_cfg = NULL;
-	GSW_PCE_rule_t *pce_rule = NULL;
 	GSW_CTP_portConfig_t *ctp_cfg = NULL;
 	GSW_BRIDGE_config_t *br_cfg = NULL;
-	GSW_return_t ret;
-	int bret = DP_FAILURE;
-
-	if (!mtr_subif) {
-		pr_err("mtr_subif NULL\n");
-		return -1;
-	}
-	gsw_handle = dp_port_prop[mtr_subif->inst].ops[GSWIP_L];
-	if (!gsw_handle)
-		return -1;
-	gsw_qos = &gsw_handle->gsw_qos_ops;
-	gsw_tflow = &gsw_handle->gsw_tflow_ops;
-	gsw_ctp = &gsw_handle->gsw_ctp_ops;
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
-	gsw_brdg = &gsw_handle->gsw_brdg_ops;
+	GSW_PCE_rule_t *pce_rule = NULL;
+	struct tflow_ops *gsw_tflow;
+	struct brdgport_ops *gsw_bp;
+	struct brdg_ops *gsw_brdg;
+	struct qos_ops *gsw_qos;
+	struct ctp_ops *gsw_ctp;
+	int ret = DP_FAILURE;
+
+	if (!mtr_cfg || !mtr_subif) {
+		pr_err("%s mtr_cfg/mtr_subif NULL\n", __func__);
+		return ret;
+	}
+
 	if (flag & DP_COL_MARKING)
-		return dp_set_col_mark(dev, meter, flag, mtr_subif);
-	memset(&meter_cfg, 0, sizeof(GSW_QoS_meterCfg_t));
-	meter_cfg.nCbs = meter->cbs;
-	meter_cfg.nRate = METER_CIR(meter->cir);
-	meter_cfg.nEbs = meter->pbs;
-	meter_cfg.nPiRate = METER_PIR(meter->pir);
-	meter_cfg.bEnable = 1;
+		return dp_set_col_mark(dev, mtr_cfg, flag, mtr_subif);
 
-	switch(meter->type) {
+	switch (mtr_cfg->type) {
 	case srTCM:
 		meter_cfg.eMtrType = GSW_QOS_Meter_srTCM;
 		break;
@@ -1172,396 +1193,624 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		meter_cfg.eMtrType = GSW_QOS_Meter_trTCM;
 		break;
 	default:
-		pr_err(" invalid meter type\n");
-		return -1;
+		pr_err("%s invalid meter type\n", __func__);
+		return DP_FAILURE;
 	}
 
-	meter_cfg.nMeterId = meter->meter_id;
-	meter_cfg.nColourBlindMode = meter->col_mode;
-	ret = gsw_qos->QoS_MeterCfgSet(gsw_handle, &meter_cfg);
-	if (ret != GSW_statusOk) {
-		pr_err("MeterCfgSet API failed:%d\n", ret);
-		return -1;
+	gsw_tflow = &gsw_ops->gsw_tflow_ops;
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
+	gsw_brdg = &gsw_ops->gsw_brdg_ops;
+	gsw_qos = &gsw_ops->gsw_qos_ops;
+	gsw_ctp = &gsw_ops->gsw_ctp_ops;
+
+	meter_cfg.nColourBlindMode = mtr_cfg->col_mode;
+	meter_cfg.nPiRate = METER_PIR(mtr_cfg->pir);
+	meter_cfg.nRate = METER_CIR(mtr_cfg->cir);
+	meter_cfg.nMeterId = mtr_cfg->meter_id;
+	meter_cfg.nCbs = mtr_cfg->cbs;
+	meter_cfg.nEbs = mtr_cfg->pbs;
+	meter_cfg.bEnable = 1;
+
+	if (gsw_qos->QoS_MeterCfgSet(gsw_ops, &meter_cfg)) {
+		pr_err("%s GSW_QOS_METER_CFG_SET fail\n", __func__);
+		return DP_FAILURE;
 	}
 
 	if (flag & DP_METER_ATTACH_PCE) {
-		/* pattern setting */
-		pce_rule = kzalloc(sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
-		if (!pce_rule) {
-			pr_err("pce_rule alloc failed\n");
+		pce_rule = kzalloc(sizeof(*pce_rule), GFP_ATOMIC);
+		if (!pce_rule)
 			goto err;
-		}
-		/* action setting */
+
+		/* Action setting */
 		pce_rule->action.eMeterAction = GSW_PCE_ACTION_METER_1;
-		pce_rule->action.nMeterId =  meter->meter_id;
-		meter_pce_info.pce_index = dp_pce_rule_add(&meter_pce_info.pce_meter_blk,
-										pce_rule);
+		pce_rule->action.nMeterId =  mtr_cfg->meter_id;
+		meter_pce_info.pce_index =
+			dp_pce_rule_add(&meter_pce_info.pce_meter_blk,
+					pce_rule);
 		if (meter_pce_info.pce_index == DP_FAILURE) {
-			pr_err("pce_rule add failed\n");
+			pr_err("%s failed to add pce_rule\n", __func__);
 			goto err;
 		}
 	}
-	if (flag & DP_METER_ATTACH_CTP) {/* CTP port Flag */
+
+	/* CTP port */
+	if (flag & DP_METER_ATTACH_CTP) {
 		struct pmac_port_info *port_info;
 
 		if (mtr_subif->subif.flag_pmapper) {
-			pr_err("can't use CTP,pmapper is enable\n");
+			pr_err("%s cannot use ctp when pmapper is enable\n",
+			       __func__);
 			goto err;
 		}
-		ctp_cfg = kzalloc(sizeof(GSW_CTP_portConfig_t), GFP_ATOMIC);
-		if (!ctp_cfg) {
-			pr_err("ctp_cfg alloc failed\n");
+
+		ctp_cfg = kzalloc(sizeof(*ctp_cfg), GFP_ATOMIC);
+		if (!ctp_cfg)
 			goto err;
-		}
+
 		port_info = get_dp_port_info(mtr_subif->subif.inst,
 					     mtr_subif->subif.port_id);
-		if (!port_info) {
-			pr_err(" port_info is NULL\n");
-			goto err;
-		}
 		ctp_cfg->nLogicalPortId = mtr_subif->subif.port_id;
 		ctp_cfg->nSubIfIdGroup  = GET_VAP(mtr_subif->subif.subif,
 						  port_info->vap_offset,
 						  port_info->vap_mask);
-		ret = gsw_ctp->CTP_PortConfigGet(gsw_handle, ctp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("PortConfigGet API failed :%d\n", ret);
+		if (gsw_ctp->CTP_PortConfigGet(gsw_ops, ctp_cfg)) {
+			pr_err("%s GSW_CTP_PORT_CONFIG_GET fail\n", __func__);
 			goto err;
 		}
-		if (meter->dir == DP_DIR_EGRESS) {
+
+		if (mtr_cfg->dir == DP_DIR_EGRESS) {
 			ctp_cfg->eMask = GSW_CTP_PORT_CONFIG_EGRESS_METER;
 			ctp_cfg->bEgressMeteringEnable = 1;
-			ctp_cfg->nEgressTrafficMeterId =  meter->meter_id;
-		} else if (meter->dir == DP_DIR_INGRESS) {
+			ctp_cfg->nEgressTrafficMeterId =  mtr_cfg->meter_id;
+		} else if (mtr_cfg->dir == DP_DIR_INGRESS) {
 			ctp_cfg->eMask = GSW_CTP_PORT_CONFIG_INGRESS_METER;
 			ctp_cfg->bIngressMeteringEnable = 1;
-			ctp_cfg->nIngressTrafficMeterId =  meter->meter_id;
+			ctp_cfg->nIngressTrafficMeterId =  mtr_cfg->meter_id;
 		} else {
-			pr_err(" invalid meter dir\n");
-			return -1;
+			pr_err("%s invalid meter dir\n", __func__);
+			goto err;
 		}
-		ret = gsw_ctp->CTP_PortConfigSet(gsw_handle, ctp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("PortConfigSet API failed :%d\n", ret);
+
+		if (gsw_ctp->CTP_PortConfigSet(gsw_ops, ctp_cfg)) {
+			pr_err("%s GSW_CTP_PORT_CONFIG_SET fail\n", __func__);
 			goto err;
 		}
 	}
-	if (flag & DP_METER_ATTACH_BRPORT) {/*BRIDGE port Flag*/
+
+	/* Bridge port */
+	if (flag & DP_METER_ATTACH_BRPORT) {
 		if (!mtr_subif->subif.flag_bp) {
-			pr_err("flag_bp value 0\n");
+			pr_err("%s flag_bp value is 0\n", __func__);
 			goto err;
 		}
-		bp_cfg = kzalloc(sizeof(GSW_BRIDGE_portConfig_t), GFP_ATOMIC);
-		if (!bp_cfg) {
-			pr_err("bp_cfg alloc failed\n");
+
+		bp_cfg = kzalloc(sizeof(*bp_cfg), GFP_ATOMIC);
+		if (!bp_cfg)
 			goto err;
-		}
+
 		bp_cfg->nBridgePortId = mtr_subif->subif.bport;
-		if (meter->dir == DP_DIR_EGRESS)
+		if (mtr_cfg->dir == DP_DIR_EGRESS)
 			bp_cfg->eMask =
 				GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_SUB_METER;
-		ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, bp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("BridgePort_ConfigGet API failed :%d\n", ret);
+		else
+			bp_cfg->eMask =
+				GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_METER;
+		if (gsw_bp->BridgePort_ConfigGet(gsw_ops, bp_cfg)) {
+			pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n",
+			       __func__);
 			goto err;
 		}
-		if (meter->dir == DP_DIR_EGRESS) {
-			bp_cfg->eMask =
-				GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_SUB_METER;
-			switch(meter->dp_pce.flow) {
+
+		if (mtr_cfg->dir == DP_DIR_EGRESS) {
+			switch (mtr_cfg->dp_pce.flow) {
 			case DP_UKNOWN_UNICAST:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_UNKNOWN_UC,
-				       meter->meter_id, 1);
+				       mtr_cfg->meter_id, 1);
 				break;
 			case DP_MULTICAST:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_MULTICAST,
-				       meter->meter_id, 1);
+				       mtr_cfg->meter_id, 1);
 				break;
 			case DP_BROADCAST:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_BROADCAST,
-				       meter->meter_id, 1);
+				       mtr_cfg->meter_id, 1);
 				break;
 			default:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_OTHERS,
-				       meter->meter_id, 1);
+				       mtr_cfg->meter_id, 1);
 			}
-		} else if (meter->dir == DP_DIR_INGRESS) {
-			bp_cfg->eMask =
-				GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_METER;
+		} else if (mtr_cfg->dir == DP_DIR_INGRESS) {
 			bp_cfg->bIngressMeteringEnable = 1;
-			bp_cfg->nIngressTrafficMeterId = meter->meter_id;
+			bp_cfg->nIngressTrafficMeterId = mtr_cfg->meter_id;
 		} else {
-			pr_err(" invalid meter dir\n");
-			return -1;
+			pr_err("%s invalid meter dir\n", __func__);
+			goto err;
 		}
-		ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, bp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("BridgePort_ConfigSet API failed :%d\n", ret);
+
+		if (gsw_bp->BridgePort_ConfigSet(gsw_ops, bp_cfg)) {
+			pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail\n",
+			       __func__);
 			goto err;
 		}
 	} else if (flag & DP_METER_ATTACH_BRIDGE) {
-		br_cfg = kzalloc(sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
-		if (!br_cfg) {
-			pr_err("br_cfg alloc failed\n");
+		br_cfg = devm_kzalloc(devm, sizeof(*br_cfg), GFP_ATOMIC);
+		if (!br_cfg)
 			goto err;
-		}
+
 		br_cfg->nBridgeId = mtr_subif->fid;
-		ret = gsw_brdg->Bridge_ConfigGet(gsw_handle, br_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("Bridge_ConfigGet API failed :%d\n", ret);
+		if (gsw_brdg->Bridge_ConfigGet(gsw_ops, br_cfg)) {
+			pr_err("%s GSW_BRIDGE_CONFIG_GET fail\n", __func__);
 			goto err;
 		}
+
 		br_cfg->eMask = GSW_BRIDGE_CONFIG_MASK_SUB_METER;
-		switch(meter->dp_pce.flow) {
+
+		switch (mtr_cfg->dp_pce.flow) {
 		case DP_UKNOWN_UNICAST:
-			BR_CFG(br_cfg,
-			       GSW_BRIDGE_PORT_EGRESS_METER_UNKNOWN_UC,
-			       meter->meter_id, 1);
+			BR_CFG(br_cfg, GSW_BRIDGE_PORT_EGRESS_METER_UNKNOWN_UC,
+			       mtr_cfg->meter_id, 1);
 			break;
 		case DP_MULTICAST:
-			BR_CFG(br_cfg,
-			       GSW_BRIDGE_PORT_EGRESS_METER_MULTICAST,
-			       meter->meter_id, 1);
+			BR_CFG(br_cfg, GSW_BRIDGE_PORT_EGRESS_METER_MULTICAST,
+			       mtr_cfg->meter_id, 1);
 			break;
 		case DP_BROADCAST:
-			BR_CFG(br_cfg,
-			       GSW_BRIDGE_PORT_EGRESS_METER_BROADCAST,
-			       meter->meter_id, 1);
+			BR_CFG(br_cfg, GSW_BRIDGE_PORT_EGRESS_METER_BROADCAST,
+			       mtr_cfg->meter_id, 1);
 			break;
 		default:
-			pr_err("Meter flow invalid\n");
+			pr_err("%s meter flow invalid\n", __func__);
 			goto err;
 		}
-		ret = gsw_brdg->Bridge_ConfigSet(gsw_handle, br_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("Bridge_ConfigSet API failed :%d\n", ret);
+
+		if (gsw_brdg->Bridge_ConfigSet(gsw_ops, br_cfg)) {
+			pr_err("%s GSW_BRIDGE_CONFIG_SET fail\n", __func__);
 			goto err;
 		}
 	}
-	bret = 0;
+
+	ret = DP_SUCCESS;
+
 err:
 	kfree(br_cfg);
 	kfree(bp_cfg);
 	kfree(ctp_cfg);
 	kfree(pce_rule);
-	return bret;
+
+	return ret;
 }
 
-int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
-		    int flag, struct dp_meter_subif *mtr_subif)
+int dp_gsw_del_meter(struct net_device *dev,  struct dp_meter_cfg  *mtr_cfg,
+		     int flag, struct dp_meter_subif *mtr_subif)
 {
-	struct core_ops *gsw_handle;
-	struct tflow_ops *gsw_tflow;
-	struct ctp_ops *gsw_ctp;
-	struct brdgport_ops *gsw_bp;
-	struct brdg_ops *gsw_brdg;
+	struct core_ops *gsw_ops = dp_port_prop[mtr_subif->inst].ops[0];
 	GSW_BRIDGE_portConfig_t *bp_cfg = NULL;
 	GSW_PCE_ruleDelete_t *pce_rule = NULL;
 	GSW_CTP_portConfig_t *ctp_cfg = NULL;
 	GSW_BRIDGE_config_t *br_cfg = NULL;
-	GSW_return_t ret;
-	int bret = DP_FAILURE;
-
-	if (!mtr_subif) {
-		pr_err(" mtr_subif NULL\n");
-		return -1;
-	}
-	gsw_handle = dp_port_prop[mtr_subif->inst].ops[GSWIP_L];
-	if (!gsw_handle)
-		return -1;
-	gsw_tflow = &gsw_handle->gsw_tflow_ops;
-	gsw_ctp = &gsw_handle->gsw_ctp_ops;
-	gsw_bp = &gsw_handle->gsw_brdgport_ops;
-	gsw_brdg = &gsw_handle->gsw_brdg_ops;
-	if (meter->dir & DP_METER_ATTACH_PCE) {
-		pce_rule = kzalloc(sizeof(GSW_PCE_ruleDelete_t), GFP_ATOMIC);
-		if (!pce_rule) {
-			pr_err("pce_rule alloc failed\n");
-			bret = DP_FAILURE;
+	struct brdgport_ops *gsw_bp;
+	struct tflow_ops *gsw_tflow;
+	struct brdg_ops *gsw_brdg;
+	struct ctp_ops *gsw_ctp;
+	int ret = DP_FAILURE;
+
+	if (!mtr_cfg || !mtr_subif) {
+		pr_err("%s mtr_cfg/mtr_subif NULL\n", __func__);
+		return DP_FAILURE;
+	}
+
+	gsw_tflow = &gsw_ops->gsw_tflow_ops;
+	gsw_bp = &gsw_ops->gsw_brdgport_ops;
+	gsw_brdg = &gsw_ops->gsw_brdg_ops;
+	gsw_ctp = &gsw_ops->gsw_ctp_ops;
+
+	if (mtr_cfg->dir & DP_METER_ATTACH_PCE) {
+		pce_rule = kzalloc(sizeof(*pce_rule), GFP_ATOMIC);
+		if (!pce_rule)
 			goto err;
-		}
-		/* pattern setting */
+
 		pce_rule->nIndex = meter_pce_info.pce_index;
-		bret = dp_pce_rule_del(&meter_pce_info.pce_meter_blk, pce_rule);
-		if (bret != DP_SUCCESS) {
-			pr_err("pce_rule delete failed\n");
+		if (dp_pce_rule_del(&meter_pce_info.pce_meter_blk, pce_rule))
 			goto err;
-		}
 	}
+
 	if (flag & DP_METER_ATTACH_CTP) {
 		struct pmac_port_info *port_info;
 
 		if (mtr_subif->subif.flag_pmapper) {
-			pr_err("flag_pmapper is set\n");
+			pr_err("%s flag_pmapper is set\n", __func__);
 			goto err;
 		}
-		ctp_cfg = kzalloc(sizeof(GSW_CTP_portConfig_t), GFP_ATOMIC);
-		if (!ctp_cfg) {
-			pr_err("ctp_cfg alloc failed\n");
+
+		ctp_cfg = kzalloc(sizeof(*ctp_cfg), GFP_ATOMIC);
+		if (!ctp_cfg)
 			goto err;
-		}
+
 		port_info = get_dp_port_info(mtr_subif->subif.inst,
 					     mtr_subif->subif.port_id);
-		if (!port_info) {
-			pr_err(" port_info is NULL\n");
-			goto err;
-		}
 		ctp_cfg->nLogicalPortId = mtr_subif->subif.port_id;
 		ctp_cfg->nSubIfIdGroup = GET_VAP(mtr_subif->subif.subif,
 						 port_info->vap_offset,
 						 port_info->vap_mask);
-		if (meter->dir == DP_DIR_EGRESS)
-			ctp_cfg->nEgressTrafficMeterId =  meter->meter_id;
-		else if (meter->dir == DP_DIR_INGRESS)
-			ctp_cfg->nIngressTrafficMeterId =  meter->meter_id;
-		ret = gsw_ctp->CTP_PortConfigGet(gsw_handle, ctp_cfg);
-		if (ret != GSW_statusOk)
+		if (mtr_cfg->dir == DP_DIR_EGRESS) {
+			ctp_cfg->nEgressTrafficMeterId =  mtr_cfg->meter_id;
+		} else if (mtr_cfg->dir == DP_DIR_INGRESS) {
+			ctp_cfg->nIngressTrafficMeterId =  mtr_cfg->meter_id;
+		} else {
+			pr_err("%s invalid meter dir\n", __func__);
 			goto err;
+		}
+
+		if (gsw_ctp->CTP_PortConfigGet(gsw_ops, ctp_cfg)) {
+			pr_err("%s GSW_CTP_PORT_CONFIG_GET fail\n", __func__);
+			goto err;
+		}
 
-		if (meter->dir == DP_DIR_EGRESS) {
+		if (mtr_cfg->dir == DP_DIR_EGRESS) {
 			ctp_cfg->eMask = GSW_CTP_PORT_CONFIG_EGRESS_METER;
 			ctp_cfg->bEgressMeteringEnable = 0;
-		} else if (meter->dir == DP_DIR_INGRESS) {
+		} else {
 			ctp_cfg->eMask = GSW_CTP_PORT_CONFIG_INGRESS_METER;
 			ctp_cfg->bIngressMeteringEnable = 0;
 		}
-		ret = gsw_ctp->CTP_PortConfigSet(gsw_handle, ctp_cfg);
-		if (ret != GSW_statusOk)
+
+		if (gsw_ctp->CTP_PortConfigSet(gsw_ops, ctp_cfg)) {
+			pr_err("%s GSW_CTP_PORT_CONFIG_SET fail\n", __func__);
 			goto err;
+		}
 	}
+
 	if (flag & DP_METER_ATTACH_BRPORT) {
 		if (!mtr_subif->subif.flag_bp) {
-			pr_err("flag_bp is 0\n");
+			pr_err("%s flag_bp is 0\n", __func__);
 			goto err;
 		}
-		bp_cfg = kzalloc(sizeof(GSW_BRIDGE_portConfig_t), GFP_ATOMIC);
-		if (!bp_cfg) {
-			pr_err("bp_cfg alloc failed\n");
+
+		bp_cfg = kzalloc(sizeof(*bp_cfg), GFP_ATOMIC);
+		if (!bp_cfg)
 			goto err;
-		}
+
 		bp_cfg->nBridgePortId = mtr_subif->subif.bport;
-		if (meter->dir == DP_DIR_EGRESS)
+		if (mtr_cfg->dir == DP_DIR_EGRESS)
 			bp_cfg->eMask =
 				GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_SUB_METER;
-		ret = gsw_bp->BridgePort_ConfigGet(gsw_handle, bp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("BridgePort_ConfigGet API failed\n");
+		else
+			bp_cfg->eMask =
+				GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_METER;
+		if (gsw_bp->BridgePort_ConfigGet(gsw_ops, bp_cfg)) {
+			pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n",
+			       __func__);
 			goto err;
 		}
-		if (meter->dir == DP_DIR_EGRESS) {
-			bp_cfg->eMask =
-				GSW_BRIDGE_PORT_CONFIG_MASK_EGRESS_SUB_METER;
-			switch(meter->dp_pce.flow) {
+
+		if (mtr_cfg->dir == DP_DIR_EGRESS) {
+			switch (mtr_cfg->dp_pce.flow) {
 			case DP_UKNOWN_UNICAST:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_UNKNOWN_UC,
-				       meter->meter_id, 0);
+				       mtr_cfg->meter_id, 0);
 				break;
 			case DP_MULTICAST:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_MULTICAST,
-				       meter->meter_id, 0);
+				       mtr_cfg->meter_id, 0);
 				break;
 			case DP_BROADCAST:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_BROADCAST,
-				       meter->meter_id, 0);
+				       mtr_cfg->meter_id, 0);
 				break;
 			default:
 				BP_CFG(bp_cfg,
 				       GSW_BRIDGE_PORT_EGRESS_METER_OTHERS,
-				       meter->meter_id, 0);
+				       mtr_cfg->meter_id, 0);
 			}
-		} else {
-			bp_cfg->eMask =
-				GSW_BRIDGE_PORT_CONFIG_MASK_INGRESS_METER;
+		} else if (mtr_cfg->dir == DP_DIR_INGRESS) {
 			bp_cfg->bIngressMeteringEnable = 0;
+		} else {
+			pr_err("%s invalid meter dir\n", __func__);
+			goto err;
 		}
-		ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, bp_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("BridgePort_ConfigSet API failed\n");
-			bret = -1;
+
+		if (gsw_bp->BridgePort_ConfigSet(gsw_ops, bp_cfg)) {
+			pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail\n",
+			       __func__);
 			goto err;
 		}
 	}
 	if (flag & DP_METER_ATTACH_BRIDGE) {
-		br_cfg = kzalloc(sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
-		if (!br_cfg) {
-			pr_err("br_cfg alloc failed\n");
+		br_cfg = kzalloc(sizeof(*br_cfg), GFP_ATOMIC);
+		if (!br_cfg)
 			goto err;
-		}
+
 		br_cfg->nBridgeId = mtr_subif->fid;
-		ret = gsw_brdg->Bridge_ConfigGet(gsw_handle, br_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("Bridge_ConfigGet API failed :%d\n", ret);
+		if (gsw_brdg->Bridge_ConfigGet(gsw_ops, br_cfg)) {
+			pr_err("%s GSW_BRIDGE_CONFIG_GET fail\n", __func__);
 			goto err;
 		}
+
 		br_cfg->eMask = GSW_BRIDGE_CONFIG_MASK_SUB_METER;
-		switch(meter->dp_pce.flow) {
+		switch (mtr_cfg->dp_pce.flow) {
 		case DP_UKNOWN_UNICAST:
 			BR_CFG(br_cfg,
 			       GSW_BRIDGE_PORT_EGRESS_METER_UNKNOWN_UC,
-			       meter->meter_id, 0);
+			       mtr_cfg->meter_id, 0);
 			break;
 		case DP_MULTICAST:
 			BR_CFG(br_cfg,
 			       GSW_BRIDGE_PORT_EGRESS_METER_MULTICAST,
-			       meter->meter_id, 0);
+			       mtr_cfg->meter_id, 0);
 			break;
 		case DP_BROADCAST:
 			BR_CFG(br_cfg,
 			       GSW_BRIDGE_PORT_EGRESS_METER_BROADCAST,
-			       meter->meter_id, 0);
+			       mtr_cfg->meter_id, 0);
 			break;
 		default:
-			pr_err("Meter flow invalid\n");
+			pr_err("%s meter flow invalid\n", __func__);
 			goto err;
 		}
-		ret = gsw_brdg->Bridge_ConfigSet(gsw_handle, br_cfg);
-		if (ret != GSW_statusOk) {
-			pr_err("Bridge_ConfigSet API failed :%d\n", ret);
+
+		if (gsw_brdg->Bridge_ConfigSet(gsw_ops, br_cfg)) {
+			pr_err("%s GSW_BRIDGE_CONFIG_SET fail\n", __func__);
 			goto err;
 		}
 	}
 
-	bret = 0;
+	ret = DP_SUCCESS;
 err:
 	kfree(br_cfg);
 	kfree(bp_cfg);
 	kfree(ctp_cfg);
 	kfree(pce_rule);
-	return bret;
+
+	return ret;
+}
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP32)
+static int set_gsw_gpid_map(int inst, u8 ep, int gpid_base, int gpid_num,
+			    int gpid_spl, u32 flags)
+{
+	struct core_ops *gsw_ops = dp_port_prop[inst].ops[0];
+	GSW_LPID_to_GPID_Assignment_t lp_gp_assign = {0};
+	GSW_GPID_to_LPID_Assignment_t gp_lp_assign = {0};
+	struct gpid_ops *gsw_gpid;
+	int i;
+
+	gsw_gpid = &gsw_ops->gsw_gpid_ops;
+
+	DP_DEBUG(DP_DBG_FLAG_REG, "gpid base=%d num=%d spl gpid=%d\n",
+		 gpid_base, gpid_num, gpid_spl);
+
+	lp_gp_assign.nLogicalPortId = ep;
+	if (gsw_gpid->LpidToGpid_AssignmentGet(gsw_ops, &lp_gp_assign)) {
+		pr_err("%s GSW_LPID_TO_GPID_ASSIGNMENT_GET fail:\n", __func__);
+		pr_err(" lpid (%d) -> gpid (%d)\n", ep, gpid_base);
+		return DP_FAILURE;
+	}
+
+	/* During de-register assign gpid same as lpid */
+	if (flags & DP_F_DEREGISTER)
+		lp_gp_assign.nFirstGlobalPortId = ep;
+	else
+		lp_gp_assign.nFirstGlobalPortId = gpid_base;
+
+	lp_gp_assign.nNumberOfGlobalPort = gpid_num;
+
+	/* Default value is 0x7, still set the value to allow all subif bits to
+	 * be selected in NON_WLAN Mode
+	 */
+	lp_gp_assign.nValidBits = GENMASK(2, 0);
+
+	/* Set lpid to gpid */
+	if (gsw_gpid->LpidToGpid_AssignmentSet(gsw_ops, &lp_gp_assign)) {
+		pr_err("%s GSW_LPID_TO_GPID_ASSIGNMENT_SET fail:\n", __func__);
+		pr_err(" lpid (%d) -> gpid (%d)\n", ep, gpid_base);
+		return DP_FAILURE;
+	}
+
+	/* Set gpid to lpid */
+	for (i = 0; i < gpid_num; i++) {
+		gp_lp_assign.nGlobalPortId = gpid_base + i;
+		if (gsw_gpid->GpidToLpid_AssignmentGet(gsw_ops,
+						       &gp_lp_assign)) {
+			pr_err("%s GSW_GPID_TO_LPID_ASSIGNMENT_SET fail:",
+			       __func__);
+			pr_err(" gpid (%d)\n", gp_lp_assign.nGlobalPortId);
+			return DP_FAILURE;
+		}
+
+		if (flags & DP_F_DEREGISTER)
+			gp_lp_assign.nLogicalPortId = 0;
+		else
+			gp_lp_assign.nLogicalPortId = ep;
+
+		if (gsw_gpid->GpidToLpid_AssignmentSet(gsw_ops,
+						       &gp_lp_assign)) {
+			pr_err("%s GSW_GPID_TO_LPID_ASSIGNMENT_SET fail:",
+			       __func__);
+			pr_err(" gpid (%d) -> lpid (%d)\n",
+			       gpid_base + i, ep);
+			return DP_FAILURE;
+		}
+
+		DP_DEBUG(DP_DBG_FLAG_REG, "%s set gpid(%d)<->lpid(%d)\n",
+			 __func__, gpid_base + i, ep);
+	}
+
+	if (gpid_spl <= 0)
+		return DP_SUCCESS;
+
+	/* Set special gpid to lpid */
+	gp_lp_assign.nGlobalPortId = gpid_spl;
+	if (gsw_gpid->GpidToLpid_AssignmentGet(gsw_ops, &gp_lp_assign)) {
+		pr_err("%s GSW_GPID_TO_LPID_ASSIGNMENT_GET fail:", __func__);
+		pr_err(" gpid (%d)\n", gpid_spl);
+		return DP_FAILURE;
+	}
+
+	if (flags & DP_F_DEREGISTER)
+		gp_lp_assign.nLogicalPortId = 0;
+	else
+		gp_lp_assign.nLogicalPortId = ep;
+
+	if (gsw_gpid->GpidToLpid_AssignmentSet(gsw_ops, &gp_lp_assign)) {
+		pr_err("%s GSW_GPID_TO_LPID_ASSIGNMENT_SET fail:", __func__);
+		pr_err(" gpid (%d) -> lpid (%d)\n",
+		       gpid_spl, gp_lp_assign.nLogicalPortId);
+		return DP_FAILURE;
+	}
+
+	return DP_SUCCESS;
+}
+
+static int set_cqm_gpid_map(int inst, u8 ep, int gpid_base, int gpid_num,
+			    int gpid_spl, u32 flags)
+{
+	struct cbm_gpid_lpid cbm_gpid = {0};
+	int i = 0;
+
+	cbm_gpid.cbm_inst = dp_port_prop[inst].cbm_inst;
+
+	if (flags & DP_F_DEREGISTER)
+		cbm_gpid.lpid = 0;
+	else
+		cbm_gpid.lpid = ep;
+
+	cbm_gpid.gpid = gpid_base;
+
+	for (i = 0; i < gpid_num; i++) {
+		cbm_gpid.gpid = gpid_base + i;
+		if (dp_cqm_gpid_lpid_map(&cbm_gpid))
+			return DP_FAILURE;
+	}
+
+	if (gpid_spl) {
+		cbm_gpid.gpid = gpid_spl;
+		if (dp_cqm_gpid_lpid_map(&cbm_gpid))
+			return DP_FAILURE;
+	}
+
+	return DP_SUCCESS;
+}
+
+int dp_gpid_free(int inst, u8 ep, u32 flags)
+{
+	struct pmac_port_info *port = get_dp_port_info(inst, ep);
+	int gpid_base, gpid_num, gpid_spl;
+
+	gpid_base = port->gpid_base;
+	gpid_num = port->gpid_num;
+	gpid_spl = port->gpid_spl;
+
+	/* To del spl gpid for non-streaming port(hence vap == -1) */
+	if (dp_del_pp_gpid(inst, ep, -1)) {
+		pr_err("%s fail to del spl gpid for dport(%d)\n", __func__, ep);
+		return DP_FAILURE;
+	}
+
+	if (set_gsw_gpid_map(inst, ep, gpid_base, gpid_num, gpid_spl, flags))
+		return DP_FAILURE;
+
+	if (set_cqm_gpid_map(inst, ep, gpid_base, gpid_num, gpid_spl, flags))
+		return DP_FAILURE;
+
+	if (free_gpid(inst, gpid_base, gpid_num, gpid_spl))
+		return DP_FAILURE;
+
+	return DP_SUCCESS;
 }
 
-int dp_set_bp_attr_31(struct dp_bp_attr *conf, int bport, uint32_t flag)
+int gpid_port_assign(int inst, u8 ep, u32 flags)
 {
-	GSW_return_t ret;
-	GSW_BRIDGE_portConfig_t brportcfg;
-	struct core_ops *ops = dp_port_prop[conf->inst].ops[0];
+	struct pmac_port_info *port = get_dp_port_info(inst, ep);
+	int gpid_base, gpid_num, i, gpid_spl = -1;
+	const struct ctp_assign *ctp_assign;
+	struct hal_priv *priv;
+
+	priv = (struct hal_priv *)dp_port_prop[inst].priv_hal;
+	if (!priv) {
+		pr_err("%s priv is NULL\n", __func__);
+		return DP_FAILURE;
+	}
+
+	flags |= port->alloc_flags;
+	if (flags & DP_F_DEREGISTER) {
+		if (dp_gpid_free(inst, ep, flags))
+			return DP_FAILURE;
+		return DP_SUCCESS;
+	}
+
+	ctp_assign = dp_gsw_get_ctp_assign(inst, port->alloc_flags);
+
+	gpid_num = ctp_assign->max_gpid;
+	gpid_base = alloc_gpid(inst, DP_DYN_GPID, gpid_num, ep);
+	if (gpid_base == DP_FAILURE) {
+		pr_err("%s fail to allocate %d gpid for ep %d\n",
+		       __func__, gpid_num, ep);
+		return DP_FAILURE;
+	}
+
+	if (!is_stream_port(flags)) {
+		gpid_spl = alloc_gpid(inst, DP_SPL_GPID, 1, ep);
+		if (gpid_spl == DP_FAILURE) {
+			pr_err("%s fail to allocate spl gpid for ep %d\n",
+			       __func__, ep);
+			return DP_FAILURE;
+		}
+
+		if (dp_add_pp_gpid(inst, ep, 0, gpid_spl, 1)) {
+			pr_err("%s fail to add spl gpid %d for ep %d\n",
+			       __func__, gpid_spl, ep);
+			return DP_FAILURE;
+		}
+
+		DP_DEBUG(DP_DBG_FLAG_REG, "Add spl gpid %d to PP\n", gpid_spl);
+		priv->gp_dp_map[gpid_spl].dpid = ep;
+	}
+
+	if (set_gsw_gpid_map(inst, ep, gpid_base, gpid_num, gpid_spl, flags))
+		return DP_FAILURE;
+
+	if (set_cqm_gpid_map(inst, ep, gpid_base, gpid_num, gpid_spl, flags))
+		return DP_FAILURE;
+
+	port->gpid_base = gpid_base;
+	port->gpid_num = gpid_num;
+	port->gpid_spl = gpid_spl;
+
+	/* Update table */
+	for (i = 0; i < gpid_num; i++)
+		priv->gp_dp_map[gpid_base + i].dpid = ep;
 
-	memset(&brportcfg, 0, sizeof(GSW_BRIDGE_portConfig_t));
+	return DP_SUCCESS;
+}
+#endif
 
-	brportcfg.nBridgePortId = bport;
-	brportcfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
+int dp_gsw_set_bp_attr(struct dp_bp_attr *bp_attr, int bp, uint32_t flag)
+{
+	struct core_ops *gsw_ops = dp_port_prop[bp_attr->inst].ops[0];
+	GSW_BRIDGE_portConfig_t bp_cfg = {0};
+
+	bp_cfg.nBridgePortId = bp;
+	bp_cfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
 
-	ret = ops->gsw_brdgport_ops.BridgePort_ConfigGet(ops, &brportcfg);
-	if (ret != GSW_statusOk) {
-		pr_err("fail in getting bridge port config\r\n");
+	if (gsw_ops->gsw_brdgport_ops.BridgePort_ConfigGet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_GET fail\n", __func__);
 		return DP_FAILURE;
 	}
 
-	if (conf->en)
-		SET_BP_MAP(brportcfg.nBridgePortMap, CPU_PORT);
+	if (bp_attr->en)
+		SET_BP_MAP(bp_cfg.nBridgePortMap, CPU_PORT);
 	else
-		UNSET_BP_MAP(brportcfg.nBridgePortMap, CPU_PORT);
+		UNSET_BP_MAP(bp_cfg.nBridgePortMap, CPU_PORT);
 
-	ret = ops->gsw_brdgport_ops.BridgePort_ConfigSet(ops, &brportcfg);
-	if (ret != GSW_statusOk) {
-		pr_err("Fail in allocating/configuring bridge port\n");
+	if (gsw_ops->gsw_brdgport_ops.BridgePort_ConfigSet(gsw_ops, &bp_cfg)) {
+		pr_err("%s GSW_BRIDGE_PORT_CONFIG_SET fail\n", __func__);
 		return DP_FAILURE;
 	}
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 0f5a4b87d06e..278d6952beba 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -396,7 +396,7 @@ static void mib_init(u32 flag, int inst)
 	dp_mib_init(0);
 #endif
 	DP_TX_CB_LOCK_INIT(&priv->cb_lock);
-	gsw_mib_reset_31(0, 0); /* GSW O */
+	dp_gsw_reset_mib(0, 0); /* GSW O */
 }
 
 void dp_sys_mib_reset_31(u32 flag)
@@ -404,8 +404,8 @@ void dp_sys_mib_reset_31(u32 flag)
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_MIB)
 	dp_reset_sys_mib(0);
 #else
-	gsw_mib_reset_31(0, 0); /* GSW L */
-	gsw_mib_reset_31(1, 0); /* GSW R */
+	dp_gsw_reset_mib(0, 0); /* GSW L */
+	dp_gsw_reset_mib(1, 0); /* GSW R */
 	dp_clear_all_mib_inside(0);
 #endif
 }
@@ -1232,14 +1232,14 @@ static int dp_platform_set(int inst, u32 flag)
 		sif->bp = CPU_BP;
 		sif->mac_learn_dis = DP_MAC_LEARNING_DIS;
 		INIT_LIST_HEAD(&sif->logic_dev);
-		priv->bp_def = alloc_bridge_port(inst, CPU_PORT, CPU_SUBIF,
-						 CPU_FID, CPU_BP, 0);
+		priv->bp_def = dp_gsw_alloc_bp(inst, CPU_PORT, CPU_SUBIF,
+					       CPU_FID, CPU_BP, 0);
 		DP_DEBUG(DP_DBG_FLAG_DBG, "bp_def[%d]=%d\n",
 			 inst, priv->bp_def);
 
 		if (!inst) /*only inst zero will support mib feature */
 			mib_init(0, inst);
-		dp_get_gsw_parser_31(NULL, NULL, NULL, NULL);
+		dp_gsw_get_parser(NULL, NULL, NULL, NULL);
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_CPUFREQ)
 		if (!inst)
 			dp_coc_cpufreq_init();
@@ -1269,7 +1269,7 @@ static int dp_platform_set(int inst, u32 flag)
 		}
 		dp_platform_color_table_set(inst);
 
-		if (cpu_vlan_mod_dis(inst)) {
+		if (dp_gsw_dis_cpu_vlan_md(inst)) {
 			pr_err("cpu_vlan_mod_dis fail\n");
 			return -1;
 		}
@@ -1279,7 +1279,7 @@ static int dp_platform_set(int inst, u32 flag)
 	/* De-initialize */
 	priv = (struct hal_priv *)dp_port_prop[inst].priv_hal;
 	if (priv->bp_def)
-		free_bridge_port(inst, priv->bp_def);
+		dp_gsw_free_bp(inst, priv->bp_def);
 	init_ppv4_qos(inst, flag); /*de-initialize qos */
 	devm_kfree(&g_dp_dev->dev, priv);
 	dp_port_prop[inst].priv_hal = NULL;
@@ -1377,7 +1377,7 @@ static int dev_platform_set(int inst, u8 ep, struct dp_dev_data *data,
 		pr_err("priv is NULL\n");
 		return DP_FAILURE;
 	}
-	itf = ctp_port_assign(inst, ep, priv->bp_def, flags, data);
+	itf = dp_gsw_assign_ctp(inst, ep, priv->bp_def, flags, data);
 	/*reset_gsw_itf(ep); */
 	get_dp_port_info(inst, ep)->itf_info = itf;
 	dp_node_reserve(inst, ep, data, flags);
@@ -1399,7 +1399,7 @@ static int port_platform_set(int inst, u8 ep, struct dp_port_data *data,
 		pr_err("priv is NULL\n");
 		return DP_FAILURE;
 	}
-	set_port_lookup_mode_31(inst, ep, flags);
+	dp_gsw_set_port_lu_md(inst, ep, flags);
 	DP_DEBUG(DP_DBG_FLAG_QOS, "priv=%p deq_port_stat=%p qdev=%p\n",
 		 priv,
 		 priv ? priv->deq_port_stat : NULL,
@@ -1799,9 +1799,9 @@ static int subif_hw_set(int inst, int portid, int subif_ix,
 		if (port_info->ctp_max == 1 && port_info->num_subif > 0)
 			bp = dp_get_subif_share_bp(inst, portid);
 		else
-			bp = alloc_bridge_port(inst, portid,
-					       subif_ix, CPU_FID, CPU_BP,
-					       data->subif_data->flag_ops);
+			bp = dp_gsw_alloc_bp(inst, portid, subif_ix,
+					     CPU_FID, CPU_BP,
+					     data->subif_data->flag_ops);
 		if (bp < 0) {
 			pr_err("Fail to alloc bridge port\n");
 			return -1;
@@ -2112,7 +2112,7 @@ static int subif_hw_reset(int inst, int portid, int subif_ix,
 						DP_DEBUG(DP_DBG_FLAG_PAE,
 							 "Free BP[%d] vap=%d\n",
 							 bp, subif_ix);
-						free_bridge_port(inst, bp);
+						dp_gsw_free_bp(inst, bp);
 					}
 				}
 			} else {
@@ -2122,7 +2122,7 @@ static int subif_hw_reset(int inst, int portid, int subif_ix,
 					DP_DEBUG(DP_DBG_FLAG_PAE,
 						 "Free BP[%d] vap=%d\n",
 						 bp, subif_ix);
-					free_bridge_port(inst, bp);
+					dp_gsw_free_bp(inst, bp);
 				}
 			}
 		}
@@ -2184,12 +2184,11 @@ static int subif_platform_set_unexplicit(int inst, int port_id,
 					 struct logic_dev *logic_dev, u32 flags)
 {
 	if (flags & DP_F_DEREGISTER) {
-		free_bridge_port(inst, logic_dev->bp);
+		dp_gsw_free_bp(inst, logic_dev->bp);
 	} else {
 		logic_dev->bp =
-			alloc_bridge_port(inst, port_id, logic_dev->ctp,
-					  CPU_FID, CPU_BP,
-					  flags);
+			dp_gsw_alloc_bp(inst, port_id, logic_dev->ctp,
+					CPU_FID, CPU_BP, flags);
 	}
 
 	return 0;
@@ -2361,14 +2360,19 @@ static int dp_get_gswip_cap(struct dp_hw_cap *cap, int flag)
 	cap->info.cap.max_num_dp_ports = cap->info.cap.max_eth_port +
 					 cap->info.cap.max_virt_eth_port;
 
-	/* For WLAN 8 bit Mode VAP=11:8
-	 * For WLAN 9 bit Mode VAP=11:9
-	 * For Non-WLAN Mode VAP=0:7
+	/* For WLAN 8 bit Mode VAP=11:8/14:8
+	 * For WLAN 9 bit Mode VAP=11:9/14:9
+	 * For Non-WLAN Mode VAP=7:0
 	 */
+	if (cap->info.type == GSWIP32_TYPE) {
+		cap->info.cap.vap_mask[GSW_LOGICAL_PORT_8BIT_WLAN] = VAP_8BIT_MASK_32;
+		cap->info.cap.vap_mask[GSW_LOGICAL_PORT_9BIT_WLAN] = VAP_9BIT_MASK_32;
+	} else {
+		cap->info.cap.vap_mask[GSW_LOGICAL_PORT_8BIT_WLAN] = VAP_8BIT_MASK_31;
+		cap->info.cap.vap_mask[GSW_LOGICAL_PORT_9BIT_WLAN] = VAP_9BIT_MASK_31;
+	}
 	cap->info.cap.vap_offset[GSW_LOGICAL_PORT_8BIT_WLAN] = VAP_8BIT_OFFSET;
-	cap->info.cap.vap_mask[GSW_LOGICAL_PORT_8BIT_WLAN] = VAP_8BIT_MASK;
 	cap->info.cap.vap_offset[GSW_LOGICAL_PORT_9BIT_WLAN] = VAP_9BIT_OFFSET;
-	cap->info.cap.vap_mask[GSW_LOGICAL_PORT_9BIT_WLAN] = VAP_9BIT_OFFSET;
 	cap->info.cap.vap_offset[GSW_LOGICAL_PORT_GPON] = VAP_NONWLAN_OFFSET;
 	cap->info.cap.vap_mask[GSW_LOGICAL_PORT_GPON] = VAP_NONWLAN_MASK;
 
@@ -2431,17 +2435,17 @@ int register_dp_cap_gswip31(int flag)
 	cap.info.dump_rx_pmac = dump_rx_pmac;
 	cap.info.dump_tx_pmac = dump_tx_pmac;
 	cap.info.supported_logic_dev = supported_logic_dev;
-	cap.info.dp_pmac_set = dp_pmac_set_31;
-	cap.info.dp_set_gsw_parser = dp_set_gsw_parser_31;
-	cap.info.dp_get_gsw_parser = dp_get_gsw_parser_31;
+	cap.info.dp_pmac_set = dp_gsw_set_pmac;
+	cap.info.dp_set_gsw_parser = dp_gsw_set_parser;
+	cap.info.dp_get_gsw_parser = dp_gsw_get_parser;
 	cap.info.dp_qos_platform_set = qos_platform_set;
-	cap.info.dp_set_gsw_pmapper = dp_set_gsw_pmapper_31;
-	cap.info.dp_get_gsw_pmapper = dp_get_gsw_pmapper_31;
+	cap.info.dp_set_gsw_pmapper = dp_gsw_set_pmapper;
+	cap.info.dp_get_gsw_pmapper = dp_gsw_get_pmapper;
 	cap.info.dp_ctp_tc_map_set = dp_ctp_tc_map_set_31;
-	cap.info.dp_meter_alloc = dp_meter_alloc_31;
-	cap.info.dp_meter_add = dp_meter_add_31;
-	cap.info.dp_meter_del = dp_meter_del_31;
-	cap.info.dp_set_bp_attr = dp_set_bp_attr_31;
+	cap.info.dp_meter_alloc = dp_gsw_alloc_meter;
+	cap.info.dp_meter_add = dp_gsw_add_meter;
+	cap.info.dp_meter_del = dp_gsw_del_meter;
+	cap.info.dp_set_bp_attr = dp_gsw_set_bp_attr;
 	cap.info.dp_rx = dp_rx_31;
 	cap.info.dp_tx = dp_xmit_31;
 	/* The full ethtool statistics support is provided in mib_counters
@@ -2450,8 +2454,8 @@ int register_dp_cap_gswip31(int flag)
 	 */
 	cap.info.dp_net_dev_get_ethtool_stats = NULL;
 
-	cap.info.dp_alloc_bridge_port = alloc_bridge_port;
-	cap.info.dp_free_bridge_port = free_bridge_port;
+	cap.info.dp_alloc_bridge_port = dp_gsw_alloc_bp;
+	cap.info.dp_free_bridge_port = dp_gsw_free_bp;
 	cap.info.dp_deq_update_info = deq_update_info;
 	cap.info.dp_set_ctp_bp = set_ctp_bp;
 	cap.info.dp_get_queue_qocc = dp_get_queue_qocc_31;
@@ -2472,8 +2476,8 @@ int register_dp_cap_gswip31(int flag)
 	cap.info.swdev_bridge_port_flags_get = dp_swdev_bridge_port_flags_get;
 	cap.info.swdev_port_learning_limit_set =
 		dp_swdev_port_learning_limit_set;
-	cap.info.dp_mac_set = dp_gswip_mac_entry_add;
-	cap.info.dp_mac_reset = dp_gswip_mac_entry_del;
+	cap.info.dp_mac_set = dp_gsw_add_mac_entry;
+	cap.info.dp_mac_reset = dp_gsw_del_mac_entry;
 	cap.info.dp_cfg_vlan = dp_gswip_ext_vlan; /*for symmetric VLAN */
 	cap.info.dp_tc_vlan_set = tc_vlan_set_31;
 #endif
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.h b/drivers/net/datapath/dpm/gswip31/datapath_misc.h
index bc7720ca1690..0d0a98bbce95 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.h
@@ -12,60 +12,89 @@
 #include <linux/debugfs.h>
 #include <linux/seq_file.h>
 
-#define PMAC_MAX_NUM  16
-#define PAMC_LAN_MAX_NUM 7
-#define VAP_DSL_OFFSET 3
-#define NEW_CBM_API 1
-#define PMAPPER_DISC_CTP 255
-#define MAX_PPV4_PORTS 38
-#define PPV4_PORT_BASE 90
-
-/* For WLAN 8 bit Mode VAP=11:8
- * For WLAN 9 bit Mode VAP=11:9
+#define PMAC_MAX_NUM		16
+#define PAMC_LAN_MAX_NUM	7
+#define VAP_DSL_OFFSET		3
+#define PMAPPER_DISC_CTP	255
+#define MAX_PPV4_PORTS		38
+
+/* For WLAN 8 bit Mode VAP=11:8/14:8
+ * For WLAN 9 bit Mode VAP=11:9/14:9
  * For Non-WLAN Mode VAP=7:0
  */
-#define VAP_8BIT_OFFSET 8
-#define VAP_8BIT_MASK   0xF
-#define VAP_9BIT_OFFSET 9
-#define VAP_9BIT_MASK   0x7
-#define VAP_NONWLAN_OFFSET 0
-#define VAP_NONWLAN_MASK 0xFF
-
-#define GSWIP_O_DEV_NAME 1
-#define GSWIP_L GSWIP_O_DEV_NAME
-#define GSWIP_R GSWIP_O_DEV_NAME
-#define MAX_SUBIF_PER_PORT 256
-#define MAX_CTP 288
-#define MAX_BP_NUM 128
-#define CPU_PORT 0
-#define CPU_SUBIF 0 /*cpu default subif ID*/
-#define CPU_BP 0 /*cpu default bridge port ID */
-#define CPU_FID 0 /*cpu default bridge ID */
+#define VAP_8BIT_OFFSET		8
+#define VAP_8BIT_MASK_31	GENMASK(3, 0)
+#define VAP_8BIT_MASK_32	GENMASK(6, 0)
+#define VAP_9BIT_OFFSET		9
+#define VAP_9BIT_MASK_31	GENMASK(2, 0)
+#define VAP_9BIT_MASK_32	GENMASK(5, 0)
+#define VAP_NONWLAN_OFFSET	0
+#define VAP_NONWLAN_MASK	GENMASK(7, 0)
+
+#define GSWIP_O_DEV_NAME	1
+#define GSWIP_L			GSWIP_O_DEV_NAME
+#define GSWIP_R			GSWIP_O_DEV_NAME
+#define MAX_SUBIF_PER_PORT	256
+#define MAX_BP_NUM		128
+#define MAX_GPID		256
+#define CPU_PORT		0
+#define CPU_SUBIF		0 /* CPU default subif ID*/
+#define CPU_BP			0 /* CPU default bridge port ID */
+#define CPU_FID			0 /* CPU default bridge ID */
+
 #define SET_BP_MAP(x, ix) (x[(ix) / 16] |= 1 << ((ix) % 16))
 #define GET_BP_MAP(x, ix) ((x[(ix) / 16] >> ((ix) % 16)) & 1)
 #define UNSET_BP_MAP(x, ix) (x[(ix) / 16] &= ~(1 << ((ix) % 16)))
+
 /* Max value for the limit of MAC addresses per bridge port. */
-#define GSWIP_LEARN_LIMIT_PORT_MAX		254
+#define GSWIP_LEARN_LIMIT_PORT_MAX	254
 
 enum CQE_LOOKUP_MODE {
-	CQE_LU_MODE0 = 0,
-	CQE_LU_MODE1,
-	CQE_LU_MODE2,
-	CQE_LU_MODE3,
-	CQE_LU_MODE_NOT_VALID,
+	CQE_LU_MODE0,		/* subif_id[13:8] + class[1:0] */
+	CQE_LU_MODE1,		/* subif_id[7:0]               */
+	CQE_LU_MODE2,		/* subif_id[11:8] + class[3:0] */
+	CQE_LU_MODE3,		/* subif_id[4:0]  + class[1:0] */
+	CQE_LU_MODE4,
+	CQE_LU_MODE5,
+	CQE_LU_MODE6,
+	CQE_LU_MODE7,
+	CQE_LU_MODE_INVALID,
 };
 
-#define PMAC_SIZE 8
 struct gsw_itf {
-	u8 ep; /*-1 means no assigned yet for dynamic case */
-	u8 fixed; /*fixed (1) or dynamically allocate (0)*/
+	u8 ep;		/* -1 means no assigned yet for dynamic case */
+	u8 fixed;	/* fixed (1) or dynamically allocate (0) */
 	u16 start;
 	u16 end;
 	u16 n;
 	u8 mode;
-	u8 cqe_mode; /*CQE look up mode */
+	u8 cqe_mode;	/* CQE look up mode */
+	u8 gpid_start;	/* First GPID port */
+	u8 gpid_num;	/* Number of GPID port allocated */
 };
 
+struct dp_gpid_map_table {
+	int alloc_flags;
+	int dpid;
+	int subif;
+	int ref_cnt;	/* reference counter */
+	unsigned long f_hif;
+};
+
+struct ctp_assign {
+	GSW_LogicalPortMode_t emode; /*mapped GSWIP CTP flag */
+	u16 max_ctp;
+	u16 max_subif;
+	u16 max_gpid;
+	u32 cqe_lu_md;
+	u16 swdev_en;
+};
+
+#define SET_PMAC_SUBIF(pmac, subif) do { \
+	(pmac)->src_dst_subif_id_lsb = (subif) & 0xff; \
+	(pmac)->src_dst_subif_id_msb =  ((subif) >> 8) & 0x1f; \
+} while (0)
+
 struct cqm_deq_stat;
 struct pp_queue_stat;
 
@@ -197,15 +226,6 @@ struct datapath_ctrl {
 	const char *name;
 };
 
-struct ctp_assign {
-	u32 flag; /*Datapath Device Flag */
-	GSW_LogicalPortMode_t emode; /*mapped GSWIP CTP flag */
-	u16 num; /*Max CTP allowed for that GSWIP logical port*/
-	u16 num_subif; /*Max allowed number of subif for that logical port*/
-	u32 lookup_mode; /*CQE lookup mode  */
-	u16 swdev_enable; /* To enable or disable switchdev feature */
-};
-
 /* brief structure to set special config for PON port registration */
 struct pon_spl_cfg {
 	int flag;
@@ -228,20 +248,9 @@ struct pon_spl_cfg {
 	(pmac)->src_dst_subif_id_msb =  ((subif) >> 8) & 0x1f; \
 } while (0)
 
-int alloc_bridge_port(int inst, int portid, int subif, int fid, int bp_member,
-		      int flags);
-int free_bridge_port(int inst, int bp);
-struct gsw_itf *ctp_port_assign(int inst, u8 ep, int bp_default,
-				u32 flags, struct dp_dev_data *data);
 void dp_sys_mib_reset_31(u32 flag);
-int dp_pmac_set_31(int inst, u32 port, dp_pmac_cfg_t *pmac_cfg);
-int dp_set_gsw_parser_31(u8 flag, u8 cpu, u8 mpe1, u8 mpe2, u8 mpe3);
-int dp_get_gsw_parser_31(u8 *cpu, u8 *mpe1, u8 *mpe2, u8 *mpe3);
-int gsw_mib_reset_31(int dev, u32 flag);
 
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-int dp_gswip_mac_entry_add(int bport, int fid, int inst, u8 *addr);
-int dp_gswip_mac_entry_del(int bport, int fid, int inst, u8 *addr);
 int set_gswip_ext_vlan(struct core_ops *ops, struct ext_vlan_info *vlan,
 		       int flag);
 #endif
@@ -266,21 +275,10 @@ int dp_queue_map_get_31(struct dp_queue_map_get *cfg, int flag);
 int dp_queue_map_set_31(struct dp_queue_map_set *cfg, int flag);
 int dp_counter_mode_set_31(struct dp_counter_conf *cfg, int flag);
 int dp_counter_mode_get_31(struct dp_counter_conf *cfg, int flag);
-int dp_set_gsw_pmapper_31(int inst, int bport, int lport,
-			  struct dp_pmapper *mapper, u32 flag);
-int dp_get_gsw_pmapper_31(int inst, int bport, int lport,
-			  struct dp_pmapper *mapper, u32 flag);
 int dp_children_get_31(struct dp_node_child *cfg, int flag);
 int dp_free_children_via_parent_31(struct dp_node_alloc *node, int flag);
 int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags);
 int dp_qos_level_get_31(struct dp_qos_level *dp, int flag);
-int dp_meter_alloc_31(int inst, int *meterid, int flag);
-int dp_meter_add_31(struct net_device *dev,
-		    struct dp_meter_cfg *meter,
-		    int flag, struct dp_meter_subif *mtr_subif);
-int dp_meter_del_31(struct net_device *dev,
-		    struct dp_meter_cfg *meter,
-		    int flag, struct dp_meter_subif *mtr_subif);
 int dp_qos_global_info_get_31(struct dp_qos_cfg_info *info, int flag);
 int dp_qos_port_conf_set_31(struct dp_port_cfg_info *info, int flag);
 int32_t dp_rx_31(struct sk_buff *skb, u32 flags);
@@ -288,7 +286,6 @@ int32_t dp_xmit_31(struct net_device *rx_if, dp_subif_t *rx_subif,
 		   struct sk_buff *skb, int32_t len, uint32_t flags);
 void set_chksum(struct pmac_tx_hdr *pmac, u32 tcp_type, u32 ip_offset,
 		int ip_off_hw_adjust, u32 tcp_h_offset);
-int dp_set_bp_attr_31(struct dp_bp_attr *conf, int bport, uint32_t flag);
 
 static inline char *parser_flag_str(u8 f)
 {
@@ -329,8 +326,6 @@ int get_q_mib(int inst, int qid,
 int get_p_mib(int inst, int pid,
 	      u32 *green /* bytes*/,
 	      u32 *yellow /*bytes*/);
-int cpu_vlan_mod_dis(int inst);
-int set_port_lookup_mode_31(int inst, u8 ep, u32 flags);
 int tc_vlan_set_31(struct core_ops *ops, struct dp_tc_vlan *vlan,
 		   struct dp_tc_vlan_info *info,
 		   int flag);
@@ -341,4 +336,38 @@ int dp_queue_block_flush_31(struct dp_qos_blk_flush_queue *cfg,
 int dp_port_block_flush_31(struct dp_qos_blk_flush_port *cfg,
 			   int flag);
 int dp_get_queue_qocc_31(struct dp_qos_queue_info *info, int flag);
+
+/* Bridge port */
+int dp_gsw_alloc_bp(int inst, int ep, int subif, int fid, int bp_member, int flags);
+int dp_gsw_free_bp(int inst, int bp);
+int dp_gsw_set_bp_attr(struct dp_bp_attr *conf, int bport, uint32_t flag);
+
+/* CTP port */
+const struct ctp_assign *dp_gsw_get_ctp_assign(int inst, int flags);
+struct gsw_itf *dp_gsw_assign_ctp(int inst, u8 ep, int bp_default, u32 flags,
+				  struct dp_dev_data *data);
+
+int gpid_port_assign(int inst, u8 ep, u32 flags);
+
+int dp_gsw_set_pmac(int inst, u32 port, dp_pmac_cfg_t *pmac_cfg);
+int dp_gsw_set_parser(u8 flag, u8 cpu, u8 mpe1, u8 mpe2, u8 mpe3);
+int dp_gsw_get_parser(u8 *cpu, u8 *mpe1, u8 *mpe2, u8 *mpe3);
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
+int dp_gsw_add_mac_entry(int bport, int fid, int inst, u8 *addr);
+int dp_gsw_del_mac_entry(int bport, int fid, int inst, u8 *addr);
+#endif
+
+int dp_gsw_set_pmapper(int inst, int bport, int ep, struct dp_pmapper *mapper, u32 flag);
+int dp_gsw_get_pmapper(int inst, int bport, int ep, struct dp_pmapper *mapper, u32 flag);
+
+int dp_gsw_alloc_meter(int inst, int *meterid, int flag);
+int dp_gsw_add_meter(struct net_device *dev, struct dp_meter_cfg *meter,
+		     int flag, struct dp_meter_subif *mtr_subif);
+int dp_gsw_del_meter(struct net_device *dev, struct dp_meter_cfg *meter,
+		     int flag, struct dp_meter_subif *mtr_subif);
+
+int dp_gsw_reset_mib(int dev, u32 flag);
+int dp_gsw_set_port_lu_md(int inst, u8 ep, u32 flags);
+int dp_gsw_dis_cpu_vlan_md(int inst);
 #endif /* DATAPATH_MISC31_H */
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_proc.c b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
index 2d5d1ada0acc..90c121297427 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_proc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
@@ -26,7 +26,7 @@ static void proc_parser_read(struct seq_file *s)
 
 	if (!capable(CAP_NET_ADMIN))
 		return;
-	dp_get_gsw_parser_31(&cpu, &mpe1, &mpe2, &mpe3);
+	dp_gsw_get_parser(&cpu, &mpe1, &mpe2, &mpe3);
 	seq_printf(s, "cpu : %s with parser size =%d bytes\n",
 		   parser_flag_str(cpu), parser_size_via_index(0));
 	seq_printf(s, "mpe1: %s with parser size =%d bytes\n",
@@ -105,7 +105,7 @@ ssize_t proc_parser_write(struct file *file, const char *buf,
 		DP_DEBUG(DP_DBG_FLAG_DBG,
 			 "flag=0x%x mpe3/2/1/cpu=%d/%d/%d/%d\n", flag, mpe3,
 			 mpe2, mpe1, cpu);
-		dp_set_gsw_parser_31(flag, cpu, mpe1, mpe2, mpe3);
+		dp_gsw_set_parser(flag, cpu, mpe1, mpe2, mpe3);
 	} else if (dp_strncmpi(param_list[0],
 			       "disable",
 			       strlen("disable")) == 0) {
@@ -150,11 +150,11 @@ ssize_t proc_parser_write(struct file *file, const char *buf,
 		DP_DEBUG(DP_DBG_FLAG_DBG,
 			 "flag=0x%x mpe3/2/1/cpu=%d/%d/%d/%d\n", flag, mpe3,
 			 mpe2, mpe1, cpu);
-		dp_set_gsw_parser_31(flag, cpu, mpe1, mpe2, mpe3);
+		dp_gsw_set_parser(flag, cpu, mpe1, mpe2, mpe3);
 	} else if (dp_strncmpi(param_list[0],
 			       "refresh",
 			       strlen("refresh")) == 0) {
-		dp_get_gsw_parser_31(NULL, NULL, NULL, NULL);
+		dp_gsw_get_parser(NULL, NULL, NULL, NULL);
 		pr_info("value:cpu=%d mpe1=%d mpe2=%d mpe3=%d\n", pinfo[0].v,
 			pinfo[1].v, pinfo[2].v, pinfo[3].v);
 		pr_info("size :cpu=%d mpe1=%d mpe2=%d mpe3=%d\n",
