From ceebd58c27ed62ee7b98c3ea2da417b37845e490 Mon Sep 17 00:00:00 2001
From: ofirbitt <ofir1.bitton@intel.com>
Date: Tue, 4 Feb 2020 14:22:17 +0200
Subject: [PATCH] LMTSYS-6759: PPv4 QoS Fix stat test

---
 .../ppv4/pp-tools/unit-test/tests/pp_qos_tests.c   | 47 +++++++++++-----------
 1 file changed, 23 insertions(+), 24 deletions(-)

diff --git a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c
index 1b85892f2161..cd5304a0f6ba 100644
--- a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c
+++ b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c
@@ -88,7 +88,7 @@ static int four_ports_four_queues_test(void *data);
 static int block_unblock_test(void *data);
 
 #ifndef PP_QOS_DISABLE_CMDQ
-/* static int stat_test(void *data); */
+static int stat_test(void *data);
 static int info_test(void *data);
 #endif
 static int reposition_test(void *data);
@@ -187,7 +187,6 @@ static struct pp_test_t pp_qos_tests[] = {
 		.test_data    = NULL,
 		.fn           = info_test,
 	},
-/*
 	{
 		.component    = PP_QOS_TESTS,
 		.test_name    = "stat_test",
@@ -196,7 +195,6 @@ static struct pp_test_t pp_qos_tests[] = {
 		.test_data    = NULL,
 		.fn           = stat_test,
 	},
-*/
 #endif
 	{
 		.component    = PP_QOS_TESTS,
@@ -4415,7 +4413,7 @@ static int wsp_test(void *data)
 }
 
 #ifndef PP_QOS_DISABLE_CMDQ
-/*
+
 static int stat_test(void *data)
 {
 	struct pp_qos_dev *qdev;
@@ -4439,6 +4437,7 @@ static int stat_test(void *data)
 
 	confp.port_parent_prop.arbitration = PP_QOS_ARBITRATION_WRR;
 	confp.ring_size = 1;
+	confp.disable = 1;
 	confp.ring_address = ring_addr_phys[0];
 
 	PPUT_FAIL_TEST(pp_qos_port_set(qdev, p, &confp) == 0,
@@ -4507,13 +4506,17 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-	create_push_desc_cmd(qdev, 0, 128, 1, qdev->hwconf.fw_stats_ddr_phys);
-	create_push_desc_cmd(qdev, 0, 256, 2, qdev->hwconf.fw_stats_ddr_phys);
-	create_push_desc_cmd(qdev, 1, 100, 1, qdev->hwconf.fw_stats_ddr_phys);
-	create_push_desc_cmd(qdev, 1, 200, 2, qdev->hwconf.fw_stats_ddr_phys);
-	update_cmd_id(&qdev->drvcmds);
-	if (transmit_cmds(qdev))
-		goto done;
+	qos_descriptor_push(qdev, q1, 128, 0, 0, PP_COLOR_GREEN, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
+
+	qos_descriptor_push(qdev, q1, 256, 0, 0, PP_COLOR_YELLOW, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
+
+	qos_descriptor_push(qdev, q2, 100, 0, 0, PP_COLOR_GREEN, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
+
+	qos_descriptor_push(qdev, q2, 200, 0, 0, PP_COLOR_YELLOW, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
 
 	memset(&qstat, 0, sizeof(qstat));
 	PPUT_FAIL_TEST(pp_qos_queue_stat_get(qdev, q1, &qstat) == 0,
@@ -4528,7 +4531,6 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-#ifdef PP_QOS_TEST_PACKET_OCCUPANCY
 	if (!(qstat.queue_packets_occupancy == 2 &&
 	      qstat.total_packets_accepted == 2 &&
 	      qstat.total_packets_dropped == 0 &&
@@ -4537,7 +4539,6 @@ static int stat_test(void *data)
 		dump_queue_stats(&qstat);
 		goto done;
 	}
-#endif
 
 	memset(&qstat, 0, sizeof(qstat));
 	qstat.reset = 1;
@@ -4553,7 +4554,6 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-#ifdef PP_QOS_TEST_PACKET_OCCUPANCY
 	if (!(qstat.queue_packets_occupancy == 2 &&
 	      qstat.total_packets_accepted == 2 &&
 	      qstat.total_packets_dropped == 0 &&
@@ -4562,7 +4562,6 @@ static int stat_test(void *data)
 		dump_queue_stats(&qstat);
 		goto done;
 	}
-#endif
 
 	memset(&pstat, 0, sizeof(pstat));
 	PPUT_FAIL_TEST(pp_qos_port_stat_get(qdev, p, &pstat) == 0,
@@ -4576,8 +4575,11 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-	create_push_desc_cmd(qdev, 0, 100, 1, qdev->hwconf.fw_stats_ddr_phys);
-	create_push_desc_cmd(qdev, 1, 300, 1, qdev->hwconf.fw_stats_ddr_phys);
+	qos_descriptor_push(qdev, q1, 100, 0, 0, PP_COLOR_GREEN, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
+
+	qos_descriptor_push(qdev, q2, 300, 0, 0, PP_COLOR_GREEN, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
 
 	memset(&qstat, 0, sizeof(qstat));
 	PPUT_FAIL_TEST(pp_qos_queue_stat_get(qdev, q1, &qstat) == 0,
@@ -4592,8 +4594,7 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-#ifdef PP_QOS_TEST_PACKET_OCCUPANCY
-	if (!(qstat.queue_packets_occupancy == 2 &&
+	if (!(qstat.queue_packets_occupancy == 3 &&
 	      qstat.total_packets_accepted == 3 &&
 	      qstat.total_packets_dropped == 0 &&
 	      qstat.total_packets_red_dropped == 0)) {
@@ -4601,7 +4602,6 @@ static int stat_test(void *data)
 		dump_queue_stats(&qstat);
 		goto done;
 	}
-#endif
 
 	PPUT_FAIL_TEST(pp_qos_queue_stat_get(qdev, q2, &qstat) == 0,
 		       "Could not get queue statistics\n");
@@ -4615,7 +4615,6 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-#ifdef PP_QOS_TEST_PACKET_OCCUPANCY
 	if (!(qstat.queue_packets_occupancy == 3 &&
 	      qstat.total_packets_accepted == 1 &&
 	      qstat.total_packets_dropped == 0 &&
@@ -4624,7 +4623,6 @@ static int stat_test(void *data)
 		dump_queue_stats(&qstat);
 		goto done;
 	}
-#endif
 
 	memset(&pstat, 0, sizeof(pstat));
 	pstat.reset = 1;
@@ -4639,7 +4637,8 @@ static int stat_test(void *data)
 		goto done;
 	}
 
-	create_push_desc_cmd(qdev, 0, 100, 2, qdev->hwconf.fw_stats_ddr_phys);
+	qos_descriptor_push(qdev, q1, 100, 0, 0, PP_COLOR_YELLOW, 0, 0,
+			    qdev->hwconf.fw_stats_ddr_phys);
 	memset(&pstat, 0, sizeof(pstat));
 
 	PPUT_FAIL_TEST(pp_qos_port_stat_get(qdev, p, &pstat) == 0,
@@ -4682,7 +4681,7 @@ static int stat_test(void *data)
 
 	return test_ret;
 }
-*/
+
 static int info_test(void *data)
 {
 	struct pp_qos_queue_conf confq;
