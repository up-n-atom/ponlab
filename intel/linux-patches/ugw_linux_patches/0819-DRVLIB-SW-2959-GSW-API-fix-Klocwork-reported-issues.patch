From 0740baaa45c72d1c994f3d43bf5b5c5565e4e389 Mon Sep 17 00:00:00 2001
From: Xu Liang <liang.xu@intel.com>
Date: Fri, 27 Sep 2019 14:10:53 +0800
Subject: [PATCH] DRVLIB_SW-2959: GSW API - fix Klocwork reported issues

---
 .../net/ethernet/lantiq/switch-api/gsw_flow_core.c | 165 ++++++++++-----------
 .../net/ethernet/lantiq/switch-api/gsw_flow_pce.c  |   6 +-
 drivers/net/ethernet/lantiq/switch-api/gsw_init.c  |   2 +-
 drivers/net/ethernet/lantiq/switch-api/gsw_init.h  |  12 +-
 .../ethernet/lantiq/switch-api/gsw_ioctl_wrapper.c |   2 +-
 drivers/net/ethernet/lantiq/switch-api/gsw_irq.c   |   8 +
 drivers/net/ethernet/lantiq/switch-api/gsw_pae.c   |   3 +-
 .../net/ethernet/lantiq/switch-api/gsw_swmcast.c   |   7 +-
 .../net/ethernet/lantiq/switch-api/mac/mac_cli.c   |   6 +
 .../net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c |   3 +-
 include/net/switch_api/lantiq_gsw.h                |   2 +-
 11 files changed, 108 insertions(+), 108 deletions(-)

diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_flow_core.c b/drivers/net/ethernet/lantiq/switch-api/gsw_flow_core.c
index 2422ccc4d7aa..73f302dd0bda 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_flow_core.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_flow_core.c
@@ -1353,9 +1353,9 @@ static int gsw3x_msw_table_rm(void *cdev, GSW_multicastTable_t *parm)
 		return GSW_statusErr;
 	}
 
-	if ((parm->eModeMember == GSW_IGMP_MEMBER_INCLUDE) &&
-	    (parm->eModeMember == GSW_IGMP_MEMBER_EXCLUDE) &&
-	    (parm->eModeMember == GSW_IGMP_MEMBER_DONT_CARE)) {
+	if ((parm->eModeMember != GSW_IGMP_MEMBER_INCLUDE) &&
+	    (parm->eModeMember != GSW_IGMP_MEMBER_EXCLUDE) &&
+	    (parm->eModeMember != GSW_IGMP_MEMBER_DONT_CARE)) {
 		pr_err("%s:%s:%d (!!!)\n", __FILE__, __func__, __LINE__);
 		return GSW_statusErr;
 	}
@@ -1697,9 +1697,9 @@ static int gsw2x_msw_table_rm(void *cdev, GSW_multicastTable_t *parm)
 		return GSW_statusErr;
 	}
 
-	if ((parm->eModeMember == GSW_IGMP_MEMBER_INCLUDE)
-	    && (parm->eModeMember == GSW_IGMP_MEMBER_EXCLUDE)
-	    && (parm->eModeMember == GSW_IGMP_MEMBER_DONT_CARE)) {
+	if ((parm->eModeMember != GSW_IGMP_MEMBER_INCLUDE)
+	    && (parm->eModeMember != GSW_IGMP_MEMBER_EXCLUDE)
+	    && (parm->eModeMember != GSW_IGMP_MEMBER_DONT_CARE)) {
 		pr_err("%s:%s:%d (!!!)\n", __FILE__, __func__, __LINE__);
 		return GSW_statusErr;
 	}
@@ -2211,7 +2211,7 @@ static u32 mratecalc(u32 ibsid, u32 expont, u32 mants)
 static int calc_mtoken(u32 mrate, u32 *ibsid, u32 *expont, u32 *mants)
 {
 	static const u16 ibs_table[] = {8 * 8, 32 * 8, 64 * 8, 96 * 8};
-	u8 i;
+	int i;
 
 	for (i = 3; i >= 0; i--) {
 		u32 exp;
@@ -2223,13 +2223,14 @@ static int calc_mtoken(u32 mrate, u32 *ibsid, u32 *expont, u32 *mants)
 			u32 mant = ((ibs * 25000) >> exp) / mrate;
 
 			if (mant < (1 << 10))  {
-				*ibsid = i;
+				*ibsid = (u32)i;
 				*expont = exp;
 				*mants = mant;
 				return 0;
 			}
 		}
 	}
+	return -1;
 }
 
 static u32 mratecalc_3_1(u32 ibsid, u32 expont, u32 mants)
@@ -3265,8 +3266,7 @@ void *ethsw_api_core_init(ethsw_core_init_t *ethcinit)
 	/** Initialize Switch Core Function Pointer */
 	ret = gsw_init_fn_ptrs(cdev);
 
-	if (ret ==
-	    GSW_statusErr) {
+	if (ret != GSW_statusOk) {
 		pr_err("%s:%s:%d (Initialize Switch Core Function Pointer failed)\n",
 		       __FILE__, __func__, __LINE__);
 		return PrvData;
@@ -5383,11 +5383,6 @@ GSW_return_t GSW_VLAN_IdDelete(void *cdev,
 			goto UNLOCK_AND_RETURN;
 		}
 
-		if (pcindex >= 64) {
-			ret = GSW_statusValueRange;
-			goto UNLOCK_AND_RETURN;
-		}
-
 		vlan_entry_set(cdev, pcindex, &avlantbl);
 		memset(&pcetable, 0, sizeof(pctbl_prog_t));
 		pcetable.pcindex = pcindex;
@@ -5835,11 +5830,6 @@ GSW_return_t GSW_VLAN_PortMemberAdd(void *cdev,
 				avlantbl.tm &= ~(1 << parm->nPortId);
 		}
 
-		if (pcindex >= 64) {
-			ret = GSW_statusValueRange;
-			goto UNLOCK_AND_RETURN;
-		}
-
 		vlan_entry_set(cdev, pcindex, &avlantbl);
 		pcetable.table = PCE_VLANMAP_INDEX;
 		pcetable.pcindex = pcindex;
@@ -6012,11 +6002,6 @@ GSW_return_t GSW_VLAN_PortMemberRemove(void *cdev,
 
 		avlantbl.tm &= ~(1 << parm->nPortId);
 
-		if (pcindex >= 64) {
-			ret = GSW_statusValueRange;
-			goto UNLOCK_AND_RETURN;
-		}
-
 		vlan_entry_set(cdev, pcindex, &avlantbl);
 		pcetable.table = PCE_VLANMAP_INDEX;
 		pcetable.pcindex = pcindex;
@@ -6110,11 +6095,6 @@ GSW_return_t GSW_VLAN_ReservedAdd(void *cdev,
 
 		avlantbl.reserved = 1;
 
-		if (pcindex >= 64) {
-			ret = GSW_statusValueRange;
-			goto UNLOCK_AND_RETURN;
-		}
-
 		vlan_entry_set(cdev, pcindex, &avlantbl);
 		memset(&pcetable, 0, sizeof(pctbl_prog_t));
 		pcetable.pcindex = pcindex;
@@ -6197,11 +6177,6 @@ GSW_return_t GSW_VLAN_ReservedRemove(void *cdev,
 			goto UNLOCK_AND_RETURN;
 		}
 
-		if (pcindex >= 64) {
-			ret = GSW_statusValueRange;
-			goto UNLOCK_AND_RETURN;
-		}
-
 		if (avlantbl.reserved == 0) {
 			pr_err("This VID was not reserve, reserve it first\n");
 			ret = GSW_statusErr;
@@ -7156,9 +7131,13 @@ GSW_return_t GSW_QoS_MeterCfgSet(void *cdev,
 
 		if (rate) {
 			if (IS_VRSN_31(gswdev->gipver))
-				calc_mtoken_3_1(rate, &ibs, &exp, &mant);
+				ret = calc_mtoken_3_1(rate, &ibs, &exp, &mant);
 			else
-				calc_mtoken(rate, &ibs, &exp, &mant);
+				ret = calc_mtoken(rate, &ibs, &exp, &mant);
+			if (ret) {
+				ret = GSW_statusErr;
+				goto UNLOCK_AND_RETURN;
+			}
 		}
 
 		/* Rate Counter Exponent */
@@ -7185,9 +7164,13 @@ GSW_return_t GSW_QoS_MeterCfgSet(void *cdev,
 
 		if (rate) {
 			if (IS_VRSN_31(gswdev->gipver))
-				calc_mtoken_3_1(rate, &ibs, &exp, &mant);
+				ret = calc_mtoken_3_1(rate, &ibs, &exp, &mant);
 			else
-				calc_mtoken(rate, &ibs, &exp, &mant);
+				ret = calc_mtoken(rate, &ibs, &exp, &mant);
+			if (ret) {
+				ret = GSW_statusErr;
+				goto UNLOCK_AND_RETURN;
+			}
 		}
 
 		/* Rate Counter Exponent */
@@ -7244,8 +7227,13 @@ GSW_return_t GSW_QoS_MeterCfgSet(void *cdev,
 		/* Calc the Rate and convert to MANT and EXP*/
 		rate = parm->nRate;
 
-		if (rate)
-			calc_mtoken(rate, &ibs, &exp, &mant);
+		if (rate) {
+			ret = calc_mtoken(rate, &ibs, &exp, &mant);
+			if (ret) {
+				ret = GSW_statusErr;
+				goto UNLOCK_AND_RETURN;
+			}
+		}
 
 		/* Rate Counter Exponent */
 		gsw_w32(cdev, (PCE_TCM_CIR_EXP_EXP_OFFSET +
@@ -7676,8 +7664,7 @@ GSW_return_t GSW_QoS_ClassDSCP_Get(void *cdev,
 	spin_lock_bh(&gswdev->lock_pce);
 #endif
 
-	if (gswdev->gipver != LTQ_GSWIP_3_0 ||
-	    IS_VRSN_NOT_31(gswdev->gipver)) {
+	if (VRSN_CMP(gswdev->gipver, LTQ_GSWIP_3_0) < 0) {
 		for (value = 0; value < 16; value++) {
 			pcetable.table = PCE_REMARKING_INDEX;
 			pcetable.pcindex = value;
@@ -7717,8 +7704,7 @@ GSW_return_t GSW_QoS_ClassDSCP_Set(void *cdev,
 	spin_lock_bh(&gswdev->lock_pce);
 #endif
 
-	if (gswdev->gipver != LTQ_GSWIP_3_0
-	    || IS_VRSN_NOT_31(gswdev->gipver)) {
+	if (VRSN_CMP(gswdev->gipver, LTQ_GSWIP_3_0) < 0) {
 		for (value = 0; value < 16; value++) {
 			pcetable.table = PCE_REMARKING_INDEX;
 			pcetable.pcindex = value;
@@ -8166,8 +8152,7 @@ GSW_return_t GSW_QoS_ClassPCP_Get(void *cdev,
 	spin_lock_bh(&gswdev->lock_pce);
 #endif
 
-	if (gswdev->gipver != LTQ_GSWIP_3_0 ||
-	    IS_VRSN_NOT_31(gswdev->gipver)) {
+	if (VRSN_CMP(gswdev->gipver, LTQ_GSWIP_3_0) < 0) {
 		for (value = 0; value < 16; value++) {
 			pcetable.table = PCE_REMARKING_INDEX;
 			pcetable.pcindex = value;
@@ -8207,8 +8192,7 @@ GSW_return_t GSW_QoS_ClassPCP_Set(void *cdev,
 	spin_lock_bh(&gswdev->lock_pce);
 #endif
 
-	if (gswdev->gipver != LTQ_GSWIP_3_0 ||
-	    IS_VRSN_NOT_31(gswdev->gipver)) {
+	if (VRSN_CMP(gswdev->gipver, LTQ_GSWIP_3_0) < 0) {
 		for (value = 0; value < 16; value++) {
 			pcetable.table = PCE_REMARKING_INDEX;
 			pcetable.pcindex = value;
@@ -8499,8 +8483,7 @@ GSW_return_t GSW_QoS_PortCfgSet(void *cdev,
 			PCE_PCTRL_2_PCP_SHIFT,
 			PCE_PCTRL_2_PCP_SIZE, 1);
 
-		if ((gswdev->gipver == LTQ_GSWIP_2_2_ETC) ||
-		    (IS_VRSN_30_31(gswdev->gipver))) {
+		if (VRSN_CMP(gswdev->gipver, LTQ_GSWIP_2_2_ETC) >= 0) {
 			gsw_w32(cdev, (PCE_PCTRL_2_SPCP_OFFSET +
 				       (10 * parm->nPortId)),
 				PCE_PCTRL_2_SPCP_SHIFT,
@@ -8509,8 +8492,26 @@ GSW_return_t GSW_QoS_PortCfgSet(void *cdev,
 
 		break;
 
-		if ((gswdev->gipver == LTQ_GSWIP_2_2_ETC) ||
-		    (IS_VRSN_30_31(gswdev->gipver))) {
+	case GSW_QOS_CLASS_SELECT_SPCP:
+	case GSW_QOS_CLASS_SELECT_SPCP_DSCP:
+	case GSW_QOS_CLASS_SELECT_DSCP_SPCP:
+	case GSW_QOS_CLASS_SELECT_SPCP_PCP:
+	case GSW_QOS_CLASS_SELECT_SPCP_PCP_DSCP:
+	case GSW_QOS_CLASS_SELECT_DSCP_SPCP_PCP:
+		/* handled in condition GSWIP >= 2.2 ETC */
+		break;
+	}
+
+	if (VRSN_CMP(gswdev->gipver, LTQ_GSWIP_2_2_ETC) >= 0) {
+		switch (parm->eClassMode) {
+		case GSW_QOS_CLASS_SELECT_NO:
+		case GSW_QOS_CLASS_SELECT_DSCP:
+		case GSW_QOS_CLASS_SELECT_PCP:
+		case GSW_QOS_CLASS_SELECT_DSCP_PCP:
+		case GSW_QOS_CLASS_SELECT_PCP_DSCP:
+			/* handled in last switch for all GSWIP version */
+			break;
+
 		case GSW_QOS_CLASS_SELECT_SPCP:
 			gsw_w32(cdev, (PCE_PCTRL_2_DSCP_OFFSET +
 				       (10 * parm->nPortId)),
@@ -8991,9 +8992,13 @@ GSW_return_t GSW_QoS_ShaperCfgSet(void *cdev,
 
 	if (rate) {
 		if (IS_VRSN_31(gswdev->gipver))
-			calc_mtoken_3_1(rate, &ibs, &exp, &mant);
+			ret = calc_mtoken_3_1(rate, &ibs, &exp, &mant);
 		else
-			calc_mtoken(rate, &ibs, &exp, &mant);
+			ret = calc_mtoken(rate, &ibs, &exp, &mant);
+		if (ret) {
+			ret = GSW_statusErr;
+			goto UNLOCK_AND_RETURN;
+		}
 	}
 
 	//Set CIR Exponent and Mantissa.
@@ -9172,11 +9177,13 @@ GSW_return_t GSW_QoS_ShaperQueueDeassign(void *cdev,
 		goto UNLOCK_AND_RETURN;
 	}
 
+#ifdef DISABLE_SHAPER_IF_NOT_ASSIGNED
 	if ((value1 == 0) && (value3 == 0) &&
 	    (0 == gswdev->shaper_idx[rshid].usageCount)) {
 		gsw_w32(cdev, RS_CTRL_RSEN_OFFSET + (rshid * 0x5),
 			RS_CTRL_RSEN_SHIFT, RS_CTRL_RSEN_SIZE, 0);
 	}
+#endif
 
 	ret = GSW_statusOk;
 
@@ -11520,7 +11527,7 @@ GSW_return_t GSW_MulticastTableEntryRead(void *cdev,
 								 | (iptbl.key[0]));
 							parm->eIPVersion =
 								GSW_IP_SELECT_IPV4;
-						} else if (iptbl.mask == 0x0) {
+						} else if (iptbl.mask[0] == 0x0) {
 							parm->uIP_Gsa.nIPv6[4] =
 								(iptbl.key[3]);
 							parm->uIP_Gsa.nIPv6[5] =
@@ -11557,7 +11564,7 @@ GSW_return_t GSW_MulticastTableEntryRead(void *cdev,
 								(iptbl.key[0]);
 						}
 					} else {
-						if (iptbl.mask  == 0) {
+						if (iptbl.mask[0] == 0) {
 							parm->uIP_Gda.nIPv6[0] =
 								(iptbl.key[3]);
 							parm->uIP_Gda.nIPv6[1] =
@@ -11594,7 +11601,7 @@ GSW_return_t GSW_MulticastTableEntryRead(void *cdev,
 								(iptbl.key[0]);
 						}
 					} else {
-						if (iptbl.mask == 0) {
+						if (iptbl.mask[0] == 0) {
 							parm->uIP_Gsa.nIPv6[0] =
 								(iptbl.key[3]);
 							parm->uIP_Gsa.nIPv6[1] =
@@ -11883,10 +11890,6 @@ GSW_return_t GSW_CapGet(void *cdev, GSW_cap_t *parm)
 		parm->nCap = gswdev->num_of_global_rules;
 		break;
 
-	case GSW_CAP_TYPE_LAST:
-		parm->nCap = GSW_CAP_TYPE_LAST;
-		break;
-
 	default:
 		parm->nCap = 0;
 		break;
@@ -14323,9 +14326,9 @@ static GSW_return_t GSW_RMON_Port_Get_Active(void *cdev, GSW_RMON_Port_cnt_t *pa
 	bmtable.adr.rmon.portOffset = PortId;
 
 	if (Portmode == GSW_CTP_PORT) {
-		bmtable.tableID = (BM_Table_ID)GSW_RMON_CTP_PORT_RX;
+		bmtable.tableID = CTP_PORT_RX_RMON;
 	} else {
-		bmtable.tableID = (BM_Table_ID)GSW_RMON_BRIDGE_PORT_RX;
+		bmtable.tableID = BRIDGE_PORT_RX_RMON;
 	}
 
 	bmtable.b64bitMode = 1;
@@ -14378,7 +14381,7 @@ static GSW_return_t GSW_RMON_Port_Get_Active(void *cdev, GSW_RMON_Port_cnt_t *pa
 			parm->nRxExtendedVlanDiscardPkts = data;
 
 			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
+			if (Portmode == GSW_CTP_PORT)
 				parm->nRxFCSErrorPkts = data1; /* Receive MAC/FCS Error frame Count */
 
 			break;
@@ -14386,36 +14389,30 @@ static GSW_return_t GSW_RMON_Port_Get_Active(void *cdev, GSW_RMON_Port_cnt_t *pa
 		case 0x10: /* Receive Undersize Bad Count */
 
 			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
+			if (Portmode == GSW_CTP_PORT) {
 				parm->nRxUnderSizeErrorPkts = data;
-
-			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
 				parm->nRxOversizeErrorPkts = data1; /* Receive Oversize Bad Count */
+			}
 
 			break;
 
 		case 0x12: /* MTU Exceed Discard Count */
 
 			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
+			if (Portmode == GSW_CTP_PORT) {
 				parm->nMtuExceedDiscardPkts = data;
-
-			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
 				parm->nRxDroppedPkts = data1; /* Receive Discard (Tail-Drop) Frame Count */
+			}
 
 			break;
 
 		case 0x14: /* Receive Bad Byte Count (Low) */
 
 			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
+			if (Portmode == GSW_CTP_PORT) {
 				rbbcl = data;
-
-			/*only applicable for CTP RX*/
-			if (Portmode == GSW_RMON_CTP_PORT_RX)
 				rbbch = data1; /* Receive Bad Byte Count (High) */
+			}
 
 			break;
 		}
@@ -20056,7 +20053,7 @@ GSW_return_t GSW_ExtendedVlanSet(void *cdev, GSW_EXTENDEDVLAN_config_t *parm)
 				dscp2pcp_get.nIndex = idx;
 				ret = GSW_QOS_Dscp2PcpTableGet(cdev, &dscp2pcp_get);
 
-				if (ret == GSW_statusErr) {
+				if (ret != GSW_statusOk) {
 					pr_err("ERROR:%s, GSW_QOS_Dscp2PcpTableGet failed\n",
 						__func__);
 					goto UNLOCK_AND_RETURN;
@@ -20101,7 +20098,7 @@ GSW_return_t GSW_ExtendedVlanSet(void *cdev, GSW_EXTENDEDVLAN_config_t *parm)
 				dscp2pcp_set.nIndex = dscp2pcp_pointer;
 				ret = GSW_QOS_Dscp2PcpTableSet(cdev, &dscp2pcp_set);
 
-				if (ret == GSW_statusErr) {
+				if (ret != GSW_statusOk) {
 					pr_err("ERROR:%s, GSW_QOS_Dscp2PcpTableSet failed\n",
 						__func__);
 					goto UNLOCK_AND_RETURN;
@@ -20625,7 +20622,7 @@ GSW_return_t GSW_ExtendedVlanGet(void *cdev, GSW_EXTENDEDVLAN_config_t *parm)
 		dscp2pcp_get.nIndex = dscp2pcp_pointer;
 		ret = GSW_QOS_Dscp2PcpTableGet(cdev, &dscp2pcp_get);
 
-		if (ret == GSW_statusErr) {
+		if (ret != GSW_statusOk) {
 			pr_err("ERROR:%s, GSW_QOS_Dscp2PcpTableGet failed\n",
 				__func__);
 			goto UNLOCK_AND_RETURN;
@@ -23942,7 +23939,7 @@ GSW_return_t GSW_BridgePortConfigGet(void *cdev, GSW_BRIDGE_portConfig_t *param)
 			param->sPmapper.nPmapperId = (tbl_prog_brdgeport_egress.val[4] & 0xFF);
 			ret = GSW_QOS_PmapperTableGet(cdev, &param->sPmapper);
 
-			if (ret == GSW_statusErr) {
+			if (ret != GSW_statusOk) {
 				pr_err("ERROR :P-mapper Get return Error\n");
 				goto UNLOCK_AND_RETURN;
 			}
@@ -25104,7 +25101,7 @@ GSW_return_t GSW_CtpPortConfigGet(void *cdev, GSW_CTP_portConfig_t *param)
 	ctp_get.nLogicalPortId = param->nLogicalPortId;
 	ret = GSW_CTP_PortAssignmentGet(cdev, &ctp_get);
 
-	if (ret == GSW_statusErr)
+	if (ret != GSW_statusOk)
 		goto UNLOCK_AND_RETURN;
 
 	/*IMPORTANT NOTE : :As per the CTP concept  nFirstCtpPortId + nSubIfIdGroup is the expected CTP port*/
@@ -25268,7 +25265,7 @@ GSW_return_t GSW_CtpPortConfigGet(void *cdev, GSW_CTP_portConfig_t *param)
 				param->sPmapper.nPmapperId = (tbl_prog_ctpport_ingress.val[4] & 0xFF);
 				ret = GSW_QOS_PmapperTableGet(cdev, &param->sPmapper);
 
-				if (ret == GSW_statusErr)
+				if (ret != GSW_statusOk)
 					goto UNLOCK_AND_RETURN;
 
 				value = ((tbl_prog_ctpport_ingress.val[4] & 0x2000) >> 13);
@@ -25502,7 +25499,7 @@ GSW_return_t GSW_CtpPortConfigReset(void *cdev, GSW_CTP_portConfig_t *param)
 	ctp_get.nLogicalPortId = param->nLogicalPortId;
 	ret = GSW_CTP_PortAssignmentGet(cdev, &ctp_get);
 
-	if (ret == GSW_statusErr)
+	if (ret != GSW_statusOk)
 		goto UNLOCK_AND_RETURN;
 
 	/*IMPORTANT NOTE : :As per the CTP concept  nFirstCtpPortId + nSubIfIdGroup is the expected CTP port*/
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_flow_pce.c b/drivers/net/ethernet/lantiq/switch-api/gsw_flow_pce.c
index 517e473148a6..25492eb63295 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_flow_pce.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_flow_pce.c
@@ -2754,7 +2754,7 @@ int pce_rule_read(void *cdev, ltq_pce_table_t *pthandle, GSW_PCE_rule_t *parm)
 				}
 			}
 		} else if ((gswdev->gipver == LTQ_GSWIP_2_2) || (gswdev->gipver == LTQ_GSWIP_2_2_ETC))
-			parm->pattern.nDstIP_Mask = dasa_tbl.mask[0] << 16;
+			parm->pattern.nDstIP_Mask = (u32)dasa_tbl.mask[0] << 16;
 
 		/* DIP LS8B used */
 		parm->pattern.eDstIP_Select = GSW_PCE_IP_V6;
@@ -2780,7 +2780,7 @@ int pce_rule_read(void *cdev, ltq_pce_table_t *pthandle, GSW_PCE_rule_t *parm)
 				}
 			}
 		} else if ((gswdev->gipver == LTQ_GSWIP_2_2) || (gswdev->gipver == LTQ_GSWIP_2_2_ETC))
-			parm->pattern.nDstIP_Mask |= dasa_tbl_lsb.mask[0];
+			parm->pattern.nDstIP_Mask |= (u32)dasa_tbl_lsb.mask[0];
 	} else if (ptable->dip_lsb_idx != 0xFF) {  //IPv4
 		pce_dasa_lsb_t dasa_tbl;
 		memset(&dasa_tbl, 0, sizeof(pce_dasa_lsb_t));
@@ -2871,7 +2871,7 @@ int pce_rule_read(void *cdev, ltq_pce_table_t *pthandle, GSW_PCE_rule_t *parm)
 				}
 			}
 		} else if ((gswdev->gipver == LTQ_GSWIP_2_2) || (gswdev->gipver == LTQ_GSWIP_2_2_ETC))
-			parm->pattern.nSrcIP_Mask |= dasa_tbl_lsb.mask[0];
+			parm->pattern.nSrcIP_Mask |= (u32)dasa_tbl_lsb.mask[0];
 	} else if (ptable->sip_lsb_idx != 0xFF) { //IPv4
 		pce_dasa_lsb_t dasa_tbl;
 		memset(&dasa_tbl, 0, sizeof(pce_dasa_lsb_t));
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_init.c b/drivers/net/ethernet/lantiq/switch-api/gsw_init.c
index f976c813818e..3bf632382407 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_init.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_init.c
@@ -935,7 +935,7 @@ int ltq_gsw_api_register(struct platform_device *pdev)
 
 	memset(&core_init, 0, sizeof(ethsw_core_init_t));
 
-	if (device_id < 0 || device_id >= 2)
+	if (device_id >= 2)
 		return -EINVAL;
 
 	/* Find and map our resources */
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_init.h b/drivers/net/ethernet/lantiq/switch-api/gsw_init.h
index e25237e6a2d7..1c633191f182 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_init.h
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_init.h
@@ -230,23 +230,15 @@ static inline void gsw_w32_raw(void *cdev, short offset, u32 value)
 
 static inline ethsw_api_dev_t *GSW_PDATA_GET(void *pdev)
 {
-	struct core_ops *gsw_ops;
 	ethsw_api_dev_t *pdata = NULL;
 
 	if (pdev == NULL) {
 		pr_err("%s:%s:%d", __FILE__, __func__, __LINE__);
-		return pdata;
+		return NULL;
 	}
 
 #ifdef __KERNEL__
-	gsw_ops = (struct core_ops *)pdev;
-
-	if (gsw_ops == NULL) {
-		pr_err("%s:%s:%d", __FILE__, __func__, __LINE__);
-		return pdata;
-	}
-
-	pdata = container_of(gsw_ops, ethsw_api_dev_t, ops);
+	pdata = container_of((struct core_ops *)pdev, ethsw_api_dev_t, ops);
 #else
 	pdata = (ethsw_api_dev_t *)pdev;
 #endif
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_ioctl_wrapper.c b/drivers/net/ethernet/lantiq/switch-api/gsw_ioctl_wrapper.c
index 4cb786fc6051..4d17caebbe67 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_ioctl_wrapper.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_ioctl_wrapper.c
@@ -293,7 +293,7 @@ int gsw_api_drv_register(u32 major)
 
 	/* Register the device class */
 	gswss_class = class_create(THIS_MODULE, ETHSW_API_DEV_NAME);
-	if (!gswss_class) {
+	if (IS_ERR_OR_NULL(gswss_class)) {
 		result = -EEXIST;
 		pr_err("SWAPI: failed to create class %d\n", result);
 		goto fail_create_class;
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_irq.c b/drivers/net/ethernet/lantiq/switch-api/gsw_irq.c
index fcb49a85c43c..67bd3cf6a54c 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_irq.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_irq.c
@@ -609,9 +609,15 @@ GSW_return_t GSW_Irq_init(void *cdev)
 #ifdef __KERNEL__
 	gswdev->PceIrqList =
 		(struct pce_irq_linklist *)kmalloc(sizeof(struct pce_irq_linklist), GFP_KERNEL);
+	if (gswdev->PceIrqList == NULL) {
+		pr_err("%s:%s:%d: kmalloc fail\n", __FILE__, __func__, __LINE__);
+		return GSW_statusErr;
+	}
 	ret = request_irq(gswdev->irq_num, GSW_ISR, 0, "gswip", cdev);
 
 	if (ret) {
+		kfree(gswdev->PceIrqList);
+		gswdev->PceIrqList = NULL;
 		pr_err("Switch irq request error %s:%s:%d", __FILE__, __func__, __LINE__);
 		return ret;
 	}
@@ -625,6 +631,8 @@ GSW_return_t GSW_Irq_init(void *cdev)
 #else
 	gswdev->PceIrqList =
 		(struct pce_irq_linklist *)malloc(sizeof(struct pce_irq_linklist));
+	if (gswdev->PceIrqList == NULL)
+		return GSW_statusErr;
 
 	gswdev->PceIrqList->first_ptr = NULL;
 	gswdev->PceIrqList->last_ptr = NULL;
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c b/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c
index f2d85a219116..c525fdd0eb44 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_pae.c
@@ -995,9 +995,8 @@ int GSW_ROUTE_SessionEntryAdd(void *cdev, GSW_ROUTE_Entry_t *rpar)
 		}
 
 		/* Tunnel Index */
+		ptable.val[12] &= ~(3 << 4);
 		if (rpar->routeEntry.action.bTunnel_Enable == 1) {
-			ptable.val[12] &= ~(3 << 4);
-
 			switch (rpar->routeEntry.action.eTunType) {
 			case GSW_ROUTE_TUNL_NULL:
 				ptable.val[12] |= (0 << 4);
diff --git a/drivers/net/ethernet/lantiq/switch-api/gsw_swmcast.c b/drivers/net/ethernet/lantiq/switch-api/gsw_swmcast.c
index 2ee2c2e17301..e93da6c65a26 100644
--- a/drivers/net/ethernet/lantiq/switch-api/gsw_swmcast.c
+++ b/drivers/net/ethernet/lantiq/switch-api/gsw_swmcast.c
@@ -454,12 +454,11 @@ int gsw_insert_hashtable_entry(void *cdev, GSW_multicastTable_t *parm)
 	   1. New hashidx entry
 	   2. Existing hashidx with a new entry
 	 */
-	if ((loc = get_hashtable_empty_slot(cdev, phtable)) == TBL_FULL) {
+	loc = (u32)get_hashtable_empty_slot(cdev, phtable);
+	if (loc == TBL_FULL) {
 		pr_err("Hash Table FULL\n");
 		return TBL_FULL;
-	}
-
-	if (loc > MCAST_TABLE_SIZE) {
+	} else if (loc > MCAST_TABLE_SIZE) {
 		pr_err("Location got is wrong\n");
 		return FAIL;
 	}
diff --git a/drivers/net/ethernet/lantiq/switch-api/mac/mac_cli.c b/drivers/net/ethernet/lantiq/switch-api/mac/mac_cli.c
index f608a97935d7..b052fe5ff632 100644
--- a/drivers/net/ethernet/lantiq/switch-api/mac/mac_cli.c
+++ b/drivers/net/ethernet/lantiq/switch-api/mac/mac_cli.c
@@ -260,6 +260,8 @@ int xgmac_cfg_main(GSW_MAC_Cli_t *params)
 			break;
 		}
 	}
+	if (i >= num_of_elem)
+		return -1;
 
 	ops = gsw_get_mac_ops(0, params->mac_idx);
 	pdata = GET_MAC_PDATA(ops);
@@ -494,6 +496,8 @@ int gswss_cfg_main(GSW_MAC_Cli_t *params)
 			break;
 		}
 	}
+	if (i >= num_of_elem)
+		return -1;
 
 	if (params->cmdType < GSWSS_MAX_MAC) {
 
@@ -598,6 +602,8 @@ int lmac_cfg_main(GSW_MAC_Cli_t *params)
 			break;
 		}
 	}
+	if (i >= num_of_elem)
+		return -1;
 
 	mac_ops = gsw_get_mac_ops(0, params->mac_idx);
 	mac_pdata = GET_MAC_PDATA(mac_ops);
diff --git a/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c b/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c
index 4f8c07bc9579..64f8b730af8e 100644
--- a/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c
+++ b/drivers/net/ethernet/lantiq/switch-api/mac/xgmac_ptp.c
@@ -766,13 +766,12 @@ static int xgmac_ptp_register(void *pdev)
 	info->enable	= xgmac_extts_enable;
 #ifdef CONFIG_PTP_1588_CLOCK
 	pdata->ptp_clock = ptp_clock_register(info, pdata->dev);
-#endif
-
 	if (IS_ERR(pdata->ptp_clock)) {
 		pdata->ptp_clock = NULL;
 		pr_err("ptp_clock_register() failed\n");
 		return -1;
 	}
+#endif
 
 	mac_dbg("Added PTP HW clock successfully\n");
 
diff --git a/include/net/switch_api/lantiq_gsw.h b/include/net/switch_api/lantiq_gsw.h
index f910cbbb1e63..7fa80d78ebec 100644
--- a/include/net/switch_api/lantiq_gsw.h
+++ b/include/net/switch_api/lantiq_gsw.h
@@ -760,7 +760,7 @@ typedef struct {
 	/** VXLAN Mask value */
 	u16 vxlan_value;
 	/** Index of entry in PCE table */
-	ltq_bool_t index;
+	u16 index;
 } gsw_vxlan_cfg_t;
 
 /*@}*/ /* GSW_IOCTL_VLAN */
