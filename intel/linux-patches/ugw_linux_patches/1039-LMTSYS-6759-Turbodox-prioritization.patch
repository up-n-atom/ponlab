From 50824551a706d197dd0b6df08cc0235f20941c92 Mon Sep 17 00:00:00 2001
From: ofirbitt <ofir1.bitton@intel.com>
Date: Thu, 9 Jan 2020 15:48:55 +0200
Subject: [PATCH] LMTSYS-6759: Turbodox prioritization

---
 drivers/net/ppv4/pp-api/pp-misc/pp_misc.c          |   2 +-
 .../ppv4/pp-api/pp-session-mgr/pp_session_mgr.c    |   7 +-
 .../ppv4/pp-api/pp-session-mgr/pp_session_mgr.h    |  16 +-
 .../pp-api/pp-session-mgr/pp_session_mgr_debugfs.c |   4 +
 .../pp-session-mgr/pp_session_mgr_internal.h       |  44 ++---
 .../pp-api/pp-session-mgr/pp_session_mgr_tdox.c    | 212 +++++++++++++--------
 drivers/net/ppv4/pp-dev/pp_dts.c                   |   6 +
 drivers/net/ppv4/pp-hal/checker/checker.c          |   8 +
 drivers/net/ppv4/pp-hal/checker/checker.h          |   6 +
 drivers/net/ppv4/pp-hal/micro-controller/uc.c      |  46 ++---
 drivers/net/ppv4/pp-hal/micro-controller/uc.h      |  21 +-
 .../net/ppv4/pp-hal/micro-controller/uc_common.h   |  40 +++-
 .../pp-tools/unit-test/tests/pp_session_tests.c    |  68 ++++---
 13 files changed, 276 insertions(+), 204 deletions(-)

diff --git a/drivers/net/ppv4/pp-api/pp-misc/pp_misc.c b/drivers/net/ppv4/pp-api/pp-misc/pp_misc.c
index 5b1779fc8a1e..cc603ac108c2 100644
--- a/drivers/net/ppv4/pp-api/pp-misc/pp_misc.c
+++ b/drivers/net/ppv4/pp-api/pp-misc/pp_misc.c
@@ -264,7 +264,7 @@ s32 pp_nf_set(enum pp_nf_type type, struct pp_nf_info *nf)
 	memcpy(&db->nf[type], nf, sizeof(*nf));
 
 	/* Notify session manager of the new NF added */
-	smgr_nf_set(type, nf);
+	ret = smgr_nf_set(type, nf);
 out:
 	return ret;
 }
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
index 89d06ee2fad0..be519c3c490b 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
@@ -4376,13 +4376,13 @@ static s32 __smgr_db_async_init(void)
 	return 0;
 }
 
-void smgr_nf_set(enum pp_nf_type type, struct pp_nf_info *nf)
+s32 smgr_nf_set(enum pp_nf_type type, struct pp_nf_info *nf)
 {
 	switch (type) {
 	case PP_NF_REASSEMBLY:
 	break;
 	case PP_NF_TURBODOX:
-		smgr_tdox_late_init();
+		return smgr_tdox_late_init();
 	break;
 	case PP_NF_FRAGMENTER:
 	break;
@@ -4392,7 +4392,10 @@ void smgr_nf_set(enum pp_nf_type type, struct pp_nf_info *nf)
 	break;
 	default:
 		pr_err("Unknown nf type %u\n", type);
+		return -EINVAL;
 	}
+
+	return 0;
 }
 
 /**
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.h b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.h
index 7662484a19ef..dabc8490f808 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.h
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.h
@@ -226,6 +226,8 @@ struct smgr_tdox_stats {
 	u32 obsolete_list_cnt;
 	u32 cand_free_list_cnt;
 	u32 cand_busy_list_cnt;
+	u32 sess_update_free_list_cnt;
+	u32 sess_update_busy_list_cnt;
 	u32 tdox_sess_free;
 	u32 tdox_free_candidates;
 	u32 tdox_sess_full_fail;
@@ -333,8 +335,9 @@ s32 smgr_sq_dbg_stats_get(struct smgr_sq_stats *stats, bool reset);
 
 /**
  * @brief Set NF in session manager
+ * @return s32 0 on success
  */
-void smgr_nf_set(enum pp_nf_type type, struct pp_nf_info *nf);
+s32 smgr_nf_set(enum pp_nf_type type, struct pp_nf_info *nf);
 
 /**
  * @brief Allocate sessions array
@@ -345,6 +348,12 @@ void smgr_nf_set(enum pp_nf_type type, struct pp_nf_info *nf);
  */
 s32 smgr_sessions_arr_alloc(u32 **sessions, u32 *n_sessions);
 
+/**
+ * @brief Initiate late parameters
+ * @return s32 0 on success, error code otherwise
+ */
+s32 smgr_tdox_late_init(void);
+
 /**
  * @brief Get tdox stats
  * @param stats statistics structure
@@ -352,11 +361,6 @@ s32 smgr_sessions_arr_alloc(u32 **sessions, u32 *n_sessions);
  */
 s32 smgr_tdox_stats_get(struct smgr_tdox_stats *stats);
 
-/**
- * @brief modify tdox timer period (debug)
- */
-void smgr_tdox_dbg_timer_set(u32 period_ms);
-
 /**
  * @brief Get tdox enable state
  * @return true if tdox is enabled
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
index 59c97c3a1018..6f26f1f97f11 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
@@ -530,6 +530,10 @@ void __smgr_dbg_tdox_stats(struct seq_file *f)
 	seq_printf(f, "| %10u |\n", tdox_stats.cand_free_list_cnt);
 	seq_printf(f, "| %-15s ", " cand_busy ");
 	seq_printf(f, "| %10u |\n", tdox_stats.cand_busy_list_cnt);
+	seq_printf(f, "| %-15s ", " update_free ");
+	seq_printf(f, "| %10u |\n", tdox_stats.sess_update_free_list_cnt);
+	seq_printf(f, "| %-15s ", " update_busy ");
+	seq_printf(f, "| %10u |\n", tdox_stats.sess_update_busy_list_cnt);
 	seq_puts(f, "|------------------------------|\n");
 }
 
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
index 29ed5b64b5fd..1cb14b29cc22 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
@@ -172,33 +172,6 @@ enum smgr_state {
 	SMGR_STATES_NUM,
 };
 
-/**
- * @brief Tdox session information
- */
-struct tdox_info {
-	u32 supp_enable:1,
-	    is_tunnel:1,
-	    is_candidate:1,
-	    low_queue:9,
-	    high_queue:9;
-};
-
-/**
- * @brief Tdox uc create command
- */
-struct tdox_record_create_cmd {
-	struct tdox_info info;
-	u16 sess_id;
-	u16 tdox_id;
-};
-
-/**
- * @brief Tdox uc remove command
- */
-struct tdox_record_remove_cmd {
-	u16 tdox_id;
-};
-
 /**
  * @brief Session manager tdox session database entry definition
  * @tdox_id tdox id
@@ -213,6 +186,18 @@ struct tdox_db_entry {
 	struct list_head tdox_node;
 } __aligned(CACHE_LINE_SIZE);
 
+/**
+ * @brief Session manager tdox update session database entry definition
+ * @tdox_id tdox id
+ * @sess_id corresponding session id
+ * @tdox_node list node for attaching the session to the tdox lists
+ */
+struct tdox_sess_update_entry {
+	u32 tdox_id;
+	u32 sess_id;
+	struct list_head tdox_node;
+} __aligned(CACHE_LINE_SIZE);
+
 /**
  * @brief Session manager session database entry definition
  * @info session info
@@ -642,11 +627,6 @@ void smgr_tdox_session_remove(struct tdox_db_entry *ent);
  */
 void smgr_tdox_enable_set(bool enable);
 
-/**
- * @brief Initiate late parameters
- */
-void smgr_tdox_late_init(void);
-
 /**
  * @brief Clean tdox resources
  */
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
index d012900c4a7c..c0e71a5c6fbd 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
@@ -20,6 +20,7 @@
 
 #include "uc.h"
 #include "classifier.h"
+#include "checker.h"
 #include "pp_session_mgr.h"
 #include "pp_session_mgr_internal.h"
 
@@ -29,6 +30,17 @@
  * @define Tdox timer interval
  */
 #define SMGR_TDOX_TIMER_INTERVAL_MS  (3000)
+
+/**
+ * @define Minimum packet length for qualification
+ */
+#define TDOX_DEFAULT_AVG_PKT_SIZE_THR 122
+
+/**
+ * @define Minimum packet length for tunnel qualification
+ */
+#define TDOX_DEFAULT_AVG_PKT_SIZE_TUNN_THR 204
+
 /**
  * @define Number of required entries is doubled due to candidates
  */
@@ -54,6 +66,15 @@
 	list_move_tail(&(_ent)->tdox_node, &(_db)->tdox_candidate_free_list)
 #define DB_TDOX_MOVE_TO_CANDIDATE_BUSY_LIST(_db, _ent)                         \
 	list_move_tail(&(_ent)->tdox_node, &(_db)->tdox_candidate_busy_list)
+#define DB_TDOX_SESS_UPDATE_FREE_LIST_GET_NODE(_db)                            \
+	list_first_entry(&(_db)->tdox_sess_update_free_list,                   \
+			 struct tdox_sess_update_entry, tdox_node)
+#define DB_TDOX_SESS_UPDATE_FREE_LIST_ADD_NODE(_db, _ent)                      \
+	list_add_tail(&(_ent)->tdox_node, &(_db)->tdox_sess_update_free_list)
+#define DB_TDOX_MOVE_TO_SESS_UPDATE_FREE_LIST(_db, _ent)                       \
+	list_move_tail(&(_ent)->tdox_node, &(_db)->tdox_sess_update_free_list)
+#define DB_TDOX_MOVE_TO_SESS_UPDATE_BUSY_LIST(_db, _ent)                       \
+	list_move_tail(&(_ent)->tdox_node, &(_db)->tdox_sess_update_busy_list)
 
 /**
  * @brief Module database
@@ -94,6 +115,12 @@ struct smgr_tdox_db {
 	struct list_head      tdox_candidate_free_list;
 	/*! turbodox busy candidate sessions list */
 	struct list_head      tdox_candidate_busy_list;
+	/*! turbodox free session update list */
+	struct list_head      tdox_sess_update_free_list;
+	/*! turbodox busy session update list */
+	struct list_head      tdox_sess_update_busy_list;
+	/*! tdox session update array */
+	struct tdox_sess_update_entry  *tdox_sess_update;
 	/*! tdox sessions array */
 	struct tdox_db_entry  *tdox_sessions;
 	/*! tdox host timer */
@@ -108,6 +135,7 @@ struct smgr_tdox_db {
 
 s32 smgr_tdox_init(struct device *dev)
 {
+	struct tdox_sess_update_entry *tdox_sess_update_ent;
 	struct tdox_db_entry *tdox_ent;
 	u32 i;
 
@@ -126,6 +154,8 @@ s32 smgr_tdox_init(struct device *dev)
 	INIT_LIST_HEAD(&db->tdox_obsolete_list);
 	INIT_LIST_HEAD(&db->tdox_candidate_free_list);
 	INIT_LIST_HEAD(&db->tdox_candidate_busy_list);
+	INIT_LIST_HEAD(&db->tdox_sess_update_free_list);
+	INIT_LIST_HEAD(&db->tdox_sess_update_busy_list);
 
 	/* allocate sessions db entries */
 	db->tdox_sessions =
@@ -139,10 +169,24 @@ s32 smgr_tdox_init(struct device *dev)
 		return -ENOMEM;
 	}
 
+	/* allocate sessions db entries */
+	db->tdox_sess_update =
+		pp_devm_kvcalloc(dev, SMGR_TDOX_DB_NUM_ENTRIES,
+				 sizeof(*db->tdox_sess_update),
+				 GFP_KERNEL);
+
+	if (!db->tdox_sess_update) {
+		pr_err("Failed to allocate %u tdox session update memory\n",
+		       SMGR_TDOX_DB_NUM_ENTRIES);
+		return -ENOMEM;
+	}
+
 	for (i = 0; i < SMGR_TDOX_DB_NUM_ENTRIES; i++) {
 		tdox_ent = &db->tdox_sessions[i];
+		tdox_sess_update_ent = &db->tdox_sess_update[i];
 		/* init sessions lists nodes */
 		INIT_LIST_HEAD(&tdox_ent->tdox_node);
+		INIT_LIST_HEAD(&tdox_sess_update_ent->tdox_node);
 		/* add the session to the free list */
 		if (i < UC_MAX_TDOX_SESSIONS) {
 			/* Free entries */
@@ -154,6 +198,8 @@ s32 smgr_tdox_init(struct device *dev)
 			DB_TDOX_CANDIDATE_FREE_LIST_ADD_NODE(db, tdox_ent);
 		}
 		tdox_ent->sess_ent = NULL;
+		DB_TDOX_SESS_UPDATE_FREE_LIST_ADD_NODE(db,
+						       tdox_sess_update_ent);
 	}
 	atomic_set(&db->stats.tdox_sess_free, UC_MAX_TDOX_SESSIONS);
 	atomic_set(&db->stats.tdox_free_candidates, UC_MAX_TDOX_SESSIONS);
@@ -208,16 +254,17 @@ s32 smgr_tdox_record_create(struct sess_db_entry *sess_ent)
 	cmd.sess_id = tdox_ent->sess_ent->info.sess_id;
 	cmd.tdox_id = tdox_ent->tdox_id;
 
-#ifndef TDOX_NO_FW
 	/* send command */
 	ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_CREATE, 0, (const void *)&cmd,
 				   sizeof(cmd), NULL, 0);
 	if (ret) {
-		pr_err("UC_CMD_TDOX_CREATE %u command failed\n", tdox_flow);
+		pr_err("UC_CMD_TDOX_CREATE %u command failed\n",
+		       tdox_ent->tdox_id);
 		return ret;
 	}
-#endif
 
+	pr_debug("Creating tdox record %u for session %u\n", cmd.tdox_id,
+		 cmd.sess_id);
 	return ret;
 }
 
@@ -270,10 +317,18 @@ static s32 tdox_set_db_entry(struct tdox_db_entry *tdox_ent,
 	u16 queue_phy;
 	s32 ret;
 
+	pr_debug("Setting tdox entry %u with queues [%u/%u]\n",
+		 tdox_ent->tdox_id, sess->args->dst_q, sess->args->dst_q_high);
+
 	/* Copy session parameters for future use */
 	tdox_ent->info.supp_enable =
 		SESS_ARGS_IS_FLAG_ON(sess, PP_SESS_FLAG_TDOX_SUPP_BIT);
-	tdox_ent->info.is_tunnel = SESS_INGRS_TUNN_BMAP(sess);
+
+	if (SESS_INGRS_TUNN_BMAP(sess))
+		tdox_ent->info.threshold = TDOX_DEFAULT_AVG_PKT_SIZE_TUNN_THR;
+	else
+		tdox_ent->info.threshold = TDOX_DEFAULT_AVG_PKT_SIZE_THR;
+
 	ret = smgr_get_queue_phy_id(sess->args->dst_q, &queue_phy);
 	if (ret)
 		return ret;
@@ -359,23 +414,49 @@ s32 smgr_tdox_sess_ent_prepare(struct sess_info *sess)
 	tdox_ent->sess_ent = sess_ent;
 	ret = tdox_set_db_entry(tdox_ent, sess);
 
+	/* Use high destination queue */
+	sess->args->dst_q = sess->args->dst_q_high;
+
 unlock:
 	spin_unlock_bh(&db->tdox_lock);
 
 	return ret;
 }
 
+static s32 tdox_session_update(u32 tdox_id, u32 sess_id)
+{
+	struct pp_hw_si si;
+	s32 ret = 0;
+
+	pr_debug("Update session %u with tdox id %u\n", sess_id, tdox_id);
+
+	/* Get session SI */
+	if (unlikely(cls_session_si_get(sess_id, &si))) {
+		pr_err("couldn't get si for session %u", sess_id);
+		return -EINVAL;
+	}
+	ret = pp_si_fld_set(&si, SI_FLD_TDOX_FLOW, tdox_id);
+	if (ret) {
+		pr_err("couldn't set si fld tdox for session %u", sess_id);
+		return ret;
+	}
+	ret = smgr_session_update(sess_id, &si);
+	if (ret) {
+		pr_err("couldn't set si fld tdox for session %u", sess_id);
+		return ret;
+	}
+
+	return ret;
+}
 /**
  * @brief Helper function to detach tdox entry from an existing session
  */
 static s32 tdox_detach(struct sess_db_entry *sess_ent, bool update_session,
 		       bool remove_record)
 {
-#ifndef TDOX_NO_FW
 	struct tdox_record_remove_cmd cmd;
-#endif
 	struct tdox_db_entry *tdox_ent;
-	struct pp_hw_si si;
+	struct tdox_sess_update_entry *tdox_sess_update_ent;
 	s32 ret = 0;
 
 	SESS_FLAG_CLR(sess_ent, SESS_FLAG_TDOX);
@@ -384,29 +465,19 @@ static s32 tdox_detach(struct sess_db_entry *sess_ent, bool update_session,
 	sess_ent->info.tdox_ent = NULL;
 	tdox_ent->sess_ent = NULL;
 
+	pr_debug("Detach tdox record %u from sess %u\n", tdox_ent->tdox_id,
+		 sess_ent->info.sess_id);
+
 	if (!update_session)
-		return 0;
+		goto remove_record;
 
-	/* Get session SI */
-	if (unlikely(cls_session_si_get(sess_ent->info.sess_id, &si))) {
-		pr_err("couldn't get si for session %u",
-		       sess_ent->info.sess_id);
-		return -EINVAL;
-	}
-	ret = pp_si_fld_set(&si, SI_FLD_TDOX_FLOW, SMGR_INVALID_TDOX_SESS_ID);
-	if (ret) {
-		pr_err("couldn't set si fld tdox for session %u",
-		       sess_ent->info.sess_id);
-		return -EINVAL;
-	}
-	ret = smgr_session_update(sess_ent->info.sess_id, &si);
-	if (ret) {
-		pr_err("couldn't set si fld tdox for session %u",
-		       sess_ent->info.sess_id);
-		return -EINVAL;
-	}
+	/* Add new session update entry to busy list */
+	tdox_sess_update_ent = DB_TDOX_SESS_UPDATE_FREE_LIST_GET_NODE(db);
+	tdox_sess_update_ent->sess_id = sess_ent->info.sess_id;
+	tdox_sess_update_ent->tdox_id = SMGR_INVALID_TDOX_SESS_ID;
+	DB_TDOX_MOVE_TO_SESS_UPDATE_BUSY_LIST(db, tdox_sess_update_ent);
 
-#ifndef TDOX_NO_FW
+remove_record:
 	if (remove_record) {
 		cmd.tdox_id = tdox_ent->tdox_id;
 		/* send command */
@@ -419,7 +490,6 @@ static s32 tdox_detach(struct sess_db_entry *sess_ent, bool update_session,
 			return ret;
 		}
 	}
-#endif
 
 	return ret;
 }
@@ -440,7 +510,7 @@ void smgr_tdox_session_remove(struct tdox_db_entry *ent)
 		/* Detach, no need to update session as it is a candidate */
 		tdox_detach(ent->sess_ent, false, false);
 		/* Update candidates db */
-		DB_TDOX_MOVE_TO_FREE_LIST(db, ent);
+		DB_TDOX_MOVE_TO_CANDIDATE_FREE_LIST(db, ent);
 		atomic_inc(&db->stats.tdox_free_candidates);
 		goto unlock;
 	} else if (SESS_IS_FLAG_OFF(ent->sess_ent, SESS_FLAG_TDOX)) {
@@ -494,33 +564,24 @@ void smgr_tdox_session_remove(struct tdox_db_entry *ent)
 static s32 tdox_attach(struct sess_db_entry *sess_ent,
 		       struct tdox_db_entry *tdox_ent)
 {
-	struct pp_hw_si si;
+	struct tdox_sess_update_entry *tdox_sess_update_ent;
 	s32 ret = 0;
 
+	pr_debug("Attach tdox record %u to sess %u\n", tdox_ent->tdox_id,
+		 sess_ent->info.sess_id);
+
 	SESS_FLAG_SET(sess_ent, SESS_FLAG_TDOX);
 	if (tdox_ent->info.supp_enable)
 		SESS_FLAG_SET(sess_ent, SESS_FLAG_TDOX_SUPP);
 
 	sess_ent->info.tdox_ent = tdox_ent;
 
-	/* Get session SI */
-	if (unlikely(cls_session_si_get(sess_ent->info.sess_id, &si))) {
-		pr_err("couldn't get si for session %u",
-		       sess_ent->info.sess_id);
-		return -EINVAL;
-	}
-	ret = pp_si_fld_set(&si, SI_FLD_TDOX_FLOW, tdox_ent->tdox_id);
-	if (ret) {
-		pr_err("couldn't set si fld tdox for session %u",
-		       sess_ent->info.sess_id);
-		return -EINVAL;
-	}
-	ret = smgr_session_update(sess_ent->info.sess_id, &si);
-	if (ret) {
-		pr_err("couldn't set si fld tdox for session %u",
-		       sess_ent->info.sess_id);
-		return -EINVAL;
-	}
+	/* Add new session update entry to busy list */
+	tdox_sess_update_ent = DB_TDOX_SESS_UPDATE_FREE_LIST_GET_NODE(db);
+	tdox_sess_update_ent->sess_id = sess_ent->info.sess_id;
+	tdox_sess_update_ent->tdox_id = tdox_ent->tdox_id;
+	DB_TDOX_MOVE_TO_SESS_UPDATE_BUSY_LIST(db, tdox_sess_update_ent);
+
 	/* Create tdox record */
 	ret = smgr_tdox_record_create(sess_ent);
 	if (ret) {
@@ -535,12 +596,10 @@ static void smgr_tdox_nonqualified_process(void)
 {
 	struct tdox_db_entry *ent, *tmp;
 	enum tdox_uc_state state;
-	u32 busy_cnt = 0, remove_busy_cnt = 0;
 	s32 ret;
 
 	/* Iterate through busy list and manage any non qualified entry */
 	list_for_each_entry_safe(ent, tmp, &db->tdox_busy_list, tdox_node) {
-		busy_cnt++;
 		ret = uc_tdox_state_get(ent->tdox_id, &state);
 		if (ret) {
 			pr_err("Failed fetching tdox id %u state\n",
@@ -549,7 +608,8 @@ static void smgr_tdox_nonqualified_process(void)
 		}
 
 		if (state == TDOX_NONQUALIFIED_UC_INACTIVE) {
-			remove_busy_cnt++;
+			pr_debug("Detach NONQUALIFIED INACT tdox %u. sess %u\n",
+				 ent->tdox_id, ent->sess_ent->info.sess_id);
 			/* Detach Tdox from session */
 			tdox_detach(ent->sess_ent, true, false);
 			/* Update Tdox db */
@@ -557,11 +617,6 @@ static void smgr_tdox_nonqualified_process(void)
 			atomic_inc(&db->stats.tdox_sess_free);
 		}
 	}
-
-	if (busy_cnt || remove_busy_cnt) {
-		pr_debug("Busy cnt: %u, removed: %u\n", busy_cnt,
-			 remove_busy_cnt);
-	}
 }
 
 static void smgr_tdox_obsolete_process(void)
@@ -636,7 +691,8 @@ static void smgr_tdox_candidate_process(void)
  */
 static void tdox_cb(struct timer_list *timer)
 {
-	u32 timer_delay_jiffies;
+	struct tdox_sess_update_entry *ent, *tmp;
+	unsigned long timer_delay_jiffies;
 
 	spin_lock_bh(&db->tdox_lock);
 
@@ -656,41 +712,30 @@ static void tdox_cb(struct timer_list *timer)
 	if (!list_empty(&db->tdox_obsolete_list))
 		smgr_tdox_obsolete_process();
 
-	/* Restart timer */
-	timer_delay_jiffies =
-		jiffies + msecs_to_jiffies(db->tdox_timer_interval);
-	mod_timer(&db->tdox_timer, timer_delay_jiffies);
-
 	spin_unlock_bh(&db->tdox_lock);
-}
-
-void smgr_tdox_dbg_timer_set(u32 period_ms)
-{
-	u32 timer_delay_jiffies;
 
-	spin_lock_bh(&db->tdox_lock);
+	/* Iterate through busy list and update sessions with correct tdox id */
+	list_for_each_entry_safe(ent, tmp, &db->tdox_sess_update_busy_list,
+				 tdox_node) {
+		tdox_session_update(ent->tdox_id, ent->sess_id);
+		DB_TDOX_MOVE_TO_SESS_UPDATE_FREE_LIST(db, ent);
+	}
 
-	del_timer(&db->tdox_timer);
-	db->tdox_timer_interval = period_ms;
+	/* Restart timer */
 	timer_delay_jiffies =
-		jiffies + msecs_to_jiffies(period_ms);
+		jiffies + msecs_to_jiffies(db->tdox_timer_interval);
 	mod_timer(&db->tdox_timer, timer_delay_jiffies);
-
-	spin_unlock_bh(&db->tdox_lock);
 }
 
-void smgr_tdox_late_init(void)
+s32 smgr_tdox_late_init(void)
 {
-	u32 timer_delay_jiffies;
+	unsigned long timer_delay_jiffies;
 
 	if (!db->enable)
-		return;
+		return -EPERM;
 
 	spin_lock_bh(&db->tdox_lock);
-
 	timer_setup(&db->tdox_timer, tdox_cb, 0);
-	timer_setup(&db->tdox_timer, tdox_cb, 0);
-
 	db->tdox_timer_interval = SMGR_TDOX_TIMER_INTERVAL_MS;
 
 	timer_delay_jiffies = jiffies +
@@ -698,6 +743,8 @@ void smgr_tdox_late_init(void)
 	mod_timer(&db->tdox_timer, timer_delay_jiffies);
 
 	spin_unlock_bh(&db->tdox_lock);
+
+	return 0;
 }
 
 s32 smgr_tdox_stats_get(struct smgr_tdox_stats *stats)
@@ -709,7 +756,7 @@ s32 smgr_tdox_stats_get(struct smgr_tdox_stats *stats)
 	/** TBD add command
 	 * ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_STATS,0, NULL, 0,
 	 *                            stats, sizeof(*stats));
-	 * if (unlikely(ret)) { 
+	 * if (unlikely(ret)) {
 	 *      pr_err("failed to get tdox uc cpu %u  counters\n", cid);
 	 *      stats->uc_rx_pkt = 0;
 	 *      stats->uc_tx_pkt = 0;
@@ -726,6 +773,10 @@ s32 smgr_tdox_stats_get(struct smgr_tdox_stats *stats)
 		tdox_list_count(&db->tdox_candidate_free_list);
 	stats->cand_busy_list_cnt =
 		tdox_list_count(&db->tdox_candidate_busy_list);
+	stats->sess_update_free_list_cnt =
+		tdox_list_count(&db->tdox_sess_update_free_list);
+	stats->sess_update_busy_list_cnt =
+		tdox_list_count(&db->tdox_sess_update_busy_list);
 	stats->tdox_sess_free = atomic_read(&db->stats.tdox_sess_free);
 	stats->tdox_free_candidates =
 		atomic_read(&db->stats.tdox_free_candidates);
@@ -735,6 +786,7 @@ s32 smgr_tdox_stats_get(struct smgr_tdox_stats *stats)
 		atomic_read(&db->stats.tdox_candidate_full_fail);
 	stats->tdox_create_args_err =
 		atomic_read(&db->stats.tdox_create_args_err);
+
 	spin_unlock_bh(&db->tdox_lock);
 
 	return 0;
diff --git a/drivers/net/ppv4/pp-dev/pp_dts.c b/drivers/net/ppv4/pp-dev/pp_dts.c
index 28f35f688c52..a4f1b256d989 100644
--- a/drivers/net/ppv4/pp-dev/pp_dts.c
+++ b/drivers/net/ppv4/pp-dev/pp_dts.c
@@ -693,6 +693,12 @@ static s32 __dts_egr_uc_cfg(struct pp_dts_cfg *dts_cfg)
 	if (unlikely(ret))
 		goto done;
 
+	/* Get checker counter base physical base */
+	ret = __dts_phys_addr_get(pdev, "chk-cnt-cache-base-addr",
+				  &ucp->chk_ctrs_cache_base_addr);
+	if (unlikely(ret))
+		goto done;
+
 	/* Get checker physical base */
 	ret = __dts_phys_addr_get(pdev, "rxdma-base-addr", &ucp->rxdma_base);
 	if (unlikely(ret))
diff --git a/drivers/net/ppv4/pp-hal/checker/checker.c b/drivers/net/ppv4/pp-hal/checker/checker.c
index 485f726a700a..9b12a7da54b7 100644
--- a/drivers/net/ppv4/pp-hal/checker/checker.c
+++ b/drivers/net/ppv4/pp-hal/checker/checker.c
@@ -86,6 +86,14 @@ dma_addr_t chk_excp_si_phys_base_get(void)
 	return pp_virt_to_phys((void *)(ulong)CHK_ALT_SI_RAM(0));
 }
 
+dma_addr_t chk_dsi_phys_base_get(void)
+{
+	if (unlikely(!db))
+		return 0;
+
+	return db->dsi_base_phys;
+}
+
 /**
  * @brief Dispatch mailbox command to the checker
  * @note MUST be called under a lock
diff --git a/drivers/net/ppv4/pp-hal/checker/checker.h b/drivers/net/ppv4/pp-hal/checker/checker.h
index ad3ba0c4bd1b..00bc3c1c9188 100644
--- a/drivers/net/ppv4/pp-hal/checker/checker.h
+++ b/drivers/net/ppv4/pp-hal/checker/checker.h
@@ -587,6 +587,12 @@ s32 chk_crawler_get(struct crawler_cfg *crawler);
  */
 dma_addr_t chk_excp_si_phys_base_get(void);
 
+/**
+ * @brief Get DSI base address
+ * @return dma_addr_t
+ */
+dma_addr_t chk_dsi_phys_base_get(void);
+
 /**
  * @brief Module init function
  * @param init_param initial parameters
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc.c b/drivers/net/ppv4/pp-hal/micro-controller/uc.c
index aa3f870092d2..a71404590d59 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc.c
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc.c
@@ -85,6 +85,11 @@ static u32 txm_cred_base;
  */
 static phys_addr_t chk_base_addr;
 
+/**
+ * @brief checker counter cache physical base address
+ */
+static phys_addr_t chk_ctrs_cache_base_addr;
+
 /**
  * @brief checker physical base address
  */
@@ -1231,47 +1236,22 @@ static s32 __writer_port_enable(u32 uc_port, u16 qos_port)
 
 static s32 __nf_tdox_set(void)
 {
-	s32 ret = 0;
-#ifndef TDOX_NO_FW
 	struct tdox_uc_config cfg;
+	s32 ret = 0;
 
-	ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_CONF_GET, SMGR_TDOX_CPU_ID, NULL,
-				   0, &cfg, sizeof(cfg));
+	ret = uc_egr_mbox_cmd_send(UC_CMD_TDOX_CONF_GET, 0, NULL, 0, &cfg,
+				   sizeof(cfg));
 	if (unlikely(ret)) {
 		pr_err("failed to get tdox config\n");
 		return ret;
 	}
-	tdox_state_arr_offset = cfg.record_state_offset;
-#else
-	u32 idx;
 
-	/* stub - zero all 512 entries */
-	tdox_state_arr_offset = 0x7E00;
+	tdox_state_arr_offset = cfg.record_state_offset;
+	pr_info("tdox_state offset: %#x\n", tdox_state_arr_offset);
 
-	for (idx = 0; idx < 128; idx += 4)
-		PP_REG_WR32(UC_SSRAM(EGRESS, tdox_state_arr_offset + idx), 0);
-#endif
 	return ret;
 }
 
-s32 uc_tdox_dbg_state_set(u16 tdox_id, enum tdox_uc_state state)
-{
-	u32 word_offset, bit_offset, states_word, offset;
-
-	if (tdox_id >= UC_MAX_TDOX_SESSIONS)
-		return -EINVAL;
-
-	word_offset = (tdox_id / 4) * 4; /* Round down */
-	offset = tdox_state_arr_offset + word_offset;
-	bit_offset = (tdox_id % 4) * BITS_PER_BYTE;
-	states_word = PP_REG_RD32(UC_SSRAM(EGRESS, offset));
-	states_word &= ~(0xFF << bit_offset);
-	states_word |= state << bit_offset;
-	PP_REG_WR32(UC_SSRAM(EGRESS, offset), states_word);
-
-	return 0;
-}
-
 s32 uc_tdox_state_get(u16 tdox_id, enum tdox_uc_state *state)
 {
 	u32 offset;
@@ -1282,9 +1262,6 @@ s32 uc_tdox_state_get(u16 tdox_id, enum tdox_uc_state *state)
 	offset = tdox_state_arr_offset + tdox_id;
 	*state = PP_REG_RD8(UC_SSRAM(EGRESS, offset));
 
-	pr_debug("Tdox record %u. state %u. state addr %#x\n", tdox_id, *state,
-		 (u32)UC_SSRAM(EGRESS, offset));
-
 	return 0;
 }
 
@@ -1430,6 +1407,8 @@ s32 __uc_egr_info_set(void)
 	info.logger_buff    = (u32)logger_buff_phys;
 	info.logger_buff_sz = logger_buff_sz;
 	info.chk_base       = UC_PP_ADDR(chk_base_addr);
+	info.chk_ctrs_cache_base = UC_PP_ADDR(chk_ctrs_cache_base_addr);
+	info.chk_dsi_ddr_base = (u32)chk_dsi_phys_base_get();
 
 	ret = uc_egr_mbox_cmd_send(UC_CMD_INIT, 0, (const void *)&info,
 				   sizeof(info), NULL, 0);
@@ -1637,6 +1616,7 @@ s32 uc_init(struct uc_init_params *init_param)
 	uc_egr_base_addr = init_param->egr.uc_base;
 	uc_ing_base_addr = init_param->ing.uc_base;
 	chk_base_addr    = init_param->egr.chk_base;
+	chk_ctrs_cache_base_addr = init_param->egr.chk_ctrs_cache_base_addr;
 	rxdma_base_addr  = init_param->egr.rxdma_base;
 
 	if (init_param->egr.cpus.max_cpu) {
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc.h b/drivers/net/ppv4/pp-hal/micro-controller/uc.h
index 08ee22f86e2a..19044af1e84b 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc.h
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc.h
@@ -11,11 +11,8 @@
 #include <linux/types.h>
 #include <linux/debugfs.h>      /* struct dentry */
 #include <linux/pp_api.h>
-
 #include "uc_common.h"
 
-#define TDOX_NO_FW
-
 /**
  * @define used as parameter to the uc API for egress uc
  *         operations
@@ -49,7 +46,8 @@ struct uc_egr_init_params {
 	struct uc_cpu_params cpus; /*! cpus information            */
 	u64    uc_base;            /*! uc base address             */
 	phys_addr_t chk_base;      /*! chk base address            */
-	phys_addr_t rxdma_base;    /*! rxdma base address          */
+	phys_addr_t chk_ctrs_cache_base_addr; /*! chk counter cache base addr */
+	phys_addr_t rxdma_base; /*! rxdma base address          */
 	phys_addr_t bm_base;       /*! bm base address             */
 	phys_addr_t qm_base;       /*! qm base address             */
 	phys_addr_t wred_base;     /*! wred base address           */
@@ -94,13 +92,6 @@ enum tdox_uc_state {
 	TDOX_NONQUALIFIED_UC_ACTIVE,
 };
 
-/**
- * @brief Config read from tdox at boot
- */
-struct tdox_uc_config {
-	u32 record_state_offset;
-};
-
 /**
  * @enum uc_mbox_set
  * @brief mailbox set, 2 sets of mailbox registers are supported
@@ -163,14 +154,6 @@ void uc_egr_mbox_buff_free(void *virt, dma_addr_t phys, size_t sz);
 s32 uc_nf_set(enum pp_nf_type nf, u16 pid, u16 qos_port, u16 tx_queue,
 	      u16 dflt_hif);
 
-/**
- * @brief This API is for testing only, host should never modify tdox state
- * @param tdox_id tdox record id
- * @param state record state
- * @return s32 0 on success, error code otherwise
- */
-s32 uc_tdox_dbg_state_set(u16 tdox_id, enum tdox_uc_state state);
-
 /**
  * @brief Read tdox state
  * @param tdox_id tdox record id
diff --git a/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h b/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h
index c227e5756466..f61735ff51ac 100644
--- a/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h
+++ b/drivers/net/ppv4/pp-hal/micro-controller/uc_common.h
@@ -13,7 +13,7 @@
  * the version here
  */
 #define EGRESS_VER_MAJOR  1
-#define EGRESS_VER_MINOR  10
+#define EGRESS_VER_MINOR  11
 
 #define INGRESS_VER_MAJOR 1
 #define INGRESS_VER_MINOR 2
@@ -127,6 +127,10 @@ struct eg_uc_init_info {
 	u32 logger_buff_sz;
 	/* checker base address */
 	u32 chk_base;
+	/* checker counters cache base */
+	u32 chk_ctrs_cache_base;
+	/* checker dsi ddr base */
+	u32 chk_dsi_ddr_base;
 };
 
 /**
@@ -247,4 +251,38 @@ struct frag_stats {
 	u32 reserved2;
 };
 
+/**
+ * @brief Tdox session information
+ */
+struct tdox_info {
+	u32 supp_enable:1,
+	    is_candidate:1,
+	    low_queue:9,
+	    high_queue:9,
+	    threshold:8;
+};
+
+/**
+ * @brief Config read from tdox at boot
+ */
+struct tdox_uc_config {
+	u32 record_state_offset;
+};
+
+/**
+ * @brief Tdox uc create command
+ */
+struct tdox_record_create_cmd {
+	struct tdox_info info;
+	u16 sess_id;
+	u16 tdox_id;
+};
+
+/**
+ * @brief Tdox uc remove command
+ */
+struct tdox_record_remove_cmd {
+	u32 tdox_id;
+};
+
 #endif /* __PP_UC_COMMON_H__ */
diff --git a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c
index 401120ae3138..dcc87c4b0aa8 100644
--- a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c
+++ b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c
@@ -814,9 +814,6 @@ static int pp_sess_post_test(void *data)
 	if (pp_port != U32_MAX)
 		pmgr_port_del(pp_port, false);
 
-	if (smgr_tdox_enable_get())
-		smgr_tdox_dbg_timer_set(3000);
-
 	kfree(created_tdox_sessions);
 
 	return PP_UT_PASS;
@@ -1394,6 +1391,9 @@ static int pp_tdox_max_session_test(void *data)
 	if (!smgr_tdox_enable_get())
 		return PP_UT_PASS;
 
+	uc_nf_set(PP_NF_TURBODOX, 0, 0, 0, 0);
+	smgr_tdox_late_init();
+
 	__sess_args_default_set(&args, &in_pkt, &eg_pkt, &nhdr);
 	args.flags = PP_SESS_FLAG_TDOX_MSK;
 
@@ -1478,40 +1478,55 @@ static int pp_tdox_candidate_test(void *data)
  */
 static int pp_tdox_nonqulaified_test(void *data)
 {
-	struct pp_sess_create_args args;
 	struct smgr_tdox_stats stats;
-	struct fv_pkt_info in_pkt; /* ingress packet */
-	struct fv_pkt_info eg_pkt; /* egress packet  */
-	struct new_hdr     nhdr;
 	s32 ret;
 	u32 i;
 
 	if (!smgr_tdox_enable_get())
 		return PP_UT_PASS;
 
-	__sess_args_default_set(&args, &in_pkt, &eg_pkt, &nhdr);
-	args.flags = PP_SESS_FLAG_TDOX_MSK;
-
 	/** 1. All active tdox sessions should be removed
 	 *  2. Candidates will take over
-	 *  3. Also candidates will be removed
-	 *     since state is still non qualified
 	 */
-
 	/* Set all records as non qualified */
 	for (i = 0; i < UC_MAX_TDOX_SESSIONS; i++) {
-		ret = uc_tdox_dbg_state_set(i, TDOX_NONQUALIFIED_UC_INACTIVE);
-		if (ret) {
-			pr_err("Failed setting tdox state\n");
-			return PP_UT_FAIL;
-		}
+		pp_session_stats_mod(created_tdox_sessions[i], PP_STATS_ADD, 15,
+				     15000);
+		pp_session_stats_mod(created_tdox_sessions[i], PP_STATS_ADD, 15,
+				     15000);
+		pp_session_stats_mod(created_tdox_sessions[i], PP_STATS_ADD, 15,
+				     15000);
 	}
 
-	/* Set tdox timer to 100 ms period */
-	smgr_tdox_dbg_timer_set(100);
+	pr_info("Wait 10 seconds...\n");
+	msleep(10000);
 
-	pr_info("Wait 5 seconds...\n");
-	msleep(5000);
+	smgr_tdox_stats_get(&stats);
+	if (stats.tdox_sess_free != 0) {
+		pr_err("Expected %u free tdox sessions. got %u\n",
+		       0, stats.tdox_sess_free);
+		return PP_UT_FAIL;
+	}
+	if (stats.tdox_free_candidates != UC_MAX_TDOX_SESSIONS) {
+		pr_err("Expected %u free candidate tdox sessions. got %u\n",
+		       UC_MAX_TDOX_SESSIONS, stats.tdox_free_candidates);
+		return PP_UT_FAIL;
+	}
+
+	/** 1. All active tdox sessions should be removed
+	 *  2. No candidates to take over
+	 */
+	for (i = UC_MAX_TDOX_SESSIONS; i < UC_MAX_TDOX_SESSIONS * 2; i++) {
+		pp_session_stats_mod(created_tdox_sessions[i], PP_STATS_ADD, 15,
+				     15000);
+		pp_session_stats_mod(created_tdox_sessions[i], PP_STATS_ADD, 15,
+				     15000);
+		pp_session_stats_mod(created_tdox_sessions[i], PP_STATS_ADD, 15,
+				     15000);
+	}
+
+	pr_info("Wait 10 seconds...\n");
+	msleep(10000);
 
 	smgr_tdox_stats_get(&stats);
 	if (stats.tdox_sess_free != UC_MAX_TDOX_SESSIONS) {
@@ -1524,6 +1539,7 @@ static int pp_tdox_nonqulaified_test(void *data)
 		       UC_MAX_TDOX_SESSIONS, stats.tdox_free_candidates);
 		return PP_UT_FAIL;
 	}
+
 	/* Delete all sessions */
 	for (i = 0; i < UC_MAX_TDOX_SESSIONS * 2; i++) {
 		ret = pp_session_delete(created_tdox_sessions[i], NULL);
@@ -1532,14 +1548,6 @@ static int pp_tdox_nonqulaified_test(void *data)
 			return PP_UT_FAIL;
 		}
 	}
-	/* Set all records state to default */
-	for (i = 0; i < UC_MAX_TDOX_SESSIONS; i++) {
-		ret = uc_tdox_dbg_state_set(i, TDOX_NEW);
-		if (ret) {
-			pr_err("Failed setting tdox state\n");
-			return PP_UT_FAIL;
-		}
-	}
 	return PP_UT_PASS;
 }
 
