From 0b1633b92a3d3d82dc2eda93ff456b04c89d1f91 Mon Sep 17 00:00:00 2001
From: ofirbitt <ofir1.bitton@intel.com>
Date: Wed, 5 Feb 2020 16:20:05 +0200
Subject: [PATCH] LMTSYS-6759: PPv4 fix SyncQ unit test

---
 .../net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c    |  6 +-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c     |  7 +-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c   |  6 +-
 .../net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h    |  2 +-
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c  | 84 +++++++++++++++++-----
 drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h  |  4 +-
 .../ppv4/pp-tools/unit-test/tests/pp_qos_tests.c   | 31 ++++++--
 7 files changed, 105 insertions(+), 35 deletions(-)

diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
index b24d75c513a2..473edf0e9e50 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_debugfs.c
@@ -1272,10 +1272,10 @@ void pp_qos_dbg_tree_remove(struct seq_file *s)
 	}
 
 	qos_pools_clean(qdev);
-	qos_pools_init(qdev,
-		       qdev->init_params.max_ports,
+	qos_pools_init(qdev, qdev->init_params.max_ports,
 		       qdev->init_params.max_queues,
-		       qdev->init_params.reserved_queues);
+		       qdev->init_params.reserved_queues,
+		       qdev->init_params.num_syncqs);
 }
 
 PP_DEFINE_DEBUGFS(destroy_tree, pp_qos_dbg_tree_remove, NULL);
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
index 48ce68097a45..a406ae0504a4 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_fw.c
@@ -2799,7 +2799,8 @@ static s32 post_process_external(struct pp_qos_dev *qdev,
 	struct queue_stats_s *qstat;
 	struct pp_qos_codel_stat *cstat;
 	struct codel_qstats_s *cstat_ptr;
-	struct pp_qos_port_stat *pstat, *pstat_ptr;
+	struct port_stats_s *pstat_ptr;
+	struct pp_qos_port_stat *pstat;
 	struct system_stats_s *sys_stats, *sys_stats_ptr;
 	struct qm_info *qminfo, *qminfo_ptr;
 	u32 stats_poll = 0;
@@ -2871,9 +2872,9 @@ static s32 post_process_external(struct pp_qos_dev *qdev,
 	case CMD_TYPE_GET_PORT_STATS:
 		pstat = (struct pp_qos_port_stat *)response;
 		pstat_ptr =
-		(struct pp_qos_port_stat *)qdev->hwconf.fw_stats_ddr_virt;
+		(struct port_stats_s *)qdev->hwconf.fw_stats_ddr_virt;
 		pstat->total_green_bytes = pstat_ptr->total_green_bytes;
-		pstat->total_yellow_bytes = pstat_ptr->total_green_bytes;
+		pstat->total_yellow_bytes = pstat_ptr->total_yellow_bytes;
 
 		pstat->debug_back_pressure_status =
 		pstat_ptr->debug_back_pressure_status;
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
index f5fd48bf99d6..074d646eee7b 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_main.c
@@ -1147,9 +1147,11 @@ static s32 check_queue_conf_validity(struct pp_qos_dev *qdev, u32 id,
 					    id);
 				return -EINVAL;
 			}
-		}
 
-		if (rlm == QOS_INVALID_RLM) {
+			/* Allocate rlm from syncq pool */
+			node->data.queue.rlm =
+				allocate_rlm(qdev, id, RLM_RANGES_SYNCQ);
+		} else if (rlm == QOS_INVALID_RLM) {
 			mapped_rlm = get_phy_from_id(qdev->rlm_mapping, id);
 			if (!QOS_PHY_VALID(mapped_rlm)) {
 				mapped_rlm = allocate_rlm(qdev, id,
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
index 3f2d2d3a3150..eab98f8a883c 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_uc_defs.h
@@ -12,7 +12,7 @@
 /* UC version */
 #define UC_VERSION_MAJOR (1)
 #define UC_VERSION_MINOR (10)
-#define UC_VERSION_BUILD (1)
+#define UC_VERSION_BUILD (2)
 
 #define QOS_MAX_PREDECESSORS       (6)
 #define QOS_AQM_CONTEXT_MAX_QUEUES (8)
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
index 4b4128a9288b..6f8c29427481 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.c
@@ -1527,6 +1527,15 @@ u16 allocate_rlm(struct pp_qos_dev *qdev, u32 id, enum rlm_range range)
 			rlm = pp_pool_get(qdev->rlms);
 		}
 
+	break;
+	case RLM_RANGES_SYNCQ:
+		if (qdev->syncq_rlms) {
+			rlm = pp_pool_get(qdev->syncq_rlms);
+		} else {
+			QOS_LOG_ERR("Cannot allocate syncq\n");
+			rlm = pp_pool_get(qdev->rlms);
+		}
+
 	break;
 	case RLM_RANGES_NORMAL:
 		rlm = pp_pool_get(qdev->rlms);
@@ -1579,7 +1588,15 @@ void release_rlm(struct pp_qos_dev *qdev, u32 rlm)
 		if (qdev->reserved_rlms) {
 			pp_pool_put(qdev->reserved_rlms, rlm);
 		} else {
-			QOS_LOG_ERR("Cannot push to reserved\n");
+			QOS_LOG_ERR("Cannot push to reserved pool\n");
+			pp_pool_put(qdev->rlms, rlm);
+		}
+	break;
+	case RLM_RANGES_SYNCQ:
+		if (qdev->syncq_rlms) {
+			pp_pool_put(qdev->syncq_rlms, rlm);
+		} else {
+			QOS_LOG_ERR("Cannot push to syncq pool\n");
 			pp_pool_put(qdev->rlms, rlm);
 		}
 	break;
@@ -1953,8 +1970,11 @@ static void link_with_parent(struct pp_qos_dev *qdev,
 }
 
 s32 qos_pools_init(struct pp_qos_dev *qdev, u32 max_ports, u32 max_queues,
-		   u32 reserved_queues)
+		   u32 reserved_queues, u32 synq_queues)
 {
+	u32 max_q_idx;
+	u32 start_q_idx;
+
 	qdev->octets = octets_init(octet_of_phy(max_ports - 1));
 	if (!qdev->octets)
 		goto fail;
@@ -1968,11 +1988,11 @@ s32 qos_pools_init(struct pp_qos_dev *qdev, u32 max_ports, u32 max_queues,
 		goto fail;
 
 	if (reserved_queues) {
-		qdev->reserved_rlms =
-			free_rlm_init(max_queues - 1, reserved_queues);
-		qdev->rlm_ranges[RLM_RANGES_RESERVED].start =
-			max_queues - reserved_queues;
-		qdev->rlm_ranges[RLM_RANGES_RESERVED].end = max_queues - 1;
+		start_q_idx = max_queues - reserved_queues;
+		max_q_idx = max_queues - 1;
+		qdev->reserved_rlms = free_rlm_init(max_q_idx, reserved_queues);
+		qdev->rlm_ranges[RLM_RANGES_RESERVED].start = start_q_idx;
+		qdev->rlm_ranges[RLM_RANGES_RESERVED].end = max_q_idx;
 
 		if (!qdev->reserved_rlms)
 			goto fail;
@@ -1982,14 +2002,27 @@ s32 qos_pools_init(struct pp_qos_dev *qdev, u32 max_ports, u32 max_queues,
 		qdev->rlm_ranges[RLM_RANGES_RESERVED].end = max_queues;
 	}
 
-	qdev->rlms = free_rlm_init(max_queues - 1 - reserved_queues,
-				   max_queues - reserved_queues);
-	qdev->rlm_ranges[RLM_RANGES_NORMAL].start = 0;
-	qdev->rlm_ranges[RLM_RANGES_NORMAL].end =
-		max_queues - 1 - reserved_queues;
+	if (synq_queues) {
+		start_q_idx = max_queues - reserved_queues - synq_queues;
+		max_q_idx = max_queues - 1 - reserved_queues;
+		qdev->syncq_rlms = free_rlm_init(max_q_idx, synq_queues);
+		qdev->rlm_ranges[RLM_RANGES_SYNCQ].start = start_q_idx;
+		qdev->rlm_ranges[RLM_RANGES_SYNCQ].end = max_q_idx;
 
-	if (!qdev->rlms)
-		goto fail;
+		if (!qdev->syncq_rlms)
+			goto fail;
+	} else {
+		qdev->syncq_rlms = NULL;
+		qdev->rlm_ranges[RLM_RANGES_SYNCQ].start = max_queues;
+		qdev->rlm_ranges[RLM_RANGES_SYNCQ].end = max_queues;
+	}
+
+	start_q_idx = 0;
+	max_q_idx = max_queues - 1 - reserved_queues - synq_queues;
+	qdev->rlms = free_rlm_init(max_q_idx,
+				   max_queues - reserved_queues - synq_queues);
+	qdev->rlm_ranges[RLM_RANGES_NORMAL].start = 0;
+	qdev->rlm_ranges[RLM_RANGES_NORMAL].end = max_q_idx;
 
 	if (!qdev->rlms)
 		goto fail;
@@ -2049,7 +2082,8 @@ s32 qos_dev_init_db(struct pp_qos_dev *qdev, u32 max_ports, u32 max_queues,
 	memset(qdev, 0, sizeof(struct pp_qos_dev));
 	qdev->init_params.max_ports = max_ports;
 
-	if (qos_pools_init(qdev, max_ports, max_queues, reserved_queues))
+	if (qos_pools_init(qdev, max_ports, max_queues, reserved_queues,
+			   num_syncqs))
 		goto fail;
 
 	qdev->syncq = syncq_init(num_syncqs);
@@ -3456,12 +3490,13 @@ s32 connect_sync_queue(struct pp_qos_dev *qdev, u32 syncq_phy, u32 dstq_phy)
 	tree_update_predecessors(qdev, sched_phy);
 	add_suspend_port(qdev, get_port(qdev->nodes, sched_phy));
 	suspend_node(qdev, syncq_phy, 0);
+	set_children_wsp_weight(qdev, new_sched_node);
 	return 0;
 }
 
 s32 disconnect_sync_queue(struct pp_qos_dev *qdev, u32 phy)
 {
-	struct qos_node *dst_node, *sched_node;
+	struct qos_node *dst_node, *sched_node, *parent;
 	u32 sched_id, sched_phy, dst_id, dst_phy = phy + 1;
 
 	QOS_LOG_DEBUG("disconnect_sync_queue: phy %u\n", phy);
@@ -3503,6 +3538,9 @@ s32 disconnect_sync_queue(struct pp_qos_dev *qdev, u32 phy)
 	map_invalidate_id(qdev->mapping, sched_id);
 	pp_pool_put(qdev->ids, sched_id);
 	add_suspend_port(qdev, get_port(qdev->nodes, sched_phy));
+	parent = get_node_from_phy(qdev->nodes,
+				   sched_node->child_prop.parent_phy);
+	update_internal_bandwidth(qdev, parent);
 
 	/* Sync queue has no parent now, suspend the queue */
 	suspend_node(qdev, phy, 1);
@@ -4108,6 +4146,7 @@ s32 qos_dbg_qm_stat_show(struct pp_qos_dev *qdev, struct seq_file *s)
 s32 qos_drv_stats_get(struct pp_qos_dev *qdev, struct qos_drv_stats *stats)
 {
 	const struct qos_node *node;
+	enum rlm_range range;
 
 	if (unlikely(ptr_is_null(qdev)))
 		return -EINVAL;
@@ -4125,11 +4164,18 @@ s32 qos_drv_stats_get(struct pp_qos_dev *qdev, struct qos_drv_stats *stats)
 		if (node_port(node)) {
 			stats->active_ports++;
 		} else if (node_queue(node)) {
-			if (RLM_RANGES_RESERVED ==
-			    rlm_get_range_type(qdev, node->data.queue.rlm))
+			range = rlm_get_range_type(qdev, node->data.queue.rlm);
+			switch (range) {
+			case RLM_RANGES_RESERVED:
 				stats->active_reserved_queues++;
-			else
+				break;
+			case RLM_RANGES_SYNCQ:
+			case RLM_RANGES_NORMAL:
 				stats->active_queues++;
+				break;
+			default:
+				QOS_LOG_ERR("Unknown queue type %u\n", range);
+			}
 		} else if (node_internal(node)) {
 			stats->active_internals++;
 		} else if (node_sched(node)) {
diff --git a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
index 95a8570804d0..b0f3081909f7 100644
--- a/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
+++ b/drivers/net/ppv4/pp-api/pp-qos-mgr/pp_qos_utils.h
@@ -402,6 +402,7 @@ struct qos_drv_stats {
 
 enum rlm_range {
 	RLM_RANGES_RESERVED,
+	RLM_RANGES_SYNCQ,
 	RLM_RANGES_NORMAL,
 	RLM_RANGES_MAX,
 };
@@ -416,6 +417,7 @@ struct pp_qos_dev {
 	struct pp_pool                 *ids;
 	struct pp_pool                 *rlms;
 	struct pp_pool                 *reserved_rlms;
+	struct pp_pool                 *syncq_rlms;
 	struct range                   rlm_ranges[RLM_RANGES_MAX];
 	struct pp_pool                 *portsphys;
 	struct pp_nodes                *nodes;
@@ -819,7 +821,7 @@ u16 pp_pool_get(struct pp_pool *pool);
 u32 pp_pool_capacity_get(struct pp_pool *pool);
 s32 pp_pool_put(struct pp_pool *pool, u16 data);
 s32 qos_pools_init(struct pp_qos_dev *qdev, u32 max_port, u32 max_queues,
-		   u32 reserved_queues);
+		   u32 reserved_queues, u32 synq_queues);
 void qos_pools_clean(struct pp_qos_dev *qdev);
 
 /* Cyclic buffer
diff --git a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c
index cd5304a0f6ba..e1d5c4d3796b 100644
--- a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c
+++ b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_qos_tests.c
@@ -28,6 +28,7 @@
 
 static void       *ring_addr_virt[QOS_TEST_NUM_RING_ADDR];
 static dma_addr_t ring_addr_phys[QOS_TEST_NUM_RING_ADDR];
+static bool       syncq_enabled;
 
 #ifdef CONFIG_PPV4_LGM
 #define QOS_TEST_MAX_PORTS 256
@@ -651,6 +652,9 @@ static struct pp_qos_dev * __must_check qos_restart(u32 instance_id)
 
 	new_conf.reserved_queues = 0;
 
+	if (!syncq_enabled)
+		new_conf.num_syncqs = 0;
+
 	ret = qos_dev_init(qdev, &new_conf, false);
 	if (ret) {
 		pr_err("qos_dev_init failed\n");
@@ -2709,11 +2713,16 @@ static int qos_syncq_test(void *data)
 	struct pp_qos_queue_conf confq;
 	unsigned int port_id, q_id, q_phy, prev_q_phy, sq_id, sq_phy;
 	static struct pp_qos_dev *qdev;
+	struct qos_node *node;
+
+	syncq_enabled = true;
 
 	qdev = qos_restart(PP_QOS_INSTANCE_ID);
 	if (!qdev)
 		return PP_UT_FAIL;
 
+	syncq_enabled = false;
+
 	if (!qdev->init_params.num_syncqs)
 		return PP_UT_PASS;
 
@@ -2774,17 +2783,18 @@ static int qos_syncq_test(void *data)
 	DUMP_QOS_TREE(qdev);
 
 	/* Get original queue conf */
-	PPUT_FAIL_RETURN(!pp_qos_queue_conf_get(qdev, q_id, &confq),
-			 "Failed to get queue %u conf\n", q_id);
+	node = get_node_from_phy(qdev->nodes,
+				 get_phy_from_id(qdev->mapping, q_id));
+
+	PPUT_FAIL_RETURN(node, "Failed to get queue node\n");
 
 	/* Get the parent phy */
-	q_phy = get_phy_from_id(qdev->mapping, confq.queue_child_prop.parent);
-	PPUT_FAIL_RETURN(QOS_PHY_VALID(q_phy),
+	PPUT_FAIL_RETURN(QOS_PHY_VALID(node->child_prop.parent_phy),
 			 "Failed to get the parent sched phy\n");
 
 	/* Compare the parent phy with the original queue phy */
-	PPUT_FAIL_RETURN(q_phy == prev_q_phy,
-		  "Orig queue %u is not connected to scheduler\n", q_id);
+	PPUT_FAIL_RETURN(node->child_prop.parent_phy == prev_q_phy,
+			 "Orig queue %u is not connected to scheduler\n", q_id);
 
 	/* disconnect the syncq */
 	PPUT_FAIL_RETURN(!qos_sync_queue_disconnect(qdev, sq_id),
@@ -2802,6 +2812,14 @@ static int qos_syncq_test(void *data)
 	PPUT_FAIL_RETURN(q_phy == prev_q_phy,
 		  "Orig queue %u wasn't return to the same phy\n", q_id);
 
+	delete_all_syncq(qdev);
+
+	PPUT_FAIL_RETURN(!pp_qos_queue_remove(qdev, q_id),
+			 "Queue remove failed\n");
+
+	PPUT_FAIL_RETURN(!pp_qos_port_remove(qdev, port_id),
+			 "Port remove failed\n");
+
 #ifdef FW_CHECK_SYNC
 	PPUT_FAIL_RETURN(!check_sync_with_fw(qdev, true),
 			 "DB not consistent with fw\n");
@@ -4659,6 +4677,7 @@ static int stat_test(void *data)
 	test_ret = PP_UT_PASS;
 	DUMP_QOS_TREE(qdev);
 done:
+
 	pp_qos_queue_block(qdev, q1);
 	qos_queue_flush(qdev, q1);
 	PPUT_FAIL_RETURN(pp_qos_queue_remove(qdev, q1) == 0,
