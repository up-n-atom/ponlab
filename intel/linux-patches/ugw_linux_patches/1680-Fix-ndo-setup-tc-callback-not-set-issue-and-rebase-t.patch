From cf54761a45d8b6c7a8e4480ddf14c4de5b9d9490 Mon Sep 17 00:00:00 2001
From: "Shao, Guohua" <gshao@maxlinear.com>
Date: Wed, 12 May 2021 12:06:01 +0800
Subject: [PATCH] Fix ndo_setup_tc callback not set issue and rebase to master

---
 drivers/net/datapath/dpm/datapath_api_fn.c          | 16 ++++++++++++++++
 drivers/net/datapath/dpm/datapath_api_others.c      | 19 -------------------
 drivers/net/datapath/mib_counters/dp_mib_counters.c |  2 +-
 include/net/datapath_api.h                          |  1 -
 include/net/datapath_api_fn.h                       | 12 +++++++++++-
 5 files changed, 28 insertions(+), 22 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath_api_fn.c b/drivers/net/datapath/dpm/datapath_api_fn.c
index 54b2f38b1e9e..189b7e6dade5 100644
--- a/drivers/net/datapath/dpm/datapath_api_fn.c
+++ b/drivers/net/datapath/dpm/datapath_api_fn.c
@@ -181,6 +181,22 @@ EXPORT_SYMBOL(dp_spl_conn_fn);
 int (*dp_register_tx_fn)(enum DP_TX_PRIORITY priority, tx_fn fn, void *priv) = NULL;
 EXPORT_SYMBOL(dp_register_tx_fn);
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(4, 14, 0)
+int (*pon_qos_setup_tc_fn)(struct net_device *dev,
+			   u32 handle,
+			   __be16 protocol,
+			   struct tc_to_netdev *tc,
+			   int port_id,
+			   int deq_idx) = NULL;
+#else
+int (*pon_qos_setup_tc_fn)(struct net_device *dev,
+			   enum tc_setup_type type,
+			   void *type_data,
+			   int port_id,
+			   int deq_idx) = NULL;
+#endif
+EXPORT_SYMBOL(pon_qos_setup_tc_fn);
+
 #define MAX_EVENTS 10
 static int num_event;
 struct event_late_init_list {
diff --git a/drivers/net/datapath/dpm/datapath_api_others.c b/drivers/net/datapath/dpm/datapath_api_others.c
index c45a58db166f..0cb43b1fa588 100644
--- a/drivers/net/datapath/dpm/datapath_api_others.c
+++ b/drivers/net/datapath/dpm/datapath_api_others.c
@@ -7,25 +7,6 @@
 
 #include <net/datapath_api.h>
 
-#if IS_ENABLED(CONFIG_PON_QOS)
-#if LINUX_VERSION_CODE < KERNEL_VERSION(4, 14, 0)
-int (*pon_qos_setup_tc_fn)(struct net_device *dev,
-			   u32 handle,
-			   __be16 protocol,
-			   struct tc_to_netdev *tc,
-			   int port_id,
-			   int deq_idx) = NULL;
-#else
-int (*pon_qos_setup_tc_fn)(struct net_device *dev,
-			   enum tc_setup_type type,
-			   void *type_data,
-			   int port_id,
-			   int deq_idx) = NULL;
-#endif
-EXPORT_SYMBOL(pon_qos_setup_tc_fn);
-#define DP_ENABLE_TC_OFFLOADS
-#endif
-
 void (*dp_dev_get_ethtool_stats_fn)(struct net_device *dev,
 				    struct ethtool_stats *stats,
 				    u64 *data) = NULL;
diff --git a/drivers/net/datapath/mib_counters/dp_mib_counters.c b/drivers/net/datapath/mib_counters/dp_mib_counters.c
index c3aaa63cc4ad..133cd6b5c298 100644
--- a/drivers/net/datapath/mib_counters/dp_mib_counters.c
+++ b/drivers/net/datapath/mib_counters/dp_mib_counters.c
@@ -226,7 +226,7 @@ static void update_dev_counters(struct mib_counters *cnt, u8 initial_update)
 	if (!tmp_counters)
 		return;
 
-	pr_info("MIB CNT - update counters for dev: %s, port_id: %d, sub_if_id_grp: %d\n",
+	pr_debug("MIB CNT - update counters for dev: %s, port_id: %d, sub_if_id_grp: %d\n",
 		 cnt->dev->name, cnt->port_id, cnt->sub_if_id_grp);
 
 	if (cnt->is_pmapper) {
diff --git a/include/net/datapath_api.h b/include/net/datapath_api.h
index d1678622419b..6eca9639e26a 100644
--- a/include/net/datapath_api.h
+++ b/include/net/datapath_api.h
@@ -3025,7 +3025,6 @@ int dp_pce_rule_update(struct dp_pce_blk_info *pce_blk_info,
  */
 int dp_pce_blk_del(struct dp_pce_blk_info *pce_blk_info);
 
-struct pmac_port_info;
 
 /*!
  *@brief dp_get_tx_cbm_pkt API, get CPU CQM TX counter via port_id and
diff --git a/include/net/datapath_api_fn.h b/include/net/datapath_api_fn.h
index afb815415764..018c119e51be 100644
--- a/include/net/datapath_api_fn.h
+++ b/include/net/datapath_api_fn.h
@@ -5,6 +5,8 @@
 #ifndef DATAPATH_API_FN_H
 #define DATAPATH_API_FN_H
 
+#include <linux/version.h>
+
 extern int (*dp_rx_enable_fn)(struct net_device *netif, char *ifname, u32 flags);
 extern int (*dp_shaper_conf_set_fn)(struct dp_shaper_conf *cfg, int flag);
 extern int (*dp_shaper_conf_get_fn)(struct dp_shaper_conf *cfg, int flag);
@@ -88,7 +90,15 @@ extern void *(*dp_get_ops_fn)(int inst, enum DP_OPS_TYPE type);
 extern int (*dp_set_mtu_size_fn)(struct net_device *dev, u32 mtu_size);
 extern int (*dp_spl_conn_fn)(int inst, struct dp_spl_cfg *conn);
 extern int (*dp_register_tx_fn)(enum DP_TX_PRIORITY priority, tx_fn fn, void *priv);
-
+#if LINUX_VERSION_CODE < KERNEL_VERSION(4, 14, 0)
+extern int32_t (*qos_mgr_hook_setup_tc)(struct net_device *dev, u32 handle,
+					__be16 protocol,
+					struct tc_to_netdev *tc);
+#else
+extern int32_t (*qos_mgr_hook_setup_tc)(struct net_device *dev,
+					enum tc_setup_type type,
+					void *type_data);
+#endif
 
 int dp_late_register_ops(void);
 int dp_late_register_event_cb(void);
