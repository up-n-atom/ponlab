From ab13a12154f0f6ca83013bbf70bbc21d972a26d6 Mon Sep 17 00:00:00 2001
From: Gu Chao <gux.chao@intel.com>
Date: Thu, 16 Apr 2020 15:08:14 +0800
Subject: [PATCH] DRV_DPM_SW-293: FLM DPM replace debugfs path string with
 macro

- update qos_create_qos_help()
---
 drivers/net/datapath/dpm/datapath.h                |  2 +
 drivers/net/datapath/dpm/datapath_proc.c           | 14 ++---
 drivers/net/datapath/dpm/datapath_proc_qos.c       | 64 +++++++++++-----------
 .../datapath/dpm/gswip30/datapath_lookup_proc.c    | 24 ++++----
 drivers/net/datapath/dpm/gswip30/datapath_proc.c   | 34 +++++++-----
 .../datapath/dpm/gswip31/datapath_lookup_proc.c    | 24 ++++----
 6 files changed, 86 insertions(+), 76 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath.h b/drivers/net/datapath/dpm/datapath.h
index fa8a5e7e3785..534e2663b671 100644
--- a/drivers/net/datapath/dpm/datapath.h
+++ b/drivers/net/datapath/dpm/datapath.h
@@ -38,6 +38,8 @@
 #include <net/datapath_inst.h>
 #include <net/datapath_api_umt.h>
 
+#define DP_DEBUGFS_PATH "/sys/kernel/debug/dp"
+
 #define MAX_SUBIFS 256  /* Max subif per DPID */
 #define MAX_DP_PORTS  16
 #define PMAC_SIZE 8
diff --git a/drivers/net/datapath/dpm/datapath_proc.c b/drivers/net/datapath/dpm/datapath_proc.c
index d84e79e99bb5..0b81fc658057 100644
--- a/drivers/net/datapath/dpm/datapath_proc.c
+++ b/drivers/net/datapath/dpm/datapath_proc.c
@@ -21,9 +21,7 @@
 #define DP_METER_CFGAPI(inst, func, dev, meter, flag, mtr_subif) \
 	dp_port_prop[(inst)].info.func((dev), &(meter), (flag), (mtr_subif))
 
-#define DP_DBGFS_NAME   "dp"
-#define DP_DBGFS_BASE   "/sys/kernel/debug/" DP_DBGFS_NAME "/"
-#define DP_DBGFS_PARENT ""
+#define DP_DBGFS_NAME  "dp"
 
 #define DBGFS_DBG      "dbg"
 #define DBGFS_PORT     "port"
@@ -36,6 +34,8 @@
 #define DBGFS_MIB_GLOBAL    "mib_global_stats"
 #define DBGFS_BR_VLAN "vlan"
 
+#define DEBUGFS_DBG DP_DEBUGFS_PATH "/" DBGFS_DBG
+
 typedef int (*print_ctp_bp_t)(struct seq_file *s, int inst,
 			      struct pmac_port_info *port,
 			      int subif_index, u32 flag);
@@ -732,7 +732,7 @@ static int set_switchdev(int inst, int ep, int vap, bool enable)
 #endif
 }
 
-#define DBG_PORT DP_DBGFS_BASE DBGFS_PORT
+#define DBG_PORT DP_DEBUGFS_PATH "/" DBGFS_PORT
 
 ssize_t proc_port_write(struct file *file, const char *buf, size_t count,
 			loff_t *ppos)
@@ -912,9 +912,7 @@ ssize_t proc_dbg_write(struct file *file, const char *buf, size_t count,
 				"%s ", dp_dbg_flag_str[i]);
 
 	num += snprintf(tmp_buf + num, BUF_SIZE_TMP - num - 1,
-			" > /proc/dp/dbg\n");
-	num += snprintf(tmp_buf + num, BUF_SIZE_TMP - num - 1,
-			" display command: cat /proc/dp/cmd\n");
+			" > %s\n", DEBUGFS_DBG);
 	pr_info("%s", tmp_buf);
 	devm_kfree(&g_dp_dev->dev, (tmp_buf));
 	return count;
@@ -947,7 +945,7 @@ EXPORT_SYMBOL(dp_proc_node);
 
 struct dentry *dp_proc_install(void)
 {
-	dp_proc_node = debugfs_create_dir(DP_DBGFS_PARENT DP_DBGFS_NAME, NULL);
+	dp_proc_node = debugfs_create_dir(DP_DBGFS_NAME, NULL);
 
 	if (dp_proc_node) {
 		int i;
diff --git a/drivers/net/datapath/dpm/datapath_proc_qos.c b/drivers/net/datapath/dpm/datapath_proc_qos.c
index baa009ce88b3..a2884379d362 100644
--- a/drivers/net/datapath/dpm/datapath_proc_qos.c
+++ b/drivers/net/datapath/dpm/datapath_proc_qos.c
@@ -15,6 +15,8 @@
 #define PROC_DP_MAX_SCH_PER_PORT ((PROC_MAX_Q_PER_PORT) * (DP_MAX_SCH_LVL))
 #define PROC_DP_MAX_LEAF 8 /* max leaf per scheduler */
 
+#define DEBUGFS_QOS DP_DEBUGFS_PATH "/qos"
+
 struct location {
 	int x1, y1; /* start axis */
 	int x2, y2; /* end axis */
@@ -909,33 +911,39 @@ int qos_dump_start(void)
 
 void qos_create_qos_help(void)
 {
-	pr_info("QOS Command Help:\n\n");
-	pr_info("     Add Queue: echo add_q <qid> <schid>:<leaf> %s%s\n\n",
-		"<schid>:<leaf> <schid>:<leaf> <schid>:<leaf> <portid> ",
-		"> /sys/kernel/debug/dp/qos");
+	pr_info("QOS Command Help:\n");
+	pr_info("\n");
+	pr_info("     ADD QUEUE: echo add_q <qid> <schid>:<leaf>... <portid> > %s\n",
+		DEBUGFS_QOS);
 	pr_info("            id: qid/schid_node/cbm_port\n");
-	pr_info("  Delete Queue: echo del_q qid > /sys/kernel/debug/dp/qos\n");
-	pr_info("  Delete Sched: echo del_sch schid > %s\n",
-		"/sys/kernel/debug/dp/qos");
-	pr_info("  SET PRIO: echo prio <id> <type> <arbi> <prio_wfq> %s\n",
-		"> /sys/kernel/debug/dp/qos");
+	pr_info("\n");
+	pr_info("  DELETE QUEUE: echo del_q <qid> > %s\n", DEBUGFS_QOS);
+	pr_info("\n");
+	pr_info("  DELETE SCHED: echo del_sch <schid> > %s\n", DEBUGFS_QOS);
+	pr_info("\n");
+	pr_info("      SET PRIO: echo prio <id> <type> <arbi> <prio_wfq> > %s\n",
+		DEBUGFS_QOS);
 	pr_info("            id: phy_queue/sched_node\n");
 	pr_info("          type: queue/sched\n");
 	pr_info("          arbi: null/sp/wsp/wrr/wrr_wsp/wfq\n");
-	pr_info("    CFG SHAPER: echo shaper <cmd> <id> <type> %s\n",
-		"<cir> <pir> <cbs> <pbs> > /sys/kernel/debug/dp/qos");
+	pr_info("\n");
+	pr_info("    CFG SHAPER: echo shaper <cmd> <id> <type> <cir> <pir> <cbs> <pbs> > %s\n",
+		DEBUGFS_QOS);
 	pr_info("           cmd: add/remove/disable\n");
 	pr_info("            id: qid/sched_node/cbm_port\n");
 	pr_info("          type: queue/sched/port\n");
 	pr_info("           cir: no_limit/max/value\n");
-	pr_info("      SET NODE: echo set_node <id> <type> <cmd>... %s\n",
-		"> /sys/kernel/debug/dp/qos");
+	pr_info("\n");
+	pr_info("      SET NODE: echo set_node <id> <type> <cmd>... > %s\n",
+		DEBUGFS_QOS);
 	pr_info("            id: phy_queue/sched_node/cbm_port\n");
 	pr_info("          type: queue/sched/port\n");
-	pr_info("           cmd: enable(unblock)/disable(block)/%s\n",
-		"resume/suspend");
+	pr_info("           cmd: enable(unblock)/disable(block)/resume/suspend\n");
 	pr_info("                enable/disable: only for queue/port\n");
 	pr_info("                resume/suspend: for all nodes\n");
+	pr_info("\n");
+	pr_info("   CLEAR STATS: echo clear > %s\n", DEBUGFS_QOS);
+	pr_info("\n");
 }
 
 static int get_arbi(struct dp_node_child *c, enum dp_arbitate *p)
@@ -1065,7 +1073,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 
 		if (num < 3 || num - 3 > DP_MAX_SCH_LVL) {
 			pr_info("Wrong Parameter(try help):echo help > %s\n",
-				"/sys/kernel/debug/dp/qos");
+				DEBUGFS_QOS);
 			return count;
 		}
 
@@ -1156,8 +1164,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 
 		if (num < 3) {
 			pr_err("id, type are required!%s%s\n",
-			       "\n(try help):echo help > ",
-			       "/sys/kernel/debug/dp/qos");
+			       "\n(try help):echo help > ", DEBUGFS_QOS);
 			return count;
 		}
 
@@ -1212,8 +1219,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 
 		if (num < 4) {
 			pr_err("cmd, id, type are required!%s%s\n",
-			       "\n(try help):echo help > ",
-			       "/sys/kernel/debug/dp/qos");
+			       "\n(try help):echo help > ", DEBUGFS_QOS);
 			return count;
 		}
 
@@ -1226,8 +1232,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 				     strlen("disable")) == 0)
 			shaper_cfg.cmd = DP_SHAPER_CMD_DISABLE;
 		else
-			pr_err("unknown cmd try: echo help %s",
-			       "> /sys/kernel/debug/dp/qos");
+			pr_err("unknown cmd try: echo help > %s", DEBUGFS_QOS);
 
 		if (dp_strncmpi(param_list[3], "queue", strlen("queue")) == 0) {
 			shaper_cfg.type = DP_NODE_QUEUE;
@@ -1273,8 +1278,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 
 		if (num < 4 || num > 5) {
 			pr_err("id, type, cmd are required!%s%s\n",
-			       "\n(try help):echo help > ",
-			       "/sys/kernel/debug/dp/qos");
+			       "\n(try help):echo help > ", DEBUGFS_QOS);
 			return count;
 		}
 
@@ -1291,8 +1295,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 			en_node.id.cqm_deq_port = dp_atoi(param_list[1]);
 		} else {
 			pr_err("Incorrect parameter!%s%s%s\n", param_list[2],
-			       "\n(try help):echo help > ",
-			       "/sys/kernel/debug/dp/qos");
+			       "\n(try help):echo help > ", DEBUGFS_QOS);
 			return count;
 		}
 
@@ -1310,8 +1313,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 			en_node.en |= DP_NODE_RESUME;
 		} else {
 			pr_err("Incorrect parameter!%s%s%s\n", param_list[3],
-			       "\n(try help):echo help > ",
-			       "/sys/kernel/debug/dp/qos");
+			       "\n(try help):echo help > ", DEBUGFS_QOS);
 			return count;
 		}
 
@@ -1332,7 +1334,7 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 				pr_err("Incorrect parameter!%s%s%s\n",
 				       param_list[4],
 				       "\n(try help):echo help > ",
-				       "/sys/kernel/debug/dp/qos");
+				       DEBUGFS_QOS);
 				return count;
 			}
 		}
@@ -1348,8 +1350,8 @@ ssize_t proc_qos_write(struct file *file, const char __user *buf,
 		qmap_set.q_id = dp_atoi(param_list[1]);
 
 		if (num < 10) {
-			pr_info("Wrong Parameter(try help):%s\n",
-				"echo help > /sys/kernel/debug/dp/qos");
+			pr_info("Wrong Parameter(try help):%s%s\n",
+				"echo help > ", DEBUGFS_QOS);
 			return count;
 		}
 		qmap_set.map.mpe1 = dp_atoi(param_list[2]);
diff --git a/drivers/net/datapath/dpm/gswip30/datapath_lookup_proc.c b/drivers/net/datapath/dpm/gswip30/datapath_lookup_proc.c
index 22b41f578776..63e9623aad0e 100644
--- a/drivers/net/datapath/dpm/gswip30/datapath_lookup_proc.c
+++ b/drivers/net/datapath/dpm/gswip30/datapath_lookup_proc.c
@@ -29,6 +29,8 @@
 #include "datapath_misc.h"
 #include <net/datapath_proc_api.h>
 
+#define DEBUGFS_LOOKUP DP_DEBUGFS_PATH "/lookup"
+
 #define SEQ_PRINTF seq_printf
 
 #define proc_printf(s, fmt, arg...) \
@@ -460,17 +462,17 @@ ssize_t proc_get_qid_via_index30(struct file *file, const char __user *buf,
 	}
 	goto help;
 help:
-	pr_info("Usage: echo set lookup_flags queue_id > /proc/dp/lookup\n");
-	pr_info("     : echo get lookup_flags > /proc/dp/lookup\n");
-	pr_info("     : echo find  <x> > /proc/dp/lookup\n");
-	pr_info("     : echo find2 <x> > /proc/dp/lookup\n");
-	pr_info("     : echo remap <old_q> <new_q> > /proc/dp/lookup\n");
-	pr_info("  Hex example: echo set 0x10 10 > /proc/dp/lookup\n");
-	pr_info("  Dec:example: echo set 16 10 > /proc/dp/lookup\n");
-	pr_info("  Bin:example: echo set b10000 10 > /proc/dp/lookup\n");
-
-	pr_info("%s: echo set b1xxxx 10 > /proc/dp/lookup\n",
-		"Special for BIN(Don't care bit)");
+	pr_info("Usage: echo set lookup_flags queue_id > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo get lookup_flags > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo find  <x> > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo find2 <x> > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo remap <old_q> <new_q> > %s\n", DEBUGFS_LOOKUP);
+	pr_info("  Hex example: echo set 0x10 10 > %s\n", DEBUGFS_LOOKUP);
+	pr_info("  Dec:example: echo set 16 10 > %s\n", DEBUGFS_LOOKUP);
+	pr_info("  Bin:example: echo set b10000 10 > %s\n", DEBUGFS_LOOKUP);
+
+	pr_info("%s: echo set b1xxxx 10 > %s\n",
+		"Special for BIN(Don't care bit)", DEBUGFS_LOOKUP);
 	pr_info("Lookup format:\n");
 	pr_info("  Bits Index: | %s\n",
 		"13   12 |  11  |  10  |  9   |  8   |7   4 | 3   0 |");
diff --git a/drivers/net/datapath/dpm/gswip30/datapath_proc.c b/drivers/net/datapath/dpm/gswip30/datapath_proc.c
index 9b27d51a034c..908aee1c94ec 100644
--- a/drivers/net/datapath/dpm/gswip30/datapath_proc.c
+++ b/drivers/net/datapath/dpm/gswip30/datapath_proc.c
@@ -25,6 +25,9 @@
 #define PROC_DPORT "dport"	/*TMU dequeue port info */
 #define DP_PROC_CBMLOOKUP "lookup"
 
+#define DEBUGFS_RMON DP_DEBUGFS_PATH "/" PROC_RMON_PORTS
+#define DEBUGFS_EP DP_DEBUGFS_PATH "/" PROC_EP
+
 #define MAX_GSW_L_PMAC_PORT  7
 #define MAX_GSW_R_PMAC_PORT  16
 static GSW_RMON_Port_cnt_t gsw_l_rmon_mib[MAX_GSW_L_PMAC_PORT];
@@ -994,11 +997,11 @@ static ssize_t proc_gsw_rmon_write(struct file *file, const char *buf,
 	return count;
 
 help:
-	pr_info("usage: echo clear > /proc/dp/rmon\n");
-	pr_info("usage: echo TMU on > /proc/dp/rmon\n");
-	pr_info("usage: echo TMU off > /proc/dp/rmon\n");
-	pr_info("usage: echo RMON Full > /proc/dp/rmon\n");
-	pr_info("usage: echo RMON Basic > /proc/dp/rmon\n");
+	pr_info("usage: echo clear > %s\n", DEBUGFS_RMON);
+	pr_info("usage: echo TMU on > %s\n", DEBUGFS_RMON);
+	pr_info("usage: echo TMU off > %s\n", DEBUGFS_RMON);
+	pr_info("usage: echo RMON Full > %s\n", DEBUGFS_RMON);
+	pr_info("usage: echo RMON Basic > %s\n", DEBUGFS_RMON);
 	return count;
 }
 
@@ -1165,18 +1168,19 @@ ssize_t ep_port_write(struct file *file, const char *buf, size_t count,
 
 	return count;
 help:
-	pr_info("echo ingress/egress [ep_port] %s > /proc/dp/ep\n",
-		"['ingress/egress fields'] [value]");
-	pr_info("(eg) echo ingress 1 pmac 1 > /proc/dp/ep\n");
-	pr_info("(eg) echo egress 1 rm_l2hdr 2 > /proc/dp/ep\n");
+	pr_info("echo ingress/egress [ep_port] %s > %s\n",
+		"['ingress/egress fields'] [value]", DEBUGFS_EP);
+	pr_info("(eg) echo ingress 1 pmac 1 > %s\n", DEBUGFS_EP);
+	pr_info("(eg) echo egress 1 rm_l2hdr 2 > %s\n", DEBUGFS_EP);
 	pr_info("echo %s ['errdisc/pmac/pmac_pmap/pmac_en_pmap/pmac_tc",
 		"ingress [ep_port] ");
-	pr_info("                         %s > /proc/dp/ep\n",
-		"/pmac_en_tc/pmac_subifid/pmac_srcport'] [value]");
-	pr_info("echo  %s %s> /proc/dp/ep\n",
-		"egress [ep_port]",
-		"['rx_dmachan/fcs/pmac/res_dw1/res1_dw0/res2_dw0] [value]");
-	pr_info("echo egress [ep_port] ['rm_l2hdr'] [value] > /proc/dp/ep\n");
+	pr_info("                         %s > %s\n",
+		"/pmac_en_tc/pmac_subifid/pmac_srcport'] [value]", DEBUGFS_EP);
+	pr_info("echo  %s %s > %s\n", "egress [ep_port]",
+		"['rx_dmachan/fcs/pmac/res_dw1/res1_dw0/res2_dw0] [value]",
+		DEBUGFS_EP);
+	pr_info("echo egress [ep_port] ['rm_l2hdr'] [value] > %s\n",
+		DEBUGFS_EP);
 	return count;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c b/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c
index e912f10d30c0..3a873796a30c 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_lookup_proc.c
@@ -34,6 +34,8 @@
 #include "datapath_misc.h"
 #include <net/datapath_proc_api.h>
 
+#define DEBUGFS_LOOKUP DP_DEBUGFS_PATH "/lookup"
+
 #define SEQ_PRINTF seq_printf
 
 #define proc_printf(s, fmt, arg...) \
@@ -478,17 +480,17 @@ ssize_t proc_get_qid_via_index31(struct file *file, const char __user *buf,
 
 	goto help;
 help:
-	pr_info("Usage: echo set lookup_flags queue_id > /proc/dp/lookup\n");
-	pr_info("     : echo get lookup_flags > /proc/dp/lookup\n");
-	pr_info("     : echo find  <x> > /proc/dp/lookup\n");
-	pr_info("     : echo find2 <x> > /proc/dp/lookup\n");
-	pr_info("     : echo remap <old_q> <new_q> > /proc/dp/lookup\n");
-	pr_info("  Hex example: echo set 0x10 10 > /proc/dp/lookup\n");
-	pr_info("  Dec:example: echo set 16 10 > /proc/dp/lookup\n");
-	pr_info("  Bin:example: echo set b10000 10 > /proc/dp/lookup\n");
-
-	pr_info("%s: echo set b1xxxx 10 > /proc/dp/lookup\n",
-		"Special for BIN(Don't care bit)");
+	pr_info("Usage: echo set lookup_flags queue_id > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo get lookup_flags > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo find  <x> > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo find2 <x> > %s\n", DEBUGFS_LOOKUP);
+	pr_info("     : echo remap <old_q> <new_q> > %s\n", DEBUGFS_LOOKUP);
+	pr_info("  Hex example: echo set 0x10 10 > %s\n", DEBUGFS_LOOKUP);
+	pr_info("  Dec:example: echo set 16 10 > %s\n", DEBUGFS_LOOKUP);
+	pr_info("  Bin:example: echo set b10000 10 > %s\n", DEBUGFS_LOOKUP);
+
+	pr_info("%s: echo set b1xxxx 10 > %s\n",
+		"Special for BIN(Don't care bit)", DEBUGFS_LOOKUP);
 	pr_info("Lookup format:\n");
 	pr_info("  Bits Index: | %s\n",
 		"13   12 |  11  |  10  |  9   |  8   |7   4 | 3   0 |");
