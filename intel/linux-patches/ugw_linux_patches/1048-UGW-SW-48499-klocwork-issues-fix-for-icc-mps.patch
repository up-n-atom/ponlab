From ec91cb67338b6d079f894ced7c8a144985c1fe58 Mon Sep 17 00:00:00 2001
From: "Vishnu Swaroop Kumar Sarma, Duddu" <duddu.swaroop@intel.com>
Date: Fri, 28 Feb 2020 23:55:08 +0530
Subject: [PATCH] UGW_SW-48499 klocwork issues fix for icc/mps

---
 drivers/char/ltq_icc/drv_icc.c        | 28 +++++++++-----------------
 drivers/char/ltq_mps2/drv_mps_linux.c | 37 +++++++++++++++--------------------
 2 files changed, 25 insertions(+), 40 deletions(-)

diff --git a/drivers/char/ltq_icc/drv_icc.c b/drivers/char/ltq_icc/drv_icc.c
index 9825d5c30540..c34e9273a1c1 100644
--- a/drivers/char/ltq_icc/drv_icc.c
+++ b/drivers/char/ltq_icc/drv_icc.c
@@ -464,7 +464,7 @@ int icc_read_d(struct file *file_p, char *buf, size_t count, loff_t *ppos)
 	memset(&icc_msg,0,sizeof(icc_msg_t));
 	/*Convert data from MPS to ICC*/
 		mps_icc_fill(pMpsmsg,&icc_msg);
-	if (0<copy_to_user (buf,&icc_msg,sizeof(icc_msg_t)))
+	if (copy_to_user(buf, &icc_msg, sizeof(icc_msg_t)) < 0)
   {
 		TRACE(ICC, DBG_LEVEL_HIGH, ("[%s %s %d]: copy_to_user error\r\n",__FILE__, __func__, __LINE__));
 		return -EAGAIN;
@@ -737,7 +737,7 @@ int icc_write_d(struct file *file_p, char *buf, size_t count, loff_t *ppos)
 	int ret , i , num;
 	memset(&Mpsmsg,0,sizeof(mps_message));
 	memset(&icc_msg,0,sizeof(icc_msg_t));
-	if (0<copy_from_user (&icc_msg,buf,sizeof(icc_msg_t)))
+	if (copy_from_user(&icc_msg, buf, sizeof(icc_msg_t)) < 0)
   {
 		TRACE(ICC, DBG_LEVEL_HIGH, ("[%s %s %d]: copy_from_user error\r\n",__FILE__, __func__, __LINE__));
 		return -EAGAIN;
@@ -858,7 +858,6 @@ static void icc_os_unregister(void)
  * \param   file_p  Pointer to file descriptor
  * \return  0       device opened
  * \return  EMFILE  Device already open
- * \return  EINVAL  Invalid minor ID
  * \ingroup API
  */
 int icc_open (struct inode * inode, struct file * file_p)
@@ -878,22 +877,13 @@ int icc_open (struct inode * inode, struct file * file_p)
    {
       return 0;        /* the real device */
    }
-	/*check for validity of client Id*/
-	 if(num>=0 && num < (MAX_CLIENT)){
-			/*check if its already opened*/
-		 if(iccdev[num].Installed==1){
-     		TRACE(ICC, DBG_LEVEL_HIGH, (" Device %d is already open!\n", num));
-				return -EMFILE;
-		 }
-		 /*mark that the device is opened in global structure
-			to avoid further open of the device*/
-		 iccdev[num].Installed=1;
-		 /* Initialize a wait_queue list for the system poll/wait_event function. */
-  	 init_waitqueue_head(&iccdev[num].wakeuplist);
-	 }else{
-			TRACE(ICC, DBG_LEVEL_HIGH, ("Max device number exceeded\n"));
-			return -EINVAL;
-	 }
+	if (iccdev[num].Installed == 1){
+		TRACE(ICC, DBG_LEVEL_HIGH,
+			(" Device %d is already open!\n", num));
+			return -EMFILE;
+	}
+	iccdev[num].Installed = 1;
+	init_waitqueue_head(&iccdev[num].wakeuplist);
 	return 0;
 }
 
diff --git a/drivers/char/ltq_mps2/drv_mps_linux.c b/drivers/char/ltq_mps2/drv_mps_linux.c
index ce9742acf7de..8e2e2baa0bda 100644
--- a/drivers/char/ltq_mps2/drv_mps_linux.c
+++ b/drivers/char/ltq_mps2/drv_mps_linux.c
@@ -445,9 +445,8 @@ long mps_ioctl (struct file *file_p,
 
             /* Initialize destination and copy mps_message from usermode */
             memset (&rw_struct, 0, sizeof (mps_message));
-            if (0<
-                copy_from_user (&rw_struct, (void *) arg,
-                                   sizeof (mps_message)))
+            if (copy_from_user(&rw_struct, (void *)arg,
+                                   sizeof(mps_message)) < 0)
             {
                TRACE (MPS2, DBG_LEVEL_HIGH,
                       (KERN_ERR "[%s %s %d]: copy_from_user error\r\n",
@@ -465,9 +464,9 @@ long mps_ioctl (struct file *file_p,
 
             /* Copy data to usermode buffer... */
 						if(CHECK_PTR(rw_struct.header.Hd.param_attr,0)){
-            	if (0<
-              	  copy_to_user ((uint8_t *)UserBuf[0], (uint8_t *)rw_struct.data[0],
-                                 rw_struct.data[1]))
+		if (copy_to_user((uint8_t *)UserBuf[0],
+				(uint8_t *)rw_struct.data[0],
+                                 rw_struct.data[1]) < 0)
             	{
                	TRACE (MPS2, DBG_LEVEL_HIGH,
                 	      (KERN_ERR "[%s %s %d]: copy_to_user error\r\n", __FILE__,
@@ -479,9 +478,8 @@ long mps_ioctl (struct file *file_p,
             /* ... and finally restore the buffer pointer and copy mps_message
                back! */
             memcpy(rw_struct.data,UserBuf,16);
-            if (0<
-                copy_to_user ((void *) arg, &rw_struct,
-                                 sizeof (mps_message)))
+            if (copy_to_user((void *) arg, &rw_struct,
+                                 sizeof(mps_message)) < 0)
             {
                TRACE (MPS2, DBG_LEVEL_HIGH,
                       (KERN_ERR "[%s %s %d]: copy_to_user error\r\n", __FILE__,
@@ -498,9 +496,8 @@ long mps_ioctl (struct file *file_p,
          else
          {
             uint32_t UserBuf[16]={0};
-            if (0<
-                copy_from_user (&rw_struct, (void *) arg,
-                                   sizeof (mps_message)))
+            if (copy_from_user(&rw_struct, (void *)arg,
+                                   sizeof(mps_message)) < 0)
             {
                TRACE (MPS2, DBG_LEVEL_HIGH,
                       (KERN_ERR "[%s %s %d]: copy_from_user error\r\n",
@@ -526,9 +523,9 @@ long mps_ioctl (struct file *file_p,
             }
 
             /* copy data to kernelmode buffer and write to mailbox FIFO */
-            if (0<
-                copy_from_user ((uint8_t *)rw_struct.data[0], (uint8_t *)UserBuf[0],
-                                   rw_struct.data[1]))
+            if (copy_from_user((uint8_t *)rw_struct.data[0],
+				(uint8_t *)UserBuf[0],
+                                rw_struct.data[1]) < 0)
             {
                TRACE (MPS2, DBG_LEVEL_HIGH,
                       (KERN_ERR "[%s %s %d]: copy_from_user error\r\n",
@@ -541,9 +538,8 @@ long mps_ioctl (struct file *file_p,
             /* ... and finally restore the buffer pointer and copy mps_message
                back! */
             memcpy(rw_struct.data,UserBuf,16);
-            if (0<
-                copy_to_user ((void *) arg, &rw_struct,
-                                 sizeof (mps_message)))
+            if (copy_to_user((void *)arg, &rw_struct,
+                                 sizeof(mps_message)) < 0)
             {
                TRACE (MPS2, DBG_LEVEL_HIGH,
                       (KERN_ERR "[%s %s %d]: copy_to_user error\r\n", __FILE__,
@@ -559,9 +555,8 @@ long mps_ioctl (struct file *file_p,
          }
          else
          {
-            if (0<
-                copy_to_user ((void *) arg, mps_device_version,
-                                 strlen (mps_device_version)))
+            if (copy_to_user((void *) arg, mps_device_version,
+                                 strlen(mps_device_version)) < 0)
             {
                TRACE (MPS2, DBG_LEVEL_HIGH,
                       (KERN_ERR "[%s %s %d]: copy_to_user error\r\n", __FILE__,
