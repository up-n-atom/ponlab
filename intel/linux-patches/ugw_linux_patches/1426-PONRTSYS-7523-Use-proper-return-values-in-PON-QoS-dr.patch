From 23f69e01c3f3da87b2f8e57938d955f89d80f7fa Mon Sep 17 00:00:00 2001
From: "Kiedrzyn, PatrykX" <patrykx.kiedrzyn@intel.com>
Date: Mon, 28 Sep 2020 11:05:11 +0200
Subject: [PATCH] PONRTSYS-7523: Use proper return values in PON QoS driver

---
 drivers/net/datapath/pon_qos/pon_qos_tc_colmark.c  |   2 +-
 drivers/net/datapath/pon_qos/pon_qos_tc_debugfs.c  |   4 +-
 drivers/net/datapath/pon_qos/pon_qos_tc_drr.c      |  36 +++----
 drivers/net/datapath/pon_qos/pon_qos_tc_ext_vlan.c |  18 ++--
 drivers/net/datapath/pon_qos/pon_qos_tc_lct.c      |  36 +++----
 drivers/net/datapath/pon_qos/pon_qos_tc_mirred.c   |   8 +-
 drivers/net/datapath/pon_qos/pon_qos_tc_mqprio.c   |  20 ++--
 drivers/net/datapath/pon_qos/pon_qos_tc_pce.c      |  40 ++++----
 drivers/net/datapath/pon_qos/pon_qos_tc_prio.c     |  23 +++--
 drivers/net/datapath/pon_qos/pon_qos_tc_qmap.c     |   2 +-
 drivers/net/datapath/pon_qos/pon_qos_tc_qos.c      | 112 ++++++++++-----------
 drivers/net/datapath/pon_qos/pon_qos_tc_red.c      |  26 ++---
 drivers/net/datapath/pon_qos/pon_qos_tc_tbf.c      |  24 ++---
 drivers/net/datapath/pon_qos/pon_qos_tc_trap.c     |   4 +-
 14 files changed, 179 insertions(+), 176 deletions(-)

diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_colmark.c b/drivers/net/datapath/pon_qos/pon_qos_tc_colmark.c
index 751b883ea33b..49bbb2e38914 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_colmark.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_colmark.c
@@ -50,7 +50,7 @@ int pon_qos_colmark_offload(struct net_device *dev,
 	ret = dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0);
 	if (ret != DP_SUCCESS) {
 		netdev_err(dev, "colmark: can not get subif\n");
-		return ret;
+		return -ENODEV;
 	}
 
 	meter_cfg = kzalloc(sizeof(*meter_cfg), GFP_KERNEL);
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_debugfs.c b/drivers/net/datapath/pon_qos/pon_qos_tc_debugfs.c
index 7adda0df7c8d..71c470935551 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_debugfs.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_debugfs.c
@@ -47,7 +47,7 @@ int pon_qos_tc_debugfs_init(void)
 
 	dir = debugfs_create_dir(DEBUGFS_DIR, 0);
 	if (!dir)
-		return -1;
+		return -ENODEV;
 
 	for (i = 0; i < ARRAY_SIZE(entries); ++i) {
 		struct pon_qos_debugfs_entry *entry = &entries[i];
@@ -63,7 +63,7 @@ int pon_qos_tc_debugfs_init(void)
 
 cleanup:
 	debugfs_remove_recursive(dir);
-	return -1;
+	return -ENOENT;
 }
 
 void pon_qos_tc_debugfs_exit(void)
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_drr.c b/drivers/net/datapath/pon_qos/pon_qos_tc_drr.c
index ec3cab3cc6b9..067c53232726 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_drr.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_drr.c
@@ -39,7 +39,7 @@ static int pon_qos_drr_add_child_qdisc(struct net_device *dev,
 	enum pon_qos_qdisc_type type = PON_QOS_QDISC_DRR;
 
 	if (!dev || !port || !p)
-		return -1;
+		return -EINVAL;
 
 	return pon_qos_add_child_qdisc(dev, port, type, p->parent, p->handle);
 }
@@ -51,7 +51,7 @@ static int pon_qos_drr_sched_update(struct net_device *dev,
 	int ret;
 
 	if (!dev || !port || !parms)
-		return -1;
+		return -EINVAL;
 
 	if (port->root_qdisc.use_cnt && parms->parent == TC_H_ROOT)
 		return 0;
@@ -59,7 +59,7 @@ static int pon_qos_drr_sched_update(struct net_device *dev,
 	ret = pon_qos_drr_add_child_qdisc(dev, port, parms);
 	if (ret < 0) {
 		netdev_err(dev, "drr child qdisc create failed\n");
-		return -1;
+		return ret;
 	}
 
 	port->sch_num++;
@@ -77,17 +77,17 @@ static int pon_qos_drr_queue_update(struct net_device *dev,
 
 	if (idx < 0 || idx > PON_QOS_TC_MAX_Q - 1) {
 		netdev_err(dev, "invalid child minor -> should be in [1-8]\n");
-		return -1;
+		return -EINVAL;
 	}
 
 	if (!sch->use_cnt || TC_H_MAJ(handle) != sch->handle) {
 		netdev_err(dev, "invalid sched configuration\n");
-		return -1;
+		return -EINVAL;
 	}
 
 	if (!parms->set_params.quantum) {
 		netdev_err(dev, "invalid quantum\n");
-		return -1;
+		return -EINVAL;
 	}
 
 	quantum = parms->set_params.quantum;
@@ -125,7 +125,7 @@ static int pon_qos_tc_drr_replace(struct net_device *dev,
 		port = pon_qos_port_alloc(dev);
 		if (!port) {
 			netdev_err(dev, "tc-drr port alloc failed\n");
-			return -1;
+			return -ENOMEM;
 		}
 		newp = true;
 	}
@@ -155,12 +155,12 @@ static int pon_qos_tc_drr_replace(struct net_device *dev,
 		qdisc = pon_qos_qdisc_find(port, handle);
 		if (!qdisc) {
 			netdev_err(dev, "%s: radix tree broken\n", __func__);
-			return -1;
+			return -ENODEV;
 		}
 		ret = pon_qos_drr_queue_update(dev, qdisc, opt);
 		if (ret < 0) {
 			netdev_err(dev, "%s: drr queue config err\n", __func__);
-			return -1;
+			return ret;
 		}
 	}
 
@@ -169,7 +169,7 @@ static int pon_qos_tc_drr_replace(struct net_device *dev,
 err_free_port:
 	if (newp)
 		pon_qos_port_delete(port);
-	return -1;
+	return ret;
 }
 
 static int pon_qos_tc_drr_queue_del(struct pon_qos_qdisc *sch, u32 handle)
@@ -182,7 +182,7 @@ static int pon_qos_tc_drr_queue_del(struct pon_qos_qdisc *sch, u32 handle)
 		return 0;
 
 	if (idx < 0 && idx >= PON_QOS_TC_MAX_Q)
-		return -1;
+		return -EINVAL;
 
 	/* check if this is a queue */
 	if (sch->qids[idx].qid) {
@@ -191,7 +191,7 @@ static int pon_qos_tc_drr_queue_del(struct pon_qos_qdisc *sch, u32 handle)
 		ret = pon_qos_queue_del(sch, idx);
 		if (ret < 0) {
 			netdev_err(sch->dev, "%s: queue del err\n", __func__);
-			return -1;
+			return ret;
 		}
 		return 1;
 	}
@@ -205,7 +205,7 @@ static int pon_qos_tc_drr_qdisc_del(struct pon_qos_port *port,
 	int ret;
 
 	if (!port || !sch)
-		return -1;
+		return -EINVAL;
 
 	netdev_dbg(sch->dev, "%s:free sch:%d parent:%#x %#x children:%d/%d\n",
 		   __func__,
@@ -223,7 +223,7 @@ static int pon_qos_tc_drr_qdisc_del(struct pon_qos_port *port,
 		if (ret < 0) {
 			netdev_err(sch->dev, "%s: sch:%d del failed\n",
 				   __func__, sch->sch_id);
-			return -1;
+			return ret;
 		}
 		port->sch_num--;
 		if (sch->parent != TC_H_ROOT && TC_H_MIN(sch->parent)) {
@@ -265,7 +265,7 @@ static int pon_qos_tc_drr_destroy(struct net_device *dev,
 
 	ret = pon_qos_tc_drr_queue_del(sch, handle);
 	if (ret < 0)
-		return -1;
+		return ret;
 
 	/* this is a queue no need to do anything further */
 	if (ret == 1)
@@ -277,7 +277,7 @@ static int pon_qos_tc_drr_destroy(struct net_device *dev,
 	if (ret < 0) {
 		netdev_err(dev, "%s: %#x id: %d failed\n", __func__,
 			   sch->handle, sch->sch_id);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -302,7 +302,7 @@ int pon_qos_tc_drr_offload(struct net_device *dev,
 					     port_id, deq_idx);
 		if (err < 0) {
 			netdev_err(dev, "tc-drr replace failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_DRR_DESTROY:
@@ -312,7 +312,7 @@ int pon_qos_tc_drr_offload(struct net_device *dev,
 		err = pon_qos_tc_drr_destroy(dev, handle, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-drr destroy failed\n");
-			return -EINVAL;
+			return err;
 		}
 
 		break;
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_ext_vlan.c b/drivers/net/datapath/pon_qos/pon_qos_tc_ext_vlan.c
index 16bbf29afcc6..daa6f80c2c5e 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_ext_vlan.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_ext_vlan.c
@@ -124,32 +124,32 @@ static int merge(int old_def,
 	int i;
 
 	if (old_def != new_def)
-		return -1;
+		return -EINVAL;
 
 	if (old_outer && new_outer)
 		if (!dp_pattern_vlan_eq(old_outer, new_outer))
-			return -1;
+			return -EINVAL;
 
 	if (old_inner && new_inner)
 		if (!dp_pattern_vlan_eq(old_inner, new_inner))
-			return -1;
+			return -EINVAL;
 
 	if (old->act != new->act)
-		return -1;
+		return -EINVAL;
 
 	if (old->push_n != new->push_n)
-		return -1;
+		return -EINVAL;
 
 	if (old->pop_n != new->pop_n)
-		return -1;
+		return -EINVAL;
 
 	for (i = 0; i < old->push_n; i++) {
 		if (old->prio[i] != new->prio[i])
-			return -1;
+			return -EINVAL;
 		if (old->tpid[i] != new->tpid[i])
-			return -1;
+			return -EINVAL;
 		if (old->vid[i] != new->vid[i])
-			return -1;
+			return -EINVAL;
 	}
 
 	if (is_dscp(new) && is_dscp(old)) {
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_lct.c b/drivers/net/datapath/pon_qos/pon_qos_tc_lct.c
index 87d068a470b3..4c22142fede4 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_lct.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_lct.c
@@ -75,7 +75,7 @@ static int pce_rule_lct_mac_to_cpu(struct net_device *dev,
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleWrite [LCT MAC to CPU] for interface \"%s\" failed: %d\n",
 		       __func__, dev->name, ret);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -113,7 +113,7 @@ static int pce_rule_lct_mc_bc_to_cpu(struct net_device *dev,
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleWrite [LCT Multicast Broadcast to CPU] for interface \"%s\" failed: %d\n",
 		       __func__, dev->name, ret);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -148,7 +148,7 @@ static int pce_rule_lct_arp_bc_to_cpu(struct net_device *dev,
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleWrite [LCT ARP Broadcast to CPU] for interface \"%s\" failed: %d\n",
 		       __func__, dev->name, ret);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -181,7 +181,7 @@ static int pce_rule_lct_ptp_to_cpu(struct net_device *dev,
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleWrite [PTP to CPU] for interface \"%s\" failed: %d\n",
 		       __func__, dev->name, ret);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -218,22 +218,22 @@ static int pce_rules_lct_to_cpu(struct net_device *dev,
 
 	gsw_handle = gsw_get_swcore_ops(0);
 	if (!gsw_handle)
-		return -1;
+		return -EINVAL;
 
 	ret = dp_get_netif_subifid(dev, NULL, NULL, NULL, &dp_subif, 0);
 	if (ret != DP_SUCCESS)
-		return -1;
+		return ENODEV;
 
 	ret = pce_rule_lct_mac_to_cpu(dev, gsw_handle, dp_subif,
 				      pon_qos_lct_db[lct_idx].mac_rule_idx);
 	if (ret != 0)
-		return -1;
+		return ret;
 
 	ret = pce_rule_lct_mc_bc_to_cpu(dev, gsw_handle, dp_subif,
 					pon_qos_lct_db[lct_idx].mc_rule_idx);
 	if (ret != 0) {
 		pce_rule_remove(pon_qos_lct_db[lct_idx].mac_rule_idx);
-		return -1;
+		return ret;
 	}
 
 	ret = pce_rule_lct_arp_bc_to_cpu(dev, gsw_handle, dp_subif,
@@ -241,7 +241,7 @@ static int pce_rules_lct_to_cpu(struct net_device *dev,
 	if (ret != 0) {
 		pce_rule_remove(pon_qos_lct_db[lct_idx].mac_rule_idx);
 		pce_rule_remove(pon_qos_lct_db[lct_idx].mc_rule_idx);
-		return -1;
+		return ret;
 	}
 
 	ret = pce_rule_lct_ptp_to_cpu(dev, gsw_handle, dp_subif,
@@ -250,7 +250,7 @@ static int pce_rules_lct_to_cpu(struct net_device *dev,
 		pce_rule_remove(pon_qos_lct_db[lct_idx].mac_rule_idx);
 		pce_rule_remove(pon_qos_lct_db[lct_idx].mc_rule_idx);
 		pce_rule_remove(pon_qos_lct_db[lct_idx].arp_rule_idx);
-		return -1;
+		return ret;
 	}
 
 	pon_qos_lct_db[lct_idx].configured = PON_QOS_PCE_CONFIGURED;
@@ -268,11 +268,11 @@ static int cpu_port_disconnect(struct net_device *dev)
 
 	gsw_handle = gsw_get_swcore_ops(0);
 	if (!gsw_handle)
-		return -1;
+		return -EINVAL;
 
 	err = dp_get_netif_subifid(dev, NULL, NULL, NULL, &dp_subif, 0);
 	if (err != GSW_statusOk)
-		return -1;
+		return -ENODEV;
 
 	bp.nBridgePortId = dp_subif.bport;
 	bp.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
@@ -281,7 +281,7 @@ static int cpu_port_disconnect(struct net_device *dev)
 	if (ret != GSW_statusOk) {
 		pr_err("%s: GSW_BRIDGE_PORT_CONFIG_GET failed: %d\n", __func__,
 		       ret);
-		return -1;
+		return ret;
 	}
 
 	bp.nBridgePortMap[0] = 0;
@@ -290,7 +290,7 @@ static int cpu_port_disconnect(struct net_device *dev)
 	if (ret != GSW_statusOk) {
 		pr_err("%s: GSW_BRIDGE_PORT_CONFIG_SET failed: %d\n", __func__,
 		       ret);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -304,7 +304,7 @@ int pon_qos_lct_configure(struct net_device *dev,
 	s32 ret, i;
 
 	if (dp_vlan->def_apply != DP_VLAN_APPLY_CTP)
-		return -1;
+		return -EINVAL;
 
 	for (i = 0; i < PON_QOS_LCT_MAX; i++) {
 		if (!strncmp(pon_qos_lct_db[i].ifname, dev->name,
@@ -313,11 +313,11 @@ int pon_qos_lct_configure(struct net_device *dev,
 	}
 
 	if (!(i < PON_QOS_LCT_MAX))
-		return -1;
+		return -EINVAL;
 
 	lct_dev = dev_get_by_name(&init_net, pon_qos_lct_db[i].lctname);
 	if (!lct_dev)
-		return -1;
+		return -ENODEV;
 
 	if (pon_qos_lct_db[i].cpu_port_mapped) {
 		ret = cpu_port_disconnect(lct_dev);
@@ -328,7 +328,7 @@ int pon_qos_lct_configure(struct net_device *dev,
 
 	ret = pce_rules_lct_to_cpu(lct_dev, i);
 	if (ret != 0)
-		return -1;
+		return ret;
 
 	rule->act.ract.act = DP_BP_REASSIGN;
 	rule->act.ract.bp_dev = lct_dev;
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_mirred.c b/drivers/net/datapath/pon_qos/pon_qos_tc_mirred.c
index 3f92cc391366..1a7a0b13d076 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_mirred.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_mirred.c
@@ -99,7 +99,7 @@ static int pon_qos_flow_get_bp_id(struct net_device *dev)
 
 	err = dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0);
 	if (err != DP_SUCCESS)
-		return -1;
+		return -ENODEV;
 
 	return subif.bport;
 }
@@ -193,7 +193,7 @@ static int pon_qos_tc_to_pce(struct net_device *dev,
 	if (ret != DP_SUCCESS) {
 		netdev_err(dev, "%s: dp_get_netif_subifid failed\n",
 			   __func__);
-		return -1;
+		return -ENODEV;
 	}
 
 	netdev_dbg(dev, "%s: using port: %d subif: %d\n",
@@ -212,7 +212,7 @@ static int pon_qos_tc_to_pce(struct net_device *dev,
 	} else {
 		netdev_err(dev, "%s: Unsupported mirred TC protocol\n",
 			   __func__);
-		return -1;
+		return -EPROTONOSUPPORT;
 	}
 
 	if (ret != 0)
@@ -227,7 +227,7 @@ static int pon_qos_tc_to_pce(struct net_device *dev,
 	if (bp < 0) {
 		netdev_err(dev, "%s: cannot get BP id from DPM\n",
 			   __func__);
-		return -1;
+		return bp;
 	}
 
 	/* Set destination bridge port id in 256 bit nForwardPortMap
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_mqprio.c b/drivers/net/datapath/pon_qos/pon_qos_tc_mqprio.c
index b275e07219af..4dfce8e0c16f 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_mqprio.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_mqprio.c
@@ -36,7 +36,7 @@ static int pon_qos_sched_update(struct net_device *dev,
 	ret = pon_qos_fill_port_data(sch);
 	if (ret < 0) {
 		netdev_err(dev, "failed getting port hw config %d\n", ret);
-		return -1;
+		return ret;
 	}
 
 	/* add to radix tree here */
@@ -44,7 +44,7 @@ static int pon_qos_sched_update(struct net_device *dev,
 	if (ret) {
 		netdev_err(dev, "qdisc insertion to radix tree failed: %d\n",
 			   ret);
-		return -1;
+		return ret;
 	}
 
 	return pon_qos_add_sched(sch, 0);
@@ -60,7 +60,7 @@ static int pon_qos_queues_update(struct net_device *dev,
 	int ret, i;
 
 	if (!sch->use_cnt || handle != sch->handle)
-		return -1;
+		return -EINVAL;
 
 	if (bands == sch->prio.bands)
 		/* TODO: nothing to do? */
@@ -68,7 +68,7 @@ static int pon_qos_queues_update(struct net_device *dev,
 
 	if (bands < sch->prio.bands) {
 		/* TODO delete queue here */
-		return -1;
+		return -EINVAL;
 	}
 
 	sch->prio.bands = bands;
@@ -99,7 +99,7 @@ static int pon_qos_tc_mqprio_replace(struct net_device *dev,
 		port = pon_qos_port_alloc(dev);
 		if (!port) {
 			netdev_err(dev, "tc-mqprio port alloc failed\n");
-			return -1;
+			return -ENOMEM;
 		}
 		newp = true;
 	}
@@ -134,7 +134,7 @@ static int pon_qos_tc_mqprio_replace(struct net_device *dev,
 err_free_port:
 	if (newp)
 		pon_qos_port_delete(port);
-	return -1;
+	return ret;
 }
 
 static int pon_qos_tc_mqprio_destroy(struct net_device *dev,
@@ -146,12 +146,12 @@ static int pon_qos_tc_mqprio_destroy(struct net_device *dev,
 
 	port = pon_qos_port_get(dev);
 	if (!port)
-		return -1;
+		return -ENODEV;
 
 	sch = &port->root_qdisc;
 
 	if (handle != sch->handle)
-		return -1;
+		return -EINVAL;
 
 	pon_qos_qdisc_tree_del(port, sch);
 
@@ -179,13 +179,13 @@ int pon_qos_tc_mqprio_offload(struct net_device *dev,
 						port_id, deq_idx);
 		if (err < 0) {
 			netdev_err(dev, "tc-mqprio replace failed\n");
-			return -EINVAL;
+			return err;
 		}
 	} else {
 		err = pon_qos_tc_mqprio_destroy(dev, handle, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-mqprio destroy failed\n");
-			return -EINVAL;
+			return err;
 		}
 	}
 
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_pce.c b/drivers/net/datapath/pon_qos/pon_qos_tc_pce.c
index 71d634f9b424..7f51933a9d59 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_pce.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_pce.c
@@ -102,7 +102,7 @@ static int pce_index_range_get(enum pce_type type,
 		break;
 	default:
 		pr_err("%s: Range not found!\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	pr_debug("%s: Range assigned, start: %u, stop: %u\n", __func__,
@@ -127,7 +127,7 @@ static int pce_free_index_get(u32 *index,
 	gsw_handle = gsw_get_swcore_ops(0);
 	if (!gsw_handle) {
 		pr_err("%s: gsw_get_swcore_ops failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	for (; i < j; i++) {
@@ -146,7 +146,7 @@ static int pce_free_index_get(u32 *index,
 		if (ret != GSW_statusOk) {
 			pr_err("%s: TFLOW_PceRuleRead failed, nIndex: %u, ret: %d\n",
 			       __func__, pce_rule.pattern.nIndex, ret);
-			return -1;
+			return ret;
 		}
 
 		if (!pce_rule.pattern.bEnable) {
@@ -158,7 +158,7 @@ static int pce_free_index_get(u32 *index,
 	}
 
 	pr_err("%s: Free index not found\n", __func__);
-	return -1;
+	return -EINVAL;
 }
 
 static int pce_rule_write(GSW_PCE_rule_t *pce_rule)
@@ -168,14 +168,14 @@ static int pce_rule_write(GSW_PCE_rule_t *pce_rule)
 
 	gsw_handle = gsw_get_swcore_ops(0);
 	if (!gsw_handle)
-		return -1;
+		return -EINVAL;
 
 	ret = gsw_handle->gsw_tflow_ops.TFLOW_PceRuleWrite(gsw_handle,
 							   pce_rule);
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleWrite failed, nIndex: %u, ret: %d\n",
 		       __func__, pce_rule->pattern.nIndex, ret);
-		return -1;
+		return ret;
 	}
 
 	pr_debug("%s: TFLOW_PceRuleWrite success, nIndex: %u\n",
@@ -205,7 +205,7 @@ static int get_subif_data(struct net_device *dev,
 	case PCE_MIRR_UNTAG_DROP:
 		ret = dp_get_netif_subifid(dev, NULL, NULL, NULL, &dp_subif, 0);
 		if (ret != DP_SUCCESS)
-			return -1;
+			return -ENODEV;
 		pr_debug("%s: portid: %u, subif: %u\n", __func__,
 			 dp_subif.port_id, dp_subif.subif);
 		*portid = dp_subif.port_id;
@@ -213,7 +213,7 @@ static int get_subif_data(struct net_device *dev,
 		break;
 	default:
 		pr_err("%s: PCE type unknown\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -233,7 +233,7 @@ static int pce_block_create(int portid, int subif,
 		gsw_handle = gsw_get_swcore_ops(0);
 		if (!gsw_handle) {
 			pr_err("%s: gsw_get_swcore_ops failed\n", __func__);
-			return -1;
+			return -EINVAL;
 		}
 
 		alloc.num_of_rules = PON_QOS_PCE_BLOCK_ENTRIES_MAX;
@@ -243,7 +243,7 @@ static int pce_block_create(int portid, int subif,
 		if (ret != GSW_statusOk) {
 			pr_err("%s: TFLOW_PceRuleAlloc failed, num_of_rules: %u, ret: %d\n",
 			       __func__, alloc.num_of_rules, ret);
-			return -1;
+			return ret;
 		}
 
 		pr_debug("%s: TFLOW_PceRuleAlloc success, num_of_rules: %u, block_id: %u\n",
@@ -259,7 +259,7 @@ static int pce_block_create(int portid, int subif,
 			pr_err("%s: CTP_PortConfigSet failed, nLogicalPortId: %u, nFirstFlowEntryIndex: %u, ret: %d\n",
 			       __func__, ctp.nLogicalPortId,
 			       ctp.nFirstFlowEntryIndex, ret);
-			return -1;
+			return ret;
 		}
 
 		pr_debug("%s: CTP_PortConfigSet success, nLogicalPortId: %u, nFirstFlowEntryIndex: %u\n",
@@ -269,7 +269,7 @@ static int pce_block_create(int portid, int subif,
 
 	pce_block = kzalloc(sizeof(*pce_block), GFP_KERNEL);
 	if (!pce_block)
-		return -1;
+		return -ENOMEM;
 
 	pce_block->subif = subif;
 	pce_block->portid = portid;
@@ -302,7 +302,7 @@ static int pce_block_get(int portid, int subif,
 	}
 
 	pr_debug("%s: PCE block not found\n", __func__);
-	return -1;
+	return -EINVAL;
 }
 
 int pon_qos_pce_rule_create(struct net_device *dev,
@@ -318,7 +318,7 @@ int pon_qos_pce_rule_create(struct net_device *dev,
 
 	ret = get_subif_data(dev, type, &portid, &subif);
 	if (ret != 0)
-		return ret;
+		return -ENODEV;
 
 	ret = pce_block_get(portid, subif, &block);
 	if (ret != 0) {
@@ -388,7 +388,7 @@ static int pce_block_delete(int portid, int subif,
 	gsw_handle = gsw_get_swcore_ops(0);
 	if (!gsw_handle) {
 		pr_err("%s: gsw_get_swcore_ops failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	ret = gsw_handle->gsw_ctp_ops.CTP_PortConfigSet(gsw_handle,
@@ -397,7 +397,7 @@ static int pce_block_delete(int portid, int subif,
 		pr_err("%s: CTP_PortConfigSet failed, nLogicalPortId: %u, nFirstFlowEntryIndex: %u, ret: %d\n",
 		       __func__, ctp.nLogicalPortId, ctp.nFirstFlowEntryIndex,
 		       ret);
-		return -1;
+		return ret;
 	}
 
 	pr_debug("%s: CTP_PortConfigSet success, nLogicalPortId: %u, nFirstFlowEntryIndex: %u\n",
@@ -408,7 +408,7 @@ static int pce_block_delete(int portid, int subif,
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleFree failed, block_id: %u, ret: %d\n",
 		       __func__, alloc.blockid, ret);
-		return -1;
+		return ret;
 	}
 
 	list_del(&block->list);
@@ -436,7 +436,7 @@ int pon_qos_pce_rule_delete(u32 tc_handle, int pref)
 
 	if (!deletion) {
 		pr_debug("%s: Nothing to delete\n", __func__);
-		return -1;
+		return -ENOENT;
 	}
 
 	ret = pce_block_get(p->portid, p->subif, &block);
@@ -453,7 +453,7 @@ int pon_qos_pce_rule_delete(u32 tc_handle, int pref)
 	gsw_handle = gsw_get_swcore_ops(0);
 	if (!gsw_handle) {
 		pr_err("%s: gsw_get_swcore_ops failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	pce_rule.nIndex = p->index;
@@ -463,7 +463,7 @@ int pon_qos_pce_rule_delete(u32 tc_handle, int pref)
 	if (ret != GSW_statusOk) {
 		pr_err("%s: TFLOW_PceRuleDelete failed, nIndex: %u, ret: %d\n",
 		       __func__, pce_rule.nIndex, ret);
-		return -1;
+		return ret;
 	}
 
 	block->reserved[p->index] = PON_QOS_PCE_UNRESERVED;
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_prio.c b/drivers/net/datapath/pon_qos/pon_qos_tc_prio.c
index 497ad96d39a7..116b2db69116 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_prio.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_prio.c
@@ -18,7 +18,7 @@ static int pon_qos_sched_update(struct net_device *dev,
 	enum pon_qos_qdisc_type type = PON_QOS_QDISC_PRIO;
 
 	if (!dev || !port || !p)
-		return -1;
+		return -EINVAL;
 
 	return pon_qos_add_child_qdisc(dev, port, type, p->parent, p->handle);
 }
@@ -32,7 +32,7 @@ static int pon_qos_queues_update(struct net_device *dev,
 	int ret, i;
 
 	if (!sch->use_cnt)
-		return -1;
+		return -EINVAL;
 
 	if (bands == sch->prio.bands)
 		/* TODO: nothing to do? */
@@ -43,7 +43,7 @@ static int pon_qos_queues_update(struct net_device *dev,
 			ret = pon_qos_queue_del(sch, i);
 			if (ret < 0) {
 				netdev_err(dev, "queue del failed\n");
-				return -1;
+				return ret;
 			}
 		}
 	}
@@ -67,7 +67,7 @@ static int pon_qos_tc_prio_update_tree(struct pon_qos_qdisc *sch,
 	/* tree update requires grafting so skip it for now */
 
 	if (sch->num_children)
-		return -1;
+		return -EINVAL;
 
 	return 0;
 }
@@ -88,7 +88,7 @@ static int pon_qos_tc_prio_replace(struct net_device *dev,
 		port = pon_qos_port_alloc(dev);
 		if (!port) {
 			netdev_err(dev, "tc-drr port alloc failed\n");
-			return -1;
+			return -ENOMEM;
 		}
 		newp = true;
 	}
@@ -114,12 +114,15 @@ static int pon_qos_tc_prio_replace(struct net_device *dev,
 	qdisc = pon_qos_qdisc_find(port, opt->handle);
 	if (!qdisc) {
 		netdev_err(dev, "%s: radix tree broken\n", __func__);
+		ret = -ENODEV;
 		goto err_free_port;
 	}
 
 	ret = pon_qos_tc_prio_update_tree(qdisc, opt);
-	if (ret < 0)
-		return -EOPNOTSUPP;
+	if (ret < 0) {
+		ret = -EOPNOTSUPP;
+		goto err_free_qdisc;
+	}
 
 	ret = pon_qos_queues_update(dev, qdisc, opt);
 	if (ret < 0) {
@@ -138,7 +141,7 @@ static int pon_qos_tc_prio_replace(struct net_device *dev,
 err_free_port:
 	if (newp)
 		pon_qos_port_delete(port);
-	return -1;
+	return ret;
 }
 
 static int pon_qos_tc_prio_destroy(struct net_device *dev,
@@ -190,7 +193,7 @@ int pon_qos_tc_prio_offload(struct net_device *dev,
 					      port_id, deq_idx);
 		if (err < 0) {
 			netdev_err(dev, "tc-prio replace failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_PRIO_DESTROY:
@@ -199,7 +202,7 @@ int pon_qos_tc_prio_offload(struct net_device *dev,
 		err = pon_qos_tc_prio_destroy(dev, handle, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-prio destroy failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_PRIO_STATS:
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_qmap.c b/drivers/net/datapath/pon_qos/pon_qos_tc_qmap.c
index 10919a06c0c7..675456686cf6 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_qmap.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_qmap.c
@@ -116,7 +116,7 @@ int pon_qos_tc_map(struct net_device *dev, struct tc_cls_flower_offload *f)
 	if (ifi) {
 		indev = dev_get_by_index(dev_net(dev), ifi);
 		if (!indev)
-			return -1;
+			return -ENODEV;
 	}
 
 	ret = pon_qos_tc_flower_action_valid(f->exts);
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_qos.c b/drivers/net/datapath/pon_qos/pon_qos_tc_qos.c
index 1c6b5e9f2c70..9a863f7a6266 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_qos.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_qos.c
@@ -63,7 +63,7 @@ static int pon_qos_overwrite_port_thresholds(struct pon_qos_qdisc *sch)
 	if (dp_qos_port_conf_set(&port_cfg_info, flags) == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: dp_qos_port_conf_set() failed\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -79,7 +79,7 @@ int pon_qos_fill_port_data(struct pon_qos_qdisc *sch)
 	if (sch->deq_idx < 0) {
 		ret = dp_get_netif_subifid(sch->dev, NULL, NULL, 0, &subif, 0);
 		if (ret < 0)
-			return -1;
+			return -ENODEV;
 		/* This is PON DS port so mark this */
 		sch->port = subif.port_id;
 		sch->deq_idx = 0;
@@ -92,7 +92,7 @@ int pon_qos_fill_port_data(struct pon_qos_qdisc *sch)
 	deq.q_res_size = ARRAY_SIZE(q_res);
 	ret = dp_deq_port_res_get(&deq, 0);
 	if (ret < 0)
-		return -2;
+		return ret;
 
 	/* save hw egress port settings */
 	sch->inst = subif.inst;
@@ -110,7 +110,7 @@ int pon_qos_get_port_info(struct pon_qos_qdisc *sch)
 	if (sch->deq_idx < 0 || sch->port < 0) {
 		ret = dp_get_netif_subifid(sch->dev, NULL, NULL, 0, &subif, 0);
 		if (ret < 0)
-			return -1;
+			return -ENODEV;
 		/* This is PON DS port so mark this */
 		sch->port = subif.port_id;
 		sch->deq_idx = 0;
@@ -126,7 +126,7 @@ int pon_qos_alloc_qdisc(struct pon_qos_qdisc **qdisc)
 
 	qp = kzalloc(sizeof(**qdisc), GFP_KERNEL);
 	if (!qp)
-		return -1;
+		return -ENOMEM;
 
 	*qdisc = qp;
 
@@ -151,11 +151,11 @@ int pon_qos_get_sch_by_handle(struct net_device *dev,
 
 	port = pon_qos_port_get(dev);
 	if (!port)
-		return -1;
+		return -ENODEV;
 
 	*sch = pon_qos_qdisc_find(port, handle);
 	if (!*sch)
-		return -2;
+		return -ENODEV;
 
 	return 0;
 }
@@ -170,13 +170,13 @@ int pon_qos_get_queue_by_handle(struct net_device *dev,
 
 	ret = pon_qos_get_sch_by_handle(dev, handle, &qdisc);
 	if (ret < 0)
-		return -1;
+		return ret;
 
 	if (idx < 0 || idx >= PON_QOS_TC_MAX_Q)
-		return -2;
+		return -EINVAL;
 
 	if (!qdisc->qids[idx].qid)
-		return -3;
+		return -EINVAL;
 
 	*qid = &qdisc->qids[idx];
 
@@ -193,7 +193,7 @@ int pon_qos_add_sched(struct pon_qos_qdisc *sch, int prio)
 	int ret;
 
 	if (sch->parent != TC_H_ROOT)
-		return -1;
+		return -EINVAL;
 
 	/* change def queue prio */
 	prio_info.inst = sch->inst;
@@ -202,7 +202,7 @@ int pon_qos_add_sched(struct pon_qos_qdisc *sch, int prio)
 	ret = dp_qos_link_prio_get(&prio_info, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, " failed to get prio node\n");
-		return -1;
+		return -EINVAL;
 	}
 
 	/* Re-set input and policy */
@@ -211,7 +211,7 @@ int pon_qos_add_sched(struct pon_qos_qdisc *sch, int prio)
 	ret = dp_qos_link_prio_set(&prio_info, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "set input of link failed\n");
-		return -1;
+		return -EINVAL;
 	}
 
 	/* Allocate sched */
@@ -222,7 +222,7 @@ int pon_qos_add_sched(struct pon_qos_qdisc *sch, int prio)
 	ret = dp_node_alloc(&anode, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "sch_id alloc fialed\n");
-		return -1;
+		return -ENOMEM;
 	}
 	sch->sch_id = anode.id.sch_id;
 
@@ -241,14 +241,14 @@ int pon_qos_add_sched(struct pon_qos_qdisc *sch, int prio)
 	if (dp_node_link_add(&node, 0) == DP_FAILURE) {
 		netdev_err(sch->dev, "failed to link WRR sched to port\n");
 		dp_node_free(&anode, DP_NODE_AUTO_FREE_RES);
-		return -1;
+		return -ENODEV;
 	}
 
 	if (sch->deq_idx >= 0) {
 		if (pon_qos_overwrite_port_thresholds(sch)) {
 			netdev_err(sch->dev, "%s: pon_qos_overwrite_port_thresholds() failed\n",
 				   __func__);
-			return -3;
+			return -EINVAL;
 		}
 	}
 
@@ -271,7 +271,7 @@ int pon_qos_add_staged_sched(struct pon_qos_qdisc *psch,
 	ret = dp_node_alloc(&anode, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(csch->dev, "sch_id alloc fialed\n");
-		return -1;
+		return -ENOMEM;
 	}
 	csch->sch_id = anode.id.sch_id;
 
@@ -293,7 +293,7 @@ int pon_qos_add_staged_sched(struct pon_qos_qdisc *psch,
 			   "failed to link sched %d to sched %d in: %d\n",
 			   csch->sch_id, psch->sch_id, prio);
 		dp_node_free(&anode, DP_NODE_AUTO_FREE_RES);
-		return -1;
+		return -ENODEV;
 	}
 
 	return 0;
@@ -311,7 +311,7 @@ int pon_qos_sched_del(struct pon_qos_qdisc *sch)
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "sched id %d unlink failed\n",
 			   node.node_id.sch_id);
-		return -1;
+		return -ENODEV;
 	}
 
 	anode.type = DP_NODE_SCH;
@@ -320,7 +320,7 @@ int pon_qos_sched_del(struct pon_qos_qdisc *sch)
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "sched id %d free failed\n",
 			   node.node_id.sch_id);
-		return -1;
+		return -ENODEV;
 	}
 
 	sch->use_cnt = 0;
@@ -342,20 +342,20 @@ int pon_qos_add_child_qdisc(struct net_device *dev,
 	int ret;
 
 	if (!dev || !port)
-		return -1;
+		return -EINVAL;
 
 	if (parent == TC_H_ROOT)
 		idx = 0;
 
 	if (idx < 0 || idx >= PON_QOS_TC_MAX_Q)
-		return -1;
+		return -EINVAL;
 
 	netdev_dbg(dev, "searching qdisc %#x idx: %d\n", parent, idx);
 	if (parent != TC_H_ROOT) {
 		psch = pon_qos_qdisc_find(port, parent);
 		if (!psch) {
 			netdev_err(dev, "%s: parent not found\n", __func__);
-			return -1;
+			return -ENODEV;
 		}
 	} else {
 		psch = &port->root_qdisc;
@@ -370,21 +370,21 @@ int pon_qos_add_child_qdisc(struct net_device *dev,
 		if (ret < 0) {
 			netdev_err(dev,
 				   "%s: queue delete failed\n", __func__);
-			return -1;
+			return ret;
 		}
 	}
 	/* if there is a sched on this input return with error for now */
 	csch = psch->children[idx];
 	if (csch && csch->sch_id) {
 		netdev_err(dev, "%s input allocated by sched\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	if (parent != TC_H_ROOT) {
 		/* ready to add the sched */
 		ret = pon_qos_alloc_qdisc(&qdisc);
 		if (ret < 0)
-			return -1;
+			return -ENOMEM;
 
 		psch->children[idx] = qdisc;
 		psch->num_children++;
@@ -451,7 +451,7 @@ int pon_qos_add_child_qdisc(struct net_device *dev,
 		psch->num_children--;
 		pon_qos_free_qdisc(qdisc);
 	}
-	return -1;
+	return ret;
 }
 
 static int pon_qos_get_subif_idx(struct net_device *dev, int *subif)
@@ -460,12 +460,12 @@ static int pon_qos_get_subif_idx(struct net_device *dev, int *subif)
 	int ret;
 
 	if (!dev)
-		return -1;
+		return -EINVAL;
 
 	ret = dp_get_netif_subifid(dev, NULL, NULL, 0, &dp_subif, 0);
 	if (ret < 0) {
 		netdev_err(dev, "%s: subif idx get failed\n", __func__);
-		return -1;
+		return -ENODEV;
 	}
 
 	*subif = dp_subif.subif;
@@ -484,7 +484,7 @@ static int pon_qos_sched_policy_update(struct pon_qos_qdisc *sch,
 	ret = dp_qos_link_prio_get(&prio_info, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "%s :get prio node failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	/* Re-set policy */
@@ -493,7 +493,7 @@ static int pon_qos_sched_policy_update(struct pon_qos_qdisc *sch,
 	ret = dp_qos_link_prio_set(&prio_info, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: set sch arbi failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	prio_info.id.sch_id = sch->qids[idx].qid;
@@ -501,14 +501,14 @@ static int pon_qos_sched_policy_update(struct pon_qos_qdisc *sch,
 	ret = dp_qos_link_prio_get(&prio_info, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "%s :get prio node failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	prio_info.prio_wfq = prio_w;
 	ret = dp_qos_link_prio_set(&prio_info, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: set sch arbi failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -535,7 +535,7 @@ static int pon_qos_queue_wred_defaults_set(struct pon_qos_qdisc *sch, int idx)
 	if (dp_queue_conf_get(&q_cfg, 0) == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: dp_queue_conf_get() failed\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	/** Modify */
@@ -556,7 +556,7 @@ static int pon_qos_queue_wred_defaults_set(struct pon_qos_qdisc *sch, int idx)
 	if (dp_queue_conf_set(&q_cfg, 1) == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: dp_queue_conf_set() failed\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -569,7 +569,7 @@ int pon_qos_queue_add(struct pon_qos_qdisc *sch, int arbi, int prio_w, int idx)
 	int ret;
 
 	if (!sch)
-		return -1;
+		return -EINVAL;
 
 	anode.inst = sch->inst;
 	anode.dp_port = sch->port;
@@ -578,7 +578,7 @@ int pon_qos_queue_add(struct pon_qos_qdisc *sch, int arbi, int prio_w, int idx)
 	ret = dp_node_alloc(&anode, 0);
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "failed to alloc queue\n");
-		return -1;
+		return -ENOMEM;
 	}
 
 	sch->qids[idx].qid = anode.id.q_id;
@@ -602,7 +602,7 @@ int pon_qos_queue_add(struct pon_qos_qdisc *sch, int arbi, int prio_w, int idx)
 			netdev_err(sch->dev, "qid %d free failed\n",
 				   anode.id.q_id);
 		memset(&sch->qids[idx], 0, sizeof(struct pon_qos_q_data));
-		return -1;
+		return -ENODEV;
 	}
 
 	sch->num_q++;
@@ -627,10 +627,10 @@ int pon_qos_queue_del(struct pon_qos_qdisc *sch, int idx)
 	int ret;
 
 	if (!sch)
-		return -1;
+		return -EINVAL;
 
 	if (idx < 0 || idx > PON_QOS_TC_MAX_Q - 1)
-		return -1;
+		return -EINVAL;
 
 	ret = pon_qos_qdata_child_remove(sch->dev, &sch->qids[idx],
 					 sch->handle);
@@ -640,7 +640,7 @@ int pon_qos_queue_del(struct pon_qos_qdisc *sch, int idx)
 	anode.type = DP_NODE_QUEUE;
 	anode.id.q_id = sch->qids[idx].qid;
 	if (!anode.id.q_id /*|| atomic_read(&sch->qids[idx].ref_cnt)*/)
-		return -1;
+		return -EINVAL;
 
 	ret = dp_node_free(&anode, DP_NODE_AUTO_FREE_RES);
 	if (ret == DP_FAILURE)
@@ -668,7 +668,7 @@ int pon_qos_tc_qdisc_unlink(struct pon_qos_port *p, struct pon_qos_qdisc *sch)
 
 	psch = pon_qos_qdisc_find(p, sch->parent);
 	if (!psch)
-		return -EINVAL;
+		return -ENODEV;
 
 	psch->children[idx] = NULL;
 	psch->num_children--;
@@ -697,7 +697,7 @@ int pon_qos_qdisc_tree_del(struct pon_qos_port *p, struct pon_qos_qdisc *root)
 				   root->sch_id);
 			ret = pon_qos_qdisc_tree_del(p, sch);
 			if (ret < 0)
-				return -1;
+				return ret;
 		}
 	}
 
@@ -713,13 +713,13 @@ int pon_qos_qdisc_tree_del(struct pon_qos_port *p, struct pon_qos_qdisc *root)
 				   __func__, root->qids[i].qid);
 			ret = pon_qos_queue_del(root, i);
 			if (ret < 0)
-				return -1;
+				return ret;
 		}
 	}
 
 	ret = pon_qos_sched_del(root);
 	if (ret < 0)
-		return -1;
+		return ret;
 
 	p->sch_num--;
 	if (root->parent != TC_H_ROOT && TC_H_MIN(root->parent)) {
@@ -792,7 +792,7 @@ static int pon_qos_set_qmap(struct pon_qos_q_data *qid,
 	ret = dp_queue_map_set(&qmap_set, 0);
 	if (ret == DP_FAILURE) {
 		pr_err("%s: queue map set failed\n", __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -808,7 +808,7 @@ static int pon_qos_config_cpu_port(struct pon_qos_q_data *qid,
 
 	ret = pon_qos_set_qmap(qid, port, tc, en);
 	if (ret != 0)
-		return -1;
+		return ret;
 
 	if (!en)
 		atomic_dec(&qid->ref_cnt);
@@ -896,23 +896,23 @@ int pon_qos_update_qmap(struct net_device *dev,
 
 	port = pon_qos_port_get(dev);
 	if (!port)
-		return -1;
+		return -ENODEV;
 
 	sch = &port->root_qdisc;
 	if (!sch)
-		return -1;
+		return -EINVAL;
 
 	ret = pon_qos_get_queue_by_handle(dev, q_tc->handle, &qid);
 	if (ret < 0 || !qid) {
 		netdev_err(dev, "%s: en=%d, handle %#x not found err %d\n",
 			   __func__, en, q_tc->handle, ret);
-		return -1;
+		return -EINVAL;
 	}
 
 	if (q_tc->indev) {
 		ret = pon_qos_get_subif_idx(q_tc->indev, &tc);
 		if (ret < 0)
-			return -1;
+			return -ENODEV;
 	} else {
 		tc = q_tc->tc;
 	}
@@ -969,7 +969,7 @@ int pon_qos_ports_cleanup(void)
 		ret = pon_qos_qdisc_tree_del(p, &p->root_qdisc);
 		if (ret < 0) {
 			pr_err("%s: error freeing port\n", __func__);
-			return -1;
+			return ret;
 		}
 		pon_qos_port_delete(p);
 	}
@@ -995,7 +995,7 @@ int pon_qos_qdata_get_by_type(struct net_device *dev,
 	}
 
 	netdev_dbg(dev, "%s: type not found\n", __func__);
-	return -1;
+	return -EINVAL;
 }
 
 int pon_qos_qdata_add(struct net_device *dev, struct pon_qos_q_data *qid,
@@ -1010,7 +1010,7 @@ int pon_qos_qdata_add(struct net_device *dev, struct pon_qos_q_data *qid,
 	if (!pon_qos_qdata_get_by_type(dev, qid, type, &tmp_h, &tmp_p)) {
 		netdev_err(dev, "%s: Duplicated setting pid:%#x class/handle:%#x\n",
 			   __func__, parent, handle);
-		return -1;
+		return -ENODEV;
 	}
 
 	params = kzalloc(sizeof(*params), GFP_KERNEL);
@@ -1151,7 +1151,7 @@ int pon_qos_shaper_add(struct pon_qos_qdisc *sch,
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: shaper config set failed %d\n",
 			   __func__, ret);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -1173,7 +1173,7 @@ int pon_qos_shaper_remove(struct pon_qos_qdisc *sch,
 	if (ret == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: shaper config set failed %d\n",
 			   __func__, ret);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_red.c b/drivers/net/datapath/pon_qos/pon_qos_tc_red.c
index f311af0be8b0..95759d6abc29 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_red.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_red.c
@@ -33,7 +33,7 @@ static int wred_params_set(struct pon_qos_qdisc *sch,
 	if (!def && !params) {
 		netdev_err(sch->dev, "%s: non-defaults params missing\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	switch (type) {
@@ -46,14 +46,14 @@ static int wred_params_set(struct pon_qos_qdisc *sch,
 	default:
 		netdev_err(sch->dev, "%s: wrong type\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	/** Read */
 	if (dp_queue_conf_get(&q_cfg, 0) == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: dp_queue_conf_get() failed\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	/** Modify */
@@ -80,7 +80,7 @@ static int wred_params_set(struct pon_qos_qdisc *sch,
 	if (dp_queue_conf_set(&q_cfg, 1) == DP_FAILURE) {
 		netdev_err(sch->dev, "%s: dp_queue_conf_set() failed\n",
 			   __func__);
-		return -1;
+		return -EINVAL;
 	}
 
 	return 0;
@@ -97,7 +97,7 @@ static int __wred_destroy(struct net_device *dev, u32 handle, u32 parent,
 	port = pon_qos_port_get(dev);
 	if (!port) {
 		netdev_err(dev, "tc-red port get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qdisc = pon_qos_qdisc_find(port, parent);
@@ -113,7 +113,7 @@ static int __wred_destroy(struct net_device *dev, u32 handle, u32 parent,
 		if (ret < 0 || !qid) {
 			netdev_err(dev, "%s: handle not found err %d\n",
 				   __func__, ret);
-			return -1;
+			return -EINVAL;
 		}
 	}
 
@@ -151,13 +151,13 @@ static int pon_qos_tc_red_replace(struct net_device *dev,
 	port = pon_qos_port_get(dev);
 	if (!port) {
 		netdev_err(dev, "tc-red port get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qdisc = pon_qos_qdisc_find(port, opt->parent);
 	if (!qdisc) {
 		netdev_err(dev, "tc-red qdisc get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qid = pon_qos_qdata_qid_get(dev, qdisc, opt->parent);
@@ -166,7 +166,7 @@ static int pon_qos_tc_red_replace(struct net_device *dev,
 		if (ret < 0 || !qid) {
 			netdev_err(dev, "%s: handle not found err %d\n",
 				   __func__, ret);
-			return -1;
+			return -EINVAL;
 		}
 	}
 
@@ -194,7 +194,7 @@ static int pon_qos_tc_red_replace(struct net_device *dev,
 	if (ret) {
 		netdev_err(dev, "%s: qdisc insertion to radix tree failed: %d\n",
 			   __func__, ret);
-		return -1;
+		return ret;
 	}
 
 	return 0;
@@ -229,7 +229,7 @@ static int pon_qos_tc_red_destroy(struct net_device *dev,
 		if (ret < 0 || !qid) {
 			netdev_err(dev, "%s: handle not found err %d\n",
 				   __func__, ret);
-			return -1;
+			return -EINVAL;
 		}
 	}
 
@@ -253,7 +253,7 @@ int pon_qos_tc_red_offload(struct net_device *dev,
 		err = pon_qos_tc_red_replace(dev, handle, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-red replace failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_RED_DESTROY:
@@ -262,7 +262,7 @@ int pon_qos_tc_red_offload(struct net_device *dev,
 		err = pon_qos_tc_red_destroy(dev, handle, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-red destroy failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_RED_STATS:
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_tbf.c b/drivers/net/datapath/pon_qos/pon_qos_tc_tbf.c
index c7bf3e09aec7..fdaafdf3e152 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_tbf.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_tbf.c
@@ -17,13 +17,13 @@ static int pon_tbf_remove(struct net_device *dev, u32 handle, u32 parent)
 	port = pon_qos_port_get(dev);
 	if (!port) {
 		netdev_err(dev, "tc-tbf port get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qdisc = pon_qos_qdisc_find(port, parent);
 	if (!qdisc) {
 		netdev_err(dev, "tc-tbf qdisc get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 
@@ -33,7 +33,7 @@ static int pon_tbf_remove(struct net_device *dev, u32 handle, u32 parent)
 		if (ret < 0 || !qid) {
 			netdev_err(dev, "%s: handle not found err %d\n",
 				__func__, ret);
-			return -1;
+			return -EINVAL;
 		}
 	}
 
@@ -57,13 +57,13 @@ static int pon_qos_tc_tbf_replace(struct net_device *dev,
 	port = pon_qos_port_get(dev);
 	if (!port) {
 		netdev_err(dev, "tc-tbf port get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qdisc = pon_qos_qdisc_find(port, opt->parent);
 	if (!qdisc) {
 		netdev_err(dev, "tc-tbf qdisc get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qid = pon_qos_qdata_qid_get(dev, qdisc, opt->parent);
@@ -72,7 +72,7 @@ static int pon_qos_tc_tbf_replace(struct net_device *dev,
 		if (ret < 0 || !qid) {
 			netdev_err(dev, "%s: handle not found err %d\n",
 				__func__, ret);
-			return -1;
+			return -EINVAL;
 		}
 	}
 
@@ -83,7 +83,7 @@ static int pon_qos_tc_tbf_replace(struct net_device *dev,
 	ret = pon_qos_qdata_add(dev, qid, opt->handle, opt->parent,
 				PON_QOS_QDATA_TBF, pon_tbf_remove);
 	if (ret < 0)
-		return -1;
+		return ret;
 
 	ret = radix_tree_insert(&port->qdiscs, TC_H_MAJ(opt->handle), qdisc);
 	if (ret < 0) {
@@ -106,13 +106,13 @@ static int pon_qos_tc_tbf_destroy(struct net_device *dev,
 	port = pon_qos_port_get(dev);
 	if (!port) {
 		netdev_err(dev, "tc-tbf port get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qdisc = pon_qos_qdisc_find(port, opt->parent);
 	if (!qdisc) {
 		netdev_err(dev, "tc-tbf qdisc get failed\n");
-		return -1;
+		return -ENODEV;
 	}
 
 	qid = pon_qos_qdata_qid_get(dev, qdisc, opt->parent);
@@ -121,7 +121,7 @@ static int pon_qos_tc_tbf_destroy(struct net_device *dev,
 		if (ret < 0 || !qid) {
 			netdev_err(dev, "%s: handle not found err %d\n",
 				__func__, ret);
-			return -1;
+			return -EINVAL;
 		}
 	}
 
@@ -140,14 +140,14 @@ int pon_qos_tc_tbf_offload(struct net_device *dev,
 		err = pon_qos_tc_tbf_replace(dev, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-tbf replace failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_TBF_DESTROY:
 		err = pon_qos_tc_tbf_destroy(dev, opt);
 		if (err < 0) {
 			netdev_err(dev, "tc-tbf destroy failed\n");
-			return -EINVAL;
+			return err;
 		}
 		break;
 	case TC_TBF_STATS:
diff --git a/drivers/net/datapath/pon_qos/pon_qos_tc_trap.c b/drivers/net/datapath/pon_qos/pon_qos_tc_trap.c
index a29dbd9fc7ca..34fb08395126 100644
--- a/drivers/net/datapath/pon_qos/pon_qos_tc_trap.c
+++ b/drivers/net/datapath/pon_qos/pon_qos_tc_trap.c
@@ -325,7 +325,7 @@ int pon_qos_trap_offload(struct net_device *dev,
 			   __func__, index);
 		kfree(pce_rule);
 		kfree(pce_blk_info);
-		return -1;
+		return -EINVAL;
 	}
 	netdev_dbg(dev, "%s: pce rule added with index %d\n", __func__, index);
 
@@ -405,7 +405,7 @@ int pon_qos_trap_unoffload(struct net_device *dev,
 	if (ret < 0) {
 		netdev_err(dev, "%s: dp_pce_rule_del failed err %d\n",
 			    __func__, ret);
-		return -1;
+		return ret;
 	}
 	netdev_dbg(dev, "%s: deleted pce rule with index %d\n", __func__,
 		    pce_rule->nIndex);
