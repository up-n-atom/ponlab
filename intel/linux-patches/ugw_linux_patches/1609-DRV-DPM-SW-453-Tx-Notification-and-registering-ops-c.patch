From 77b88c822cdae51bc844c3dd7c0338ef8f6a8446 Mon Sep 17 00:00:00 2001
From: thampan <joby.thampan@intel.com>
Date: Thu, 28 Jan 2021 15:26:16 +0800
Subject: [PATCH] DRV_DPM_SW-453: Tx Notification and registering ops
 capability

---
 drivers/net/datapath/dpm/datapath.h              |   9 +-
 drivers/net/datapath/dpm/datapath_api.c          |  86 +++++++++++-
 drivers/net/datapath/dpm/datapath_misc.c         |  22 +++
 drivers/net/datapath/dpm/datapath_tx.c           | 168 ++++++++---------------
 drivers/net/datapath/dpm/datapath_tx.h           |  41 +++++-
 drivers/net/datapath/dpm/gswip31/datapath_misc.c |   3 +-
 drivers/net/datapath/dpm/gswip31/datapath_misc.h |   3 +-
 drivers/net/datapath/dpm/gswip31/datapath_tx.c   |  35 +++--
 include/net/datapath_api.h                       |   1 +
 include/net/datapath_api_tx.h                    |  51 +++++--
 include/net/datapath_inst.h                      |   5 +-
 11 files changed, 263 insertions(+), 161 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath.h b/drivers/net/datapath/dpm/datapath.h
index b5edc2b9bb0f..513c6d78a02c 100644
--- a/drivers/net/datapath/dpm/datapath.h
+++ b/drivers/net/datapath/dpm/datapath.h
@@ -131,10 +131,11 @@ extern DP_LOCK_T dp_lock;
 #define PARSER_FLAG_SIZE   40
 #define PARSER_OFFSET_SIZE 8
 
+#define PMAC_HDR_SIZE (sizeof(struct pmac_rx_hdr))
 #define PKT_PASER_FLAG_OFFSET   0
 #define PKT_PASER_OFFSET_OFFSET (PARSER_FLAG_SIZE)
 #define PKT_PMAC_OFFSET         ((PARSER_FLAG_SIZE) + (PARSER_OFFSET_SIZE))
-#define PKT_DATA_OFFSET         ((PKT_PMAC_OFFSET) + (PMAC_SIZE))
+#define PKT_DATA_OFFSET         ((PKT_PMAC_OFFSET) + (PMAC_HDR_SIZE))
 
 #define CHECK_BIT(var, pos) (((var) & (1 << (pos))) ? 1 : 0)
 
@@ -541,6 +542,7 @@ enum DP_TEMP_DMA_PMAC {
 	TEMPL_PTP,
 	TEMPL_INSERT,
 	TEMPL_OTHERS,
+	TEMPL_CHECKSUM_PTP,
 	MAX_TEMPLATE
 };
 
@@ -893,7 +895,6 @@ extern u32 dp_print_num_en;
 extern int aca_portid;
 #endif
 
-int dp_loop_eth_dev_init(struct dentry *parent);
 void dp_loop_eth_dev_exit(void);
 
 #ifdef CONFIG_DEBUG_FS
@@ -951,7 +952,6 @@ void dump_parser_flag(char *buf);
 //int dp_reset_sys_mib(u32 flag);
 void dp_clear_all_mib_inside(uint32_t flag);
 
-struct sk_buff *dp_create_new_skb(struct sk_buff *skb);
 extern int ip_offset_hw_adjust;
 int register_notifier(u32 flag);
 int unregister_notifier(u32 flag);
@@ -1061,6 +1061,7 @@ int dp_cbm_deq_port_enable(struct module *owner, int inst, int port_id,
 			   u32 dma_ch_off);
 int32_t dp_get_subifid_for_update(int inst, struct net_device *netif,
 				  dp_subif_t *subif, uint32_t flags);
+int do_tx_hwtstamp(int inst, int dpid, struct sk_buff *skb);
 struct hlist_head *get_dp_g_bridge_id_entry_hash_table_info(int index);
 struct dp_evt_notif_info {
 	struct notifier_block nb;
@@ -1118,8 +1119,6 @@ int dp_cqm_port_alloc_complete(struct module *owner,
 int dp_cqm_deq_port_res_get(u32 dp_port, cbm_dq_port_res_t *res,
 			    u32 flags);
 
-int init_br_vlan(u32 maxvlanid);
-
 u32 dp_get_tx_cbm_pkt(int inst, int port_id, int subif_id_grp);
 
 int dp_init_pce(void);
diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index 6f8733ce8f47..c5fe98d28eee 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -2531,12 +2531,92 @@ void set_chksum(struct pmac_tx_hdr *pmac, u32 tcp_type,
 int32_t dp_xmit(struct net_device *rx_if, dp_subif_t *rx_subif,
 		struct sk_buff *skb, int32_t len, u32 flags)
 {
-	s32 res;
 	int inst = 0;
+	struct dp_tx_common_ex ex = {
+		.cmn = {
+			.inst = 0,
+			.flags = flags,
+			.subif = rx_subif->subif,
+			.tx_portid = rx_subif->port_id,
+		},
+	};
+	struct pmac_port_info *port;
+	u32 vap;
+	struct inst_info *dp_info = get_dp_prop_info(inst);
+	enum DP_TX_FN_RET ret = DP_TX_FN_DROPPED;
 
-	res = dp_port_prop[inst].info.dp_tx(rx_if, rx_subif, skb, len, flags);
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_EXTRA_DEBUG)
+	if (unlikely(!dp_init_ok)) {
+		printk_ratelimited("%s failed for dp no init yet\n", __func__);
+		goto exit;
+	}
 
-	return res;
+	if (unlikely(!rx_subif)) {
+		printk_ratelimited("%s failed for rx_subif null\n", __func__);
+		goto exit;
+	}
+
+	if (unlikely(!skb)) {
+		printk_ratelimited("%s skb NULL\n", __func__);
+		goto exit;
+	}
+
+	if (unlikely(in_irq())) {
+		printk_ratelimited("%s not allowed in interrupt context\n",
+				   __func__);
+		goto exit;
+	}
+#endif
+
+	if (unlikely(rx_subif->port_id >=
+		     dp_info->cap.max_num_dp_ports)) {
+		printk_ratelimited("rx_subif->port_id >= max_ports");
+		UP_STATS(get_dp_port_info(0, 0)->tx_err_drop);
+		MIB_G_STATS_INC(tx_drop);
+		goto exit;
+	}
+
+#if IS_ENABLED(CONFIG_INTEL_DATAPATH_MPE_FASTHOOK_TEST)
+	if (unlikely(ltq_mpe_fasthook_tx_fn))
+		ltq_mpe_fasthook_tx_fn(skb, 0, NULL);
+#endif
+
+	port = get_dp_port_info(inst, rx_subif->port_id);
+	if (unlikely(!rx_if && !is_dsl(port))) {
+		printk_ratelimited("null dev but not DSL\n");
+		goto exit;
+	}
+
+	ex.dev = rx_if;
+	ex.rx_subif = rx_subif;
+	ex.port = port;
+	vap = GET_VAP(ex.cmn.subif, port->vap_offset, port->vap_mask);
+	ex.sif = get_dp_port_subif(port, vap);
+	ex.mib = get_dp_port_subif_mib(ex.sif);
+	ex.cmn.alloc_flags = port->alloc_flags;
+	ret = dp_info->dp_tx(skb, &ex.cmn);
+	if (likely(ret == DP_TX_FN_CONSUMED)) {
+		MIB_G_STATS_INC(tx_pkts);
+	} else if (ret != DP_TX_FN_BUSY) {
+		if (ret == DP_TX_FN_CONTINUE)
+			ret = DP_TX_FN_DROPPED;
+
+		UP_STATS(ex.mib->tx_pkt_dropped);
+		MIB_G_STATS_INC(tx_drop);
+exit:
+		dev_kfree_skb_any(skb);
+	}
+
+	if (flags & DP_TX_NEWRET)
+		return ret;
+
+	if (ret == DP_TX_FN_BUSY) {
+		UP_STATS(ex.mib->tx_pkt_dropped);
+		MIB_G_STATS_INC(tx_drop);
+		dev_kfree_skb_any(skb);
+	}
+
+	return ret ? DP_FAILURE : DP_SUCCESS;
 }
 EXPORT_SYMBOL(dp_xmit);
 
diff --git a/drivers/net/datapath/dpm/datapath_misc.c b/drivers/net/datapath/dpm/datapath_misc.c
index 9105386684b4..4f8030505353 100644
--- a/drivers/net/datapath/dpm/datapath_misc.c
+++ b/drivers/net/datapath/dpm/datapath_misc.c
@@ -1296,6 +1296,28 @@ void free_dp_port_subif_info(int inst)
 	}
 }
 
+int do_tx_hwtstamp(int inst, int dpid, struct sk_buff *skb)
+{
+	struct mac_ops *ops;
+	int rec_id = 0;
+
+	if (!(skb_shinfo(skb)->tx_flags & SKBTX_HW_TSTAMP))
+		return 0;
+	ops = dp_port_prop[inst].mac_ops[dpid];
+	if (!ops) {
+		pr_err("%s: mac_ops is NULL\n", __func__);
+		return -EINVAL;
+	}
+
+	rec_id = ops->do_tx_hwts(ops, skb);
+	if (rec_id < 0) {
+		pr_err("%s: do_tx_hwts failed\n", __func__);
+		return -EINVAL;
+	}
+
+	return rec_id;
+}
+
 /**
  * dp_get_dma_chan_num - Get number of dma_channel
  * @inst: DP instance.
diff --git a/drivers/net/datapath/dpm/datapath_tx.c b/drivers/net/datapath/dpm/datapath_tx.c
index fb9d4db343f8..0cf04171fb0f 100644
--- a/drivers/net/datapath/dpm/datapath_tx.c
+++ b/drivers/net/datapath/dpm/datapath_tx.c
@@ -5,10 +5,17 @@
 
 #include <linux/kernel.h>
 #include <linux/types.h>
+#include <net/xfrm.h>
 #include <linux/log2.h>
+#include <net/datapath_api.h>
 #include "datapath.h"
 #include "datapath_tx.h"
 
+/**
+ * struct tx_entry - hook setup
+ * @cb: callback function
+ * @priv: data
+ */
 struct tx_entry {
 	tx_fn cb;
 	void *priv;
@@ -28,12 +35,13 @@ struct tx_stored {
 
 /**
  * struct tx_hook_list - datapath TX call chain
- * @fn: callback function, sorted by priority
- * @cnt: number of callback function
+ * @rcu_head: used by RCU
+ * @entry: TX entry for preprocess and process sorted by priority
+ * @cnt: number of entry in the list
  */
 struct tx_hook_list {
 	struct rcu_head rcu_head;
-	struct tx_entry chain[DP_TX_CNT * 2];
+	struct tx_entry entry[DP_TX_CNT * 2];
 	u8 cnt;
 };
 
@@ -44,12 +52,12 @@ struct tx_hook_list {
 /**
  * struct dp_tx_context - datapath TX runtime
  * @lock: Lock to pretect concurrent update
- * @en: TX list for reader
- * @stored: TX lists for updater
+ * @hook_list: TX entry list for reader
+ * @stored: TX information from updater
  */
 struct dp_tx_context {
 	DP_TX_CTX_LOCK_T lock;
-	struct tx_hook_list __rcu *en;
+	struct tx_hook_list __rcu *hook_list;
 	struct tx_stored stored[DP_TX_CNT];
 };
 
@@ -57,41 +65,39 @@ static struct dp_tx_context *dp_tx_ctx;
 
 #define POS(VAL) ilog2(DP_TX_##VAL)
 const char *dp_tx_flag_str[] = {
-	[0] 				= "NONE",
+	[POS(CAL_CHKSUM)] 	= "DP_TX_CAL_CHKSUM",
 	[POS(DSL_FCS)] 		= "DP_TX_DSL_FCS",
-	[POS(INSERT_PMAC)] 	= "DP_TX_INSERT_PMAC",
+	[POS(BYPASS_QOS)]	= "DP_TX_BYPASS_QOS",
+	[POS(BYPASS_FLOW)]  = "DP_TX_BYPASS_FLOW",
 	[POS(OAM)] 			= "DP_TX_OAM",
 	[POS(TO_DL_MPEFW)] 	= "DP_TX_TO_DL_MPEFW",
 	[POS(INSERT)] 		= "DP_TX_INSERT",
 	[POS(INSERT_POINT)] = "DP_TX_INSERT_POINT",
+	[POS(WITH_PMAC)] 	= "DP_TX_WITH_PMAC",
 };
 
 #define dp_tx_flags_str_len \
 		(sizeof (dp_tx_flag_str) / sizeof (dp_tx_flag_str[0]))
 
-void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
-	       struct pmac_tx_hdr *pmac, struct dp_subif *rx_subif,
-	       int need_pmac, int gso, int checksum)
+void dp_tx_dbg(char *title, struct sk_buff *skb, struct dp_tx_common_ex *ex)
 {
 	struct inst_info *dp_info = get_dp_prop_info(0);
+
 #if defined(DP_SKB_HACK)
 	DP_DEBUG(DP_DBG_FLAG_DUMP_TX,
 		 "%s: dp_xmit:skb->data/len=0x%px/%d data_ptr=%x from port=%d and subitf=%d\n",
 		 title,
-		 skb->data, len,
+		 skb->data, skb->len,
 		 ((struct dma_tx_desc_2 *)&skb->DW2)->field.data_ptr,
-		 ep, rx_subif->subif);
+		 ex->port->port_id, ex->cmn.subif);
 #endif
 	if (dp_dbg_flag & DP_DBG_FLAG_DUMP_TX_DATA) {
-		if (pmac) {
-			dp_dump_raw_data((char *)pmac, PMAC_SIZE, "Tx Data");
-			dp_dump_raw_data(skb->data,
-					 skb->len,
-					 "Tx Data");
-		} else
-			dp_dump_raw_data(skb->data,
-					 skb->len,
+		if (dp_tx_get_pmac_len(ex)) {
+			dp_dump_raw_data(dp_tx_get_pmac(ex),
+					 dp_tx_get_pmac_len(ex),
 					 "Tx Data");
+		}
+		dp_dump_raw_data(skb->data, skb->len, "Tx Data");
 	}
 
 	DP_DEBUG(DP_DBG_FLAG_DUMP_TX_SUM,
@@ -125,7 +131,7 @@ void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
 					  (struct dma_tx_desc_2 *)&skb->DW2,
 					  (struct dma_tx_desc_3 *)&skb->DW3);
 #else
-	;
+		;
 #endif
 
 	DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "skb->len=%d skb->priority=%d\n",
@@ -134,10 +140,10 @@ void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
 	if (dp_dbg_flag & DP_DBG_FLAG_DUMP_TX) {
 		int i;
 		pr_info("flags =");
-		if (flags) {
+		if (ex->cmn.flags) {
 			/* valid flags start from index 1 */
 			for (i = 1; i < dp_tx_flags_str_len; i++) {
-				if (flags & (1 << i))
+				if (ex->cmn.flags & (1 << i))
 					pr_info(" %s |", dp_tx_flag_str[i]);
 			}
 		} else {
@@ -150,22 +156,21 @@ void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
 		 "skb->data=0x%px with pmac hdr size=%zu\n", skb->data,
 		 sizeof(struct pmac_tx_hdr));
 
-	if (need_pmac) { /*insert one pmac header */
-		DP_DEBUG(DP_DBG_FLAG_DUMP_TX,
-			 "need pmac\n");
+	if (dp_tx_get_pmac_len(ex)) { /*insert one pmac header */
+		DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "need pmac\n");
 
-		if (pmac && (dp_dbg_flag & DP_DBG_FLAG_DUMP_TX_DESCRIPTOR))
-			dp_info->dump_tx_pmac(pmac);
+		if (dp_dbg_flag & DP_DBG_FLAG_DUMP_TX_DESCRIPTOR)
+			dp_info->dump_tx_pmac(dp_tx_get_pmac(ex));
 	} else {
 		DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "no pmac\n");
 	}
 
-	if (gso)
+	if (skb_is_gso(skb))
 		DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "GSO pkt\n");
 	else
 		DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "Non-GSO pkt\n");
 
-	if (checksum)
+	if (ex->cmn.flags & DP_TX_CAL_CHKSUM)
 		DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "Need checksum offload\n");
 	else
 		DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "No need checksum offload pkt\n");
@@ -173,102 +178,37 @@ void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
 	DP_DEBUG(DP_DBG_FLAG_DUMP_TX, "\n\n");
 }
 
-int dp_tx_err(struct sk_buff *skb, struct dp_tx_common *cmn, int ret)
-{
-	switch (ret) {
-	case DP_XMIT_ERR_NOT_INIT:
-		printk_ratelimited("dp_xmit failed for dp no init yet\n");
-		break;
-
-	case DP_XMIT_ERR_IN_IRQ:
-		printk_ratelimited("dp_xmit not allowed in interrupt context\n");
-		break;
-
-	case DP_XMIT_ERR_NULL_SUBIF:
-		printk_ratelimited("dp_xmit failed for rx_subif null\n");
-		UP_STATS(get_dp_port_info(0, 0)->tx_err_drop);
-		break;
-
-	case DP_XMIT_ERR_PORT_TOO_BIG:
-		UP_STATS(get_dp_port_info(0, 0)->tx_err_drop);
-		printk_ratelimited("rx_subif->port_id >= max_ports");
-		break;
-
-	case DP_XMIT_ERR_NULL_SKB:
-		printk_ratelimited("skb NULL");
-		UP_STATS(get_dp_port_info(0, cmn->dpid)->tx_err_drop);
-		break;
-
-	case DP_XMIT_ERR_NULL_IF:
-		UP_STATS(cmn->mib->tx_pkt_dropped);
-		printk_ratelimited("rx_if NULL");
-		break;
-
-	case DP_XMIT_ERR_REALLOC_SKB:
-		pr_info_once("dp_create_new_skb failed\n");
-		break;
-
-	case DP_XMIT_ERR_EP_ZERO:
-		pr_err("Why ep zero in dp_xmit for %s\n",
-		       skb->dev ? skb->dev->name : "NULL");
-		break;
-
-	case DP_XMIT_ERR_GSO_NOHEADROOM:
-		pr_err("No enough skb headerroom(GSO). Need tune SKB buffer\n");
-		break;
-
-	case DP_XMIT_ERR_CSM_NO_SUPPORT:
-		printk_ratelimited("dp_xmit not support checksum\n");
-		break;
-
-	case DP_XMIT_PTP_ERR:
-		break;
-
-	default:
-		UP_STATS(cmn->mib->tx_pkt_dropped);
-		pr_info_once("Why come to here:%x\n",
-			     get_dp_port_info(0, cmn->dpid)->status);
-	}
-
-	if (skb) {
-		dev_kfree_skb_any(skb);
-		MIB_G_STATS_INC(tx_drop);
-	}
-	return DP_FAILURE;
-}
-
 int dp_tx_update_list(void)
 {
 	struct dp_tx_context *ctx = dp_tx_ctx;
-	struct tx_hook_list *new_data;
-	struct tx_hook_list *old_data;
+	struct tx_hook_list *new_list;
+	struct tx_hook_list *old_list;
 	u8 cnt = 0;
 	u8 i;
 
-	/* update list for fast read */
-	new_data = devm_kzalloc(&g_dp_dev->dev, sizeof(struct tx_hook_list),
-				GFP_ATOMIC);
-	if (!new_data)
+	/* update stored tx info into fast list */
+	new_list = kzalloc(sizeof(*new_list), GFP_ATOMIC);
+	if (!new_list)
 		return -ENOMEM;
 	for (i = 0; i < DP_TX_CNT; i++) {
 		const struct tx_stored *s = &ctx->stored[i];
 		bool en = s->preprocess_always_enabled || s->process.cb;
 
 		if (s->preprocess.cb && en) {
-			memcpy(&new_data->chain[cnt++], &s->preprocess,
+			memcpy(&new_list->entry[cnt++], &s->preprocess,
 			       sizeof(struct tx_entry));
 		}
 		if (s->process.cb) {
-			memcpy(&new_data->chain[cnt++], &s->process,
+			memcpy(&new_list->entry[cnt++], &s->process,
 			       sizeof(struct tx_entry));
 		}
 	}
-	new_data->cnt = cnt;
-	old_data = rcu_access_pointer(ctx->en);
-	rcu_assign_pointer(ctx->en, new_data);
+	new_list->cnt = cnt;
+	old_list = rcu_access_pointer(ctx->hook_list);
+	rcu_assign_pointer(ctx->hook_list, new_list);
 
-	if (old_data)
-		kfree_rcu(old_data, rcu_head);
+	if (old_list)
+		kfree_rcu(old_list, rcu_head);
 	return 0;
 }
 
@@ -300,15 +240,15 @@ int dp_tx_start(struct sk_buff *skb, struct dp_tx_common *cmn)
 {
 	int ret = DP_XMIT_ERR_NOT_INIT;
 	struct tx_hook_list *list;
-	u8 cnt;
 	u8 i;
 
 	/* dp_tx_start() could be called in either normal context or softirq */
 	rcu_read_lock();
-	list = rcu_dereference(dp_tx_ctx->en);
-	cnt = list->cnt;
-	for (i = 0; i < cnt; i++) {
-		const struct tx_entry *entry = &list->chain[i];
+	list = rcu_dereference(dp_tx_ctx->hook_list);
+	if (!list)
+		return ret;
+	for (i = 0; i < list->cnt; i++) {
+		const struct tx_entry *entry = &list->entry[i];
 
 		ret = entry->cb(skb, cmn, entry->priv);
 		if (ret != DP_TX_FN_CONTINUE)
@@ -322,7 +262,7 @@ int dp_tx_init(int inst)
 {
 	if (inst)
 		return 0;
-	dp_tx_ctx = devm_kzalloc(&g_dp_dev->dev, sizeof(struct dp_tx_context),
+	dp_tx_ctx = devm_kzalloc(&g_dp_dev->dev, sizeof(*dp_tx_ctx),
 				 GFP_ATOMIC);
 	if (!dp_tx_ctx)
 		return -ENOMEM;
diff --git a/drivers/net/datapath/dpm/datapath_tx.h b/drivers/net/datapath/dpm/datapath_tx.h
index 3bebc2057801..8393a43a0549 100644
--- a/drivers/net/datapath/dpm/datapath_tx.h
+++ b/drivers/net/datapath/dpm/datapath_tx.h
@@ -8,6 +8,32 @@
 
 #include <net/datapath_api_tx.h>
 
+struct pmac_port_info;
+struct dp_subif_info;
+struct pmac_tx_hdr;
+struct dev_mib;
+
+/**
+ * struct dp_checksum_info - datapath checksum info to pass to GSWIP HW
+ */
+/*! @brief datapath checksum parameters */
+struct dp_checksum_info {
+	u32 tcp_h_offset;
+	u32 ip_offset;
+	u32 tcp_type;
+};
+
+struct dp_tx_common_ex {
+	struct dp_tx_common cmn;
+	struct net_device *dev;	  	  /*!< net device */
+	struct dev_mib *mib;          /*!< mib info */
+	struct pmac_port_info *port;  /*!< port info */
+	struct dp_subif_info *sif;    /*!< subif info */
+	dp_subif_t *rx_subif;		  /*!< rx_subif */
+	enum DP_TEMP_DMA_PMAC tmpl;   /*!< DMA Pmac Template */
+	struct dp_checksum_info csum_info;      /*!< DP checksum parameters */
+};
+
 int dp_tx_init(int inst);
 
 void dp_tx_register_process(enum DP_TX_PRIORITY priority, tx_fn fn,
@@ -20,10 +46,17 @@ int dp_tx_update_list(void);
 
 int dp_tx_start(struct sk_buff *skb, struct dp_tx_common *cmn);
 
-int dp_tx_err(struct sk_buff *skb, struct dp_tx_common *cmn, int ret);
+void dp_tx_dbg(char *title, struct sk_buff *skb, struct dp_tx_common_ex *ex);
+
+static inline struct pmac_tx_hdr *
+dp_tx_get_pmac(const struct dp_tx_common_ex *ex)
+{
+	return (struct pmac_tx_hdr *)ex->cmn.pmac;
+}
 
-void dp_tx_dbg(char *title, struct sk_buff *skb, s32 ep, s32 len, u32 flags,
-	       struct pmac_tx_hdr *pmac, struct dp_subif *rx_subif,
-	       int need_pmac, int gso, int checksum);
+static inline u8 dp_tx_get_pmac_len(const struct dp_tx_common_ex *ex)
+{
+	return ex->cmn.pmac_len;
+}
 
 #endif /* end of include guard: DATAPATH_TX_H */
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 54a61dd6bea9..51b8b218dc1f 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -2817,18 +2817,17 @@ int register_dp_capability(int flag)
 	cap.info.cap.max_cpu = CQM_MAX_CPU;
 	cap.info.dp_tc_vlan_set = tc_vlan_set;
 	cap.info.dp_rx = _dp_rx;
+	cap.info.dp_tx = _dp_tx;
 
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP32)
 	cap.info.cap.max_port_per_cpu = 2;
 	cap.info.cap.max_num_spl_conn = MAX_SPL_CONN_CNT;
-	cap.info.dp_tx = dp_xmit_32;
 	cap.info.dp_spl_conn = _dp_spl_conn;
 	cap.info.dp_spl_conn_get = _dp_spl_conn_get;
 	cap.info.dp_set_io_port = dp_set_io_port;
 #elif IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP31)
 	cap.info.cap.max_port_per_cpu = 1;
 	cap.info.vlan_aware_flag = 1;
-	cap.info.dp_tx = dp_xmit_31;
 	/* The full ethtool statistics support is provided in mib_counters
 	 * kernel module where special callback fn is registered for the
 	 * statistics purpose
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.h b/drivers/net/datapath/dpm/gswip31/datapath_misc.h
index c4b971ec4137..78968f5ab925 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.h
@@ -387,8 +387,7 @@ int _dp_qos_max_level_get(struct dp_qos_level *dp, int flag);
 int _dp_qos_global_info_get(struct dp_qos_cfg_info *info, int flag);
 int _dp_qos_port_conf_set(struct dp_port_cfg_info *info, int flag);
 int _dp_rx(struct sk_buff *skb, u32 flags);
-int32_t dp_xmit_31(struct net_device *rx_if, dp_subif_t *rx_subif,
-		   struct sk_buff *skb, int32_t len, uint32_t flags);
+int _dp_tx(struct sk_buff *skb, struct dp_tx_common *cmn);
 void set_chksum(struct pmac_tx_hdr *pmac, u32 tcp_type, u32 ip_offset,
 		int ip_off_hw_adjust, u32 tcp_h_offset);
 int dp_lan_wan_bridging(int port_id, struct sk_buff *skb);
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_tx.c b/drivers/net/datapath/dpm/gswip31/datapath_tx.c
index d92b906bd0ab..7b87feb6ae20 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_tx.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_tx.c
@@ -1,16 +1,17 @@
 // SPDX-License-Identifier: GPL-2.0
-/* Copyright (C) Intel Corporation
- * Author: Shao Guohua <guohua.shao@intel.com>
+/* Copyright (C) 2016-2020 Intel Corporation
+ * Copyright (C) 2020 MaxLinear
  */
 
+#include <linux/bitfield.h>
 #include <linux/kernel.h>
 #include <linux/types.h>
 #include <linux/etherdevice.h>
 #include <net/datapath_api.h>
 #include "../datapath.h"
-#include "datapath_misc.h"
-#include "../datapath_instance.h"
 #include "../datapath_tx.h"
+#include "../datapath_instance.h"
+#include "datapath_misc.h"
 
 static int dp_reinsert_pkt_cnt(struct pmac_port_info *dp_info, int vap,
 			       struct sk_buff *skb)
@@ -53,8 +54,7 @@ static int dp_reinsert_pkt_cnt(struct pmac_port_info *dp_info, int vap,
 	return DP_SUCCESS;
 }
 
-int32_t dp_xmit_31(struct net_device *rx_if, dp_subif_t *rx_subif,
-		   struct sk_buff *skb, int32_t len, uint32_t flags)
+int _dp_tx(struct sk_buff *skb, struct dp_tx_common *cmn)
 {
 	struct dma_tx_desc_0 *desc_0;
 	struct dma_tx_desc_1 *desc_1;
@@ -73,7 +73,19 @@ int32_t dp_xmit_31(struct net_device *rx_if, dp_subif_t *rx_subif,
 	struct mac_ops *ops;
 	int rec_id = 0;
 	struct dev_mib *mib;
+	struct net_device *rx_if;
+	dp_subif_t *rx_subif;
+	int32_t len;
+	uint32_t flags;
+
+	struct dp_tx_common_ex *ex =
+		container_of(cmn, struct dp_tx_common_ex, cmn);
 
+	rx_subif = ex->rx_subif;
+	flags = ex->cmn.flags;
+	len = skb->len;
+	rx_if = ex->dev;
+	
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_EXTRA_DEBUG)
 	if (unlikely(!dp_init_ok)) {
 		err_ret = DP_XMIT_ERR_NOT_INIT;
@@ -112,8 +124,7 @@ int32_t dp_xmit_31(struct net_device *rx_if, dp_subif_t *rx_subif,
 		ltq_mpe_fasthook_tx_fn(skb, 0, NULL);
 #endif
 	if (unlikely(dp_dbg_flag))
-		dp_tx_dbg("\nOrig", skb, ep, len, flags,
-			    NULL, rx_subif, 0, 0, flags & DP_TX_CAL_CHKSUM);
+		dp_tx_dbg("\nOrig", skb, ex);
 
 	/*No PMAC for WAVE500 and DSL by default except bonding case */
 	if (unlikely(NO_NEED_PMAC(dp_info->alloc_flags)))
@@ -285,13 +296,9 @@ int32_t dp_xmit_31(struct net_device *rx_if, dp_subif_t *rx_subif,
 
 	if (unlikely(dp_dbg_flag)) {
 		if (insert_pmac_f)
-			dp_tx_dbg("After", skb, ep, len, flags, &pmac,
-				    rx_subif, insert_pmac_f, skb_is_gso(skb),
-				    tx_chksum_flag);
+			dp_tx_dbg("After", skb, ex);
 		else
-			dp_tx_dbg("After", skb, ep, len, flags, NULL,
-				    rx_subif, insert_pmac_f, skb_is_gso(skb),
-				    tx_chksum_flag);
+			dp_tx_dbg("After", skb, ex);
 	}
 
 #if IS_ENABLED(CONFIG_LTQ_TOE_DRIVER)
diff --git a/include/net/datapath_api.h b/include/net/datapath_api.h
index e981a8ceca86..25973ee1e4c9 100644
--- a/include/net/datapath_api.h
+++ b/include/net/datapath_api.h
@@ -25,6 +25,7 @@
 #error "wrong DP HAL selected"
 #endif
 #endif /*DATAPATH_HAL_LAYER */
+#include <net/datapath_api_tx.h>
 #if !IS_ENABLED(CONFIG_INTEL_DATAPATH_HAL_GSWIP30) /*GRX500 GSWIP30*/
 #include <net/datapath_api_umt.h>
 #endif
diff --git a/include/net/datapath_api_tx.h b/include/net/datapath_api_tx.h
index 0b0a45c369cf..52cc714cc28b 100644
--- a/include/net/datapath_api_tx.h
+++ b/include/net/datapath_api_tx.h
@@ -5,6 +5,11 @@
 
 #ifndef _DATAPATH_API_TX_H
 #define _DATAPATH_API_TX_H
+#include <linux/bits.h>
+#include <linux/types.h>
+#include <linux/netdevice.h>
+
+struct sk_buff;
 
 /*! @{ */
 /*!
@@ -12,12 +17,20 @@
  *
  * @brief Datapath TX path API
  */
- /*! @} */
+/*! @} */
 
+/**
+ * enum DP_TX_FN_RET - define TX return value for dp_xmit() with DP_TX_NEWRET
+ * flag
+ */
+/*! @brief define TX return value for dp_xmit() with DP_TX_NEWRET flag */
 enum DP_TX_FN_RET {
-	DP_TX_FN_CONSUMED,
-	DP_TX_FN_CONTINUE,
-	DP_TX_FN_DROPPED,
+	DP_TX_FN_CONSUMED = NETDEV_TX_OK,
+	DP_TX_FN_DROPPED = NET_XMIT_DROP,
+	DP_TX_FN_BUSY = NETDEV_TX_BUSY,
+	/* DP_TX_FN_CONTINUE is an internal state, will not be returned to
+	 * dp_xmit() */
+	DP_TX_FN_CONTINUE = INT_MAX,
 };
 
 /**
@@ -26,8 +39,8 @@ enum DP_TX_FN_RET {
 /*! @brief define TX chain priority from high to low */
 enum DP_TX_PRIORITY {
 	DP_TX_PP, /*!< packet processor */
-	DP_TX_TSO, /*!< traffic offload engine */
 	DP_TX_VPNA, /*!< VPN adapter */
+	DP_TX_TSO, /*!< traffic offload engine */
 	DP_TX_CQM,  /*!< CQM CPU port */
 	DP_TX_CNT,
 };
@@ -37,8 +50,18 @@ enum DP_TX_PRIORITY {
  */
 /*! @brief define TX path common flags */
 enum DP_TX_FLAGS {
-	DP_TX_FLAG_INSERT_PMAC = BIT(0), /*!< insert PMAC header*/
-	DP_TX_FLAG_STREAM_PORT = BIT(1), /*!< is stream port*/
+	DP_TX_CAL_CHKSUM      = BIT(0), /*!< @brief enable HW checksum offload */
+	DP_TX_DSL_FCS         = BIT(1), /*!< @brief enable DSL FCS checksum offload */
+	DP_TX_BYPASS_QOS      = BIT(2), /*!< @brief bypass QoS */
+	DP_TX_BYPASS_FLOW     = BIT(3), /*!< @brief bypass DMA descriptor filling flow */
+	DP_TX_OAM             = BIT(4), /*!< @brief OAM packet */
+	DP_TX_TO_DL_MPEFW     = BIT(5), /*!< @brief Send pkt directly to DL FW */
+	DP_TX_INSERT          = BIT(6), /*!< @brief GSWIP insertion Support */
+	DP_TX_INSERT_POINT    = BIT(7), /*!< @brief For GSWIP insertion Point 0 or 1 */
+	DP_TX_WITH_PMAC       = BIT(8), /*!< @brief caller already put pmac in
+					*           the skb->data-16 to skb->data-1
+					*/
+	DP_TX_NEWRET          = BIT(9), /*!< @brief dp_xmit() returns enum DP_TX_FN_RET */
 };
 
 /**
@@ -48,13 +71,13 @@ enum DP_TX_FLAGS {
  */
 /*! @brief datapath TX callback parameters */
 struct dp_tx_common {
-	u8 *pmac; /*!< PMAC */
-	u32 flags; /*!< bitmap of enum DP_TX_FLAGS */
-	u8 len; /*!< length of PMAC */
-	u8 dpid; /*!< datapath port ID */
-	u8 vap; /*!< datapath port subif vap */
-	u8 gpid; /*!< GPID */
-	struct dev_mib *mib; /*!< per vap MIB */
+	u32 subif;                    /*!< subif id */
+	u32 flags;                    /*!< bitmap of enum DP_TX_FLAGS */
+	int alloc_flags;              /*!< bitmap of enum DP_F_FLAG */
+	u8 *pmac;                     /*!< PMAC */
+	u8 pmac_len;                  /*!< length of PMAC */
+	u8 tx_portid;                 /*!< tx portid */
+	u8 inst;                      /*!< inst */
 };
 
 /**
diff --git a/include/net/datapath_inst.h b/include/net/datapath_inst.h
index 76c32928c035..e1d6ca6a56ef 100644
--- a/include/net/datapath_inst.h
+++ b/include/net/datapath_inst.h
@@ -53,6 +53,7 @@ struct dp_inst_info {
 	int qos_inst; /*! QOS instance for this DP instance*/
 };
 
+struct dp_tx_common;
 struct dp_tc_vlan_info;
 struct inst_info {
 	enum DP_HW_CAP_TYPE type;
@@ -126,9 +127,7 @@ struct inst_info {
 	int (*dp_set_bp_attr)(struct dp_bp_attr *bp_attr, int bport, u32 flags);
 
 	int32_t (*dp_rx)(struct sk_buff *skb, uint32_t flags);
-	int32_t (*dp_tx)(struct net_device *rx_if, dp_subif_t *rx_subif,
-			 struct sk_buff *skb, int32_t len, uint32_t flags);
-
+	int32_t (*dp_tx)(struct sk_buff *skb, struct dp_tx_common *cmn);
 	void (*dp_net_dev_get_ss_stat_strings)(struct net_device *dev,
 					       u8 *data);
 	int (*dp_net_dev_get_ss_stat_strings_count)(struct net_device *dev);
