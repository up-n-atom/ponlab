From b5744bceeed031d59b525735bb03c3816d6fb9bd Mon Sep 17 00:00:00 2001
From: Rekha Eswaran <rekha.eswaran@intel.com>
Date: Tue, 19 Jan 2021 16:16:09 +0800
Subject: [PATCH] DRV_DPM_SW-436 - DPM move unrelated instance API's to misc
 file

---
 drivers/net/datapath/dpm/datapath.h              |  3 ++
 drivers/net/datapath/dpm/datapath_api.c          | 14 +++----
 drivers/net/datapath/dpm/datapath_instance.c     | 53 ------------------------
 drivers/net/datapath/dpm/datapath_instance.h     |  5 ---
 drivers/net/datapath/dpm/datapath_misc.c         | 53 ++++++++++++++++++++++++
 drivers/net/datapath/dpm/gswip31/datapath_misc.c |  6 +--
 6 files changed, 66 insertions(+), 68 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath.h b/drivers/net/datapath/dpm/datapath.h
index cd3fe7c663aa..527e7c6045bc 100644
--- a/drivers/net/datapath/dpm/datapath.h
+++ b/drivers/net/datapath/dpm/datapath.h
@@ -1121,6 +1121,9 @@ int dp_init_pce(void);
 int proc_pce_dump(struct seq_file *s, int pos);
 ssize_t proc_pce_write(struct file *file, const char *buf, size_t count,
 		       loff_t *ppos);
+int dp_ctp_dev_list_add(struct list_head *head, struct net_device *dev, int bp,
+			int vap);
+int dp_ctp_dev_list_del(struct list_head *head, struct net_device *dev);
 
 #endif /*DATAPATH_H */
 
diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index b327e7dccc16..d937c8445166 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -1088,8 +1088,8 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 				bp_info->ref_cnt = 1;
 				bp_info->flag = 1;
 				INIT_LIST_HEAD(&bp_info->ctp_dev);
-				ctp_dev_list_add(&bp_info->ctp_dev,
-						 info->new_ctp_dev, bp, vap);
+				dp_ctp_dev_list_add(&bp_info->ctp_dev,
+						    info->new_ctp_dev, bp, vap);
 			}
 			dp_inst_add_dev(info->new_dev, info->inst,
 					info->dp_port, bp, vap, 0);
@@ -1111,8 +1111,8 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 				DP_DEBUG(DP_DBG_FLAG_REG,
 					 "update bp refcnt:%d\n",
 					 bp_info->ref_cnt);
-				ctp_dev_list_add(&bp_info->ctp_dev,
-						 info->new_ctp_dev, bp, vap);
+				dp_ctp_dev_list_add(&bp_info->ctp_dev,
+						    info->new_ctp_dev, bp, vap);
 			}
 		}
 		/* flag set to clear old dp dev information */
@@ -1127,8 +1127,8 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 				    &platfrm_data);
 		if (res) {
 			if (info->new_ctp_dev)
-				ctp_dev_list_del(&bp_info->ctp_dev,
-						 info->new_ctp_dev);
+				dp_ctp_dev_list_del(&bp_info->ctp_dev,
+						    info->new_ctp_dev);
 			goto exit;
 		}
 	}
@@ -1136,7 +1136,7 @@ int32_t dp_update_subif_info(struct dp_subif_upd_info *info)
 	if (old_ctp_dev) {
 		if (old_ctp_dev != info->new_ctp_dev) {
 			/* remove the CTP dev from pmapper */
-			ctp_dev_list_del(&bp_info->ctp_dev, old_ctp_dev);
+			dp_ctp_dev_list_del(&bp_info->ctp_dev, old_ctp_dev);
 			bp_info->ref_cnt--;
 			if (!bp_info->ref_cnt) {
 				bp_info->dev = NULL;
diff --git a/drivers/net/datapath/dpm/datapath_instance.c b/drivers/net/datapath/dpm/datapath_instance.c
index 6504a7f41026..8cd24a21d2aa 100644
--- a/drivers/net/datapath/dpm/datapath_instance.c
+++ b/drivers/net/datapath/dpm/datapath_instance.c
@@ -243,59 +243,6 @@ static void dp_check_and_free_dp_dev(struct net_device *dev)
 	}
 }
 
-int ctp_dev_list_add(struct list_head *head, struct net_device *dev, int bp,
-		     int vap)
-{
-	struct ctp_dev *ctp_dev_list;
-
-	ctp_dev_list = ctp_dev_list_lookup(head, dev);
-	if (!ctp_dev_list) {
-		ctp_dev_list = kzalloc(sizeof(*ctp_dev_list), GFP_ATOMIC);
-		if (!ctp_dev_list) {
-			DP_DEBUG(DP_DBG_FLAG_DBG, "kmalloc fail %zd bytes\n",
-				 sizeof(*ctp_dev_list));
-			return -1;
-		}
-		ctp_dev_list->dev = dev;
-		ctp_dev_list->bp = bp;
-		ctp_dev_list->ctp = vap;
-		DP_DEBUG(DP_DBG_FLAG_DBG, "add ctp dev list\n");
-		list_add(&ctp_dev_list->list, head);
-	} else {
-		DP_DEBUG(DP_DBG_FLAG_DBG, "Device already exist: %s\n",
-			 ctp_dev_list->dev ? ctp_dev_list->dev->name : "NULL");
-	}
-	return 0;
-}
-
-int ctp_dev_list_del(struct list_head *head, struct net_device *dev)
-{
-	struct ctp_dev *ctp_dev_list;
-
-	ctp_dev_list = ctp_dev_list_lookup(head, dev);
-	if (!ctp_dev_list) {
-		DP_DEBUG(DP_DBG_FLAG_DBG, "Device not found: %s\n",
-			 dev ? dev->name : "NULL");
-		return -1;
-	}
-	DP_DEBUG(DP_DBG_FLAG_DBG, "del ctp dev list\n");
-	list_del(&ctp_dev_list->list);
-	kfree(ctp_dev_list);
-	return 0;
-}
-
-struct ctp_dev *ctp_dev_list_lookup(struct list_head *head,
-				    struct net_device *dev)
-{
-	struct ctp_dev *pos;
-
-	list_for_each_entry(pos, head, list) {
-		if (pos->dev == dev)
-			return pos;
-	}
-	return NULL;
-}
-
 int dp_inst_add_dev(struct net_device *dev, int inst,
 		    int ep, int bp, int ctp, u32 flag)
 {
diff --git a/drivers/net/datapath/dpm/datapath_instance.h b/drivers/net/datapath/dpm/datapath_instance.h
index 3da3064dcb17..4101a32da15a 100644
--- a/drivers/net/datapath/dpm/datapath_instance.h
+++ b/drivers/net/datapath/dpm/datapath_instance.h
@@ -26,8 +26,6 @@ extern struct dp_hw_cap hw_cap_list[DP_MAX_HW_CAP];
 extern struct hlist_head dp_dev_list[DP_DEV_HASH_SIZE];
 u32 dp_dev_hash(void *dev);
 struct dp_dev *dp_dev_lookup(struct net_device *dev);
-struct ctp_dev *ctp_dev_list_lookup(struct list_head *head,
-				    struct net_device *dev);
 
 struct dp_mod {
 	struct hlist_node hlist;
@@ -71,9 +69,6 @@ int dp_inst_del_mod(struct module *owner, u16 ep, u32 flag);
 int dp_set_net_dev_ops_priv(struct net_device *dev, void *ops_cb,
 			    int ops_offset, u32 flag);
 
-int ctp_dev_list_add(struct list_head *head, struct net_device *dev, int bp,
-		     int vap);
-int ctp_dev_list_del(struct list_head *head, struct net_device *dev);
 int proc_inst_dev_dump(struct seq_file *s, int pos);
 int proc_inst_dev_start(void);
 int proc_inst_mod_dump(struct seq_file *s, int pos);
diff --git a/drivers/net/datapath/dpm/datapath_misc.c b/drivers/net/datapath/dpm/datapath_misc.c
index 68f2c806ace0..4ea87b178bfb 100644
--- a/drivers/net/datapath/dpm/datapath_misc.c
+++ b/drivers/net/datapath/dpm/datapath_misc.c
@@ -1524,3 +1524,56 @@ int dp_getopt(char *cmd[], int cmd_size, int *cmd_offset,
 	}
 }
 EXPORT_SYMBOL(dp_getopt);
+
+static struct ctp_dev *dp_ctp_dev_list_lookup(struct list_head *head,
+					      struct net_device *dev)
+{
+	struct ctp_dev *pos;
+
+	list_for_each_entry(pos, head, list) {
+		if (pos->dev == dev)
+			return pos;
+	}
+	return NULL;
+}
+
+int dp_ctp_dev_list_add(struct list_head *head, struct net_device *dev, int bp,
+			int vap)
+{
+	struct ctp_dev *ctp_dev_list;
+
+	ctp_dev_list = dp_ctp_dev_list_lookup(head, dev);
+	if (!ctp_dev_list) {
+		ctp_dev_list = kzalloc(sizeof(*ctp_dev_list), GFP_ATOMIC);
+		if (!ctp_dev_list) {
+			pr_err("%s alloc ctp_dev_list fail\n", __func__);
+			return DP_FAILURE;
+		}
+		ctp_dev_list->dev = dev;
+		ctp_dev_list->bp = bp;
+		ctp_dev_list->ctp = vap;
+		DP_DEBUG(DP_DBG_FLAG_DBG, "add ctp dev list\n");
+		list_add(&ctp_dev_list->list, head);
+	} else {
+		DP_DEBUG(DP_DBG_FLAG_DBG, "ctp dev list exist: %s\n",
+			 ctp_dev_list->dev ? ctp_dev_list->dev->name : "NULL");
+	}
+	return DP_SUCCESS;
+}
+
+int dp_ctp_dev_list_del(struct list_head *head, struct net_device *dev)
+{
+	struct ctp_dev *ctp_dev_list;
+
+	ctp_dev_list = dp_ctp_dev_list_lookup(head, dev);
+	if (!ctp_dev_list) {
+		pr_err("%s ctp dev(%s) not found\n", __func__,
+		       dev ? dev->name : "NULL");
+		return DP_FAILURE;
+	}
+	DP_DEBUG(DP_DBG_FLAG_DBG, "del ctp dev list\n");
+	list_del(&ctp_dev_list->list);
+	kfree(ctp_dev_list);
+	return DP_SUCCESS;
+}
+
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index e34eed83d6a6..ca4c2fd41833 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -2206,7 +2206,7 @@ void subif_pmapper_cfg(int inst, struct subif_platform_data *data,
 	bp_info = get_dp_bp_info(inst, bp);
 
 	if (flags & DP_F_DEREGISTER) {
-		ctp_dev_list_del(&bp_info->ctp_dev, sif->ctp_dev);
+		dp_ctp_dev_list_del(&bp_info->ctp_dev, sif->ctp_dev);
 		sif->ctp_dev = NULL;
 		bp_info->ref_cnt--;
 		if (!bp_info->ref_cnt) {
@@ -2226,8 +2226,8 @@ void subif_pmapper_cfg(int inst, struct subif_platform_data *data,
 		bp_info->ref_cnt++;
 		bp_info->flag = 1;
 		sif->ctp_dev = data->subif_data->ctp_dev;
-		ctp_dev_list_add(&bp_info->ctp_dev, sif->ctp_dev, bp,
-				 subif_idx);
+		dp_ctp_dev_list_add(&bp_info->ctp_dev, sif->ctp_dev, bp,
+				    subif_idx);
 	}
 
 	DP_DEBUG(DP_DBG_FLAG_REG,
