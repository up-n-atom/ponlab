From b88f29d6f60e68608ce80fa50409fedea6259309 Mon Sep 17 00:00:00 2001
From: Rekha Eswaran <rekha.eswaran@intel.com>
Date: Fri, 24 Apr 2020 15:34:01 +0800
Subject: [PATCH] DRV_DPM_SW-69 - dpm swdev enhancement to directly use info
 from pmac_port_info

---
 drivers/net/datapath/dpm/datapath_swdev.c          | 105 +++++++++------------
 drivers/net/datapath/dpm/datapath_swdev.h          |  13 +--
 drivers/net/datapath/dpm/gswip31/datapath_misc.c   |  54 ++++++-----
 .../net/datapath/dpm/gswip31/datapath_switchdev.c  |  49 +++++-----
 .../net/datapath/dpm/gswip31/datapath_switchdev.h  |   2 +-
 include/net/datapath_inst.h                        |   2 +-
 6 files changed, 105 insertions(+), 120 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath_swdev.c b/drivers/net/datapath/dpm/datapath_swdev.c
index e818133a494c..e07dfb36690d 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.c
+++ b/drivers/net/datapath/dpm/datapath_swdev.c
@@ -18,9 +18,9 @@
 static void dp_swdev_insert_bridge_id_entry(struct br_info *);
 static void dp_swdev_remove_bridge_id_entry(struct br_info *);
 static int dp_swdev_add_bport_to_list(struct br_info *br_item,
-				      int bport);
+				      struct bridge_id_entry_item *brid_item);
 static int dp_swdev_del_bport_from_list(struct br_info *br_item,
-					int bport, u16 *hairpin_port);
+					int bport);
 
 struct hlist_head
 	g_bridge_id_entry_hash_table[DP_MAX_INST][BR_ID_ENTRY_HASH_TABLE_SIZE];
@@ -105,7 +105,7 @@ int dp_swdev_chk_bport_in_br(struct net_device *bp_dev, int bport, int inst)
 	if (!br_info)
 		return -1;
 	list_for_each_entry(temp_list, &br_info->bp_list, list) {
-		if (temp_list->portid == bport) {
+		if (temp_list->bportid == bport) {
 			found = 1;
 			DP_DEBUG(DP_DBG_FLAG_SWDEV, "bport(%s) in bridge\n",
 				 bp_dev->name ? bp_dev->name : "NULL");
@@ -193,15 +193,14 @@ int dp_swdev_bridge_id_entry_init(void)
 }
 
 static int dp_swdev_del_bport_from_list(struct br_info *br_item,
-					int bport, u16 *hairpin_bport)
+					int bport)
 {
 	int found = 0;
 	struct bridge_member_port *temp_list = NULL;
 
 	list_for_each_entry(temp_list, &br_item->bp_list, list) {
-		if (temp_list->portid == bport) {
+		if (temp_list->bportid == bport) {
 			found = 1;
-			*hairpin_bport = temp_list->hairpin;
 			break;
 		}
 	}
@@ -214,14 +213,14 @@ static int dp_swdev_del_bport_from_list(struct br_info *br_item,
 }
 
 static int dp_swdev_add_bport_to_list(struct br_info *br_item,
-				      int bport)
+				      struct bridge_id_entry_item *brid_item)
 {
 	int found = 0;
 	struct bridge_member_port *bport_list = NULL;
 	struct bridge_member_port *temp_list = NULL;
 
 	list_for_each_entry(temp_list, &br_item->bp_list, list) {
-		if (temp_list->portid == bport) {
+		if (temp_list->bportid == brid_item->bportid) {
 			found = 1;
 			break;
 		}
@@ -236,14 +235,12 @@ static int dp_swdev_add_bport_to_list(struct br_info *br_item,
 			return -1;
 		}
 		bport_list->dev_reg_flag = br_item->flag;
-		bport_list->portid = bport;
-		bport_list->no_loop_flag = br_item->no_loop_flag;
-		bport_list->hairpin = br_item->hairpin;
-		bport_list->dev_priv_flag = br_item->bp_priv_flag;
+		bport_list->bportid = brid_item->bportid;
+		bport_list->dev_priv_flag = brid_item->priv_flag;
 		bport_list->dp_port = br_item->dp_port;
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "bport:%d reg_flag:%d priv_flag:0x%x\n",
-			 bport_list->portid, bport_list->dev_reg_flag,
+			 bport_list->bportid, bport_list->dev_reg_flag,
 			 bport_list->dev_priv_flag);
 		list_add(&bport_list->list, &br_item->bp_list);
 	}
@@ -255,8 +252,10 @@ static int dp_swdev_clr_gswip_cfg(struct bridge_id_entry_item *br_item,
 {
 	struct br_info *br_info;
 	struct inst_info *i_info;
-	u16 hairpin = 0;
 	int ret;
+	struct pmac_port_info *p_info;
+
+	p_info = get_dp_port_info(br_item->inst, br_item->dp_port);
 
 	if (br_item->flags == BRIDGE_NO_ACTION) {
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
@@ -266,19 +265,20 @@ static int dp_swdev_clr_gswip_cfg(struct bridge_id_entry_item *br_item,
 	br_info = dp_swdev_bridge_entry_lookup(br_item->br_device_name);
 	if (!br_info)
 		return 0;
-	if (!dp_swdev_del_bport_from_list(br_info, br_item->portid, &hairpin))
+	if (!dp_swdev_del_bport_from_list(br_info, br_item->bportid))
 		return 0;
 	i_info = &dp_port_prop[br_item->inst].info;
 	ret = i_info->swdev_bridge_port_cfg_reset(br_info, br_item->inst,
-						  br_item->portid, hairpin);
+						  br_item->bportid,
+						  p_info->bp_hairpin_cap);
 	if (ret == DEL_BRENTRY) {
 		i_info->swdev_free_brcfg(br_item->inst, br_item->fid);
 		dp_swdev_remove_bridge_id_entry(br_info);
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "last bport(%d) to remove from bridge(%s)\n",
-			 br_item->portid, br_item->br_device_name);
+			 br_item->bportid, br_item->br_device_name);
 	}
-	DP_DEBUG(DP_DBG_FLAG_SWDEV, "rem bport(%d)\n", br_item->portid);
+	DP_DEBUG(DP_DBG_FLAG_SWDEV, "rem bport(%d)\n", br_item->bportid);
 	return 0;
 }
 
@@ -317,7 +317,7 @@ static int dp_swdev_cfg_vlan(struct bridge_id_entry_item *br_item,
 			conf.inst = inst;
 			conf.dev = dev;
 			conf.en = 0;
-			i_info->dp_set_bp_attr(&conf, br_item->portid, 0);
+			i_info->dp_set_bp_attr(&conf, br_item->bportid, 0);
 		}
 		i_info->dp_cfg_vlan(inst, vap, dp_dev->ep);
 	}
@@ -343,11 +343,10 @@ static int dp_swdev_filter_vlan(struct net_device *dev,
 		br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item),
 				       GFP_ATOMIC);
 		if (!br_item)
-			//TODO need to check dequeue if no memory
 			return -ENOMEM;
 		br_item->inst = subif.inst;
 		/* current bridge member port*/
-		br_item->portid = subif.bport;
+		br_item->bportid = subif.bport;
 		swdev_lock();
 		br_info = dp_swdev_bridge_entry_lookup(br_dev->name);
 		if (br_info) {
@@ -391,15 +390,10 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 			br_info = devm_kzalloc(&g_dp_dev->dev,
 					       sizeof(*br_info), GFP_ATOMIC);
 			if (!br_info)
-				/*TODO need to check return value
-				 *for switchdev commit
-				 */
+				/* cannot return failure in switchdev commit */
 				return 0;
 			br_info->fid = br_item->fid;
 			br_info->inst = br_item->inst;
-			br_info->no_loop_flag = br_item->no_loop_flag;
-			br_info->hairpin = br_item->hairpin;
-			br_info->bp_priv_flag = br_item->priv_flag;
 			br_info->dp_port = br_item->dp_port;
 
 			/* Logic dev flag added to verify if SWDEV registered
@@ -413,16 +407,15 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 			       br_item->br_device_name);
 			INIT_LIST_HEAD(&br_info->bp_list);
 			dp_swdev_insert_bridge_id_entry(br_info);
-			dp_swdev_add_bport_to_list(br_info, br_item->portid);
+			dp_swdev_add_bport_to_list(br_info, br_item);
 
 			i_info->swdev_bridge_port_cfg_set(br_info,
 							  br_item->inst,
-							  br_item->portid,
-							  dev->priv_flags);
+							  br_item->bportid);
 			br_item->flags &= ~ADD_BRENTRY;
 			DP_DEBUG(DP_DBG_FLAG_SWDEV,
 				 "added bport(%d),bridge(%s)\n",
-				 br_item->portid,
+				 br_item->bportid,
 				 br_info->br_device_name);
 			return 0;
 		}
@@ -431,19 +424,15 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 		if (!br_info)
 			return 0;
 		br_info->flag = 0;
-		br_info->no_loop_flag = br_item->no_loop_flag;
-		br_info->hairpin = br_item->hairpin;
-		br_info->bp_priv_flag = br_item->priv_flag;
 		br_info->dp_port = br_item->dp_port;
 
 		if (br_item->flags & LOGIC_DEV_REGISTER)
 			br_info->flag = LOGIC_DEV_REGISTER;
-		dp_swdev_add_bport_to_list(br_info, br_item->portid);
+		dp_swdev_add_bport_to_list(br_info, br_item);
 		i_info->swdev_bridge_port_cfg_set(br_info, br_item->inst,
-						  br_item->portid,
-						  dev->priv_flags);
+						  br_item->bportid);
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "added bport(%d)\n",
-			 br_item->portid);
+			 br_item->bportid);
 		return 0;
 	}
 	DP_DEBUG(DP_DBG_FLAG_SWDEV, "No configuration,Pls check!!\n");
@@ -476,7 +465,7 @@ static int dp_del_swdev_reg_vlan(struct bridge_id_entry_item *br_item,
 		dp_swdev_cfg_vlan(br_item, dev);
 		swdev_unlock();
 		if (dp_get_netif_subifid(base, NULL, NULL,
-					NULL, &subif, 0)) {
+					 NULL, &subif, 0)) {
 			pr_err("dp_get_netif_subifid fail:%s(%s)\n",
 			       base->name, __func__);
 			/*Cannot Return -EOPNOTSUPP
@@ -545,8 +534,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 		p_info = get_dp_port_info(inst, port);
 		if (dp_register_subif(p_info->owner, dev, dev->name,
 				      &subif, DP_F_SUBIF_LOGICAL)) {
-			pr_err("dp_register_subif fail: %s\n",
-					dev->name);
+			pr_err("dp_register_subif fail: %s\n", dev->name);
 			return -EOPNOTSUPP;
 		}
 		flag = LOGIC_DEV_REGISTER;
@@ -563,11 +551,11 @@ static int dp_swdev_add_if(struct net_device *dev,
 		br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item),
 				       GFP_ATOMIC);
 		if (!br_item)
-			/*TODO need to check dequeue if no memory*/
+			/*need to check dequeue if no memory*/
 			return -ENOMEM;
 		br_item->inst = subif.inst;
 		/* current bridge member port*/
-		br_item->portid = subif.bport;
+		br_item->bportid = subif.bport;
 		br_item->dp_port = subif.port_id;
 		/* dev->priv_flags should be set only for base dev */
 		if (!(flag & LOGIC_DEV_REGISTER))
@@ -575,8 +563,6 @@ static int dp_swdev_add_if(struct net_device *dev,
 
 		swdev_lock();
 		br_info = dp_swdev_bridge_entry_lookup(br_dev->name);
-		br_item->no_loop_flag = p_info->loop_dis;
-		br_item->hairpin = p_info->bp_hairpin_cap;
 		if (!sif->cpu_port_en)
 			br_item->flags |= CPU_PORT_DISABLE;
 		if (br_info) {
@@ -609,9 +595,9 @@ static int dp_swdev_add_if(struct net_device *dev,
 				br_item->fid = br_id;
 			}
 		}
+		swdev_unlock();
 		switchdev_trans_item_enqueue(trans, br_item,
 					     dp_kfree, &br_item->tritem);
-		swdev_unlock();
 		return 0;
 	}
 	/*configure switch in commit phase and it cannot return failure*/
@@ -651,8 +637,7 @@ static int dp_swdev_del_if(struct net_device *dev,
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "dp_get_netif_subifid failed for %s\n",
 			 dev->name);
-	/*	if (!is_vlan_dev(dev))*/
-			return -EINVAL;
+		return -EINVAL;
 	}
 	p_info = get_dp_port_info(subif.inst, subif.port_id);
 	vap = GET_VAP(subif.subif, p_info->vap_offset,
@@ -671,7 +656,7 @@ static int dp_swdev_del_if(struct net_device *dev,
 			br_item->fid = br_info->fid;
 			br_item->inst = subif.inst;
 			/* current bridge member port*/
-			br_item->portid = subif.bport;
+			br_item->bportid = subif.bport;
 			br_item->dp_port = subif.port_id;
 			br_item->flags = BRIDGE_NO_ACTION;
 			strcpy(br_item->br_device_name,
@@ -679,7 +664,7 @@ static int dp_swdev_del_if(struct net_device *dev,
 
 			list_for_each_entry(temp_list, &br_info->bp_list,
 					    list) {
-				if (temp_list->portid == br_item->portid) {
+				if (temp_list->bportid == br_item->bportid) {
 					br_item->flags =
 						temp_list->dev_reg_flag;
 					break;
@@ -700,10 +685,10 @@ static int dp_swdev_del_if(struct net_device *dev,
 				br_item->flags = BRIDGE_NO_ACTION;
 			}
 		}
-		/*TODO check return value & enqueue*/
+		swdev_unlock();
+		/*check return value & enqueue*/
 		switchdev_trans_item_enqueue(trans, br_item, dp_kfree,
 					     &br_item->tritem);
-		swdev_unlock();
 		return 0;
 	}
 	if (flags == DP_SWDEV_BRIDGE_ITEM)
@@ -731,11 +716,11 @@ int dp_del_br_if(struct net_device *dev, struct net_device *br_dev,
 	if (br_info) {
 		br_item->fid = br_info->fid;
 		br_item->inst = inst;
-		br_item->portid = bport;
+		br_item->bportid = bport;
 		strcpy(br_item->br_device_name, br_info->br_device_name);
 
 		list_for_each_entry(temp_list, &br_info->bp_list, list) {
-			if (temp_list->portid == br_item->portid) {
+			if (temp_list->bportid == br_item->bportid) {
 				br_item->flags =
 					temp_list->dev_reg_flag;
 				break;
@@ -748,7 +733,7 @@ int dp_del_br_if(struct net_device *dev, struct net_device *br_dev,
 				BRIDGE_NO_ACTION;
 		} else {
 			DP_DEBUG(DP_DBG_FLAG_SWDEV,
-					"No bridge entry or bport to delete\r\n");
+				 "No bridge entry or bport to delete\r\n");
 			br_item->flags = BRIDGE_NO_ACTION;
 		}
 	}
@@ -773,7 +758,7 @@ static int dp_swdev_port_attr_bridge_flags_set(struct net_device *dev,
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "dp_get_netif_subifid failed for %s\n",
 			 dev->name);
-			return -EINVAL;
+		return -EINVAL;
 	}
 
 	i_info = &dp_port_prop[subif.inst].info;
@@ -797,7 +782,7 @@ static int dp_swdev_port_attr_bridge_flags_get(struct net_device *dev,
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "dp_get_netif_subifid failed for %s\n",
 			 dev->name);
-			return -EINVAL;
+		return -EINVAL;
 	}
 
 	i_info = &dp_port_prop[subif.inst].info;
@@ -930,15 +915,12 @@ static int dp_swdev_port_attr_get(struct net_device *dev,
 		br_info = dp_swdev_bridge_entry_lookup(br_dev->name);
 		if (!br_info)
 			return 0;
-		if (br_info->fid < 0)
-			return -EOPNOTSUPP;
 		attr->u.ppid.id_len = sizeof(br_info->fid);
 		memcpy(&attr->u.ppid.id, &br_info->fid, attr->u.ppid.id_len);
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "SWITCHDEV_ATTR_ID_PORT_PARENT_ID:%s fid=%d\n",
 			 attr->orig_dev ? attr->orig_dev->name : "NULL",
 			 br_info->fid);
-		//err = 0;
 		break;
 	case SWITCHDEV_ATTR_ID_PORT_BRIDGE_FLAGS:
 		err =
@@ -1024,8 +1006,7 @@ int dp_notif_br_alloc(struct net_device *br_dev)
 }
 
 /* This function registers the created port in datapath to switchdev */
-int dp_register_switchdev_ops(struct dp_dev  *dp_dev,
-			      struct net_device *dev)
+int dp_register_switchdev_ops(struct dp_dev  *dp_dev, struct net_device *dev)
 {
 	int err = DP_SUCCESS;
 	struct inst_info *inst_info = get_dp_prop_info(dp_dev->inst);
diff --git a/drivers/net/datapath/dpm/datapath_swdev.h b/drivers/net/datapath/dpm/datapath_swdev.h
index b26e43209841..b7b1f8eaf4d8 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.h
+++ b/drivers/net/datapath/dpm/datapath_swdev.h
@@ -29,12 +29,10 @@ struct bridge_id_entry_item {
 	u8 br_device_name[IFNAMSIZ];
 	u16 fid;
 	u32 flags;
-	u32 portid; /*Bridge port*/
+	u32 bportid; /*Bridge port*/
 	u32 dp_port; /*DP port*/
 	u32 inst;
-	u32 priv_flag;
-	u32 no_loop_flag; /* same as loop_dis flag during dp_alloc_port*/
-	u32 hairpin; /* to store if dev has special bridge member config */
+	u32 priv_flag; /* to store bp dev priv flags (like IFF_NO_QUEUE) */
 	struct switchdev_trans_item tritem;
 };
 
@@ -44,22 +42,17 @@ struct br_info {
 	u16 fid;
 	u32 flag;
 	u32 inst;
-	u32 bp_priv_flag; /* to store bp dev priv flags (like IFF_NO_QUEUE) */
-	u32 no_loop_flag; /* same as loop_dis flag during dp_alloc_port*/
-	u32 hairpin; /* to store if dev has special bridge member config */
 	u32 dp_port;
 	struct list_head bp_list;
 };
 
 struct bridge_member_port {
 	struct list_head list;
-	u32 portid; /* bridge port */
+	u32 bportid; /* bridge port */
 	u32 dev_reg_flag;
 	u16 bport[8];
 	u32 dev_priv_flag; /* to store bp dev priv flags (like IFF_NO_QUEUE) */
 	u32 dp_port;
-	u32 no_loop_flag; /* same as loop_dis flag during dp_alloc_port*/
-	u32 hairpin; /* if bridge port needs special bridge member config */
 };
 
 struct fdb_tbl {
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 3b9a200eddb5..297f102a126b 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -2259,12 +2259,16 @@ static int get_itf_start_end(struct gsw_itf *itf_info, u16 *start, u16 *end)
 	return 0;
 }
 
-static void dp_get_gswip_cap(struct dp_hw_cap *cap, int flag)
+static int dp_get_gswip_cap(struct dp_hw_cap *cap, int flag)
 {
 	struct core_ops *gsw_ops;
 	GSW_cap_t parm;
 
 	gsw_ops = gsw_get_swcore_ops(0);
+	if (!gsw_ops) {
+		pr_err("%s swcore ops is NULL\n", __func__);
+		return DP_FAILURE;
+	}
 
 	/* Max number of CTP */
 	parm.nCapType = GSW_CAP_TYPE_CTP;
@@ -2308,27 +2312,28 @@ static void dp_get_gswip_cap(struct dp_hw_cap *cap, int flag)
 	cap->info.cap.hw_ptp = 1;
 
 	DP_DEBUG(DP_DBG_FLAG_REG, "max_num_subif            = %d\n"
-	       "max_num_bridge_port      = %d\n"
-	       "max_eth_port             = %d\n"
-	       "max_virt_eth_port        = %d\n"
-	       "max_num_dp_ports         = %d\n"
-	       "vap_offset 8-bit Mode    = %d\n"
-	       "vap_mask 8-bit Mode      = %d\n"
-	       "vap_offset 9-bit Mode    = %d\n"
-	       "vap_mask 9-bit Mode      = %d\n"
-	       "vap_offset Non-WLAN mode = %d\n"
-	       "vap_mask Non-WLAN mode   = %d\n",
-	       cap->info.cap.max_num_subif,
-	       cap->info.cap.max_num_bridge_port,
-	       cap->info.cap.max_eth_port,
-	       cap->info.cap.max_virt_eth_port,
-	       cap->info.cap.max_num_dp_ports,
-	       cap->info.cap.vap_offset[GSW_LOGICAL_PORT_8BIT_WLAN],
-	       cap->info.cap.vap_mask[GSW_LOGICAL_PORT_8BIT_WLAN],
-	       cap->info.cap.vap_offset[GSW_LOGICAL_PORT_9BIT_WLAN],
-	       cap->info.cap.vap_mask[GSW_LOGICAL_PORT_9BIT_WLAN],
-	       cap->info.cap.vap_offset[GSW_LOGICAL_PORT_GPON],
-	       cap->info.cap.vap_mask[GSW_LOGICAL_PORT_GPON]);
+		 "max_num_bridge_port      = %d\n"
+		 "max_eth_port             = %d\n"
+		 "max_virt_eth_port        = %d\n"
+		 "max_num_dp_ports         = %d\n"
+		 "vap_offset 8-bit Mode    = %d\n"
+		 "vap_mask 8-bit Mode      = %d\n"
+		 "vap_offset 9-bit Mode    = %d\n"
+		 "vap_mask 9-bit Mode      = %d\n"
+		 "vap_offset Non-WLAN mode = %d\n"
+		 "vap_mask Non-WLAN mode   = %d\n",
+		 cap->info.cap.max_num_subif,
+		 cap->info.cap.max_num_bridge_port,
+		 cap->info.cap.max_eth_port,
+		 cap->info.cap.max_virt_eth_port,
+		 cap->info.cap.max_num_dp_ports,
+		 cap->info.cap.vap_offset[GSW_LOGICAL_PORT_8BIT_WLAN],
+		 cap->info.cap.vap_mask[GSW_LOGICAL_PORT_8BIT_WLAN],
+		 cap->info.cap.vap_offset[GSW_LOGICAL_PORT_9BIT_WLAN],
+		 cap->info.cap.vap_mask[GSW_LOGICAL_PORT_9BIT_WLAN],
+		 cap->info.cap.vap_offset[GSW_LOGICAL_PORT_GPON],
+		 cap->info.cap.vap_mask[GSW_LOGICAL_PORT_GPON]);
+	return DP_SUCCESS;
 }
 
 int register_dp_cap_gswip31(int flag)
@@ -2428,7 +2433,10 @@ int register_dp_cap_gswip31(int flag)
 	cap.info.cap.max_num_qos_ports = MAX_PPV4_PORT;
 	cap.info.cap.max_num_subif_per_port = MAX_SUBIF_PER_PORT;
 
-	dp_get_gswip_cap(&cap, flag);
+	if (dp_get_gswip_cap(&cap, flag)) {
+		pr_err("dp_get_gswip_cap fail\n");
+		return -1;
+	}
 
 	if (register_dp_hw_cap(&cap, flag)) {
 		pr_err("Why register_dp_hw_cap fail\n");
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
index 8dae0c28247f..110e0fa938b1 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
@@ -35,7 +35,7 @@ int dp_swdev_get_bport_dp_port(struct br_info *br_item,	int bport,
 	struct bridge_member_port *bport_list = NULL;
 
 	list_for_each_entry(bport_list, &br_item->bp_list, list) {
-		if (bport_list->portid == bport) {
+		if (bport_list->bportid == bport) {
 			*priv_flag = bport_list->dev_priv_flag;
 			return bport_list->dp_port;
 		}
@@ -44,7 +44,7 @@ int dp_swdev_get_bport_dp_port(struct br_info *br_item,	int bport,
 }
 
 int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
-				 int inst, int bport, u32 flags)
+				 int inst, int bport)
 {
 	GSW_return_t ret;
 	struct bridge_member_port *bport_list = NULL;
@@ -52,6 +52,9 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 	struct core_ops *gsw_handle;
 	struct brdgport_ops *gsw_bp;
 	u32 dport, priv_flag = 0;
+	struct pmac_port_info *p_info;
+
+	p_info = get_dp_port_info(br_item->inst, br_item->dp_port);
 
 	gsw_handle = dp_port_prop[inst].ops[0];
 	gsw_bp = &gsw_handle->gsw_brdgport_ops;
@@ -69,7 +72,7 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 	dport = dp_swdev_get_bport_dp_port(br_item, bport,
 					   &priv_flag);
 	list_for_each_entry(bport_list, &br_item->bp_list, list) {
-		if (bport_list->portid != bport) {
+		if (bport_list->bportid != bport) {
 			/* if member to be added for current bp,
 			 * share same dp_port with no loop set or
 			 * with IFF_NO_QUEUE set
@@ -77,23 +80,23 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 			 * then don't add the member to its portmap
 			 */
 			if ((bport_list->dev_priv_flag & IFF_NO_QUEUE) ||
-			    (bport_list->no_loop_flag &&
+			    (p_info->loop_dis &&
 			     bport_list->dp_port == dport)) {
 				DP_DEBUG(DP_DBG_FLAG_SWDEV, "%s%d to %d%s",
 					 "IFF_NO_QUEUE set Don't add BP=",
-					 bport_list->portid, bport,
+					 bport_list->bportid, bport,
 					 " bp map\n");
 				UNSET_BP_MAP(brportcfg.nBridgePortMap,
-					     bport_list->portid);
+					     bport_list->bportid);
 				continue;
 			}
 			DP_DEBUG(DP_DBG_FLAG_SWDEV, "%s%d to %d%s",
-				 "add BP=", bport_list->portid, bport,
+				 "add BP=", bport_list->bportid, bport,
 				 " bp map\n");
 			SET_BP_MAP(brportcfg.nBridgePortMap,
-				   bport_list->portid);
-		} else if (bport_list->portid == bport) {
-			if (bport_list->hairpin) {
+				   bport_list->bportid);
+		} else if (bport_list->bportid == bport) {
+			if (p_info->bp_hairpin_cap) {
 				SET_BP_MAP(brportcfg.nBridgePortMap,
 					   bport);
 				DP_DEBUG(DP_DBG_FLAG_SWDEV, "set SPL BP:%d\n",
@@ -119,10 +122,10 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 	}
 	/* To set other member portmap with current bridge port map */
 	list_for_each_entry(bport_list, &br_item->bp_list, list) {
-		if (bport_list->portid == bport)
+		if (bport_list->bportid == bport)
 			continue;
 		memset(&brportcfg, 0, sizeof(GSW_BRIDGE_portConfig_t));
-		brportcfg.nBridgePortId = bport_list->portid;
+		brportcfg.nBridgePortId = bport_list->bportid;
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "Set other BP=%d inst:%d\n",
 			 brportcfg.nBridgePortId, inst);
 		brportcfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
@@ -137,24 +140,24 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 		 * then dont add the interface to gswip bridge portmap
 		 */
 		if ((priv_flag & IFF_NO_QUEUE) ||
-		    (bport_list->no_loop_flag &&
+		    (p_info->loop_dis &&
 		     bport_list->dp_port == dport)) {
 			DP_DEBUG(DP_DBG_FLAG_SWDEV,
 				 "%s %d to %d %s",
 				 "IFF_NO_QUEUE/no_loop Don't add BP",
-				 bport, bport_list->portid,
+				 bport, bport_list->bportid,
 				 "bp map\n");
 			UNSET_BP_MAP(brportcfg.nBridgePortMap,
 				     bport);
 		} else {
 			DP_DEBUG(DP_DBG_FLAG_SWDEV,
 				 "set %d to %d %s",
-				 bport, bport_list->portid,
+				 bport, bport_list->bportid,
 				 "bp pmap\n");
 			SET_BP_MAP(brportcfg.nBridgePortMap, bport);
 		}
 		brportcfg.nBridgeId = br_item->fid;
-		brportcfg.nBridgePortId = bport_list->portid;
+		brportcfg.nBridgePortId = bport_list->bportid;
 		brportcfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID |
 				  GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
 		ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, &brportcfg);
@@ -201,7 +204,7 @@ int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 		return -1;
 	}
 	list_for_each_entry(bport_list, &br_item->bp_list, list) {
-		if (bport_list->portid)
+		if (bport_list->bportid)
 			cnt++;
 	}
 	DP_DEBUG(DP_DBG_FLAG_SWDEV, "cnt:%d last bp:%d\n", cnt, bp);
@@ -210,13 +213,13 @@ int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "reset spl BP %d\n", bport);
 	}
 	list_for_each_entry(bport_list, &br_item->bp_list, list) {
-		if (bport_list->portid != bport) {
+		if (bport_list->bportid != bport) {
 			DP_DEBUG(DP_DBG_FLAG_SWDEV,
 				 "reset BP %d from current BP=%d inst:%d\n",
-				 bport_list->portid,
+				 bport_list->bportid,
 				 brportcfg.nBridgePortId, inst);
 			UNSET_BP_MAP(brportcfg.nBridgePortMap,
-				     bport_list->portid);
+				     bport_list->bportid);
 		}
 	}
 	brportcfg.nBridgeId = CPU_FID; /*reset of FID*/
@@ -231,10 +234,10 @@ int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 	}
 	/*Reset current bp from all other bridge port's port map*/
 	list_for_each_entry(bport_list, &br_item->bp_list, list) {
-		if (bport_list->portid == bport)
+		if (bport_list->bportid == bport)
 			continue;
 		memset(&brportcfg, 0, sizeof(GSW_BRIDGE_portConfig_t));
-		brportcfg.nBridgePortId = bport_list->portid;
+		brportcfg.nBridgePortId = bport_list->bportid;
 		DP_DEBUG(DP_DBG_FLAG_SWDEV,
 			 "reset current BP(%d) from other BP=%d inst:%d\n",
 			 bport, brportcfg.nBridgePortId, inst);
@@ -246,7 +249,7 @@ int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 			return -1;
 		}
 		UNSET_BP_MAP(brportcfg.nBridgePortMap, bport);
-		brportcfg.nBridgePortId = bport_list->portid;
+		brportcfg.nBridgePortId = bport_list->bportid;
 		brportcfg.eMask = GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_ID |
 				  GSW_BRIDGE_PORT_CONFIG_MASK_BRIDGE_PORT_MAP;
 		ret = gsw_bp->BridgePort_ConfigSet(gsw_handle, &brportcfg);
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h
index 1da2b321a799..77b9241e816f 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.h
@@ -8,7 +8,7 @@
 
 int dp_swdev_alloc_bridge_id(int inst);
 int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
-				 int inst, int bport, u32 flags);
+				 int inst, int bport);
 int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 				   int inst, int bport, u16 hairpin);
 int dp_swdev_bridge_cfg_set(int inst, u16 fid);
diff --git a/include/net/datapath_inst.h b/include/net/datapath_inst.h
index fd8dccd29c45..77af3487c163 100644
--- a/include/net/datapath_inst.h
+++ b/include/net/datapath_inst.h
@@ -147,7 +147,7 @@ struct inst_info {
 	int (*swdev_free_brcfg)(int inst, u16 fid);
 	int (*swdev_bridge_cfg_set)(int inst, u16 fid);
 	int (*swdev_bridge_port_cfg_set)(struct br_info *br_item, int inst,
-					 int port, u32 flags);
+					 int port);
 	int (*swdev_bridge_port_cfg_reset)(struct br_info *br_item,
 					   int inst, int bport, u16 hairpin);
 	int (*swdev_bridge_port_flags_set)(int inst, int bport,
