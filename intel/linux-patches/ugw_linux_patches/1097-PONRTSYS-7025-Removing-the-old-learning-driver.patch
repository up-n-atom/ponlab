From d587d7a8b7db1bdd66efe96a10476ea32d7911a4 Mon Sep 17 00:00:00 2001
From: Elad Fernandes <elad.fernandes@intel.com>
Date: Mon, 23 Mar 2020 18:06:15 +0200
Subject: [PATCH] PONRTSYS-7025: Removing the old learning driver

---
 .../ppv4/pp-api/pp-session-mgr/pp_session_mgr.c    | 22 ++-----
 .../pp-api/pp-session-mgr/pp_session_mgr_debugfs.c |  2 +-
 .../pp-session-mgr/pp_session_mgr_internal.h       |  2 +-
 .../pp-api/pp-session-mgr/pp_session_mgr_tdox.c    | 13 ++--
 drivers/net/ppv4/pp-hal/field-vector/pp_fv.c       |  4 +-
 .../pp-tools/unit-test/tests/pp_session_tests.c    | 71 +++++++++++-----------
 include/linux/pp_api.h                             | 10 ---
 7 files changed, 51 insertions(+), 73 deletions(-)

diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
index 3eecf67d161c..bfe05f4046a9 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr.c
@@ -24,7 +24,6 @@
 #include <linux/workqueue.h>
 #include <linux/jiffies.h>
 #include <net/checksum.h>
-#include <linux/learning_layer_api.h> /* TBD - remove in phase3 */
 #include <linux/pp_api.h>
 #include <linux/pktprs.h>
 
@@ -2287,8 +2286,8 @@ static s32 __smgr_si_ipv4_nat_set(struct sess_info *s)
 	/* to reduce number of recipes in the modifier we always
 	 * modify the ip addresses
 	 */
-	si->dpu.nat_sz = FIELD_SIZEOF(struct fv_ipv4, saddr) +
-			 FIELD_SIZEOF(struct fv_ipv4, daddr);
+	si->dpu.nat_sz = FIELD_SIZEOF(struct pp_fv_ipv4, saddr) +
+			 FIELD_SIZEOF(struct pp_fv_ipv4, daddr);
 	si->bce.nat.v4.saddr = tx_ip->saddr;
 	si->bce.nat.v4.daddr = tx_ip->daddr;
 
@@ -3054,15 +3053,6 @@ __smgr_sess_cr_args_prepare(struct pp_request *req,
 	/* set arguments */
 	if (PP_IS_SYNC_REQUEST(req)) {
 		work_args->args = sess_args;
-		/* TBD - after phase3, replace this with sess_args->rx/tx */
-		if (sess_args->in_pkt)
-			work_args->args->rx = sess_args->in_pkt->h;
-		else
-			work_args->args->rx = NULL;
-		if (sess_args->eg_pkt)
-			work_args->args->tx = sess_args->eg_pkt->h;
-		else
-			work_args->args->tx = NULL;
 		return work_args;
 	}
 
@@ -3070,13 +3060,13 @@ __smgr_sess_cr_args_prepare(struct pp_request *req,
 	memcpy(&work_args->args_buf, sess_args, sizeof(*sess_args));
 	work_args->args = &work_args->args_buf;
 
-	if (sess_args->in_pkt && sess_args->in_pkt->h) {
-		memcpy(&work_args->rx, sess_args->in_pkt->h,
+	if (sess_args->rx) {
+		memcpy(&work_args->rx, sess_args->rx,
 		       sizeof(work_args->rx));
 		work_args->args->rx = &work_args->rx;
 	}
-	if (sess_args->eg_pkt && sess_args->eg_pkt->h) {
-		memcpy(&work_args->tx, sess_args->eg_pkt->h,
+	if (sess_args->tx) {
+		memcpy(&work_args->tx, sess_args->tx,
 		       sizeof(work_args->tx));
 		work_args->args->tx = &work_args->tx;
 	}
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
index e4fcebfc09da..437bdb4930b9 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_debugfs.c
@@ -17,7 +17,7 @@
 #include <linux/string.h>
 #include <linux/jiffies.h>
 #include <linux/pp_api.h>
-#include <linux/learning_layer_api.h>
+#include <linux/pktprs.h>
 
 #include "classifier.h"
 #include "pp_common.h"
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
index 6cceb1a13eaa..825ba574e8b6 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_internal.h
@@ -14,7 +14,7 @@
 #include <linux/bitops.h>
 #include <linux/pp_api.h>
 #ifdef CONFIG_DEBUG_FS
-#include <linux/learning_layer_api.h>
+#include <linux/pktprs.h>
 #endif
 
 #include "pp_si.h"
diff --git a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
index 6a921de665ca..51d23f30965a 100644
--- a/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
+++ b/drivers/net/ppv4/pp-api/pp-session-mgr/pp_session_mgr_tdox.c
@@ -16,7 +16,6 @@
 #include <linux/timer.h>      /* For sync/done timers      */
 #include <linux/jiffies.h>    /* For jiffies               */
 #include <linux/pp_qos_api.h> /* For QoS manager APIs      */
-#include <linux/learning_layer_api.h>
 #include <linux/pktprs.h>
 
 #include "uc.h"
@@ -282,22 +281,22 @@ bool smgr_tdox_is_info_valid(struct pp_sess_create_args *args)
 		return false;
 	}
 
-	if (!args->in_pkt || !args->eg_pkt) {
+	if (!args->rx || !args->tx) {
 		pr_debug("TDOX requires in/eg packets\n");
 		atomic_inc(&db->stats.tdox_create_args_err);
 		return false;
 	}
 
-	if (!(args->in_pkt->fv_outer_bitmap & FV_L4_TCP)) {
+	if (!PKTPRS_IS_TCP(args->rx, HDR_OUTER)) {
 		pr_debug("TDOX supports only TCP\n");
 		atomic_inc(&db->stats.tdox_create_args_err);
 		return false;
 	}
 
-	if (args->in_pkt->fv_outer_bitmap & FV_L3_FRAG_1ST ||
-	    args->in_pkt->fv_inner_bitmap & FV_L3_FRAG_1ST ||
-	    args->eg_pkt->fv_outer_bitmap & FV_L3_FRAG_1ST ||
-	    args->eg_pkt->fv_inner_bitmap & FV_L3_FRAG_1ST) {
+	if (pktprs_first_frag(args->rx, HDR_OUTER) ||
+	    pktprs_first_frag(args->rx, HDR_INNER) ||
+	    pktprs_first_frag(args->tx, HDR_OUTER) ||
+	    pktprs_first_frag(args->rx, HDR_INNER)) {
 		pr_debug("TDOX does not support fragmented packets\n");
 		atomic_inc(&db->stats.tdox_create_args_err);
 		return false;
diff --git a/drivers/net/ppv4/pp-hal/field-vector/pp_fv.c b/drivers/net/ppv4/pp-hal/field-vector/pp_fv.c
index 80cdf9b372c8..15e7c6804721 100644
--- a/drivers/net/ppv4/pp-hal/field-vector/pp_fv.c
+++ b/drivers/net/ppv4/pp-hal/field-vector/pp_fv.c
@@ -12,7 +12,7 @@
 #include <linux/if_ether.h>
 #include <linux/ppp_defs.h>
 #include <linux/pp_api.h>
-#include <linux/learning_layer_api.h>
+#include <linux/pktprs.h>
 
 #include "pp_fv.h"
 
@@ -269,4 +269,4 @@ u8 pp_fv_proto_set(struct pktprs_hdr *pkt, u8 lvl)
 	}
 
 	return proto;
-}
\ No newline at end of file
+}
diff --git a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c
index dcc87c4b0aa8..8ee522b29ace 100644
--- a/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c
+++ b/drivers/net/ppv4/pp-tools/unit-test/tests/pp_session_tests.c
@@ -15,7 +15,7 @@
 #include <linux/module.h>
 #include <linux/slab.h>
 #include <linux/random.h>
-#include <linux/learning_layer_api.h>
+#include <linux/pktprs.h>
 #include <linux/pp_api.h>
 #include "../pput.h"
 #include "uc_regs.h"
@@ -649,11 +649,17 @@ static void __smgr_test_cb(struct pp_cb_args *args)
 	}
 }
 
-static void __smgr_test_pkt_set(struct fv_pkt_info *pkt)
+static void __smgr_test_pkt_set(struct pktprs_hdr *h)
 {
-	pkt->fv_outer_bitmap |= FV_L2_BASE;
-	pkt->fv_outer_bitmap |= FV_L3_IPV4;
-	pkt->fv_outer_bitmap |= FV_L4_TCP;
+	h->buf_sz = 54;
+	set_bit(PKTPRS_PROTO_MAC, &h->proto_bmap[0]);
+	h->proto_info[PKTPRS_PROTO_MAC][0].off = 0;
+	set_bit(PKTPRS_PROTO_IPV4, &h->proto_bmap[0]);
+        h->proto_info[PKTPRS_PROTO_IPV4][0].off = 14;
+	set_bit(PKTPRS_PROTO_TCP, &h->proto_bmap[0]);
+        h->proto_info[PKTPRS_PROTO_TCP][0].off = 34;
+	set_bit(PKTPRS_PROTO_PAYLOAD, &h->proto_bmap[0]);
+        h->proto_info[PKTPRS_PROTO_PAYLOAD][0].off = 54;
 }
 
 static int __sess_create_test_sync(void)
@@ -680,27 +686,24 @@ static int __sess_create_test_sync(void)
 }
 
 static void __sess_args_default_set(struct pp_sess_create_args *args,
-				    struct fv_pkt_info *in_pkt,
-				    struct fv_pkt_info *eg_pkt,
-				    struct new_hdr *nhdr)
+				    struct pktprs_hdr *rx,
+				    struct pktprs_hdr *tx)
 {
 	u32 i;
 
 	memset(args, 0, sizeof(*args));
-	memset(in_pkt, 0, sizeof(*in_pkt));
-	memset(eg_pkt, 0, sizeof(*eg_pkt));
-	memset(nhdr,   0, sizeof(*nhdr));
+	memset(rx, 0, sizeof(*rx));
+	memset(tx, 0, sizeof(*tx));
 
-	__smgr_test_pkt_set(in_pkt);
-	__smgr_test_pkt_set(eg_pkt);
+	__smgr_test_pkt_set(rx);
+	__smgr_test_pkt_set(tx);
 
 	args->color = PP_COLOR_GREEN;
-	args->in_pkt  = in_pkt;
-	args->eg_pkt  = eg_pkt;
-	args->nhdr    = nhdr;
-	args->dst_q   = qos_queue;
+	args->rx = rx;
+	args->tx = tx;
+	args->dst_q = qos_queue;
 	args->dst_q_high = qos_queue;
-	args->ps_sz   = 0;
+	args->ps_sz = 0;
 	args->ps_off  = PP_INVALID_PS_OFF;
 	args->eg_port = pp_port;
 	args->in_port = pp_port;
@@ -719,12 +722,11 @@ static int pp_sess_create_test(void *unused)
 	u16 rand;
 	int ret;
 	struct pp_request req;
-	struct fv_pkt_info in_pkt; /* ingress packet */
-	struct fv_pkt_info eg_pkt; /* egress packet  */
-	struct new_hdr     nhdr;
+	struct pktprs_hdr rx; /* ingress packet */
+	struct pktprs_hdr tx; /* egress packet  */
 
 	memset(&req,    0, sizeof(req));
-	__sess_args_default_set(&args, &in_pkt, &eg_pkt, &nhdr);
+	__sess_args_default_set(&args, &rx, &tx);
 	req.cb       = __smgr_test_cb;
 
 	for (i = 0; i < ARRAY_SIZE(created_sessions); i++) {
@@ -735,8 +737,8 @@ static int pp_sess_create_test(void *unused)
 		get_random_bytes(&rand, sizeof(rand));
 		rand %= 2;
 
-		*(u32 *)(&in_pkt.fv.outer.l2.h_source[0]) += 1;
-		*(u32 *)(&eg_pkt.fv.outer.l2.h_source[0]) += 1;
+		pktprs_eth_hdr(&rx, 0)->h_source[0] += 1;
+		pktprs_eth_hdr(&tx, 0)->h_source[0] += 1;
 		args.hash.h1++;
 		args.hash.h2++;
 		args.hash.sig++;
@@ -1382,9 +1384,8 @@ static int pp_tdox_max_session_test(void *data)
 {
 	struct pp_sess_create_args args;
 	struct smgr_tdox_stats stats;
-	struct fv_pkt_info in_pkt; /* ingress packet */
-	struct fv_pkt_info eg_pkt; /* egress packet  */
-	struct new_hdr     nhdr;
+	struct pktprs_hdr rx; /* ingress packet */
+	struct pktprs_hdr tx; /* egress packet  */
 	s32 ret;
 	u32 i;
 
@@ -1394,7 +1395,7 @@ static int pp_tdox_max_session_test(void *data)
 	uc_nf_set(PP_NF_TURBODOX, 0, 0, 0, 0);
 	smgr_tdox_late_init();
 
-	__sess_args_default_set(&args, &in_pkt, &eg_pkt, &nhdr);
+	__sess_args_default_set(&args, &rx, &tx);
 	args.flags = PP_SESS_FLAG_TDOX_MSK;
 
 	/** 1. Create Max tdox sessions
@@ -1433,16 +1434,15 @@ static int pp_tdox_candidate_test(void *data)
 {
 	struct pp_sess_create_args args;
 	struct smgr_tdox_stats stats;
-	struct fv_pkt_info in_pkt; /* ingress packet */
-	struct fv_pkt_info eg_pkt; /* egress packet  */
-	struct new_hdr     nhdr;
+	struct pktprs_hdr rx; /* ingress packet */
+	struct pktprs_hdr tx; /* egress packet  */
 	s32 ret;
 	u32 i;
 
 	if (!smgr_tdox_enable_get())
 		return PP_UT_PASS;
 
-	__sess_args_default_set(&args, &in_pkt, &eg_pkt, &nhdr);
+	__sess_args_default_set(&args, &rx, &tx);
 	args.flags = PP_SESS_FLAG_TDOX_MSK;
 
 	/** 1. Create Additional Max tdox sessions
@@ -1558,16 +1558,15 @@ static int pp_tdox_session_remove_test(void *data)
 {
 	struct pp_sess_create_args args;
 	struct smgr_tdox_stats stats;
-	struct fv_pkt_info in_pkt; /* ingress packet */
-	struct fv_pkt_info eg_pkt; /* egress packet  */
-	struct new_hdr     nhdr;
+	struct pktprs_hdr rx; /* ingress packet */
+	struct pktprs_hdr tx; /* egress packet  */
 	s32 ret;
 	u32 i;
 
 	if (!smgr_tdox_enable_get())
 		return PP_UT_PASS;
 
-	__sess_args_default_set(&args, &in_pkt, &eg_pkt, &nhdr);
+	__sess_args_default_set(&args, &rx, &tx);
 	args.flags = PP_SESS_FLAG_TDOX_MSK;
 
 	/** 1. Create Max tdox sessions
diff --git a/include/linux/pp_api.h b/include/linux/pp_api.h
index 4e5447981cd1..3eeb6eed9868 100755
--- a/include/linux/pp_api.h
+++ b/include/linux/pp_api.h
@@ -14,7 +14,6 @@
 #include <linux/pp_qos_api.h>
 
 #ifdef CONFIG_PPV4_LGM
-#include <linux/learning_layer_api.h>
 #include <linux/pktprs.h>
 #endif
 
@@ -694,15 +693,6 @@ struct pp_sess_create_args {
 	/*! matched the ingress port classification info                 */
 	struct pp_port_cls_data cls;
 
-	/*! ingress packet info as received from the learning driver */
-	struct fv_pkt_info *in_pkt;
-
-	/*! egress packet info as received from the learning driver */
-	struct fv_pkt_info *eg_pkt;
-
-	/*! new header info as received from the learning driver */
-	struct new_hdr *nhdr;
-
 	/*! rx packet info as received from the packet parse module */
 	struct pktprs_hdr *rx;
 
