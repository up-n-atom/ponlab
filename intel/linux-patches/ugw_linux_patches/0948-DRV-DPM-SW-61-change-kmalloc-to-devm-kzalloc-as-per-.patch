From b4e56ad32a31028d895aeda750d4f36c79aa6ac6 Mon Sep 17 00:00:00 2001
From: Rekha Eswaran <rekha.eswaran@intel.com>
Date: Mon, 11 Nov 2019 13:21:23 +0800
Subject: [PATCH] DRV_DPM_SW-61 - change kmalloc to devm_kzalloc as per review
 comments

---
 drivers/net/datapath/dpm/Makefile                  |  2 +-
 drivers/net/datapath/dpm/datapath.h                |  4 +-
 drivers/net/datapath/dpm/datapath_api.c            | 45 ++++++----
 drivers/net/datapath/dpm/datapath_dev.c            | 97 ----------------------
 drivers/net/datapath/dpm/datapath_instance.c       | 13 +--
 drivers/net/datapath/dpm/datapath_logical_dev.c    |  5 +-
 drivers/net/datapath/dpm/datapath_misc.c           | 40 ++++-----
 drivers/net/datapath/dpm/datapath_proc.c           |  4 +-
 drivers/net/datapath/dpm/datapath_proc_api.c       |  5 +-
 drivers/net/datapath/dpm/datapath_proc_qos.c       | 12 +--
 drivers/net/datapath/dpm/datapath_qos.c            |  5 +-
 drivers/net/datapath/dpm/datapath_swdev.c          | 41 +++++----
 drivers/net/datapath/dpm/datapath_tx.c             |  5 +-
 drivers/net/datapath/dpm/gswip30/datapath_mib.c    |  8 +-
 .../net/datapath/dpm/gswip31/datapath_ext_vlan.c   |  4 +-
 drivers/net/datapath/dpm/gswip31/datapath_gswip.c  | 53 +++++++-----
 drivers/net/datapath/dpm/gswip31/datapath_misc.c   | 34 ++++----
 drivers/net/datapath/dpm/gswip31/datapath_ppv4.c   |  9 +-
 .../net/datapath/dpm/gswip31/datapath_ppv4_api.c   | 72 ++++++++--------
 drivers/net/datapath/dpm/gswip31/datapath_proc.c   |  5 +-
 .../net/datapath/dpm/gswip31/datapath_switchdev.c  | 32 +++----
 21 files changed, 212 insertions(+), 283 deletions(-)

diff --git a/drivers/net/datapath/dpm/Makefile b/drivers/net/datapath/dpm/Makefile
index 9eac91c0c1e6..d7221ce73879 100644
--- a/drivers/net/datapath/dpm/Makefile
+++ b/drivers/net/datapath/dpm/Makefile
@@ -1,5 +1,5 @@
 obj-$(CONFIG_INTEL_DATAPATH) = datapath_api.o datapath_proc_api.o datapath_proc.o  datapath_misc.o datapath_notifier.o datapath_logical_dev.o datapath_instance.o datapath_soc.o datapath_qos.o datapath_proc_qos.o datapath_tx.o
-obj-$(CONFIG_INTEL_DATAPATH) += datapath_stats.o datapath_dev.o
+obj-$(CONFIG_INTEL_DATAPATH) += datapath_stats.o
 
 ifneq ($(CONFIG_INTEL_DATAPATH_HAL_GSWIP32),)
 obj-$(CONFIG_INTEL_DATAPATH) += gswip32/
diff --git a/drivers/net/datapath/dpm/datapath.h b/drivers/net/datapath/dpm/datapath.h
index 54ce31410b54..20d583a32c01 100644
--- a/drivers/net/datapath/dpm/datapath.h
+++ b/drivers/net/datapath/dpm/datapath.h
@@ -841,7 +841,7 @@ enum TEST_MODE {
 	DPR_RX_MODE_MAX
 };
 
-extern struct net_device *g_dp_dev;
+extern struct platform_device *g_dp_dev;
 extern u32 dp_rx_test_mode;
 extern struct dma_rx_desc_1 dma_rx_desc_mask1;
 extern struct dma_rx_desc_2 dma_rx_desc_mask2;
@@ -890,8 +890,6 @@ int dp_get_port_subitf_via_dev_private(struct net_device *dev,
 int get_vlan_via_dev(struct net_device *dev, struct vlan_prop *vlan_prop);
 void dp_parser_info_refresh(u32 cpu, u32 mpe1, u32 mpe2, u32 mpe3, u32 verify);
 int dp_inst_init(u32 flag);
-int dp_create_device(void);
-int dp_remove_device(void);
 int request_dp(u32 flag);
 /*static __init */ int dp_init_module(void);
 /*static __exit*/ void dp_cleanup_module(void);
diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index 96d8a45e9638..55a2dba70513 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -82,6 +82,7 @@ static int dp_build_cqm_data(int inst, uint32_t port_id,
 			     struct cbm_dp_alloc_complete_data *cbm_data,
 			     struct dp_dev_data *data);
 
+struct platform_device *g_dp_dev;
 /*port 0 is reserved and never assigned to any one */
 int dp_inst_num;
 /* Keep per DP instance information here */
@@ -1046,7 +1047,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 	port_info->res_qid_base = data->qos_resv_q_base;
 	port_info->num_resv_q = data->num_resv_q;
 
-	cbm_data = kzalloc(sizeof(*cbm_data), GFP_ATOMIC);
+	cbm_data = devm_kzalloc(&g_dp_dev->dev, sizeof(*cbm_data), GFP_ATOMIC);
 	if (!cbm_data) {
 		DP_LIB_UNLOCK(&dp_lock);
 		return res;
@@ -1054,7 +1055,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 
 	res = dp_build_cqm_data(inst, port_id, cbm_data, data);
 	if (res == DP_FAILURE) {
-		kfree(cbm_data);
+		devm_kfree(&g_dp_dev->dev, cbm_data);
 		DP_LIB_UNLOCK(&dp_lock);
 		return res;
 	}
@@ -1067,7 +1068,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 				       cbm_data,
 				       port_info->alloc_flags)) {
 		DP_DEBUG(DP_DBG_FLAG_REG, "CBM port alloc failed\n");
-		kfree(cbm_data);
+		devm_kfree(&g_dp_dev->dev, cbm_data);
 		DP_LIB_UNLOCK(&dp_lock);
 		return DP_FAILURE;
 	}
@@ -1075,7 +1076,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 	if (data->num_rx_ring || data->num_tx_ring) {
 		res = dp_register_dc(inst, port_id, cbm_data, data, flags);
 		if (res == DP_FAILURE) {
-			kfree(cbm_data);
+			devm_kfree(&g_dp_dev->dev, cbm_data);
 			DP_LIB_UNLOCK(&dp_lock);
 			return res;
 		}
@@ -1088,7 +1089,7 @@ int32_t dp_register_dev_ext(int inst, struct module *owner, uint32_t port_id,
 	if (dp_cb)
 		port_info->cb = *dp_cb;
 	DP_LIB_UNLOCK(&dp_lock);
-	kfree(cbm_data);
+	devm_kfree(&g_dp_dev->dev, cbm_data);
 
 	return DP_SUCCESS;
 }
@@ -1185,7 +1186,8 @@ int32_t dp_register_subif_ext(int inst, struct module *owner,
 	if (!(flags & DP_F_SUBIF_LOGICAL))
 		subifid_fn_t = port_info->cb.get_subifid_fn;
 
-	subif_id_sync = kmalloc(sizeof(*subif_id_sync) * 2, GFP_ATOMIC);
+	subif_id_sync = devm_kzalloc(&g_dp_dev->dev,
+				     sizeof(*subif_id_sync) * 2, GFP_ATOMIC);
 	if (!subif_id_sync) {
 		DP_LIB_UNLOCK(&dp_lock);
 		return DP_FAILURE;
@@ -1197,7 +1199,7 @@ int32_t dp_register_subif_ext(int inst, struct module *owner,
 	if (!res)
 		dp_sync_subifid_priv(dev, subif_name, subif_id_sync, data,
 				     flags, subifid_fn_t, &f_subif);
-	kfree(subif_id_sync);
+	devm_kfree(&g_dp_dev->dev, subif_id_sync);
 	return res;
 }
 EXPORT_SYMBOL(dp_register_subif_ext);
@@ -1304,13 +1306,16 @@ int32_t dp_get_netif_subifid_priv(struct net_device *netif, struct sk_buff *skb,
 	struct logic_dev *tmp = NULL;
 	u16 gpid = 0;
 
-	subifs = kmalloc(sizeof(*subifs) * DP_MAX_CTP_PER_DEV, GFP_ATOMIC);
+	subifs = devm_kzalloc(&g_dp_dev->dev,
+			      sizeof(*subifs) * DP_MAX_CTP_PER_DEV,
+			      GFP_ATOMIC);
 	if (!subifs)
 		return res;
-	subif_flag = kmalloc(sizeof(*subif_flag) * DP_MAX_CTP_PER_DEV,
-			     GFP_ATOMIC);
+	subif_flag = devm_kzalloc(&g_dp_dev->dev,
+				  sizeof(*subif_flag) * DP_MAX_CTP_PER_DEV,
+				  GFP_ATOMIC);
 	if (!subif_flag) {
-		kfree(subifs);
+		devm_kfree(&g_dp_dev->dev, subifs);
 		return res;
 	}
 	if (!netif && !subif_data) {
@@ -1461,8 +1466,8 @@ int32_t dp_get_netif_subifid_priv(struct net_device *netif, struct sk_buff *skb,
 	}
 	res = 0;
 EXIT:
-	kfree(subifs);
-	kfree(subif_flag);
+	devm_kfree(&g_dp_dev->dev, subifs);
+	devm_kfree(&g_dp_dev->dev, subif_flag);
 	return res;
 }
 
@@ -1939,7 +1944,7 @@ int dp_set_pmapper(struct net_device *dev, struct dp_pmapper *mapper, u32 flag)
 		pr_err("BP port(%d) out of range %d\n", bport, DP_MAX_BP_NUM);
 		return DP_FAILURE;
 	}
-	map = kmalloc(sizeof(*map), GFP_ATOMIC);
+	map = devm_kzalloc(&g_dp_dev->dev, sizeof(*map), GFP_ATOMIC);
 	if (!map)
 		return DP_FAILURE;
 	memcpy(map, mapper, sizeof(*map));
@@ -1975,7 +1980,7 @@ int dp_set_pmapper(struct net_device *dev, struct dp_pmapper *mapper, u32 flag)
 		dp_bp_dev_tbl[inst][bport].dscp[i] = map->dscp_map[i];
 	res = DP_SUCCESS;
 EXIT:
-	kfree(map);
+	devm_kfree(&g_dp_dev->dev, map);
 	return res;
 }
 EXPORT_SYMBOL(dp_set_pmapper);
@@ -2419,11 +2424,15 @@ int dp_basic_proc(void)
 	/*mask to reset some field as SWAS required  all others try to keep */
 	memset(dp_port_prop, 0, sizeof(dp_port_prop));
 	memset(dp_port_info, 0, sizeof(dp_port_info));
+	g_dp_dev = platform_device_register_simple("dp_plat_dev", 0, NULL, 0);
+	if (IS_ERR(g_dp_dev)) {
+		pr_err("register DP platform device fail\n");
+		return -1;
+	}
 	p_node = dp_proc_install();
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_LOOPETH)
 	dp_loop_eth_dev_init(p_node);
 #endif
-	dp_create_device();
 	dp_inst_init(0);
 	dp_subif_list_init();
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
@@ -2481,9 +2490,9 @@ int dp_basic_proc(void)
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_CPUFREQ)
 		dp_cpufreq_notify_exit();
 #endif
-		dp_remove_device();
-		unregister_notifier(0);
 		DP_LIB_UNLOCK(&dp_lock);
+		unregister_notifier(0);
+		platform_device_unregister(g_dp_dev);
 	}
 }
 
diff --git a/drivers/net/datapath/dpm/datapath_dev.c b/drivers/net/datapath/dpm/datapath_dev.c
deleted file mode 100644
index d8120b85e0fd..000000000000
--- a/drivers/net/datapath/dpm/datapath_dev.c
+++ /dev/null
@@ -1,97 +0,0 @@
-
-#include<linux/init.h>
-#include<linux/module.h>
-#include <linux/kernel.h>
-#include <linux/types.h>
-#include <linux/version.h>
-#include <linux/ethtool.h>
-#include <linux/proc_fs.h>
-#include <linux/delay.h>
-#include <linux/init.h>
-#include <linux/clk.h>
-#include <linux/if_ether.h>
-#include <net/datapath_api.h>
-#include "datapath.h"
-
-struct net_device *g_dp_dev;
-static int dp_device_set_mac_address(struct net_device *dev,
-				     void *p)
-{
-	struct sockaddr *addr = p;
-
-	if (netif_running(dev))
-		return -EBUSY;
-
-	if (!is_valid_ether_addr(addr->sa_data))
-		return -EINVAL;
-
-	memcpy(dev->dev_addr, addr->sa_data, dev->addr_len);
-
-	return 0;
-}
-
-static int dp_device_open(struct net_device *dev)
-{
-	netif_start_queue(dev);
-	return 0;
-}
-
-static int dp_device_close(struct net_device *dev)
-{
-	netif_stop_queue(dev);
-	return 0;
-}
-
-static int dp_device_xmit(struct sk_buff *skb, struct net_device *dev)
-{
-	dev_kfree_skb(skb);
-	return 0;
-}
-
-static int dp_device_ioctl(struct net_device *dev, struct ifreq *ifr, int cmd)
-{
-	return -EOPNOTSUPP;
-}
-
-static int dp_device_init(struct net_device *dev)
-{
-	return 0;
-}
-
-static const struct net_device_ops dp_device_ops = {
-	.ndo_init         = dp_device_init,
-	.ndo_open         = dp_device_open,
-	.ndo_stop         = dp_device_close,
-	.ndo_start_xmit   = dp_device_xmit,
-	.ndo_do_ioctl     = dp_device_ioctl,
-	.ndo_set_mac_address = dp_device_set_mac_address,
-};
-
-static void dp_device_setup(struct net_device *dev)
-{
-	ether_setup(dev);
-	dev->netdev_ops = &dp_device_ops;
-}
-
-int dp_create_device(void)
-{
-	char ifname[IFNAMSIZ];
-
-	snprintf(ifname, sizeof(ifname), "dp_dev");
-	g_dp_dev = alloc_netdev(0, ifname, NET_NAME_ENUM,
-				dp_device_setup);
-
-	if (register_netdev(g_dp_dev)) {
-		free_netdev(g_dp_dev);
-		pr_err("register datapath device fail:%s\n", ifname);
-		return -1;
-	}
-	return 0;
-}
-
-int dp_remove_device(void)
-{
-	unregister_netdev(g_dp_dev);
-	free_netdev(g_dp_dev);
-	return 0;
-}
diff --git a/drivers/net/datapath/dpm/datapath_instance.c b/drivers/net/datapath/dpm/datapath_instance.c
index 83d0f2594a74..d95f0716976f 100644
--- a/drivers/net/datapath/dpm/datapath_instance.c
+++ b/drivers/net/datapath/dpm/datapath_instance.c
@@ -257,11 +257,9 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 		return -1;
 	}
 	idx = dp_dev_hash(dev, subif_name);
-	subif = kmalloc(sizeof(*subif), GFP_KERNEL);
-	if (!subif) {
-		pr_err("failed to alloc %zd bytes\n", sizeof(*subif));
+	subif = devm_kzalloc(&g_dp_dev->dev, sizeof(*subif), GFP_KERNEL);
+	if (!subif)
 		return -1;
-	}
 
 	subif->subif = ctp;
 	dp_dev = dp_dev_lookup(&dp_dev_list[idx], dev, subif_name, flag);
@@ -287,8 +285,7 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 		}
 	}
 	if (!dp_dev) {
-		pr_err("Failed to kmalloc %zd bytes\n", sizeof(*dp_dev));
-		kfree(subif);
+		devm_kfree(&g_dp_dev->dev, subif);
 		return -1;
 	}
 	if (new_f) {
@@ -437,10 +434,8 @@ int dp_inst_insert_mod(struct module *owner, u16 ep, u32 inst, u32 flag)
 			new_f = 1;
 		}
 	}
-	if (!dp_mod) {
-		pr_err("Failed to kmalloc %zd bytes\n", sizeof(*dp_mod));
+	if (!dp_mod)
 		return -1;
-	}
 	if (new_f)
 		hlist_add_head(&dp_mod->hlist, &dp_mod_list[idx]);
 	DP_DEBUG(DP_DBG_FLAG_INST, "dp_inst_insert_mod:%s\n", owner->name);
diff --git a/drivers/net/datapath/dpm/datapath_logical_dev.c b/drivers/net/datapath/dpm/datapath_logical_dev.c
index b3291d816362..d274f8471909 100644
--- a/drivers/net/datapath/dpm/datapath_logical_dev.c
+++ b/drivers/net/datapath/dpm/datapath_logical_dev.c
@@ -158,7 +158,8 @@ int add_logic_dev(int inst, int port_id, struct net_device *dev,
 			 dev->name);
 		return -1;
 	}
-	logic_dev_tmp = kmalloc(sizeof(*logic_dev_tmp), GFP_KERNEL);
+	logic_dev_tmp = devm_kzalloc(&g_dp_dev->dev,
+				     sizeof(*logic_dev_tmp), GFP_KERNEL);
 	if (!logic_dev_tmp) {
 		DP_DEBUG(DP_DBG_FLAG_LOGIC, "kmalloc fail for %zd bytes\n",
 			 sizeof(*logic_dev_tmp));
@@ -202,7 +203,7 @@ int del_logic_dev(int inst, struct list_head *head, struct net_device *dev,
 		logic_dev, flags);
 	dp_inst_del_dev(dev, NULL, inst, logic_dev->ep, logic_dev->ctp, 0);
 	list_del(&logic_dev->list);
-	kfree(logic_dev);
+	devm_kfree(&g_dp_dev->dev, logic_dev);
 
 	return 0;
 }
diff --git a/drivers/net/datapath/dpm/datapath_misc.c b/drivers/net/datapath/dpm/datapath_misc.c
index ecc36410bc87..fc79d7725706 100644
--- a/drivers/net/datapath/dpm/datapath_misc.c
+++ b/drivers/net/datapath/dpm/datapath_misc.c
@@ -140,7 +140,7 @@ void dump_parser_flag(char *buf)
 		pr_err("dump_parser_flag buf NULL\n");
 		return;
 	}
-	p = kmalloc(2000, GFP_KERNEL);
+	p = devm_kzalloc(&g_dp_dev->dev, 2000, GFP_KERNEL);
 	if (!p)
 		return;
 
@@ -183,7 +183,7 @@ void dump_parser_flag(char *buf)
 			}
 		}
 	}
-	kfree(p);
+	devm_kfree(&g_dp_dev->dev, p);
 }
 
 /*will be used at any context */
@@ -199,7 +199,7 @@ void dp_dump_raw_data(char *buf, int len, char *prefix_str)
 		pr_err("dp_dump_raw_data: p NULL ?\n");
 		return;
 	}
-	s = kmalloc(bytes, GFP_KERNEL);
+	s = devm_kzalloc(&g_dp_dev->dev, bytes, GFP_KERNEL);
 	if (!s)
 		return;
 	sprintf(s, "%s in hex at 0x%px\n",
@@ -213,7 +213,7 @@ void dp_dump_raw_data(char *buf, int len, char *prefix_str)
 		sprintf(s + l, "\n");
 		pr_info("%s", s);
 	}
-	kfree(s);
+	devm_kfree(&g_dp_dev->dev, s);
 }
 EXPORT_SYMBOL(dp_dump_raw_data);
 
@@ -1499,14 +1499,11 @@ u32 get_dma_chan_idx(int inst, int num_dma_chan)
  */
 u32 alloc_dma_chan_tbl(int inst)
 {
-	dp_dma_chan_tbl[inst] = kzalloc((sizeof(struct dma_chan_info) *
-					DP_MAX_DMA_CHAN), GFP_ATOMIC);
-
-	if (!dp_dma_chan_tbl[inst]) {
-		pr_err("Failed for kmalloc: %zu bytes\n",
-		       (sizeof(struct dma_chan_info) * DP_MAX_DMA_CHAN));
+	dp_dma_chan_tbl[inst] = devm_kzalloc(&g_dp_dev->dev,
+					     (sizeof(struct dma_chan_info) *
+					     DP_MAX_DMA_CHAN), GFP_ATOMIC);
+	if (!dp_dma_chan_tbl[inst])
 		return DP_FAILURE;
-	}
 	return DP_SUCCESS;
 }
 
@@ -1530,20 +1527,23 @@ u32 alloc_dp_port_subif_info(int inst)
 	max_dp_ports = info->cap.max_num_dp_ports;
 	max_subif = info->cap.max_num_subif_per_port;
 
-	dp_port_info[inst] = kzalloc((sizeof(struct pmac_port_info) *
-				     max_dp_ports), GFP_KERNEL);
+	dp_port_info[inst] = devm_kzalloc(&g_dp_dev->dev,
+					  (sizeof(struct pmac_port_info) *
+					  max_dp_ports), GFP_KERNEL);
 	if (!dp_port_info[inst])
 		return DP_FAILURE;
 	for (port_id = 0; port_id < max_dp_ports; port_id++) {
 		get_dp_port_info(inst, port_id)->subif_info =
-			kzalloc(sizeof(struct dp_subif_info) * max_subif,
-				GFP_KERNEL);
+			devm_kzalloc(&g_dp_dev->dev,
+				     sizeof(struct dp_subif_info) * max_subif,
+				     GFP_KERNEL);
 		if (!get_dp_port_info(inst, port_id)->subif_info) {
 			pr_err("Failed for kmalloc: %zu bytes\n",
 			       max_subif * sizeof(struct dp_subif_info));
 			while (--port_id >= 0)
-				kfree(get_dp_port_info(inst,
-						       port_id)->subif_info);
+				devm_kfree(&g_dp_dev->dev,
+					   get_dp_port_info(inst,
+					   port_id)->subif_info);
 			return DP_FAILURE;
 		}
 	}
@@ -1557,7 +1557,7 @@ u32 alloc_dp_port_subif_info(int inst)
 void free_dma_chan_tbl(int inst)
 {
 	/* free dma chan tbl */
-	kfree(dp_dma_chan_tbl[inst]);
+	devm_kfree(&g_dp_dev->dev, dp_dma_chan_tbl[inst]);
 }
 
 /**
@@ -1578,8 +1578,8 @@ void free_dp_port_subif_info(int inst)
 	if (dp_port_info[inst]) {
 		for (port_id = 0; port_id < max_dp_ports; port_id++) {
 			port_info = get_dp_port_info(inst, port_id);
-			kfree(port_info->subif_info);
+			devm_kfree(&g_dp_dev->dev, port_info->subif_info);
 		}
-		kfree(dp_port_info[inst]);
+		devm_kfree(&g_dp_dev->dev, dp_port_info[inst]);
 	}
 }
diff --git a/drivers/net/datapath/dpm/datapath_proc.c b/drivers/net/datapath/dpm/datapath_proc.c
index f9c58bbd09b3..ab16b22b4509 100644
--- a/drivers/net/datapath/dpm/datapath_proc.c
+++ b/drivers/net/datapath/dpm/datapath_proc.c
@@ -554,7 +554,7 @@ ssize_t proc_dbg_write(struct file *file, const char *buf, size_t count,
 EXIT:
 	return count;
 help:
-	tmp_buf = kmalloc(BUF_SIZE_TMP, GFP_KERNEL);
+	tmp_buf = devm_kzalloc(&g_dp_dev->dev, BUF_SIZE_TMP, GFP_KERNEL);
 	if (!tmp_buf)
 		return count;
 	num = 0;
@@ -569,7 +569,7 @@ ssize_t proc_dbg_write(struct file *file, const char *buf, size_t count,
 	num += snprintf(tmp_buf + num, BUF_SIZE_TMP - num - 1,
 			" display command: cat /proc/dp/cmd\n");
 	pr_info("%s", tmp_buf);
-	kfree(tmp_buf);
+	devm_kfree(&g_dp_dev->dev, (tmp_buf));
 	return count;
 }
 #endif
diff --git a/drivers/net/datapath/dpm/datapath_proc_api.c b/drivers/net/datapath/dpm/datapath_proc_api.c
index 7b852e9fd4df..5f8bbf66caf3 100644
--- a/drivers/net/datapath/dpm/datapath_proc_api.c
+++ b/drivers/net/datapath/dpm/datapath_proc_api.c
@@ -232,8 +232,7 @@ static int dp_proc_open(struct inode *inode, struct file *file)
 		return ret;
 
 	s = file->private_data;
-	p = kmalloc(sizeof(*p), GFP_KERNEL);
-
+	p = devm_kzalloc(&g_dp_dev->dev, sizeof(*p), GFP_KERNEL);
 	if (!p) {
 		(void)seq_release(inode, file);
 		return -ENOMEM;
@@ -264,7 +263,7 @@ static int dp_proc_release(struct inode *inode, struct file *file)
 	struct seq_file *s;
 
 	s = file->private_data;
-	kfree(s->private);
+	devm_kfree(&g_dp_dev->dev, s->private);
 
 	return seq_release(inode, file);
 }
diff --git a/drivers/net/datapath/dpm/datapath_proc_qos.c b/drivers/net/datapath/dpm/datapath_proc_qos.c
index 913eaeab6f2e..d4bcf08d155d 100644
--- a/drivers/net/datapath/dpm/datapath_proc_qos.c
+++ b/drivers/net/datapath/dpm/datapath_proc_qos.c
@@ -504,7 +504,7 @@ void print_all(struct seq_file *s, struct q_print_info *q_info)
 
 	if (cols <= 0)
 		return;
-	buf = kmalloc(cols * rows + 1, GFP_KERNEL);
+	buf = devm_kzalloc(&g_dp_dev->dev, cols * rows + 1, GFP_KERNEL);
 	if (!buf)
 		return;
 	memset(buf, ' ', cols * rows);
@@ -530,7 +530,7 @@ void print_all(struct seq_file *s, struct q_print_info *q_info)
 		seq_printf(s, "%s\n", p);
 		p += cols;
 	}
-	kfree(buf);
+	devm_kfree(&g_dp_dev->dev, buf);
 }
 
 /* print_box_lvl must bigger 1 than sch_lvl */
@@ -553,7 +553,7 @@ struct q_print_info *collect_info(struct seq_file *s,
 	}
 
 	size = sizeof(*q_info) + 1;
-	q_info = kmalloc(size, GFP_KERNEL);
+	q_info = devm_kzalloc(&g_dp_dev->dev, size, GFP_KERNEL);
 	if (!q_info)
 		return NULL;
 	memset(q_info, 0, size);
@@ -635,7 +635,7 @@ struct q_print_info *collect_info(struct seq_file *s,
 	}
 	return q_info;
 ERR_EXIT:
-	kfree(q_info);
+	devm_kfree(&g_dp_dev->dev, q_info);
 	return NULL;
 }
 
@@ -890,11 +890,11 @@ int qos_dump(struct seq_file *s, int pos)
 		set_child_per_box(q_info);
 		set_location_size(&q_info->port_box, 0);
 		if (check_location(q_info)) {
-			kfree(q_info);
+			devm_kfree(&g_dp_dev->dev, q_info);
 			continue;
 		}
 		print_all(s, q_info);
-		kfree(q_info);
+		devm_kfree(&g_dp_dev->dev, q_info);
 	}
 NEXT_DP_PORT:
 	if (!seq_has_overflowed(s))
diff --git a/drivers/net/datapath/dpm/datapath_qos.c b/drivers/net/datapath/dpm/datapath_qos.c
index e1ff86f9119b..c3d5ac3c2606 100644
--- a/drivers/net/datapath/dpm/datapath_qos.c
+++ b/drivers/net/datapath/dpm/datapath_qos.c
@@ -153,7 +153,8 @@ int dp_deq_port_res_get(struct dp_dequeue_res *res, int flag)
 	if (!res || !dp_port_prop[res->inst].info.dp_qos_platform_set)
 		return DP_FAILURE;
 	if (res->dev) { /*fill dp_port if dev is provided */
-		subif = kzalloc(sizeof(*subif), GFP_ATOMIC);
+		subif = devm_kzalloc(&g_dp_dev->dev, sizeof(*subif),
+				     GFP_ATOMIC);
 		if (!subif)
 			return DP_FAILURE;
 		dp_get_netif_subifid(res->dev, NULL, NULL, NULL, subif, 0);
@@ -162,7 +163,7 @@ int dp_deq_port_res_get(struct dp_dequeue_res *res, int flag)
 			return DP_FAILURE;
 		}
 		res->dp_port = subif->port_id;
-		kfree(subif);
+		devm_kfree(&g_dp_dev->dev, subif);
 		subif = NULL;
 	}
 	port_info = get_dp_port_info(res->inst, res->dp_port);
diff --git a/drivers/net/datapath/dpm/datapath_swdev.c b/drivers/net/datapath/dpm/datapath_swdev.c
index ae76ab83751c..0659f484133d 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.c
+++ b/drivers/net/datapath/dpm/datapath_swdev.c
@@ -60,6 +60,14 @@ u16 dp_swdev_cal_hash(unsigned char *name)
 	return (u16)(hash & 0x3F);
 }
 
+/* dp_kfree - used as destructor for
+ * switchdev_trans_item_enqueue() API
+ */
+void dp_kfree(void const *data)
+{
+	devm_kfree(&g_dp_dev->dev, (void *)data);
+}
+
 struct hlist_head *get_dp_g_bridge_id_entry_hash_table_info(int instance,
 							    int index)
 {
@@ -201,7 +209,7 @@ static int dp_swdev_del_bport_from_list(struct br_info *br_item,
 	}
 	if (found) {
 		list_del(&temp_list->list);
-		kfree(temp_list);
+		devm_kfree(&g_dp_dev->dev, temp_list);
 		return 1;
 	}
 	return 0;
@@ -222,8 +230,9 @@ static int dp_swdev_add_bport_to_list(struct br_info *br_item,
 	}
 	if (found == 0) {
 		bport_list = (struct bridge_member_port *)
-			kmalloc(sizeof(struct bridge_member_port),
-				GFP_KERNEL);
+			devm_kzalloc(&g_dp_dev->dev,
+				     sizeof(struct bridge_member_port),
+				     GFP_KERNEL);
 		if (!bport_list) {
 			pr_err("\n Node creation failed\n");
 			return -1;
@@ -320,7 +329,8 @@ static int dp_swdev_filter_vlan(struct net_device *dev,
 			       __func__, dev->name);
 			return -EOPNOTSUPP;
 		}
-		br_item = kmalloc(sizeof(*br_item), GFP_KERNEL);
+		br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item),
+				       GFP_KERNEL);
 		if (!br_item)
 			//TODO need to check dequeue if no memory
 			return -ENOMEM;
@@ -335,11 +345,11 @@ static int dp_swdev_filter_vlan(struct net_device *dev,
 			br_item->fid = br_info->fid;
 		} else {
 			swdev_unlock();
-			kfree(br_item);
+			devm_kfree(&g_dp_dev->dev, br_item);
 			return -EOPNOTSUPP;
 		}
 		switchdev_trans_item_enqueue(trans, br_item,
-					     kfree, &br_item->tritem);
+					     dp_kfree, &br_item->tritem);
 		swdev_unlock();
 		return 0;
 	}
@@ -367,7 +377,8 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 
 		ret = i_info->swdev_bridge_cfg_set(br_item->inst, br_item->fid);
 		if (ret == 0) {
-			br_info = kmalloc(sizeof(*br_info), GFP_KERNEL);
+			br_info = devm_kzalloc(&g_dp_dev->dev,
+					       sizeof(*br_info), GFP_KERNEL);
 			if (!br_info)
 				/*TODO need to check return value
 				 *for switchdev commit
@@ -493,7 +504,8 @@ static int dp_swdev_add_if(struct net_device *dev,
 	sif = get_dp_port_subif(p_info, vap);
 	/* SWITCHDEV_TRANS_PREPARE phase */
 	if (switchdev_trans_ph_prepare(trans)) {
-		br_item = kzalloc(sizeof(*br_item), GFP_KERNEL);
+		br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item),
+				       GFP_KERNEL);
 		if (!br_item)
 			/*TODO need to check dequeue if no memory*/
 			return -ENOMEM;
@@ -532,7 +544,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 					br_item->fid = br_id;
 				} else {
 					pr_err("Switch config failed\r\n");
-					kfree(br_item);
+					devm_kfree(&g_dp_dev->dev, br_item);
 					swdev_unlock();
 					return -EOPNOTSUPP;
 				}
@@ -542,7 +554,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 			}
 		}
 		switchdev_trans_item_enqueue(trans, br_item,
-					     kfree, &br_item->tritem);
+					     dp_kfree, &br_item->tritem);
 		swdev_unlock();
 		return 0;
 	}
@@ -590,7 +602,8 @@ static int dp_swdev_del_if(struct net_device *dev,
 	sif = get_dp_port_subif(p_info, vap);
 	/* SWITCHDEV_TRANS_PREPARE phase */
 	if (switchdev_trans_ph_prepare(trans)) {
-		br_item = kmalloc(sizeof(*br_item), GFP_KERNEL);
+		br_item = devm_kzalloc(&g_dp_dev->dev,
+				       sizeof(*br_item), GFP_KERNEL);
 		if (!br_item)
 			/*TODO need to check dequeue if no memory*/
 			return -ENOMEM;
@@ -620,7 +633,7 @@ static int dp_swdev_del_if(struct net_device *dev,
 			br_item->flags = BRIDGE_NO_ACTION;
 		}
 		/*TODO check return value & enqueue*/
-		switchdev_trans_item_enqueue(trans, br_item, kfree,
+		switchdev_trans_item_enqueue(trans, br_item, dp_kfree,
 					     &br_item->tritem);
 		swdev_unlock();
 		return 0;
@@ -686,7 +699,7 @@ int dp_del_br_if(struct net_device *dev, struct net_device *br_dev,
 	struct bridge_member_port *temp_list = NULL;
 	u8 *addr = (u8 *)dev->dev_addr;
 
-	br_item = kzalloc(sizeof(*br_item), GFP_KERNEL);
+	br_item = devm_kzalloc(&g_dp_dev->dev, sizeof(*br_item), GFP_KERNEL);
 	if (!br_item)
 		return -1;
 	swdev_lock();
@@ -713,7 +726,7 @@ int dp_del_br_if(struct net_device *dev, struct net_device *br_dev,
 	if (br_item)
 		dp_swdev_clr_gswip_cfg(br_item, addr);
 	swdev_unlock();
-	kfree(br_item);
+	devm_kfree(&g_dp_dev->dev, br_item);
 	return 0;
 }
 
diff --git a/drivers/net/datapath/dpm/datapath_tx.c b/drivers/net/datapath/dpm/datapath_tx.c
index a59e389bed7b..b6a045847427 100644
--- a/drivers/net/datapath/dpm/datapath_tx.c
+++ b/drivers/net/datapath/dpm/datapath_tx.c
@@ -205,7 +205,7 @@ int dp_tx_update_list(void)
 	u8 i;
 
 	/* update list for fast read */
-	new_data = devm_kmalloc(&g_dp_dev->dev, sizeof(struct tx_hook_list),
+	new_data = devm_kzalloc(&g_dp_dev->dev, sizeof(struct tx_hook_list),
 				GFP_ATOMIC);
 	if (!new_data)
 		return -ENOMEM;
@@ -284,7 +284,8 @@ int dp_tx_init(int inst)
 {
 	if (inst)
 		return 0;
-	dp_tx_ctx = kzalloc(sizeof(struct dp_tx_context), GFP_ATOMIC);
+	dp_tx_ctx = devm_kzalloc(&g_dp_dev->dev, sizeof(struct dp_tx_context),
+				 GFP_ATOMIC);
 	if (!dp_tx_ctx)
 		return -ENOMEM;
 	spin_lock_init(&dp_tx_ctx->lock);
diff --git a/drivers/net/datapath/dpm/gswip30/datapath_mib.c b/drivers/net/datapath/dpm/gswip30/datapath_mib.c
index a510166d5c22..d8153d59fa91 100644
--- a/drivers/net/datapath/dpm/gswip30/datapath_mib.c
+++ b/drivers/net/datapath/dpm/gswip30/datapath_mib.c
@@ -579,7 +579,7 @@ static int update_port_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 				 subif->port_id);
 		return -1;
 	}
-	curr = kmalloc(sizeof(*curr), GFP_KERNEL);
+	curr = devm_kzalloc(&g_dp_dev->dev, sizeof(*curr), GFP_KERNEL);
 	if (!curr)
 		return -1;
 	memset(curr, 0, sizeof(*curr));
@@ -677,7 +677,7 @@ static int update_port_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 HANDLE_WRAPWROUND:
 	ret =
 	    port_mib_wraparound(tmp.port_id, curr, &last[tmp.port_id]);
-	kfree(curr);
+	devm_kfree(&g_dp_dev->dev, curr);
 	return ret;
 }
 
@@ -728,7 +728,7 @@ static int update_vap_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 	if (!subif || (subif->port_id <= 0) || (subif->port_id >= PMAC_MAX_NUM))
 		return -1;
 
-	curr = kmalloc(sizeof(*curr), GFP_KERNEL);
+	curr = devm_kzalloc(&g_dp_dev->dev, sizeof(*curr), GFP_KERNEL);
 	if (!curr)
 		return -1;
 	memset(curr, 0, sizeof(*curr));
@@ -796,7 +796,7 @@ static int update_vap_mib_lower_lvl(dp_subif_t *subif, u32 flag)
 
 	ret = vap_mib_wraparound(subif, curr, &last_vap[port_id][vap]);
 EXIT:
-	kfree(curr);
+	devm_kfree(&g_dp_dev->dev, curr);
 
 	return ret;
 }
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c b/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c
index 9e7590a357be..265603909187 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ext_vlan.c
@@ -580,7 +580,7 @@ int set_gswip_ext_vlan(struct core_ops *ops, struct ext_vlan_info *vlan,
 	new_priv_size = sizeof(*new_priv);
 	new_priv_size += sizeof(new_priv->bp[0])
 			 * (vlan->n_vlan1 + vlan->n_vlan2);
-	new_priv = kmalloc(new_priv_size, GFP_KERNEL);
+	new_priv = devm_kzalloc(&g_dp_dev->dev, new_priv_size, GFP_KERNEL);
 
 	if (!new_priv)
 		return -ENOMEM;
@@ -618,6 +618,6 @@ int set_gswip_ext_vlan(struct core_ops *ops, struct ext_vlan_info *vlan,
 	vlan->priv = new_priv;
 
 	/* free old private data if any */
-	kfree(old_priv);
+	devm_kfree(&g_dp_dev->dev, (old_priv));
 	return ret;
 }
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
index dca320d0fdb8..0d5c5c665402 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
@@ -709,8 +709,8 @@ int free_bridge_port(int inst, int bp)
 	if (bp == CPU_BP)
 		return 0;
 
-	tmp = kzalloc(sizeof(*tmp), GFP_KERNEL);
-	tmp2 = kzalloc(sizeof(*tmp2), GFP_KERNEL);
+	tmp = devm_kzalloc(&g_dp_dev->dev, sizeof(*tmp), GFP_KERNEL);
+	tmp2 = devm_kzalloc(&g_dp_dev->dev, sizeof(*tmp2), GFP_KERNEL);
 	if (!tmp || !tmp2)
 		goto FREE_EXIT;
 
@@ -755,8 +755,8 @@ int free_bridge_port(int inst, int bp)
 	if (ret != GSW_statusOk)
 		pr_err("Failed to GSW_BRIDGE_PORT_FREE:%d\n", bp);
 FREE_EXIT:
-	kfree(tmp);
-	kfree(tmp2);
+	devm_kfree(&g_dp_dev->dev, tmp);
+	devm_kfree(&g_dp_dev->dev, tmp2);
 	return 0;
 }
 
@@ -1154,7 +1154,8 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 
 	if (flag & DP_METER_ATTACH_PCE) {
 		/* pattern setting */
-		pce_rule = kzalloc(sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
+		pce_rule = devm_kzalloc(&g_dp_dev->dev,
+					sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
 		if (!pce_rule) {
 			bret = -1;
 			goto err;
@@ -1178,7 +1179,9 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		ctp_cfg = kzalloc(sizeof(GSW_CTP_portConfig_t), GFP_ATOMIC);
+		ctp_cfg = devm_kzalloc(&g_dp_dev->dev,
+				       sizeof(GSW_CTP_portConfig_t),
+				       GFP_ATOMIC);
 		if (!ctp_cfg) {
 			bret = -1;
 			goto err;
@@ -1225,7 +1228,9 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		bp_cfg = kzalloc(sizeof(GSW_BRIDGE_portConfig_t), GFP_ATOMIC);
+		bp_cfg = devm_kzalloc(&g_dp_dev->dev,
+				      sizeof(GSW_BRIDGE_portConfig_t),
+				      GFP_ATOMIC);
 		if (!bp_cfg) {
 			bret = -1;
 			goto err;
@@ -1272,7 +1277,9 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			goto err;
 		}
 	} else if (flag & DP_METER_ATTACH_BRIDGE) {
-		br_cfg = kzalloc(sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
+		br_cfg = devm_kzalloc(&g_dp_dev->dev,
+				      sizeof(GSW_BRIDGE_config_t),
+				      GFP_ATOMIC);
 		if (!br_cfg) {
 			bret = -1;
 			goto err;
@@ -1310,10 +1317,10 @@ int dp_meter_add_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 	}
 err:
-	kfree(br_cfg);
-	kfree(bp_cfg);
-	kfree(ctp_cfg);
-	kfree(pce_rule);
+	devm_kfree(&g_dp_dev->dev, br_cfg);
+	devm_kfree(&g_dp_dev->dev, bp_cfg);
+	devm_kfree(&g_dp_dev->dev, ctp_cfg);
+	devm_kfree(&g_dp_dev->dev, pce_rule);
 	return bret;
 }
 
@@ -1344,7 +1351,8 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 	gsw_bp = &gsw_handle->gsw_brdgport_ops;
 	gsw_brdg = &gsw_handle->gsw_brdg_ops;
 	if (meter->dir & DP_METER_ATTACH_PCE) {
-		pce_rule = kzalloc(sizeof(GSW_PCE_rule_t), GFP_ATOMIC);
+		pce_rule = devm_kzalloc(&g_dp_dev->dev, sizeof(GSW_PCE_rule_t),
+					GFP_ATOMIC);
 		if (!pce_rule) {
 			bret = -1;
 			goto err;
@@ -1367,7 +1375,9 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		ctp_cfg = kzalloc(sizeof(GSW_CTP_portConfig_t), GFP_ATOMIC);
+		ctp_cfg = devm_kzalloc(&g_dp_dev->dev,
+				       sizeof(GSW_CTP_portConfig_t),
+				       GFP_ATOMIC);
 		if (!ctp_cfg) {
 			bret = -1;
 			goto err;
@@ -1411,7 +1421,9 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 			bret = -1;
 			goto err;
 		}
-		bp_cfg = kzalloc(sizeof(GSW_BRIDGE_portConfig_t), GFP_ATOMIC);
+		bp_cfg = devm_kzalloc(&g_dp_dev->dev,
+				      sizeof(GSW_BRIDGE_portConfig_t),
+				      GFP_ATOMIC);
 		if (!bp_cfg) {
 			bret = -1;
 			goto err;
@@ -1455,7 +1467,8 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 	}
 	if (flag & DP_METER_ATTACH_BRIDGE) {
-		br_cfg = kzalloc(sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
+		br_cfg = devm_kzalloc(&g_dp_dev->dev,
+				      sizeof(GSW_BRIDGE_config_t), GFP_ATOMIC);
 		if (!br_cfg) {
 			bret = -1;
 			goto err;
@@ -1493,10 +1506,10 @@ int dp_meter_del_31(struct net_device *dev,  struct dp_meter_cfg  *meter,
 		}
 	}
 err:
-	kfree(br_cfg);
-	kfree(bp_cfg);
-	kfree(ctp_cfg);
-	kfree(pce_rule);
+	devm_kfree(&g_dp_dev->dev, br_cfg);
+	devm_kfree(&g_dp_dev->dev, bp_cfg);
+	devm_kfree(&g_dp_dev->dev, ctp_cfg);
+	devm_kfree(&g_dp_dev->dev, pce_rule);
 	return bret;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_misc.c b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
index 3833c950dda8..6266bf70524d 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_misc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_misc.c
@@ -1175,12 +1175,9 @@ static int dp_platform_set(int inst, u32 flag)
 		struct dp_subif_info *sif;
 
 		dp_port_prop[inst].priv_hal =
-			kzalloc(sizeof(*priv), GFP_KERNEL);
-		if (!dp_port_prop[inst].priv_hal) {
-			pr_err("kmalloc failed: %d bytes\n",
-			       sizeof(struct hal_priv));
+			devm_kzalloc(&g_dp_dev->dev, sizeof(*priv), GFP_KERNEL);
+		if (!dp_port_prop[inst].priv_hal)
 			return -1;
-		}
 		priv = (struct hal_priv *)dp_port_prop[inst].priv_hal;
 		priv->ppv4_drop_q = MAX_QUEUE - 1; /*Need change later */
 		gsw_handle = dp_port_prop[inst].ops[0];
@@ -1252,7 +1249,7 @@ static int dp_platform_set(int inst, u32 flag)
 	if (priv->bp_def)
 		free_bridge_port(inst, priv->bp_def);
 	init_ppv4_qos(inst, flag); /*de-initialize qos */
-	kfree(priv);
+	devm_kfree(&g_dp_dev->dev, priv);
 	dp_port_prop[inst].priv_hal = NULL;
 	return 0;
 }
@@ -1794,9 +1791,11 @@ static int subif_hw_set(int inst, int portid, int subif_ix,
 		if (port_info->alloc_flags & DP_F_EPON) {
 			lookup.sub_if_id = deq_port_idx + i;
 		} else {
-			lookup.sub_if_id = subif; /*Note:CQM need full subif(15bits)*/
+			/*Note:CQM need full subif(15bits)*/
+			lookup.sub_if_id = subif;
 		}
-			/* For 1st subif and mode 0, use CBM_QUEUE_MAP_F_SUBIF_DONTCARE,
+			/* For 1st subif and mode 0,
+			 * use CBM_QUEUE_MAP_F_SUBIF_DONTCARE,
 			 * otherwise, don't use this flag
 			 */
 			if (!port_info->num_subif &&
@@ -1890,7 +1889,8 @@ static int subif_hw_reset(int inst, int portid, int subif_ix,
 				reset_ctp_bp(inst, subif_ix, portid, bp);
 				if (!dp_bp_dev_tbl[inst][bp].dev) {
 					/*NULL already, then free it */
-					DP_DEBUG(DP_DBG_FLAG_PAE, "Free BP[%d] vap=%d\n",
+					DP_DEBUG(DP_DBG_FLAG_PAE,
+						 "Free BP[%d] vap=%d\n",
 						 bp, subif_ix);
 					free_bridge_port(inst, bp);
 				}
@@ -2211,7 +2211,8 @@ void dp_net_dev_get_ethtool_stats_31(struct net_device *dev,
 
 	ops = dp_port_prop[0].ops[0];
 
-	ps_subif = kzalloc(sizeof(dp_subif_t), GFP_KERNEL);
+	ps_subif = devm_kzalloc(&g_dp_dev->dev, (sizeof(dp_subif_t)),
+				GFP_KERNEL);
 	if (!ps_subif)
 		return;
 
@@ -2219,16 +2220,19 @@ void dp_net_dev_get_ethtool_stats_31(struct net_device *dev,
 	 * one set for BP or regular CTP
 	 * second set for CTP PCE-Bypass
 	 */
-	g_counters = kzalloc(2 * sizeof(GSW_RMON_Port_cnt_t), GFP_KERNEL);
+	g_counters = devm_kzalloc(&g_dp_dev->dev,
+				  (2 * sizeof(GSW_RMON_Port_cnt_t)),
+				  GFP_KERNEL);
 	if (!g_counters) {
-		kfree(ps_subif);
+		devm_kfree(&g_dp_dev->dev, ps_subif);
 		return;
 	}
-
 	/* get subif information from net device */
 	ret = dp_get_netif_subifid(dev, NULL, NULL, NULL, ps_subif, flags);
 	if (ret != DP_SUCCESS) {
 		netdev_err(dev, "dp_get_netif_subifid returned %d\n", ret);
+		devm_kfree(&g_dp_dev->dev, ps_subif);
+		devm_kfree(&g_dp_dev->dev, g_counters);
 		/* dev_put(dev); */
 		kfree(g_counters);
 		kfree(ps_subif);
@@ -2277,7 +2281,7 @@ void dp_net_dev_get_ethtool_stats_31(struct net_device *dev,
 	DP_ETHTOOL_STAT_REG_DECLARE
 #undef _DP_ETHTOOL
 
-	kfree(g_counters);
-	kfree(ps_subif);
+	devm_kfree(&g_dp_dev->dev, g_counters);
+	devm_kfree(&g_dp_dev->dev, ps_subif);
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
index ae441aa376d5..5e79eacf4c9e 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
@@ -202,11 +202,9 @@ int init_ppv4_qos(int inst, int flag)
 		       dp_port_prop[inst].qos_inst);
 		return DP_FAILURE;
 	}
-	t = kzalloc(sizeof(*t), GFP_ATOMIC);
-	if (!t) {
-		pr_err("kzalloc fail: %d bytes\n", sizeof(*t));
+	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
+	if (!t)
 		return DP_FAILURE;
-	}
 	if (cbm_cpu_port_get(&cpu_data, 0)) {
 		pr_err("cbm_cpu_port_get for CPU port?\n");
 		goto EXIT;
@@ -312,8 +310,7 @@ int init_ppv4_qos(int inst, int flag)
 	res = DP_SUCCESS;
 
 EXIT:
-	kfree(t);
-	t = NULL;
+	devm_kfree(&g_dp_dev->dev, t);
 	return res;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
index b23d460db270..52d89b96b8f6 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4_api.c
@@ -324,7 +324,7 @@ static int queue_flush_31(int inst, int node_id, int flag)
 		res = DP_FAILURE;
 		goto EXIT;
 	}
-	lookup = kzalloc(sizeof(*lookup), GFP_ATOMIC);
+	lookup = devm_kzalloc(&g_dp_dev->dev, sizeof(*lookup), GFP_ATOMIC);
 	if (!lookup) {
 		res = DP_FAILURE;
 		goto EXIT;
@@ -436,7 +436,7 @@ static int queue_flush_31(int inst, int node_id, int flag)
 		}
 	}
 EXIT:
-	kfree(lookup);
+	devm_kfree(&g_dp_dev->dev, lookup);
 	lookup = NULL;
 	return res;
 }
@@ -1234,11 +1234,12 @@ static int dp_node_alloc_global_pool(struct dp_node_alloc *node, int flag)
 		goto EXIT;
 	}
 
-	q_conf = kzalloc(sizeof(*q_conf), GFP_KERNEL);
+	q_conf = devm_kzalloc(&g_dp_dev->dev, sizeof(*q_conf), GFP_KERNEL);
 	if (!q_conf)
 		goto EXIT;
 
-	qos_queue_info = kzalloc(sizeof(*qos_queue_info), GFP_KERNEL);
+	qos_queue_info = devm_kzalloc(&g_dp_dev->dev,
+				      sizeof(*qos_queue_info), GFP_KERNEL);
 	if (!qos_queue_info)
 		goto EXIT;
 
@@ -1315,8 +1316,8 @@ static int dp_node_alloc_global_pool(struct dp_node_alloc *node, int flag)
 	}
 
 EXIT:
-	kfree(q_conf);
-	kfree(qos_queue_info);
+	devm_kfree(&g_dp_dev->dev, q_conf);
+	devm_kfree(&g_dp_dev->dev, qos_queue_info);
 	return res;
 }
 
@@ -1830,7 +1831,7 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 		pr_err("priv is NULL cannot proceed!!\n");
 		return DP_FAILURE;
 	}
-	t = kzalloc(sizeof(*t), GFP_ATOMIC);
+	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
 	if (!t)
 		return DP_FAILURE;
 
@@ -2045,8 +2046,7 @@ int dp_node_free_31(struct dp_node_alloc *node, int flag)
 	if (res == DP_FAILURE)
 		pr_err("failed to free node:%d res %d\n",
 		       node->id.q_id, res);
-	kfree(t);
-	t = NULL;
+	devm_kfree(&g_dp_dev->dev, t);
 	return res;
 }
 
@@ -2285,8 +2285,10 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 	struct pp_qos_queue_conf *queue_cfg;
 	struct pp_qos_sched_conf *sched_cfg;
 
-	queue_cfg = kzalloc(sizeof(*queue_cfg), GFP_KERNEL);
-	sched_cfg = kzalloc(sizeof(*sched_cfg), GFP_KERNEL);
+	queue_cfg = devm_kzalloc(&g_dp_dev->dev, sizeof(*queue_cfg),
+				 GFP_KERNEL);
+	sched_cfg = devm_kzalloc(&g_dp_dev->dev, sizeof(*sched_cfg),
+				 GFP_KERNEL);
 	if (!queue_cfg || !sched_cfg)
 		goto ERROR_EXIT;
 
@@ -2368,9 +2370,9 @@ static int dp_link_set(struct dp_node_link *info, int parent_node, int flag)
 	dp_map_qid_to_cqmdeq(info, flag);
 ERROR_EXIT:
 
-	kfree(queue_cfg);
+	devm_kfree(&g_dp_dev->dev, queue_cfg);
 
-	kfree(sched_cfg);
+	devm_kfree(&g_dp_dev->dev, sched_cfg);
 	return res;
 }
 
@@ -2866,7 +2868,7 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		       info->dp_port, DP_PORT(info).dp_port);
 		info->dp_port = DP_PORT(info).dp_port;
 	}
-	t = kzalloc(sizeof(*t), GFP_ATOMIC);
+	t = devm_kzalloc(&g_dp_dev->dev, sizeof(*t), GFP_ATOMIC);
 	if (!t)
 		return DP_FAILURE;
 	for (i = 0; i < ARRAY_SIZE(t->q_orig_block); i++) {
@@ -3086,8 +3088,7 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 			res = DP_FAILURE;
 		}
 	}
-	kfree(t);
-	t = NULL;
+	devm_kfree(&g_dp_dev->dev, t);
 	return res;
 
 EXIT_ERR:
@@ -3119,8 +3120,7 @@ int dp_node_link_add_31(struct dp_node_link *info, int flag)
 		}
 	}
 
-	kfree(t);
-	t = NULL;
+	devm_kfree(&g_dp_dev->dev, t);
 	return res;
 }
 
@@ -3145,7 +3145,7 @@ int dp_queue_conf_set_31(struct dp_queue_conf *cfg, int flag)
 		return DP_FAILURE;
 	}
 
-	conf = kzalloc(sizeof(*conf), GFP_ATOMIC);
+	conf = devm_kzalloc(&g_dp_dev->dev, sizeof(*conf), GFP_ATOMIC);
 	if (!conf)
 		return DP_FAILURE;
 
@@ -3191,8 +3191,7 @@ int dp_queue_conf_set_31(struct dp_queue_conf *cfg, int flag)
 	res = DP_SUCCESS;
 
 EXIT:
-	kfree(conf);
-	conf = NULL;
+	devm_kfree(&g_dp_dev->dev, conf);
 	return res;
 }
 
@@ -3217,7 +3216,7 @@ int dp_queue_conf_get_31(struct dp_queue_conf *cfg, int flag)
 		return DP_FAILURE;
 	}
 
-	conf = kzalloc(sizeof(*conf), GFP_ATOMIC);
+	conf = devm_kzalloc(&g_dp_dev->dev, sizeof(*conf), GFP_ATOMIC);
 	if (!conf)
 		return DP_FAILURE;
 
@@ -3268,8 +3267,7 @@ int dp_queue_conf_get_31(struct dp_queue_conf *cfg, int flag)
 	res = DP_SUCCESS;
 
 EXIT:
-	kfree(conf);
-	conf = NULL;
+	devm_kfree(&g_dp_dev->dev, conf);
 	return res;
 }
 
@@ -4554,18 +4552,19 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 	}
 
 	len = sizeof(struct resv_q) * data->num_resv_q;
-	priv->resv[ep].resv_q = kzalloc(len, GFP_ATOMIC);
+	priv->resv[ep].resv_q = devm_kzalloc(&g_dp_dev->dev, len, GFP_ATOMIC);
 	if (!priv->resv[ep].resv_q)
 		return DP_FAILURE;
-	tmp_resv_q = kzalloc(sizeof(*tmp_resv_q) * MAX_QUEUE, GFP_ATOMIC);
+	tmp_resv_q = devm_kzalloc(&g_dp_dev->dev,
+				  sizeof(*tmp_resv_q) * MAX_QUEUE, GFP_ATOMIC);
 	if (!tmp_resv_q) {
-		kfree(priv->resv[ep].resv_q);
+		devm_kfree(&g_dp_dev->dev, priv->resv[ep].resv_q);
 		return DP_FAILURE;
 	}
 	if (dp_queue_alloc_conf(inst, &id, &qid)) {
 		pr_err("qos_queue_allocate failed\n");
-		kfree(priv->resv[ep].resv_q);
-		kfree(tmp_resv_q);
+		devm_kfree(&g_dp_dev->dev, priv->resv[ep].resv_q);
+		devm_kfree(&g_dp_dev->dev, tmp_resv_q);
 		return DP_FAILURE;
 	}
 	tmp_resv_q[curr_num].id = id;
@@ -4575,7 +4574,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 		if (dp_queue_alloc_conf(inst, &id, &qid)) {
 			res = DP_FAILURE;
 			pr_err("qos_queue_allocate failed\n");
-			kfree(priv->resv[ep].resv_q);
+			devm_kfree(&g_dp_dev->dev, priv->resv[ep].resv_q);
 			goto clear_q;
 		}
 		tmp_resv_q[curr_num].id = id;
@@ -4609,7 +4608,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 	/* clear other non-continuously allocated Q */
 	for (i = 0; i < curr_off; i++)
 		qos_queue_remove(priv->qdev, tmp_resv_q[i].id);
-	kfree(tmp_resv_q);
+	devm_kfree(&g_dp_dev->dev, tmp_resv_q);
 	return res;
 FREE_EXIT:
 	if (priv->resv[ep].resv_q) {
@@ -4617,7 +4616,7 @@ int dp_q_reserve_continuous(int inst, int ep, struct dp_dev_data *data,
 
 		for (i = 0; i < priv->resv[ep].num_resv_q; i++)
 			qos_queue_remove(priv->qdev, resv_q[i].id);
-		kfree(resv_q);
+		devm_kfree(&g_dp_dev->dev, resv_q);
 		priv->resv[ep].resv_q = NULL;
 		priv->resv[ep].num_resv_q = 0;
 	}
@@ -4655,7 +4654,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 	if (data->num_resv_q <= 0)
 		goto RESERVE_SCHED;
 	len = sizeof(struct resv_q) * data->num_resv_q;
-	priv->resv[ep].resv_q = kzalloc(len, GFP_ATOMIC);
+	priv->resv[ep].resv_q = devm_kzalloc(&g_dp_dev->dev, len, GFP_ATOMIC);
 	if (!priv->resv[ep].resv_q) {
 		res = DP_FAILURE;
 		goto FREE_EXIT;
@@ -4705,7 +4704,8 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 		struct resv_sch *p_sch = priv->resv[ep].resv_sched;
 
 		len = sizeof(struct resv_sch) * data->num_resv_sched;
-		priv->resv[ep].resv_sched = kzalloc(len, GFP_ATOMIC);
+		priv->resv[ep].resv_sched = devm_kzalloc(&g_dp_dev->dev,
+							 len, GFP_ATOMIC);
 		DP_DEBUG(DP_DBG_FLAG_QOS, "sched size =%d for ep=%d\n",
 			 len, ep);
 		if (!priv->resv[ep].resv_sched) {
@@ -4735,7 +4735,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 
 		for (i = 0; i < priv->resv[ep].num_resv_q; i++)
 			qos_queue_remove(priv->qdev, resv_q[i].id);
-		kfree(resv_q);
+		devm_kfree(&g_dp_dev->dev, resv_q);
 		priv->resv[ep].resv_q = NULL;
 		priv->resv[ep].num_resv_q = 0;
 	}
@@ -4744,7 +4744,7 @@ int dp_node_reserve(int inst, int ep, struct dp_dev_data *data, int flags)
 
 		for (i = 0; i < priv->resv[ep].num_resv_sched; i++)
 			qos_sched_remove(priv->qdev, resv_sch[i].id);
-		kfree(resv_sch);
+		devm_kfree(&g_dp_dev->dev, resv_sch);
 		priv->resv[ep].resv_sched = NULL;
 		priv->resv[ep].num_resv_sched = 0;
 	}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_proc.c b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
index 70eacab499de..096a159dcb2c 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_proc.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_proc.c
@@ -282,7 +282,8 @@ int proc_print_ctp_bp_info(struct seq_file *s, int inst,
 	struct logic_dev *tmp;
 	struct dp_subif_info *sif = get_dp_port_subif(port, subif_index);
 	int bp = sif->bp;
-	unsigned char *buf = kmalloc(MAX_BP_NUM * 5 + 1, GFP_KERNEL);
+	unsigned char *buf = devm_kzalloc(&g_dp_dev->dev, MAX_BP_NUM * 5 + 1,
+					  GFP_KERNEL);
 	u32 len = MAX_BP_NUM * 5 + 1;
 
 	seq_printf(s, "           bp=%d(member:%s)\n", bp,
@@ -293,7 +294,7 @@ int proc_print_ctp_bp_info(struct seq_file *s, int inst,
 			   get_bp_member_string(inst, tmp->bp, buf, len));
 	}
 
-	kfree(buf);
+	devm_kfree(&g_dp_dev->dev, buf);
 	return 0;
 }
 
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
index ff705f200278..6dc2c2f8cf64 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_switchdev.c
@@ -97,7 +97,7 @@ int dp_swdev_bridge_port_cfg_set(struct br_info *br_item,
 			SET_BP_MAP(brportcfg.nBridgePortMap,
 				   bport_list->portid);
 		} else if (bport_list->portid == bport) {
-			if(bport_list->hairpin) {
+			if (bport_list->hairpin) {
 				SET_BP_MAP(brportcfg.nBridgePortMap,
 					   bport);
 				DP_DEBUG(DP_DBG_FLAG_SWDEV, "set SPL BP:%d\n",
@@ -209,7 +209,7 @@ int dp_swdev_bridge_port_cfg_reset(struct br_info *br_item,
 			cnt++;
 	}
 	DP_DEBUG(DP_DBG_FLAG_SWDEV, "cnt:%d last bp:%d\n", cnt, bp);
-	if(hairpin == 1) {
+	if (hairpin == 1) {
 		UNSET_BP_MAP(brportcfg.nBridgePortMap, bport);
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "reset spl BP %d\n", bport);
 	}
@@ -425,23 +425,17 @@ int dp_gswip_ext_vlan(int inst, int vap, int ep)
 
 	gsw_handle = dp_port_prop[inst].ops[0];
 	port = get_dp_port_info(inst, ep);
-	vlan = kzalloc(sizeof(*vlan), GFP_KERNEL);
-	if (!vlan) {
-		pr_err("failed to alloc ext_vlan of %d bytes\n", sizeof(*vlan));
+	vlan = devm_kzalloc(&g_dp_dev->dev, sizeof(*vlan), GFP_KERNEL);
+	if (!vlan)
 		return 0;
-	}
-	vlan->vlan2_list = kzalloc(sizeof(*vlan->vlan2_list), GFP_KERNEL);
-	if (!vlan->vlan2_list) {
-		pr_err("failed to alloc ext_vlan of %d bytes\n",
-		       sizeof(*vlan->vlan2_list));
+	vlan->vlan2_list = devm_kzalloc(&g_dp_dev->dev,
+					sizeof(*vlan->vlan2_list), GFP_KERNEL);
+	if (!vlan->vlan2_list)
 		goto EXIT;
-	}
-	vlan->vlan1_list = kzalloc(sizeof(*vlan->vlan1_list), GFP_KERNEL);
-	if (!vlan->vlan1_list) {
-		pr_err("failed to alloc ext_vlan of %d bytes\n",
-		       sizeof(*vlan->vlan1_list));
+	vlan->vlan1_list = devm_kzalloc(&g_dp_dev->dev,
+					sizeof(*vlan->vlan1_list), GFP_KERNEL);
+	if (!vlan->vlan1_list)
 		goto EXIT;
-	}
 	sif = get_dp_port_subif(port, vap);
 	list_for_each_entry(tmp, &sif->logic_dev, list) {
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "tmp dev name:%s\n",
@@ -506,8 +500,8 @@ int dp_gswip_ext_vlan(int inst, int vap, int ep)
 		pr_err("set gswip ext vlan return error\n");
 
 EXIT:
-	kfree(vlan->vlan2_list);
-	kfree(vlan->vlan1_list);
-	kfree(vlan);
+	devm_kfree(&g_dp_dev->dev, vlan->vlan2_list);
+	devm_kfree(&g_dp_dev->dev, vlan->vlan1_list);
+	devm_kfree(&g_dp_dev->dev, vlan);
 	return 0; /*return -EIO from GSWIP but later cannot fail swdev*/
 }
