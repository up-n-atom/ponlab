From 06b046e5d2a6fc914c50acd327db37d4e0c20520 Mon Sep 17 00:00:00 2001
From: Rekha Eswaran <rekha.eswaran@intel.com>
Date: Fri, 13 Dec 2019 20:35:14 +0800
Subject: [PATCH] Revert "DRV_DPM_SW-90 : Add additional swdev_en field as part
 of subif"

This reverts commit 438429240c887f3d03e6a28d07c036851cb2d01a.
---
 drivers/net/datapath/dpm/datapath.h               |   1 -
 drivers/net/datapath/dpm/datapath_api.c           |   7 --
 drivers/net/datapath/dpm/datapath_instance.c      |   4 -
 drivers/net/datapath/dpm/datapath_notifier.c      |  46 ++++----
 drivers/net/datapath/dpm/datapath_proc.c          |   6 +-
 drivers/net/datapath/dpm/datapath_swdev.c         | 136 +++++++++++-----------
 drivers/net/datapath/dpm/gswip31/datapath_gswip.c |   4 +-
 drivers/net/datapath/dpm/gswip31/datapath_ppv4.c  |  12 --
 include/net/datapath_api.h                        |   6 -
 9 files changed, 94 insertions(+), 128 deletions(-)

diff --git a/drivers/net/datapath/dpm/datapath.h b/drivers/net/datapath/dpm/datapath.h
index 6ad84d5bc97c..fae53de5cb9c 100644
--- a/drivers/net/datapath/dpm/datapath.h
+++ b/drivers/net/datapath/dpm/datapath.h
@@ -478,7 +478,6 @@ struct dp_subif_info {
 	u8  pkt_only_en;
 	u8  seg_en;
 	u16 gpid;
-	u16 swdev_en;
 	bool cpu_port_en;
 };
 
diff --git a/drivers/net/datapath/dpm/datapath_api.c b/drivers/net/datapath/dpm/datapath_api.c
index 8d5e6ba49a3d..8bdd0862631e 100644
--- a/drivers/net/datapath/dpm/datapath_api.c
+++ b/drivers/net/datapath/dpm/datapath_api.c
@@ -632,12 +632,6 @@ int32_t dp_register_subif_private(int inst, struct module *owner,
 			STATS_SET(sif->rx_flag, !!data->rx_en_flag);
 		else
 			STATS_SET(sif->rx_flag, 1);
-#if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-		if (data->flag_ops & DP_SUBIF_SWDEV)
-			sif->swdev_en = data->swdev_en_flag;
-		else
-			sif->swdev_en = port_info->swdev_en;
-#endif
 		port_info->status = PORT_SUBIF_REGISTERED;
 		subif_id->port_id = port_id;
 		subif_id->subif = sif->subif;
@@ -2487,7 +2481,6 @@ int dp_basic_proc(void)
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_CPUFREQ)
 		dp_cpufreq_notify_exit();
 #endif
-		DP_LIB_UNLOCK(&dp_lock);
 		unregister_notifier(0);
 	}
 }
diff --git a/drivers/net/datapath/dpm/datapath_instance.c b/drivers/net/datapath/dpm/datapath_instance.c
index fceb4765fe97..0c6eba53f0a5 100644
--- a/drivers/net/datapath/dpm/datapath_instance.c
+++ b/drivers/net/datapath/dpm/datapath_instance.c
@@ -244,9 +244,7 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 	struct dp_dev *dp_dev;
 	u8 new_f = 0;
 	u32 idx;
-	u32 vap;
 	struct subif_basic *subif;
-	struct dp_subif_info *sif;
 #if IS_ENABLED(CONFIG_PPA)
 	int err = DP_SUCCESS;
 #endif
@@ -316,8 +314,6 @@ int dp_inst_add_dev(struct net_device *dev, char *subif_name, int inst,
 				return DP_FAILURE;
 		}
 #endif
-	vap = GET_VAP(subif->subif, port_info->vap_offset, port_info->vap_mask);
-	sif = get_dp_port_subif(port_info, vap);
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
 	if ((!(flag & DP_F_SUBIF_LOGICAL)) &&
 		(port_info->swdev_en == 1))
diff --git a/drivers/net/datapath/dpm/datapath_notifier.c b/drivers/net/datapath/dpm/datapath_notifier.c
index be66ed144d55..fe12453d2993 100644
--- a/drivers/net/datapath/dpm/datapath_notifier.c
+++ b/drivers/net/datapath/dpm/datapath_notifier.c
@@ -40,13 +40,13 @@ int dp_event(struct notifier_block *this, unsigned long event, void *ptr)
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
 	struct net_device *dev;
 	u8 *addr;
+	int i;
 	struct net_device *br_dev;
 	struct dp_dev *dp_dev;
 	struct br_info *br_info;
 	int fid, inst, vap = 0;
 	u32 idx;
 	struct pmac_port_info *port;
-	dp_subif_t subif = {0};
 	struct inst_property *prop;
 	struct dp_subif_info *sif;
 
@@ -71,14 +71,6 @@ int dp_event(struct notifier_block *this, unsigned long event, void *ptr)
 	}
 	inst = dp_dev->inst;
 	port = get_dp_port_info(inst, dp_dev->ep);
-	if (dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0)) {
-		DP_DEBUG(DP_DBG_FLAG_DBG,
-			 "get subifid fail(%s), No Mac config\n",
-			 dev ? dev->name : "NULL");
-		return DP_FAILURE;
-	}
-	vap = GET_VAP(subif.subif, port->vap_offset,
-		      port->vap_mask);
 	prop = &dp_port_prop[inst];
 	sif = get_dp_port_subif(port, vap);
 	switch (event) {
@@ -90,7 +82,14 @@ int dp_event(struct notifier_block *this, unsigned long event, void *ptr)
 			 "dev:", dev ? dev->name : "NULL",
 			 "MAC:", addr[0], addr[1], addr[2],
 			 addr[3], addr[4], addr[5]);
-		sif->fid = 0;
+		for (i = 0; i < prop->info.cap.max_num_subif_per_port; i++) {
+			if (get_dp_port_subif(port, i)->netif == dev) {
+				vap = i;
+				sif = get_dp_port_subif(port, vap);
+				DP_DEBUG(DP_DBG_FLAG_NOTIFY, "vap:%d\n", vap);
+				sif->fid = 0;
+			}
+		}
 		prop->info.dp_mac_reset(0,
 					dp_dev->fid,
 					dp_dev->inst,
@@ -116,9 +115,9 @@ int dp_event(struct notifier_block *this, unsigned long event, void *ptr)
 		if (!br_dev) {
 			DP_DEBUG(DP_DBG_FLAG_NOTIFY,
 				 "chg upper:without br name\n");
-			/* Set FID to Zero when br not attached to
-			 * bport
-			 */
+				/* Set FID to Zero when br not attached to
+				 * bport
+				 */
 			dp_dev->fid = 0;
 			sif->fid = dp_dev->fid;
 			goto dev_status;
@@ -128,10 +127,10 @@ int dp_event(struct notifier_block *this, unsigned long event, void *ptr)
 		DP_DEBUG(DP_DBG_FLAG_NOTIFY, "Bridge name:%s\n",
 			 br_dev ? br_dev->name : "NULL");
 		br_info = dp_swdev_bridge_entry_lookup(br_dev->name);
-		if (sif->swdev_en == 1) {
-			if (br_info) {
-				dp_dev->fid = br_info->fid;
-			} else {
+		if (br_info) {
+			dp_dev->fid = br_info->fid;
+		} else {
+			if (port->swdev_en == 1) {
 				fid = dp_notif_br_alloc(br_dev);
 				if (fid > 0) {
 					dp_dev->fid = fid;
@@ -140,11 +139,18 @@ int dp_event(struct notifier_block *this, unsigned long event, void *ptr)
 					       __func__);
 					return 0;
 				}
+			} else {
+				dp_dev->fid = 0;
+			}
+		}
+		for (i = 0; i < prop->info.cap.max_num_subif_per_port; i++) {
+			if (get_dp_port_subif(port, i)->netif == dev) {
+				vap = i;
+				sif = get_dp_port_subif(port, vap);
+				DP_DEBUG(DP_DBG_FLAG_NOTIFY, "vap:%d\n", vap);
+				sif->fid = dp_dev->fid;
 			}
-		} else {
-			dp_dev->fid = 0;
 		}
-		sif->fid = dp_dev->fid;
  dev_status:
 		if (dev->flags & IFF_UP) {
 			DP_DEBUG(DP_DBG_FLAG_NOTIFY,
diff --git a/drivers/net/datapath/dpm/datapath_proc.c b/drivers/net/datapath/dpm/datapath_proc.c
index f9c58bbd09b3..346b931c83ff 100644
--- a/drivers/net/datapath/dpm/datapath_proc.c
+++ b/drivers/net/datapath/dpm/datapath_proc.c
@@ -185,7 +185,7 @@ int proc_port_dump(struct seq_file *s, int pos)
 	seq_printf(s, "    LCT:               %d\n", port->lct_idx);
 	seq_printf(s, "    subif_max:         %d\n", port->subif_max);
 #if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-	seq_printf(s, "    p_swdev:	       %d\n", port->swdev_en);
+	seq_printf(s, "    Swdev:             %d\n", port->swdev_en);
 #endif
 	seq_printf(s, "    cb->rx_fn:         0x%px\n", port->cb.rx_fn);
 	seq_printf(s, "    cb->restart_fn:    0x%px\n", port->cb.restart_fn);
@@ -257,10 +257,6 @@ int proc_port_dump(struct seq_file *s, int pos)
 			   STATS_GET(mib->tx_clone_pkt));
 		seq_printf(s, "           tx_hdr_room_pkt=0x%08x\n",
 			   STATS_GET(mib->tx_hdr_room_pkt));
-#if IS_ENABLED(CONFIG_INTEL_DATAPATH_SWITCHDEV)
-		seq_printf(s, "           swdev:	   %d\n",
-			   sif->swdev_en);
-#endif
 		if (print_ctp_bp)
 			print_ctp_bp(s, tmp_inst, port, i, 0);
 		seq_printf(s, "           subif_qid:      %d\n", sif->num_qid);
diff --git a/drivers/net/datapath/dpm/datapath_swdev.c b/drivers/net/datapath/dpm/datapath_swdev.c
index 81ce60637999..f974c5f7aa78 100644
--- a/drivers/net/datapath/dpm/datapath_swdev.c
+++ b/drivers/net/datapath/dpm/datapath_swdev.c
@@ -247,6 +247,7 @@ static int dp_swdev_clr_gswip_cfg(struct bridge_id_entry_item *br_item,
 				  u8 *addr)
 {
 	struct br_info *br_info;
+	struct pmac_port_info *p_info;
 	struct inst_info *i_info;
 	u16 hairpin = 0;
 	int ret;
@@ -261,6 +262,9 @@ static int dp_swdev_clr_gswip_cfg(struct bridge_id_entry_item *br_item,
 		return 0;
 	if (!dp_swdev_del_bport_from_list(br_info, br_item->portid, &hairpin))
 		return 0;
+	p_info = get_dp_port_info(br_item->inst, br_item->dp_port);
+	if (p_info->swdev_en != 1)
+		return 0;
 	i_info = &dp_port_prop[br_item->inst].info;
 	ret = i_info->swdev_bridge_port_cfg_reset(br_info, br_item->inst,
 						  br_item->portid, hairpin);
@@ -354,13 +358,20 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 			      struct net_device *dev)
 {
 	struct br_info *br_info;
+	struct pmac_port_info *p_info;
 	struct inst_info *i_info;
 	int ret;
 
+	p_info = get_dp_port_info(br_item->inst, br_item->dp_port);
 	i_info = &dp_port_prop[br_item->inst].info;
 	DP_DEBUG(DP_DBG_FLAG_SWDEV, "britem flags:%x\n", br_item->flags);
 
 	if (br_item->flags & ADD_BRENTRY) {
+		if (p_info->swdev_en == 0) {
+			DP_DEBUG(DP_DBG_FLAG_SWDEV, "swdev disable for bp %d\n",
+				 br_item->portid);
+			return 0;
+		}
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "Add br entry %s\n",
 			 br_item->br_device_name);
 
@@ -420,9 +431,12 @@ static int dp_swdev_cfg_gswip(struct bridge_id_entry_item *br_item,
 		if (br_item->flags & LOGIC_DEV_REGISTER)
 			br_info->flag = LOGIC_DEV_REGISTER;
 		dp_swdev_add_bport_to_list(br_info, br_item->portid);
-		i_info->swdev_bridge_port_cfg_set(br_info, br_item->inst,
-						  br_item->portid,
-						  dev->priv_flags);
+		if (p_info->swdev_en == 1) {
+			i_info->swdev_bridge_port_cfg_set(br_info,
+							  br_item->inst,
+							  br_item->portid,
+							  dev->priv_flags);
+		}
 		DP_DEBUG(DP_DBG_FLAG_SWDEV, "added bport(%d)\n",
 			 br_item->portid);
 		return 0;
@@ -443,59 +457,54 @@ static int dp_swdev_add_if(struct net_device *dev,
 	int br_id = 0;
 	struct net_device *base;
 	dp_subif_t subif = {0};
-	u32 flag = 0, vap;
+	u32 flag = 0;
 	int port, inst;
 	int32_t res;
-	struct dp_subif_info *sif;
 
-	/*Get current BPORT ID,instance from DP*/
-	res = dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0);
-	if (res) {
-		DP_DEBUG(DP_DBG_FLAG_SWDEV,
-			 "get subifid failed for %s, chk if vlan dev\n",
-			 dev->name);
-		/*Check bridge port exists otherwise register
-		 *device with datapath  i.e. only in case of new
-		 *VLAN interface
-		 */
-		/*Check if dev is a VLAN device */
-		if (!is_vlan_dev(dev))
-			return -EOPNOTSUPP;
-		base = get_base_dev(dev, -1);
-		DP_DEBUG(DP_DBG_FLAG_SWDEV, "base dev name:%s\n",
-			 base ? base->name : "NULL");
-		if (!base)
-			base = dev;
-		res = dp_get_netif_subifid(base, NULL, NULL, NULL,
-					   &subif, 0);
-		if (res) {
-			pr_err("dp_get_netif_subifid fail:%s\n",
-			       base->name);
-			return -EOPNOTSUPP;
-		}
-		port = subif.port_id;
-		inst = subif.inst;
-		subif.subif = -1;
-		if (dp_register_subif(
-			get_dp_port_info(inst, port)->owner,
-			dev, dev->name, &subif,
-			DP_F_SUBIF_LOGICAL)) {
-			pr_err("dp_register_subif fail: %s\n",
-			       dev->name);
-			return -EOPNOTSUPP;
-		}
-		flag = LOGIC_DEV_REGISTER;
-		DP_DEBUG(DP_DBG_FLAG_SWDEV,
-			 "registered subif,bp=%d port=%d\n",
-			 subif.bport, subif.port_id);
-	}
-	p_info = get_dp_port_info(subif.inst, subif.port_id);
-	i_info = &dp_port_prop[subif.inst].info;
-	vap = GET_VAP(subif.subif, p_info->vap_offset,
-		      p_info->vap_mask);
-	sif = get_dp_port_subif(p_info, vap);
 	/* SWITCHDEV_TRANS_PREPARE phase */
 	if (switchdev_trans_ph_prepare(trans)) {
+		/*Get current BPORT ID,instance from DP*/
+		res = dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0);
+		if (res) {
+			DP_DEBUG(DP_DBG_FLAG_SWDEV,
+				 "get subifid failed for %s, chk if vlan dev\n",
+				 dev->name);
+			/*Check bridge port exists otherwise register
+			 *device with datapath  i.e. only in case of new
+			 *VLAN interface
+			 */
+			/*Check if dev is a VLAN device */
+			if (!is_vlan_dev(dev))
+				return -EOPNOTSUPP;
+
+			base = get_base_dev(dev, -1);
+			DP_DEBUG(DP_DBG_FLAG_SWDEV, "base dev name:%s\n",
+				 base ? base->name : "NULL");
+			if (!base)
+				base = dev;
+			res = dp_get_netif_subifid(base, NULL, NULL, NULL,
+						   &subif, 0);
+			if (res) {
+				pr_err("dp_get_netif_subifid fail:%s\n",
+				       base->name);
+				return -EOPNOTSUPP;
+			}
+			port = subif.port_id;
+			inst = subif.inst;
+			subif.subif = -1;
+			if (dp_register_subif(
+				get_dp_port_info(inst, port)->owner,
+				dev, dev->name, &subif,
+				DP_F_SUBIF_LOGICAL)) {
+				pr_err("dp_register_subif fail: %s\n",
+				       dev->name);
+				return -EOPNOTSUPP;
+			}
+			flag = LOGIC_DEV_REGISTER;
+			DP_DEBUG(DP_DBG_FLAG_SWDEV,
+				 "registered subif,bp=%d port=%d\n",
+				 subif.bport, subif.port_id);
+		}
 		br_item = kmalloc(sizeof(*br_item), GFP_KERNEL);
 		if (!br_item)
 			/*TODO need to check dequeue if no memory*/
@@ -512,6 +521,8 @@ static int dp_swdev_add_if(struct net_device *dev,
 
 		swdev_lock();
 		br_info = dp_swdev_bridge_entry_lookup(br_dev->name);
+		p_info = get_dp_port_info(br_item->inst, br_item->dp_port);
+		i_info = &dp_port_prop[br_item->inst].info;
 		br_item->no_loop_flag = p_info->loop_dis;
 		br_item->hairpin = p_info->bp_hairpin_cap;
 		if (br_info) {
@@ -521,7 +532,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 			br_item->flags = flag;
 		} else {
 			br_item->flags = ADD_BRENTRY | flag;
-			if (sif->swdev_en == 1) {
+			if (p_info->swdev_en == 1) {
 				br_id = i_info->swdev_alloc_bridge_id(
 							br_item->inst);
 				if (br_id) {
@@ -553,8 +564,7 @@ static int dp_swdev_add_if(struct net_device *dev,
 	swdev_lock();
 	br_item = switchdev_trans_item_dequeue(trans);
 	if (br_item) {
-		if (sif->swdev_en == 1)
-			dp_swdev_cfg_gswip(br_item, dev);
+		dp_swdev_cfg_gswip(br_item, dev);
 		if (br_item->flags & LOGIC_DEV_REGISTER)
 			/*do only for vlan flag*/
 			dp_swdev_cfg_vlan(br_item, dev);
@@ -569,28 +579,13 @@ static int dp_swdev_del_if(struct net_device *dev,
 			   struct net_device *br_dev)
 {
 	struct br_info *br_info;
-	struct pmac_port_info *p_info;
 	struct bridge_id_entry_item *br_item;
 	struct net_device *base;
 	struct bridge_member_port *temp_list = NULL;
 	dp_subif_t subif = {0};
-	int vap = 0;
 	int port, inst;
 	u8 *addr = (u8 *)dev->dev_addr;
-	struct dp_subif_info *sif;
 
-	/*Get current BR_PORT ID from DP*/
-	if (dp_get_netif_subifid(dev, NULL, NULL, NULL, &subif, 0)) {
-		DP_DEBUG(DP_DBG_FLAG_SWDEV,
-			 "dp_get_netif_subifid failed for %s\n",
-			 dev->name);
-	/*	if (!is_vlan_dev(dev))*/
-			return -EINVAL;
-	}
-	p_info = get_dp_port_info(subif.inst, subif.port_id);
-	vap = GET_VAP(subif.subif, p_info->vap_offset,
-		      p_info->vap_mask);
-	sif = get_dp_port_subif(p_info, vap);
 	/* SWITCHDEV_TRANS_PREPARE phase */
 	if (switchdev_trans_ph_prepare(trans)) {
 		/*Get current BR_PORT ID from DP*/
@@ -643,8 +638,7 @@ static int dp_swdev_del_if(struct net_device *dev,
 	swdev_lock();
 	br_item = switchdev_trans_item_dequeue(trans);
 	if (br_item) {
-		if (sif->swdev_en == 1)
-			dp_swdev_clr_gswip_cfg(br_item, addr);
+		dp_swdev_clr_gswip_cfg(br_item, addr);
 		/* De-Register Logical Dev i.e. VLAN DEV
 		 * if it is registered
 		 */
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
index 284a95454a9d..0653022fac47 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_gswip.c
@@ -40,7 +40,7 @@ struct ctp_assign {
 
 static struct ctp_assign ctp_assign_info[] = {
 	/*note: multiple flags must put first */
-	{DP_F_CPU, GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, 8, 0xF, CQE_LU_MODE0, 0},
+	{DP_F_CPU, GSW_LOGICAL_PORT_8BIT_WLAN, 16, 16, 8, 0xF, CQE_LU_MODE0, 8},
 	{DP_F_GPON, GSW_LOGICAL_PORT_GPON, 256, 256, 0, 0xFF, CQE_LU_MODE1, 1},
 	{DP_F_EPON, GSW_LOGICAL_PORT_EPON, 64, 64, 0, 0xFF, CQE_LU_MODE1, 1},
 	{DP_F_GINT, GSW_LOGICAL_PORT_GINT, 16, 16, 0, 0xFF, CQE_LU_MODE1, 1},
@@ -125,7 +125,7 @@ int dp_pmac_set_31(int inst, u32 port, dp_pmac_cfg_t *pmac_cfg)
 		for (i = 0; i < dqport.num_deq_ports; i++) {
 			igcfg.nPmacId = pmacid;
 			igcfg.nTxDmaChanId = dqport.deq_info[i].dma_tx_chan;
-			if (igcfg.nTxDmaChanId == (u8)-1) {
+			if (igcfg.nTxDmaChanId == (u8) - 1) {
 				igcfg.nTxDmaChanId =
 					pmac_cfg->ig_pmac.tx_dma_chan;
 			}
diff --git a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
index b86e5314bd6d..ae441aa376d5 100644
--- a/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
+++ b/drivers/net/datapath/dpm/gswip31/datapath_ppv4.c
@@ -207,18 +207,6 @@ int init_ppv4_qos(int inst, int flag)
 		pr_err("kzalloc fail: %d bytes\n", sizeof(*t));
 		return DP_FAILURE;
 	}
-	t->param.wred_total_avail_resources = 0x38000;
-	t->param.wred_p_const = 512;
-	t->param.wred_max_q_size = 0x3fffff;
-	/*reserve all ppv4 port to 1:1 sequnce link cqm port */
-	for (i = 0; i < ARRAY_SIZE(t->param.reserved_ports); i++)
-		t->param.reserved_ports[i] = 1;
-	res = qos_dev_init(priv->qdev, &t->param);
-	if (res) {
-		pr_err("qos_dev_init fail for qos inst %d\n",
-		       dp_port_prop[inst].qos_inst);
-		goto EXIT;
-	}
 	if (cbm_cpu_port_get(&cpu_data, 0)) {
 		pr_err("cbm_cpu_port_get for CPU port?\n");
 		goto EXIT;
diff --git a/include/net/datapath_api.h b/include/net/datapath_api.h
index d012f6deefe3..3e4af28960ef 100644
--- a/include/net/datapath_api.h
+++ b/include/net/datapath_api.h
@@ -773,9 +773,6 @@ enum DP_SUBIF_DATA_FLAG {
 				    * DP register subif using rx_en_flag
 				    * specified under struct dp_subif_data
 				    */
-	DP_SUBIF_SWDEV = BIT(6), /*!< To specify subif level swdev
-				  * enable/disable
-				  */
 };
 
 /*! @brief dp_subif_id struct for get_netif_subif */
@@ -885,9 +882,6 @@ struct dp_subif_data {
 	u32 rx_en_flag; /*!< [in] rx_en_flag = 1 - To enable dp_rx
 			 * rx_en_flag = 0 - disable dp_rx
 			 */
-	u16 swdev_en_flag; /*!< [in] swdev_en_flag =1 - Support h/w offload
-			    * swdev_en_flag = 0 - no h/w offload support
-			    */
 };
 
 /*! @brief enum DP_F_DATA_RESV_CQM_PORT */
