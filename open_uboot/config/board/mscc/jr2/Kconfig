# SPDX-License-Identifier: (GPL-2.0+ OR MIT)

config UBOOT_CONFIG_SYS_VENDOR
	default "mscc"

if UBOOT_CONFIG_SOC_JR2

config UBOOT_CONFIG_SYS_BOARD
	default "jr2"

config UBOOT_CONFIG_SYS_CONFIG_NAME
	default "jr2"

endif

