[ "$ACTION" = "add" -o "$ACTION" = "remove" ] && \
	mkdir -p "/tmp/usb" && \
	mkdir -p "/tmp/blockd" && \
	mkdir -p "/tmp/mounts" && \
	/sbin/block detect> /tmp/blockd/fstab && \
	/sbin/block hotplug && \
	chmod 777 /tmp/mounts/*


