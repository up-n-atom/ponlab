From 9f58de3600f3f0e1ede2632d113316a10409c826 Mon Sep 17 00:00:00 2001
From: "Rywacki, Kamil" <kamil.rywacki@intel.com>
Date: Tue, 2 Mar 2021 12:02:32 +0100
Subject: flower: add support for IPV4 and IPV6

---
 include/netlink-private/types.h    |  12 +-
 include/netlink/route/cls/flower.h |  16 ++
 lib/route/cls/flower.c             | 274 ++++++++++++++++++++++++++---
 libnl-route-3.sym                  |  16 ++
 4 files changed, 292 insertions(+), 26 deletions(-)

diff --git a/include/netlink-private/types.h b/include/netlink-private/types.h
index 5e30fc5..00a3090 100644
--- a/include/netlink-private/types.h
+++ b/include/netlink-private/types.h
@@ -642,6 +642,8 @@ struct rtnl_mall
 	int              m_mask;
 };
 
+#define IPV6_ALEN 16
+
 struct rtnl_flower
 {
 	uint32_t		fl_vlan_id;
@@ -669,7 +671,15 @@ struct rtnl_flower
 	uint8_t			fl_icmpv6_code_mask;
 	uint8_t			fl_icmpv6_type;
 	uint8_t			fl_icmpv6_type_mask;
-	int			fl_mask;
+	uint32_t		fl_ipv4_src;
+	uint32_t		fl_ipv4_src_mask;
+	uint32_t		fl_ipv4_dst;
+	uint32_t		fl_ipv4_dst_mask;
+	uint8_t			fl_ipv6_src[IPV6_ALEN];
+	uint8_t			fl_ipv6_src_mask[IPV6_ALEN];
+	uint8_t			fl_ipv6_dst[IPV6_ALEN];
+	uint8_t			fl_ipv6_dst_mask[IPV6_ALEN];
+	uint64_t		fl_mask;
 };
 
 struct rtnl_cgroup
diff --git a/include/netlink/route/cls/flower.h b/include/netlink/route/cls/flower.h
index 41b49ab..3afa7f2 100644
--- a/include/netlink/route/cls/flower.h
+++ b/include/netlink/route/cls/flower.h
@@ -72,6 +72,22 @@ extern int rtnl_flower_set_icmpv6_type(struct rtnl_cls *, uint8_t);
 extern int rtnl_flower_get_icmpv6_type(struct rtnl_cls *, uint8_t *);
 extern int rtnl_flower_set_icmpv6_type_mask(struct rtnl_cls *, uint8_t);
 extern int rtnl_flower_get_icmpv6_type_mask(struct rtnl_cls *, uint8_t *);
+extern int rtnl_flower_set_ipv4_src(struct rtnl_cls *, uint32_t);
+extern int rtnl_flower_get_ipv4_src(struct rtnl_cls *, uint32_t *);
+extern int rtnl_flower_set_ipv4_src_mask(struct rtnl_cls *, uint32_t);
+extern int rtnl_flower_get_ipv4_src_mask(struct rtnl_cls *, uint32_t *);
+extern int rtnl_flower_set_ipv4_dst(struct rtnl_cls *, uint32_t);
+extern int rtnl_flower_get_ipv4_dst(struct rtnl_cls *, uint32_t *);
+extern int rtnl_flower_set_ipv4_dst_mask(struct rtnl_cls *, uint32_t);
+extern int rtnl_flower_get_ipv4_dst_mask(struct rtnl_cls *, uint32_t *);
+extern int rtnl_flower_set_ipv6_src(struct rtnl_cls *, const uint8_t *, unsigned int);
+extern int rtnl_flower_get_ipv6_src(struct rtnl_cls *, uint8_t *, unsigned int);
+extern int rtnl_flower_set_ipv6_src_mask(struct rtnl_cls *, const uint8_t *, unsigned int);
+extern int rtnl_flower_get_ipv6_src_mask(struct rtnl_cls *, uint8_t *, unsigned int);
+extern int rtnl_flower_set_ipv6_dst(struct rtnl_cls *, const uint8_t *, unsigned int);
+extern int rtnl_flower_get_ipv6_dst(struct rtnl_cls *, uint8_t *, unsigned int);
+extern int rtnl_flower_set_ipv6_dst_mask(struct rtnl_cls *, const uint8_t *, unsigned int);
+extern int rtnl_flower_get_ipv6_dst_mask(struct rtnl_cls *, uint8_t *, unsigned int);
 
 #ifdef __cplusplus
 }
diff --git a/lib/route/cls/flower.c b/lib/route/cls/flower.c
index f5f653b..a266ca3 100644
--- a/lib/route/cls/flower.c
+++ b/lib/route/cls/flower.c
@@ -29,31 +29,39 @@
 #include <netlink/route/action.h>
 
 /** @cond SKIP */
-#define FLOWER_ATTR_KEY_VLAN_ID			(0x1 << 0)
-#define FLOWER_ATTR_KEY_VLAN_PRIO		(0x1 << 1)
-#define FLOWER_ATTR_KEY_VLAN_ETH_TYPE		(0x1 << 2)
-#define FLOWER_ATTR_INDEV			(0x1 << 3)
-#define FLOWER_ATTR_ACTION			(0x1 << 4)
-#define FLOWER_ATTR_FLAGS			(0x1 << 5)
-#define FLOWER_ATTR_CLASSID			(0x1 << 6)
-#define FLOWER_ATTR_KEY_ETH_DST			(0x1 << 7)
-#define FLOWER_ATTR_KEY_ETH_DST_MASK		(0x1 << 8)
-#define FLOWER_ATTR_KEY_ETH_SRC			(0x1 << 9)
-#define FLOWER_ATTR_KEY_ETH_SRC_MASK		(0x1 << 10)
-#define FLOWER_ATTR_KEY_IP_TOS			(0x1 << 11)
-#define FLOWER_ATTR_KEY_IP_TOS_MASK		(0x1 << 12)
-#define FLOWER_ATTR_KEY_CVLAN_ID		(0x1 << 13)
-#define FLOWER_ATTR_KEY_CVLAN_PRIO		(0x1 << 14)
-#define FLOWER_ATTR_KEY_CVLAN_ETH_TYPE		(0x1 << 15)
-#define FLOWER_ATTR_KEY_IP_PROTO		(0x1 << 16)
-#define FLOWER_ATTR_KEY_ICMPV4_CODE		(0x1 << 17)
-#define FLOWER_ATTR_KEY_ICMPV4_CODE_MASK	(0x1 << 18)
-#define FLOWER_ATTR_KEY_ICMPV4_TYPE		(0x1 << 19)
-#define FLOWER_ATTR_KEY_ICMPV4_TYPE_MASK	(0x1 << 20)
-#define FLOWER_ATTR_KEY_ICMPV6_CODE		(0x1 << 21)
-#define FLOWER_ATTR_KEY_ICMPV6_CODE_MASK	(0x1 << 22)
-#define FLOWER_ATTR_KEY_ICMPV6_TYPE		(0x1 << 23)
-#define FLOWER_ATTR_KEY_ICMPV6_TYPE_MASK	(0x1 << 24)
+#define FLOWER_ATTR_KEY_VLAN_ID			(0x1ULL << 0)
+#define FLOWER_ATTR_KEY_VLAN_PRIO		(0x1ULL << 1)
+#define FLOWER_ATTR_KEY_VLAN_ETH_TYPE		(0x1ULL << 2)
+#define FLOWER_ATTR_INDEV			(0x1ULL << 3)
+#define FLOWER_ATTR_ACTION			(0x1ULL << 4)
+#define FLOWER_ATTR_FLAGS			(0x1ULL << 5)
+#define FLOWER_ATTR_CLASSID			(0x1ULL << 6)
+#define FLOWER_ATTR_KEY_ETH_DST			(0x1ULL << 7)
+#define FLOWER_ATTR_KEY_ETH_DST_MASK		(0x1ULL << 8)
+#define FLOWER_ATTR_KEY_ETH_SRC			(0x1ULL << 9)
+#define FLOWER_ATTR_KEY_ETH_SRC_MASK		(0x1ULL << 10)
+#define FLOWER_ATTR_KEY_IP_TOS			(0x1ULL << 11)
+#define FLOWER_ATTR_KEY_IP_TOS_MASK		(0x1ULL << 12)
+#define FLOWER_ATTR_KEY_CVLAN_ID		(0x1ULL << 13)
+#define FLOWER_ATTR_KEY_CVLAN_PRIO		(0x1ULL << 14)
+#define FLOWER_ATTR_KEY_CVLAN_ETH_TYPE		(0x1ULL << 15)
+#define FLOWER_ATTR_KEY_IP_PROTO		(0x1ULL << 16)
+#define FLOWER_ATTR_KEY_ICMPV4_CODE		(0x1ULL << 17)
+#define FLOWER_ATTR_KEY_ICMPV4_CODE_MASK	(0x1ULL << 18)
+#define FLOWER_ATTR_KEY_ICMPV4_TYPE		(0x1ULL << 19)
+#define FLOWER_ATTR_KEY_ICMPV4_TYPE_MASK	(0x1ULL << 20)
+#define FLOWER_ATTR_KEY_ICMPV6_CODE		(0x1ULL << 21)
+#define FLOWER_ATTR_KEY_ICMPV6_CODE_MASK	(0x1ULL << 22)
+#define FLOWER_ATTR_KEY_ICMPV6_TYPE		(0x1ULL << 23)
+#define FLOWER_ATTR_KEY_ICMPV6_TYPE_MASK	(0x1ULL << 24)
+#define FLOWER_ATTR_KEY_IPV4_SRC		(0x1ULL << 25)
+#define FLOWER_ATTR_KEY_IPV4_SRC_MASK		(0x1ULL << 26)
+#define FLOWER_ATTR_KEY_IPV4_DST		(0x1ULL << 27)
+#define FLOWER_ATTR_KEY_IPV4_DST_MASK		(0x1ULL << 28)
+#define FLOWER_ATTR_KEY_IPV6_SRC		(0x1ULL << 29)
+#define FLOWER_ATTR_KEY_IPV6_SRC_MASK		(0x1ULL << 30)
+#define FLOWER_ATTR_KEY_IPV6_DST		(0x1ULL << 31)
+#define FLOWER_ATTR_KEY_IPV6_DST_MASK		(0x1ULL << 32)
 /** @endcond */
 
 #ifndef MIN
@@ -107,6 +115,14 @@ static struct nla_policy flower_policy[TCA_FLOWER_MAX + 1] = {
 	[TCA_FLOWER_KEY_ICMPV6_CODE_MASK] = {.type = NLA_U8},
 	[TCA_FLOWER_KEY_ICMPV6_TYPE] = {.type = NLA_U8},
 	[TCA_FLOWER_KEY_ICMPV6_TYPE_MASK] = {.type = NLA_U8},
+	[TCA_FLOWER_KEY_IPV4_SRC] = {.type = NLA_U32},
+	[TCA_FLOWER_KEY_IPV4_SRC_MASK] = {.type = NLA_U32},
+	[TCA_FLOWER_KEY_IPV4_DST] = {.type = NLA_U32},
+	[TCA_FLOWER_KEY_IPV4_DST_MASK] = {.type = NLA_U32},
+	[TCA_FLOWER_KEY_IPV6_SRC] = {.minlen = IPV6_ALEN, .maxlen = IPV6_ALEN},
+	[TCA_FLOWER_KEY_IPV6_SRC_MASK] = {.minlen = IPV6_ALEN, .maxlen = IPV6_ALEN},
+	[TCA_FLOWER_KEY_IPV6_DST] = {.minlen = IPV6_ALEN, .maxlen = IPV6_ALEN},
+	[TCA_FLOWER_KEY_IPV6_DST_MASK] = {.minlen = IPV6_ALEN, .maxlen = IPV6_ALEN},
 };
 
 static int flower_msg_parser(struct rtnl_tc *tc, void *data)
@@ -249,6 +265,56 @@ static int flower_msg_parser(struct rtnl_tc *tc, void *data)
 		f->fl_mask |= FLOWER_ATTR_KEY_ICMPV6_TYPE_MASK;
 	}
 
+	if (tb[TCA_FLOWER_KEY_IPV4_SRC]) {
+		f->fl_ipv4_src =
+		    nla_get_u32(tb[TCA_FLOWER_KEY_IPV4_SRC]);
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV4_SRC;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV4_SRC_MASK]) {
+		f->fl_ipv4_src_mask =
+		    nla_get_u32(tb[TCA_FLOWER_KEY_IPV4_SRC_MASK]);
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV4_SRC_MASK;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV4_DST]) {
+		f->fl_ipv4_dst =
+		    nla_get_u32(tb[TCA_FLOWER_KEY_IPV4_DST]);
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV4_DST;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV4_DST_MASK]) {
+		f->fl_ipv4_dst_mask =
+		    nla_get_u32(tb[TCA_FLOWER_KEY_IPV4_DST_MASK]);
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV4_DST_MASK;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV6_SRC]) {
+		nla_memcpy(f->fl_ipv6_src, tb[TCA_FLOWER_KEY_IPV6_SRC],
+			   sizeof(f->fl_ipv6_src));
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV6_SRC;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV6_SRC_MASK]) {
+		nla_memcpy(f->fl_ipv6_src_mask,
+			   tb[TCA_FLOWER_KEY_IPV6_SRC_MASK],
+			   sizeof(f->fl_ipv6_src_mask));
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV6_SRC_MASK;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV6_DST]) {
+		nla_memcpy(f->fl_ipv6_dst, tb[TCA_FLOWER_KEY_IPV6_DST],
+			   sizeof(f->fl_ipv6_dst));
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV6_DST;
+	}
+
+	if (tb[TCA_FLOWER_KEY_IPV6_DST_MASK]) {
+		nla_memcpy(f->fl_ipv6_dst_mask,
+			   tb[TCA_FLOWER_KEY_IPV6_DST_MASK],
+			   sizeof(f->fl_ipv6_dst_mask));
+		f->fl_mask |= FLOWER_ATTR_KEY_IPV6_DST_MASK;
+	}
+
 	if (tb[TCA_FLOWER_ACT]) {
 		f->fl_mask |= FLOWER_ATTR_ACTION;
 		err = rtnl_act_parse(&f->fl_act, tb[TCA_FLOWER_ACT]);
@@ -457,6 +523,40 @@ static int flower_msg_fill(struct rtnl_tc *tc, void *data, struct nl_msg *msg)
 		NLA_PUT_U8(msg, TCA_FLOWER_KEY_ICMPV6_TYPE_MASK,
 			   f->fl_icmpv6_type_mask);
 
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV4_SRC)
+		NLA_PUT_U32(msg, TCA_FLOWER_KEY_IPV4_SRC,
+			    f->fl_ipv4_src);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV4_SRC_MASK)
+		NLA_PUT_U32(msg, TCA_FLOWER_KEY_IPV4_SRC_MASK,
+			    f->fl_ipv4_src_mask);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV4_DST)
+		NLA_PUT_U32(msg, TCA_FLOWER_KEY_IPV4_DST,
+			    f->fl_ipv4_dst);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV4_DST_MASK)
+		NLA_PUT_U32(msg, TCA_FLOWER_KEY_IPV4_DST_MASK,
+			    f->fl_ipv4_dst_mask);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV6_SRC)
+		NLA_PUT(msg, TCA_FLOWER_KEY_IPV6_SRC, sizeof(f->fl_ipv6_src),
+			f->fl_ipv6_src);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV6_SRC_MASK)
+		NLA_PUT(msg, TCA_FLOWER_KEY_IPV6_SRC_MASK,
+			sizeof(f->fl_ipv6_src_mask),
+			f->fl_ipv6_src_mask);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV6_DST)
+		NLA_PUT(msg, TCA_FLOWER_KEY_IPV6_DST, sizeof(f->fl_ipv6_dst),
+			f->fl_ipv6_dst);
+
+	if (f->fl_mask & FLOWER_ATTR_KEY_IPV6_DST_MASK)
+		NLA_PUT(msg, TCA_FLOWER_KEY_IPV6_DST_MASK,
+			sizeof(f->fl_ipv6_dst_mask),
+			f->fl_ipv6_dst_mask);
+
 	if (f->fl_mask & FLOWER_ATTR_ACTION) {
 		int err;
 
@@ -965,6 +1065,130 @@ int rtnl_flower_get_icmpv6_type_mask(struct rtnl_cls *cls, uint8_t *val)
 	return 0;
 }
 
+/* IPV4 SRC sector */
+int rtnl_flower_set_ipv4_src(struct rtnl_cls *cls, uint32_t val)
+{
+	SETTER(cls, fl_ipv4_src, &val, sizeof(val),
+	       FLOWER_ATTR_KEY_IPV4_SRC);
+	return 0;
+}
+
+int rtnl_flower_get_ipv4_src(struct rtnl_cls *cls, uint32_t *val)
+{
+	GETTER(cls, fl_ipv4_src, val, sizeof(*val),
+	       FLOWER_ATTR_KEY_IPV4_SRC);
+	return 0;
+}
+
+int rtnl_flower_set_ipv4_src_mask(struct rtnl_cls *cls, uint32_t val)
+{
+	SETTER(cls, fl_ipv4_src_mask, &val, sizeof(val),
+	       FLOWER_ATTR_KEY_IPV4_SRC_MASK);
+	return 0;
+}
+
+int rtnl_flower_get_ipv4_src_mask(struct rtnl_cls *cls, uint32_t *val)
+{
+	GETTER(cls, fl_ipv4_src_mask, val, sizeof(*val),
+	       FLOWER_ATTR_KEY_IPV4_SRC_MASK);
+	return 0;
+}
+
+/* IPV4 DST sector */
+int rtnl_flower_set_ipv4_dst(struct rtnl_cls *cls, uint32_t val)
+{
+	SETTER(cls, fl_ipv4_dst, &val, sizeof(val),
+	       FLOWER_ATTR_KEY_IPV4_DST);
+	return 0;
+}
+
+int rtnl_flower_get_ipv4_dst(struct rtnl_cls *cls, uint32_t *val)
+{
+	GETTER(cls, fl_ipv4_dst, val, sizeof(*val),
+	       FLOWER_ATTR_KEY_IPV4_DST);
+	return 0;
+}
+
+int rtnl_flower_set_ipv4_dst_mask(struct rtnl_cls *cls, uint32_t val)
+{
+	SETTER(cls, fl_ipv4_dst_mask, &val, sizeof(val),
+	       FLOWER_ATTR_KEY_IPV4_DST_MASK);
+	return 0;
+}
+
+int rtnl_flower_get_ipv4_dst_mask(struct rtnl_cls *cls, uint32_t *val)
+{
+	GETTER(cls, fl_ipv4_dst_mask, val, sizeof(*val),
+	       FLOWER_ATTR_KEY_IPV4_DST_MASK);
+	return 0;
+}
+
+/* IPV6 SRC sector */
+int rtnl_flower_set_ipv6_src(struct rtnl_cls *cls, const uint8_t *val,
+			     unsigned int size)
+{
+	SETTER(cls, fl_ipv6_src, val, size,
+	       FLOWER_ATTR_KEY_IPV6_SRC);
+	return 0;
+}
+
+int rtnl_flower_get_ipv6_src(struct rtnl_cls *cls, uint8_t *val,
+			     unsigned int size)
+{
+	GETTER(cls, fl_ipv6_src, val, size,
+	       FLOWER_ATTR_KEY_IPV6_SRC);
+	return 0;
+}
+
+int rtnl_flower_set_ipv6_src_mask(struct rtnl_cls *cls, const uint8_t *val,
+				  unsigned int size)
+{
+	SETTER(cls, fl_ipv6_src_mask, val, size,
+	       FLOWER_ATTR_KEY_IPV6_SRC_MASK);
+	return 0;
+}
+
+int rtnl_flower_get_ipv6_src_mask(struct rtnl_cls *cls, uint8_t *val,
+				  unsigned int size)
+{
+	GETTER(cls, fl_ipv6_src_mask, val, size,
+	       FLOWER_ATTR_KEY_IPV6_SRC_MASK);
+	return 0;
+}
+
+/* IPV6 DST sector */
+int rtnl_flower_set_ipv6_dst(struct rtnl_cls *cls, const uint8_t *val,
+			     unsigned int size)
+{
+	SETTER(cls, fl_ipv6_dst, val, size,
+	       FLOWER_ATTR_KEY_IPV6_DST);
+	return 0;
+}
+
+int rtnl_flower_get_ipv6_dst(struct rtnl_cls *cls, uint8_t *val,
+			     unsigned int size)
+{
+	GETTER(cls, fl_ipv6_dst, val, size,
+	       FLOWER_ATTR_KEY_IPV6_DST);
+	return 0;
+}
+
+int rtnl_flower_set_ipv6_dst_mask(struct rtnl_cls *cls, const uint8_t *val,
+				  unsigned int size)
+{
+	SETTER(cls, fl_ipv6_dst_mask, val, size,
+	       FLOWER_ATTR_KEY_IPV6_DST_MASK);
+	return 0;
+}
+
+int rtnl_flower_get_ipv6_dst_mask(struct rtnl_cls *cls, uint8_t *val,
+				  unsigned int size)
+{
+	GETTER(cls, fl_ipv6_dst_mask, val, size,
+	       FLOWER_ATTR_KEY_IPV6_DST_MASK);
+	return 0;
+}
+
 /** @} */
 
 static struct rtnl_tc_ops flower_ops = {
diff --git a/libnl-route-3.sym b/libnl-route-3.sym
index 2a4bd3d..7cc5a3d 100644
--- a/libnl-route-3.sym
+++ b/libnl-route-3.sym
@@ -965,6 +965,22 @@ global:
 	rtnl_flower_get_icmpv6_type;
 	rtnl_flower_set_icmpv6_type_mask;
 	rtnl_flower_get_icmpv6_type_mask;
+	rtnl_flower_set_ipv4_src;
+	rtnl_flower_get_ipv4_src;
+	rtnl_flower_set_ipv4_src_mask;
+	rtnl_flower_get_ipv4_src_mask;
+	rtnl_flower_set_ipv4_dst;
+	rtnl_flower_get_ipv4_dst;
+	rtnl_flower_set_ipv4_dst_mask;
+	rtnl_flower_get_ipv4_dst_mask;
+	rtnl_flower_set_ipv6_src;
+	rtnl_flower_get_ipv6_src;
+	rtnl_flower_set_ipv6_src_mask;
+	rtnl_flower_get_ipv6_src_mask;
+	rtnl_flower_set_ipv6_dst;
+	rtnl_flower_get_ipv6_dst;
+	rtnl_flower_set_ipv6_dst_mask;
+	rtnl_flower_get_ipv6_dst_mask;
 
 	# The following symbols were added during the development of 3.2.26.
 	# Keep them in libnl_3 to avoid breaking users.
