config UBOOT_CONFIG_CLK_OWL
        bool "Actions Semi OWL clock drivers"
        depends on UBOOT_CONFIG_CLK && UBOOT_CONFIG_ARCH_OWL
        help
          Enable support for clock managemet unit present in Actions Semi
	  Owl series S900/S700 SoCs.


